import type { ReactNode } from 'react';
export interface PanelItemProps {
    header?: string | ReactNode;
    title?: string | ReactNode;
    actions?: any[];
    active?: boolean;
    withCheckmark?: boolean;
}
export declare const PanelItem: ({ header, title, actions, active, withCheckmark }: PanelItemProps) => import("react/jsx-runtime").JSX.Element;
