import type { TabType } from '../../models/tab';
interface LanguageItemProps {
    item: TabType & {
        lang: string;
    };
    active?: boolean;
    withCheckmark?: boolean;
    withIcon?: boolean;
}
export declare const LanguageItem: ({ item, active, withCheckmark, withIcon }: LanguageItemProps) => import("react/jsx-runtime").JSX.Element;
export {};
