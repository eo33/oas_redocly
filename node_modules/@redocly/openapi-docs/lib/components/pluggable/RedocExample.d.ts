import type { ReactElement } from 'react';
import type { OpenAPIEncoding } from '../../types';
interface RedocExampleProps {
    pointer: string;
    mimeType: string;
    encoding?: {
        [field: string]: OpenAPIEncoding;
    };
}
export declare const RedocExample: ({ pointer, mimeType, encoding }: RedocExampleProps) => ReactElement;
export {};
