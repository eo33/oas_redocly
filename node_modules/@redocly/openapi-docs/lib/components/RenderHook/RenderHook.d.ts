import type { ReactElement } from 'react';
import type { HookConfig } from '../../services';
import type { SchemaModel } from '../../models';
export declare function RenderHook<T>(props: {
    Hook?: HookConfig<T>;
    props: T;
}): ReactElement | null;
export declare function getAccessModeLabelText(schema: SchemaModel): string | null;
