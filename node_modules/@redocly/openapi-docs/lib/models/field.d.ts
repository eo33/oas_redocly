import type { OpenAPIParameter, Referenced } from '../types';
import type { FieldModel, Deps } from './types';
import { type OpenAPIParser, type Options } from '../services';
export declare function getField(parser: OpenAPIParser, infoOrRef: Referenced<OpenAPIParameter> & {
    name?: string;
    kind?: string;
}, pointer: string, options: Options, deps: Deps, refsStack?: string[], absolutePointer?: string): FieldModel;
