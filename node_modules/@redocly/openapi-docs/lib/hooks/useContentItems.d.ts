import type { ContentItemModel } from '../models';
import type { ExtendedMenuItem, IMenuItem, OpenAPIParser, Options } from '../services';
type UseContentItemsInput = {
    parser: OpenAPIParser;
    options: Options;
};
type UseContentItemsReturnType = {
    contentItems: ContentItemModel[];
    flatItems: (ExtendedMenuItem & IMenuItem)[];
};
export declare const useContentItems: ({ options, parser, }: UseContentItemsInput) => UseContentItemsReturnType;
export {};
