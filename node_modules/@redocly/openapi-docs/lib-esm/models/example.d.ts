import type { OpenAPIExample, Referenced } from '../types';
import type { OpenAPIParser } from '../services';
import type { ExampleModel } from './types';
type GetExamplesInput = Pick<ExampleModel, 'mime' | 'encoding'> & {
    parser: OpenAPIParser;
    infoOrRef: Referenced<OpenAPIExample>;
};
export declare function getExamples({ parser, infoOrRef, mime, encoding }: GetExamplesInput): ExampleModel;
export {};
