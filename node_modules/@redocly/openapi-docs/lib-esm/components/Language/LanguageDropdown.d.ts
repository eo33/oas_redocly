import type { ReactElement } from 'react';
import type { TabType } from '../../models/tab';
export interface LanguageDropdownProps {
    samples: TabType<{
        lang: string;
    }>[];
    activeTab?: string;
    onChange: (key: string) => void;
    trigger?: ReactElement;
    withArrow?: boolean;
    withCheckmark?: boolean;
    withIcon?: boolean;
    width?: number;
}
export declare const LanguageDropdown: ({ samples, activeTab, onChange, trigger, width, withArrow, withCheckmark, withIcon, }: LanguageDropdownProps) => ReactElement;
