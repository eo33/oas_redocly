import type { TabType } from '../../models/tab';
interface UseTabsStateProps {
    tabs: TabType[];
    defaultTab?: string;
    onChange?: (key: string) => void;
}
export declare function useTabsState({ tabs, defaultTab, onChange }: UseTabsStateProps): {
    activeTab: string;
    handleTabChange: (tab: TabType) => void;
};
export {};
