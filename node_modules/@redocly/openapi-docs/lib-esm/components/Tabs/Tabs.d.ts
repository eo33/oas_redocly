import type { TabType } from '../../models/tab';
type TabsProps = {
    tabs: TabType[];
    onChange: (tab: TabType) => void;
    activeTab?: TabType;
};
export declare function Tabs({ tabs, onChange, activeTab }: TabsProps): import("react/jsx-runtime").JSX.Element;
export {};
