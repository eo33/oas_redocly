import type { GraphQLNamedType } from 'graphql';
export type ReturnType = Omit<GraphQLNamedType, 'toConfig' | 'toString' | 'toJSON' | 'inspect' | typeof Symbol.toStringTag> & {
    isNonNull: boolean;
    isList: boolean;
    isListNonNull: boolean;
};
