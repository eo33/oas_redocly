import React from 'react';
import type { Field } from '../../schema';
export interface FieldViewProps {
    field: Field;
    contrast?: boolean;
    isArgument?: boolean;
    key?: string | number;
    fieldExpandLevel: number;
}
export declare function FieldView({ field, contrast, key, fieldExpandLevel }: FieldViewProps): React.JSX.Element;
