import type { Field } from '../../schema';
export declare function useIsFieldExpandable(field: Field, fieldExpandLevel: number): boolean;
export declare function useShowExpandableBorder(field: Field, fieldExpandLevel: number, { expandedTypes, expandedArguments }: {
    expandedTypes: boolean;
    expandedArguments: boolean;
}): boolean;
