interface ActiveState {
    isActive: boolean;
}
interface UseActiveStateOptions {
    locationMatchDepth: number;
    elementIdOffset: number;
}
export declare function useActiveState(navigationId: string, { locationMatchDepth, elementIdOffset }?: UseActiveStateOptions): ActiveState;
export {};
