import * as React from 'react';
import type { ReferenceDocsLocation } from '../settings';
export interface NavigationContext {
    prefix: string;
    navigationId: string;
    location: ReferenceDocsLocation;
}
export declare const NavigationContextProvider: React.Provider<NavigationContext | undefined>;
export declare function useNavigationContext(): NavigationContext;
