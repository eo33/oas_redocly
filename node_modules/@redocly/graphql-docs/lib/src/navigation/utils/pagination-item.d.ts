import * as React from 'react';
import type { ReferenceDocsLocation } from '../../settings';
import type { Item } from '../components/Item';
export interface PaginationItemProps {
    name: string;
    path: string;
    next?: Item;
    prev?: Item;
}
export type PaginationItem = React.ReactElement<PaginationItemProps>;
export declare function withPaginationItem<T extends PaginationItem>(item: T, itemIndex: number, allItems: PaginationItem[], parentLocation: ReferenceDocsLocation, itemType: keyof ReferenceDocsLocation): T;
export declare function withPaginationItemFallback<T extends PaginationItem>(item: T, fallback: Item): T;
export declare function toItemRoute(item: PaginationItem, parentPath: string): JSX.Element;
