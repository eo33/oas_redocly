interface MarkdownProps {
    html?: string;
    rawMarkdown?: string | null;
    parser?: (rawMarkdown: string) => string;
    isField?: boolean;
}
export declare function Markdown({ html, rawMarkdown, parser, isField, }: MarkdownProps): JSX.Element | null;
export {};
