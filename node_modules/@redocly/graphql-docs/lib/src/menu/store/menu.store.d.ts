import type { GraphQLSchema } from 'graphql';
import type { MenuGroup, SidebarItem } from '../models';
import type { GraphQLConfig } from '@redocly/config';
export declare class MenuStore {
    private readonly groups;
    private readonly typeGroupIndex;
    private readonly sidebarItems;
    constructor(schema: GraphQLSchema, config?: GraphQLConfig);
    getGroups(): MenuGroup[];
    getGroupIdByTypeName(typeName: string): string | undefined;
    getSidebarItems(): SidebarItem[];
}
