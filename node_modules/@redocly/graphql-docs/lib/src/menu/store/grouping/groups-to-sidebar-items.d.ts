import type { PaginationType } from '../../../settings';
import type { MenuGroup, SidebarItem } from '../../models';
export declare const groupsToSidebarItems: (menuGroups: MenuGroup[], pagination?: PaginationType, pathPrefix?: string) => SidebarItem[];
