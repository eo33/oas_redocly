import type { GraphQLSchema } from 'graphql';
import type { IndexedMenuGroup } from '../../models';
import type { IndexedMenuGroupingConfig } from '../config';
export declare function groupAndIndexTypes(groups: Map<string, IndexedMenuGroup>, index: Map<string, string>, schema: GraphQLSchema, menuConfig: IndexedMenuGroupingConfig): void;
