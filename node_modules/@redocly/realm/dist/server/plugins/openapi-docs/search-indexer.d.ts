import { type SearchDocument } from '@redocly/theme';
import type { OpenAPIParser, ContentItemModel, Options } from '@redocly/openapi-docs';
import type { OpenAPIInfo } from '@redocly/openapi-docs/lib/types';
export declare class SearchIndexer {
    private parser;
    private readonly options;
    private basePath;
    private readonly resultArray;
    constructor(parser: OpenAPIParser, options: Options, basePath: string);
    addItem(item: ContentItemModel): void;
    addInfo(info: OpenAPIInfo, metadata: Record<string, any>): void;
    getResult(): SearchDocument[];
    private getParameterId;
    private addOperation;
    private addSchema;
    private addTag;
    private addSection;
}
//# sourceMappingURL=search-indexer.d.ts.map