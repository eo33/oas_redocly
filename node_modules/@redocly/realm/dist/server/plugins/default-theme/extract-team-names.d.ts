import type { RbacConfig } from '../../config/schema';
/**
 * Extracts unique team names from the RBAC configuration object.
 * Filters out default team names and email addresses.
 *
 * @param rbac - The RBAC configuration object.
 * @returns An array of unique team names.
 */
export declare function extractTeamNames(rbac: RbacConfig | undefined): string[];
//# sourceMappingURL=extract-team-names.d.ts.map