import { createRequire as topLevelCreateRequire } from 'module';
 const require = topLevelCreateRequire(import.meta.url);
import {
  require_cjs
} from "./chunk-LGQQW4UX.js";
import {
  Sr,
  n
} from "./chunk-JJ5XY26P.js";
import "./chunk-7WVTT2DN.js";
import {
  init_src,
  src_exports
} from "./chunk-EAFIJ2VV.js";
import "./chunk-AB7LF64Z.js";
import "./chunk-Q6LZZN6W.js";
import "./chunk-WLDWULA7.js";
import "./chunk-GHSOG4VG.js";
import "./chunk-D237NEF2.js";
import "./chunk-2J7OHCSZ.js";
import "./chunk-XQXU7C5Q.js";
import "./chunk-SX3NFZJN.js";
import {
  init_styled_components_esm,
  styled_components_esm_default,
  styled_components_esm_exports,
  useThemeConfig
} from "./chunk-WNBJGIID.js";
import "./chunk-R4PATEVC.js";
import {
  __commonJS,
  __esm,
  __export,
  __require,
  __toCommonJS,
  __toESM
} from "./chunk-35AYGKFE.js";

// node_modules/@redocly/graphql-docs/lib/src/polyfills.js
var require_polyfills = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/polyfills.js"(exports, module) {
    var p = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var d2 = Object.getOwnPropertyNames;
    var T = Object.prototype.hasOwnProperty;
    var a = (e2, t2, s, i) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let o of d2(t2))
          !T.call(e2, o) && o !== s && p(e2, o, { get: () => t2[o], enumerable: !(i = c(t2, o)) || i.enumerable });
      return e2;
    };
    var g = (e2) => a(p({}, "__esModule", { value: true }), e2);
    var L = {};
    module.exports = g(L);
    typeof Element < "u" && !Element.prototype.scrollIntoViewIfNeeded && (Element.prototype.scrollIntoViewIfNeeded = function(e2) {
      e2 = arguments.length === 0 ? true : !!e2;
      const t2 = this.parentNode, s = window.getComputedStyle(t2, void 0), i = parseInt(s.getPropertyValue("border-top-width"), 10), o = parseInt(s.getPropertyValue("border-left-width"), 10);
      if (t2) {
        const f = this.offsetTop - t2.offsetTop < t2.scrollTop, l = this.offsetTop - t2.offsetTop + this.clientHeight - i > t2.scrollTop + t2.clientHeight, n2 = this.offsetLeft - t2.offsetLeft < t2.scrollLeft, r = this.offsetLeft - t2.offsetLeft + this.clientWidth - o > t2.scrollLeft + t2.clientWidth, h2 = f && !l;
        (f || l) && e2 && (t2.scrollTop = this.offsetTop - t2.offsetTop - t2.clientHeight / 2 - i + this.clientHeight / 2), (n2 || r) && e2 && (t2.scrollLeft = this.offsetLeft - t2.offsetLeft - t2.clientWidth / 2 - o + this.clientWidth / 2), (f || l || n2 || r) && !e2 && this.scrollIntoView(h2);
      }
    });
  }
});

// node_modules/graphql/version.mjs
var version, versionInfo;
var init_version = __esm({
  "node_modules/graphql/version.mjs"() {
    version = "16.9.0";
    versionInfo = Object.freeze({
      major: 16,
      minor: 9,
      patch: 0,
      preReleaseTag: null
    });
  }
});

// node_modules/graphql/jsutils/devAssert.mjs
function devAssert(condition, message) {
  const booleanCondition = Boolean(condition);
  if (!booleanCondition) {
    throw new Error(message);
  }
}
var init_devAssert = __esm({
  "node_modules/graphql/jsutils/devAssert.mjs"() {
  }
});

// node_modules/graphql/jsutils/isPromise.mjs
function isPromise(value) {
  return typeof (value === null || value === void 0 ? void 0 : value.then) === "function";
}
var init_isPromise = __esm({
  "node_modules/graphql/jsutils/isPromise.mjs"() {
  }
});

// node_modules/graphql/jsutils/isObjectLike.mjs
function isObjectLike(value) {
  return typeof value == "object" && value !== null;
}
var init_isObjectLike = __esm({
  "node_modules/graphql/jsutils/isObjectLike.mjs"() {
  }
});

// node_modules/graphql/jsutils/invariant.mjs
function invariant(condition, message) {
  const booleanCondition = Boolean(condition);
  if (!booleanCondition) {
    throw new Error(
      message != null ? message : "Unexpected invariant triggered."
    );
  }
}
var init_invariant = __esm({
  "node_modules/graphql/jsutils/invariant.mjs"() {
  }
});

// node_modules/graphql/language/location.mjs
function getLocation(source, position) {
  let lastLineStart = 0;
  let line = 1;
  for (const match of source.body.matchAll(LineRegExp)) {
    typeof match.index === "number" || invariant(false);
    if (match.index >= position) {
      break;
    }
    lastLineStart = match.index + match[0].length;
    line += 1;
  }
  return {
    line,
    column: position + 1 - lastLineStart
  };
}
var LineRegExp;
var init_location = __esm({
  "node_modules/graphql/language/location.mjs"() {
    init_invariant();
    LineRegExp = /\r\n|[\n\r]/g;
  }
});

// node_modules/graphql/language/printLocation.mjs
function printLocation(location) {
  return printSourceLocation(
    location.source,
    getLocation(location.source, location.start)
  );
}
function printSourceLocation(source, sourceLocation) {
  const firstLineColumnOffset = source.locationOffset.column - 1;
  const body = "".padStart(firstLineColumnOffset) + source.body;
  const lineIndex = sourceLocation.line - 1;
  const lineOffset = source.locationOffset.line - 1;
  const lineNum = sourceLocation.line + lineOffset;
  const columnOffset = sourceLocation.line === 1 ? firstLineColumnOffset : 0;
  const columnNum = sourceLocation.column + columnOffset;
  const locationStr = `${source.name}:${lineNum}:${columnNum}
`;
  const lines = body.split(/\r\n|[\n\r]/g);
  const locationLine = lines[lineIndex];
  if (locationLine.length > 120) {
    const subLineIndex = Math.floor(columnNum / 80);
    const subLineColumnNum = columnNum % 80;
    const subLines = [];
    for (let i = 0; i < locationLine.length; i += 80) {
      subLines.push(locationLine.slice(i, i + 80));
    }
    return locationStr + printPrefixedLines([
      [`${lineNum} |`, subLines[0]],
      ...subLines.slice(1, subLineIndex + 1).map((subLine) => ["|", subLine]),
      ["|", "^".padStart(subLineColumnNum)],
      ["|", subLines[subLineIndex + 1]]
    ]);
  }
  return locationStr + printPrefixedLines([
    // Lines specified like this: ["prefix", "string"],
    [`${lineNum - 1} |`, lines[lineIndex - 1]],
    [`${lineNum} |`, locationLine],
    ["|", "^".padStart(columnNum)],
    [`${lineNum + 1} |`, lines[lineIndex + 1]]
  ]);
}
function printPrefixedLines(lines) {
  const existingLines = lines.filter(([_, line]) => line !== void 0);
  const padLen = Math.max(...existingLines.map(([prefix]) => prefix.length));
  return existingLines.map(([prefix, line]) => prefix.padStart(padLen) + (line ? " " + line : "")).join("\n");
}
var init_printLocation = __esm({
  "node_modules/graphql/language/printLocation.mjs"() {
    init_location();
  }
});

// node_modules/graphql/error/GraphQLError.mjs
function toNormalizedOptions(args) {
  const firstArg = args[0];
  if (firstArg == null || "kind" in firstArg || "length" in firstArg) {
    return {
      nodes: firstArg,
      source: args[1],
      positions: args[2],
      path: args[3],
      originalError: args[4],
      extensions: args[5]
    };
  }
  return firstArg;
}
function undefinedIfEmpty(array) {
  return array === void 0 || array.length === 0 ? void 0 : array;
}
function printError(error) {
  return error.toString();
}
function formatError(error) {
  return error.toJSON();
}
var GraphQLError;
var init_GraphQLError = __esm({
  "node_modules/graphql/error/GraphQLError.mjs"() {
    init_isObjectLike();
    init_location();
    init_printLocation();
    GraphQLError = class extends Error {
      /**
       * An array of `{ line, column }` locations within the source GraphQL document
       * which correspond to this error.
       *
       * Errors during validation often contain multiple locations, for example to
       * point out two things with the same name. Errors during execution include a
       * single location, the field which produced the error.
       *
       * Enumerable, and appears in the result of JSON.stringify().
       */
      /**
       * An array describing the JSON-path into the execution response which
       * corresponds to this error. Only included for errors during execution.
       *
       * Enumerable, and appears in the result of JSON.stringify().
       */
      /**
       * An array of GraphQL AST Nodes corresponding to this error.
       */
      /**
       * The source GraphQL document for the first location of this error.
       *
       * Note that if this Error represents more than one node, the source may not
       * represent nodes after the first node.
       */
      /**
       * An array of character offsets within the source GraphQL document
       * which correspond to this error.
       */
      /**
       * The original error thrown from a field resolver during execution.
       */
      /**
       * Extension fields to add to the formatted error.
       */
      /**
       * @deprecated Please use the `GraphQLErrorOptions` constructor overload instead.
       */
      constructor(message, ...rawArgs) {
        var _this$nodes, _nodeLocations$, _ref;
        const { nodes, source, positions, path, originalError, extensions } = toNormalizedOptions(rawArgs);
        super(message);
        this.name = "GraphQLError";
        this.path = path !== null && path !== void 0 ? path : void 0;
        this.originalError = originalError !== null && originalError !== void 0 ? originalError : void 0;
        this.nodes = undefinedIfEmpty(
          Array.isArray(nodes) ? nodes : nodes ? [nodes] : void 0
        );
        const nodeLocations = undefinedIfEmpty(
          (_this$nodes = this.nodes) === null || _this$nodes === void 0 ? void 0 : _this$nodes.map((node) => node.loc).filter((loc) => loc != null)
        );
        this.source = source !== null && source !== void 0 ? source : nodeLocations === null || nodeLocations === void 0 ? void 0 : (_nodeLocations$ = nodeLocations[0]) === null || _nodeLocations$ === void 0 ? void 0 : _nodeLocations$.source;
        this.positions = positions !== null && positions !== void 0 ? positions : nodeLocations === null || nodeLocations === void 0 ? void 0 : nodeLocations.map((loc) => loc.start);
        this.locations = positions && source ? positions.map((pos) => getLocation(source, pos)) : nodeLocations === null || nodeLocations === void 0 ? void 0 : nodeLocations.map((loc) => getLocation(loc.source, loc.start));
        const originalExtensions = isObjectLike(
          originalError === null || originalError === void 0 ? void 0 : originalError.extensions
        ) ? originalError === null || originalError === void 0 ? void 0 : originalError.extensions : void 0;
        this.extensions = (_ref = extensions !== null && extensions !== void 0 ? extensions : originalExtensions) !== null && _ref !== void 0 ? _ref : /* @__PURE__ */ Object.create(null);
        Object.defineProperties(this, {
          message: {
            writable: true,
            enumerable: true
          },
          name: {
            enumerable: false
          },
          nodes: {
            enumerable: false
          },
          source: {
            enumerable: false
          },
          positions: {
            enumerable: false
          },
          originalError: {
            enumerable: false
          }
        });
        if (originalError !== null && originalError !== void 0 && originalError.stack) {
          Object.defineProperty(this, "stack", {
            value: originalError.stack,
            writable: true,
            configurable: true
          });
        } else if (Error.captureStackTrace) {
          Error.captureStackTrace(this, GraphQLError);
        } else {
          Object.defineProperty(this, "stack", {
            value: Error().stack,
            writable: true,
            configurable: true
          });
        }
      }
      get [Symbol.toStringTag]() {
        return "GraphQLError";
      }
      toString() {
        let output = this.message;
        if (this.nodes) {
          for (const node of this.nodes) {
            if (node.loc) {
              output += "\n\n" + printLocation(node.loc);
            }
          }
        } else if (this.source && this.locations) {
          for (const location of this.locations) {
            output += "\n\n" + printSourceLocation(this.source, location);
          }
        }
        return output;
      }
      toJSON() {
        const formattedError = {
          message: this.message
        };
        if (this.locations != null) {
          formattedError.locations = this.locations;
        }
        if (this.path != null) {
          formattedError.path = this.path;
        }
        if (this.extensions != null && Object.keys(this.extensions).length > 0) {
          formattedError.extensions = this.extensions;
        }
        return formattedError;
      }
    };
  }
});

// node_modules/graphql/error/syntaxError.mjs
function syntaxError(source, position, description) {
  return new GraphQLError(`Syntax Error: ${description}`, {
    source,
    positions: [position]
  });
}
var init_syntaxError = __esm({
  "node_modules/graphql/error/syntaxError.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/language/ast.mjs
function isNode(maybeNode) {
  const maybeKind = maybeNode === null || maybeNode === void 0 ? void 0 : maybeNode.kind;
  return typeof maybeKind === "string" && kindValues.has(maybeKind);
}
var Location, Token, QueryDocumentKeys, kindValues, OperationTypeNode;
var init_ast = __esm({
  "node_modules/graphql/language/ast.mjs"() {
    Location = class {
      /**
       * The character offset at which this Node begins.
       */
      /**
       * The character offset at which this Node ends.
       */
      /**
       * The Token at which this Node begins.
       */
      /**
       * The Token at which this Node ends.
       */
      /**
       * The Source document the AST represents.
       */
      constructor(startToken, endToken, source) {
        this.start = startToken.start;
        this.end = endToken.end;
        this.startToken = startToken;
        this.endToken = endToken;
        this.source = source;
      }
      get [Symbol.toStringTag]() {
        return "Location";
      }
      toJSON() {
        return {
          start: this.start,
          end: this.end
        };
      }
    };
    Token = class {
      /**
       * The kind of Token.
       */
      /**
       * The character offset at which this Node begins.
       */
      /**
       * The character offset at which this Node ends.
       */
      /**
       * The 1-indexed line number on which this Token appears.
       */
      /**
       * The 1-indexed column number at which this Token begins.
       */
      /**
       * For non-punctuation tokens, represents the interpreted value of the token.
       *
       * Note: is undefined for punctuation tokens, but typed as string for
       * convenience in the parser.
       */
      /**
       * Tokens exist as nodes in a double-linked-list amongst all tokens
       * including ignored tokens. <SOF> is always the first node and <EOF>
       * the last.
       */
      constructor(kind, start, end, line, column, value) {
        this.kind = kind;
        this.start = start;
        this.end = end;
        this.line = line;
        this.column = column;
        this.value = value;
        this.prev = null;
        this.next = null;
      }
      get [Symbol.toStringTag]() {
        return "Token";
      }
      toJSON() {
        return {
          kind: this.kind,
          value: this.value,
          line: this.line,
          column: this.column
        };
      }
    };
    QueryDocumentKeys = {
      Name: [],
      Document: ["definitions"],
      OperationDefinition: [
        "name",
        "variableDefinitions",
        "directives",
        "selectionSet"
      ],
      VariableDefinition: ["variable", "type", "defaultValue", "directives"],
      Variable: ["name"],
      SelectionSet: ["selections"],
      Field: ["alias", "name", "arguments", "directives", "selectionSet"],
      Argument: ["name", "value"],
      FragmentSpread: ["name", "directives"],
      InlineFragment: ["typeCondition", "directives", "selectionSet"],
      FragmentDefinition: [
        "name",
        // Note: fragment variable definitions are deprecated and will removed in v17.0.0
        "variableDefinitions",
        "typeCondition",
        "directives",
        "selectionSet"
      ],
      IntValue: [],
      FloatValue: [],
      StringValue: [],
      BooleanValue: [],
      NullValue: [],
      EnumValue: [],
      ListValue: ["values"],
      ObjectValue: ["fields"],
      ObjectField: ["name", "value"],
      Directive: ["name", "arguments"],
      NamedType: ["name"],
      ListType: ["type"],
      NonNullType: ["type"],
      SchemaDefinition: ["description", "directives", "operationTypes"],
      OperationTypeDefinition: ["type"],
      ScalarTypeDefinition: ["description", "name", "directives"],
      ObjectTypeDefinition: [
        "description",
        "name",
        "interfaces",
        "directives",
        "fields"
      ],
      FieldDefinition: ["description", "name", "arguments", "type", "directives"],
      InputValueDefinition: [
        "description",
        "name",
        "type",
        "defaultValue",
        "directives"
      ],
      InterfaceTypeDefinition: [
        "description",
        "name",
        "interfaces",
        "directives",
        "fields"
      ],
      UnionTypeDefinition: ["description", "name", "directives", "types"],
      EnumTypeDefinition: ["description", "name", "directives", "values"],
      EnumValueDefinition: ["description", "name", "directives"],
      InputObjectTypeDefinition: ["description", "name", "directives", "fields"],
      DirectiveDefinition: ["description", "name", "arguments", "locations"],
      SchemaExtension: ["directives", "operationTypes"],
      ScalarTypeExtension: ["name", "directives"],
      ObjectTypeExtension: ["name", "interfaces", "directives", "fields"],
      InterfaceTypeExtension: ["name", "interfaces", "directives", "fields"],
      UnionTypeExtension: ["name", "directives", "types"],
      EnumTypeExtension: ["name", "directives", "values"],
      InputObjectTypeExtension: ["name", "directives", "fields"]
    };
    kindValues = new Set(Object.keys(QueryDocumentKeys));
    (function(OperationTypeNode2) {
      OperationTypeNode2["QUERY"] = "query";
      OperationTypeNode2["MUTATION"] = "mutation";
      OperationTypeNode2["SUBSCRIPTION"] = "subscription";
    })(OperationTypeNode || (OperationTypeNode = {}));
  }
});

// node_modules/graphql/language/directiveLocation.mjs
var DirectiveLocation;
var init_directiveLocation = __esm({
  "node_modules/graphql/language/directiveLocation.mjs"() {
    (function(DirectiveLocation2) {
      DirectiveLocation2["QUERY"] = "QUERY";
      DirectiveLocation2["MUTATION"] = "MUTATION";
      DirectiveLocation2["SUBSCRIPTION"] = "SUBSCRIPTION";
      DirectiveLocation2["FIELD"] = "FIELD";
      DirectiveLocation2["FRAGMENT_DEFINITION"] = "FRAGMENT_DEFINITION";
      DirectiveLocation2["FRAGMENT_SPREAD"] = "FRAGMENT_SPREAD";
      DirectiveLocation2["INLINE_FRAGMENT"] = "INLINE_FRAGMENT";
      DirectiveLocation2["VARIABLE_DEFINITION"] = "VARIABLE_DEFINITION";
      DirectiveLocation2["SCHEMA"] = "SCHEMA";
      DirectiveLocation2["SCALAR"] = "SCALAR";
      DirectiveLocation2["OBJECT"] = "OBJECT";
      DirectiveLocation2["FIELD_DEFINITION"] = "FIELD_DEFINITION";
      DirectiveLocation2["ARGUMENT_DEFINITION"] = "ARGUMENT_DEFINITION";
      DirectiveLocation2["INTERFACE"] = "INTERFACE";
      DirectiveLocation2["UNION"] = "UNION";
      DirectiveLocation2["ENUM"] = "ENUM";
      DirectiveLocation2["ENUM_VALUE"] = "ENUM_VALUE";
      DirectiveLocation2["INPUT_OBJECT"] = "INPUT_OBJECT";
      DirectiveLocation2["INPUT_FIELD_DEFINITION"] = "INPUT_FIELD_DEFINITION";
    })(DirectiveLocation || (DirectiveLocation = {}));
  }
});

// node_modules/graphql/language/kinds.mjs
var Kind;
var init_kinds = __esm({
  "node_modules/graphql/language/kinds.mjs"() {
    (function(Kind2) {
      Kind2["NAME"] = "Name";
      Kind2["DOCUMENT"] = "Document";
      Kind2["OPERATION_DEFINITION"] = "OperationDefinition";
      Kind2["VARIABLE_DEFINITION"] = "VariableDefinition";
      Kind2["SELECTION_SET"] = "SelectionSet";
      Kind2["FIELD"] = "Field";
      Kind2["ARGUMENT"] = "Argument";
      Kind2["FRAGMENT_SPREAD"] = "FragmentSpread";
      Kind2["INLINE_FRAGMENT"] = "InlineFragment";
      Kind2["FRAGMENT_DEFINITION"] = "FragmentDefinition";
      Kind2["VARIABLE"] = "Variable";
      Kind2["INT"] = "IntValue";
      Kind2["FLOAT"] = "FloatValue";
      Kind2["STRING"] = "StringValue";
      Kind2["BOOLEAN"] = "BooleanValue";
      Kind2["NULL"] = "NullValue";
      Kind2["ENUM"] = "EnumValue";
      Kind2["LIST"] = "ListValue";
      Kind2["OBJECT"] = "ObjectValue";
      Kind2["OBJECT_FIELD"] = "ObjectField";
      Kind2["DIRECTIVE"] = "Directive";
      Kind2["NAMED_TYPE"] = "NamedType";
      Kind2["LIST_TYPE"] = "ListType";
      Kind2["NON_NULL_TYPE"] = "NonNullType";
      Kind2["SCHEMA_DEFINITION"] = "SchemaDefinition";
      Kind2["OPERATION_TYPE_DEFINITION"] = "OperationTypeDefinition";
      Kind2["SCALAR_TYPE_DEFINITION"] = "ScalarTypeDefinition";
      Kind2["OBJECT_TYPE_DEFINITION"] = "ObjectTypeDefinition";
      Kind2["FIELD_DEFINITION"] = "FieldDefinition";
      Kind2["INPUT_VALUE_DEFINITION"] = "InputValueDefinition";
      Kind2["INTERFACE_TYPE_DEFINITION"] = "InterfaceTypeDefinition";
      Kind2["UNION_TYPE_DEFINITION"] = "UnionTypeDefinition";
      Kind2["ENUM_TYPE_DEFINITION"] = "EnumTypeDefinition";
      Kind2["ENUM_VALUE_DEFINITION"] = "EnumValueDefinition";
      Kind2["INPUT_OBJECT_TYPE_DEFINITION"] = "InputObjectTypeDefinition";
      Kind2["DIRECTIVE_DEFINITION"] = "DirectiveDefinition";
      Kind2["SCHEMA_EXTENSION"] = "SchemaExtension";
      Kind2["SCALAR_TYPE_EXTENSION"] = "ScalarTypeExtension";
      Kind2["OBJECT_TYPE_EXTENSION"] = "ObjectTypeExtension";
      Kind2["INTERFACE_TYPE_EXTENSION"] = "InterfaceTypeExtension";
      Kind2["UNION_TYPE_EXTENSION"] = "UnionTypeExtension";
      Kind2["ENUM_TYPE_EXTENSION"] = "EnumTypeExtension";
      Kind2["INPUT_OBJECT_TYPE_EXTENSION"] = "InputObjectTypeExtension";
    })(Kind || (Kind = {}));
  }
});

// node_modules/graphql/language/characterClasses.mjs
function isWhiteSpace(code) {
  return code === 9 || code === 32;
}
function isDigit(code) {
  return code >= 48 && code <= 57;
}
function isLetter(code) {
  return code >= 97 && code <= 122 || // A-Z
  code >= 65 && code <= 90;
}
function isNameStart(code) {
  return isLetter(code) || code === 95;
}
function isNameContinue(code) {
  return isLetter(code) || isDigit(code) || code === 95;
}
var init_characterClasses = __esm({
  "node_modules/graphql/language/characterClasses.mjs"() {
  }
});

// node_modules/graphql/language/blockString.mjs
function dedentBlockStringLines(lines) {
  var _firstNonEmptyLine2;
  let commonIndent = Number.MAX_SAFE_INTEGER;
  let firstNonEmptyLine = null;
  let lastNonEmptyLine = -1;
  for (let i = 0; i < lines.length; ++i) {
    var _firstNonEmptyLine;
    const line = lines[i];
    const indent3 = leadingWhitespace(line);
    if (indent3 === line.length) {
      continue;
    }
    firstNonEmptyLine = (_firstNonEmptyLine = firstNonEmptyLine) !== null && _firstNonEmptyLine !== void 0 ? _firstNonEmptyLine : i;
    lastNonEmptyLine = i;
    if (i !== 0 && indent3 < commonIndent) {
      commonIndent = indent3;
    }
  }
  return lines.map((line, i) => i === 0 ? line : line.slice(commonIndent)).slice(
    (_firstNonEmptyLine2 = firstNonEmptyLine) !== null && _firstNonEmptyLine2 !== void 0 ? _firstNonEmptyLine2 : 0,
    lastNonEmptyLine + 1
  );
}
function leadingWhitespace(str) {
  let i = 0;
  while (i < str.length && isWhiteSpace(str.charCodeAt(i))) {
    ++i;
  }
  return i;
}
function isPrintableAsBlockString(value) {
  if (value === "") {
    return true;
  }
  let isEmptyLine = true;
  let hasIndent = false;
  let hasCommonIndent = true;
  let seenNonEmptyLine = false;
  for (let i = 0; i < value.length; ++i) {
    switch (value.codePointAt(i)) {
      case 0:
      case 1:
      case 2:
      case 3:
      case 4:
      case 5:
      case 6:
      case 7:
      case 8:
      case 11:
      case 12:
      case 14:
      case 15:
        return false;
      case 13:
        return false;
      case 10:
        if (isEmptyLine && !seenNonEmptyLine) {
          return false;
        }
        seenNonEmptyLine = true;
        isEmptyLine = true;
        hasIndent = false;
        break;
      case 9:
      case 32:
        hasIndent || (hasIndent = isEmptyLine);
        break;
      default:
        hasCommonIndent && (hasCommonIndent = hasIndent);
        isEmptyLine = false;
    }
  }
  if (isEmptyLine) {
    return false;
  }
  if (hasCommonIndent && seenNonEmptyLine) {
    return false;
  }
  return true;
}
function printBlockString(value, options) {
  const escapedValue = value.replace(/"""/g, '\\"""');
  const lines = escapedValue.split(/\r\n|[\n\r]/g);
  const isSingleLine = lines.length === 1;
  const forceLeadingNewLine = lines.length > 1 && lines.slice(1).every((line) => line.length === 0 || isWhiteSpace(line.charCodeAt(0)));
  const hasTrailingTripleQuotes = escapedValue.endsWith('\\"""');
  const hasTrailingQuote = value.endsWith('"') && !hasTrailingTripleQuotes;
  const hasTrailingSlash = value.endsWith("\\");
  const forceTrailingNewline = hasTrailingQuote || hasTrailingSlash;
  const printAsMultipleLines = !(options !== null && options !== void 0 && options.minimize) && // add leading and trailing new lines only if it improves readability
  (!isSingleLine || value.length > 70 || forceTrailingNewline || forceLeadingNewLine || hasTrailingTripleQuotes);
  let result = "";
  const skipLeadingNewLine = isSingleLine && isWhiteSpace(value.charCodeAt(0));
  if (printAsMultipleLines && !skipLeadingNewLine || forceLeadingNewLine) {
    result += "\n";
  }
  result += escapedValue;
  if (printAsMultipleLines || forceTrailingNewline) {
    result += "\n";
  }
  return '"""' + result + '"""';
}
var init_blockString = __esm({
  "node_modules/graphql/language/blockString.mjs"() {
    init_characterClasses();
  }
});

// node_modules/graphql/language/tokenKind.mjs
var TokenKind;
var init_tokenKind = __esm({
  "node_modules/graphql/language/tokenKind.mjs"() {
    (function(TokenKind2) {
      TokenKind2["SOF"] = "<SOF>";
      TokenKind2["EOF"] = "<EOF>";
      TokenKind2["BANG"] = "!";
      TokenKind2["DOLLAR"] = "$";
      TokenKind2["AMP"] = "&";
      TokenKind2["PAREN_L"] = "(";
      TokenKind2["PAREN_R"] = ")";
      TokenKind2["SPREAD"] = "...";
      TokenKind2["COLON"] = ":";
      TokenKind2["EQUALS"] = "=";
      TokenKind2["AT"] = "@";
      TokenKind2["BRACKET_L"] = "[";
      TokenKind2["BRACKET_R"] = "]";
      TokenKind2["BRACE_L"] = "{";
      TokenKind2["PIPE"] = "|";
      TokenKind2["BRACE_R"] = "}";
      TokenKind2["NAME"] = "Name";
      TokenKind2["INT"] = "Int";
      TokenKind2["FLOAT"] = "Float";
      TokenKind2["STRING"] = "String";
      TokenKind2["BLOCK_STRING"] = "BlockString";
      TokenKind2["COMMENT"] = "Comment";
    })(TokenKind || (TokenKind = {}));
  }
});

// node_modules/graphql/language/lexer.mjs
function isPunctuatorTokenKind(kind) {
  return kind === TokenKind.BANG || kind === TokenKind.DOLLAR || kind === TokenKind.AMP || kind === TokenKind.PAREN_L || kind === TokenKind.PAREN_R || kind === TokenKind.SPREAD || kind === TokenKind.COLON || kind === TokenKind.EQUALS || kind === TokenKind.AT || kind === TokenKind.BRACKET_L || kind === TokenKind.BRACKET_R || kind === TokenKind.BRACE_L || kind === TokenKind.PIPE || kind === TokenKind.BRACE_R;
}
function isUnicodeScalarValue(code) {
  return code >= 0 && code <= 55295 || code >= 57344 && code <= 1114111;
}
function isSupplementaryCodePoint(body, location) {
  return isLeadingSurrogate(body.charCodeAt(location)) && isTrailingSurrogate(body.charCodeAt(location + 1));
}
function isLeadingSurrogate(code) {
  return code >= 55296 && code <= 56319;
}
function isTrailingSurrogate(code) {
  return code >= 56320 && code <= 57343;
}
function printCodePointAt(lexer, location) {
  const code = lexer.source.body.codePointAt(location);
  if (code === void 0) {
    return TokenKind.EOF;
  } else if (code >= 32 && code <= 126) {
    const char = String.fromCodePoint(code);
    return char === '"' ? `'"'` : `"${char}"`;
  }
  return "U+" + code.toString(16).toUpperCase().padStart(4, "0");
}
function createToken(lexer, kind, start, end, value) {
  const line = lexer.line;
  const col = 1 + start - lexer.lineStart;
  return new Token(kind, start, end, line, col, value);
}
function readNextToken(lexer, start) {
  const body = lexer.source.body;
  const bodyLength = body.length;
  let position = start;
  while (position < bodyLength) {
    const code = body.charCodeAt(position);
    switch (code) {
      case 65279:
      case 9:
      case 32:
      case 44:
        ++position;
        continue;
      case 10:
        ++position;
        ++lexer.line;
        lexer.lineStart = position;
        continue;
      case 13:
        if (body.charCodeAt(position + 1) === 10) {
          position += 2;
        } else {
          ++position;
        }
        ++lexer.line;
        lexer.lineStart = position;
        continue;
      case 35:
        return readComment(lexer, position);
      case 33:
        return createToken(lexer, TokenKind.BANG, position, position + 1);
      case 36:
        return createToken(lexer, TokenKind.DOLLAR, position, position + 1);
      case 38:
        return createToken(lexer, TokenKind.AMP, position, position + 1);
      case 40:
        return createToken(lexer, TokenKind.PAREN_L, position, position + 1);
      case 41:
        return createToken(lexer, TokenKind.PAREN_R, position, position + 1);
      case 46:
        if (body.charCodeAt(position + 1) === 46 && body.charCodeAt(position + 2) === 46) {
          return createToken(lexer, TokenKind.SPREAD, position, position + 3);
        }
        break;
      case 58:
        return createToken(lexer, TokenKind.COLON, position, position + 1);
      case 61:
        return createToken(lexer, TokenKind.EQUALS, position, position + 1);
      case 64:
        return createToken(lexer, TokenKind.AT, position, position + 1);
      case 91:
        return createToken(lexer, TokenKind.BRACKET_L, position, position + 1);
      case 93:
        return createToken(lexer, TokenKind.BRACKET_R, position, position + 1);
      case 123:
        return createToken(lexer, TokenKind.BRACE_L, position, position + 1);
      case 124:
        return createToken(lexer, TokenKind.PIPE, position, position + 1);
      case 125:
        return createToken(lexer, TokenKind.BRACE_R, position, position + 1);
      case 34:
        if (body.charCodeAt(position + 1) === 34 && body.charCodeAt(position + 2) === 34) {
          return readBlockString(lexer, position);
        }
        return readString(lexer, position);
    }
    if (isDigit(code) || code === 45) {
      return readNumber(lexer, position, code);
    }
    if (isNameStart(code)) {
      return readName(lexer, position);
    }
    throw syntaxError(
      lexer.source,
      position,
      code === 39 ? `Unexpected single quote character ('), did you mean to use a double quote (")?` : isUnicodeScalarValue(code) || isSupplementaryCodePoint(body, position) ? `Unexpected character: ${printCodePointAt(lexer, position)}.` : `Invalid character: ${printCodePointAt(lexer, position)}.`
    );
  }
  return createToken(lexer, TokenKind.EOF, bodyLength, bodyLength);
}
function readComment(lexer, start) {
  const body = lexer.source.body;
  const bodyLength = body.length;
  let position = start + 1;
  while (position < bodyLength) {
    const code = body.charCodeAt(position);
    if (code === 10 || code === 13) {
      break;
    }
    if (isUnicodeScalarValue(code)) {
      ++position;
    } else if (isSupplementaryCodePoint(body, position)) {
      position += 2;
    } else {
      break;
    }
  }
  return createToken(
    lexer,
    TokenKind.COMMENT,
    start,
    position,
    body.slice(start + 1, position)
  );
}
function readNumber(lexer, start, firstCode) {
  const body = lexer.source.body;
  let position = start;
  let code = firstCode;
  let isFloat = false;
  if (code === 45) {
    code = body.charCodeAt(++position);
  }
  if (code === 48) {
    code = body.charCodeAt(++position);
    if (isDigit(code)) {
      throw syntaxError(
        lexer.source,
        position,
        `Invalid number, unexpected digit after 0: ${printCodePointAt(
          lexer,
          position
        )}.`
      );
    }
  } else {
    position = readDigits(lexer, position, code);
    code = body.charCodeAt(position);
  }
  if (code === 46) {
    isFloat = true;
    code = body.charCodeAt(++position);
    position = readDigits(lexer, position, code);
    code = body.charCodeAt(position);
  }
  if (code === 69 || code === 101) {
    isFloat = true;
    code = body.charCodeAt(++position);
    if (code === 43 || code === 45) {
      code = body.charCodeAt(++position);
    }
    position = readDigits(lexer, position, code);
    code = body.charCodeAt(position);
  }
  if (code === 46 || isNameStart(code)) {
    throw syntaxError(
      lexer.source,
      position,
      `Invalid number, expected digit but got: ${printCodePointAt(
        lexer,
        position
      )}.`
    );
  }
  return createToken(
    lexer,
    isFloat ? TokenKind.FLOAT : TokenKind.INT,
    start,
    position,
    body.slice(start, position)
  );
}
function readDigits(lexer, start, firstCode) {
  if (!isDigit(firstCode)) {
    throw syntaxError(
      lexer.source,
      start,
      `Invalid number, expected digit but got: ${printCodePointAt(
        lexer,
        start
      )}.`
    );
  }
  const body = lexer.source.body;
  let position = start + 1;
  while (isDigit(body.charCodeAt(position))) {
    ++position;
  }
  return position;
}
function readString(lexer, start) {
  const body = lexer.source.body;
  const bodyLength = body.length;
  let position = start + 1;
  let chunkStart = position;
  let value = "";
  while (position < bodyLength) {
    const code = body.charCodeAt(position);
    if (code === 34) {
      value += body.slice(chunkStart, position);
      return createToken(lexer, TokenKind.STRING, start, position + 1, value);
    }
    if (code === 92) {
      value += body.slice(chunkStart, position);
      const escape = body.charCodeAt(position + 1) === 117 ? body.charCodeAt(position + 2) === 123 ? readEscapedUnicodeVariableWidth(lexer, position) : readEscapedUnicodeFixedWidth(lexer, position) : readEscapedCharacter(lexer, position);
      value += escape.value;
      position += escape.size;
      chunkStart = position;
      continue;
    }
    if (code === 10 || code === 13) {
      break;
    }
    if (isUnicodeScalarValue(code)) {
      ++position;
    } else if (isSupplementaryCodePoint(body, position)) {
      position += 2;
    } else {
      throw syntaxError(
        lexer.source,
        position,
        `Invalid character within String: ${printCodePointAt(
          lexer,
          position
        )}.`
      );
    }
  }
  throw syntaxError(lexer.source, position, "Unterminated string.");
}
function readEscapedUnicodeVariableWidth(lexer, position) {
  const body = lexer.source.body;
  let point = 0;
  let size = 3;
  while (size < 12) {
    const code = body.charCodeAt(position + size++);
    if (code === 125) {
      if (size < 5 || !isUnicodeScalarValue(point)) {
        break;
      }
      return {
        value: String.fromCodePoint(point),
        size
      };
    }
    point = point << 4 | readHexDigit(code);
    if (point < 0) {
      break;
    }
  }
  throw syntaxError(
    lexer.source,
    position,
    `Invalid Unicode escape sequence: "${body.slice(
      position,
      position + size
    )}".`
  );
}
function readEscapedUnicodeFixedWidth(lexer, position) {
  const body = lexer.source.body;
  const code = read16BitHexCode(body, position + 2);
  if (isUnicodeScalarValue(code)) {
    return {
      value: String.fromCodePoint(code),
      size: 6
    };
  }
  if (isLeadingSurrogate(code)) {
    if (body.charCodeAt(position + 6) === 92 && body.charCodeAt(position + 7) === 117) {
      const trailingCode = read16BitHexCode(body, position + 8);
      if (isTrailingSurrogate(trailingCode)) {
        return {
          value: String.fromCodePoint(code, trailingCode),
          size: 12
        };
      }
    }
  }
  throw syntaxError(
    lexer.source,
    position,
    `Invalid Unicode escape sequence: "${body.slice(position, position + 6)}".`
  );
}
function read16BitHexCode(body, position) {
  return readHexDigit(body.charCodeAt(position)) << 12 | readHexDigit(body.charCodeAt(position + 1)) << 8 | readHexDigit(body.charCodeAt(position + 2)) << 4 | readHexDigit(body.charCodeAt(position + 3));
}
function readHexDigit(code) {
  return code >= 48 && code <= 57 ? code - 48 : code >= 65 && code <= 70 ? code - 55 : code >= 97 && code <= 102 ? code - 87 : -1;
}
function readEscapedCharacter(lexer, position) {
  const body = lexer.source.body;
  const code = body.charCodeAt(position + 1);
  switch (code) {
    case 34:
      return {
        value: '"',
        size: 2
      };
    case 92:
      return {
        value: "\\",
        size: 2
      };
    case 47:
      return {
        value: "/",
        size: 2
      };
    case 98:
      return {
        value: "\b",
        size: 2
      };
    case 102:
      return {
        value: "\f",
        size: 2
      };
    case 110:
      return {
        value: "\n",
        size: 2
      };
    case 114:
      return {
        value: "\r",
        size: 2
      };
    case 116:
      return {
        value: "	",
        size: 2
      };
  }
  throw syntaxError(
    lexer.source,
    position,
    `Invalid character escape sequence: "${body.slice(
      position,
      position + 2
    )}".`
  );
}
function readBlockString(lexer, start) {
  const body = lexer.source.body;
  const bodyLength = body.length;
  let lineStart = lexer.lineStart;
  let position = start + 3;
  let chunkStart = position;
  let currentLine = "";
  const blockLines = [];
  while (position < bodyLength) {
    const code = body.charCodeAt(position);
    if (code === 34 && body.charCodeAt(position + 1) === 34 && body.charCodeAt(position + 2) === 34) {
      currentLine += body.slice(chunkStart, position);
      blockLines.push(currentLine);
      const token = createToken(
        lexer,
        TokenKind.BLOCK_STRING,
        start,
        position + 3,
        // Return a string of the lines joined with U+000A.
        dedentBlockStringLines(blockLines).join("\n")
      );
      lexer.line += blockLines.length - 1;
      lexer.lineStart = lineStart;
      return token;
    }
    if (code === 92 && body.charCodeAt(position + 1) === 34 && body.charCodeAt(position + 2) === 34 && body.charCodeAt(position + 3) === 34) {
      currentLine += body.slice(chunkStart, position);
      chunkStart = position + 1;
      position += 4;
      continue;
    }
    if (code === 10 || code === 13) {
      currentLine += body.slice(chunkStart, position);
      blockLines.push(currentLine);
      if (code === 13 && body.charCodeAt(position + 1) === 10) {
        position += 2;
      } else {
        ++position;
      }
      currentLine = "";
      chunkStart = position;
      lineStart = position;
      continue;
    }
    if (isUnicodeScalarValue(code)) {
      ++position;
    } else if (isSupplementaryCodePoint(body, position)) {
      position += 2;
    } else {
      throw syntaxError(
        lexer.source,
        position,
        `Invalid character within String: ${printCodePointAt(
          lexer,
          position
        )}.`
      );
    }
  }
  throw syntaxError(lexer.source, position, "Unterminated string.");
}
function readName(lexer, start) {
  const body = lexer.source.body;
  const bodyLength = body.length;
  let position = start + 1;
  while (position < bodyLength) {
    const code = body.charCodeAt(position);
    if (isNameContinue(code)) {
      ++position;
    } else {
      break;
    }
  }
  return createToken(
    lexer,
    TokenKind.NAME,
    start,
    position,
    body.slice(start, position)
  );
}
var Lexer;
var init_lexer = __esm({
  "node_modules/graphql/language/lexer.mjs"() {
    init_syntaxError();
    init_ast();
    init_blockString();
    init_characterClasses();
    init_tokenKind();
    Lexer = class {
      /**
       * The previously focused non-ignored token.
       */
      /**
       * The currently focused non-ignored token.
       */
      /**
       * The (1-indexed) line containing the current token.
       */
      /**
       * The character offset at which the current line begins.
       */
      constructor(source) {
        const startOfFileToken = new Token(TokenKind.SOF, 0, 0, 0, 0);
        this.source = source;
        this.lastToken = startOfFileToken;
        this.token = startOfFileToken;
        this.line = 1;
        this.lineStart = 0;
      }
      get [Symbol.toStringTag]() {
        return "Lexer";
      }
      /**
       * Advances the token stream to the next non-ignored token.
       */
      advance() {
        this.lastToken = this.token;
        const token = this.token = this.lookahead();
        return token;
      }
      /**
       * Looks ahead and returns the next non-ignored token, but does not change
       * the state of Lexer.
       */
      lookahead() {
        let token = this.token;
        if (token.kind !== TokenKind.EOF) {
          do {
            if (token.next) {
              token = token.next;
            } else {
              const nextToken = readNextToken(this, token.end);
              token.next = nextToken;
              nextToken.prev = token;
              token = nextToken;
            }
          } while (token.kind === TokenKind.COMMENT);
        }
        return token;
      }
    };
  }
});

// node_modules/graphql/jsutils/inspect.mjs
function inspect(value) {
  return formatValue(value, []);
}
function formatValue(value, seenValues) {
  switch (typeof value) {
    case "string":
      return JSON.stringify(value);
    case "function":
      return value.name ? `[function ${value.name}]` : "[function]";
    case "object":
      return formatObjectValue(value, seenValues);
    default:
      return String(value);
  }
}
function formatObjectValue(value, previouslySeenValues) {
  if (value === null) {
    return "null";
  }
  if (previouslySeenValues.includes(value)) {
    return "[Circular]";
  }
  const seenValues = [...previouslySeenValues, value];
  if (isJSONable(value)) {
    const jsonValue = value.toJSON();
    if (jsonValue !== value) {
      return typeof jsonValue === "string" ? jsonValue : formatValue(jsonValue, seenValues);
    }
  } else if (Array.isArray(value)) {
    return formatArray(value, seenValues);
  }
  return formatObject(value, seenValues);
}
function isJSONable(value) {
  return typeof value.toJSON === "function";
}
function formatObject(object, seenValues) {
  const entries = Object.entries(object);
  if (entries.length === 0) {
    return "{}";
  }
  if (seenValues.length > MAX_RECURSIVE_DEPTH) {
    return "[" + getObjectTag(object) + "]";
  }
  const properties = entries.map(
    ([key, value]) => key + ": " + formatValue(value, seenValues)
  );
  return "{ " + properties.join(", ") + " }";
}
function formatArray(array, seenValues) {
  if (array.length === 0) {
    return "[]";
  }
  if (seenValues.length > MAX_RECURSIVE_DEPTH) {
    return "[Array]";
  }
  const len = Math.min(MAX_ARRAY_LENGTH, array.length);
  const remaining = array.length - len;
  const items = [];
  for (let i = 0; i < len; ++i) {
    items.push(formatValue(array[i], seenValues));
  }
  if (remaining === 1) {
    items.push("... 1 more item");
  } else if (remaining > 1) {
    items.push(`... ${remaining} more items`);
  }
  return "[" + items.join(", ") + "]";
}
function getObjectTag(object) {
  const tag = Object.prototype.toString.call(object).replace(/^\[object /, "").replace(/]$/, "");
  if (tag === "Object" && typeof object.constructor === "function") {
    const name = object.constructor.name;
    if (typeof name === "string" && name !== "") {
      return name;
    }
  }
  return tag;
}
var MAX_ARRAY_LENGTH, MAX_RECURSIVE_DEPTH;
var init_inspect = __esm({
  "node_modules/graphql/jsutils/inspect.mjs"() {
    MAX_ARRAY_LENGTH = 10;
    MAX_RECURSIVE_DEPTH = 2;
  }
});

// node_modules/graphql/jsutils/instanceOf.mjs
var isProduction, instanceOf;
var init_instanceOf = __esm({
  "node_modules/graphql/jsutils/instanceOf.mjs"() {
    init_inspect();
    isProduction = globalThis.process && // eslint-disable-next-line no-undef
    false;
    instanceOf = /* c8 ignore next 6 */
    // FIXME: https://github.com/graphql/graphql-js/issues/2317
    isProduction ? function instanceOf2(value, constructor) {
      return value instanceof constructor;
    } : function instanceOf3(value, constructor) {
      if (value instanceof constructor) {
        return true;
      }
      if (typeof value === "object" && value !== null) {
        var _value$constructor;
        const className = constructor.prototype[Symbol.toStringTag];
        const valueClassName = (
          // We still need to support constructor's name to detect conflicts with older versions of this library.
          Symbol.toStringTag in value ? value[Symbol.toStringTag] : (_value$constructor = value.constructor) === null || _value$constructor === void 0 ? void 0 : _value$constructor.name
        );
        if (className === valueClassName) {
          const stringifiedValue = inspect(value);
          throw new Error(`Cannot use ${className} "${stringifiedValue}" from another module or realm.

Ensure that there is only one instance of "graphql" in the node_modules
directory. If different versions of "graphql" are the dependencies of other
relied on modules, use "resolutions" to ensure only one version is installed.

https://yarnpkg.com/en/docs/selective-version-resolutions

Duplicate "graphql" modules cannot be used at the same time since different
versions may have different capabilities and behavior. The data from one
version used in the function from another could produce confusing and
spurious results.`);
        }
      }
      return false;
    };
  }
});

// node_modules/graphql/language/source.mjs
function isSource(source) {
  return instanceOf(source, Source);
}
var Source;
var init_source = __esm({
  "node_modules/graphql/language/source.mjs"() {
    init_devAssert();
    init_inspect();
    init_instanceOf();
    Source = class {
      constructor(body, name = "GraphQL request", locationOffset = {
        line: 1,
        column: 1
      }) {
        typeof body === "string" || devAssert(false, `Body must be a string. Received: ${inspect(body)}.`);
        this.body = body;
        this.name = name;
        this.locationOffset = locationOffset;
        this.locationOffset.line > 0 || devAssert(
          false,
          "line in locationOffset is 1-indexed and must be positive."
        );
        this.locationOffset.column > 0 || devAssert(
          false,
          "column in locationOffset is 1-indexed and must be positive."
        );
      }
      get [Symbol.toStringTag]() {
        return "Source";
      }
    };
  }
});

// node_modules/graphql/language/parser.mjs
function parse(source, options) {
  const parser = new Parser(source, options);
  return parser.parseDocument();
}
function parseValue(source, options) {
  const parser = new Parser(source, options);
  parser.expectToken(TokenKind.SOF);
  const value = parser.parseValueLiteral(false);
  parser.expectToken(TokenKind.EOF);
  return value;
}
function parseConstValue(source, options) {
  const parser = new Parser(source, options);
  parser.expectToken(TokenKind.SOF);
  const value = parser.parseConstValueLiteral();
  parser.expectToken(TokenKind.EOF);
  return value;
}
function parseType(source, options) {
  const parser = new Parser(source, options);
  parser.expectToken(TokenKind.SOF);
  const type = parser.parseTypeReference();
  parser.expectToken(TokenKind.EOF);
  return type;
}
function getTokenDesc(token) {
  const value = token.value;
  return getTokenKindDesc(token.kind) + (value != null ? ` "${value}"` : "");
}
function getTokenKindDesc(kind) {
  return isPunctuatorTokenKind(kind) ? `"${kind}"` : kind;
}
var Parser;
var init_parser = __esm({
  "node_modules/graphql/language/parser.mjs"() {
    init_syntaxError();
    init_ast();
    init_directiveLocation();
    init_kinds();
    init_lexer();
    init_source();
    init_tokenKind();
    Parser = class {
      constructor(source, options = {}) {
        const sourceObj = isSource(source) ? source : new Source(source);
        this._lexer = new Lexer(sourceObj);
        this._options = options;
        this._tokenCounter = 0;
      }
      /**
       * Converts a name lex token into a name parse node.
       */
      parseName() {
        const token = this.expectToken(TokenKind.NAME);
        return this.node(token, {
          kind: Kind.NAME,
          value: token.value
        });
      }
      // Implements the parsing rules in the Document section.
      /**
       * Document : Definition+
       */
      parseDocument() {
        return this.node(this._lexer.token, {
          kind: Kind.DOCUMENT,
          definitions: this.many(
            TokenKind.SOF,
            this.parseDefinition,
            TokenKind.EOF
          )
        });
      }
      /**
       * Definition :
       *   - ExecutableDefinition
       *   - TypeSystemDefinition
       *   - TypeSystemExtension
       *
       * ExecutableDefinition :
       *   - OperationDefinition
       *   - FragmentDefinition
       *
       * TypeSystemDefinition :
       *   - SchemaDefinition
       *   - TypeDefinition
       *   - DirectiveDefinition
       *
       * TypeDefinition :
       *   - ScalarTypeDefinition
       *   - ObjectTypeDefinition
       *   - InterfaceTypeDefinition
       *   - UnionTypeDefinition
       *   - EnumTypeDefinition
       *   - InputObjectTypeDefinition
       */
      parseDefinition() {
        if (this.peek(TokenKind.BRACE_L)) {
          return this.parseOperationDefinition();
        }
        const hasDescription = this.peekDescription();
        const keywordToken = hasDescription ? this._lexer.lookahead() : this._lexer.token;
        if (keywordToken.kind === TokenKind.NAME) {
          switch (keywordToken.value) {
            case "schema":
              return this.parseSchemaDefinition();
            case "scalar":
              return this.parseScalarTypeDefinition();
            case "type":
              return this.parseObjectTypeDefinition();
            case "interface":
              return this.parseInterfaceTypeDefinition();
            case "union":
              return this.parseUnionTypeDefinition();
            case "enum":
              return this.parseEnumTypeDefinition();
            case "input":
              return this.parseInputObjectTypeDefinition();
            case "directive":
              return this.parseDirectiveDefinition();
          }
          if (hasDescription) {
            throw syntaxError(
              this._lexer.source,
              this._lexer.token.start,
              "Unexpected description, descriptions are supported only on type definitions."
            );
          }
          switch (keywordToken.value) {
            case "query":
            case "mutation":
            case "subscription":
              return this.parseOperationDefinition();
            case "fragment":
              return this.parseFragmentDefinition();
            case "extend":
              return this.parseTypeSystemExtension();
          }
        }
        throw this.unexpected(keywordToken);
      }
      // Implements the parsing rules in the Operations section.
      /**
       * OperationDefinition :
       *  - SelectionSet
       *  - OperationType Name? VariableDefinitions? Directives? SelectionSet
       */
      parseOperationDefinition() {
        const start = this._lexer.token;
        if (this.peek(TokenKind.BRACE_L)) {
          return this.node(start, {
            kind: Kind.OPERATION_DEFINITION,
            operation: OperationTypeNode.QUERY,
            name: void 0,
            variableDefinitions: [],
            directives: [],
            selectionSet: this.parseSelectionSet()
          });
        }
        const operation = this.parseOperationType();
        let name;
        if (this.peek(TokenKind.NAME)) {
          name = this.parseName();
        }
        return this.node(start, {
          kind: Kind.OPERATION_DEFINITION,
          operation,
          name,
          variableDefinitions: this.parseVariableDefinitions(),
          directives: this.parseDirectives(false),
          selectionSet: this.parseSelectionSet()
        });
      }
      /**
       * OperationType : one of query mutation subscription
       */
      parseOperationType() {
        const operationToken = this.expectToken(TokenKind.NAME);
        switch (operationToken.value) {
          case "query":
            return OperationTypeNode.QUERY;
          case "mutation":
            return OperationTypeNode.MUTATION;
          case "subscription":
            return OperationTypeNode.SUBSCRIPTION;
        }
        throw this.unexpected(operationToken);
      }
      /**
       * VariableDefinitions : ( VariableDefinition+ )
       */
      parseVariableDefinitions() {
        return this.optionalMany(
          TokenKind.PAREN_L,
          this.parseVariableDefinition,
          TokenKind.PAREN_R
        );
      }
      /**
       * VariableDefinition : Variable : Type DefaultValue? Directives[Const]?
       */
      parseVariableDefinition() {
        return this.node(this._lexer.token, {
          kind: Kind.VARIABLE_DEFINITION,
          variable: this.parseVariable(),
          type: (this.expectToken(TokenKind.COLON), this.parseTypeReference()),
          defaultValue: this.expectOptionalToken(TokenKind.EQUALS) ? this.parseConstValueLiteral() : void 0,
          directives: this.parseConstDirectives()
        });
      }
      /**
       * Variable : $ Name
       */
      parseVariable() {
        const start = this._lexer.token;
        this.expectToken(TokenKind.DOLLAR);
        return this.node(start, {
          kind: Kind.VARIABLE,
          name: this.parseName()
        });
      }
      /**
       * ```
       * SelectionSet : { Selection+ }
       * ```
       */
      parseSelectionSet() {
        return this.node(this._lexer.token, {
          kind: Kind.SELECTION_SET,
          selections: this.many(
            TokenKind.BRACE_L,
            this.parseSelection,
            TokenKind.BRACE_R
          )
        });
      }
      /**
       * Selection :
       *   - Field
       *   - FragmentSpread
       *   - InlineFragment
       */
      parseSelection() {
        return this.peek(TokenKind.SPREAD) ? this.parseFragment() : this.parseField();
      }
      /**
       * Field : Alias? Name Arguments? Directives? SelectionSet?
       *
       * Alias : Name :
       */
      parseField() {
        const start = this._lexer.token;
        const nameOrAlias = this.parseName();
        let alias;
        let name;
        if (this.expectOptionalToken(TokenKind.COLON)) {
          alias = nameOrAlias;
          name = this.parseName();
        } else {
          name = nameOrAlias;
        }
        return this.node(start, {
          kind: Kind.FIELD,
          alias,
          name,
          arguments: this.parseArguments(false),
          directives: this.parseDirectives(false),
          selectionSet: this.peek(TokenKind.BRACE_L) ? this.parseSelectionSet() : void 0
        });
      }
      /**
       * Arguments[Const] : ( Argument[?Const]+ )
       */
      parseArguments(isConst) {
        const item = isConst ? this.parseConstArgument : this.parseArgument;
        return this.optionalMany(TokenKind.PAREN_L, item, TokenKind.PAREN_R);
      }
      /**
       * Argument[Const] : Name : Value[?Const]
       */
      parseArgument(isConst = false) {
        const start = this._lexer.token;
        const name = this.parseName();
        this.expectToken(TokenKind.COLON);
        return this.node(start, {
          kind: Kind.ARGUMENT,
          name,
          value: this.parseValueLiteral(isConst)
        });
      }
      parseConstArgument() {
        return this.parseArgument(true);
      }
      // Implements the parsing rules in the Fragments section.
      /**
       * Corresponds to both FragmentSpread and InlineFragment in the spec.
       *
       * FragmentSpread : ... FragmentName Directives?
       *
       * InlineFragment : ... TypeCondition? Directives? SelectionSet
       */
      parseFragment() {
        const start = this._lexer.token;
        this.expectToken(TokenKind.SPREAD);
        const hasTypeCondition = this.expectOptionalKeyword("on");
        if (!hasTypeCondition && this.peek(TokenKind.NAME)) {
          return this.node(start, {
            kind: Kind.FRAGMENT_SPREAD,
            name: this.parseFragmentName(),
            directives: this.parseDirectives(false)
          });
        }
        return this.node(start, {
          kind: Kind.INLINE_FRAGMENT,
          typeCondition: hasTypeCondition ? this.parseNamedType() : void 0,
          directives: this.parseDirectives(false),
          selectionSet: this.parseSelectionSet()
        });
      }
      /**
       * FragmentDefinition :
       *   - fragment FragmentName on TypeCondition Directives? SelectionSet
       *
       * TypeCondition : NamedType
       */
      parseFragmentDefinition() {
        const start = this._lexer.token;
        this.expectKeyword("fragment");
        if (this._options.allowLegacyFragmentVariables === true) {
          return this.node(start, {
            kind: Kind.FRAGMENT_DEFINITION,
            name: this.parseFragmentName(),
            variableDefinitions: this.parseVariableDefinitions(),
            typeCondition: (this.expectKeyword("on"), this.parseNamedType()),
            directives: this.parseDirectives(false),
            selectionSet: this.parseSelectionSet()
          });
        }
        return this.node(start, {
          kind: Kind.FRAGMENT_DEFINITION,
          name: this.parseFragmentName(),
          typeCondition: (this.expectKeyword("on"), this.parseNamedType()),
          directives: this.parseDirectives(false),
          selectionSet: this.parseSelectionSet()
        });
      }
      /**
       * FragmentName : Name but not `on`
       */
      parseFragmentName() {
        if (this._lexer.token.value === "on") {
          throw this.unexpected();
        }
        return this.parseName();
      }
      // Implements the parsing rules in the Values section.
      /**
       * Value[Const] :
       *   - [~Const] Variable
       *   - IntValue
       *   - FloatValue
       *   - StringValue
       *   - BooleanValue
       *   - NullValue
       *   - EnumValue
       *   - ListValue[?Const]
       *   - ObjectValue[?Const]
       *
       * BooleanValue : one of `true` `false`
       *
       * NullValue : `null`
       *
       * EnumValue : Name but not `true`, `false` or `null`
       */
      parseValueLiteral(isConst) {
        const token = this._lexer.token;
        switch (token.kind) {
          case TokenKind.BRACKET_L:
            return this.parseList(isConst);
          case TokenKind.BRACE_L:
            return this.parseObject(isConst);
          case TokenKind.INT:
            this.advanceLexer();
            return this.node(token, {
              kind: Kind.INT,
              value: token.value
            });
          case TokenKind.FLOAT:
            this.advanceLexer();
            return this.node(token, {
              kind: Kind.FLOAT,
              value: token.value
            });
          case TokenKind.STRING:
          case TokenKind.BLOCK_STRING:
            return this.parseStringLiteral();
          case TokenKind.NAME:
            this.advanceLexer();
            switch (token.value) {
              case "true":
                return this.node(token, {
                  kind: Kind.BOOLEAN,
                  value: true
                });
              case "false":
                return this.node(token, {
                  kind: Kind.BOOLEAN,
                  value: false
                });
              case "null":
                return this.node(token, {
                  kind: Kind.NULL
                });
              default:
                return this.node(token, {
                  kind: Kind.ENUM,
                  value: token.value
                });
            }
          case TokenKind.DOLLAR:
            if (isConst) {
              this.expectToken(TokenKind.DOLLAR);
              if (this._lexer.token.kind === TokenKind.NAME) {
                const varName = this._lexer.token.value;
                throw syntaxError(
                  this._lexer.source,
                  token.start,
                  `Unexpected variable "$${varName}" in constant value.`
                );
              } else {
                throw this.unexpected(token);
              }
            }
            return this.parseVariable();
          default:
            throw this.unexpected();
        }
      }
      parseConstValueLiteral() {
        return this.parseValueLiteral(true);
      }
      parseStringLiteral() {
        const token = this._lexer.token;
        this.advanceLexer();
        return this.node(token, {
          kind: Kind.STRING,
          value: token.value,
          block: token.kind === TokenKind.BLOCK_STRING
        });
      }
      /**
       * ListValue[Const] :
       *   - [ ]
       *   - [ Value[?Const]+ ]
       */
      parseList(isConst) {
        const item = () => this.parseValueLiteral(isConst);
        return this.node(this._lexer.token, {
          kind: Kind.LIST,
          values: this.any(TokenKind.BRACKET_L, item, TokenKind.BRACKET_R)
        });
      }
      /**
       * ```
       * ObjectValue[Const] :
       *   - { }
       *   - { ObjectField[?Const]+ }
       * ```
       */
      parseObject(isConst) {
        const item = () => this.parseObjectField(isConst);
        return this.node(this._lexer.token, {
          kind: Kind.OBJECT,
          fields: this.any(TokenKind.BRACE_L, item, TokenKind.BRACE_R)
        });
      }
      /**
       * ObjectField[Const] : Name : Value[?Const]
       */
      parseObjectField(isConst) {
        const start = this._lexer.token;
        const name = this.parseName();
        this.expectToken(TokenKind.COLON);
        return this.node(start, {
          kind: Kind.OBJECT_FIELD,
          name,
          value: this.parseValueLiteral(isConst)
        });
      }
      // Implements the parsing rules in the Directives section.
      /**
       * Directives[Const] : Directive[?Const]+
       */
      parseDirectives(isConst) {
        const directives = [];
        while (this.peek(TokenKind.AT)) {
          directives.push(this.parseDirective(isConst));
        }
        return directives;
      }
      parseConstDirectives() {
        return this.parseDirectives(true);
      }
      /**
       * ```
       * Directive[Const] : @ Name Arguments[?Const]?
       * ```
       */
      parseDirective(isConst) {
        const start = this._lexer.token;
        this.expectToken(TokenKind.AT);
        return this.node(start, {
          kind: Kind.DIRECTIVE,
          name: this.parseName(),
          arguments: this.parseArguments(isConst)
        });
      }
      // Implements the parsing rules in the Types section.
      /**
       * Type :
       *   - NamedType
       *   - ListType
       *   - NonNullType
       */
      parseTypeReference() {
        const start = this._lexer.token;
        let type;
        if (this.expectOptionalToken(TokenKind.BRACKET_L)) {
          const innerType = this.parseTypeReference();
          this.expectToken(TokenKind.BRACKET_R);
          type = this.node(start, {
            kind: Kind.LIST_TYPE,
            type: innerType
          });
        } else {
          type = this.parseNamedType();
        }
        if (this.expectOptionalToken(TokenKind.BANG)) {
          return this.node(start, {
            kind: Kind.NON_NULL_TYPE,
            type
          });
        }
        return type;
      }
      /**
       * NamedType : Name
       */
      parseNamedType() {
        return this.node(this._lexer.token, {
          kind: Kind.NAMED_TYPE,
          name: this.parseName()
        });
      }
      // Implements the parsing rules in the Type Definition section.
      peekDescription() {
        return this.peek(TokenKind.STRING) || this.peek(TokenKind.BLOCK_STRING);
      }
      /**
       * Description : StringValue
       */
      parseDescription() {
        if (this.peekDescription()) {
          return this.parseStringLiteral();
        }
      }
      /**
       * ```
       * SchemaDefinition : Description? schema Directives[Const]? { OperationTypeDefinition+ }
       * ```
       */
      parseSchemaDefinition() {
        const start = this._lexer.token;
        const description = this.parseDescription();
        this.expectKeyword("schema");
        const directives = this.parseConstDirectives();
        const operationTypes = this.many(
          TokenKind.BRACE_L,
          this.parseOperationTypeDefinition,
          TokenKind.BRACE_R
        );
        return this.node(start, {
          kind: Kind.SCHEMA_DEFINITION,
          description,
          directives,
          operationTypes
        });
      }
      /**
       * OperationTypeDefinition : OperationType : NamedType
       */
      parseOperationTypeDefinition() {
        const start = this._lexer.token;
        const operation = this.parseOperationType();
        this.expectToken(TokenKind.COLON);
        const type = this.parseNamedType();
        return this.node(start, {
          kind: Kind.OPERATION_TYPE_DEFINITION,
          operation,
          type
        });
      }
      /**
       * ScalarTypeDefinition : Description? scalar Name Directives[Const]?
       */
      parseScalarTypeDefinition() {
        const start = this._lexer.token;
        const description = this.parseDescription();
        this.expectKeyword("scalar");
        const name = this.parseName();
        const directives = this.parseConstDirectives();
        return this.node(start, {
          kind: Kind.SCALAR_TYPE_DEFINITION,
          description,
          name,
          directives
        });
      }
      /**
       * ObjectTypeDefinition :
       *   Description?
       *   type Name ImplementsInterfaces? Directives[Const]? FieldsDefinition?
       */
      parseObjectTypeDefinition() {
        const start = this._lexer.token;
        const description = this.parseDescription();
        this.expectKeyword("type");
        const name = this.parseName();
        const interfaces = this.parseImplementsInterfaces();
        const directives = this.parseConstDirectives();
        const fields = this.parseFieldsDefinition();
        return this.node(start, {
          kind: Kind.OBJECT_TYPE_DEFINITION,
          description,
          name,
          interfaces,
          directives,
          fields
        });
      }
      /**
       * ImplementsInterfaces :
       *   - implements `&`? NamedType
       *   - ImplementsInterfaces & NamedType
       */
      parseImplementsInterfaces() {
        return this.expectOptionalKeyword("implements") ? this.delimitedMany(TokenKind.AMP, this.parseNamedType) : [];
      }
      /**
       * ```
       * FieldsDefinition : { FieldDefinition+ }
       * ```
       */
      parseFieldsDefinition() {
        return this.optionalMany(
          TokenKind.BRACE_L,
          this.parseFieldDefinition,
          TokenKind.BRACE_R
        );
      }
      /**
       * FieldDefinition :
       *   - Description? Name ArgumentsDefinition? : Type Directives[Const]?
       */
      parseFieldDefinition() {
        const start = this._lexer.token;
        const description = this.parseDescription();
        const name = this.parseName();
        const args = this.parseArgumentDefs();
        this.expectToken(TokenKind.COLON);
        const type = this.parseTypeReference();
        const directives = this.parseConstDirectives();
        return this.node(start, {
          kind: Kind.FIELD_DEFINITION,
          description,
          name,
          arguments: args,
          type,
          directives
        });
      }
      /**
       * ArgumentsDefinition : ( InputValueDefinition+ )
       */
      parseArgumentDefs() {
        return this.optionalMany(
          TokenKind.PAREN_L,
          this.parseInputValueDef,
          TokenKind.PAREN_R
        );
      }
      /**
       * InputValueDefinition :
       *   - Description? Name : Type DefaultValue? Directives[Const]?
       */
      parseInputValueDef() {
        const start = this._lexer.token;
        const description = this.parseDescription();
        const name = this.parseName();
        this.expectToken(TokenKind.COLON);
        const type = this.parseTypeReference();
        let defaultValue;
        if (this.expectOptionalToken(TokenKind.EQUALS)) {
          defaultValue = this.parseConstValueLiteral();
        }
        const directives = this.parseConstDirectives();
        return this.node(start, {
          kind: Kind.INPUT_VALUE_DEFINITION,
          description,
          name,
          type,
          defaultValue,
          directives
        });
      }
      /**
       * InterfaceTypeDefinition :
       *   - Description? interface Name Directives[Const]? FieldsDefinition?
       */
      parseInterfaceTypeDefinition() {
        const start = this._lexer.token;
        const description = this.parseDescription();
        this.expectKeyword("interface");
        const name = this.parseName();
        const interfaces = this.parseImplementsInterfaces();
        const directives = this.parseConstDirectives();
        const fields = this.parseFieldsDefinition();
        return this.node(start, {
          kind: Kind.INTERFACE_TYPE_DEFINITION,
          description,
          name,
          interfaces,
          directives,
          fields
        });
      }
      /**
       * UnionTypeDefinition :
       *   - Description? union Name Directives[Const]? UnionMemberTypes?
       */
      parseUnionTypeDefinition() {
        const start = this._lexer.token;
        const description = this.parseDescription();
        this.expectKeyword("union");
        const name = this.parseName();
        const directives = this.parseConstDirectives();
        const types = this.parseUnionMemberTypes();
        return this.node(start, {
          kind: Kind.UNION_TYPE_DEFINITION,
          description,
          name,
          directives,
          types
        });
      }
      /**
       * UnionMemberTypes :
       *   - = `|`? NamedType
       *   - UnionMemberTypes | NamedType
       */
      parseUnionMemberTypes() {
        return this.expectOptionalToken(TokenKind.EQUALS) ? this.delimitedMany(TokenKind.PIPE, this.parseNamedType) : [];
      }
      /**
       * EnumTypeDefinition :
       *   - Description? enum Name Directives[Const]? EnumValuesDefinition?
       */
      parseEnumTypeDefinition() {
        const start = this._lexer.token;
        const description = this.parseDescription();
        this.expectKeyword("enum");
        const name = this.parseName();
        const directives = this.parseConstDirectives();
        const values = this.parseEnumValuesDefinition();
        return this.node(start, {
          kind: Kind.ENUM_TYPE_DEFINITION,
          description,
          name,
          directives,
          values
        });
      }
      /**
       * ```
       * EnumValuesDefinition : { EnumValueDefinition+ }
       * ```
       */
      parseEnumValuesDefinition() {
        return this.optionalMany(
          TokenKind.BRACE_L,
          this.parseEnumValueDefinition,
          TokenKind.BRACE_R
        );
      }
      /**
       * EnumValueDefinition : Description? EnumValue Directives[Const]?
       */
      parseEnumValueDefinition() {
        const start = this._lexer.token;
        const description = this.parseDescription();
        const name = this.parseEnumValueName();
        const directives = this.parseConstDirectives();
        return this.node(start, {
          kind: Kind.ENUM_VALUE_DEFINITION,
          description,
          name,
          directives
        });
      }
      /**
       * EnumValue : Name but not `true`, `false` or `null`
       */
      parseEnumValueName() {
        if (this._lexer.token.value === "true" || this._lexer.token.value === "false" || this._lexer.token.value === "null") {
          throw syntaxError(
            this._lexer.source,
            this._lexer.token.start,
            `${getTokenDesc(
              this._lexer.token
            )} is reserved and cannot be used for an enum value.`
          );
        }
        return this.parseName();
      }
      /**
       * InputObjectTypeDefinition :
       *   - Description? input Name Directives[Const]? InputFieldsDefinition?
       */
      parseInputObjectTypeDefinition() {
        const start = this._lexer.token;
        const description = this.parseDescription();
        this.expectKeyword("input");
        const name = this.parseName();
        const directives = this.parseConstDirectives();
        const fields = this.parseInputFieldsDefinition();
        return this.node(start, {
          kind: Kind.INPUT_OBJECT_TYPE_DEFINITION,
          description,
          name,
          directives,
          fields
        });
      }
      /**
       * ```
       * InputFieldsDefinition : { InputValueDefinition+ }
       * ```
       */
      parseInputFieldsDefinition() {
        return this.optionalMany(
          TokenKind.BRACE_L,
          this.parseInputValueDef,
          TokenKind.BRACE_R
        );
      }
      /**
       * TypeSystemExtension :
       *   - SchemaExtension
       *   - TypeExtension
       *
       * TypeExtension :
       *   - ScalarTypeExtension
       *   - ObjectTypeExtension
       *   - InterfaceTypeExtension
       *   - UnionTypeExtension
       *   - EnumTypeExtension
       *   - InputObjectTypeDefinition
       */
      parseTypeSystemExtension() {
        const keywordToken = this._lexer.lookahead();
        if (keywordToken.kind === TokenKind.NAME) {
          switch (keywordToken.value) {
            case "schema":
              return this.parseSchemaExtension();
            case "scalar":
              return this.parseScalarTypeExtension();
            case "type":
              return this.parseObjectTypeExtension();
            case "interface":
              return this.parseInterfaceTypeExtension();
            case "union":
              return this.parseUnionTypeExtension();
            case "enum":
              return this.parseEnumTypeExtension();
            case "input":
              return this.parseInputObjectTypeExtension();
          }
        }
        throw this.unexpected(keywordToken);
      }
      /**
       * ```
       * SchemaExtension :
       *  - extend schema Directives[Const]? { OperationTypeDefinition+ }
       *  - extend schema Directives[Const]
       * ```
       */
      parseSchemaExtension() {
        const start = this._lexer.token;
        this.expectKeyword("extend");
        this.expectKeyword("schema");
        const directives = this.parseConstDirectives();
        const operationTypes = this.optionalMany(
          TokenKind.BRACE_L,
          this.parseOperationTypeDefinition,
          TokenKind.BRACE_R
        );
        if (directives.length === 0 && operationTypes.length === 0) {
          throw this.unexpected();
        }
        return this.node(start, {
          kind: Kind.SCHEMA_EXTENSION,
          directives,
          operationTypes
        });
      }
      /**
       * ScalarTypeExtension :
       *   - extend scalar Name Directives[Const]
       */
      parseScalarTypeExtension() {
        const start = this._lexer.token;
        this.expectKeyword("extend");
        this.expectKeyword("scalar");
        const name = this.parseName();
        const directives = this.parseConstDirectives();
        if (directives.length === 0) {
          throw this.unexpected();
        }
        return this.node(start, {
          kind: Kind.SCALAR_TYPE_EXTENSION,
          name,
          directives
        });
      }
      /**
       * ObjectTypeExtension :
       *  - extend type Name ImplementsInterfaces? Directives[Const]? FieldsDefinition
       *  - extend type Name ImplementsInterfaces? Directives[Const]
       *  - extend type Name ImplementsInterfaces
       */
      parseObjectTypeExtension() {
        const start = this._lexer.token;
        this.expectKeyword("extend");
        this.expectKeyword("type");
        const name = this.parseName();
        const interfaces = this.parseImplementsInterfaces();
        const directives = this.parseConstDirectives();
        const fields = this.parseFieldsDefinition();
        if (interfaces.length === 0 && directives.length === 0 && fields.length === 0) {
          throw this.unexpected();
        }
        return this.node(start, {
          kind: Kind.OBJECT_TYPE_EXTENSION,
          name,
          interfaces,
          directives,
          fields
        });
      }
      /**
       * InterfaceTypeExtension :
       *  - extend interface Name ImplementsInterfaces? Directives[Const]? FieldsDefinition
       *  - extend interface Name ImplementsInterfaces? Directives[Const]
       *  - extend interface Name ImplementsInterfaces
       */
      parseInterfaceTypeExtension() {
        const start = this._lexer.token;
        this.expectKeyword("extend");
        this.expectKeyword("interface");
        const name = this.parseName();
        const interfaces = this.parseImplementsInterfaces();
        const directives = this.parseConstDirectives();
        const fields = this.parseFieldsDefinition();
        if (interfaces.length === 0 && directives.length === 0 && fields.length === 0) {
          throw this.unexpected();
        }
        return this.node(start, {
          kind: Kind.INTERFACE_TYPE_EXTENSION,
          name,
          interfaces,
          directives,
          fields
        });
      }
      /**
       * UnionTypeExtension :
       *   - extend union Name Directives[Const]? UnionMemberTypes
       *   - extend union Name Directives[Const]
       */
      parseUnionTypeExtension() {
        const start = this._lexer.token;
        this.expectKeyword("extend");
        this.expectKeyword("union");
        const name = this.parseName();
        const directives = this.parseConstDirectives();
        const types = this.parseUnionMemberTypes();
        if (directives.length === 0 && types.length === 0) {
          throw this.unexpected();
        }
        return this.node(start, {
          kind: Kind.UNION_TYPE_EXTENSION,
          name,
          directives,
          types
        });
      }
      /**
       * EnumTypeExtension :
       *   - extend enum Name Directives[Const]? EnumValuesDefinition
       *   - extend enum Name Directives[Const]
       */
      parseEnumTypeExtension() {
        const start = this._lexer.token;
        this.expectKeyword("extend");
        this.expectKeyword("enum");
        const name = this.parseName();
        const directives = this.parseConstDirectives();
        const values = this.parseEnumValuesDefinition();
        if (directives.length === 0 && values.length === 0) {
          throw this.unexpected();
        }
        return this.node(start, {
          kind: Kind.ENUM_TYPE_EXTENSION,
          name,
          directives,
          values
        });
      }
      /**
       * InputObjectTypeExtension :
       *   - extend input Name Directives[Const]? InputFieldsDefinition
       *   - extend input Name Directives[Const]
       */
      parseInputObjectTypeExtension() {
        const start = this._lexer.token;
        this.expectKeyword("extend");
        this.expectKeyword("input");
        const name = this.parseName();
        const directives = this.parseConstDirectives();
        const fields = this.parseInputFieldsDefinition();
        if (directives.length === 0 && fields.length === 0) {
          throw this.unexpected();
        }
        return this.node(start, {
          kind: Kind.INPUT_OBJECT_TYPE_EXTENSION,
          name,
          directives,
          fields
        });
      }
      /**
       * ```
       * DirectiveDefinition :
       *   - Description? directive @ Name ArgumentsDefinition? `repeatable`? on DirectiveLocations
       * ```
       */
      parseDirectiveDefinition() {
        const start = this._lexer.token;
        const description = this.parseDescription();
        this.expectKeyword("directive");
        this.expectToken(TokenKind.AT);
        const name = this.parseName();
        const args = this.parseArgumentDefs();
        const repeatable = this.expectOptionalKeyword("repeatable");
        this.expectKeyword("on");
        const locations = this.parseDirectiveLocations();
        return this.node(start, {
          kind: Kind.DIRECTIVE_DEFINITION,
          description,
          name,
          arguments: args,
          repeatable,
          locations
        });
      }
      /**
       * DirectiveLocations :
       *   - `|`? DirectiveLocation
       *   - DirectiveLocations | DirectiveLocation
       */
      parseDirectiveLocations() {
        return this.delimitedMany(TokenKind.PIPE, this.parseDirectiveLocation);
      }
      /*
       * DirectiveLocation :
       *   - ExecutableDirectiveLocation
       *   - TypeSystemDirectiveLocation
       *
       * ExecutableDirectiveLocation : one of
       *   `QUERY`
       *   `MUTATION`
       *   `SUBSCRIPTION`
       *   `FIELD`
       *   `FRAGMENT_DEFINITION`
       *   `FRAGMENT_SPREAD`
       *   `INLINE_FRAGMENT`
       *
       * TypeSystemDirectiveLocation : one of
       *   `SCHEMA`
       *   `SCALAR`
       *   `OBJECT`
       *   `FIELD_DEFINITION`
       *   `ARGUMENT_DEFINITION`
       *   `INTERFACE`
       *   `UNION`
       *   `ENUM`
       *   `ENUM_VALUE`
       *   `INPUT_OBJECT`
       *   `INPUT_FIELD_DEFINITION`
       */
      parseDirectiveLocation() {
        const start = this._lexer.token;
        const name = this.parseName();
        if (Object.prototype.hasOwnProperty.call(DirectiveLocation, name.value)) {
          return name;
        }
        throw this.unexpected(start);
      }
      // Core parsing utility functions
      /**
       * Returns a node that, if configured to do so, sets a "loc" field as a
       * location object, used to identify the place in the source that created a
       * given parsed object.
       */
      node(startToken, node) {
        if (this._options.noLocation !== true) {
          node.loc = new Location(
            startToken,
            this._lexer.lastToken,
            this._lexer.source
          );
        }
        return node;
      }
      /**
       * Determines if the next token is of a given kind
       */
      peek(kind) {
        return this._lexer.token.kind === kind;
      }
      /**
       * If the next token is of the given kind, return that token after advancing the lexer.
       * Otherwise, do not change the parser state and throw an error.
       */
      expectToken(kind) {
        const token = this._lexer.token;
        if (token.kind === kind) {
          this.advanceLexer();
          return token;
        }
        throw syntaxError(
          this._lexer.source,
          token.start,
          `Expected ${getTokenKindDesc(kind)}, found ${getTokenDesc(token)}.`
        );
      }
      /**
       * If the next token is of the given kind, return "true" after advancing the lexer.
       * Otherwise, do not change the parser state and return "false".
       */
      expectOptionalToken(kind) {
        const token = this._lexer.token;
        if (token.kind === kind) {
          this.advanceLexer();
          return true;
        }
        return false;
      }
      /**
       * If the next token is a given keyword, advance the lexer.
       * Otherwise, do not change the parser state and throw an error.
       */
      expectKeyword(value) {
        const token = this._lexer.token;
        if (token.kind === TokenKind.NAME && token.value === value) {
          this.advanceLexer();
        } else {
          throw syntaxError(
            this._lexer.source,
            token.start,
            `Expected "${value}", found ${getTokenDesc(token)}.`
          );
        }
      }
      /**
       * If the next token is a given keyword, return "true" after advancing the lexer.
       * Otherwise, do not change the parser state and return "false".
       */
      expectOptionalKeyword(value) {
        const token = this._lexer.token;
        if (token.kind === TokenKind.NAME && token.value === value) {
          this.advanceLexer();
          return true;
        }
        return false;
      }
      /**
       * Helper function for creating an error when an unexpected lexed token is encountered.
       */
      unexpected(atToken) {
        const token = atToken !== null && atToken !== void 0 ? atToken : this._lexer.token;
        return syntaxError(
          this._lexer.source,
          token.start,
          `Unexpected ${getTokenDesc(token)}.`
        );
      }
      /**
       * Returns a possibly empty list of parse nodes, determined by the parseFn.
       * This list begins with a lex token of openKind and ends with a lex token of closeKind.
       * Advances the parser to the next lex token after the closing token.
       */
      any(openKind, parseFn, closeKind) {
        this.expectToken(openKind);
        const nodes = [];
        while (!this.expectOptionalToken(closeKind)) {
          nodes.push(parseFn.call(this));
        }
        return nodes;
      }
      /**
       * Returns a list of parse nodes, determined by the parseFn.
       * It can be empty only if open token is missing otherwise it will always return non-empty list
       * that begins with a lex token of openKind and ends with a lex token of closeKind.
       * Advances the parser to the next lex token after the closing token.
       */
      optionalMany(openKind, parseFn, closeKind) {
        if (this.expectOptionalToken(openKind)) {
          const nodes = [];
          do {
            nodes.push(parseFn.call(this));
          } while (!this.expectOptionalToken(closeKind));
          return nodes;
        }
        return [];
      }
      /**
       * Returns a non-empty list of parse nodes, determined by the parseFn.
       * This list begins with a lex token of openKind and ends with a lex token of closeKind.
       * Advances the parser to the next lex token after the closing token.
       */
      many(openKind, parseFn, closeKind) {
        this.expectToken(openKind);
        const nodes = [];
        do {
          nodes.push(parseFn.call(this));
        } while (!this.expectOptionalToken(closeKind));
        return nodes;
      }
      /**
       * Returns a non-empty list of parse nodes, determined by the parseFn.
       * This list may begin with a lex token of delimiterKind followed by items separated by lex tokens of tokenKind.
       * Advances the parser to the next lex token after last item in the list.
       */
      delimitedMany(delimiterKind, parseFn) {
        this.expectOptionalToken(delimiterKind);
        const nodes = [];
        do {
          nodes.push(parseFn.call(this));
        } while (this.expectOptionalToken(delimiterKind));
        return nodes;
      }
      advanceLexer() {
        const { maxTokens } = this._options;
        const token = this._lexer.advance();
        if (maxTokens !== void 0 && token.kind !== TokenKind.EOF) {
          ++this._tokenCounter;
          if (this._tokenCounter > maxTokens) {
            throw syntaxError(
              this._lexer.source,
              token.start,
              `Document contains more that ${maxTokens} tokens. Parsing aborted.`
            );
          }
        }
      }
    };
  }
});

// node_modules/graphql/jsutils/didYouMean.mjs
function didYouMean(firstArg, secondArg) {
  const [subMessage, suggestionsArg] = secondArg ? [firstArg, secondArg] : [void 0, firstArg];
  let message = " Did you mean ";
  if (subMessage) {
    message += subMessage + " ";
  }
  const suggestions = suggestionsArg.map((x) => `"${x}"`);
  switch (suggestions.length) {
    case 0:
      return "";
    case 1:
      return message + suggestions[0] + "?";
    case 2:
      return message + suggestions[0] + " or " + suggestions[1] + "?";
  }
  const selected = suggestions.slice(0, MAX_SUGGESTIONS);
  const lastItem = selected.pop();
  return message + selected.join(", ") + ", or " + lastItem + "?";
}
var MAX_SUGGESTIONS;
var init_didYouMean = __esm({
  "node_modules/graphql/jsutils/didYouMean.mjs"() {
    MAX_SUGGESTIONS = 5;
  }
});

// node_modules/graphql/jsutils/identityFunc.mjs
function identityFunc(x) {
  return x;
}
var init_identityFunc = __esm({
  "node_modules/graphql/jsutils/identityFunc.mjs"() {
  }
});

// node_modules/graphql/jsutils/keyMap.mjs
function keyMap(list, keyFn) {
  const result = /* @__PURE__ */ Object.create(null);
  for (const item of list) {
    result[keyFn(item)] = item;
  }
  return result;
}
var init_keyMap = __esm({
  "node_modules/graphql/jsutils/keyMap.mjs"() {
  }
});

// node_modules/graphql/jsutils/keyValMap.mjs
function keyValMap(list, keyFn, valFn) {
  const result = /* @__PURE__ */ Object.create(null);
  for (const item of list) {
    result[keyFn(item)] = valFn(item);
  }
  return result;
}
var init_keyValMap = __esm({
  "node_modules/graphql/jsutils/keyValMap.mjs"() {
  }
});

// node_modules/graphql/jsutils/mapValue.mjs
function mapValue(map, fn) {
  const result = /* @__PURE__ */ Object.create(null);
  for (const key of Object.keys(map)) {
    result[key] = fn(map[key], key);
  }
  return result;
}
var init_mapValue = __esm({
  "node_modules/graphql/jsutils/mapValue.mjs"() {
  }
});

// node_modules/graphql/jsutils/naturalCompare.mjs
function naturalCompare(aStr, bStr) {
  let aIndex = 0;
  let bIndex = 0;
  while (aIndex < aStr.length && bIndex < bStr.length) {
    let aChar = aStr.charCodeAt(aIndex);
    let bChar = bStr.charCodeAt(bIndex);
    if (isDigit2(aChar) && isDigit2(bChar)) {
      let aNum = 0;
      do {
        ++aIndex;
        aNum = aNum * 10 + aChar - DIGIT_0;
        aChar = aStr.charCodeAt(aIndex);
      } while (isDigit2(aChar) && aNum > 0);
      let bNum = 0;
      do {
        ++bIndex;
        bNum = bNum * 10 + bChar - DIGIT_0;
        bChar = bStr.charCodeAt(bIndex);
      } while (isDigit2(bChar) && bNum > 0);
      if (aNum < bNum) {
        return -1;
      }
      if (aNum > bNum) {
        return 1;
      }
    } else {
      if (aChar < bChar) {
        return -1;
      }
      if (aChar > bChar) {
        return 1;
      }
      ++aIndex;
      ++bIndex;
    }
  }
  return aStr.length - bStr.length;
}
function isDigit2(code) {
  return !isNaN(code) && DIGIT_0 <= code && code <= DIGIT_9;
}
var DIGIT_0, DIGIT_9;
var init_naturalCompare = __esm({
  "node_modules/graphql/jsutils/naturalCompare.mjs"() {
    DIGIT_0 = 48;
    DIGIT_9 = 57;
  }
});

// node_modules/graphql/jsutils/suggestionList.mjs
function suggestionList(input, options) {
  const optionsByDistance = /* @__PURE__ */ Object.create(null);
  const lexicalDistance = new LexicalDistance(input);
  const threshold = Math.floor(input.length * 0.4) + 1;
  for (const option of options) {
    const distance = lexicalDistance.measure(option, threshold);
    if (distance !== void 0) {
      optionsByDistance[option] = distance;
    }
  }
  return Object.keys(optionsByDistance).sort((a, b) => {
    const distanceDiff = optionsByDistance[a] - optionsByDistance[b];
    return distanceDiff !== 0 ? distanceDiff : naturalCompare(a, b);
  });
}
function stringToArray(str) {
  const strLength = str.length;
  const array = new Array(strLength);
  for (let i = 0; i < strLength; ++i) {
    array[i] = str.charCodeAt(i);
  }
  return array;
}
var LexicalDistance;
var init_suggestionList = __esm({
  "node_modules/graphql/jsutils/suggestionList.mjs"() {
    init_naturalCompare();
    LexicalDistance = class {
      constructor(input) {
        this._input = input;
        this._inputLowerCase = input.toLowerCase();
        this._inputArray = stringToArray(this._inputLowerCase);
        this._rows = [
          new Array(input.length + 1).fill(0),
          new Array(input.length + 1).fill(0),
          new Array(input.length + 1).fill(0)
        ];
      }
      measure(option, threshold) {
        if (this._input === option) {
          return 0;
        }
        const optionLowerCase = option.toLowerCase();
        if (this._inputLowerCase === optionLowerCase) {
          return 1;
        }
        let a = stringToArray(optionLowerCase);
        let b = this._inputArray;
        if (a.length < b.length) {
          const tmp = a;
          a = b;
          b = tmp;
        }
        const aLength = a.length;
        const bLength = b.length;
        if (aLength - bLength > threshold) {
          return void 0;
        }
        const rows = this._rows;
        for (let j = 0; j <= bLength; j++) {
          rows[0][j] = j;
        }
        for (let i = 1; i <= aLength; i++) {
          const upRow = rows[(i - 1) % 3];
          const currentRow = rows[i % 3];
          let smallestCell = currentRow[0] = i;
          for (let j = 1; j <= bLength; j++) {
            const cost = a[i - 1] === b[j - 1] ? 0 : 1;
            let currentCell = Math.min(
              upRow[j] + 1,
              // delete
              currentRow[j - 1] + 1,
              // insert
              upRow[j - 1] + cost
              // substitute
            );
            if (i > 1 && j > 1 && a[i - 1] === b[j - 2] && a[i - 2] === b[j - 1]) {
              const doubleDiagonalCell = rows[(i - 2) % 3][j - 2];
              currentCell = Math.min(currentCell, doubleDiagonalCell + 1);
            }
            if (currentCell < smallestCell) {
              smallestCell = currentCell;
            }
            currentRow[j] = currentCell;
          }
          if (smallestCell > threshold) {
            return void 0;
          }
        }
        const distance = rows[aLength % 3][bLength];
        return distance <= threshold ? distance : void 0;
      }
    };
  }
});

// node_modules/graphql/jsutils/toObjMap.mjs
function toObjMap(obj) {
  if (obj == null) {
    return /* @__PURE__ */ Object.create(null);
  }
  if (Object.getPrototypeOf(obj) === null) {
    return obj;
  }
  const map = /* @__PURE__ */ Object.create(null);
  for (const [key, value] of Object.entries(obj)) {
    map[key] = value;
  }
  return map;
}
var init_toObjMap = __esm({
  "node_modules/graphql/jsutils/toObjMap.mjs"() {
  }
});

// node_modules/graphql/language/printString.mjs
function printString(str) {
  return `"${str.replace(escapedRegExp, escapedReplacer)}"`;
}
function escapedReplacer(str) {
  return escapeSequences[str.charCodeAt(0)];
}
var escapedRegExp, escapeSequences;
var init_printString = __esm({
  "node_modules/graphql/language/printString.mjs"() {
    escapedRegExp = /[\x00-\x1f\x22\x5c\x7f-\x9f]/g;
    escapeSequences = [
      "\\u0000",
      "\\u0001",
      "\\u0002",
      "\\u0003",
      "\\u0004",
      "\\u0005",
      "\\u0006",
      "\\u0007",
      "\\b",
      "\\t",
      "\\n",
      "\\u000B",
      "\\f",
      "\\r",
      "\\u000E",
      "\\u000F",
      "\\u0010",
      "\\u0011",
      "\\u0012",
      "\\u0013",
      "\\u0014",
      "\\u0015",
      "\\u0016",
      "\\u0017",
      "\\u0018",
      "\\u0019",
      "\\u001A",
      "\\u001B",
      "\\u001C",
      "\\u001D",
      "\\u001E",
      "\\u001F",
      "",
      "",
      '\\"',
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      // 2F
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      // 3F
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      // 4F
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "\\\\",
      "",
      "",
      "",
      // 5F
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      // 6F
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "",
      "\\u007F",
      "\\u0080",
      "\\u0081",
      "\\u0082",
      "\\u0083",
      "\\u0084",
      "\\u0085",
      "\\u0086",
      "\\u0087",
      "\\u0088",
      "\\u0089",
      "\\u008A",
      "\\u008B",
      "\\u008C",
      "\\u008D",
      "\\u008E",
      "\\u008F",
      "\\u0090",
      "\\u0091",
      "\\u0092",
      "\\u0093",
      "\\u0094",
      "\\u0095",
      "\\u0096",
      "\\u0097",
      "\\u0098",
      "\\u0099",
      "\\u009A",
      "\\u009B",
      "\\u009C",
      "\\u009D",
      "\\u009E",
      "\\u009F"
    ];
  }
});

// node_modules/graphql/language/visitor.mjs
function visit(root, visitor, visitorKeys = QueryDocumentKeys) {
  const enterLeaveMap = /* @__PURE__ */ new Map();
  for (const kind of Object.values(Kind)) {
    enterLeaveMap.set(kind, getEnterLeaveForKind(visitor, kind));
  }
  let stack = void 0;
  let inArray = Array.isArray(root);
  let keys = [root];
  let index = -1;
  let edits = [];
  let node = root;
  let key = void 0;
  let parent = void 0;
  const path = [];
  const ancestors = [];
  do {
    index++;
    const isLeaving = index === keys.length;
    const isEdited = isLeaving && edits.length !== 0;
    if (isLeaving) {
      key = ancestors.length === 0 ? void 0 : path[path.length - 1];
      node = parent;
      parent = ancestors.pop();
      if (isEdited) {
        if (inArray) {
          node = node.slice();
          let editOffset = 0;
          for (const [editKey, editValue] of edits) {
            const arrayKey = editKey - editOffset;
            if (editValue === null) {
              node.splice(arrayKey, 1);
              editOffset++;
            } else {
              node[arrayKey] = editValue;
            }
          }
        } else {
          node = Object.defineProperties(
            {},
            Object.getOwnPropertyDescriptors(node)
          );
          for (const [editKey, editValue] of edits) {
            node[editKey] = editValue;
          }
        }
      }
      index = stack.index;
      keys = stack.keys;
      edits = stack.edits;
      inArray = stack.inArray;
      stack = stack.prev;
    } else if (parent) {
      key = inArray ? index : keys[index];
      node = parent[key];
      if (node === null || node === void 0) {
        continue;
      }
      path.push(key);
    }
    let result;
    if (!Array.isArray(node)) {
      var _enterLeaveMap$get, _enterLeaveMap$get2;
      isNode(node) || devAssert(false, `Invalid AST Node: ${inspect(node)}.`);
      const visitFn = isLeaving ? (_enterLeaveMap$get = enterLeaveMap.get(node.kind)) === null || _enterLeaveMap$get === void 0 ? void 0 : _enterLeaveMap$get.leave : (_enterLeaveMap$get2 = enterLeaveMap.get(node.kind)) === null || _enterLeaveMap$get2 === void 0 ? void 0 : _enterLeaveMap$get2.enter;
      result = visitFn === null || visitFn === void 0 ? void 0 : visitFn.call(visitor, node, key, parent, path, ancestors);
      if (result === BREAK) {
        break;
      }
      if (result === false) {
        if (!isLeaving) {
          path.pop();
          continue;
        }
      } else if (result !== void 0) {
        edits.push([key, result]);
        if (!isLeaving) {
          if (isNode(result)) {
            node = result;
          } else {
            path.pop();
            continue;
          }
        }
      }
    }
    if (result === void 0 && isEdited) {
      edits.push([key, node]);
    }
    if (isLeaving) {
      path.pop();
    } else {
      var _node$kind;
      stack = {
        inArray,
        index,
        keys,
        edits,
        prev: stack
      };
      inArray = Array.isArray(node);
      keys = inArray ? node : (_node$kind = visitorKeys[node.kind]) !== null && _node$kind !== void 0 ? _node$kind : [];
      index = -1;
      edits = [];
      if (parent) {
        ancestors.push(parent);
      }
      parent = node;
    }
  } while (stack !== void 0);
  if (edits.length !== 0) {
    return edits[edits.length - 1][1];
  }
  return root;
}
function visitInParallel(visitors) {
  const skipping = new Array(visitors.length).fill(null);
  const mergedVisitor = /* @__PURE__ */ Object.create(null);
  for (const kind of Object.values(Kind)) {
    let hasVisitor = false;
    const enterList = new Array(visitors.length).fill(void 0);
    const leaveList = new Array(visitors.length).fill(void 0);
    for (let i = 0; i < visitors.length; ++i) {
      const { enter, leave } = getEnterLeaveForKind(visitors[i], kind);
      hasVisitor || (hasVisitor = enter != null || leave != null);
      enterList[i] = enter;
      leaveList[i] = leave;
    }
    if (!hasVisitor) {
      continue;
    }
    const mergedEnterLeave = {
      enter(...args) {
        const node = args[0];
        for (let i = 0; i < visitors.length; i++) {
          if (skipping[i] === null) {
            var _enterList$i;
            const result = (_enterList$i = enterList[i]) === null || _enterList$i === void 0 ? void 0 : _enterList$i.apply(visitors[i], args);
            if (result === false) {
              skipping[i] = node;
            } else if (result === BREAK) {
              skipping[i] = BREAK;
            } else if (result !== void 0) {
              return result;
            }
          }
        }
      },
      leave(...args) {
        const node = args[0];
        for (let i = 0; i < visitors.length; i++) {
          if (skipping[i] === null) {
            var _leaveList$i;
            const result = (_leaveList$i = leaveList[i]) === null || _leaveList$i === void 0 ? void 0 : _leaveList$i.apply(visitors[i], args);
            if (result === BREAK) {
              skipping[i] = BREAK;
            } else if (result !== void 0 && result !== false) {
              return result;
            }
          } else if (skipping[i] === node) {
            skipping[i] = null;
          }
        }
      }
    };
    mergedVisitor[kind] = mergedEnterLeave;
  }
  return mergedVisitor;
}
function getEnterLeaveForKind(visitor, kind) {
  const kindVisitor = visitor[kind];
  if (typeof kindVisitor === "object") {
    return kindVisitor;
  } else if (typeof kindVisitor === "function") {
    return {
      enter: kindVisitor,
      leave: void 0
    };
  }
  return {
    enter: visitor.enter,
    leave: visitor.leave
  };
}
function getVisitFn(visitor, kind, isLeaving) {
  const { enter, leave } = getEnterLeaveForKind(visitor, kind);
  return isLeaving ? leave : enter;
}
var BREAK;
var init_visitor = __esm({
  "node_modules/graphql/language/visitor.mjs"() {
    init_devAssert();
    init_inspect();
    init_ast();
    init_kinds();
    BREAK = Object.freeze({});
  }
});

// node_modules/graphql/language/printer.mjs
function print(ast) {
  return visit(ast, printDocASTReducer);
}
function join(maybeArray, separator = "") {
  var _maybeArray$filter$jo;
  return (_maybeArray$filter$jo = maybeArray === null || maybeArray === void 0 ? void 0 : maybeArray.filter((x) => x).join(separator)) !== null && _maybeArray$filter$jo !== void 0 ? _maybeArray$filter$jo : "";
}
function block(array) {
  return wrap("{\n", indent2(join(array, "\n")), "\n}");
}
function wrap(start, maybeString, end = "") {
  return maybeString != null && maybeString !== "" ? start + maybeString + end : "";
}
function indent2(str) {
  return wrap("  ", str.replace(/\n/g, "\n  "));
}
function hasMultilineItems(maybeArray) {
  var _maybeArray$some;
  return (_maybeArray$some = maybeArray === null || maybeArray === void 0 ? void 0 : maybeArray.some((str) => str.includes("\n"))) !== null && _maybeArray$some !== void 0 ? _maybeArray$some : false;
}
var MAX_LINE_LENGTH, printDocASTReducer;
var init_printer = __esm({
  "node_modules/graphql/language/printer.mjs"() {
    init_blockString();
    init_printString();
    init_visitor();
    MAX_LINE_LENGTH = 80;
    printDocASTReducer = {
      Name: {
        leave: (node) => node.value
      },
      Variable: {
        leave: (node) => "$" + node.name
      },
      // Document
      Document: {
        leave: (node) => join(node.definitions, "\n\n")
      },
      OperationDefinition: {
        leave(node) {
          const varDefs = wrap("(", join(node.variableDefinitions, ", "), ")");
          const prefix = join(
            [
              node.operation,
              join([node.name, varDefs]),
              join(node.directives, " ")
            ],
            " "
          );
          return (prefix === "query" ? "" : prefix + " ") + node.selectionSet;
        }
      },
      VariableDefinition: {
        leave: ({ variable, type, defaultValue, directives }) => variable + ": " + type + wrap(" = ", defaultValue) + wrap(" ", join(directives, " "))
      },
      SelectionSet: {
        leave: ({ selections }) => block(selections)
      },
      Field: {
        leave({ alias, name, arguments: args, directives, selectionSet }) {
          const prefix = wrap("", alias, ": ") + name;
          let argsLine = prefix + wrap("(", join(args, ", "), ")");
          if (argsLine.length > MAX_LINE_LENGTH) {
            argsLine = prefix + wrap("(\n", indent2(join(args, "\n")), "\n)");
          }
          return join([argsLine, join(directives, " "), selectionSet], " ");
        }
      },
      Argument: {
        leave: ({ name, value }) => name + ": " + value
      },
      // Fragments
      FragmentSpread: {
        leave: ({ name, directives }) => "..." + name + wrap(" ", join(directives, " "))
      },
      InlineFragment: {
        leave: ({ typeCondition, directives, selectionSet }) => join(
          [
            "...",
            wrap("on ", typeCondition),
            join(directives, " "),
            selectionSet
          ],
          " "
        )
      },
      FragmentDefinition: {
        leave: ({ name, typeCondition, variableDefinitions, directives, selectionSet }) => (
          // or removed in the future.
          `fragment ${name}${wrap("(", join(variableDefinitions, ", "), ")")} on ${typeCondition} ${wrap("", join(directives, " "), " ")}` + selectionSet
        )
      },
      // Value
      IntValue: {
        leave: ({ value }) => value
      },
      FloatValue: {
        leave: ({ value }) => value
      },
      StringValue: {
        leave: ({ value, block: isBlockString }) => isBlockString ? printBlockString(value) : printString(value)
      },
      BooleanValue: {
        leave: ({ value }) => value ? "true" : "false"
      },
      NullValue: {
        leave: () => "null"
      },
      EnumValue: {
        leave: ({ value }) => value
      },
      ListValue: {
        leave: ({ values }) => "[" + join(values, ", ") + "]"
      },
      ObjectValue: {
        leave: ({ fields }) => "{" + join(fields, ", ") + "}"
      },
      ObjectField: {
        leave: ({ name, value }) => name + ": " + value
      },
      // Directive
      Directive: {
        leave: ({ name, arguments: args }) => "@" + name + wrap("(", join(args, ", "), ")")
      },
      // Type
      NamedType: {
        leave: ({ name }) => name
      },
      ListType: {
        leave: ({ type }) => "[" + type + "]"
      },
      NonNullType: {
        leave: ({ type }) => type + "!"
      },
      // Type System Definitions
      SchemaDefinition: {
        leave: ({ description, directives, operationTypes }) => wrap("", description, "\n") + join(["schema", join(directives, " "), block(operationTypes)], " ")
      },
      OperationTypeDefinition: {
        leave: ({ operation, type }) => operation + ": " + type
      },
      ScalarTypeDefinition: {
        leave: ({ description, name, directives }) => wrap("", description, "\n") + join(["scalar", name, join(directives, " ")], " ")
      },
      ObjectTypeDefinition: {
        leave: ({ description, name, interfaces, directives, fields }) => wrap("", description, "\n") + join(
          [
            "type",
            name,
            wrap("implements ", join(interfaces, " & ")),
            join(directives, " "),
            block(fields)
          ],
          " "
        )
      },
      FieldDefinition: {
        leave: ({ description, name, arguments: args, type, directives }) => wrap("", description, "\n") + name + (hasMultilineItems(args) ? wrap("(\n", indent2(join(args, "\n")), "\n)") : wrap("(", join(args, ", "), ")")) + ": " + type + wrap(" ", join(directives, " "))
      },
      InputValueDefinition: {
        leave: ({ description, name, type, defaultValue, directives }) => wrap("", description, "\n") + join(
          [name + ": " + type, wrap("= ", defaultValue), join(directives, " ")],
          " "
        )
      },
      InterfaceTypeDefinition: {
        leave: ({ description, name, interfaces, directives, fields }) => wrap("", description, "\n") + join(
          [
            "interface",
            name,
            wrap("implements ", join(interfaces, " & ")),
            join(directives, " "),
            block(fields)
          ],
          " "
        )
      },
      UnionTypeDefinition: {
        leave: ({ description, name, directives, types }) => wrap("", description, "\n") + join(
          ["union", name, join(directives, " "), wrap("= ", join(types, " | "))],
          " "
        )
      },
      EnumTypeDefinition: {
        leave: ({ description, name, directives, values }) => wrap("", description, "\n") + join(["enum", name, join(directives, " "), block(values)], " ")
      },
      EnumValueDefinition: {
        leave: ({ description, name, directives }) => wrap("", description, "\n") + join([name, join(directives, " ")], " ")
      },
      InputObjectTypeDefinition: {
        leave: ({ description, name, directives, fields }) => wrap("", description, "\n") + join(["input", name, join(directives, " "), block(fields)], " ")
      },
      DirectiveDefinition: {
        leave: ({ description, name, arguments: args, repeatable, locations }) => wrap("", description, "\n") + "directive @" + name + (hasMultilineItems(args) ? wrap("(\n", indent2(join(args, "\n")), "\n)") : wrap("(", join(args, ", "), ")")) + (repeatable ? " repeatable" : "") + " on " + join(locations, " | ")
      },
      SchemaExtension: {
        leave: ({ directives, operationTypes }) => join(
          ["extend schema", join(directives, " "), block(operationTypes)],
          " "
        )
      },
      ScalarTypeExtension: {
        leave: ({ name, directives }) => join(["extend scalar", name, join(directives, " ")], " ")
      },
      ObjectTypeExtension: {
        leave: ({ name, interfaces, directives, fields }) => join(
          [
            "extend type",
            name,
            wrap("implements ", join(interfaces, " & ")),
            join(directives, " "),
            block(fields)
          ],
          " "
        )
      },
      InterfaceTypeExtension: {
        leave: ({ name, interfaces, directives, fields }) => join(
          [
            "extend interface",
            name,
            wrap("implements ", join(interfaces, " & ")),
            join(directives, " "),
            block(fields)
          ],
          " "
        )
      },
      UnionTypeExtension: {
        leave: ({ name, directives, types }) => join(
          [
            "extend union",
            name,
            join(directives, " "),
            wrap("= ", join(types, " | "))
          ],
          " "
        )
      },
      EnumTypeExtension: {
        leave: ({ name, directives, values }) => join(["extend enum", name, join(directives, " "), block(values)], " ")
      },
      InputObjectTypeExtension: {
        leave: ({ name, directives, fields }) => join(["extend input", name, join(directives, " "), block(fields)], " ")
      }
    };
  }
});

// node_modules/graphql/utilities/valueFromASTUntyped.mjs
function valueFromASTUntyped(valueNode, variables) {
  switch (valueNode.kind) {
    case Kind.NULL:
      return null;
    case Kind.INT:
      return parseInt(valueNode.value, 10);
    case Kind.FLOAT:
      return parseFloat(valueNode.value);
    case Kind.STRING:
    case Kind.ENUM:
    case Kind.BOOLEAN:
      return valueNode.value;
    case Kind.LIST:
      return valueNode.values.map(
        (node) => valueFromASTUntyped(node, variables)
      );
    case Kind.OBJECT:
      return keyValMap(
        valueNode.fields,
        (field) => field.name.value,
        (field) => valueFromASTUntyped(field.value, variables)
      );
    case Kind.VARIABLE:
      return variables === null || variables === void 0 ? void 0 : variables[valueNode.name.value];
  }
}
var init_valueFromASTUntyped = __esm({
  "node_modules/graphql/utilities/valueFromASTUntyped.mjs"() {
    init_keyValMap();
    init_kinds();
  }
});

// node_modules/graphql/type/assertName.mjs
function assertName(name) {
  name != null || devAssert(false, "Must provide name.");
  typeof name === "string" || devAssert(false, "Expected name to be a string.");
  if (name.length === 0) {
    throw new GraphQLError("Expected name to be a non-empty string.");
  }
  for (let i = 1; i < name.length; ++i) {
    if (!isNameContinue(name.charCodeAt(i))) {
      throw new GraphQLError(
        `Names must only contain [_a-zA-Z0-9] but "${name}" does not.`
      );
    }
  }
  if (!isNameStart(name.charCodeAt(0))) {
    throw new GraphQLError(
      `Names must start with [_a-zA-Z] but "${name}" does not.`
    );
  }
  return name;
}
function assertEnumValueName(name) {
  if (name === "true" || name === "false" || name === "null") {
    throw new GraphQLError(`Enum values cannot be named: ${name}`);
  }
  return assertName(name);
}
var init_assertName = __esm({
  "node_modules/graphql/type/assertName.mjs"() {
    init_devAssert();
    init_GraphQLError();
    init_characterClasses();
  }
});

// node_modules/graphql/type/definition.mjs
function isType(type) {
  return isScalarType(type) || isObjectType(type) || isInterfaceType(type) || isUnionType(type) || isEnumType(type) || isInputObjectType(type) || isListType(type) || isNonNullType(type);
}
function assertType(type) {
  if (!isType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL type.`);
  }
  return type;
}
function isScalarType(type) {
  return instanceOf(type, GraphQLScalarType);
}
function assertScalarType(type) {
  if (!isScalarType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL Scalar type.`);
  }
  return type;
}
function isObjectType(type) {
  return instanceOf(type, GraphQLObjectType);
}
function assertObjectType(type) {
  if (!isObjectType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL Object type.`);
  }
  return type;
}
function isInterfaceType(type) {
  return instanceOf(type, GraphQLInterfaceType);
}
function assertInterfaceType(type) {
  if (!isInterfaceType(type)) {
    throw new Error(
      `Expected ${inspect(type)} to be a GraphQL Interface type.`
    );
  }
  return type;
}
function isUnionType(type) {
  return instanceOf(type, GraphQLUnionType);
}
function assertUnionType(type) {
  if (!isUnionType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL Union type.`);
  }
  return type;
}
function isEnumType(type) {
  return instanceOf(type, GraphQLEnumType);
}
function assertEnumType(type) {
  if (!isEnumType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL Enum type.`);
  }
  return type;
}
function isInputObjectType(type) {
  return instanceOf(type, GraphQLInputObjectType);
}
function assertInputObjectType(type) {
  if (!isInputObjectType(type)) {
    throw new Error(
      `Expected ${inspect(type)} to be a GraphQL Input Object type.`
    );
  }
  return type;
}
function isListType(type) {
  return instanceOf(type, GraphQLList);
}
function assertListType(type) {
  if (!isListType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL List type.`);
  }
  return type;
}
function isNonNullType(type) {
  return instanceOf(type, GraphQLNonNull);
}
function assertNonNullType(type) {
  if (!isNonNullType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL Non-Null type.`);
  }
  return type;
}
function isInputType(type) {
  return isScalarType(type) || isEnumType(type) || isInputObjectType(type) || isWrappingType(type) && isInputType(type.ofType);
}
function assertInputType(type) {
  if (!isInputType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL input type.`);
  }
  return type;
}
function isOutputType(type) {
  return isScalarType(type) || isObjectType(type) || isInterfaceType(type) || isUnionType(type) || isEnumType(type) || isWrappingType(type) && isOutputType(type.ofType);
}
function assertOutputType(type) {
  if (!isOutputType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL output type.`);
  }
  return type;
}
function isLeafType(type) {
  return isScalarType(type) || isEnumType(type);
}
function assertLeafType(type) {
  if (!isLeafType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL leaf type.`);
  }
  return type;
}
function isCompositeType(type) {
  return isObjectType(type) || isInterfaceType(type) || isUnionType(type);
}
function assertCompositeType(type) {
  if (!isCompositeType(type)) {
    throw new Error(
      `Expected ${inspect(type)} to be a GraphQL composite type.`
    );
  }
  return type;
}
function isAbstractType(type) {
  return isInterfaceType(type) || isUnionType(type);
}
function assertAbstractType(type) {
  if (!isAbstractType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL abstract type.`);
  }
  return type;
}
function isWrappingType(type) {
  return isListType(type) || isNonNullType(type);
}
function assertWrappingType(type) {
  if (!isWrappingType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL wrapping type.`);
  }
  return type;
}
function isNullableType(type) {
  return isType(type) && !isNonNullType(type);
}
function assertNullableType(type) {
  if (!isNullableType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL nullable type.`);
  }
  return type;
}
function getNullableType(type) {
  if (type) {
    return isNonNullType(type) ? type.ofType : type;
  }
}
function isNamedType(type) {
  return isScalarType(type) || isObjectType(type) || isInterfaceType(type) || isUnionType(type) || isEnumType(type) || isInputObjectType(type);
}
function assertNamedType(type) {
  if (!isNamedType(type)) {
    throw new Error(`Expected ${inspect(type)} to be a GraphQL named type.`);
  }
  return type;
}
function getNamedType(type) {
  if (type) {
    let unwrappedType = type;
    while (isWrappingType(unwrappedType)) {
      unwrappedType = unwrappedType.ofType;
    }
    return unwrappedType;
  }
}
function resolveReadonlyArrayThunk(thunk) {
  return typeof thunk === "function" ? thunk() : thunk;
}
function resolveObjMapThunk(thunk) {
  return typeof thunk === "function" ? thunk() : thunk;
}
function defineInterfaces(config) {
  var _config$interfaces;
  const interfaces = resolveReadonlyArrayThunk(
    (_config$interfaces = config.interfaces) !== null && _config$interfaces !== void 0 ? _config$interfaces : []
  );
  Array.isArray(interfaces) || devAssert(
    false,
    `${config.name} interfaces must be an Array or a function which returns an Array.`
  );
  return interfaces;
}
function defineFieldMap(config) {
  const fieldMap = resolveObjMapThunk(config.fields);
  isPlainObj(fieldMap) || devAssert(
    false,
    `${config.name} fields must be an object with field names as keys or a function which returns such an object.`
  );
  return mapValue(fieldMap, (fieldConfig, fieldName) => {
    var _fieldConfig$args;
    isPlainObj(fieldConfig) || devAssert(
      false,
      `${config.name}.${fieldName} field config must be an object.`
    );
    fieldConfig.resolve == null || typeof fieldConfig.resolve === "function" || devAssert(
      false,
      `${config.name}.${fieldName} field resolver must be a function if provided, but got: ${inspect(fieldConfig.resolve)}.`
    );
    const argsConfig = (_fieldConfig$args = fieldConfig.args) !== null && _fieldConfig$args !== void 0 ? _fieldConfig$args : {};
    isPlainObj(argsConfig) || devAssert(
      false,
      `${config.name}.${fieldName} args must be an object with argument names as keys.`
    );
    return {
      name: assertName(fieldName),
      description: fieldConfig.description,
      type: fieldConfig.type,
      args: defineArguments(argsConfig),
      resolve: fieldConfig.resolve,
      subscribe: fieldConfig.subscribe,
      deprecationReason: fieldConfig.deprecationReason,
      extensions: toObjMap(fieldConfig.extensions),
      astNode: fieldConfig.astNode
    };
  });
}
function defineArguments(config) {
  return Object.entries(config).map(([argName, argConfig]) => ({
    name: assertName(argName),
    description: argConfig.description,
    type: argConfig.type,
    defaultValue: argConfig.defaultValue,
    deprecationReason: argConfig.deprecationReason,
    extensions: toObjMap(argConfig.extensions),
    astNode: argConfig.astNode
  }));
}
function isPlainObj(obj) {
  return isObjectLike(obj) && !Array.isArray(obj);
}
function fieldsToFieldsConfig(fields) {
  return mapValue(fields, (field) => ({
    description: field.description,
    type: field.type,
    args: argsToArgsConfig(field.args),
    resolve: field.resolve,
    subscribe: field.subscribe,
    deprecationReason: field.deprecationReason,
    extensions: field.extensions,
    astNode: field.astNode
  }));
}
function argsToArgsConfig(args) {
  return keyValMap(
    args,
    (arg) => arg.name,
    (arg) => ({
      description: arg.description,
      type: arg.type,
      defaultValue: arg.defaultValue,
      deprecationReason: arg.deprecationReason,
      extensions: arg.extensions,
      astNode: arg.astNode
    })
  );
}
function isRequiredArgument(arg) {
  return isNonNullType(arg.type) && arg.defaultValue === void 0;
}
function defineTypes(config) {
  const types = resolveReadonlyArrayThunk(config.types);
  Array.isArray(types) || devAssert(
    false,
    `Must provide Array of types or a function which returns such an array for Union ${config.name}.`
  );
  return types;
}
function didYouMeanEnumValue(enumType, unknownValueStr) {
  const allNames = enumType.getValues().map((value) => value.name);
  const suggestedValues = suggestionList(unknownValueStr, allNames);
  return didYouMean("the enum value", suggestedValues);
}
function defineEnumValues(typeName, valueMap) {
  isPlainObj(valueMap) || devAssert(
    false,
    `${typeName} values must be an object with value names as keys.`
  );
  return Object.entries(valueMap).map(([valueName, valueConfig]) => {
    isPlainObj(valueConfig) || devAssert(
      false,
      `${typeName}.${valueName} must refer to an object with a "value" key representing an internal value but got: ${inspect(valueConfig)}.`
    );
    return {
      name: assertEnumValueName(valueName),
      description: valueConfig.description,
      value: valueConfig.value !== void 0 ? valueConfig.value : valueName,
      deprecationReason: valueConfig.deprecationReason,
      extensions: toObjMap(valueConfig.extensions),
      astNode: valueConfig.astNode
    };
  });
}
function defineInputFieldMap(config) {
  const fieldMap = resolveObjMapThunk(config.fields);
  isPlainObj(fieldMap) || devAssert(
    false,
    `${config.name} fields must be an object with field names as keys or a function which returns such an object.`
  );
  return mapValue(fieldMap, (fieldConfig, fieldName) => {
    !("resolve" in fieldConfig) || devAssert(
      false,
      `${config.name}.${fieldName} field has a resolve property, but Input Types cannot define resolvers.`
    );
    return {
      name: assertName(fieldName),
      description: fieldConfig.description,
      type: fieldConfig.type,
      defaultValue: fieldConfig.defaultValue,
      deprecationReason: fieldConfig.deprecationReason,
      extensions: toObjMap(fieldConfig.extensions),
      astNode: fieldConfig.astNode
    };
  });
}
function isRequiredInputField(field) {
  return isNonNullType(field.type) && field.defaultValue === void 0;
}
var GraphQLList, GraphQLNonNull, GraphQLScalarType, GraphQLObjectType, GraphQLInterfaceType, GraphQLUnionType, GraphQLEnumType, GraphQLInputObjectType;
var init_definition = __esm({
  "node_modules/graphql/type/definition.mjs"() {
    init_devAssert();
    init_didYouMean();
    init_identityFunc();
    init_inspect();
    init_instanceOf();
    init_isObjectLike();
    init_keyMap();
    init_keyValMap();
    init_mapValue();
    init_suggestionList();
    init_toObjMap();
    init_GraphQLError();
    init_kinds();
    init_printer();
    init_valueFromASTUntyped();
    init_assertName();
    GraphQLList = class {
      constructor(ofType) {
        isType(ofType) || devAssert(false, `Expected ${inspect(ofType)} to be a GraphQL type.`);
        this.ofType = ofType;
      }
      get [Symbol.toStringTag]() {
        return "GraphQLList";
      }
      toString() {
        return "[" + String(this.ofType) + "]";
      }
      toJSON() {
        return this.toString();
      }
    };
    GraphQLNonNull = class {
      constructor(ofType) {
        isNullableType(ofType) || devAssert(
          false,
          `Expected ${inspect(ofType)} to be a GraphQL nullable type.`
        );
        this.ofType = ofType;
      }
      get [Symbol.toStringTag]() {
        return "GraphQLNonNull";
      }
      toString() {
        return String(this.ofType) + "!";
      }
      toJSON() {
        return this.toString();
      }
    };
    GraphQLScalarType = class {
      constructor(config) {
        var _config$parseValue, _config$serialize, _config$parseLiteral, _config$extensionASTN;
        const parseValue2 = (_config$parseValue = config.parseValue) !== null && _config$parseValue !== void 0 ? _config$parseValue : identityFunc;
        this.name = assertName(config.name);
        this.description = config.description;
        this.specifiedByURL = config.specifiedByURL;
        this.serialize = (_config$serialize = config.serialize) !== null && _config$serialize !== void 0 ? _config$serialize : identityFunc;
        this.parseValue = parseValue2;
        this.parseLiteral = (_config$parseLiteral = config.parseLiteral) !== null && _config$parseLiteral !== void 0 ? _config$parseLiteral : (node, variables) => parseValue2(valueFromASTUntyped(node, variables));
        this.extensions = toObjMap(config.extensions);
        this.astNode = config.astNode;
        this.extensionASTNodes = (_config$extensionASTN = config.extensionASTNodes) !== null && _config$extensionASTN !== void 0 ? _config$extensionASTN : [];
        config.specifiedByURL == null || typeof config.specifiedByURL === "string" || devAssert(
          false,
          `${this.name} must provide "specifiedByURL" as a string, but got: ${inspect(config.specifiedByURL)}.`
        );
        config.serialize == null || typeof config.serialize === "function" || devAssert(
          false,
          `${this.name} must provide "serialize" function. If this custom Scalar is also used as an input type, ensure "parseValue" and "parseLiteral" functions are also provided.`
        );
        if (config.parseLiteral) {
          typeof config.parseValue === "function" && typeof config.parseLiteral === "function" || devAssert(
            false,
            `${this.name} must provide both "parseValue" and "parseLiteral" functions.`
          );
        }
      }
      get [Symbol.toStringTag]() {
        return "GraphQLScalarType";
      }
      toConfig() {
        return {
          name: this.name,
          description: this.description,
          specifiedByURL: this.specifiedByURL,
          serialize: this.serialize,
          parseValue: this.parseValue,
          parseLiteral: this.parseLiteral,
          extensions: this.extensions,
          astNode: this.astNode,
          extensionASTNodes: this.extensionASTNodes
        };
      }
      toString() {
        return this.name;
      }
      toJSON() {
        return this.toString();
      }
    };
    GraphQLObjectType = class {
      constructor(config) {
        var _config$extensionASTN2;
        this.name = assertName(config.name);
        this.description = config.description;
        this.isTypeOf = config.isTypeOf;
        this.extensions = toObjMap(config.extensions);
        this.astNode = config.astNode;
        this.extensionASTNodes = (_config$extensionASTN2 = config.extensionASTNodes) !== null && _config$extensionASTN2 !== void 0 ? _config$extensionASTN2 : [];
        this._fields = () => defineFieldMap(config);
        this._interfaces = () => defineInterfaces(config);
        config.isTypeOf == null || typeof config.isTypeOf === "function" || devAssert(
          false,
          `${this.name} must provide "isTypeOf" as a function, but got: ${inspect(config.isTypeOf)}.`
        );
      }
      get [Symbol.toStringTag]() {
        return "GraphQLObjectType";
      }
      getFields() {
        if (typeof this._fields === "function") {
          this._fields = this._fields();
        }
        return this._fields;
      }
      getInterfaces() {
        if (typeof this._interfaces === "function") {
          this._interfaces = this._interfaces();
        }
        return this._interfaces;
      }
      toConfig() {
        return {
          name: this.name,
          description: this.description,
          interfaces: this.getInterfaces(),
          fields: fieldsToFieldsConfig(this.getFields()),
          isTypeOf: this.isTypeOf,
          extensions: this.extensions,
          astNode: this.astNode,
          extensionASTNodes: this.extensionASTNodes
        };
      }
      toString() {
        return this.name;
      }
      toJSON() {
        return this.toString();
      }
    };
    GraphQLInterfaceType = class {
      constructor(config) {
        var _config$extensionASTN3;
        this.name = assertName(config.name);
        this.description = config.description;
        this.resolveType = config.resolveType;
        this.extensions = toObjMap(config.extensions);
        this.astNode = config.astNode;
        this.extensionASTNodes = (_config$extensionASTN3 = config.extensionASTNodes) !== null && _config$extensionASTN3 !== void 0 ? _config$extensionASTN3 : [];
        this._fields = defineFieldMap.bind(void 0, config);
        this._interfaces = defineInterfaces.bind(void 0, config);
        config.resolveType == null || typeof config.resolveType === "function" || devAssert(
          false,
          `${this.name} must provide "resolveType" as a function, but got: ${inspect(config.resolveType)}.`
        );
      }
      get [Symbol.toStringTag]() {
        return "GraphQLInterfaceType";
      }
      getFields() {
        if (typeof this._fields === "function") {
          this._fields = this._fields();
        }
        return this._fields;
      }
      getInterfaces() {
        if (typeof this._interfaces === "function") {
          this._interfaces = this._interfaces();
        }
        return this._interfaces;
      }
      toConfig() {
        return {
          name: this.name,
          description: this.description,
          interfaces: this.getInterfaces(),
          fields: fieldsToFieldsConfig(this.getFields()),
          resolveType: this.resolveType,
          extensions: this.extensions,
          astNode: this.astNode,
          extensionASTNodes: this.extensionASTNodes
        };
      }
      toString() {
        return this.name;
      }
      toJSON() {
        return this.toString();
      }
    };
    GraphQLUnionType = class {
      constructor(config) {
        var _config$extensionASTN4;
        this.name = assertName(config.name);
        this.description = config.description;
        this.resolveType = config.resolveType;
        this.extensions = toObjMap(config.extensions);
        this.astNode = config.astNode;
        this.extensionASTNodes = (_config$extensionASTN4 = config.extensionASTNodes) !== null && _config$extensionASTN4 !== void 0 ? _config$extensionASTN4 : [];
        this._types = defineTypes.bind(void 0, config);
        config.resolveType == null || typeof config.resolveType === "function" || devAssert(
          false,
          `${this.name} must provide "resolveType" as a function, but got: ${inspect(config.resolveType)}.`
        );
      }
      get [Symbol.toStringTag]() {
        return "GraphQLUnionType";
      }
      getTypes() {
        if (typeof this._types === "function") {
          this._types = this._types();
        }
        return this._types;
      }
      toConfig() {
        return {
          name: this.name,
          description: this.description,
          types: this.getTypes(),
          resolveType: this.resolveType,
          extensions: this.extensions,
          astNode: this.astNode,
          extensionASTNodes: this.extensionASTNodes
        };
      }
      toString() {
        return this.name;
      }
      toJSON() {
        return this.toString();
      }
    };
    GraphQLEnumType = class {
      /* <T> */
      constructor(config) {
        var _config$extensionASTN5;
        this.name = assertName(config.name);
        this.description = config.description;
        this.extensions = toObjMap(config.extensions);
        this.astNode = config.astNode;
        this.extensionASTNodes = (_config$extensionASTN5 = config.extensionASTNodes) !== null && _config$extensionASTN5 !== void 0 ? _config$extensionASTN5 : [];
        this._values = typeof config.values === "function" ? config.values : defineEnumValues(this.name, config.values);
        this._valueLookup = null;
        this._nameLookup = null;
      }
      get [Symbol.toStringTag]() {
        return "GraphQLEnumType";
      }
      getValues() {
        if (typeof this._values === "function") {
          this._values = defineEnumValues(this.name, this._values());
        }
        return this._values;
      }
      getValue(name) {
        if (this._nameLookup === null) {
          this._nameLookup = keyMap(this.getValues(), (value) => value.name);
        }
        return this._nameLookup[name];
      }
      serialize(outputValue) {
        if (this._valueLookup === null) {
          this._valueLookup = new Map(
            this.getValues().map((enumValue2) => [enumValue2.value, enumValue2])
          );
        }
        const enumValue = this._valueLookup.get(outputValue);
        if (enumValue === void 0) {
          throw new GraphQLError(
            `Enum "${this.name}" cannot represent value: ${inspect(outputValue)}`
          );
        }
        return enumValue.name;
      }
      parseValue(inputValue) {
        if (typeof inputValue !== "string") {
          const valueStr = inspect(inputValue);
          throw new GraphQLError(
            `Enum "${this.name}" cannot represent non-string value: ${valueStr}.` + didYouMeanEnumValue(this, valueStr)
          );
        }
        const enumValue = this.getValue(inputValue);
        if (enumValue == null) {
          throw new GraphQLError(
            `Value "${inputValue}" does not exist in "${this.name}" enum.` + didYouMeanEnumValue(this, inputValue)
          );
        }
        return enumValue.value;
      }
      parseLiteral(valueNode, _variables) {
        if (valueNode.kind !== Kind.ENUM) {
          const valueStr = print(valueNode);
          throw new GraphQLError(
            `Enum "${this.name}" cannot represent non-enum value: ${valueStr}.` + didYouMeanEnumValue(this, valueStr),
            {
              nodes: valueNode
            }
          );
        }
        const enumValue = this.getValue(valueNode.value);
        if (enumValue == null) {
          const valueStr = print(valueNode);
          throw new GraphQLError(
            `Value "${valueStr}" does not exist in "${this.name}" enum.` + didYouMeanEnumValue(this, valueStr),
            {
              nodes: valueNode
            }
          );
        }
        return enumValue.value;
      }
      toConfig() {
        const values = keyValMap(
          this.getValues(),
          (value) => value.name,
          (value) => ({
            description: value.description,
            value: value.value,
            deprecationReason: value.deprecationReason,
            extensions: value.extensions,
            astNode: value.astNode
          })
        );
        return {
          name: this.name,
          description: this.description,
          values,
          extensions: this.extensions,
          astNode: this.astNode,
          extensionASTNodes: this.extensionASTNodes
        };
      }
      toString() {
        return this.name;
      }
      toJSON() {
        return this.toString();
      }
    };
    GraphQLInputObjectType = class {
      constructor(config) {
        var _config$extensionASTN6, _config$isOneOf;
        this.name = assertName(config.name);
        this.description = config.description;
        this.extensions = toObjMap(config.extensions);
        this.astNode = config.astNode;
        this.extensionASTNodes = (_config$extensionASTN6 = config.extensionASTNodes) !== null && _config$extensionASTN6 !== void 0 ? _config$extensionASTN6 : [];
        this.isOneOf = (_config$isOneOf = config.isOneOf) !== null && _config$isOneOf !== void 0 ? _config$isOneOf : false;
        this._fields = defineInputFieldMap.bind(void 0, config);
      }
      get [Symbol.toStringTag]() {
        return "GraphQLInputObjectType";
      }
      getFields() {
        if (typeof this._fields === "function") {
          this._fields = this._fields();
        }
        return this._fields;
      }
      toConfig() {
        const fields = mapValue(this.getFields(), (field) => ({
          description: field.description,
          type: field.type,
          defaultValue: field.defaultValue,
          deprecationReason: field.deprecationReason,
          extensions: field.extensions,
          astNode: field.astNode
        }));
        return {
          name: this.name,
          description: this.description,
          fields,
          extensions: this.extensions,
          astNode: this.astNode,
          extensionASTNodes: this.extensionASTNodes,
          isOneOf: this.isOneOf
        };
      }
      toString() {
        return this.name;
      }
      toJSON() {
        return this.toString();
      }
    };
  }
});

// node_modules/graphql/utilities/typeComparators.mjs
function isEqualType(typeA, typeB) {
  if (typeA === typeB) {
    return true;
  }
  if (isNonNullType(typeA) && isNonNullType(typeB)) {
    return isEqualType(typeA.ofType, typeB.ofType);
  }
  if (isListType(typeA) && isListType(typeB)) {
    return isEqualType(typeA.ofType, typeB.ofType);
  }
  return false;
}
function isTypeSubTypeOf(schema, maybeSubType, superType) {
  if (maybeSubType === superType) {
    return true;
  }
  if (isNonNullType(superType)) {
    if (isNonNullType(maybeSubType)) {
      return isTypeSubTypeOf(schema, maybeSubType.ofType, superType.ofType);
    }
    return false;
  }
  if (isNonNullType(maybeSubType)) {
    return isTypeSubTypeOf(schema, maybeSubType.ofType, superType);
  }
  if (isListType(superType)) {
    if (isListType(maybeSubType)) {
      return isTypeSubTypeOf(schema, maybeSubType.ofType, superType.ofType);
    }
    return false;
  }
  if (isListType(maybeSubType)) {
    return false;
  }
  return isAbstractType(superType) && (isInterfaceType(maybeSubType) || isObjectType(maybeSubType)) && schema.isSubType(superType, maybeSubType);
}
function doTypesOverlap(schema, typeA, typeB) {
  if (typeA === typeB) {
    return true;
  }
  if (isAbstractType(typeA)) {
    if (isAbstractType(typeB)) {
      return schema.getPossibleTypes(typeA).some((type) => schema.isSubType(typeB, type));
    }
    return schema.isSubType(typeA, typeB);
  }
  if (isAbstractType(typeB)) {
    return schema.isSubType(typeB, typeA);
  }
  return false;
}
var init_typeComparators = __esm({
  "node_modules/graphql/utilities/typeComparators.mjs"() {
    init_definition();
  }
});

// node_modules/graphql/type/scalars.mjs
function isSpecifiedScalarType(type) {
  return specifiedScalarTypes.some(({ name }) => type.name === name);
}
function serializeObject(outputValue) {
  if (isObjectLike(outputValue)) {
    if (typeof outputValue.valueOf === "function") {
      const valueOfResult = outputValue.valueOf();
      if (!isObjectLike(valueOfResult)) {
        return valueOfResult;
      }
    }
    if (typeof outputValue.toJSON === "function") {
      return outputValue.toJSON();
    }
  }
  return outputValue;
}
var GRAPHQL_MAX_INT, GRAPHQL_MIN_INT, GraphQLInt, GraphQLFloat, GraphQLString, GraphQLBoolean, GraphQLID, specifiedScalarTypes;
var init_scalars = __esm({
  "node_modules/graphql/type/scalars.mjs"() {
    init_inspect();
    init_isObjectLike();
    init_GraphQLError();
    init_kinds();
    init_printer();
    init_definition();
    GRAPHQL_MAX_INT = 2147483647;
    GRAPHQL_MIN_INT = -2147483648;
    GraphQLInt = new GraphQLScalarType({
      name: "Int",
      description: "The `Int` scalar type represents non-fractional signed whole numeric values. Int can represent values between -(2^31) and 2^31 - 1.",
      serialize(outputValue) {
        const coercedValue = serializeObject(outputValue);
        if (typeof coercedValue === "boolean") {
          return coercedValue ? 1 : 0;
        }
        let num = coercedValue;
        if (typeof coercedValue === "string" && coercedValue !== "") {
          num = Number(coercedValue);
        }
        if (typeof num !== "number" || !Number.isInteger(num)) {
          throw new GraphQLError(
            `Int cannot represent non-integer value: ${inspect(coercedValue)}`
          );
        }
        if (num > GRAPHQL_MAX_INT || num < GRAPHQL_MIN_INT) {
          throw new GraphQLError(
            "Int cannot represent non 32-bit signed integer value: " + inspect(coercedValue)
          );
        }
        return num;
      },
      parseValue(inputValue) {
        if (typeof inputValue !== "number" || !Number.isInteger(inputValue)) {
          throw new GraphQLError(
            `Int cannot represent non-integer value: ${inspect(inputValue)}`
          );
        }
        if (inputValue > GRAPHQL_MAX_INT || inputValue < GRAPHQL_MIN_INT) {
          throw new GraphQLError(
            `Int cannot represent non 32-bit signed integer value: ${inputValue}`
          );
        }
        return inputValue;
      },
      parseLiteral(valueNode) {
        if (valueNode.kind !== Kind.INT) {
          throw new GraphQLError(
            `Int cannot represent non-integer value: ${print(valueNode)}`,
            {
              nodes: valueNode
            }
          );
        }
        const num = parseInt(valueNode.value, 10);
        if (num > GRAPHQL_MAX_INT || num < GRAPHQL_MIN_INT) {
          throw new GraphQLError(
            `Int cannot represent non 32-bit signed integer value: ${valueNode.value}`,
            {
              nodes: valueNode
            }
          );
        }
        return num;
      }
    });
    GraphQLFloat = new GraphQLScalarType({
      name: "Float",
      description: "The `Float` scalar type represents signed double-precision fractional values as specified by [IEEE 754](https://en.wikipedia.org/wiki/IEEE_floating_point).",
      serialize(outputValue) {
        const coercedValue = serializeObject(outputValue);
        if (typeof coercedValue === "boolean") {
          return coercedValue ? 1 : 0;
        }
        let num = coercedValue;
        if (typeof coercedValue === "string" && coercedValue !== "") {
          num = Number(coercedValue);
        }
        if (typeof num !== "number" || !Number.isFinite(num)) {
          throw new GraphQLError(
            `Float cannot represent non numeric value: ${inspect(coercedValue)}`
          );
        }
        return num;
      },
      parseValue(inputValue) {
        if (typeof inputValue !== "number" || !Number.isFinite(inputValue)) {
          throw new GraphQLError(
            `Float cannot represent non numeric value: ${inspect(inputValue)}`
          );
        }
        return inputValue;
      },
      parseLiteral(valueNode) {
        if (valueNode.kind !== Kind.FLOAT && valueNode.kind !== Kind.INT) {
          throw new GraphQLError(
            `Float cannot represent non numeric value: ${print(valueNode)}`,
            valueNode
          );
        }
        return parseFloat(valueNode.value);
      }
    });
    GraphQLString = new GraphQLScalarType({
      name: "String",
      description: "The `String` scalar type represents textual data, represented as UTF-8 character sequences. The String type is most often used by GraphQL to represent free-form human-readable text.",
      serialize(outputValue) {
        const coercedValue = serializeObject(outputValue);
        if (typeof coercedValue === "string") {
          return coercedValue;
        }
        if (typeof coercedValue === "boolean") {
          return coercedValue ? "true" : "false";
        }
        if (typeof coercedValue === "number" && Number.isFinite(coercedValue)) {
          return coercedValue.toString();
        }
        throw new GraphQLError(
          `String cannot represent value: ${inspect(outputValue)}`
        );
      },
      parseValue(inputValue) {
        if (typeof inputValue !== "string") {
          throw new GraphQLError(
            `String cannot represent a non string value: ${inspect(inputValue)}`
          );
        }
        return inputValue;
      },
      parseLiteral(valueNode) {
        if (valueNode.kind !== Kind.STRING) {
          throw new GraphQLError(
            `String cannot represent a non string value: ${print(valueNode)}`,
            {
              nodes: valueNode
            }
          );
        }
        return valueNode.value;
      }
    });
    GraphQLBoolean = new GraphQLScalarType({
      name: "Boolean",
      description: "The `Boolean` scalar type represents `true` or `false`.",
      serialize(outputValue) {
        const coercedValue = serializeObject(outputValue);
        if (typeof coercedValue === "boolean") {
          return coercedValue;
        }
        if (Number.isFinite(coercedValue)) {
          return coercedValue !== 0;
        }
        throw new GraphQLError(
          `Boolean cannot represent a non boolean value: ${inspect(coercedValue)}`
        );
      },
      parseValue(inputValue) {
        if (typeof inputValue !== "boolean") {
          throw new GraphQLError(
            `Boolean cannot represent a non boolean value: ${inspect(inputValue)}`
          );
        }
        return inputValue;
      },
      parseLiteral(valueNode) {
        if (valueNode.kind !== Kind.BOOLEAN) {
          throw new GraphQLError(
            `Boolean cannot represent a non boolean value: ${print(valueNode)}`,
            {
              nodes: valueNode
            }
          );
        }
        return valueNode.value;
      }
    });
    GraphQLID = new GraphQLScalarType({
      name: "ID",
      description: 'The `ID` scalar type represents a unique identifier, often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"4"`) or integer (such as `4`) input value will be accepted as an ID.',
      serialize(outputValue) {
        const coercedValue = serializeObject(outputValue);
        if (typeof coercedValue === "string") {
          return coercedValue;
        }
        if (Number.isInteger(coercedValue)) {
          return String(coercedValue);
        }
        throw new GraphQLError(
          `ID cannot represent value: ${inspect(outputValue)}`
        );
      },
      parseValue(inputValue) {
        if (typeof inputValue === "string") {
          return inputValue;
        }
        if (typeof inputValue === "number" && Number.isInteger(inputValue)) {
          return inputValue.toString();
        }
        throw new GraphQLError(`ID cannot represent value: ${inspect(inputValue)}`);
      },
      parseLiteral(valueNode) {
        if (valueNode.kind !== Kind.STRING && valueNode.kind !== Kind.INT) {
          throw new GraphQLError(
            "ID cannot represent a non-string and non-integer value: " + print(valueNode),
            {
              nodes: valueNode
            }
          );
        }
        return valueNode.value;
      }
    });
    specifiedScalarTypes = Object.freeze([
      GraphQLString,
      GraphQLInt,
      GraphQLFloat,
      GraphQLBoolean,
      GraphQLID
    ]);
  }
});

// node_modules/graphql/type/directives.mjs
function isDirective(directive) {
  return instanceOf(directive, GraphQLDirective);
}
function assertDirective(directive) {
  if (!isDirective(directive)) {
    throw new Error(
      `Expected ${inspect(directive)} to be a GraphQL directive.`
    );
  }
  return directive;
}
function isSpecifiedDirective(directive) {
  return specifiedDirectives.some(({ name }) => name === directive.name);
}
var GraphQLDirective, GraphQLIncludeDirective, GraphQLSkipDirective, DEFAULT_DEPRECATION_REASON, GraphQLDeprecatedDirective, GraphQLSpecifiedByDirective, GraphQLOneOfDirective, specifiedDirectives;
var init_directives = __esm({
  "node_modules/graphql/type/directives.mjs"() {
    init_devAssert();
    init_inspect();
    init_instanceOf();
    init_isObjectLike();
    init_toObjMap();
    init_directiveLocation();
    init_assertName();
    init_definition();
    init_scalars();
    GraphQLDirective = class {
      constructor(config) {
        var _config$isRepeatable, _config$args;
        this.name = assertName(config.name);
        this.description = config.description;
        this.locations = config.locations;
        this.isRepeatable = (_config$isRepeatable = config.isRepeatable) !== null && _config$isRepeatable !== void 0 ? _config$isRepeatable : false;
        this.extensions = toObjMap(config.extensions);
        this.astNode = config.astNode;
        Array.isArray(config.locations) || devAssert(false, `@${config.name} locations must be an Array.`);
        const args = (_config$args = config.args) !== null && _config$args !== void 0 ? _config$args : {};
        isObjectLike(args) && !Array.isArray(args) || devAssert(
          false,
          `@${config.name} args must be an object with argument names as keys.`
        );
        this.args = defineArguments(args);
      }
      get [Symbol.toStringTag]() {
        return "GraphQLDirective";
      }
      toConfig() {
        return {
          name: this.name,
          description: this.description,
          locations: this.locations,
          args: argsToArgsConfig(this.args),
          isRepeatable: this.isRepeatable,
          extensions: this.extensions,
          astNode: this.astNode
        };
      }
      toString() {
        return "@" + this.name;
      }
      toJSON() {
        return this.toString();
      }
    };
    GraphQLIncludeDirective = new GraphQLDirective({
      name: "include",
      description: "Directs the executor to include this field or fragment only when the `if` argument is true.",
      locations: [
        DirectiveLocation.FIELD,
        DirectiveLocation.FRAGMENT_SPREAD,
        DirectiveLocation.INLINE_FRAGMENT
      ],
      args: {
        if: {
          type: new GraphQLNonNull(GraphQLBoolean),
          description: "Included when true."
        }
      }
    });
    GraphQLSkipDirective = new GraphQLDirective({
      name: "skip",
      description: "Directs the executor to skip this field or fragment when the `if` argument is true.",
      locations: [
        DirectiveLocation.FIELD,
        DirectiveLocation.FRAGMENT_SPREAD,
        DirectiveLocation.INLINE_FRAGMENT
      ],
      args: {
        if: {
          type: new GraphQLNonNull(GraphQLBoolean),
          description: "Skipped when true."
        }
      }
    });
    DEFAULT_DEPRECATION_REASON = "No longer supported";
    GraphQLDeprecatedDirective = new GraphQLDirective({
      name: "deprecated",
      description: "Marks an element of a GraphQL schema as no longer supported.",
      locations: [
        DirectiveLocation.FIELD_DEFINITION,
        DirectiveLocation.ARGUMENT_DEFINITION,
        DirectiveLocation.INPUT_FIELD_DEFINITION,
        DirectiveLocation.ENUM_VALUE
      ],
      args: {
        reason: {
          type: GraphQLString,
          description: "Explains why this element was deprecated, usually also including a suggestion for how to access supported similar data. Formatted using the Markdown syntax, as specified by [CommonMark](https://commonmark.org/).",
          defaultValue: DEFAULT_DEPRECATION_REASON
        }
      }
    });
    GraphQLSpecifiedByDirective = new GraphQLDirective({
      name: "specifiedBy",
      description: "Exposes a URL that specifies the behavior of this scalar.",
      locations: [DirectiveLocation.SCALAR],
      args: {
        url: {
          type: new GraphQLNonNull(GraphQLString),
          description: "The URL that specifies the behavior of this scalar."
        }
      }
    });
    GraphQLOneOfDirective = new GraphQLDirective({
      name: "oneOf",
      description: "Indicates exactly one field must be supplied and this field must not be `null`.",
      locations: [DirectiveLocation.INPUT_OBJECT],
      args: {}
    });
    specifiedDirectives = Object.freeze([
      GraphQLIncludeDirective,
      GraphQLSkipDirective,
      GraphQLDeprecatedDirective,
      GraphQLSpecifiedByDirective,
      GraphQLOneOfDirective
    ]);
  }
});

// node_modules/graphql/jsutils/isIterableObject.mjs
function isIterableObject(maybeIterable) {
  return typeof maybeIterable === "object" && typeof (maybeIterable === null || maybeIterable === void 0 ? void 0 : maybeIterable[Symbol.iterator]) === "function";
}
var init_isIterableObject = __esm({
  "node_modules/graphql/jsutils/isIterableObject.mjs"() {
  }
});

// node_modules/graphql/utilities/astFromValue.mjs
function astFromValue(value, type) {
  if (isNonNullType(type)) {
    const astValue = astFromValue(value, type.ofType);
    if ((astValue === null || astValue === void 0 ? void 0 : astValue.kind) === Kind.NULL) {
      return null;
    }
    return astValue;
  }
  if (value === null) {
    return {
      kind: Kind.NULL
    };
  }
  if (value === void 0) {
    return null;
  }
  if (isListType(type)) {
    const itemType = type.ofType;
    if (isIterableObject(value)) {
      const valuesNodes = [];
      for (const item of value) {
        const itemNode = astFromValue(item, itemType);
        if (itemNode != null) {
          valuesNodes.push(itemNode);
        }
      }
      return {
        kind: Kind.LIST,
        values: valuesNodes
      };
    }
    return astFromValue(value, itemType);
  }
  if (isInputObjectType(type)) {
    if (!isObjectLike(value)) {
      return null;
    }
    const fieldNodes = [];
    for (const field of Object.values(type.getFields())) {
      const fieldValue = astFromValue(value[field.name], field.type);
      if (fieldValue) {
        fieldNodes.push({
          kind: Kind.OBJECT_FIELD,
          name: {
            kind: Kind.NAME,
            value: field.name
          },
          value: fieldValue
        });
      }
    }
    return {
      kind: Kind.OBJECT,
      fields: fieldNodes
    };
  }
  if (isLeafType(type)) {
    const serialized = type.serialize(value);
    if (serialized == null) {
      return null;
    }
    if (typeof serialized === "boolean") {
      return {
        kind: Kind.BOOLEAN,
        value: serialized
      };
    }
    if (typeof serialized === "number" && Number.isFinite(serialized)) {
      const stringNum = String(serialized);
      return integerStringRegExp.test(stringNum) ? {
        kind: Kind.INT,
        value: stringNum
      } : {
        kind: Kind.FLOAT,
        value: stringNum
      };
    }
    if (typeof serialized === "string") {
      if (isEnumType(type)) {
        return {
          kind: Kind.ENUM,
          value: serialized
        };
      }
      if (type === GraphQLID && integerStringRegExp.test(serialized)) {
        return {
          kind: Kind.INT,
          value: serialized
        };
      }
      return {
        kind: Kind.STRING,
        value: serialized
      };
    }
    throw new TypeError(`Cannot convert value to AST: ${inspect(serialized)}.`);
  }
  invariant(false, "Unexpected input type: " + inspect(type));
}
var integerStringRegExp;
var init_astFromValue = __esm({
  "node_modules/graphql/utilities/astFromValue.mjs"() {
    init_inspect();
    init_invariant();
    init_isIterableObject();
    init_isObjectLike();
    init_kinds();
    init_definition();
    init_scalars();
    integerStringRegExp = /^-?(?:0|[1-9][0-9]*)$/;
  }
});

// node_modules/graphql/type/introspection.mjs
function isIntrospectionType(type) {
  return introspectionTypes.some(({ name }) => type.name === name);
}
var __Schema, __Directive, __DirectiveLocation, __Type, __Field, __InputValue, __EnumValue, TypeKind, __TypeKind, SchemaMetaFieldDef, TypeMetaFieldDef, TypeNameMetaFieldDef, introspectionTypes;
var init_introspection = __esm({
  "node_modules/graphql/type/introspection.mjs"() {
    init_inspect();
    init_invariant();
    init_directiveLocation();
    init_printer();
    init_astFromValue();
    init_definition();
    init_scalars();
    __Schema = new GraphQLObjectType({
      name: "__Schema",
      description: "A GraphQL Schema defines the capabilities of a GraphQL server. It exposes all available types and directives on the server, as well as the entry points for query, mutation, and subscription operations.",
      fields: () => ({
        description: {
          type: GraphQLString,
          resolve: (schema) => schema.description
        },
        types: {
          description: "A list of all types supported by this server.",
          type: new GraphQLNonNull(new GraphQLList(new GraphQLNonNull(__Type))),
          resolve(schema) {
            return Object.values(schema.getTypeMap());
          }
        },
        queryType: {
          description: "The type that query operations will be rooted at.",
          type: new GraphQLNonNull(__Type),
          resolve: (schema) => schema.getQueryType()
        },
        mutationType: {
          description: "If this server supports mutation, the type that mutation operations will be rooted at.",
          type: __Type,
          resolve: (schema) => schema.getMutationType()
        },
        subscriptionType: {
          description: "If this server support subscription, the type that subscription operations will be rooted at.",
          type: __Type,
          resolve: (schema) => schema.getSubscriptionType()
        },
        directives: {
          description: "A list of all directives supported by this server.",
          type: new GraphQLNonNull(
            new GraphQLList(new GraphQLNonNull(__Directive))
          ),
          resolve: (schema) => schema.getDirectives()
        }
      })
    });
    __Directive = new GraphQLObjectType({
      name: "__Directive",
      description: "A Directive provides a way to describe alternate runtime execution and type validation behavior in a GraphQL document.\n\nIn some cases, you need to provide options to alter GraphQL's execution behavior in ways field arguments will not suffice, such as conditionally including or skipping a field. Directives provide this by describing additional information to the executor.",
      fields: () => ({
        name: {
          type: new GraphQLNonNull(GraphQLString),
          resolve: (directive) => directive.name
        },
        description: {
          type: GraphQLString,
          resolve: (directive) => directive.description
        },
        isRepeatable: {
          type: new GraphQLNonNull(GraphQLBoolean),
          resolve: (directive) => directive.isRepeatable
        },
        locations: {
          type: new GraphQLNonNull(
            new GraphQLList(new GraphQLNonNull(__DirectiveLocation))
          ),
          resolve: (directive) => directive.locations
        },
        args: {
          type: new GraphQLNonNull(
            new GraphQLList(new GraphQLNonNull(__InputValue))
          ),
          args: {
            includeDeprecated: {
              type: GraphQLBoolean,
              defaultValue: false
            }
          },
          resolve(field, { includeDeprecated }) {
            return includeDeprecated ? field.args : field.args.filter((arg) => arg.deprecationReason == null);
          }
        }
      })
    });
    __DirectiveLocation = new GraphQLEnumType({
      name: "__DirectiveLocation",
      description: "A Directive can be adjacent to many parts of the GraphQL language, a __DirectiveLocation describes one such possible adjacencies.",
      values: {
        QUERY: {
          value: DirectiveLocation.QUERY,
          description: "Location adjacent to a query operation."
        },
        MUTATION: {
          value: DirectiveLocation.MUTATION,
          description: "Location adjacent to a mutation operation."
        },
        SUBSCRIPTION: {
          value: DirectiveLocation.SUBSCRIPTION,
          description: "Location adjacent to a subscription operation."
        },
        FIELD: {
          value: DirectiveLocation.FIELD,
          description: "Location adjacent to a field."
        },
        FRAGMENT_DEFINITION: {
          value: DirectiveLocation.FRAGMENT_DEFINITION,
          description: "Location adjacent to a fragment definition."
        },
        FRAGMENT_SPREAD: {
          value: DirectiveLocation.FRAGMENT_SPREAD,
          description: "Location adjacent to a fragment spread."
        },
        INLINE_FRAGMENT: {
          value: DirectiveLocation.INLINE_FRAGMENT,
          description: "Location adjacent to an inline fragment."
        },
        VARIABLE_DEFINITION: {
          value: DirectiveLocation.VARIABLE_DEFINITION,
          description: "Location adjacent to a variable definition."
        },
        SCHEMA: {
          value: DirectiveLocation.SCHEMA,
          description: "Location adjacent to a schema definition."
        },
        SCALAR: {
          value: DirectiveLocation.SCALAR,
          description: "Location adjacent to a scalar definition."
        },
        OBJECT: {
          value: DirectiveLocation.OBJECT,
          description: "Location adjacent to an object type definition."
        },
        FIELD_DEFINITION: {
          value: DirectiveLocation.FIELD_DEFINITION,
          description: "Location adjacent to a field definition."
        },
        ARGUMENT_DEFINITION: {
          value: DirectiveLocation.ARGUMENT_DEFINITION,
          description: "Location adjacent to an argument definition."
        },
        INTERFACE: {
          value: DirectiveLocation.INTERFACE,
          description: "Location adjacent to an interface definition."
        },
        UNION: {
          value: DirectiveLocation.UNION,
          description: "Location adjacent to a union definition."
        },
        ENUM: {
          value: DirectiveLocation.ENUM,
          description: "Location adjacent to an enum definition."
        },
        ENUM_VALUE: {
          value: DirectiveLocation.ENUM_VALUE,
          description: "Location adjacent to an enum value definition."
        },
        INPUT_OBJECT: {
          value: DirectiveLocation.INPUT_OBJECT,
          description: "Location adjacent to an input object type definition."
        },
        INPUT_FIELD_DEFINITION: {
          value: DirectiveLocation.INPUT_FIELD_DEFINITION,
          description: "Location adjacent to an input object field definition."
        }
      }
    });
    __Type = new GraphQLObjectType({
      name: "__Type",
      description: "The fundamental unit of any GraphQL Schema is the type. There are many kinds of types in GraphQL as represented by the `__TypeKind` enum.\n\nDepending on the kind of a type, certain fields describe information about that type. Scalar types provide no information beyond a name, description and optional `specifiedByURL`, while Enum types provide their values. Object and Interface types provide the fields they describe. Abstract types, Union and Interface, provide the Object types possible at runtime. List and NonNull types compose other types.",
      fields: () => ({
        kind: {
          type: new GraphQLNonNull(__TypeKind),
          resolve(type) {
            if (isScalarType(type)) {
              return TypeKind.SCALAR;
            }
            if (isObjectType(type)) {
              return TypeKind.OBJECT;
            }
            if (isInterfaceType(type)) {
              return TypeKind.INTERFACE;
            }
            if (isUnionType(type)) {
              return TypeKind.UNION;
            }
            if (isEnumType(type)) {
              return TypeKind.ENUM;
            }
            if (isInputObjectType(type)) {
              return TypeKind.INPUT_OBJECT;
            }
            if (isListType(type)) {
              return TypeKind.LIST;
            }
            if (isNonNullType(type)) {
              return TypeKind.NON_NULL;
            }
            invariant(false, `Unexpected type: "${inspect(type)}".`);
          }
        },
        name: {
          type: GraphQLString,
          resolve: (type) => "name" in type ? type.name : void 0
        },
        description: {
          type: GraphQLString,
          resolve: (type) => (
            /* c8 ignore next */
            "description" in type ? type.description : void 0
          )
        },
        specifiedByURL: {
          type: GraphQLString,
          resolve: (obj) => "specifiedByURL" in obj ? obj.specifiedByURL : void 0
        },
        fields: {
          type: new GraphQLList(new GraphQLNonNull(__Field)),
          args: {
            includeDeprecated: {
              type: GraphQLBoolean,
              defaultValue: false
            }
          },
          resolve(type, { includeDeprecated }) {
            if (isObjectType(type) || isInterfaceType(type)) {
              const fields = Object.values(type.getFields());
              return includeDeprecated ? fields : fields.filter((field) => field.deprecationReason == null);
            }
          }
        },
        interfaces: {
          type: new GraphQLList(new GraphQLNonNull(__Type)),
          resolve(type) {
            if (isObjectType(type) || isInterfaceType(type)) {
              return type.getInterfaces();
            }
          }
        },
        possibleTypes: {
          type: new GraphQLList(new GraphQLNonNull(__Type)),
          resolve(type, _args, _context, { schema }) {
            if (isAbstractType(type)) {
              return schema.getPossibleTypes(type);
            }
          }
        },
        enumValues: {
          type: new GraphQLList(new GraphQLNonNull(__EnumValue)),
          args: {
            includeDeprecated: {
              type: GraphQLBoolean,
              defaultValue: false
            }
          },
          resolve(type, { includeDeprecated }) {
            if (isEnumType(type)) {
              const values = type.getValues();
              return includeDeprecated ? values : values.filter((field) => field.deprecationReason == null);
            }
          }
        },
        inputFields: {
          type: new GraphQLList(new GraphQLNonNull(__InputValue)),
          args: {
            includeDeprecated: {
              type: GraphQLBoolean,
              defaultValue: false
            }
          },
          resolve(type, { includeDeprecated }) {
            if (isInputObjectType(type)) {
              const values = Object.values(type.getFields());
              return includeDeprecated ? values : values.filter((field) => field.deprecationReason == null);
            }
          }
        },
        ofType: {
          type: __Type,
          resolve: (type) => "ofType" in type ? type.ofType : void 0
        },
        isOneOf: {
          type: GraphQLBoolean,
          resolve: (type) => {
            if (isInputObjectType(type)) {
              return type.isOneOf;
            }
          }
        }
      })
    });
    __Field = new GraphQLObjectType({
      name: "__Field",
      description: "Object and Interface types are described by a list of Fields, each of which has a name, potentially a list of arguments, and a return type.",
      fields: () => ({
        name: {
          type: new GraphQLNonNull(GraphQLString),
          resolve: (field) => field.name
        },
        description: {
          type: GraphQLString,
          resolve: (field) => field.description
        },
        args: {
          type: new GraphQLNonNull(
            new GraphQLList(new GraphQLNonNull(__InputValue))
          ),
          args: {
            includeDeprecated: {
              type: GraphQLBoolean,
              defaultValue: false
            }
          },
          resolve(field, { includeDeprecated }) {
            return includeDeprecated ? field.args : field.args.filter((arg) => arg.deprecationReason == null);
          }
        },
        type: {
          type: new GraphQLNonNull(__Type),
          resolve: (field) => field.type
        },
        isDeprecated: {
          type: new GraphQLNonNull(GraphQLBoolean),
          resolve: (field) => field.deprecationReason != null
        },
        deprecationReason: {
          type: GraphQLString,
          resolve: (field) => field.deprecationReason
        }
      })
    });
    __InputValue = new GraphQLObjectType({
      name: "__InputValue",
      description: "Arguments provided to Fields or Directives and the input fields of an InputObject are represented as Input Values which describe their type and optionally a default value.",
      fields: () => ({
        name: {
          type: new GraphQLNonNull(GraphQLString),
          resolve: (inputValue) => inputValue.name
        },
        description: {
          type: GraphQLString,
          resolve: (inputValue) => inputValue.description
        },
        type: {
          type: new GraphQLNonNull(__Type),
          resolve: (inputValue) => inputValue.type
        },
        defaultValue: {
          type: GraphQLString,
          description: "A GraphQL-formatted string representing the default value for this input value.",
          resolve(inputValue) {
            const { type, defaultValue } = inputValue;
            const valueAST = astFromValue(defaultValue, type);
            return valueAST ? print(valueAST) : null;
          }
        },
        isDeprecated: {
          type: new GraphQLNonNull(GraphQLBoolean),
          resolve: (field) => field.deprecationReason != null
        },
        deprecationReason: {
          type: GraphQLString,
          resolve: (obj) => obj.deprecationReason
        }
      })
    });
    __EnumValue = new GraphQLObjectType({
      name: "__EnumValue",
      description: "One possible value for a given Enum. Enum values are unique values, not a placeholder for a string or numeric value. However an Enum value is returned in a JSON response as a string.",
      fields: () => ({
        name: {
          type: new GraphQLNonNull(GraphQLString),
          resolve: (enumValue) => enumValue.name
        },
        description: {
          type: GraphQLString,
          resolve: (enumValue) => enumValue.description
        },
        isDeprecated: {
          type: new GraphQLNonNull(GraphQLBoolean),
          resolve: (enumValue) => enumValue.deprecationReason != null
        },
        deprecationReason: {
          type: GraphQLString,
          resolve: (enumValue) => enumValue.deprecationReason
        }
      })
    });
    (function(TypeKind2) {
      TypeKind2["SCALAR"] = "SCALAR";
      TypeKind2["OBJECT"] = "OBJECT";
      TypeKind2["INTERFACE"] = "INTERFACE";
      TypeKind2["UNION"] = "UNION";
      TypeKind2["ENUM"] = "ENUM";
      TypeKind2["INPUT_OBJECT"] = "INPUT_OBJECT";
      TypeKind2["LIST"] = "LIST";
      TypeKind2["NON_NULL"] = "NON_NULL";
    })(TypeKind || (TypeKind = {}));
    __TypeKind = new GraphQLEnumType({
      name: "__TypeKind",
      description: "An enum describing what kind of type a given `__Type` is.",
      values: {
        SCALAR: {
          value: TypeKind.SCALAR,
          description: "Indicates this type is a scalar."
        },
        OBJECT: {
          value: TypeKind.OBJECT,
          description: "Indicates this type is an object. `fields` and `interfaces` are valid fields."
        },
        INTERFACE: {
          value: TypeKind.INTERFACE,
          description: "Indicates this type is an interface. `fields`, `interfaces`, and `possibleTypes` are valid fields."
        },
        UNION: {
          value: TypeKind.UNION,
          description: "Indicates this type is a union. `possibleTypes` is a valid field."
        },
        ENUM: {
          value: TypeKind.ENUM,
          description: "Indicates this type is an enum. `enumValues` is a valid field."
        },
        INPUT_OBJECT: {
          value: TypeKind.INPUT_OBJECT,
          description: "Indicates this type is an input object. `inputFields` is a valid field."
        },
        LIST: {
          value: TypeKind.LIST,
          description: "Indicates this type is a list. `ofType` is a valid field."
        },
        NON_NULL: {
          value: TypeKind.NON_NULL,
          description: "Indicates this type is a non-null. `ofType` is a valid field."
        }
      }
    });
    SchemaMetaFieldDef = {
      name: "__schema",
      type: new GraphQLNonNull(__Schema),
      description: "Access the current type schema of this server.",
      args: [],
      resolve: (_source, _args, _context, { schema }) => schema,
      deprecationReason: void 0,
      extensions: /* @__PURE__ */ Object.create(null),
      astNode: void 0
    };
    TypeMetaFieldDef = {
      name: "__type",
      type: __Type,
      description: "Request the type information of a single type.",
      args: [
        {
          name: "name",
          description: void 0,
          type: new GraphQLNonNull(GraphQLString),
          defaultValue: void 0,
          deprecationReason: void 0,
          extensions: /* @__PURE__ */ Object.create(null),
          astNode: void 0
        }
      ],
      resolve: (_source, { name }, _context, { schema }) => schema.getType(name),
      deprecationReason: void 0,
      extensions: /* @__PURE__ */ Object.create(null),
      astNode: void 0
    };
    TypeNameMetaFieldDef = {
      name: "__typename",
      type: new GraphQLNonNull(GraphQLString),
      description: "The name of the current Object type at runtime.",
      args: [],
      resolve: (_source, _args, _context, { parentType }) => parentType.name,
      deprecationReason: void 0,
      extensions: /* @__PURE__ */ Object.create(null),
      astNode: void 0
    };
    introspectionTypes = Object.freeze([
      __Schema,
      __Directive,
      __DirectiveLocation,
      __Type,
      __Field,
      __InputValue,
      __EnumValue,
      __TypeKind
    ]);
  }
});

// node_modules/graphql/type/schema.mjs
function isSchema(schema) {
  return instanceOf(schema, GraphQLSchema);
}
function assertSchema(schema) {
  if (!isSchema(schema)) {
    throw new Error(`Expected ${inspect(schema)} to be a GraphQL schema.`);
  }
  return schema;
}
function collectReferencedTypes(type, typeSet) {
  const namedType = getNamedType(type);
  if (!typeSet.has(namedType)) {
    typeSet.add(namedType);
    if (isUnionType(namedType)) {
      for (const memberType of namedType.getTypes()) {
        collectReferencedTypes(memberType, typeSet);
      }
    } else if (isObjectType(namedType) || isInterfaceType(namedType)) {
      for (const interfaceType of namedType.getInterfaces()) {
        collectReferencedTypes(interfaceType, typeSet);
      }
      for (const field of Object.values(namedType.getFields())) {
        collectReferencedTypes(field.type, typeSet);
        for (const arg of field.args) {
          collectReferencedTypes(arg.type, typeSet);
        }
      }
    } else if (isInputObjectType(namedType)) {
      for (const field of Object.values(namedType.getFields())) {
        collectReferencedTypes(field.type, typeSet);
      }
    }
  }
  return typeSet;
}
var GraphQLSchema;
var init_schema = __esm({
  "node_modules/graphql/type/schema.mjs"() {
    init_devAssert();
    init_inspect();
    init_instanceOf();
    init_isObjectLike();
    init_toObjMap();
    init_ast();
    init_definition();
    init_directives();
    init_introspection();
    GraphQLSchema = class {
      // Used as a cache for validateSchema().
      constructor(config) {
        var _config$extensionASTN, _config$directives;
        this.__validationErrors = config.assumeValid === true ? [] : void 0;
        isObjectLike(config) || devAssert(false, "Must provide configuration object.");
        !config.types || Array.isArray(config.types) || devAssert(
          false,
          `"types" must be Array if provided but got: ${inspect(config.types)}.`
        );
        !config.directives || Array.isArray(config.directives) || devAssert(
          false,
          `"directives" must be Array if provided but got: ${inspect(config.directives)}.`
        );
        this.description = config.description;
        this.extensions = toObjMap(config.extensions);
        this.astNode = config.astNode;
        this.extensionASTNodes = (_config$extensionASTN = config.extensionASTNodes) !== null && _config$extensionASTN !== void 0 ? _config$extensionASTN : [];
        this._queryType = config.query;
        this._mutationType = config.mutation;
        this._subscriptionType = config.subscription;
        this._directives = (_config$directives = config.directives) !== null && _config$directives !== void 0 ? _config$directives : specifiedDirectives;
        const allReferencedTypes = new Set(config.types);
        if (config.types != null) {
          for (const type of config.types) {
            allReferencedTypes.delete(type);
            collectReferencedTypes(type, allReferencedTypes);
          }
        }
        if (this._queryType != null) {
          collectReferencedTypes(this._queryType, allReferencedTypes);
        }
        if (this._mutationType != null) {
          collectReferencedTypes(this._mutationType, allReferencedTypes);
        }
        if (this._subscriptionType != null) {
          collectReferencedTypes(this._subscriptionType, allReferencedTypes);
        }
        for (const directive of this._directives) {
          if (isDirective(directive)) {
            for (const arg of directive.args) {
              collectReferencedTypes(arg.type, allReferencedTypes);
            }
          }
        }
        collectReferencedTypes(__Schema, allReferencedTypes);
        this._typeMap = /* @__PURE__ */ Object.create(null);
        this._subTypeMap = /* @__PURE__ */ Object.create(null);
        this._implementationsMap = /* @__PURE__ */ Object.create(null);
        for (const namedType of allReferencedTypes) {
          if (namedType == null) {
            continue;
          }
          const typeName = namedType.name;
          typeName || devAssert(
            false,
            "One of the provided types for building the Schema is missing a name."
          );
          if (this._typeMap[typeName] !== void 0) {
            throw new Error(
              `Schema must contain uniquely named types but contains multiple types named "${typeName}".`
            );
          }
          this._typeMap[typeName] = namedType;
          if (isInterfaceType(namedType)) {
            for (const iface of namedType.getInterfaces()) {
              if (isInterfaceType(iface)) {
                let implementations = this._implementationsMap[iface.name];
                if (implementations === void 0) {
                  implementations = this._implementationsMap[iface.name] = {
                    objects: [],
                    interfaces: []
                  };
                }
                implementations.interfaces.push(namedType);
              }
            }
          } else if (isObjectType(namedType)) {
            for (const iface of namedType.getInterfaces()) {
              if (isInterfaceType(iface)) {
                let implementations = this._implementationsMap[iface.name];
                if (implementations === void 0) {
                  implementations = this._implementationsMap[iface.name] = {
                    objects: [],
                    interfaces: []
                  };
                }
                implementations.objects.push(namedType);
              }
            }
          }
        }
      }
      get [Symbol.toStringTag]() {
        return "GraphQLSchema";
      }
      getQueryType() {
        return this._queryType;
      }
      getMutationType() {
        return this._mutationType;
      }
      getSubscriptionType() {
        return this._subscriptionType;
      }
      getRootType(operation) {
        switch (operation) {
          case OperationTypeNode.QUERY:
            return this.getQueryType();
          case OperationTypeNode.MUTATION:
            return this.getMutationType();
          case OperationTypeNode.SUBSCRIPTION:
            return this.getSubscriptionType();
        }
      }
      getTypeMap() {
        return this._typeMap;
      }
      getType(name) {
        return this.getTypeMap()[name];
      }
      getPossibleTypes(abstractType) {
        return isUnionType(abstractType) ? abstractType.getTypes() : this.getImplementations(abstractType).objects;
      }
      getImplementations(interfaceType) {
        const implementations = this._implementationsMap[interfaceType.name];
        return implementations !== null && implementations !== void 0 ? implementations : {
          objects: [],
          interfaces: []
        };
      }
      isSubType(abstractType, maybeSubType) {
        let map = this._subTypeMap[abstractType.name];
        if (map === void 0) {
          map = /* @__PURE__ */ Object.create(null);
          if (isUnionType(abstractType)) {
            for (const type of abstractType.getTypes()) {
              map[type.name] = true;
            }
          } else {
            const implementations = this.getImplementations(abstractType);
            for (const type of implementations.objects) {
              map[type.name] = true;
            }
            for (const type of implementations.interfaces) {
              map[type.name] = true;
            }
          }
          this._subTypeMap[abstractType.name] = map;
        }
        return map[maybeSubType.name] !== void 0;
      }
      getDirectives() {
        return this._directives;
      }
      getDirective(name) {
        return this.getDirectives().find((directive) => directive.name === name);
      }
      toConfig() {
        return {
          description: this.description,
          query: this.getQueryType(),
          mutation: this.getMutationType(),
          subscription: this.getSubscriptionType(),
          types: Object.values(this.getTypeMap()),
          directives: this.getDirectives(),
          extensions: this.extensions,
          astNode: this.astNode,
          extensionASTNodes: this.extensionASTNodes,
          assumeValid: this.__validationErrors !== void 0
        };
      }
    };
  }
});

// node_modules/graphql/type/validate.mjs
function validateSchema(schema) {
  assertSchema(schema);
  if (schema.__validationErrors) {
    return schema.__validationErrors;
  }
  const context = new SchemaValidationContext(schema);
  validateRootTypes(context);
  validateDirectives(context);
  validateTypes(context);
  const errors = context.getErrors();
  schema.__validationErrors = errors;
  return errors;
}
function assertValidSchema(schema) {
  const errors = validateSchema(schema);
  if (errors.length !== 0) {
    throw new Error(errors.map((error) => error.message).join("\n\n"));
  }
}
function validateRootTypes(context) {
  const schema = context.schema;
  const queryType = schema.getQueryType();
  if (!queryType) {
    context.reportError("Query root type must be provided.", schema.astNode);
  } else if (!isObjectType(queryType)) {
    var _getOperationTypeNode;
    context.reportError(
      `Query root type must be Object type, it cannot be ${inspect(
        queryType
      )}.`,
      (_getOperationTypeNode = getOperationTypeNode(
        schema,
        OperationTypeNode.QUERY
      )) !== null && _getOperationTypeNode !== void 0 ? _getOperationTypeNode : queryType.astNode
    );
  }
  const mutationType = schema.getMutationType();
  if (mutationType && !isObjectType(mutationType)) {
    var _getOperationTypeNode2;
    context.reportError(
      `Mutation root type must be Object type if provided, it cannot be ${inspect(mutationType)}.`,
      (_getOperationTypeNode2 = getOperationTypeNode(
        schema,
        OperationTypeNode.MUTATION
      )) !== null && _getOperationTypeNode2 !== void 0 ? _getOperationTypeNode2 : mutationType.astNode
    );
  }
  const subscriptionType = schema.getSubscriptionType();
  if (subscriptionType && !isObjectType(subscriptionType)) {
    var _getOperationTypeNode3;
    context.reportError(
      `Subscription root type must be Object type if provided, it cannot be ${inspect(subscriptionType)}.`,
      (_getOperationTypeNode3 = getOperationTypeNode(
        schema,
        OperationTypeNode.SUBSCRIPTION
      )) !== null && _getOperationTypeNode3 !== void 0 ? _getOperationTypeNode3 : subscriptionType.astNode
    );
  }
}
function getOperationTypeNode(schema, operation) {
  var _flatMap$find;
  return (_flatMap$find = [schema.astNode, ...schema.extensionASTNodes].flatMap(
    // FIXME: https://github.com/graphql/graphql-js/issues/2203
    (schemaNode) => {
      var _schemaNode$operation;
      return (
        /* c8 ignore next */
        (_schemaNode$operation = schemaNode === null || schemaNode === void 0 ? void 0 : schemaNode.operationTypes) !== null && _schemaNode$operation !== void 0 ? _schemaNode$operation : []
      );
    }
  ).find((operationNode) => operationNode.operation === operation)) === null || _flatMap$find === void 0 ? void 0 : _flatMap$find.type;
}
function validateDirectives(context) {
  for (const directive of context.schema.getDirectives()) {
    if (!isDirective(directive)) {
      context.reportError(
        `Expected directive but got: ${inspect(directive)}.`,
        directive === null || directive === void 0 ? void 0 : directive.astNode
      );
      continue;
    }
    validateName(context, directive);
    for (const arg of directive.args) {
      validateName(context, arg);
      if (!isInputType(arg.type)) {
        context.reportError(
          `The type of @${directive.name}(${arg.name}:) must be Input Type but got: ${inspect(arg.type)}.`,
          arg.astNode
        );
      }
      if (isRequiredArgument(arg) && arg.deprecationReason != null) {
        var _arg$astNode;
        context.reportError(
          `Required argument @${directive.name}(${arg.name}:) cannot be deprecated.`,
          [
            getDeprecatedDirectiveNode(arg.astNode),
            (_arg$astNode = arg.astNode) === null || _arg$astNode === void 0 ? void 0 : _arg$astNode.type
          ]
        );
      }
    }
  }
}
function validateName(context, node) {
  if (node.name.startsWith("__")) {
    context.reportError(
      `Name "${node.name}" must not begin with "__", which is reserved by GraphQL introspection.`,
      node.astNode
    );
  }
}
function validateTypes(context) {
  const validateInputObjectCircularRefs = createInputObjectCircularRefsValidator(context);
  const typeMap = context.schema.getTypeMap();
  for (const type of Object.values(typeMap)) {
    if (!isNamedType(type)) {
      context.reportError(
        `Expected GraphQL named type but got: ${inspect(type)}.`,
        type.astNode
      );
      continue;
    }
    if (!isIntrospectionType(type)) {
      validateName(context, type);
    }
    if (isObjectType(type)) {
      validateFields(context, type);
      validateInterfaces(context, type);
    } else if (isInterfaceType(type)) {
      validateFields(context, type);
      validateInterfaces(context, type);
    } else if (isUnionType(type)) {
      validateUnionMembers(context, type);
    } else if (isEnumType(type)) {
      validateEnumValues(context, type);
    } else if (isInputObjectType(type)) {
      validateInputFields(context, type);
      validateInputObjectCircularRefs(type);
    }
  }
}
function validateFields(context, type) {
  const fields = Object.values(type.getFields());
  if (fields.length === 0) {
    context.reportError(`Type ${type.name} must define one or more fields.`, [
      type.astNode,
      ...type.extensionASTNodes
    ]);
  }
  for (const field of fields) {
    validateName(context, field);
    if (!isOutputType(field.type)) {
      var _field$astNode;
      context.reportError(
        `The type of ${type.name}.${field.name} must be Output Type but got: ${inspect(field.type)}.`,
        (_field$astNode = field.astNode) === null || _field$astNode === void 0 ? void 0 : _field$astNode.type
      );
    }
    for (const arg of field.args) {
      const argName = arg.name;
      validateName(context, arg);
      if (!isInputType(arg.type)) {
        var _arg$astNode2;
        context.reportError(
          `The type of ${type.name}.${field.name}(${argName}:) must be Input Type but got: ${inspect(arg.type)}.`,
          (_arg$astNode2 = arg.astNode) === null || _arg$astNode2 === void 0 ? void 0 : _arg$astNode2.type
        );
      }
      if (isRequiredArgument(arg) && arg.deprecationReason != null) {
        var _arg$astNode3;
        context.reportError(
          `Required argument ${type.name}.${field.name}(${argName}:) cannot be deprecated.`,
          [
            getDeprecatedDirectiveNode(arg.astNode),
            (_arg$astNode3 = arg.astNode) === null || _arg$astNode3 === void 0 ? void 0 : _arg$astNode3.type
          ]
        );
      }
    }
  }
}
function validateInterfaces(context, type) {
  const ifaceTypeNames = /* @__PURE__ */ Object.create(null);
  for (const iface of type.getInterfaces()) {
    if (!isInterfaceType(iface)) {
      context.reportError(
        `Type ${inspect(type)} must only implement Interface types, it cannot implement ${inspect(iface)}.`,
        getAllImplementsInterfaceNodes(type, iface)
      );
      continue;
    }
    if (type === iface) {
      context.reportError(
        `Type ${type.name} cannot implement itself because it would create a circular reference.`,
        getAllImplementsInterfaceNodes(type, iface)
      );
      continue;
    }
    if (ifaceTypeNames[iface.name]) {
      context.reportError(
        `Type ${type.name} can only implement ${iface.name} once.`,
        getAllImplementsInterfaceNodes(type, iface)
      );
      continue;
    }
    ifaceTypeNames[iface.name] = true;
    validateTypeImplementsAncestors(context, type, iface);
    validateTypeImplementsInterface(context, type, iface);
  }
}
function validateTypeImplementsInterface(context, type, iface) {
  const typeFieldMap = type.getFields();
  for (const ifaceField of Object.values(iface.getFields())) {
    const fieldName = ifaceField.name;
    const typeField = typeFieldMap[fieldName];
    if (!typeField) {
      context.reportError(
        `Interface field ${iface.name}.${fieldName} expected but ${type.name} does not provide it.`,
        [ifaceField.astNode, type.astNode, ...type.extensionASTNodes]
      );
      continue;
    }
    if (!isTypeSubTypeOf(context.schema, typeField.type, ifaceField.type)) {
      var _ifaceField$astNode, _typeField$astNode;
      context.reportError(
        `Interface field ${iface.name}.${fieldName} expects type ${inspect(ifaceField.type)} but ${type.name}.${fieldName} is type ${inspect(typeField.type)}.`,
        [
          (_ifaceField$astNode = ifaceField.astNode) === null || _ifaceField$astNode === void 0 ? void 0 : _ifaceField$astNode.type,
          (_typeField$astNode = typeField.astNode) === null || _typeField$astNode === void 0 ? void 0 : _typeField$astNode.type
        ]
      );
    }
    for (const ifaceArg of ifaceField.args) {
      const argName = ifaceArg.name;
      const typeArg = typeField.args.find((arg) => arg.name === argName);
      if (!typeArg) {
        context.reportError(
          `Interface field argument ${iface.name}.${fieldName}(${argName}:) expected but ${type.name}.${fieldName} does not provide it.`,
          [ifaceArg.astNode, typeField.astNode]
        );
        continue;
      }
      if (!isEqualType(ifaceArg.type, typeArg.type)) {
        var _ifaceArg$astNode, _typeArg$astNode;
        context.reportError(
          `Interface field argument ${iface.name}.${fieldName}(${argName}:) expects type ${inspect(ifaceArg.type)} but ${type.name}.${fieldName}(${argName}:) is type ${inspect(typeArg.type)}.`,
          [
            (_ifaceArg$astNode = ifaceArg.astNode) === null || _ifaceArg$astNode === void 0 ? void 0 : _ifaceArg$astNode.type,
            (_typeArg$astNode = typeArg.astNode) === null || _typeArg$astNode === void 0 ? void 0 : _typeArg$astNode.type
          ]
        );
      }
    }
    for (const typeArg of typeField.args) {
      const argName = typeArg.name;
      const ifaceArg = ifaceField.args.find((arg) => arg.name === argName);
      if (!ifaceArg && isRequiredArgument(typeArg)) {
        context.reportError(
          `Object field ${type.name}.${fieldName} includes required argument ${argName} that is missing from the Interface field ${iface.name}.${fieldName}.`,
          [typeArg.astNode, ifaceField.astNode]
        );
      }
    }
  }
}
function validateTypeImplementsAncestors(context, type, iface) {
  const ifaceInterfaces = type.getInterfaces();
  for (const transitive of iface.getInterfaces()) {
    if (!ifaceInterfaces.includes(transitive)) {
      context.reportError(
        transitive === type ? `Type ${type.name} cannot implement ${iface.name} because it would create a circular reference.` : `Type ${type.name} must implement ${transitive.name} because it is implemented by ${iface.name}.`,
        [
          ...getAllImplementsInterfaceNodes(iface, transitive),
          ...getAllImplementsInterfaceNodes(type, iface)
        ]
      );
    }
  }
}
function validateUnionMembers(context, union) {
  const memberTypes = union.getTypes();
  if (memberTypes.length === 0) {
    context.reportError(
      `Union type ${union.name} must define one or more member types.`,
      [union.astNode, ...union.extensionASTNodes]
    );
  }
  const includedTypeNames = /* @__PURE__ */ Object.create(null);
  for (const memberType of memberTypes) {
    if (includedTypeNames[memberType.name]) {
      context.reportError(
        `Union type ${union.name} can only include type ${memberType.name} once.`,
        getUnionMemberTypeNodes(union, memberType.name)
      );
      continue;
    }
    includedTypeNames[memberType.name] = true;
    if (!isObjectType(memberType)) {
      context.reportError(
        `Union type ${union.name} can only include Object types, it cannot include ${inspect(memberType)}.`,
        getUnionMemberTypeNodes(union, String(memberType))
      );
    }
  }
}
function validateEnumValues(context, enumType) {
  const enumValues = enumType.getValues();
  if (enumValues.length === 0) {
    context.reportError(
      `Enum type ${enumType.name} must define one or more values.`,
      [enumType.astNode, ...enumType.extensionASTNodes]
    );
  }
  for (const enumValue of enumValues) {
    validateName(context, enumValue);
  }
}
function validateInputFields(context, inputObj) {
  const fields = Object.values(inputObj.getFields());
  if (fields.length === 0) {
    context.reportError(
      `Input Object type ${inputObj.name} must define one or more fields.`,
      [inputObj.astNode, ...inputObj.extensionASTNodes]
    );
  }
  for (const field of fields) {
    validateName(context, field);
    if (!isInputType(field.type)) {
      var _field$astNode2;
      context.reportError(
        `The type of ${inputObj.name}.${field.name} must be Input Type but got: ${inspect(field.type)}.`,
        (_field$astNode2 = field.astNode) === null || _field$astNode2 === void 0 ? void 0 : _field$astNode2.type
      );
    }
    if (isRequiredInputField(field) && field.deprecationReason != null) {
      var _field$astNode3;
      context.reportError(
        `Required input field ${inputObj.name}.${field.name} cannot be deprecated.`,
        [
          getDeprecatedDirectiveNode(field.astNode),
          (_field$astNode3 = field.astNode) === null || _field$astNode3 === void 0 ? void 0 : _field$astNode3.type
        ]
      );
    }
    if (inputObj.isOneOf) {
      validateOneOfInputObjectField(inputObj, field, context);
    }
  }
}
function validateOneOfInputObjectField(type, field, context) {
  if (isNonNullType(field.type)) {
    var _field$astNode4;
    context.reportError(
      `OneOf input field ${type.name}.${field.name} must be nullable.`,
      (_field$astNode4 = field.astNode) === null || _field$astNode4 === void 0 ? void 0 : _field$astNode4.type
    );
  }
  if (field.defaultValue !== void 0) {
    context.reportError(
      `OneOf input field ${type.name}.${field.name} cannot have a default value.`,
      field.astNode
    );
  }
}
function createInputObjectCircularRefsValidator(context) {
  const visitedTypes = /* @__PURE__ */ Object.create(null);
  const fieldPath = [];
  const fieldPathIndexByTypeName = /* @__PURE__ */ Object.create(null);
  return detectCycleRecursive;
  function detectCycleRecursive(inputObj) {
    if (visitedTypes[inputObj.name]) {
      return;
    }
    visitedTypes[inputObj.name] = true;
    fieldPathIndexByTypeName[inputObj.name] = fieldPath.length;
    const fields = Object.values(inputObj.getFields());
    for (const field of fields) {
      if (isNonNullType(field.type) && isInputObjectType(field.type.ofType)) {
        const fieldType = field.type.ofType;
        const cycleIndex = fieldPathIndexByTypeName[fieldType.name];
        fieldPath.push(field);
        if (cycleIndex === void 0) {
          detectCycleRecursive(fieldType);
        } else {
          const cyclePath = fieldPath.slice(cycleIndex);
          const pathStr = cyclePath.map((fieldObj) => fieldObj.name).join(".");
          context.reportError(
            `Cannot reference Input Object "${fieldType.name}" within itself through a series of non-null fields: "${pathStr}".`,
            cyclePath.map((fieldObj) => fieldObj.astNode)
          );
        }
        fieldPath.pop();
      }
    }
    fieldPathIndexByTypeName[inputObj.name] = void 0;
  }
}
function getAllImplementsInterfaceNodes(type, iface) {
  const { astNode, extensionASTNodes } = type;
  const nodes = astNode != null ? [astNode, ...extensionASTNodes] : extensionASTNodes;
  return nodes.flatMap((typeNode) => {
    var _typeNode$interfaces;
    return (
      /* c8 ignore next */
      (_typeNode$interfaces = typeNode.interfaces) !== null && _typeNode$interfaces !== void 0 ? _typeNode$interfaces : []
    );
  }).filter((ifaceNode) => ifaceNode.name.value === iface.name);
}
function getUnionMemberTypeNodes(union, typeName) {
  const { astNode, extensionASTNodes } = union;
  const nodes = astNode != null ? [astNode, ...extensionASTNodes] : extensionASTNodes;
  return nodes.flatMap((unionNode) => {
    var _unionNode$types;
    return (
      /* c8 ignore next */
      (_unionNode$types = unionNode.types) !== null && _unionNode$types !== void 0 ? _unionNode$types : []
    );
  }).filter((typeNode) => typeNode.name.value === typeName);
}
function getDeprecatedDirectiveNode(definitionNode) {
  var _definitionNode$direc;
  return definitionNode === null || definitionNode === void 0 ? void 0 : (_definitionNode$direc = definitionNode.directives) === null || _definitionNode$direc === void 0 ? void 0 : _definitionNode$direc.find(
    (node) => node.name.value === GraphQLDeprecatedDirective.name
  );
}
var SchemaValidationContext;
var init_validate = __esm({
  "node_modules/graphql/type/validate.mjs"() {
    init_inspect();
    init_GraphQLError();
    init_ast();
    init_typeComparators();
    init_definition();
    init_directives();
    init_introspection();
    init_schema();
    SchemaValidationContext = class {
      constructor(schema) {
        this._errors = [];
        this.schema = schema;
      }
      reportError(message, nodes) {
        const _nodes = Array.isArray(nodes) ? nodes.filter(Boolean) : nodes;
        this._errors.push(
          new GraphQLError(message, {
            nodes: _nodes
          })
        );
      }
      getErrors() {
        return this._errors;
      }
    };
  }
});

// node_modules/graphql/utilities/typeFromAST.mjs
function typeFromAST(schema, typeNode) {
  switch (typeNode.kind) {
    case Kind.LIST_TYPE: {
      const innerType = typeFromAST(schema, typeNode.type);
      return innerType && new GraphQLList(innerType);
    }
    case Kind.NON_NULL_TYPE: {
      const innerType = typeFromAST(schema, typeNode.type);
      return innerType && new GraphQLNonNull(innerType);
    }
    case Kind.NAMED_TYPE:
      return schema.getType(typeNode.name.value);
  }
}
var init_typeFromAST = __esm({
  "node_modules/graphql/utilities/typeFromAST.mjs"() {
    init_kinds();
    init_definition();
  }
});

// node_modules/graphql/utilities/TypeInfo.mjs
function getFieldDef(schema, parentType, fieldNode) {
  const name = fieldNode.name.value;
  if (name === SchemaMetaFieldDef.name && schema.getQueryType() === parentType) {
    return SchemaMetaFieldDef;
  }
  if (name === TypeMetaFieldDef.name && schema.getQueryType() === parentType) {
    return TypeMetaFieldDef;
  }
  if (name === TypeNameMetaFieldDef.name && isCompositeType(parentType)) {
    return TypeNameMetaFieldDef;
  }
  if (isObjectType(parentType) || isInterfaceType(parentType)) {
    return parentType.getFields()[name];
  }
}
function visitWithTypeInfo(typeInfo, visitor) {
  return {
    enter(...args) {
      const node = args[0];
      typeInfo.enter(node);
      const fn = getEnterLeaveForKind(visitor, node.kind).enter;
      if (fn) {
        const result = fn.apply(visitor, args);
        if (result !== void 0) {
          typeInfo.leave(node);
          if (isNode(result)) {
            typeInfo.enter(result);
          }
        }
        return result;
      }
    },
    leave(...args) {
      const node = args[0];
      const fn = getEnterLeaveForKind(visitor, node.kind).leave;
      let result;
      if (fn) {
        result = fn.apply(visitor, args);
      }
      typeInfo.leave(node);
      return result;
    }
  };
}
var TypeInfo;
var init_TypeInfo = __esm({
  "node_modules/graphql/utilities/TypeInfo.mjs"() {
    init_ast();
    init_kinds();
    init_visitor();
    init_definition();
    init_introspection();
    init_typeFromAST();
    TypeInfo = class {
      constructor(schema, initialType, getFieldDefFn) {
        this._schema = schema;
        this._typeStack = [];
        this._parentTypeStack = [];
        this._inputTypeStack = [];
        this._fieldDefStack = [];
        this._defaultValueStack = [];
        this._directive = null;
        this._argument = null;
        this._enumValue = null;
        this._getFieldDef = getFieldDefFn !== null && getFieldDefFn !== void 0 ? getFieldDefFn : getFieldDef;
        if (initialType) {
          if (isInputType(initialType)) {
            this._inputTypeStack.push(initialType);
          }
          if (isCompositeType(initialType)) {
            this._parentTypeStack.push(initialType);
          }
          if (isOutputType(initialType)) {
            this._typeStack.push(initialType);
          }
        }
      }
      get [Symbol.toStringTag]() {
        return "TypeInfo";
      }
      getType() {
        if (this._typeStack.length > 0) {
          return this._typeStack[this._typeStack.length - 1];
        }
      }
      getParentType() {
        if (this._parentTypeStack.length > 0) {
          return this._parentTypeStack[this._parentTypeStack.length - 1];
        }
      }
      getInputType() {
        if (this._inputTypeStack.length > 0) {
          return this._inputTypeStack[this._inputTypeStack.length - 1];
        }
      }
      getParentInputType() {
        if (this._inputTypeStack.length > 1) {
          return this._inputTypeStack[this._inputTypeStack.length - 2];
        }
      }
      getFieldDef() {
        if (this._fieldDefStack.length > 0) {
          return this._fieldDefStack[this._fieldDefStack.length - 1];
        }
      }
      getDefaultValue() {
        if (this._defaultValueStack.length > 0) {
          return this._defaultValueStack[this._defaultValueStack.length - 1];
        }
      }
      getDirective() {
        return this._directive;
      }
      getArgument() {
        return this._argument;
      }
      getEnumValue() {
        return this._enumValue;
      }
      enter(node) {
        const schema = this._schema;
        switch (node.kind) {
          case Kind.SELECTION_SET: {
            const namedType = getNamedType(this.getType());
            this._parentTypeStack.push(
              isCompositeType(namedType) ? namedType : void 0
            );
            break;
          }
          case Kind.FIELD: {
            const parentType = this.getParentType();
            let fieldDef;
            let fieldType;
            if (parentType) {
              fieldDef = this._getFieldDef(schema, parentType, node);
              if (fieldDef) {
                fieldType = fieldDef.type;
              }
            }
            this._fieldDefStack.push(fieldDef);
            this._typeStack.push(isOutputType(fieldType) ? fieldType : void 0);
            break;
          }
          case Kind.DIRECTIVE:
            this._directive = schema.getDirective(node.name.value);
            break;
          case Kind.OPERATION_DEFINITION: {
            const rootType = schema.getRootType(node.operation);
            this._typeStack.push(isObjectType(rootType) ? rootType : void 0);
            break;
          }
          case Kind.INLINE_FRAGMENT:
          case Kind.FRAGMENT_DEFINITION: {
            const typeConditionAST = node.typeCondition;
            const outputType = typeConditionAST ? typeFromAST(schema, typeConditionAST) : getNamedType(this.getType());
            this._typeStack.push(isOutputType(outputType) ? outputType : void 0);
            break;
          }
          case Kind.VARIABLE_DEFINITION: {
            const inputType = typeFromAST(schema, node.type);
            this._inputTypeStack.push(
              isInputType(inputType) ? inputType : void 0
            );
            break;
          }
          case Kind.ARGUMENT: {
            var _this$getDirective;
            let argDef;
            let argType;
            const fieldOrDirective = (_this$getDirective = this.getDirective()) !== null && _this$getDirective !== void 0 ? _this$getDirective : this.getFieldDef();
            if (fieldOrDirective) {
              argDef = fieldOrDirective.args.find(
                (arg) => arg.name === node.name.value
              );
              if (argDef) {
                argType = argDef.type;
              }
            }
            this._argument = argDef;
            this._defaultValueStack.push(argDef ? argDef.defaultValue : void 0);
            this._inputTypeStack.push(isInputType(argType) ? argType : void 0);
            break;
          }
          case Kind.LIST: {
            const listType = getNullableType(this.getInputType());
            const itemType = isListType(listType) ? listType.ofType : listType;
            this._defaultValueStack.push(void 0);
            this._inputTypeStack.push(isInputType(itemType) ? itemType : void 0);
            break;
          }
          case Kind.OBJECT_FIELD: {
            const objectType = getNamedType(this.getInputType());
            let inputFieldType;
            let inputField;
            if (isInputObjectType(objectType)) {
              inputField = objectType.getFields()[node.name.value];
              if (inputField) {
                inputFieldType = inputField.type;
              }
            }
            this._defaultValueStack.push(
              inputField ? inputField.defaultValue : void 0
            );
            this._inputTypeStack.push(
              isInputType(inputFieldType) ? inputFieldType : void 0
            );
            break;
          }
          case Kind.ENUM: {
            const enumType = getNamedType(this.getInputType());
            let enumValue;
            if (isEnumType(enumType)) {
              enumValue = enumType.getValue(node.value);
            }
            this._enumValue = enumValue;
            break;
          }
          default:
        }
      }
      leave(node) {
        switch (node.kind) {
          case Kind.SELECTION_SET:
            this._parentTypeStack.pop();
            break;
          case Kind.FIELD:
            this._fieldDefStack.pop();
            this._typeStack.pop();
            break;
          case Kind.DIRECTIVE:
            this._directive = null;
            break;
          case Kind.OPERATION_DEFINITION:
          case Kind.INLINE_FRAGMENT:
          case Kind.FRAGMENT_DEFINITION:
            this._typeStack.pop();
            break;
          case Kind.VARIABLE_DEFINITION:
            this._inputTypeStack.pop();
            break;
          case Kind.ARGUMENT:
            this._argument = null;
            this._defaultValueStack.pop();
            this._inputTypeStack.pop();
            break;
          case Kind.LIST:
          case Kind.OBJECT_FIELD:
            this._defaultValueStack.pop();
            this._inputTypeStack.pop();
            break;
          case Kind.ENUM:
            this._enumValue = null;
            break;
          default:
        }
      }
    };
  }
});

// node_modules/graphql/language/predicates.mjs
function isDefinitionNode(node) {
  return isExecutableDefinitionNode(node) || isTypeSystemDefinitionNode(node) || isTypeSystemExtensionNode(node);
}
function isExecutableDefinitionNode(node) {
  return node.kind === Kind.OPERATION_DEFINITION || node.kind === Kind.FRAGMENT_DEFINITION;
}
function isSelectionNode(node) {
  return node.kind === Kind.FIELD || node.kind === Kind.FRAGMENT_SPREAD || node.kind === Kind.INLINE_FRAGMENT;
}
function isValueNode(node) {
  return node.kind === Kind.VARIABLE || node.kind === Kind.INT || node.kind === Kind.FLOAT || node.kind === Kind.STRING || node.kind === Kind.BOOLEAN || node.kind === Kind.NULL || node.kind === Kind.ENUM || node.kind === Kind.LIST || node.kind === Kind.OBJECT;
}
function isConstValueNode(node) {
  return isValueNode(node) && (node.kind === Kind.LIST ? node.values.some(isConstValueNode) : node.kind === Kind.OBJECT ? node.fields.some((field) => isConstValueNode(field.value)) : node.kind !== Kind.VARIABLE);
}
function isTypeNode(node) {
  return node.kind === Kind.NAMED_TYPE || node.kind === Kind.LIST_TYPE || node.kind === Kind.NON_NULL_TYPE;
}
function isTypeSystemDefinitionNode(node) {
  return node.kind === Kind.SCHEMA_DEFINITION || isTypeDefinitionNode(node) || node.kind === Kind.DIRECTIVE_DEFINITION;
}
function isTypeDefinitionNode(node) {
  return node.kind === Kind.SCALAR_TYPE_DEFINITION || node.kind === Kind.OBJECT_TYPE_DEFINITION || node.kind === Kind.INTERFACE_TYPE_DEFINITION || node.kind === Kind.UNION_TYPE_DEFINITION || node.kind === Kind.ENUM_TYPE_DEFINITION || node.kind === Kind.INPUT_OBJECT_TYPE_DEFINITION;
}
function isTypeSystemExtensionNode(node) {
  return node.kind === Kind.SCHEMA_EXTENSION || isTypeExtensionNode(node);
}
function isTypeExtensionNode(node) {
  return node.kind === Kind.SCALAR_TYPE_EXTENSION || node.kind === Kind.OBJECT_TYPE_EXTENSION || node.kind === Kind.INTERFACE_TYPE_EXTENSION || node.kind === Kind.UNION_TYPE_EXTENSION || node.kind === Kind.ENUM_TYPE_EXTENSION || node.kind === Kind.INPUT_OBJECT_TYPE_EXTENSION;
}
var init_predicates = __esm({
  "node_modules/graphql/language/predicates.mjs"() {
    init_kinds();
  }
});

// node_modules/graphql/validation/rules/ExecutableDefinitionsRule.mjs
function ExecutableDefinitionsRule(context) {
  return {
    Document(node) {
      for (const definition of node.definitions) {
        if (!isExecutableDefinitionNode(definition)) {
          const defName = definition.kind === Kind.SCHEMA_DEFINITION || definition.kind === Kind.SCHEMA_EXTENSION ? "schema" : '"' + definition.name.value + '"';
          context.reportError(
            new GraphQLError(`The ${defName} definition is not executable.`, {
              nodes: definition
            })
          );
        }
      }
      return false;
    }
  };
}
var init_ExecutableDefinitionsRule = __esm({
  "node_modules/graphql/validation/rules/ExecutableDefinitionsRule.mjs"() {
    init_GraphQLError();
    init_kinds();
    init_predicates();
  }
});

// node_modules/graphql/validation/rules/FieldsOnCorrectTypeRule.mjs
function FieldsOnCorrectTypeRule(context) {
  return {
    Field(node) {
      const type = context.getParentType();
      if (type) {
        const fieldDef = context.getFieldDef();
        if (!fieldDef) {
          const schema = context.getSchema();
          const fieldName = node.name.value;
          let suggestion = didYouMean(
            "to use an inline fragment on",
            getSuggestedTypeNames(schema, type, fieldName)
          );
          if (suggestion === "") {
            suggestion = didYouMean(getSuggestedFieldNames(type, fieldName));
          }
          context.reportError(
            new GraphQLError(
              `Cannot query field "${fieldName}" on type "${type.name}".` + suggestion,
              {
                nodes: node
              }
            )
          );
        }
      }
    }
  };
}
function getSuggestedTypeNames(schema, type, fieldName) {
  if (!isAbstractType(type)) {
    return [];
  }
  const suggestedTypes = /* @__PURE__ */ new Set();
  const usageCount = /* @__PURE__ */ Object.create(null);
  for (const possibleType of schema.getPossibleTypes(type)) {
    if (!possibleType.getFields()[fieldName]) {
      continue;
    }
    suggestedTypes.add(possibleType);
    usageCount[possibleType.name] = 1;
    for (const possibleInterface of possibleType.getInterfaces()) {
      var _usageCount$possibleI;
      if (!possibleInterface.getFields()[fieldName]) {
        continue;
      }
      suggestedTypes.add(possibleInterface);
      usageCount[possibleInterface.name] = ((_usageCount$possibleI = usageCount[possibleInterface.name]) !== null && _usageCount$possibleI !== void 0 ? _usageCount$possibleI : 0) + 1;
    }
  }
  return [...suggestedTypes].sort((typeA, typeB) => {
    const usageCountDiff = usageCount[typeB.name] - usageCount[typeA.name];
    if (usageCountDiff !== 0) {
      return usageCountDiff;
    }
    if (isInterfaceType(typeA) && schema.isSubType(typeA, typeB)) {
      return -1;
    }
    if (isInterfaceType(typeB) && schema.isSubType(typeB, typeA)) {
      return 1;
    }
    return naturalCompare(typeA.name, typeB.name);
  }).map((x) => x.name);
}
function getSuggestedFieldNames(type, fieldName) {
  if (isObjectType(type) || isInterfaceType(type)) {
    const possibleFieldNames = Object.keys(type.getFields());
    return suggestionList(fieldName, possibleFieldNames);
  }
  return [];
}
var init_FieldsOnCorrectTypeRule = __esm({
  "node_modules/graphql/validation/rules/FieldsOnCorrectTypeRule.mjs"() {
    init_didYouMean();
    init_naturalCompare();
    init_suggestionList();
    init_GraphQLError();
    init_definition();
  }
});

// node_modules/graphql/validation/rules/FragmentsOnCompositeTypesRule.mjs
function FragmentsOnCompositeTypesRule(context) {
  return {
    InlineFragment(node) {
      const typeCondition = node.typeCondition;
      if (typeCondition) {
        const type = typeFromAST(context.getSchema(), typeCondition);
        if (type && !isCompositeType(type)) {
          const typeStr = print(typeCondition);
          context.reportError(
            new GraphQLError(
              `Fragment cannot condition on non composite type "${typeStr}".`,
              {
                nodes: typeCondition
              }
            )
          );
        }
      }
    },
    FragmentDefinition(node) {
      const type = typeFromAST(context.getSchema(), node.typeCondition);
      if (type && !isCompositeType(type)) {
        const typeStr = print(node.typeCondition);
        context.reportError(
          new GraphQLError(
            `Fragment "${node.name.value}" cannot condition on non composite type "${typeStr}".`,
            {
              nodes: node.typeCondition
            }
          )
        );
      }
    }
  };
}
var init_FragmentsOnCompositeTypesRule = __esm({
  "node_modules/graphql/validation/rules/FragmentsOnCompositeTypesRule.mjs"() {
    init_GraphQLError();
    init_printer();
    init_definition();
    init_typeFromAST();
  }
});

// node_modules/graphql/validation/rules/KnownArgumentNamesRule.mjs
function KnownArgumentNamesRule(context) {
  return {
    // eslint-disable-next-line new-cap
    ...KnownArgumentNamesOnDirectivesRule(context),
    Argument(argNode) {
      const argDef = context.getArgument();
      const fieldDef = context.getFieldDef();
      const parentType = context.getParentType();
      if (!argDef && fieldDef && parentType) {
        const argName = argNode.name.value;
        const knownArgsNames = fieldDef.args.map((arg) => arg.name);
        const suggestions = suggestionList(argName, knownArgsNames);
        context.reportError(
          new GraphQLError(
            `Unknown argument "${argName}" on field "${parentType.name}.${fieldDef.name}".` + didYouMean(suggestions),
            {
              nodes: argNode
            }
          )
        );
      }
    }
  };
}
function KnownArgumentNamesOnDirectivesRule(context) {
  const directiveArgs = /* @__PURE__ */ Object.create(null);
  const schema = context.getSchema();
  const definedDirectives = schema ? schema.getDirectives() : specifiedDirectives;
  for (const directive of definedDirectives) {
    directiveArgs[directive.name] = directive.args.map((arg) => arg.name);
  }
  const astDefinitions = context.getDocument().definitions;
  for (const def of astDefinitions) {
    if (def.kind === Kind.DIRECTIVE_DEFINITION) {
      var _def$arguments;
      const argsNodes = (_def$arguments = def.arguments) !== null && _def$arguments !== void 0 ? _def$arguments : [];
      directiveArgs[def.name.value] = argsNodes.map((arg) => arg.name.value);
    }
  }
  return {
    Directive(directiveNode) {
      const directiveName = directiveNode.name.value;
      const knownArgs = directiveArgs[directiveName];
      if (directiveNode.arguments && knownArgs) {
        for (const argNode of directiveNode.arguments) {
          const argName = argNode.name.value;
          if (!knownArgs.includes(argName)) {
            const suggestions = suggestionList(argName, knownArgs);
            context.reportError(
              new GraphQLError(
                `Unknown argument "${argName}" on directive "@${directiveName}".` + didYouMean(suggestions),
                {
                  nodes: argNode
                }
              )
            );
          }
        }
      }
      return false;
    }
  };
}
var init_KnownArgumentNamesRule = __esm({
  "node_modules/graphql/validation/rules/KnownArgumentNamesRule.mjs"() {
    init_didYouMean();
    init_suggestionList();
    init_GraphQLError();
    init_kinds();
    init_directives();
  }
});

// node_modules/graphql/validation/rules/KnownDirectivesRule.mjs
function KnownDirectivesRule(context) {
  const locationsMap = /* @__PURE__ */ Object.create(null);
  const schema = context.getSchema();
  const definedDirectives = schema ? schema.getDirectives() : specifiedDirectives;
  for (const directive of definedDirectives) {
    locationsMap[directive.name] = directive.locations;
  }
  const astDefinitions = context.getDocument().definitions;
  for (const def of astDefinitions) {
    if (def.kind === Kind.DIRECTIVE_DEFINITION) {
      locationsMap[def.name.value] = def.locations.map((name) => name.value);
    }
  }
  return {
    Directive(node, _key, _parent, _path, ancestors) {
      const name = node.name.value;
      const locations = locationsMap[name];
      if (!locations) {
        context.reportError(
          new GraphQLError(`Unknown directive "@${name}".`, {
            nodes: node
          })
        );
        return;
      }
      const candidateLocation = getDirectiveLocationForASTPath(ancestors);
      if (candidateLocation && !locations.includes(candidateLocation)) {
        context.reportError(
          new GraphQLError(
            `Directive "@${name}" may not be used on ${candidateLocation}.`,
            {
              nodes: node
            }
          )
        );
      }
    }
  };
}
function getDirectiveLocationForASTPath(ancestors) {
  const appliedTo = ancestors[ancestors.length - 1];
  "kind" in appliedTo || invariant(false);
  switch (appliedTo.kind) {
    case Kind.OPERATION_DEFINITION:
      return getDirectiveLocationForOperation(appliedTo.operation);
    case Kind.FIELD:
      return DirectiveLocation.FIELD;
    case Kind.FRAGMENT_SPREAD:
      return DirectiveLocation.FRAGMENT_SPREAD;
    case Kind.INLINE_FRAGMENT:
      return DirectiveLocation.INLINE_FRAGMENT;
    case Kind.FRAGMENT_DEFINITION:
      return DirectiveLocation.FRAGMENT_DEFINITION;
    case Kind.VARIABLE_DEFINITION:
      return DirectiveLocation.VARIABLE_DEFINITION;
    case Kind.SCHEMA_DEFINITION:
    case Kind.SCHEMA_EXTENSION:
      return DirectiveLocation.SCHEMA;
    case Kind.SCALAR_TYPE_DEFINITION:
    case Kind.SCALAR_TYPE_EXTENSION:
      return DirectiveLocation.SCALAR;
    case Kind.OBJECT_TYPE_DEFINITION:
    case Kind.OBJECT_TYPE_EXTENSION:
      return DirectiveLocation.OBJECT;
    case Kind.FIELD_DEFINITION:
      return DirectiveLocation.FIELD_DEFINITION;
    case Kind.INTERFACE_TYPE_DEFINITION:
    case Kind.INTERFACE_TYPE_EXTENSION:
      return DirectiveLocation.INTERFACE;
    case Kind.UNION_TYPE_DEFINITION:
    case Kind.UNION_TYPE_EXTENSION:
      return DirectiveLocation.UNION;
    case Kind.ENUM_TYPE_DEFINITION:
    case Kind.ENUM_TYPE_EXTENSION:
      return DirectiveLocation.ENUM;
    case Kind.ENUM_VALUE_DEFINITION:
      return DirectiveLocation.ENUM_VALUE;
    case Kind.INPUT_OBJECT_TYPE_DEFINITION:
    case Kind.INPUT_OBJECT_TYPE_EXTENSION:
      return DirectiveLocation.INPUT_OBJECT;
    case Kind.INPUT_VALUE_DEFINITION: {
      const parentNode = ancestors[ancestors.length - 3];
      "kind" in parentNode || invariant(false);
      return parentNode.kind === Kind.INPUT_OBJECT_TYPE_DEFINITION ? DirectiveLocation.INPUT_FIELD_DEFINITION : DirectiveLocation.ARGUMENT_DEFINITION;
    }
    default:
      invariant(false, "Unexpected kind: " + inspect(appliedTo.kind));
  }
}
function getDirectiveLocationForOperation(operation) {
  switch (operation) {
    case OperationTypeNode.QUERY:
      return DirectiveLocation.QUERY;
    case OperationTypeNode.MUTATION:
      return DirectiveLocation.MUTATION;
    case OperationTypeNode.SUBSCRIPTION:
      return DirectiveLocation.SUBSCRIPTION;
  }
}
var init_KnownDirectivesRule = __esm({
  "node_modules/graphql/validation/rules/KnownDirectivesRule.mjs"() {
    init_inspect();
    init_invariant();
    init_GraphQLError();
    init_ast();
    init_directiveLocation();
    init_kinds();
    init_directives();
  }
});

// node_modules/graphql/validation/rules/KnownFragmentNamesRule.mjs
function KnownFragmentNamesRule(context) {
  return {
    FragmentSpread(node) {
      const fragmentName = node.name.value;
      const fragment = context.getFragment(fragmentName);
      if (!fragment) {
        context.reportError(
          new GraphQLError(`Unknown fragment "${fragmentName}".`, {
            nodes: node.name
          })
        );
      }
    }
  };
}
var init_KnownFragmentNamesRule = __esm({
  "node_modules/graphql/validation/rules/KnownFragmentNamesRule.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/KnownTypeNamesRule.mjs
function KnownTypeNamesRule(context) {
  const schema = context.getSchema();
  const existingTypesMap = schema ? schema.getTypeMap() : /* @__PURE__ */ Object.create(null);
  const definedTypes = /* @__PURE__ */ Object.create(null);
  for (const def of context.getDocument().definitions) {
    if (isTypeDefinitionNode(def)) {
      definedTypes[def.name.value] = true;
    }
  }
  const typeNames = [
    ...Object.keys(existingTypesMap),
    ...Object.keys(definedTypes)
  ];
  return {
    NamedType(node, _1, parent, _2, ancestors) {
      const typeName = node.name.value;
      if (!existingTypesMap[typeName] && !definedTypes[typeName]) {
        var _ancestors$;
        const definitionNode = (_ancestors$ = ancestors[2]) !== null && _ancestors$ !== void 0 ? _ancestors$ : parent;
        const isSDL = definitionNode != null && isSDLNode(definitionNode);
        if (isSDL && standardTypeNames.includes(typeName)) {
          return;
        }
        const suggestedTypes = suggestionList(
          typeName,
          isSDL ? standardTypeNames.concat(typeNames) : typeNames
        );
        context.reportError(
          new GraphQLError(
            `Unknown type "${typeName}".` + didYouMean(suggestedTypes),
            {
              nodes: node
            }
          )
        );
      }
    }
  };
}
function isSDLNode(value) {
  return "kind" in value && (isTypeSystemDefinitionNode(value) || isTypeSystemExtensionNode(value));
}
var standardTypeNames;
var init_KnownTypeNamesRule = __esm({
  "node_modules/graphql/validation/rules/KnownTypeNamesRule.mjs"() {
    init_didYouMean();
    init_suggestionList();
    init_GraphQLError();
    init_predicates();
    init_introspection();
    init_scalars();
    standardTypeNames = [...specifiedScalarTypes, ...introspectionTypes].map(
      (type) => type.name
    );
  }
});

// node_modules/graphql/validation/rules/LoneAnonymousOperationRule.mjs
function LoneAnonymousOperationRule(context) {
  let operationCount = 0;
  return {
    Document(node) {
      operationCount = node.definitions.filter(
        (definition) => definition.kind === Kind.OPERATION_DEFINITION
      ).length;
    },
    OperationDefinition(node) {
      if (!node.name && operationCount > 1) {
        context.reportError(
          new GraphQLError(
            "This anonymous operation must be the only defined operation.",
            {
              nodes: node
            }
          )
        );
      }
    }
  };
}
var init_LoneAnonymousOperationRule = __esm({
  "node_modules/graphql/validation/rules/LoneAnonymousOperationRule.mjs"() {
    init_GraphQLError();
    init_kinds();
  }
});

// node_modules/graphql/validation/rules/LoneSchemaDefinitionRule.mjs
function LoneSchemaDefinitionRule(context) {
  var _ref, _ref2, _oldSchema$astNode;
  const oldSchema = context.getSchema();
  const alreadyDefined = (_ref = (_ref2 = (_oldSchema$astNode = oldSchema === null || oldSchema === void 0 ? void 0 : oldSchema.astNode) !== null && _oldSchema$astNode !== void 0 ? _oldSchema$astNode : oldSchema === null || oldSchema === void 0 ? void 0 : oldSchema.getQueryType()) !== null && _ref2 !== void 0 ? _ref2 : oldSchema === null || oldSchema === void 0 ? void 0 : oldSchema.getMutationType()) !== null && _ref !== void 0 ? _ref : oldSchema === null || oldSchema === void 0 ? void 0 : oldSchema.getSubscriptionType();
  let schemaDefinitionsCount = 0;
  return {
    SchemaDefinition(node) {
      if (alreadyDefined) {
        context.reportError(
          new GraphQLError(
            "Cannot define a new schema within a schema extension.",
            {
              nodes: node
            }
          )
        );
        return;
      }
      if (schemaDefinitionsCount > 0) {
        context.reportError(
          new GraphQLError("Must provide only one schema definition.", {
            nodes: node
          })
        );
      }
      ++schemaDefinitionsCount;
    }
  };
}
var init_LoneSchemaDefinitionRule = __esm({
  "node_modules/graphql/validation/rules/LoneSchemaDefinitionRule.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/MaxIntrospectionDepthRule.mjs
function MaxIntrospectionDepthRule(context) {
  function checkDepth(node, visitedFragments = /* @__PURE__ */ Object.create(null), depth = 0) {
    if (node.kind === Kind.FRAGMENT_SPREAD) {
      const fragmentName = node.name.value;
      if (visitedFragments[fragmentName] === true) {
        return false;
      }
      const fragment = context.getFragment(fragmentName);
      if (!fragment) {
        return false;
      }
      try {
        visitedFragments[fragmentName] = true;
        return checkDepth(fragment, visitedFragments, depth);
      } finally {
        visitedFragments[fragmentName] = void 0;
      }
    }
    if (node.kind === Kind.FIELD && // check all introspection lists
    (node.name.value === "fields" || node.name.value === "interfaces" || node.name.value === "possibleTypes" || node.name.value === "inputFields")) {
      depth++;
      if (depth >= MAX_LISTS_DEPTH) {
        return true;
      }
    }
    if ("selectionSet" in node && node.selectionSet) {
      for (const child of node.selectionSet.selections) {
        if (checkDepth(child, visitedFragments, depth)) {
          return true;
        }
      }
    }
    return false;
  }
  return {
    Field(node) {
      if (node.name.value === "__schema" || node.name.value === "__type") {
        if (checkDepth(node)) {
          context.reportError(
            new GraphQLError("Maximum introspection depth exceeded", {
              nodes: [node]
            })
          );
          return false;
        }
      }
    }
  };
}
var MAX_LISTS_DEPTH;
var init_MaxIntrospectionDepthRule = __esm({
  "node_modules/graphql/validation/rules/MaxIntrospectionDepthRule.mjs"() {
    init_GraphQLError();
    init_kinds();
    MAX_LISTS_DEPTH = 3;
  }
});

// node_modules/graphql/validation/rules/NoFragmentCyclesRule.mjs
function NoFragmentCyclesRule(context) {
  const visitedFrags = /* @__PURE__ */ Object.create(null);
  const spreadPath = [];
  const spreadPathIndexByName = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition: () => false,
    FragmentDefinition(node) {
      detectCycleRecursive(node);
      return false;
    }
  };
  function detectCycleRecursive(fragment) {
    if (visitedFrags[fragment.name.value]) {
      return;
    }
    const fragmentName = fragment.name.value;
    visitedFrags[fragmentName] = true;
    const spreadNodes = context.getFragmentSpreads(fragment.selectionSet);
    if (spreadNodes.length === 0) {
      return;
    }
    spreadPathIndexByName[fragmentName] = spreadPath.length;
    for (const spreadNode of spreadNodes) {
      const spreadName = spreadNode.name.value;
      const cycleIndex = spreadPathIndexByName[spreadName];
      spreadPath.push(spreadNode);
      if (cycleIndex === void 0) {
        const spreadFragment = context.getFragment(spreadName);
        if (spreadFragment) {
          detectCycleRecursive(spreadFragment);
        }
      } else {
        const cyclePath = spreadPath.slice(cycleIndex);
        const viaPath = cyclePath.slice(0, -1).map((s) => '"' + s.name.value + '"').join(", ");
        context.reportError(
          new GraphQLError(
            `Cannot spread fragment "${spreadName}" within itself` + (viaPath !== "" ? ` via ${viaPath}.` : "."),
            {
              nodes: cyclePath
            }
          )
        );
      }
      spreadPath.pop();
    }
    spreadPathIndexByName[fragmentName] = void 0;
  }
}
var init_NoFragmentCyclesRule = __esm({
  "node_modules/graphql/validation/rules/NoFragmentCyclesRule.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/NoUndefinedVariablesRule.mjs
function NoUndefinedVariablesRule(context) {
  let variableNameDefined = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition: {
      enter() {
        variableNameDefined = /* @__PURE__ */ Object.create(null);
      },
      leave(operation) {
        const usages = context.getRecursiveVariableUsages(operation);
        for (const { node } of usages) {
          const varName = node.name.value;
          if (variableNameDefined[varName] !== true) {
            context.reportError(
              new GraphQLError(
                operation.name ? `Variable "$${varName}" is not defined by operation "${operation.name.value}".` : `Variable "$${varName}" is not defined.`,
                {
                  nodes: [node, operation]
                }
              )
            );
          }
        }
      }
    },
    VariableDefinition(node) {
      variableNameDefined[node.variable.name.value] = true;
    }
  };
}
var init_NoUndefinedVariablesRule = __esm({
  "node_modules/graphql/validation/rules/NoUndefinedVariablesRule.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/NoUnusedFragmentsRule.mjs
function NoUnusedFragmentsRule(context) {
  const operationDefs = [];
  const fragmentDefs = [];
  return {
    OperationDefinition(node) {
      operationDefs.push(node);
      return false;
    },
    FragmentDefinition(node) {
      fragmentDefs.push(node);
      return false;
    },
    Document: {
      leave() {
        const fragmentNameUsed = /* @__PURE__ */ Object.create(null);
        for (const operation of operationDefs) {
          for (const fragment of context.getRecursivelyReferencedFragments(
            operation
          )) {
            fragmentNameUsed[fragment.name.value] = true;
          }
        }
        for (const fragmentDef of fragmentDefs) {
          const fragName = fragmentDef.name.value;
          if (fragmentNameUsed[fragName] !== true) {
            context.reportError(
              new GraphQLError(`Fragment "${fragName}" is never used.`, {
                nodes: fragmentDef
              })
            );
          }
        }
      }
    }
  };
}
var init_NoUnusedFragmentsRule = __esm({
  "node_modules/graphql/validation/rules/NoUnusedFragmentsRule.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/NoUnusedVariablesRule.mjs
function NoUnusedVariablesRule(context) {
  let variableDefs = [];
  return {
    OperationDefinition: {
      enter() {
        variableDefs = [];
      },
      leave(operation) {
        const variableNameUsed = /* @__PURE__ */ Object.create(null);
        const usages = context.getRecursiveVariableUsages(operation);
        for (const { node } of usages) {
          variableNameUsed[node.name.value] = true;
        }
        for (const variableDef of variableDefs) {
          const variableName = variableDef.variable.name.value;
          if (variableNameUsed[variableName] !== true) {
            context.reportError(
              new GraphQLError(
                operation.name ? `Variable "$${variableName}" is never used in operation "${operation.name.value}".` : `Variable "$${variableName}" is never used.`,
                {
                  nodes: variableDef
                }
              )
            );
          }
        }
      }
    },
    VariableDefinition(def) {
      variableDefs.push(def);
    }
  };
}
var init_NoUnusedVariablesRule = __esm({
  "node_modules/graphql/validation/rules/NoUnusedVariablesRule.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/utilities/sortValueNode.mjs
function sortValueNode(valueNode) {
  switch (valueNode.kind) {
    case Kind.OBJECT:
      return { ...valueNode, fields: sortFields(valueNode.fields) };
    case Kind.LIST:
      return { ...valueNode, values: valueNode.values.map(sortValueNode) };
    case Kind.INT:
    case Kind.FLOAT:
    case Kind.STRING:
    case Kind.BOOLEAN:
    case Kind.NULL:
    case Kind.ENUM:
    case Kind.VARIABLE:
      return valueNode;
  }
}
function sortFields(fields) {
  return fields.map((fieldNode) => ({
    ...fieldNode,
    value: sortValueNode(fieldNode.value)
  })).sort(
    (fieldA, fieldB) => naturalCompare(fieldA.name.value, fieldB.name.value)
  );
}
var init_sortValueNode = __esm({
  "node_modules/graphql/utilities/sortValueNode.mjs"() {
    init_naturalCompare();
    init_kinds();
  }
});

// node_modules/graphql/validation/rules/OverlappingFieldsCanBeMergedRule.mjs
function reasonMessage(reason) {
  if (Array.isArray(reason)) {
    return reason.map(
      ([responseName, subReason]) => `subfields "${responseName}" conflict because ` + reasonMessage(subReason)
    ).join(" and ");
  }
  return reason;
}
function OverlappingFieldsCanBeMergedRule(context) {
  const comparedFragmentPairs = new PairSet();
  const cachedFieldsAndFragmentNames = /* @__PURE__ */ new Map();
  return {
    SelectionSet(selectionSet) {
      const conflicts = findConflictsWithinSelectionSet(
        context,
        cachedFieldsAndFragmentNames,
        comparedFragmentPairs,
        context.getParentType(),
        selectionSet
      );
      for (const [[responseName, reason], fields1, fields2] of conflicts) {
        const reasonMsg = reasonMessage(reason);
        context.reportError(
          new GraphQLError(
            `Fields "${responseName}" conflict because ${reasonMsg}. Use different aliases on the fields to fetch both if this was intentional.`,
            {
              nodes: fields1.concat(fields2)
            }
          )
        );
      }
    }
  };
}
function findConflictsWithinSelectionSet(context, cachedFieldsAndFragmentNames, comparedFragmentPairs, parentType, selectionSet) {
  const conflicts = [];
  const [fieldMap, fragmentNames] = getFieldsAndFragmentNames(
    context,
    cachedFieldsAndFragmentNames,
    parentType,
    selectionSet
  );
  collectConflictsWithin(
    context,
    conflicts,
    cachedFieldsAndFragmentNames,
    comparedFragmentPairs,
    fieldMap
  );
  if (fragmentNames.length !== 0) {
    for (let i = 0; i < fragmentNames.length; i++) {
      collectConflictsBetweenFieldsAndFragment(
        context,
        conflicts,
        cachedFieldsAndFragmentNames,
        comparedFragmentPairs,
        false,
        fieldMap,
        fragmentNames[i]
      );
      for (let j = i + 1; j < fragmentNames.length; j++) {
        collectConflictsBetweenFragments(
          context,
          conflicts,
          cachedFieldsAndFragmentNames,
          comparedFragmentPairs,
          false,
          fragmentNames[i],
          fragmentNames[j]
        );
      }
    }
  }
  return conflicts;
}
function collectConflictsBetweenFieldsAndFragment(context, conflicts, cachedFieldsAndFragmentNames, comparedFragmentPairs, areMutuallyExclusive, fieldMap, fragmentName) {
  const fragment = context.getFragment(fragmentName);
  if (!fragment) {
    return;
  }
  const [fieldMap2, referencedFragmentNames] = getReferencedFieldsAndFragmentNames(
    context,
    cachedFieldsAndFragmentNames,
    fragment
  );
  if (fieldMap === fieldMap2) {
    return;
  }
  collectConflictsBetween(
    context,
    conflicts,
    cachedFieldsAndFragmentNames,
    comparedFragmentPairs,
    areMutuallyExclusive,
    fieldMap,
    fieldMap2
  );
  for (const referencedFragmentName of referencedFragmentNames) {
    if (comparedFragmentPairs.has(
      referencedFragmentName,
      fragmentName,
      areMutuallyExclusive
    )) {
      continue;
    }
    comparedFragmentPairs.add(
      referencedFragmentName,
      fragmentName,
      areMutuallyExclusive
    );
    collectConflictsBetweenFieldsAndFragment(
      context,
      conflicts,
      cachedFieldsAndFragmentNames,
      comparedFragmentPairs,
      areMutuallyExclusive,
      fieldMap,
      referencedFragmentName
    );
  }
}
function collectConflictsBetweenFragments(context, conflicts, cachedFieldsAndFragmentNames, comparedFragmentPairs, areMutuallyExclusive, fragmentName1, fragmentName2) {
  if (fragmentName1 === fragmentName2) {
    return;
  }
  if (comparedFragmentPairs.has(
    fragmentName1,
    fragmentName2,
    areMutuallyExclusive
  )) {
    return;
  }
  comparedFragmentPairs.add(fragmentName1, fragmentName2, areMutuallyExclusive);
  const fragment1 = context.getFragment(fragmentName1);
  const fragment2 = context.getFragment(fragmentName2);
  if (!fragment1 || !fragment2) {
    return;
  }
  const [fieldMap1, referencedFragmentNames1] = getReferencedFieldsAndFragmentNames(
    context,
    cachedFieldsAndFragmentNames,
    fragment1
  );
  const [fieldMap2, referencedFragmentNames2] = getReferencedFieldsAndFragmentNames(
    context,
    cachedFieldsAndFragmentNames,
    fragment2
  );
  collectConflictsBetween(
    context,
    conflicts,
    cachedFieldsAndFragmentNames,
    comparedFragmentPairs,
    areMutuallyExclusive,
    fieldMap1,
    fieldMap2
  );
  for (const referencedFragmentName2 of referencedFragmentNames2) {
    collectConflictsBetweenFragments(
      context,
      conflicts,
      cachedFieldsAndFragmentNames,
      comparedFragmentPairs,
      areMutuallyExclusive,
      fragmentName1,
      referencedFragmentName2
    );
  }
  for (const referencedFragmentName1 of referencedFragmentNames1) {
    collectConflictsBetweenFragments(
      context,
      conflicts,
      cachedFieldsAndFragmentNames,
      comparedFragmentPairs,
      areMutuallyExclusive,
      referencedFragmentName1,
      fragmentName2
    );
  }
}
function findConflictsBetweenSubSelectionSets(context, cachedFieldsAndFragmentNames, comparedFragmentPairs, areMutuallyExclusive, parentType1, selectionSet1, parentType2, selectionSet2) {
  const conflicts = [];
  const [fieldMap1, fragmentNames1] = getFieldsAndFragmentNames(
    context,
    cachedFieldsAndFragmentNames,
    parentType1,
    selectionSet1
  );
  const [fieldMap2, fragmentNames2] = getFieldsAndFragmentNames(
    context,
    cachedFieldsAndFragmentNames,
    parentType2,
    selectionSet2
  );
  collectConflictsBetween(
    context,
    conflicts,
    cachedFieldsAndFragmentNames,
    comparedFragmentPairs,
    areMutuallyExclusive,
    fieldMap1,
    fieldMap2
  );
  for (const fragmentName2 of fragmentNames2) {
    collectConflictsBetweenFieldsAndFragment(
      context,
      conflicts,
      cachedFieldsAndFragmentNames,
      comparedFragmentPairs,
      areMutuallyExclusive,
      fieldMap1,
      fragmentName2
    );
  }
  for (const fragmentName1 of fragmentNames1) {
    collectConflictsBetweenFieldsAndFragment(
      context,
      conflicts,
      cachedFieldsAndFragmentNames,
      comparedFragmentPairs,
      areMutuallyExclusive,
      fieldMap2,
      fragmentName1
    );
  }
  for (const fragmentName1 of fragmentNames1) {
    for (const fragmentName2 of fragmentNames2) {
      collectConflictsBetweenFragments(
        context,
        conflicts,
        cachedFieldsAndFragmentNames,
        comparedFragmentPairs,
        areMutuallyExclusive,
        fragmentName1,
        fragmentName2
      );
    }
  }
  return conflicts;
}
function collectConflictsWithin(context, conflicts, cachedFieldsAndFragmentNames, comparedFragmentPairs, fieldMap) {
  for (const [responseName, fields] of Object.entries(fieldMap)) {
    if (fields.length > 1) {
      for (let i = 0; i < fields.length; i++) {
        for (let j = i + 1; j < fields.length; j++) {
          const conflict = findConflict(
            context,
            cachedFieldsAndFragmentNames,
            comparedFragmentPairs,
            false,
            // within one collection is never mutually exclusive
            responseName,
            fields[i],
            fields[j]
          );
          if (conflict) {
            conflicts.push(conflict);
          }
        }
      }
    }
  }
}
function collectConflictsBetween(context, conflicts, cachedFieldsAndFragmentNames, comparedFragmentPairs, parentFieldsAreMutuallyExclusive, fieldMap1, fieldMap2) {
  for (const [responseName, fields1] of Object.entries(fieldMap1)) {
    const fields2 = fieldMap2[responseName];
    if (fields2) {
      for (const field1 of fields1) {
        for (const field2 of fields2) {
          const conflict = findConflict(
            context,
            cachedFieldsAndFragmentNames,
            comparedFragmentPairs,
            parentFieldsAreMutuallyExclusive,
            responseName,
            field1,
            field2
          );
          if (conflict) {
            conflicts.push(conflict);
          }
        }
      }
    }
  }
}
function findConflict(context, cachedFieldsAndFragmentNames, comparedFragmentPairs, parentFieldsAreMutuallyExclusive, responseName, field1, field2) {
  const [parentType1, node1, def1] = field1;
  const [parentType2, node2, def2] = field2;
  const areMutuallyExclusive = parentFieldsAreMutuallyExclusive || parentType1 !== parentType2 && isObjectType(parentType1) && isObjectType(parentType2);
  if (!areMutuallyExclusive) {
    const name1 = node1.name.value;
    const name2 = node2.name.value;
    if (name1 !== name2) {
      return [
        [responseName, `"${name1}" and "${name2}" are different fields`],
        [node1],
        [node2]
      ];
    }
    if (!sameArguments(node1, node2)) {
      return [
        [responseName, "they have differing arguments"],
        [node1],
        [node2]
      ];
    }
  }
  const type1 = def1 === null || def1 === void 0 ? void 0 : def1.type;
  const type2 = def2 === null || def2 === void 0 ? void 0 : def2.type;
  if (type1 && type2 && doTypesConflict(type1, type2)) {
    return [
      [
        responseName,
        `they return conflicting types "${inspect(type1)}" and "${inspect(
          type2
        )}"`
      ],
      [node1],
      [node2]
    ];
  }
  const selectionSet1 = node1.selectionSet;
  const selectionSet2 = node2.selectionSet;
  if (selectionSet1 && selectionSet2) {
    const conflicts = findConflictsBetweenSubSelectionSets(
      context,
      cachedFieldsAndFragmentNames,
      comparedFragmentPairs,
      areMutuallyExclusive,
      getNamedType(type1),
      selectionSet1,
      getNamedType(type2),
      selectionSet2
    );
    return subfieldConflicts(conflicts, responseName, node1, node2);
  }
}
function sameArguments(node1, node2) {
  const args1 = node1.arguments;
  const args2 = node2.arguments;
  if (args1 === void 0 || args1.length === 0) {
    return args2 === void 0 || args2.length === 0;
  }
  if (args2 === void 0 || args2.length === 0) {
    return false;
  }
  if (args1.length !== args2.length) {
    return false;
  }
  const values2 = new Map(args2.map(({ name, value }) => [name.value, value]));
  return args1.every((arg1) => {
    const value1 = arg1.value;
    const value2 = values2.get(arg1.name.value);
    if (value2 === void 0) {
      return false;
    }
    return stringifyValue(value1) === stringifyValue(value2);
  });
}
function stringifyValue(value) {
  return print(sortValueNode(value));
}
function doTypesConflict(type1, type2) {
  if (isListType(type1)) {
    return isListType(type2) ? doTypesConflict(type1.ofType, type2.ofType) : true;
  }
  if (isListType(type2)) {
    return true;
  }
  if (isNonNullType(type1)) {
    return isNonNullType(type2) ? doTypesConflict(type1.ofType, type2.ofType) : true;
  }
  if (isNonNullType(type2)) {
    return true;
  }
  if (isLeafType(type1) || isLeafType(type2)) {
    return type1 !== type2;
  }
  return false;
}
function getFieldsAndFragmentNames(context, cachedFieldsAndFragmentNames, parentType, selectionSet) {
  const cached = cachedFieldsAndFragmentNames.get(selectionSet);
  if (cached) {
    return cached;
  }
  const nodeAndDefs = /* @__PURE__ */ Object.create(null);
  const fragmentNames = /* @__PURE__ */ Object.create(null);
  _collectFieldsAndFragmentNames(
    context,
    parentType,
    selectionSet,
    nodeAndDefs,
    fragmentNames
  );
  const result = [nodeAndDefs, Object.keys(fragmentNames)];
  cachedFieldsAndFragmentNames.set(selectionSet, result);
  return result;
}
function getReferencedFieldsAndFragmentNames(context, cachedFieldsAndFragmentNames, fragment) {
  const cached = cachedFieldsAndFragmentNames.get(fragment.selectionSet);
  if (cached) {
    return cached;
  }
  const fragmentType = typeFromAST(context.getSchema(), fragment.typeCondition);
  return getFieldsAndFragmentNames(
    context,
    cachedFieldsAndFragmentNames,
    fragmentType,
    fragment.selectionSet
  );
}
function _collectFieldsAndFragmentNames(context, parentType, selectionSet, nodeAndDefs, fragmentNames) {
  for (const selection of selectionSet.selections) {
    switch (selection.kind) {
      case Kind.FIELD: {
        const fieldName = selection.name.value;
        let fieldDef;
        if (isObjectType(parentType) || isInterfaceType(parentType)) {
          fieldDef = parentType.getFields()[fieldName];
        }
        const responseName = selection.alias ? selection.alias.value : fieldName;
        if (!nodeAndDefs[responseName]) {
          nodeAndDefs[responseName] = [];
        }
        nodeAndDefs[responseName].push([parentType, selection, fieldDef]);
        break;
      }
      case Kind.FRAGMENT_SPREAD:
        fragmentNames[selection.name.value] = true;
        break;
      case Kind.INLINE_FRAGMENT: {
        const typeCondition = selection.typeCondition;
        const inlineFragmentType = typeCondition ? typeFromAST(context.getSchema(), typeCondition) : parentType;
        _collectFieldsAndFragmentNames(
          context,
          inlineFragmentType,
          selection.selectionSet,
          nodeAndDefs,
          fragmentNames
        );
        break;
      }
    }
  }
}
function subfieldConflicts(conflicts, responseName, node1, node2) {
  if (conflicts.length > 0) {
    return [
      [responseName, conflicts.map(([reason]) => reason)],
      [node1, ...conflicts.map(([, fields1]) => fields1).flat()],
      [node2, ...conflicts.map(([, , fields2]) => fields2).flat()]
    ];
  }
}
var PairSet;
var init_OverlappingFieldsCanBeMergedRule = __esm({
  "node_modules/graphql/validation/rules/OverlappingFieldsCanBeMergedRule.mjs"() {
    init_inspect();
    init_GraphQLError();
    init_kinds();
    init_printer();
    init_definition();
    init_sortValueNode();
    init_typeFromAST();
    PairSet = class {
      constructor() {
        this._data = /* @__PURE__ */ new Map();
      }
      has(a, b, areMutuallyExclusive) {
        var _this$_data$get;
        const [key1, key2] = a < b ? [a, b] : [b, a];
        const result = (_this$_data$get = this._data.get(key1)) === null || _this$_data$get === void 0 ? void 0 : _this$_data$get.get(key2);
        if (result === void 0) {
          return false;
        }
        return areMutuallyExclusive ? true : areMutuallyExclusive === result;
      }
      add(a, b, areMutuallyExclusive) {
        const [key1, key2] = a < b ? [a, b] : [b, a];
        const map = this._data.get(key1);
        if (map === void 0) {
          this._data.set(key1, /* @__PURE__ */ new Map([[key2, areMutuallyExclusive]]));
        } else {
          map.set(key2, areMutuallyExclusive);
        }
      }
    };
  }
});

// node_modules/graphql/validation/rules/PossibleFragmentSpreadsRule.mjs
function PossibleFragmentSpreadsRule(context) {
  return {
    InlineFragment(node) {
      const fragType = context.getType();
      const parentType = context.getParentType();
      if (isCompositeType(fragType) && isCompositeType(parentType) && !doTypesOverlap(context.getSchema(), fragType, parentType)) {
        const parentTypeStr = inspect(parentType);
        const fragTypeStr = inspect(fragType);
        context.reportError(
          new GraphQLError(
            `Fragment cannot be spread here as objects of type "${parentTypeStr}" can never be of type "${fragTypeStr}".`,
            {
              nodes: node
            }
          )
        );
      }
    },
    FragmentSpread(node) {
      const fragName = node.name.value;
      const fragType = getFragmentType(context, fragName);
      const parentType = context.getParentType();
      if (fragType && parentType && !doTypesOverlap(context.getSchema(), fragType, parentType)) {
        const parentTypeStr = inspect(parentType);
        const fragTypeStr = inspect(fragType);
        context.reportError(
          new GraphQLError(
            `Fragment "${fragName}" cannot be spread here as objects of type "${parentTypeStr}" can never be of type "${fragTypeStr}".`,
            {
              nodes: node
            }
          )
        );
      }
    }
  };
}
function getFragmentType(context, name) {
  const frag = context.getFragment(name);
  if (frag) {
    const type = typeFromAST(context.getSchema(), frag.typeCondition);
    if (isCompositeType(type)) {
      return type;
    }
  }
}
var init_PossibleFragmentSpreadsRule = __esm({
  "node_modules/graphql/validation/rules/PossibleFragmentSpreadsRule.mjs"() {
    init_inspect();
    init_GraphQLError();
    init_definition();
    init_typeComparators();
    init_typeFromAST();
  }
});

// node_modules/graphql/validation/rules/PossibleTypeExtensionsRule.mjs
function PossibleTypeExtensionsRule(context) {
  const schema = context.getSchema();
  const definedTypes = /* @__PURE__ */ Object.create(null);
  for (const def of context.getDocument().definitions) {
    if (isTypeDefinitionNode(def)) {
      definedTypes[def.name.value] = def;
    }
  }
  return {
    ScalarTypeExtension: checkExtension,
    ObjectTypeExtension: checkExtension,
    InterfaceTypeExtension: checkExtension,
    UnionTypeExtension: checkExtension,
    EnumTypeExtension: checkExtension,
    InputObjectTypeExtension: checkExtension
  };
  function checkExtension(node) {
    const typeName = node.name.value;
    const defNode = definedTypes[typeName];
    const existingType = schema === null || schema === void 0 ? void 0 : schema.getType(typeName);
    let expectedKind;
    if (defNode) {
      expectedKind = defKindToExtKind[defNode.kind];
    } else if (existingType) {
      expectedKind = typeToExtKind(existingType);
    }
    if (expectedKind) {
      if (expectedKind !== node.kind) {
        const kindStr = extensionKindToTypeName(node.kind);
        context.reportError(
          new GraphQLError(`Cannot extend non-${kindStr} type "${typeName}".`, {
            nodes: defNode ? [defNode, node] : node
          })
        );
      }
    } else {
      const allTypeNames = Object.keys({
        ...definedTypes,
        ...schema === null || schema === void 0 ? void 0 : schema.getTypeMap()
      });
      const suggestedTypes = suggestionList(typeName, allTypeNames);
      context.reportError(
        new GraphQLError(
          `Cannot extend type "${typeName}" because it is not defined.` + didYouMean(suggestedTypes),
          {
            nodes: node.name
          }
        )
      );
    }
  }
}
function typeToExtKind(type) {
  if (isScalarType(type)) {
    return Kind.SCALAR_TYPE_EXTENSION;
  }
  if (isObjectType(type)) {
    return Kind.OBJECT_TYPE_EXTENSION;
  }
  if (isInterfaceType(type)) {
    return Kind.INTERFACE_TYPE_EXTENSION;
  }
  if (isUnionType(type)) {
    return Kind.UNION_TYPE_EXTENSION;
  }
  if (isEnumType(type)) {
    return Kind.ENUM_TYPE_EXTENSION;
  }
  if (isInputObjectType(type)) {
    return Kind.INPUT_OBJECT_TYPE_EXTENSION;
  }
  invariant(false, "Unexpected type: " + inspect(type));
}
function extensionKindToTypeName(kind) {
  switch (kind) {
    case Kind.SCALAR_TYPE_EXTENSION:
      return "scalar";
    case Kind.OBJECT_TYPE_EXTENSION:
      return "object";
    case Kind.INTERFACE_TYPE_EXTENSION:
      return "interface";
    case Kind.UNION_TYPE_EXTENSION:
      return "union";
    case Kind.ENUM_TYPE_EXTENSION:
      return "enum";
    case Kind.INPUT_OBJECT_TYPE_EXTENSION:
      return "input object";
    default:
      invariant(false, "Unexpected kind: " + inspect(kind));
  }
}
var defKindToExtKind;
var init_PossibleTypeExtensionsRule = __esm({
  "node_modules/graphql/validation/rules/PossibleTypeExtensionsRule.mjs"() {
    init_didYouMean();
    init_inspect();
    init_invariant();
    init_suggestionList();
    init_GraphQLError();
    init_kinds();
    init_predicates();
    init_definition();
    defKindToExtKind = {
      [Kind.SCALAR_TYPE_DEFINITION]: Kind.SCALAR_TYPE_EXTENSION,
      [Kind.OBJECT_TYPE_DEFINITION]: Kind.OBJECT_TYPE_EXTENSION,
      [Kind.INTERFACE_TYPE_DEFINITION]: Kind.INTERFACE_TYPE_EXTENSION,
      [Kind.UNION_TYPE_DEFINITION]: Kind.UNION_TYPE_EXTENSION,
      [Kind.ENUM_TYPE_DEFINITION]: Kind.ENUM_TYPE_EXTENSION,
      [Kind.INPUT_OBJECT_TYPE_DEFINITION]: Kind.INPUT_OBJECT_TYPE_EXTENSION
    };
  }
});

// node_modules/graphql/validation/rules/ProvidedRequiredArgumentsRule.mjs
function ProvidedRequiredArgumentsRule(context) {
  return {
    // eslint-disable-next-line new-cap
    ...ProvidedRequiredArgumentsOnDirectivesRule(context),
    Field: {
      // Validate on leave to allow for deeper errors to appear first.
      leave(fieldNode) {
        var _fieldNode$arguments;
        const fieldDef = context.getFieldDef();
        if (!fieldDef) {
          return false;
        }
        const providedArgs = new Set(
          // FIXME: https://github.com/graphql/graphql-js/issues/2203
          /* c8 ignore next */
          (_fieldNode$arguments = fieldNode.arguments) === null || _fieldNode$arguments === void 0 ? void 0 : _fieldNode$arguments.map((arg) => arg.name.value)
        );
        for (const argDef of fieldDef.args) {
          if (!providedArgs.has(argDef.name) && isRequiredArgument(argDef)) {
            const argTypeStr = inspect(argDef.type);
            context.reportError(
              new GraphQLError(
                `Field "${fieldDef.name}" argument "${argDef.name}" of type "${argTypeStr}" is required, but it was not provided.`,
                {
                  nodes: fieldNode
                }
              )
            );
          }
        }
      }
    }
  };
}
function ProvidedRequiredArgumentsOnDirectivesRule(context) {
  var _schema$getDirectives;
  const requiredArgsMap = /* @__PURE__ */ Object.create(null);
  const schema = context.getSchema();
  const definedDirectives = (_schema$getDirectives = schema === null || schema === void 0 ? void 0 : schema.getDirectives()) !== null && _schema$getDirectives !== void 0 ? _schema$getDirectives : specifiedDirectives;
  for (const directive of definedDirectives) {
    requiredArgsMap[directive.name] = keyMap(
      directive.args.filter(isRequiredArgument),
      (arg) => arg.name
    );
  }
  const astDefinitions = context.getDocument().definitions;
  for (const def of astDefinitions) {
    if (def.kind === Kind.DIRECTIVE_DEFINITION) {
      var _def$arguments;
      const argNodes = (_def$arguments = def.arguments) !== null && _def$arguments !== void 0 ? _def$arguments : [];
      requiredArgsMap[def.name.value] = keyMap(
        argNodes.filter(isRequiredArgumentNode),
        (arg) => arg.name.value
      );
    }
  }
  return {
    Directive: {
      // Validate on leave to allow for deeper errors to appear first.
      leave(directiveNode) {
        const directiveName = directiveNode.name.value;
        const requiredArgs = requiredArgsMap[directiveName];
        if (requiredArgs) {
          var _directiveNode$argume;
          const argNodes = (_directiveNode$argume = directiveNode.arguments) !== null && _directiveNode$argume !== void 0 ? _directiveNode$argume : [];
          const argNodeMap = new Set(argNodes.map((arg) => arg.name.value));
          for (const [argName, argDef] of Object.entries(requiredArgs)) {
            if (!argNodeMap.has(argName)) {
              const argType = isType(argDef.type) ? inspect(argDef.type) : print(argDef.type);
              context.reportError(
                new GraphQLError(
                  `Directive "@${directiveName}" argument "${argName}" of type "${argType}" is required, but it was not provided.`,
                  {
                    nodes: directiveNode
                  }
                )
              );
            }
          }
        }
      }
    }
  };
}
function isRequiredArgumentNode(arg) {
  return arg.type.kind === Kind.NON_NULL_TYPE && arg.defaultValue == null;
}
var init_ProvidedRequiredArgumentsRule = __esm({
  "node_modules/graphql/validation/rules/ProvidedRequiredArgumentsRule.mjs"() {
    init_inspect();
    init_keyMap();
    init_GraphQLError();
    init_kinds();
    init_printer();
    init_definition();
    init_directives();
  }
});

// node_modules/graphql/validation/rules/ScalarLeafsRule.mjs
function ScalarLeafsRule(context) {
  return {
    Field(node) {
      const type = context.getType();
      const selectionSet = node.selectionSet;
      if (type) {
        if (isLeafType(getNamedType(type))) {
          if (selectionSet) {
            const fieldName = node.name.value;
            const typeStr = inspect(type);
            context.reportError(
              new GraphQLError(
                `Field "${fieldName}" must not have a selection since type "${typeStr}" has no subfields.`,
                {
                  nodes: selectionSet
                }
              )
            );
          }
        } else if (!selectionSet) {
          const fieldName = node.name.value;
          const typeStr = inspect(type);
          context.reportError(
            new GraphQLError(
              `Field "${fieldName}" of type "${typeStr}" must have a selection of subfields. Did you mean "${fieldName} { ... }"?`,
              {
                nodes: node
              }
            )
          );
        }
      }
    }
  };
}
var init_ScalarLeafsRule = __esm({
  "node_modules/graphql/validation/rules/ScalarLeafsRule.mjs"() {
    init_inspect();
    init_GraphQLError();
    init_definition();
  }
});

// node_modules/graphql/jsutils/printPathArray.mjs
function printPathArray(path) {
  return path.map(
    (key) => typeof key === "number" ? "[" + key.toString() + "]" : "." + key
  ).join("");
}
var init_printPathArray = __esm({
  "node_modules/graphql/jsutils/printPathArray.mjs"() {
  }
});

// node_modules/graphql/jsutils/Path.mjs
function addPath(prev, key, typename) {
  return {
    prev,
    key,
    typename
  };
}
function pathToArray(path) {
  const flattened = [];
  let curr = path;
  while (curr) {
    flattened.push(curr.key);
    curr = curr.prev;
  }
  return flattened.reverse();
}
var init_Path = __esm({
  "node_modules/graphql/jsutils/Path.mjs"() {
  }
});

// node_modules/graphql/utilities/coerceInputValue.mjs
function coerceInputValue(inputValue, type, onError = defaultOnError) {
  return coerceInputValueImpl(inputValue, type, onError, void 0);
}
function defaultOnError(path, invalidValue, error) {
  let errorPrefix = "Invalid value " + inspect(invalidValue);
  if (path.length > 0) {
    errorPrefix += ` at "value${printPathArray(path)}"`;
  }
  error.message = errorPrefix + ": " + error.message;
  throw error;
}
function coerceInputValueImpl(inputValue, type, onError, path) {
  if (isNonNullType(type)) {
    if (inputValue != null) {
      return coerceInputValueImpl(inputValue, type.ofType, onError, path);
    }
    onError(
      pathToArray(path),
      inputValue,
      new GraphQLError(
        `Expected non-nullable type "${inspect(type)}" not to be null.`
      )
    );
    return;
  }
  if (inputValue == null) {
    return null;
  }
  if (isListType(type)) {
    const itemType = type.ofType;
    if (isIterableObject(inputValue)) {
      return Array.from(inputValue, (itemValue, index) => {
        const itemPath = addPath(path, index, void 0);
        return coerceInputValueImpl(itemValue, itemType, onError, itemPath);
      });
    }
    return [coerceInputValueImpl(inputValue, itemType, onError, path)];
  }
  if (isInputObjectType(type)) {
    if (!isObjectLike(inputValue)) {
      onError(
        pathToArray(path),
        inputValue,
        new GraphQLError(`Expected type "${type.name}" to be an object.`)
      );
      return;
    }
    const coercedValue = {};
    const fieldDefs = type.getFields();
    for (const field of Object.values(fieldDefs)) {
      const fieldValue = inputValue[field.name];
      if (fieldValue === void 0) {
        if (field.defaultValue !== void 0) {
          coercedValue[field.name] = field.defaultValue;
        } else if (isNonNullType(field.type)) {
          const typeStr = inspect(field.type);
          onError(
            pathToArray(path),
            inputValue,
            new GraphQLError(
              `Field "${field.name}" of required type "${typeStr}" was not provided.`
            )
          );
        }
        continue;
      }
      coercedValue[field.name] = coerceInputValueImpl(
        fieldValue,
        field.type,
        onError,
        addPath(path, field.name, type.name)
      );
    }
    for (const fieldName of Object.keys(inputValue)) {
      if (!fieldDefs[fieldName]) {
        const suggestions = suggestionList(
          fieldName,
          Object.keys(type.getFields())
        );
        onError(
          pathToArray(path),
          inputValue,
          new GraphQLError(
            `Field "${fieldName}" is not defined by type "${type.name}".` + didYouMean(suggestions)
          )
        );
      }
    }
    if (type.isOneOf) {
      const keys = Object.keys(coercedValue);
      if (keys.length !== 1) {
        onError(
          pathToArray(path),
          inputValue,
          new GraphQLError(
            `Exactly one key must be specified for OneOf type "${type.name}".`
          )
        );
      }
      const key = keys[0];
      const value = coercedValue[key];
      if (value === null) {
        onError(
          pathToArray(path).concat(key),
          value,
          new GraphQLError(`Field "${key}" must be non-null.`)
        );
      }
    }
    return coercedValue;
  }
  if (isLeafType(type)) {
    let parseResult;
    try {
      parseResult = type.parseValue(inputValue);
    } catch (error) {
      if (error instanceof GraphQLError) {
        onError(pathToArray(path), inputValue, error);
      } else {
        onError(
          pathToArray(path),
          inputValue,
          new GraphQLError(`Expected type "${type.name}". ` + error.message, {
            originalError: error
          })
        );
      }
      return;
    }
    if (parseResult === void 0) {
      onError(
        pathToArray(path),
        inputValue,
        new GraphQLError(`Expected type "${type.name}".`)
      );
    }
    return parseResult;
  }
  invariant(false, "Unexpected input type: " + inspect(type));
}
var init_coerceInputValue = __esm({
  "node_modules/graphql/utilities/coerceInputValue.mjs"() {
    init_didYouMean();
    init_inspect();
    init_invariant();
    init_isIterableObject();
    init_isObjectLike();
    init_Path();
    init_printPathArray();
    init_suggestionList();
    init_GraphQLError();
    init_definition();
  }
});

// node_modules/graphql/utilities/valueFromAST.mjs
function valueFromAST(valueNode, type, variables) {
  if (!valueNode) {
    return;
  }
  if (valueNode.kind === Kind.VARIABLE) {
    const variableName = valueNode.name.value;
    if (variables == null || variables[variableName] === void 0) {
      return;
    }
    const variableValue = variables[variableName];
    if (variableValue === null && isNonNullType(type)) {
      return;
    }
    return variableValue;
  }
  if (isNonNullType(type)) {
    if (valueNode.kind === Kind.NULL) {
      return;
    }
    return valueFromAST(valueNode, type.ofType, variables);
  }
  if (valueNode.kind === Kind.NULL) {
    return null;
  }
  if (isListType(type)) {
    const itemType = type.ofType;
    if (valueNode.kind === Kind.LIST) {
      const coercedValues = [];
      for (const itemNode of valueNode.values) {
        if (isMissingVariable(itemNode, variables)) {
          if (isNonNullType(itemType)) {
            return;
          }
          coercedValues.push(null);
        } else {
          const itemValue = valueFromAST(itemNode, itemType, variables);
          if (itemValue === void 0) {
            return;
          }
          coercedValues.push(itemValue);
        }
      }
      return coercedValues;
    }
    const coercedValue = valueFromAST(valueNode, itemType, variables);
    if (coercedValue === void 0) {
      return;
    }
    return [coercedValue];
  }
  if (isInputObjectType(type)) {
    if (valueNode.kind !== Kind.OBJECT) {
      return;
    }
    const coercedObj = /* @__PURE__ */ Object.create(null);
    const fieldNodes = keyMap(valueNode.fields, (field) => field.name.value);
    for (const field of Object.values(type.getFields())) {
      const fieldNode = fieldNodes[field.name];
      if (!fieldNode || isMissingVariable(fieldNode.value, variables)) {
        if (field.defaultValue !== void 0) {
          coercedObj[field.name] = field.defaultValue;
        } else if (isNonNullType(field.type)) {
          return;
        }
        continue;
      }
      const fieldValue = valueFromAST(fieldNode.value, field.type, variables);
      if (fieldValue === void 0) {
        return;
      }
      coercedObj[field.name] = fieldValue;
    }
    if (type.isOneOf) {
      const keys = Object.keys(coercedObj);
      if (keys.length !== 1) {
        return;
      }
      if (coercedObj[keys[0]] === null) {
        return;
      }
    }
    return coercedObj;
  }
  if (isLeafType(type)) {
    let result;
    try {
      result = type.parseLiteral(valueNode, variables);
    } catch (_error) {
      return;
    }
    if (result === void 0) {
      return;
    }
    return result;
  }
  invariant(false, "Unexpected input type: " + inspect(type));
}
function isMissingVariable(valueNode, variables) {
  return valueNode.kind === Kind.VARIABLE && (variables == null || variables[valueNode.name.value] === void 0);
}
var init_valueFromAST = __esm({
  "node_modules/graphql/utilities/valueFromAST.mjs"() {
    init_inspect();
    init_invariant();
    init_keyMap();
    init_kinds();
    init_definition();
  }
});

// node_modules/graphql/execution/values.mjs
function getVariableValues(schema, varDefNodes, inputs, options) {
  const errors = [];
  const maxErrors = options === null || options === void 0 ? void 0 : options.maxErrors;
  try {
    const coerced = coerceVariableValues(
      schema,
      varDefNodes,
      inputs,
      (error) => {
        if (maxErrors != null && errors.length >= maxErrors) {
          throw new GraphQLError(
            "Too many errors processing variables, error limit reached. Execution aborted."
          );
        }
        errors.push(error);
      }
    );
    if (errors.length === 0) {
      return {
        coerced
      };
    }
  } catch (error) {
    errors.push(error);
  }
  return {
    errors
  };
}
function coerceVariableValues(schema, varDefNodes, inputs, onError) {
  const coercedValues = {};
  for (const varDefNode of varDefNodes) {
    const varName = varDefNode.variable.name.value;
    const varType = typeFromAST(schema, varDefNode.type);
    if (!isInputType(varType)) {
      const varTypeStr = print(varDefNode.type);
      onError(
        new GraphQLError(
          `Variable "$${varName}" expected value of type "${varTypeStr}" which cannot be used as an input type.`,
          {
            nodes: varDefNode.type
          }
        )
      );
      continue;
    }
    if (!hasOwnProperty(inputs, varName)) {
      if (varDefNode.defaultValue) {
        coercedValues[varName] = valueFromAST(varDefNode.defaultValue, varType);
      } else if (isNonNullType(varType)) {
        const varTypeStr = inspect(varType);
        onError(
          new GraphQLError(
            `Variable "$${varName}" of required type "${varTypeStr}" was not provided.`,
            {
              nodes: varDefNode
            }
          )
        );
      }
      continue;
    }
    const value = inputs[varName];
    if (value === null && isNonNullType(varType)) {
      const varTypeStr = inspect(varType);
      onError(
        new GraphQLError(
          `Variable "$${varName}" of non-null type "${varTypeStr}" must not be null.`,
          {
            nodes: varDefNode
          }
        )
      );
      continue;
    }
    coercedValues[varName] = coerceInputValue(
      value,
      varType,
      (path, invalidValue, error) => {
        let prefix = `Variable "$${varName}" got invalid value ` + inspect(invalidValue);
        if (path.length > 0) {
          prefix += ` at "${varName}${printPathArray(path)}"`;
        }
        onError(
          new GraphQLError(prefix + "; " + error.message, {
            nodes: varDefNode,
            originalError: error
          })
        );
      }
    );
  }
  return coercedValues;
}
function getArgumentValues(def, node, variableValues) {
  var _node$arguments;
  const coercedValues = {};
  const argumentNodes = (_node$arguments = node.arguments) !== null && _node$arguments !== void 0 ? _node$arguments : [];
  const argNodeMap = keyMap(argumentNodes, (arg) => arg.name.value);
  for (const argDef of def.args) {
    const name = argDef.name;
    const argType = argDef.type;
    const argumentNode = argNodeMap[name];
    if (!argumentNode) {
      if (argDef.defaultValue !== void 0) {
        coercedValues[name] = argDef.defaultValue;
      } else if (isNonNullType(argType)) {
        throw new GraphQLError(
          `Argument "${name}" of required type "${inspect(argType)}" was not provided.`,
          {
            nodes: node
          }
        );
      }
      continue;
    }
    const valueNode = argumentNode.value;
    let isNull = valueNode.kind === Kind.NULL;
    if (valueNode.kind === Kind.VARIABLE) {
      const variableName = valueNode.name.value;
      if (variableValues == null || !hasOwnProperty(variableValues, variableName)) {
        if (argDef.defaultValue !== void 0) {
          coercedValues[name] = argDef.defaultValue;
        } else if (isNonNullType(argType)) {
          throw new GraphQLError(
            `Argument "${name}" of required type "${inspect(argType)}" was provided the variable "$${variableName}" which was not provided a runtime value.`,
            {
              nodes: valueNode
            }
          );
        }
        continue;
      }
      isNull = variableValues[variableName] == null;
    }
    if (isNull && isNonNullType(argType)) {
      throw new GraphQLError(
        `Argument "${name}" of non-null type "${inspect(argType)}" must not be null.`,
        {
          nodes: valueNode
        }
      );
    }
    const coercedValue = valueFromAST(valueNode, argType, variableValues);
    if (coercedValue === void 0) {
      throw new GraphQLError(
        `Argument "${name}" has invalid value ${print(valueNode)}.`,
        {
          nodes: valueNode
        }
      );
    }
    coercedValues[name] = coercedValue;
  }
  return coercedValues;
}
function getDirectiveValues(directiveDef, node, variableValues) {
  var _node$directives;
  const directiveNode = (_node$directives = node.directives) === null || _node$directives === void 0 ? void 0 : _node$directives.find(
    (directive) => directive.name.value === directiveDef.name
  );
  if (directiveNode) {
    return getArgumentValues(directiveDef, directiveNode, variableValues);
  }
}
function hasOwnProperty(obj, prop) {
  return Object.prototype.hasOwnProperty.call(obj, prop);
}
var init_values = __esm({
  "node_modules/graphql/execution/values.mjs"() {
    init_inspect();
    init_keyMap();
    init_printPathArray();
    init_GraphQLError();
    init_kinds();
    init_printer();
    init_definition();
    init_coerceInputValue();
    init_typeFromAST();
    init_valueFromAST();
  }
});

// node_modules/graphql/execution/collectFields.mjs
function collectFields(schema, fragments, variableValues, runtimeType, selectionSet) {
  const fields = /* @__PURE__ */ new Map();
  collectFieldsImpl(
    schema,
    fragments,
    variableValues,
    runtimeType,
    selectionSet,
    fields,
    /* @__PURE__ */ new Set()
  );
  return fields;
}
function collectSubfields(schema, fragments, variableValues, returnType, fieldNodes) {
  const subFieldNodes = /* @__PURE__ */ new Map();
  const visitedFragmentNames = /* @__PURE__ */ new Set();
  for (const node of fieldNodes) {
    if (node.selectionSet) {
      collectFieldsImpl(
        schema,
        fragments,
        variableValues,
        returnType,
        node.selectionSet,
        subFieldNodes,
        visitedFragmentNames
      );
    }
  }
  return subFieldNodes;
}
function collectFieldsImpl(schema, fragments, variableValues, runtimeType, selectionSet, fields, visitedFragmentNames) {
  for (const selection of selectionSet.selections) {
    switch (selection.kind) {
      case Kind.FIELD: {
        if (!shouldIncludeNode(variableValues, selection)) {
          continue;
        }
        const name = getFieldEntryKey(selection);
        const fieldList = fields.get(name);
        if (fieldList !== void 0) {
          fieldList.push(selection);
        } else {
          fields.set(name, [selection]);
        }
        break;
      }
      case Kind.INLINE_FRAGMENT: {
        if (!shouldIncludeNode(variableValues, selection) || !doesFragmentConditionMatch(schema, selection, runtimeType)) {
          continue;
        }
        collectFieldsImpl(
          schema,
          fragments,
          variableValues,
          runtimeType,
          selection.selectionSet,
          fields,
          visitedFragmentNames
        );
        break;
      }
      case Kind.FRAGMENT_SPREAD: {
        const fragName = selection.name.value;
        if (visitedFragmentNames.has(fragName) || !shouldIncludeNode(variableValues, selection)) {
          continue;
        }
        visitedFragmentNames.add(fragName);
        const fragment = fragments[fragName];
        if (!fragment || !doesFragmentConditionMatch(schema, fragment, runtimeType)) {
          continue;
        }
        collectFieldsImpl(
          schema,
          fragments,
          variableValues,
          runtimeType,
          fragment.selectionSet,
          fields,
          visitedFragmentNames
        );
        break;
      }
    }
  }
}
function shouldIncludeNode(variableValues, node) {
  const skip = getDirectiveValues(GraphQLSkipDirective, node, variableValues);
  if ((skip === null || skip === void 0 ? void 0 : skip.if) === true) {
    return false;
  }
  const include = getDirectiveValues(
    GraphQLIncludeDirective,
    node,
    variableValues
  );
  if ((include === null || include === void 0 ? void 0 : include.if) === false) {
    return false;
  }
  return true;
}
function doesFragmentConditionMatch(schema, fragment, type) {
  const typeConditionNode = fragment.typeCondition;
  if (!typeConditionNode) {
    return true;
  }
  const conditionalType = typeFromAST(schema, typeConditionNode);
  if (conditionalType === type) {
    return true;
  }
  if (isAbstractType(conditionalType)) {
    return schema.isSubType(conditionalType, type);
  }
  return false;
}
function getFieldEntryKey(node) {
  return node.alias ? node.alias.value : node.name.value;
}
var init_collectFields = __esm({
  "node_modules/graphql/execution/collectFields.mjs"() {
    init_kinds();
    init_definition();
    init_directives();
    init_typeFromAST();
    init_values();
  }
});

// node_modules/graphql/validation/rules/SingleFieldSubscriptionsRule.mjs
function SingleFieldSubscriptionsRule(context) {
  return {
    OperationDefinition(node) {
      if (node.operation === "subscription") {
        const schema = context.getSchema();
        const subscriptionType = schema.getSubscriptionType();
        if (subscriptionType) {
          const operationName = node.name ? node.name.value : null;
          const variableValues = /* @__PURE__ */ Object.create(null);
          const document2 = context.getDocument();
          const fragments = /* @__PURE__ */ Object.create(null);
          for (const definition of document2.definitions) {
            if (definition.kind === Kind.FRAGMENT_DEFINITION) {
              fragments[definition.name.value] = definition;
            }
          }
          const fields = collectFields(
            schema,
            fragments,
            variableValues,
            subscriptionType,
            node.selectionSet
          );
          if (fields.size > 1) {
            const fieldSelectionLists = [...fields.values()];
            const extraFieldSelectionLists = fieldSelectionLists.slice(1);
            const extraFieldSelections = extraFieldSelectionLists.flat();
            context.reportError(
              new GraphQLError(
                operationName != null ? `Subscription "${operationName}" must select only one top level field.` : "Anonymous Subscription must select only one top level field.",
                {
                  nodes: extraFieldSelections
                }
              )
            );
          }
          for (const fieldNodes of fields.values()) {
            const field = fieldNodes[0];
            const fieldName = field.name.value;
            if (fieldName.startsWith("__")) {
              context.reportError(
                new GraphQLError(
                  operationName != null ? `Subscription "${operationName}" must not select an introspection top level field.` : "Anonymous Subscription must not select an introspection top level field.",
                  {
                    nodes: fieldNodes
                  }
                )
              );
            }
          }
        }
      }
    }
  };
}
var init_SingleFieldSubscriptionsRule = __esm({
  "node_modules/graphql/validation/rules/SingleFieldSubscriptionsRule.mjs"() {
    init_GraphQLError();
    init_kinds();
    init_collectFields();
  }
});

// node_modules/graphql/jsutils/groupBy.mjs
function groupBy(list, keyFn) {
  const result = /* @__PURE__ */ new Map();
  for (const item of list) {
    const key = keyFn(item);
    const group = result.get(key);
    if (group === void 0) {
      result.set(key, [item]);
    } else {
      group.push(item);
    }
  }
  return result;
}
var init_groupBy = __esm({
  "node_modules/graphql/jsutils/groupBy.mjs"() {
  }
});

// node_modules/graphql/validation/rules/UniqueArgumentDefinitionNamesRule.mjs
function UniqueArgumentDefinitionNamesRule(context) {
  return {
    DirectiveDefinition(directiveNode) {
      var _directiveNode$argume;
      const argumentNodes = (_directiveNode$argume = directiveNode.arguments) !== null && _directiveNode$argume !== void 0 ? _directiveNode$argume : [];
      return checkArgUniqueness(`@${directiveNode.name.value}`, argumentNodes);
    },
    InterfaceTypeDefinition: checkArgUniquenessPerField,
    InterfaceTypeExtension: checkArgUniquenessPerField,
    ObjectTypeDefinition: checkArgUniquenessPerField,
    ObjectTypeExtension: checkArgUniquenessPerField
  };
  function checkArgUniquenessPerField(typeNode) {
    var _typeNode$fields;
    const typeName = typeNode.name.value;
    const fieldNodes = (_typeNode$fields = typeNode.fields) !== null && _typeNode$fields !== void 0 ? _typeNode$fields : [];
    for (const fieldDef of fieldNodes) {
      var _fieldDef$arguments;
      const fieldName = fieldDef.name.value;
      const argumentNodes = (_fieldDef$arguments = fieldDef.arguments) !== null && _fieldDef$arguments !== void 0 ? _fieldDef$arguments : [];
      checkArgUniqueness(`${typeName}.${fieldName}`, argumentNodes);
    }
    return false;
  }
  function checkArgUniqueness(parentName, argumentNodes) {
    const seenArgs = groupBy(argumentNodes, (arg) => arg.name.value);
    for (const [argName, argNodes] of seenArgs) {
      if (argNodes.length > 1) {
        context.reportError(
          new GraphQLError(
            `Argument "${parentName}(${argName}:)" can only be defined once.`,
            {
              nodes: argNodes.map((node) => node.name)
            }
          )
        );
      }
    }
    return false;
  }
}
var init_UniqueArgumentDefinitionNamesRule = __esm({
  "node_modules/graphql/validation/rules/UniqueArgumentDefinitionNamesRule.mjs"() {
    init_groupBy();
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/UniqueArgumentNamesRule.mjs
function UniqueArgumentNamesRule(context) {
  return {
    Field: checkArgUniqueness,
    Directive: checkArgUniqueness
  };
  function checkArgUniqueness(parentNode) {
    var _parentNode$arguments;
    const argumentNodes = (_parentNode$arguments = parentNode.arguments) !== null && _parentNode$arguments !== void 0 ? _parentNode$arguments : [];
    const seenArgs = groupBy(argumentNodes, (arg) => arg.name.value);
    for (const [argName, argNodes] of seenArgs) {
      if (argNodes.length > 1) {
        context.reportError(
          new GraphQLError(
            `There can be only one argument named "${argName}".`,
            {
              nodes: argNodes.map((node) => node.name)
            }
          )
        );
      }
    }
  }
}
var init_UniqueArgumentNamesRule = __esm({
  "node_modules/graphql/validation/rules/UniqueArgumentNamesRule.mjs"() {
    init_groupBy();
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/UniqueDirectiveNamesRule.mjs
function UniqueDirectiveNamesRule(context) {
  const knownDirectiveNames = /* @__PURE__ */ Object.create(null);
  const schema = context.getSchema();
  return {
    DirectiveDefinition(node) {
      const directiveName = node.name.value;
      if (schema !== null && schema !== void 0 && schema.getDirective(directiveName)) {
        context.reportError(
          new GraphQLError(
            `Directive "@${directiveName}" already exists in the schema. It cannot be redefined.`,
            {
              nodes: node.name
            }
          )
        );
        return;
      }
      if (knownDirectiveNames[directiveName]) {
        context.reportError(
          new GraphQLError(
            `There can be only one directive named "@${directiveName}".`,
            {
              nodes: [knownDirectiveNames[directiveName], node.name]
            }
          )
        );
      } else {
        knownDirectiveNames[directiveName] = node.name;
      }
      return false;
    }
  };
}
var init_UniqueDirectiveNamesRule = __esm({
  "node_modules/graphql/validation/rules/UniqueDirectiveNamesRule.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/UniqueDirectivesPerLocationRule.mjs
function UniqueDirectivesPerLocationRule(context) {
  const uniqueDirectiveMap = /* @__PURE__ */ Object.create(null);
  const schema = context.getSchema();
  const definedDirectives = schema ? schema.getDirectives() : specifiedDirectives;
  for (const directive of definedDirectives) {
    uniqueDirectiveMap[directive.name] = !directive.isRepeatable;
  }
  const astDefinitions = context.getDocument().definitions;
  for (const def of astDefinitions) {
    if (def.kind === Kind.DIRECTIVE_DEFINITION) {
      uniqueDirectiveMap[def.name.value] = !def.repeatable;
    }
  }
  const schemaDirectives = /* @__PURE__ */ Object.create(null);
  const typeDirectivesMap = /* @__PURE__ */ Object.create(null);
  return {
    // Many different AST nodes may contain directives. Rather than listing
    // them all, just listen for entering any node, and check to see if it
    // defines any directives.
    enter(node) {
      if (!("directives" in node) || !node.directives) {
        return;
      }
      let seenDirectives;
      if (node.kind === Kind.SCHEMA_DEFINITION || node.kind === Kind.SCHEMA_EXTENSION) {
        seenDirectives = schemaDirectives;
      } else if (isTypeDefinitionNode(node) || isTypeExtensionNode(node)) {
        const typeName = node.name.value;
        seenDirectives = typeDirectivesMap[typeName];
        if (seenDirectives === void 0) {
          typeDirectivesMap[typeName] = seenDirectives = /* @__PURE__ */ Object.create(null);
        }
      } else {
        seenDirectives = /* @__PURE__ */ Object.create(null);
      }
      for (const directive of node.directives) {
        const directiveName = directive.name.value;
        if (uniqueDirectiveMap[directiveName]) {
          if (seenDirectives[directiveName]) {
            context.reportError(
              new GraphQLError(
                `The directive "@${directiveName}" can only be used once at this location.`,
                {
                  nodes: [seenDirectives[directiveName], directive]
                }
              )
            );
          } else {
            seenDirectives[directiveName] = directive;
          }
        }
      }
    }
  };
}
var init_UniqueDirectivesPerLocationRule = __esm({
  "node_modules/graphql/validation/rules/UniqueDirectivesPerLocationRule.mjs"() {
    init_GraphQLError();
    init_kinds();
    init_predicates();
    init_directives();
  }
});

// node_modules/graphql/validation/rules/UniqueEnumValueNamesRule.mjs
function UniqueEnumValueNamesRule(context) {
  const schema = context.getSchema();
  const existingTypeMap = schema ? schema.getTypeMap() : /* @__PURE__ */ Object.create(null);
  const knownValueNames = /* @__PURE__ */ Object.create(null);
  return {
    EnumTypeDefinition: checkValueUniqueness,
    EnumTypeExtension: checkValueUniqueness
  };
  function checkValueUniqueness(node) {
    var _node$values;
    const typeName = node.name.value;
    if (!knownValueNames[typeName]) {
      knownValueNames[typeName] = /* @__PURE__ */ Object.create(null);
    }
    const valueNodes = (_node$values = node.values) !== null && _node$values !== void 0 ? _node$values : [];
    const valueNames = knownValueNames[typeName];
    for (const valueDef of valueNodes) {
      const valueName = valueDef.name.value;
      const existingType = existingTypeMap[typeName];
      if (isEnumType(existingType) && existingType.getValue(valueName)) {
        context.reportError(
          new GraphQLError(
            `Enum value "${typeName}.${valueName}" already exists in the schema. It cannot also be defined in this type extension.`,
            {
              nodes: valueDef.name
            }
          )
        );
      } else if (valueNames[valueName]) {
        context.reportError(
          new GraphQLError(
            `Enum value "${typeName}.${valueName}" can only be defined once.`,
            {
              nodes: [valueNames[valueName], valueDef.name]
            }
          )
        );
      } else {
        valueNames[valueName] = valueDef.name;
      }
    }
    return false;
  }
}
var init_UniqueEnumValueNamesRule = __esm({
  "node_modules/graphql/validation/rules/UniqueEnumValueNamesRule.mjs"() {
    init_GraphQLError();
    init_definition();
  }
});

// node_modules/graphql/validation/rules/UniqueFieldDefinitionNamesRule.mjs
function UniqueFieldDefinitionNamesRule(context) {
  const schema = context.getSchema();
  const existingTypeMap = schema ? schema.getTypeMap() : /* @__PURE__ */ Object.create(null);
  const knownFieldNames = /* @__PURE__ */ Object.create(null);
  return {
    InputObjectTypeDefinition: checkFieldUniqueness,
    InputObjectTypeExtension: checkFieldUniqueness,
    InterfaceTypeDefinition: checkFieldUniqueness,
    InterfaceTypeExtension: checkFieldUniqueness,
    ObjectTypeDefinition: checkFieldUniqueness,
    ObjectTypeExtension: checkFieldUniqueness
  };
  function checkFieldUniqueness(node) {
    var _node$fields;
    const typeName = node.name.value;
    if (!knownFieldNames[typeName]) {
      knownFieldNames[typeName] = /* @__PURE__ */ Object.create(null);
    }
    const fieldNodes = (_node$fields = node.fields) !== null && _node$fields !== void 0 ? _node$fields : [];
    const fieldNames = knownFieldNames[typeName];
    for (const fieldDef of fieldNodes) {
      const fieldName = fieldDef.name.value;
      if (hasField(existingTypeMap[typeName], fieldName)) {
        context.reportError(
          new GraphQLError(
            `Field "${typeName}.${fieldName}" already exists in the schema. It cannot also be defined in this type extension.`,
            {
              nodes: fieldDef.name
            }
          )
        );
      } else if (fieldNames[fieldName]) {
        context.reportError(
          new GraphQLError(
            `Field "${typeName}.${fieldName}" can only be defined once.`,
            {
              nodes: [fieldNames[fieldName], fieldDef.name]
            }
          )
        );
      } else {
        fieldNames[fieldName] = fieldDef.name;
      }
    }
    return false;
  }
}
function hasField(type, fieldName) {
  if (isObjectType(type) || isInterfaceType(type) || isInputObjectType(type)) {
    return type.getFields()[fieldName] != null;
  }
  return false;
}
var init_UniqueFieldDefinitionNamesRule = __esm({
  "node_modules/graphql/validation/rules/UniqueFieldDefinitionNamesRule.mjs"() {
    init_GraphQLError();
    init_definition();
  }
});

// node_modules/graphql/validation/rules/UniqueFragmentNamesRule.mjs
function UniqueFragmentNamesRule(context) {
  const knownFragmentNames = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition: () => false,
    FragmentDefinition(node) {
      const fragmentName = node.name.value;
      if (knownFragmentNames[fragmentName]) {
        context.reportError(
          new GraphQLError(
            `There can be only one fragment named "${fragmentName}".`,
            {
              nodes: [knownFragmentNames[fragmentName], node.name]
            }
          )
        );
      } else {
        knownFragmentNames[fragmentName] = node.name;
      }
      return false;
    }
  };
}
var init_UniqueFragmentNamesRule = __esm({
  "node_modules/graphql/validation/rules/UniqueFragmentNamesRule.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/UniqueInputFieldNamesRule.mjs
function UniqueInputFieldNamesRule(context) {
  const knownNameStack = [];
  let knownNames = /* @__PURE__ */ Object.create(null);
  return {
    ObjectValue: {
      enter() {
        knownNameStack.push(knownNames);
        knownNames = /* @__PURE__ */ Object.create(null);
      },
      leave() {
        const prevKnownNames = knownNameStack.pop();
        prevKnownNames || invariant(false);
        knownNames = prevKnownNames;
      }
    },
    ObjectField(node) {
      const fieldName = node.name.value;
      if (knownNames[fieldName]) {
        context.reportError(
          new GraphQLError(
            `There can be only one input field named "${fieldName}".`,
            {
              nodes: [knownNames[fieldName], node.name]
            }
          )
        );
      } else {
        knownNames[fieldName] = node.name;
      }
    }
  };
}
var init_UniqueInputFieldNamesRule = __esm({
  "node_modules/graphql/validation/rules/UniqueInputFieldNamesRule.mjs"() {
    init_invariant();
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/UniqueOperationNamesRule.mjs
function UniqueOperationNamesRule(context) {
  const knownOperationNames = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition(node) {
      const operationName = node.name;
      if (operationName) {
        if (knownOperationNames[operationName.value]) {
          context.reportError(
            new GraphQLError(
              `There can be only one operation named "${operationName.value}".`,
              {
                nodes: [
                  knownOperationNames[operationName.value],
                  operationName
                ]
              }
            )
          );
        } else {
          knownOperationNames[operationName.value] = operationName;
        }
      }
      return false;
    },
    FragmentDefinition: () => false
  };
}
var init_UniqueOperationNamesRule = __esm({
  "node_modules/graphql/validation/rules/UniqueOperationNamesRule.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/UniqueOperationTypesRule.mjs
function UniqueOperationTypesRule(context) {
  const schema = context.getSchema();
  const definedOperationTypes = /* @__PURE__ */ Object.create(null);
  const existingOperationTypes = schema ? {
    query: schema.getQueryType(),
    mutation: schema.getMutationType(),
    subscription: schema.getSubscriptionType()
  } : {};
  return {
    SchemaDefinition: checkOperationTypes,
    SchemaExtension: checkOperationTypes
  };
  function checkOperationTypes(node) {
    var _node$operationTypes;
    const operationTypesNodes = (_node$operationTypes = node.operationTypes) !== null && _node$operationTypes !== void 0 ? _node$operationTypes : [];
    for (const operationType of operationTypesNodes) {
      const operation = operationType.operation;
      const alreadyDefinedOperationType = definedOperationTypes[operation];
      if (existingOperationTypes[operation]) {
        context.reportError(
          new GraphQLError(
            `Type for ${operation} already defined in the schema. It cannot be redefined.`,
            {
              nodes: operationType
            }
          )
        );
      } else if (alreadyDefinedOperationType) {
        context.reportError(
          new GraphQLError(
            `There can be only one ${operation} type in schema.`,
            {
              nodes: [alreadyDefinedOperationType, operationType]
            }
          )
        );
      } else {
        definedOperationTypes[operation] = operationType;
      }
    }
    return false;
  }
}
var init_UniqueOperationTypesRule = __esm({
  "node_modules/graphql/validation/rules/UniqueOperationTypesRule.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/UniqueTypeNamesRule.mjs
function UniqueTypeNamesRule(context) {
  const knownTypeNames = /* @__PURE__ */ Object.create(null);
  const schema = context.getSchema();
  return {
    ScalarTypeDefinition: checkTypeName,
    ObjectTypeDefinition: checkTypeName,
    InterfaceTypeDefinition: checkTypeName,
    UnionTypeDefinition: checkTypeName,
    EnumTypeDefinition: checkTypeName,
    InputObjectTypeDefinition: checkTypeName
  };
  function checkTypeName(node) {
    const typeName = node.name.value;
    if (schema !== null && schema !== void 0 && schema.getType(typeName)) {
      context.reportError(
        new GraphQLError(
          `Type "${typeName}" already exists in the schema. It cannot also be defined in this type definition.`,
          {
            nodes: node.name
          }
        )
      );
      return;
    }
    if (knownTypeNames[typeName]) {
      context.reportError(
        new GraphQLError(`There can be only one type named "${typeName}".`, {
          nodes: [knownTypeNames[typeName], node.name]
        })
      );
    } else {
      knownTypeNames[typeName] = node.name;
    }
    return false;
  }
}
var init_UniqueTypeNamesRule = __esm({
  "node_modules/graphql/validation/rules/UniqueTypeNamesRule.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/UniqueVariableNamesRule.mjs
function UniqueVariableNamesRule(context) {
  return {
    OperationDefinition(operationNode) {
      var _operationNode$variab;
      const variableDefinitions = (_operationNode$variab = operationNode.variableDefinitions) !== null && _operationNode$variab !== void 0 ? _operationNode$variab : [];
      const seenVariableDefinitions = groupBy(
        variableDefinitions,
        (node) => node.variable.name.value
      );
      for (const [variableName, variableNodes] of seenVariableDefinitions) {
        if (variableNodes.length > 1) {
          context.reportError(
            new GraphQLError(
              `There can be only one variable named "$${variableName}".`,
              {
                nodes: variableNodes.map((node) => node.variable.name)
              }
            )
          );
        }
      }
    }
  };
}
var init_UniqueVariableNamesRule = __esm({
  "node_modules/graphql/validation/rules/UniqueVariableNamesRule.mjs"() {
    init_groupBy();
    init_GraphQLError();
  }
});

// node_modules/graphql/validation/rules/ValuesOfCorrectTypeRule.mjs
function ValuesOfCorrectTypeRule(context) {
  let variableDefinitions = {};
  return {
    OperationDefinition: {
      enter() {
        variableDefinitions = {};
      }
    },
    VariableDefinition(definition) {
      variableDefinitions[definition.variable.name.value] = definition;
    },
    ListValue(node) {
      const type = getNullableType(context.getParentInputType());
      if (!isListType(type)) {
        isValidValueNode(context, node);
        return false;
      }
    },
    ObjectValue(node) {
      const type = getNamedType(context.getInputType());
      if (!isInputObjectType(type)) {
        isValidValueNode(context, node);
        return false;
      }
      const fieldNodeMap = keyMap(node.fields, (field) => field.name.value);
      for (const fieldDef of Object.values(type.getFields())) {
        const fieldNode = fieldNodeMap[fieldDef.name];
        if (!fieldNode && isRequiredInputField(fieldDef)) {
          const typeStr = inspect(fieldDef.type);
          context.reportError(
            new GraphQLError(
              `Field "${type.name}.${fieldDef.name}" of required type "${typeStr}" was not provided.`,
              {
                nodes: node
              }
            )
          );
        }
      }
      if (type.isOneOf) {
        validateOneOfInputObject(
          context,
          node,
          type,
          fieldNodeMap,
          variableDefinitions
        );
      }
    },
    ObjectField(node) {
      const parentType = getNamedType(context.getParentInputType());
      const fieldType = context.getInputType();
      if (!fieldType && isInputObjectType(parentType)) {
        const suggestions = suggestionList(
          node.name.value,
          Object.keys(parentType.getFields())
        );
        context.reportError(
          new GraphQLError(
            `Field "${node.name.value}" is not defined by type "${parentType.name}".` + didYouMean(suggestions),
            {
              nodes: node
            }
          )
        );
      }
    },
    NullValue(node) {
      const type = context.getInputType();
      if (isNonNullType(type)) {
        context.reportError(
          new GraphQLError(
            `Expected value of type "${inspect(type)}", found ${print(node)}.`,
            {
              nodes: node
            }
          )
        );
      }
    },
    EnumValue: (node) => isValidValueNode(context, node),
    IntValue: (node) => isValidValueNode(context, node),
    FloatValue: (node) => isValidValueNode(context, node),
    StringValue: (node) => isValidValueNode(context, node),
    BooleanValue: (node) => isValidValueNode(context, node)
  };
}
function isValidValueNode(context, node) {
  const locationType = context.getInputType();
  if (!locationType) {
    return;
  }
  const type = getNamedType(locationType);
  if (!isLeafType(type)) {
    const typeStr = inspect(locationType);
    context.reportError(
      new GraphQLError(
        `Expected value of type "${typeStr}", found ${print(node)}.`,
        {
          nodes: node
        }
      )
    );
    return;
  }
  try {
    const parseResult = type.parseLiteral(
      node,
      void 0
      /* variables */
    );
    if (parseResult === void 0) {
      const typeStr = inspect(locationType);
      context.reportError(
        new GraphQLError(
          `Expected value of type "${typeStr}", found ${print(node)}.`,
          {
            nodes: node
          }
        )
      );
    }
  } catch (error) {
    const typeStr = inspect(locationType);
    if (error instanceof GraphQLError) {
      context.reportError(error);
    } else {
      context.reportError(
        new GraphQLError(
          `Expected value of type "${typeStr}", found ${print(node)}; ` + error.message,
          {
            nodes: node,
            originalError: error
          }
        )
      );
    }
  }
}
function validateOneOfInputObject(context, node, type, fieldNodeMap, variableDefinitions) {
  var _fieldNodeMap$keys$;
  const keys = Object.keys(fieldNodeMap);
  const isNotExactlyOneField = keys.length !== 1;
  if (isNotExactlyOneField) {
    context.reportError(
      new GraphQLError(
        `OneOf Input Object "${type.name}" must specify exactly one key.`,
        {
          nodes: [node]
        }
      )
    );
    return;
  }
  const value = (_fieldNodeMap$keys$ = fieldNodeMap[keys[0]]) === null || _fieldNodeMap$keys$ === void 0 ? void 0 : _fieldNodeMap$keys$.value;
  const isNullLiteral = !value || value.kind === Kind.NULL;
  const isVariable = (value === null || value === void 0 ? void 0 : value.kind) === Kind.VARIABLE;
  if (isNullLiteral) {
    context.reportError(
      new GraphQLError(`Field "${type.name}.${keys[0]}" must be non-null.`, {
        nodes: [node]
      })
    );
    return;
  }
  if (isVariable) {
    const variableName = value.name.value;
    const definition = variableDefinitions[variableName];
    const isNullableVariable = definition.type.kind !== Kind.NON_NULL_TYPE;
    if (isNullableVariable) {
      context.reportError(
        new GraphQLError(
          `Variable "${variableName}" must be non-nullable to be used for OneOf Input Object "${type.name}".`,
          {
            nodes: [node]
          }
        )
      );
    }
  }
}
var init_ValuesOfCorrectTypeRule = __esm({
  "node_modules/graphql/validation/rules/ValuesOfCorrectTypeRule.mjs"() {
    init_didYouMean();
    init_inspect();
    init_keyMap();
    init_suggestionList();
    init_GraphQLError();
    init_kinds();
    init_printer();
    init_definition();
  }
});

// node_modules/graphql/validation/rules/VariablesAreInputTypesRule.mjs
function VariablesAreInputTypesRule(context) {
  return {
    VariableDefinition(node) {
      const type = typeFromAST(context.getSchema(), node.type);
      if (type !== void 0 && !isInputType(type)) {
        const variableName = node.variable.name.value;
        const typeName = print(node.type);
        context.reportError(
          new GraphQLError(
            `Variable "$${variableName}" cannot be non-input type "${typeName}".`,
            {
              nodes: node.type
            }
          )
        );
      }
    }
  };
}
var init_VariablesAreInputTypesRule = __esm({
  "node_modules/graphql/validation/rules/VariablesAreInputTypesRule.mjs"() {
    init_GraphQLError();
    init_printer();
    init_definition();
    init_typeFromAST();
  }
});

// node_modules/graphql/validation/rules/VariablesInAllowedPositionRule.mjs
function VariablesInAllowedPositionRule(context) {
  let varDefMap = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition: {
      enter() {
        varDefMap = /* @__PURE__ */ Object.create(null);
      },
      leave(operation) {
        const usages = context.getRecursiveVariableUsages(operation);
        for (const { node, type, defaultValue } of usages) {
          const varName = node.name.value;
          const varDef = varDefMap[varName];
          if (varDef && type) {
            const schema = context.getSchema();
            const varType = typeFromAST(schema, varDef.type);
            if (varType && !allowedVariableUsage(
              schema,
              varType,
              varDef.defaultValue,
              type,
              defaultValue
            )) {
              const varTypeStr = inspect(varType);
              const typeStr = inspect(type);
              context.reportError(
                new GraphQLError(
                  `Variable "$${varName}" of type "${varTypeStr}" used in position expecting type "${typeStr}".`,
                  {
                    nodes: [varDef, node]
                  }
                )
              );
            }
          }
        }
      }
    },
    VariableDefinition(node) {
      varDefMap[node.variable.name.value] = node;
    }
  };
}
function allowedVariableUsage(schema, varType, varDefaultValue, locationType, locationDefaultValue) {
  if (isNonNullType(locationType) && !isNonNullType(varType)) {
    const hasNonNullVariableDefaultValue = varDefaultValue != null && varDefaultValue.kind !== Kind.NULL;
    const hasLocationDefaultValue = locationDefaultValue !== void 0;
    if (!hasNonNullVariableDefaultValue && !hasLocationDefaultValue) {
      return false;
    }
    const nullableLocationType = locationType.ofType;
    return isTypeSubTypeOf(schema, varType, nullableLocationType);
  }
  return isTypeSubTypeOf(schema, varType, locationType);
}
var init_VariablesInAllowedPositionRule = __esm({
  "node_modules/graphql/validation/rules/VariablesInAllowedPositionRule.mjs"() {
    init_inspect();
    init_GraphQLError();
    init_kinds();
    init_definition();
    init_typeComparators();
    init_typeFromAST();
  }
});

// node_modules/graphql/validation/specifiedRules.mjs
var recommendedRules, specifiedRules, specifiedSDLRules;
var init_specifiedRules = __esm({
  "node_modules/graphql/validation/specifiedRules.mjs"() {
    init_ExecutableDefinitionsRule();
    init_FieldsOnCorrectTypeRule();
    init_FragmentsOnCompositeTypesRule();
    init_KnownArgumentNamesRule();
    init_KnownDirectivesRule();
    init_KnownFragmentNamesRule();
    init_KnownTypeNamesRule();
    init_LoneAnonymousOperationRule();
    init_LoneSchemaDefinitionRule();
    init_MaxIntrospectionDepthRule();
    init_NoFragmentCyclesRule();
    init_NoUndefinedVariablesRule();
    init_NoUnusedFragmentsRule();
    init_NoUnusedVariablesRule();
    init_OverlappingFieldsCanBeMergedRule();
    init_PossibleFragmentSpreadsRule();
    init_PossibleTypeExtensionsRule();
    init_ProvidedRequiredArgumentsRule();
    init_ScalarLeafsRule();
    init_SingleFieldSubscriptionsRule();
    init_UniqueArgumentDefinitionNamesRule();
    init_UniqueArgumentNamesRule();
    init_UniqueDirectiveNamesRule();
    init_UniqueDirectivesPerLocationRule();
    init_UniqueEnumValueNamesRule();
    init_UniqueFieldDefinitionNamesRule();
    init_UniqueFragmentNamesRule();
    init_UniqueInputFieldNamesRule();
    init_UniqueOperationNamesRule();
    init_UniqueOperationTypesRule();
    init_UniqueTypeNamesRule();
    init_UniqueVariableNamesRule();
    init_ValuesOfCorrectTypeRule();
    init_VariablesAreInputTypesRule();
    init_VariablesInAllowedPositionRule();
    recommendedRules = Object.freeze([MaxIntrospectionDepthRule]);
    specifiedRules = Object.freeze([
      ExecutableDefinitionsRule,
      UniqueOperationNamesRule,
      LoneAnonymousOperationRule,
      SingleFieldSubscriptionsRule,
      KnownTypeNamesRule,
      FragmentsOnCompositeTypesRule,
      VariablesAreInputTypesRule,
      ScalarLeafsRule,
      FieldsOnCorrectTypeRule,
      UniqueFragmentNamesRule,
      KnownFragmentNamesRule,
      NoUnusedFragmentsRule,
      PossibleFragmentSpreadsRule,
      NoFragmentCyclesRule,
      UniqueVariableNamesRule,
      NoUndefinedVariablesRule,
      NoUnusedVariablesRule,
      KnownDirectivesRule,
      UniqueDirectivesPerLocationRule,
      KnownArgumentNamesRule,
      UniqueArgumentNamesRule,
      ValuesOfCorrectTypeRule,
      ProvidedRequiredArgumentsRule,
      VariablesInAllowedPositionRule,
      OverlappingFieldsCanBeMergedRule,
      UniqueInputFieldNamesRule,
      ...recommendedRules
    ]);
    specifiedSDLRules = Object.freeze([
      LoneSchemaDefinitionRule,
      UniqueOperationTypesRule,
      UniqueTypeNamesRule,
      UniqueEnumValueNamesRule,
      UniqueFieldDefinitionNamesRule,
      UniqueArgumentDefinitionNamesRule,
      UniqueDirectiveNamesRule,
      KnownTypeNamesRule,
      KnownDirectivesRule,
      UniqueDirectivesPerLocationRule,
      PossibleTypeExtensionsRule,
      KnownArgumentNamesOnDirectivesRule,
      UniqueArgumentNamesRule,
      UniqueInputFieldNamesRule,
      ProvidedRequiredArgumentsOnDirectivesRule
    ]);
  }
});

// node_modules/graphql/validation/ValidationContext.mjs
var ASTValidationContext, SDLValidationContext, ValidationContext;
var init_ValidationContext = __esm({
  "node_modules/graphql/validation/ValidationContext.mjs"() {
    init_kinds();
    init_visitor();
    init_TypeInfo();
    ASTValidationContext = class {
      constructor(ast, onError) {
        this._ast = ast;
        this._fragments = void 0;
        this._fragmentSpreads = /* @__PURE__ */ new Map();
        this._recursivelyReferencedFragments = /* @__PURE__ */ new Map();
        this._onError = onError;
      }
      get [Symbol.toStringTag]() {
        return "ASTValidationContext";
      }
      reportError(error) {
        this._onError(error);
      }
      getDocument() {
        return this._ast;
      }
      getFragment(name) {
        let fragments;
        if (this._fragments) {
          fragments = this._fragments;
        } else {
          fragments = /* @__PURE__ */ Object.create(null);
          for (const defNode of this.getDocument().definitions) {
            if (defNode.kind === Kind.FRAGMENT_DEFINITION) {
              fragments[defNode.name.value] = defNode;
            }
          }
          this._fragments = fragments;
        }
        return fragments[name];
      }
      getFragmentSpreads(node) {
        let spreads = this._fragmentSpreads.get(node);
        if (!spreads) {
          spreads = [];
          const setsToVisit = [node];
          let set;
          while (set = setsToVisit.pop()) {
            for (const selection of set.selections) {
              if (selection.kind === Kind.FRAGMENT_SPREAD) {
                spreads.push(selection);
              } else if (selection.selectionSet) {
                setsToVisit.push(selection.selectionSet);
              }
            }
          }
          this._fragmentSpreads.set(node, spreads);
        }
        return spreads;
      }
      getRecursivelyReferencedFragments(operation) {
        let fragments = this._recursivelyReferencedFragments.get(operation);
        if (!fragments) {
          fragments = [];
          const collectedNames = /* @__PURE__ */ Object.create(null);
          const nodesToVisit = [operation.selectionSet];
          let node;
          while (node = nodesToVisit.pop()) {
            for (const spread of this.getFragmentSpreads(node)) {
              const fragName = spread.name.value;
              if (collectedNames[fragName] !== true) {
                collectedNames[fragName] = true;
                const fragment = this.getFragment(fragName);
                if (fragment) {
                  fragments.push(fragment);
                  nodesToVisit.push(fragment.selectionSet);
                }
              }
            }
          }
          this._recursivelyReferencedFragments.set(operation, fragments);
        }
        return fragments;
      }
    };
    SDLValidationContext = class extends ASTValidationContext {
      constructor(ast, schema, onError) {
        super(ast, onError);
        this._schema = schema;
      }
      get [Symbol.toStringTag]() {
        return "SDLValidationContext";
      }
      getSchema() {
        return this._schema;
      }
    };
    ValidationContext = class extends ASTValidationContext {
      constructor(schema, ast, typeInfo, onError) {
        super(ast, onError);
        this._schema = schema;
        this._typeInfo = typeInfo;
        this._variableUsages = /* @__PURE__ */ new Map();
        this._recursiveVariableUsages = /* @__PURE__ */ new Map();
      }
      get [Symbol.toStringTag]() {
        return "ValidationContext";
      }
      getSchema() {
        return this._schema;
      }
      getVariableUsages(node) {
        let usages = this._variableUsages.get(node);
        if (!usages) {
          const newUsages = [];
          const typeInfo = new TypeInfo(this._schema);
          visit(
            node,
            visitWithTypeInfo(typeInfo, {
              VariableDefinition: () => false,
              Variable(variable) {
                newUsages.push({
                  node: variable,
                  type: typeInfo.getInputType(),
                  defaultValue: typeInfo.getDefaultValue()
                });
              }
            })
          );
          usages = newUsages;
          this._variableUsages.set(node, usages);
        }
        return usages;
      }
      getRecursiveVariableUsages(operation) {
        let usages = this._recursiveVariableUsages.get(operation);
        if (!usages) {
          usages = this.getVariableUsages(operation);
          for (const frag of this.getRecursivelyReferencedFragments(operation)) {
            usages = usages.concat(this.getVariableUsages(frag));
          }
          this._recursiveVariableUsages.set(operation, usages);
        }
        return usages;
      }
      getType() {
        return this._typeInfo.getType();
      }
      getParentType() {
        return this._typeInfo.getParentType();
      }
      getInputType() {
        return this._typeInfo.getInputType();
      }
      getParentInputType() {
        return this._typeInfo.getParentInputType();
      }
      getFieldDef() {
        return this._typeInfo.getFieldDef();
      }
      getDirective() {
        return this._typeInfo.getDirective();
      }
      getArgument() {
        return this._typeInfo.getArgument();
      }
      getEnumValue() {
        return this._typeInfo.getEnumValue();
      }
    };
  }
});

// node_modules/graphql/validation/validate.mjs
function validate2(schema, documentAST, rules = specifiedRules, options, typeInfo = new TypeInfo(schema)) {
  var _options$maxErrors;
  const maxErrors = (_options$maxErrors = options === null || options === void 0 ? void 0 : options.maxErrors) !== null && _options$maxErrors !== void 0 ? _options$maxErrors : 100;
  documentAST || devAssert(false, "Must provide document.");
  assertValidSchema(schema);
  const abortObj = Object.freeze({});
  const errors = [];
  const context = new ValidationContext(
    schema,
    documentAST,
    typeInfo,
    (error) => {
      if (errors.length >= maxErrors) {
        errors.push(
          new GraphQLError(
            "Too many validation errors, error limit reached. Validation aborted."
          )
        );
        throw abortObj;
      }
      errors.push(error);
    }
  );
  const visitor = visitInParallel(rules.map((rule) => rule(context)));
  try {
    visit(documentAST, visitWithTypeInfo(typeInfo, visitor));
  } catch (e2) {
    if (e2 !== abortObj) {
      throw e2;
    }
  }
  return errors;
}
function validateSDL(documentAST, schemaToExtend, rules = specifiedSDLRules) {
  const errors = [];
  const context = new SDLValidationContext(
    documentAST,
    schemaToExtend,
    (error) => {
      errors.push(error);
    }
  );
  const visitors = rules.map((rule) => rule(context));
  visit(documentAST, visitInParallel(visitors));
  return errors;
}
function assertValidSDL(documentAST) {
  const errors = validateSDL(documentAST);
  if (errors.length !== 0) {
    throw new Error(errors.map((error) => error.message).join("\n\n"));
  }
}
function assertValidSDLExtension(documentAST, schema) {
  const errors = validateSDL(documentAST, schema);
  if (errors.length !== 0) {
    throw new Error(errors.map((error) => error.message).join("\n\n"));
  }
}
var init_validate2 = __esm({
  "node_modules/graphql/validation/validate.mjs"() {
    init_devAssert();
    init_GraphQLError();
    init_visitor();
    init_validate();
    init_TypeInfo();
    init_specifiedRules();
    init_ValidationContext();
  }
});

// node_modules/graphql/jsutils/memoize3.mjs
function memoize3(fn) {
  let cache0;
  return function memoized(a1, a2, a3) {
    if (cache0 === void 0) {
      cache0 = /* @__PURE__ */ new WeakMap();
    }
    let cache1 = cache0.get(a1);
    if (cache1 === void 0) {
      cache1 = /* @__PURE__ */ new WeakMap();
      cache0.set(a1, cache1);
    }
    let cache2 = cache1.get(a2);
    if (cache2 === void 0) {
      cache2 = /* @__PURE__ */ new WeakMap();
      cache1.set(a2, cache2);
    }
    let fnResult = cache2.get(a3);
    if (fnResult === void 0) {
      fnResult = fn(a1, a2, a3);
      cache2.set(a3, fnResult);
    }
    return fnResult;
  };
}
var init_memoize3 = __esm({
  "node_modules/graphql/jsutils/memoize3.mjs"() {
  }
});

// node_modules/graphql/jsutils/promiseForObject.mjs
function promiseForObject(object) {
  return Promise.all(Object.values(object)).then((resolvedValues) => {
    const resolvedObject = /* @__PURE__ */ Object.create(null);
    for (const [i, key] of Object.keys(object).entries()) {
      resolvedObject[key] = resolvedValues[i];
    }
    return resolvedObject;
  });
}
var init_promiseForObject = __esm({
  "node_modules/graphql/jsutils/promiseForObject.mjs"() {
  }
});

// node_modules/graphql/jsutils/promiseReduce.mjs
function promiseReduce(values, callbackFn, initialValue) {
  let accumulator = initialValue;
  for (const value of values) {
    accumulator = isPromise(accumulator) ? accumulator.then((resolved) => callbackFn(resolved, value)) : callbackFn(accumulator, value);
  }
  return accumulator;
}
var init_promiseReduce = __esm({
  "node_modules/graphql/jsutils/promiseReduce.mjs"() {
    init_isPromise();
  }
});

// node_modules/graphql/jsutils/toError.mjs
function toError(thrownValue) {
  return thrownValue instanceof Error ? thrownValue : new NonErrorThrown(thrownValue);
}
var NonErrorThrown;
var init_toError = __esm({
  "node_modules/graphql/jsutils/toError.mjs"() {
    init_inspect();
    NonErrorThrown = class extends Error {
      constructor(thrownValue) {
        super("Unexpected error value: " + inspect(thrownValue));
        this.name = "NonErrorThrown";
        this.thrownValue = thrownValue;
      }
    };
  }
});

// node_modules/graphql/error/locatedError.mjs
function locatedError(rawOriginalError, nodes, path) {
  var _nodes;
  const originalError = toError(rawOriginalError);
  if (isLocatedGraphQLError(originalError)) {
    return originalError;
  }
  return new GraphQLError(originalError.message, {
    nodes: (_nodes = originalError.nodes) !== null && _nodes !== void 0 ? _nodes : nodes,
    source: originalError.source,
    positions: originalError.positions,
    path,
    originalError
  });
}
function isLocatedGraphQLError(error) {
  return Array.isArray(error.path);
}
var init_locatedError = __esm({
  "node_modules/graphql/error/locatedError.mjs"() {
    init_toError();
    init_GraphQLError();
  }
});

// node_modules/graphql/execution/execute.mjs
function execute(args) {
  arguments.length < 2 || devAssert(
    false,
    "graphql@16 dropped long-deprecated support for positional arguments, please pass an object instead."
  );
  const { schema, document: document2, variableValues, rootValue } = args;
  assertValidExecutionArguments(schema, document2, variableValues);
  const exeContext = buildExecutionContext(args);
  if (!("schema" in exeContext)) {
    return {
      errors: exeContext
    };
  }
  try {
    const { operation } = exeContext;
    const result = executeOperation(exeContext, operation, rootValue);
    if (isPromise(result)) {
      return result.then(
        (data) => buildResponse(data, exeContext.errors),
        (error) => {
          exeContext.errors.push(error);
          return buildResponse(null, exeContext.errors);
        }
      );
    }
    return buildResponse(result, exeContext.errors);
  } catch (error) {
    exeContext.errors.push(error);
    return buildResponse(null, exeContext.errors);
  }
}
function executeSync(args) {
  const result = execute(args);
  if (isPromise(result)) {
    throw new Error("GraphQL execution failed to complete synchronously.");
  }
  return result;
}
function buildResponse(data, errors) {
  return errors.length === 0 ? {
    data
  } : {
    errors,
    data
  };
}
function assertValidExecutionArguments(schema, document2, rawVariableValues) {
  document2 || devAssert(false, "Must provide document.");
  assertValidSchema(schema);
  rawVariableValues == null || isObjectLike(rawVariableValues) || devAssert(
    false,
    "Variables must be provided as an Object where each property is a variable value. Perhaps look to see if an unparsed JSON string was provided."
  );
}
function buildExecutionContext(args) {
  var _definition$name, _operation$variableDe;
  const {
    schema,
    document: document2,
    rootValue,
    contextValue,
    variableValues: rawVariableValues,
    operationName,
    fieldResolver,
    typeResolver,
    subscribeFieldResolver
  } = args;
  let operation;
  const fragments = /* @__PURE__ */ Object.create(null);
  for (const definition of document2.definitions) {
    switch (definition.kind) {
      case Kind.OPERATION_DEFINITION:
        if (operationName == null) {
          if (operation !== void 0) {
            return [
              new GraphQLError(
                "Must provide operation name if query contains multiple operations."
              )
            ];
          }
          operation = definition;
        } else if (((_definition$name = definition.name) === null || _definition$name === void 0 ? void 0 : _definition$name.value) === operationName) {
          operation = definition;
        }
        break;
      case Kind.FRAGMENT_DEFINITION:
        fragments[definition.name.value] = definition;
        break;
      default:
    }
  }
  if (!operation) {
    if (operationName != null) {
      return [new GraphQLError(`Unknown operation named "${operationName}".`)];
    }
    return [new GraphQLError("Must provide an operation.")];
  }
  const variableDefinitions = (_operation$variableDe = operation.variableDefinitions) !== null && _operation$variableDe !== void 0 ? _operation$variableDe : [];
  const coercedVariableValues = getVariableValues(
    schema,
    variableDefinitions,
    rawVariableValues !== null && rawVariableValues !== void 0 ? rawVariableValues : {},
    {
      maxErrors: 50
    }
  );
  if (coercedVariableValues.errors) {
    return coercedVariableValues.errors;
  }
  return {
    schema,
    fragments,
    rootValue,
    contextValue,
    operation,
    variableValues: coercedVariableValues.coerced,
    fieldResolver: fieldResolver !== null && fieldResolver !== void 0 ? fieldResolver : defaultFieldResolver,
    typeResolver: typeResolver !== null && typeResolver !== void 0 ? typeResolver : defaultTypeResolver,
    subscribeFieldResolver: subscribeFieldResolver !== null && subscribeFieldResolver !== void 0 ? subscribeFieldResolver : defaultFieldResolver,
    errors: []
  };
}
function executeOperation(exeContext, operation, rootValue) {
  const rootType = exeContext.schema.getRootType(operation.operation);
  if (rootType == null) {
    throw new GraphQLError(
      `Schema is not configured to execute ${operation.operation} operation.`,
      {
        nodes: operation
      }
    );
  }
  const rootFields = collectFields(
    exeContext.schema,
    exeContext.fragments,
    exeContext.variableValues,
    rootType,
    operation.selectionSet
  );
  const path = void 0;
  switch (operation.operation) {
    case OperationTypeNode.QUERY:
      return executeFields(exeContext, rootType, rootValue, path, rootFields);
    case OperationTypeNode.MUTATION:
      return executeFieldsSerially(
        exeContext,
        rootType,
        rootValue,
        path,
        rootFields
      );
    case OperationTypeNode.SUBSCRIPTION:
      return executeFields(exeContext, rootType, rootValue, path, rootFields);
  }
}
function executeFieldsSerially(exeContext, parentType, sourceValue, path, fields) {
  return promiseReduce(
    fields.entries(),
    (results, [responseName, fieldNodes]) => {
      const fieldPath = addPath(path, responseName, parentType.name);
      const result = executeField(
        exeContext,
        parentType,
        sourceValue,
        fieldNodes,
        fieldPath
      );
      if (result === void 0) {
        return results;
      }
      if (isPromise(result)) {
        return result.then((resolvedResult) => {
          results[responseName] = resolvedResult;
          return results;
        });
      }
      results[responseName] = result;
      return results;
    },
    /* @__PURE__ */ Object.create(null)
  );
}
function executeFields(exeContext, parentType, sourceValue, path, fields) {
  const results = /* @__PURE__ */ Object.create(null);
  let containsPromise = false;
  try {
    for (const [responseName, fieldNodes] of fields.entries()) {
      const fieldPath = addPath(path, responseName, parentType.name);
      const result = executeField(
        exeContext,
        parentType,
        sourceValue,
        fieldNodes,
        fieldPath
      );
      if (result !== void 0) {
        results[responseName] = result;
        if (isPromise(result)) {
          containsPromise = true;
        }
      }
    }
  } catch (error) {
    if (containsPromise) {
      return promiseForObject(results).finally(() => {
        throw error;
      });
    }
    throw error;
  }
  if (!containsPromise) {
    return results;
  }
  return promiseForObject(results);
}
function executeField(exeContext, parentType, source, fieldNodes, path) {
  var _fieldDef$resolve;
  const fieldDef = getFieldDef2(exeContext.schema, parentType, fieldNodes[0]);
  if (!fieldDef) {
    return;
  }
  const returnType = fieldDef.type;
  const resolveFn = (_fieldDef$resolve = fieldDef.resolve) !== null && _fieldDef$resolve !== void 0 ? _fieldDef$resolve : exeContext.fieldResolver;
  const info = buildResolveInfo(
    exeContext,
    fieldDef,
    fieldNodes,
    parentType,
    path
  );
  try {
    const args = getArgumentValues(
      fieldDef,
      fieldNodes[0],
      exeContext.variableValues
    );
    const contextValue = exeContext.contextValue;
    const result = resolveFn(source, args, contextValue, info);
    let completed;
    if (isPromise(result)) {
      completed = result.then(
        (resolved) => completeValue(exeContext, returnType, fieldNodes, info, path, resolved)
      );
    } else {
      completed = completeValue(
        exeContext,
        returnType,
        fieldNodes,
        info,
        path,
        result
      );
    }
    if (isPromise(completed)) {
      return completed.then(void 0, (rawError) => {
        const error = locatedError(rawError, fieldNodes, pathToArray(path));
        return handleFieldError(error, returnType, exeContext);
      });
    }
    return completed;
  } catch (rawError) {
    const error = locatedError(rawError, fieldNodes, pathToArray(path));
    return handleFieldError(error, returnType, exeContext);
  }
}
function buildResolveInfo(exeContext, fieldDef, fieldNodes, parentType, path) {
  return {
    fieldName: fieldDef.name,
    fieldNodes,
    returnType: fieldDef.type,
    parentType,
    path,
    schema: exeContext.schema,
    fragments: exeContext.fragments,
    rootValue: exeContext.rootValue,
    operation: exeContext.operation,
    variableValues: exeContext.variableValues
  };
}
function handleFieldError(error, returnType, exeContext) {
  if (isNonNullType(returnType)) {
    throw error;
  }
  exeContext.errors.push(error);
  return null;
}
function completeValue(exeContext, returnType, fieldNodes, info, path, result) {
  if (result instanceof Error) {
    throw result;
  }
  if (isNonNullType(returnType)) {
    const completed = completeValue(
      exeContext,
      returnType.ofType,
      fieldNodes,
      info,
      path,
      result
    );
    if (completed === null) {
      throw new Error(
        `Cannot return null for non-nullable field ${info.parentType.name}.${info.fieldName}.`
      );
    }
    return completed;
  }
  if (result == null) {
    return null;
  }
  if (isListType(returnType)) {
    return completeListValue(
      exeContext,
      returnType,
      fieldNodes,
      info,
      path,
      result
    );
  }
  if (isLeafType(returnType)) {
    return completeLeafValue(returnType, result);
  }
  if (isAbstractType(returnType)) {
    return completeAbstractValue(
      exeContext,
      returnType,
      fieldNodes,
      info,
      path,
      result
    );
  }
  if (isObjectType(returnType)) {
    return completeObjectValue(
      exeContext,
      returnType,
      fieldNodes,
      info,
      path,
      result
    );
  }
  invariant(
    false,
    "Cannot complete value of unexpected output type: " + inspect(returnType)
  );
}
function completeListValue(exeContext, returnType, fieldNodes, info, path, result) {
  if (!isIterableObject(result)) {
    throw new GraphQLError(
      `Expected Iterable, but did not find one for field "${info.parentType.name}.${info.fieldName}".`
    );
  }
  const itemType = returnType.ofType;
  let containsPromise = false;
  const completedResults = Array.from(result, (item, index) => {
    const itemPath = addPath(path, index, void 0);
    try {
      let completedItem;
      if (isPromise(item)) {
        completedItem = item.then(
          (resolved) => completeValue(
            exeContext,
            itemType,
            fieldNodes,
            info,
            itemPath,
            resolved
          )
        );
      } else {
        completedItem = completeValue(
          exeContext,
          itemType,
          fieldNodes,
          info,
          itemPath,
          item
        );
      }
      if (isPromise(completedItem)) {
        containsPromise = true;
        return completedItem.then(void 0, (rawError) => {
          const error = locatedError(
            rawError,
            fieldNodes,
            pathToArray(itemPath)
          );
          return handleFieldError(error, itemType, exeContext);
        });
      }
      return completedItem;
    } catch (rawError) {
      const error = locatedError(rawError, fieldNodes, pathToArray(itemPath));
      return handleFieldError(error, itemType, exeContext);
    }
  });
  return containsPromise ? Promise.all(completedResults) : completedResults;
}
function completeLeafValue(returnType, result) {
  const serializedResult = returnType.serialize(result);
  if (serializedResult == null) {
    throw new Error(
      `Expected \`${inspect(returnType)}.serialize(${inspect(result)})\` to return non-nullable value, returned: ${inspect(serializedResult)}`
    );
  }
  return serializedResult;
}
function completeAbstractValue(exeContext, returnType, fieldNodes, info, path, result) {
  var _returnType$resolveTy;
  const resolveTypeFn = (_returnType$resolveTy = returnType.resolveType) !== null && _returnType$resolveTy !== void 0 ? _returnType$resolveTy : exeContext.typeResolver;
  const contextValue = exeContext.contextValue;
  const runtimeType = resolveTypeFn(result, contextValue, info, returnType);
  if (isPromise(runtimeType)) {
    return runtimeType.then(
      (resolvedRuntimeType) => completeObjectValue(
        exeContext,
        ensureValidRuntimeType(
          resolvedRuntimeType,
          exeContext,
          returnType,
          fieldNodes,
          info,
          result
        ),
        fieldNodes,
        info,
        path,
        result
      )
    );
  }
  return completeObjectValue(
    exeContext,
    ensureValidRuntimeType(
      runtimeType,
      exeContext,
      returnType,
      fieldNodes,
      info,
      result
    ),
    fieldNodes,
    info,
    path,
    result
  );
}
function ensureValidRuntimeType(runtimeTypeName, exeContext, returnType, fieldNodes, info, result) {
  if (runtimeTypeName == null) {
    throw new GraphQLError(
      `Abstract type "${returnType.name}" must resolve to an Object type at runtime for field "${info.parentType.name}.${info.fieldName}". Either the "${returnType.name}" type should provide a "resolveType" function or each possible type should provide an "isTypeOf" function.`,
      fieldNodes
    );
  }
  if (isObjectType(runtimeTypeName)) {
    throw new GraphQLError(
      "Support for returning GraphQLObjectType from resolveType was removed in graphql-js@16.0.0 please return type name instead."
    );
  }
  if (typeof runtimeTypeName !== "string") {
    throw new GraphQLError(
      `Abstract type "${returnType.name}" must resolve to an Object type at runtime for field "${info.parentType.name}.${info.fieldName}" with value ${inspect(result)}, received "${inspect(runtimeTypeName)}".`
    );
  }
  const runtimeType = exeContext.schema.getType(runtimeTypeName);
  if (runtimeType == null) {
    throw new GraphQLError(
      `Abstract type "${returnType.name}" was resolved to a type "${runtimeTypeName}" that does not exist inside the schema.`,
      {
        nodes: fieldNodes
      }
    );
  }
  if (!isObjectType(runtimeType)) {
    throw new GraphQLError(
      `Abstract type "${returnType.name}" was resolved to a non-object type "${runtimeTypeName}".`,
      {
        nodes: fieldNodes
      }
    );
  }
  if (!exeContext.schema.isSubType(returnType, runtimeType)) {
    throw new GraphQLError(
      `Runtime Object type "${runtimeType.name}" is not a possible type for "${returnType.name}".`,
      {
        nodes: fieldNodes
      }
    );
  }
  return runtimeType;
}
function completeObjectValue(exeContext, returnType, fieldNodes, info, path, result) {
  const subFieldNodes = collectSubfields2(exeContext, returnType, fieldNodes);
  if (returnType.isTypeOf) {
    const isTypeOf = returnType.isTypeOf(result, exeContext.contextValue, info);
    if (isPromise(isTypeOf)) {
      return isTypeOf.then((resolvedIsTypeOf) => {
        if (!resolvedIsTypeOf) {
          throw invalidReturnTypeError(returnType, result, fieldNodes);
        }
        return executeFields(
          exeContext,
          returnType,
          result,
          path,
          subFieldNodes
        );
      });
    }
    if (!isTypeOf) {
      throw invalidReturnTypeError(returnType, result, fieldNodes);
    }
  }
  return executeFields(exeContext, returnType, result, path, subFieldNodes);
}
function invalidReturnTypeError(returnType, result, fieldNodes) {
  return new GraphQLError(
    `Expected value of type "${returnType.name}" but got: ${inspect(result)}.`,
    {
      nodes: fieldNodes
    }
  );
}
function getFieldDef2(schema, parentType, fieldNode) {
  const fieldName = fieldNode.name.value;
  if (fieldName === SchemaMetaFieldDef.name && schema.getQueryType() === parentType) {
    return SchemaMetaFieldDef;
  } else if (fieldName === TypeMetaFieldDef.name && schema.getQueryType() === parentType) {
    return TypeMetaFieldDef;
  } else if (fieldName === TypeNameMetaFieldDef.name) {
    return TypeNameMetaFieldDef;
  }
  return parentType.getFields()[fieldName];
}
var collectSubfields2, defaultTypeResolver, defaultFieldResolver;
var init_execute = __esm({
  "node_modules/graphql/execution/execute.mjs"() {
    init_devAssert();
    init_inspect();
    init_invariant();
    init_isIterableObject();
    init_isObjectLike();
    init_isPromise();
    init_memoize3();
    init_Path();
    init_promiseForObject();
    init_promiseReduce();
    init_GraphQLError();
    init_locatedError();
    init_ast();
    init_kinds();
    init_definition();
    init_introspection();
    init_validate();
    init_collectFields();
    init_values();
    collectSubfields2 = memoize3(
      (exeContext, returnType, fieldNodes) => collectSubfields(
        exeContext.schema,
        exeContext.fragments,
        exeContext.variableValues,
        returnType,
        fieldNodes
      )
    );
    defaultTypeResolver = function(value, contextValue, info, abstractType) {
      if (isObjectLike(value) && typeof value.__typename === "string") {
        return value.__typename;
      }
      const possibleTypes = info.schema.getPossibleTypes(abstractType);
      const promisedIsTypeOfResults = [];
      for (let i = 0; i < possibleTypes.length; i++) {
        const type = possibleTypes[i];
        if (type.isTypeOf) {
          const isTypeOfResult = type.isTypeOf(value, contextValue, info);
          if (isPromise(isTypeOfResult)) {
            promisedIsTypeOfResults[i] = isTypeOfResult;
          } else if (isTypeOfResult) {
            return type.name;
          }
        }
      }
      if (promisedIsTypeOfResults.length) {
        return Promise.all(promisedIsTypeOfResults).then((isTypeOfResults) => {
          for (let i = 0; i < isTypeOfResults.length; i++) {
            if (isTypeOfResults[i]) {
              return possibleTypes[i].name;
            }
          }
        });
      }
    };
    defaultFieldResolver = function(source, args, contextValue, info) {
      if (isObjectLike(source) || typeof source === "function") {
        const property = source[info.fieldName];
        if (typeof property === "function") {
          return source[info.fieldName](args, contextValue, info);
        }
        return property;
      }
    };
  }
});

// node_modules/graphql/graphql.mjs
function graphql(args) {
  return new Promise((resolve) => resolve(graphqlImpl(args)));
}
function graphqlSync(args) {
  const result = graphqlImpl(args);
  if (isPromise(result)) {
    throw new Error("GraphQL execution failed to complete synchronously.");
  }
  return result;
}
function graphqlImpl(args) {
  arguments.length < 2 || devAssert(
    false,
    "graphql@16 dropped long-deprecated support for positional arguments, please pass an object instead."
  );
  const {
    schema,
    source,
    rootValue,
    contextValue,
    variableValues,
    operationName,
    fieldResolver,
    typeResolver
  } = args;
  const schemaValidationErrors = validateSchema(schema);
  if (schemaValidationErrors.length > 0) {
    return {
      errors: schemaValidationErrors
    };
  }
  let document2;
  try {
    document2 = parse(source);
  } catch (syntaxError2) {
    return {
      errors: [syntaxError2]
    };
  }
  const validationErrors = validate2(schema, document2);
  if (validationErrors.length > 0) {
    return {
      errors: validationErrors
    };
  }
  return execute({
    schema,
    document: document2,
    rootValue,
    contextValue,
    variableValues,
    operationName,
    fieldResolver,
    typeResolver
  });
}
var init_graphql = __esm({
  "node_modules/graphql/graphql.mjs"() {
    init_devAssert();
    init_isPromise();
    init_parser();
    init_validate();
    init_validate2();
    init_execute();
  }
});

// node_modules/graphql/type/index.mjs
var init_type = __esm({
  "node_modules/graphql/type/index.mjs"() {
    init_schema();
    init_definition();
    init_directives();
    init_scalars();
    init_introspection();
    init_validate();
    init_assertName();
  }
});

// node_modules/graphql/language/index.mjs
var init_language = __esm({
  "node_modules/graphql/language/index.mjs"() {
    init_source();
    init_location();
    init_printLocation();
    init_kinds();
    init_tokenKind();
    init_lexer();
    init_parser();
    init_printer();
    init_visitor();
    init_ast();
    init_predicates();
    init_directiveLocation();
  }
});

// node_modules/graphql/jsutils/isAsyncIterable.mjs
function isAsyncIterable(maybeAsyncIterable) {
  return typeof (maybeAsyncIterable === null || maybeAsyncIterable === void 0 ? void 0 : maybeAsyncIterable[Symbol.asyncIterator]) === "function";
}
var init_isAsyncIterable = __esm({
  "node_modules/graphql/jsutils/isAsyncIterable.mjs"() {
  }
});

// node_modules/graphql/execution/mapAsyncIterator.mjs
function mapAsyncIterator(iterable, callback) {
  const iterator = iterable[Symbol.asyncIterator]();
  async function mapResult(result) {
    if (result.done) {
      return result;
    }
    try {
      return {
        value: await callback(result.value),
        done: false
      };
    } catch (error) {
      if (typeof iterator.return === "function") {
        try {
          await iterator.return();
        } catch (_e) {
        }
      }
      throw error;
    }
  }
  return {
    async next() {
      return mapResult(await iterator.next());
    },
    async return() {
      return typeof iterator.return === "function" ? mapResult(await iterator.return()) : {
        value: void 0,
        done: true
      };
    },
    async throw(error) {
      if (typeof iterator.throw === "function") {
        return mapResult(await iterator.throw(error));
      }
      throw error;
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
var init_mapAsyncIterator = __esm({
  "node_modules/graphql/execution/mapAsyncIterator.mjs"() {
  }
});

// node_modules/graphql/execution/subscribe.mjs
async function subscribe(args) {
  arguments.length < 2 || devAssert(
    false,
    "graphql@16 dropped long-deprecated support for positional arguments, please pass an object instead."
  );
  const resultOrStream = await createSourceEventStream(args);
  if (!isAsyncIterable(resultOrStream)) {
    return resultOrStream;
  }
  const mapSourceToResponse = (payload) => execute({ ...args, rootValue: payload });
  return mapAsyncIterator(resultOrStream, mapSourceToResponse);
}
function toNormalizedArgs(args) {
  const firstArg = args[0];
  if (firstArg && "document" in firstArg) {
    return firstArg;
  }
  return {
    schema: firstArg,
    // FIXME: when underlying TS bug fixed, see https://github.com/microsoft/TypeScript/issues/31613
    document: args[1],
    rootValue: args[2],
    contextValue: args[3],
    variableValues: args[4],
    operationName: args[5],
    subscribeFieldResolver: args[6]
  };
}
async function createSourceEventStream(...rawArgs) {
  const args = toNormalizedArgs(rawArgs);
  const { schema, document: document2, variableValues } = args;
  assertValidExecutionArguments(schema, document2, variableValues);
  const exeContext = buildExecutionContext(args);
  if (!("schema" in exeContext)) {
    return {
      errors: exeContext
    };
  }
  try {
    const eventStream = await executeSubscription(exeContext);
    if (!isAsyncIterable(eventStream)) {
      throw new Error(
        `Subscription field must return Async Iterable. Received: ${inspect(eventStream)}.`
      );
    }
    return eventStream;
  } catch (error) {
    if (error instanceof GraphQLError) {
      return {
        errors: [error]
      };
    }
    throw error;
  }
}
async function executeSubscription(exeContext) {
  const { schema, fragments, operation, variableValues, rootValue } = exeContext;
  const rootType = schema.getSubscriptionType();
  if (rootType == null) {
    throw new GraphQLError(
      "Schema is not configured to execute subscription operation.",
      {
        nodes: operation
      }
    );
  }
  const rootFields = collectFields(
    schema,
    fragments,
    variableValues,
    rootType,
    operation.selectionSet
  );
  const [responseName, fieldNodes] = [...rootFields.entries()][0];
  const fieldDef = getFieldDef2(schema, rootType, fieldNodes[0]);
  if (!fieldDef) {
    const fieldName = fieldNodes[0].name.value;
    throw new GraphQLError(
      `The subscription field "${fieldName}" is not defined.`,
      {
        nodes: fieldNodes
      }
    );
  }
  const path = addPath(void 0, responseName, rootType.name);
  const info = buildResolveInfo(
    exeContext,
    fieldDef,
    fieldNodes,
    rootType,
    path
  );
  try {
    var _fieldDef$subscribe;
    const args = getArgumentValues(fieldDef, fieldNodes[0], variableValues);
    const contextValue = exeContext.contextValue;
    const resolveFn = (_fieldDef$subscribe = fieldDef.subscribe) !== null && _fieldDef$subscribe !== void 0 ? _fieldDef$subscribe : exeContext.subscribeFieldResolver;
    const eventStream = await resolveFn(rootValue, args, contextValue, info);
    if (eventStream instanceof Error) {
      throw eventStream;
    }
    return eventStream;
  } catch (error) {
    throw locatedError(error, fieldNodes, pathToArray(path));
  }
}
var init_subscribe = __esm({
  "node_modules/graphql/execution/subscribe.mjs"() {
    init_devAssert();
    init_inspect();
    init_isAsyncIterable();
    init_Path();
    init_GraphQLError();
    init_locatedError();
    init_collectFields();
    init_execute();
    init_mapAsyncIterator();
    init_values();
  }
});

// node_modules/graphql/execution/index.mjs
var init_execution = __esm({
  "node_modules/graphql/execution/index.mjs"() {
    init_Path();
    init_execute();
    init_subscribe();
    init_values();
  }
});

// node_modules/graphql/validation/rules/custom/NoDeprecatedCustomRule.mjs
function NoDeprecatedCustomRule(context) {
  return {
    Field(node) {
      const fieldDef = context.getFieldDef();
      const deprecationReason = fieldDef === null || fieldDef === void 0 ? void 0 : fieldDef.deprecationReason;
      if (fieldDef && deprecationReason != null) {
        const parentType = context.getParentType();
        parentType != null || invariant(false);
        context.reportError(
          new GraphQLError(
            `The field ${parentType.name}.${fieldDef.name} is deprecated. ${deprecationReason}`,
            {
              nodes: node
            }
          )
        );
      }
    },
    Argument(node) {
      const argDef = context.getArgument();
      const deprecationReason = argDef === null || argDef === void 0 ? void 0 : argDef.deprecationReason;
      if (argDef && deprecationReason != null) {
        const directiveDef = context.getDirective();
        if (directiveDef != null) {
          context.reportError(
            new GraphQLError(
              `Directive "@${directiveDef.name}" argument "${argDef.name}" is deprecated. ${deprecationReason}`,
              {
                nodes: node
              }
            )
          );
        } else {
          const parentType = context.getParentType();
          const fieldDef = context.getFieldDef();
          parentType != null && fieldDef != null || invariant(false);
          context.reportError(
            new GraphQLError(
              `Field "${parentType.name}.${fieldDef.name}" argument "${argDef.name}" is deprecated. ${deprecationReason}`,
              {
                nodes: node
              }
            )
          );
        }
      }
    },
    ObjectField(node) {
      const inputObjectDef = getNamedType(context.getParentInputType());
      if (isInputObjectType(inputObjectDef)) {
        const inputFieldDef = inputObjectDef.getFields()[node.name.value];
        const deprecationReason = inputFieldDef === null || inputFieldDef === void 0 ? void 0 : inputFieldDef.deprecationReason;
        if (deprecationReason != null) {
          context.reportError(
            new GraphQLError(
              `The input field ${inputObjectDef.name}.${inputFieldDef.name} is deprecated. ${deprecationReason}`,
              {
                nodes: node
              }
            )
          );
        }
      }
    },
    EnumValue(node) {
      const enumValueDef = context.getEnumValue();
      const deprecationReason = enumValueDef === null || enumValueDef === void 0 ? void 0 : enumValueDef.deprecationReason;
      if (enumValueDef && deprecationReason != null) {
        const enumTypeDef = getNamedType(context.getInputType());
        enumTypeDef != null || invariant(false);
        context.reportError(
          new GraphQLError(
            `The enum value "${enumTypeDef.name}.${enumValueDef.name}" is deprecated. ${deprecationReason}`,
            {
              nodes: node
            }
          )
        );
      }
    }
  };
}
var init_NoDeprecatedCustomRule = __esm({
  "node_modules/graphql/validation/rules/custom/NoDeprecatedCustomRule.mjs"() {
    init_invariant();
    init_GraphQLError();
    init_definition();
  }
});

// node_modules/graphql/validation/rules/custom/NoSchemaIntrospectionCustomRule.mjs
function NoSchemaIntrospectionCustomRule(context) {
  return {
    Field(node) {
      const type = getNamedType(context.getType());
      if (type && isIntrospectionType(type)) {
        context.reportError(
          new GraphQLError(
            `GraphQL introspection has been disabled, but the requested query contained the field "${node.name.value}".`,
            {
              nodes: node
            }
          )
        );
      }
    }
  };
}
var init_NoSchemaIntrospectionCustomRule = __esm({
  "node_modules/graphql/validation/rules/custom/NoSchemaIntrospectionCustomRule.mjs"() {
    init_GraphQLError();
    init_definition();
    init_introspection();
  }
});

// node_modules/graphql/validation/index.mjs
var init_validation = __esm({
  "node_modules/graphql/validation/index.mjs"() {
    init_validate2();
    init_ValidationContext();
    init_specifiedRules();
    init_ExecutableDefinitionsRule();
    init_FieldsOnCorrectTypeRule();
    init_FragmentsOnCompositeTypesRule();
    init_KnownArgumentNamesRule();
    init_KnownDirectivesRule();
    init_KnownFragmentNamesRule();
    init_KnownTypeNamesRule();
    init_LoneAnonymousOperationRule();
    init_NoFragmentCyclesRule();
    init_NoUndefinedVariablesRule();
    init_NoUnusedFragmentsRule();
    init_NoUnusedVariablesRule();
    init_OverlappingFieldsCanBeMergedRule();
    init_PossibleFragmentSpreadsRule();
    init_ProvidedRequiredArgumentsRule();
    init_ScalarLeafsRule();
    init_SingleFieldSubscriptionsRule();
    init_UniqueArgumentNamesRule();
    init_UniqueDirectivesPerLocationRule();
    init_UniqueFragmentNamesRule();
    init_UniqueInputFieldNamesRule();
    init_UniqueOperationNamesRule();
    init_UniqueVariableNamesRule();
    init_ValuesOfCorrectTypeRule();
    init_VariablesAreInputTypesRule();
    init_VariablesInAllowedPositionRule();
    init_MaxIntrospectionDepthRule();
    init_LoneSchemaDefinitionRule();
    init_UniqueOperationTypesRule();
    init_UniqueTypeNamesRule();
    init_UniqueEnumValueNamesRule();
    init_UniqueFieldDefinitionNamesRule();
    init_UniqueArgumentDefinitionNamesRule();
    init_UniqueDirectiveNamesRule();
    init_PossibleTypeExtensionsRule();
    init_NoDeprecatedCustomRule();
    init_NoSchemaIntrospectionCustomRule();
  }
});

// node_modules/graphql/error/index.mjs
var init_error = __esm({
  "node_modules/graphql/error/index.mjs"() {
    init_GraphQLError();
    init_syntaxError();
    init_locatedError();
  }
});

// node_modules/graphql/utilities/getIntrospectionQuery.mjs
function getIntrospectionQuery(options) {
  const optionsWithDefault = {
    descriptions: true,
    specifiedByUrl: false,
    directiveIsRepeatable: false,
    schemaDescription: false,
    inputValueDeprecation: false,
    oneOf: false,
    ...options
  };
  const descriptions = optionsWithDefault.descriptions ? "description" : "";
  const specifiedByUrl = optionsWithDefault.specifiedByUrl ? "specifiedByURL" : "";
  const directiveIsRepeatable = optionsWithDefault.directiveIsRepeatable ? "isRepeatable" : "";
  const schemaDescription = optionsWithDefault.schemaDescription ? descriptions : "";
  function inputDeprecation(str) {
    return optionsWithDefault.inputValueDeprecation ? str : "";
  }
  const oneOf = optionsWithDefault.oneOf ? "isOneOf" : "";
  return `
    query IntrospectionQuery {
      __schema {
        ${schemaDescription}
        queryType { name }
        mutationType { name }
        subscriptionType { name }
        types {
          ...FullType
        }
        directives {
          name
          ${descriptions}
          ${directiveIsRepeatable}
          locations
          args${inputDeprecation("(includeDeprecated: true)")} {
            ...InputValue
          }
        }
      }
    }

    fragment FullType on __Type {
      kind
      name
      ${descriptions}
      ${specifiedByUrl}
      ${oneOf}
      fields(includeDeprecated: true) {
        name
        ${descriptions}
        args${inputDeprecation("(includeDeprecated: true)")} {
          ...InputValue
        }
        type {
          ...TypeRef
        }
        isDeprecated
        deprecationReason
      }
      inputFields${inputDeprecation("(includeDeprecated: true)")} {
        ...InputValue
      }
      interfaces {
        ...TypeRef
      }
      enumValues(includeDeprecated: true) {
        name
        ${descriptions}
        isDeprecated
        deprecationReason
      }
      possibleTypes {
        ...TypeRef
      }
    }

    fragment InputValue on __InputValue {
      name
      ${descriptions}
      type { ...TypeRef }
      defaultValue
      ${inputDeprecation("isDeprecated")}
      ${inputDeprecation("deprecationReason")}
    }

    fragment TypeRef on __Type {
      kind
      name
      ofType {
        kind
        name
        ofType {
          kind
          name
          ofType {
            kind
            name
            ofType {
              kind
              name
              ofType {
                kind
                name
                ofType {
                  kind
                  name
                  ofType {
                    kind
                    name
                    ofType {
                      kind
                      name
                      ofType {
                        kind
                        name
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  `;
}
var init_getIntrospectionQuery = __esm({
  "node_modules/graphql/utilities/getIntrospectionQuery.mjs"() {
  }
});

// node_modules/graphql/utilities/getOperationAST.mjs
function getOperationAST(documentAST, operationName) {
  let operation = null;
  for (const definition of documentAST.definitions) {
    if (definition.kind === Kind.OPERATION_DEFINITION) {
      var _definition$name;
      if (operationName == null) {
        if (operation) {
          return null;
        }
        operation = definition;
      } else if (((_definition$name = definition.name) === null || _definition$name === void 0 ? void 0 : _definition$name.value) === operationName) {
        return definition;
      }
    }
  }
  return operation;
}
var init_getOperationAST = __esm({
  "node_modules/graphql/utilities/getOperationAST.mjs"() {
    init_kinds();
  }
});

// node_modules/graphql/utilities/getOperationRootType.mjs
function getOperationRootType(schema, operation) {
  if (operation.operation === "query") {
    const queryType = schema.getQueryType();
    if (!queryType) {
      throw new GraphQLError(
        "Schema does not define the required query root type.",
        {
          nodes: operation
        }
      );
    }
    return queryType;
  }
  if (operation.operation === "mutation") {
    const mutationType = schema.getMutationType();
    if (!mutationType) {
      throw new GraphQLError("Schema is not configured for mutations.", {
        nodes: operation
      });
    }
    return mutationType;
  }
  if (operation.operation === "subscription") {
    const subscriptionType = schema.getSubscriptionType();
    if (!subscriptionType) {
      throw new GraphQLError("Schema is not configured for subscriptions.", {
        nodes: operation
      });
    }
    return subscriptionType;
  }
  throw new GraphQLError(
    "Can only have query, mutation and subscription operations.",
    {
      nodes: operation
    }
  );
}
var init_getOperationRootType = __esm({
  "node_modules/graphql/utilities/getOperationRootType.mjs"() {
    init_GraphQLError();
  }
});

// node_modules/graphql/utilities/introspectionFromSchema.mjs
function introspectionFromSchema(schema, options) {
  const optionsWithDefaults = {
    specifiedByUrl: true,
    directiveIsRepeatable: true,
    schemaDescription: true,
    inputValueDeprecation: true,
    oneOf: true,
    ...options
  };
  const document2 = parse(getIntrospectionQuery(optionsWithDefaults));
  const result = executeSync({
    schema,
    document: document2
  });
  !result.errors && result.data || invariant(false);
  return result.data;
}
var init_introspectionFromSchema = __esm({
  "node_modules/graphql/utilities/introspectionFromSchema.mjs"() {
    init_invariant();
    init_parser();
    init_execute();
    init_getIntrospectionQuery();
  }
});

// node_modules/graphql/utilities/buildClientSchema.mjs
function buildClientSchema(introspection, options) {
  isObjectLike(introspection) && isObjectLike(introspection.__schema) || devAssert(
    false,
    `Invalid or incomplete introspection result. Ensure that you are passing "data" property of introspection response and no "errors" was returned alongside: ${inspect(
      introspection
    )}.`
  );
  const schemaIntrospection = introspection.__schema;
  const typeMap = keyValMap(
    schemaIntrospection.types,
    (typeIntrospection) => typeIntrospection.name,
    (typeIntrospection) => buildType(typeIntrospection)
  );
  for (const stdType of [...specifiedScalarTypes, ...introspectionTypes]) {
    if (typeMap[stdType.name]) {
      typeMap[stdType.name] = stdType;
    }
  }
  const queryType = schemaIntrospection.queryType ? getObjectType(schemaIntrospection.queryType) : null;
  const mutationType = schemaIntrospection.mutationType ? getObjectType(schemaIntrospection.mutationType) : null;
  const subscriptionType = schemaIntrospection.subscriptionType ? getObjectType(schemaIntrospection.subscriptionType) : null;
  const directives = schemaIntrospection.directives ? schemaIntrospection.directives.map(buildDirective) : [];
  return new GraphQLSchema({
    description: schemaIntrospection.description,
    query: queryType,
    mutation: mutationType,
    subscription: subscriptionType,
    types: Object.values(typeMap),
    directives,
    assumeValid: options === null || options === void 0 ? void 0 : options.assumeValid
  });
  function getType(typeRef) {
    if (typeRef.kind === TypeKind.LIST) {
      const itemRef = typeRef.ofType;
      if (!itemRef) {
        throw new Error("Decorated type deeper than introspection query.");
      }
      return new GraphQLList(getType(itemRef));
    }
    if (typeRef.kind === TypeKind.NON_NULL) {
      const nullableRef = typeRef.ofType;
      if (!nullableRef) {
        throw new Error("Decorated type deeper than introspection query.");
      }
      const nullableType = getType(nullableRef);
      return new GraphQLNonNull(assertNullableType(nullableType));
    }
    return getNamedType2(typeRef);
  }
  function getNamedType2(typeRef) {
    const typeName = typeRef.name;
    if (!typeName) {
      throw new Error(`Unknown type reference: ${inspect(typeRef)}.`);
    }
    const type = typeMap[typeName];
    if (!type) {
      throw new Error(
        `Invalid or incomplete schema, unknown type: ${typeName}. Ensure that a full introspection query is used in order to build a client schema.`
      );
    }
    return type;
  }
  function getObjectType(typeRef) {
    return assertObjectType(getNamedType2(typeRef));
  }
  function getInterfaceType(typeRef) {
    return assertInterfaceType(getNamedType2(typeRef));
  }
  function buildType(type) {
    if (type != null && type.name != null && type.kind != null) {
      switch (type.kind) {
        case TypeKind.SCALAR:
          return buildScalarDef(type);
        case TypeKind.OBJECT:
          return buildObjectDef(type);
        case TypeKind.INTERFACE:
          return buildInterfaceDef(type);
        case TypeKind.UNION:
          return buildUnionDef(type);
        case TypeKind.ENUM:
          return buildEnumDef(type);
        case TypeKind.INPUT_OBJECT:
          return buildInputObjectDef(type);
      }
    }
    const typeStr = inspect(type);
    throw new Error(
      `Invalid or incomplete introspection result. Ensure that a full introspection query is used in order to build a client schema: ${typeStr}.`
    );
  }
  function buildScalarDef(scalarIntrospection) {
    return new GraphQLScalarType({
      name: scalarIntrospection.name,
      description: scalarIntrospection.description,
      specifiedByURL: scalarIntrospection.specifiedByURL
    });
  }
  function buildImplementationsList(implementingIntrospection) {
    if (implementingIntrospection.interfaces === null && implementingIntrospection.kind === TypeKind.INTERFACE) {
      return [];
    }
    if (!implementingIntrospection.interfaces) {
      const implementingIntrospectionStr = inspect(implementingIntrospection);
      throw new Error(
        `Introspection result missing interfaces: ${implementingIntrospectionStr}.`
      );
    }
    return implementingIntrospection.interfaces.map(getInterfaceType);
  }
  function buildObjectDef(objectIntrospection) {
    return new GraphQLObjectType({
      name: objectIntrospection.name,
      description: objectIntrospection.description,
      interfaces: () => buildImplementationsList(objectIntrospection),
      fields: () => buildFieldDefMap(objectIntrospection)
    });
  }
  function buildInterfaceDef(interfaceIntrospection) {
    return new GraphQLInterfaceType({
      name: interfaceIntrospection.name,
      description: interfaceIntrospection.description,
      interfaces: () => buildImplementationsList(interfaceIntrospection),
      fields: () => buildFieldDefMap(interfaceIntrospection)
    });
  }
  function buildUnionDef(unionIntrospection) {
    if (!unionIntrospection.possibleTypes) {
      const unionIntrospectionStr = inspect(unionIntrospection);
      throw new Error(
        `Introspection result missing possibleTypes: ${unionIntrospectionStr}.`
      );
    }
    return new GraphQLUnionType({
      name: unionIntrospection.name,
      description: unionIntrospection.description,
      types: () => unionIntrospection.possibleTypes.map(getObjectType)
    });
  }
  function buildEnumDef(enumIntrospection) {
    if (!enumIntrospection.enumValues) {
      const enumIntrospectionStr = inspect(enumIntrospection);
      throw new Error(
        `Introspection result missing enumValues: ${enumIntrospectionStr}.`
      );
    }
    return new GraphQLEnumType({
      name: enumIntrospection.name,
      description: enumIntrospection.description,
      values: keyValMap(
        enumIntrospection.enumValues,
        (valueIntrospection) => valueIntrospection.name,
        (valueIntrospection) => ({
          description: valueIntrospection.description,
          deprecationReason: valueIntrospection.deprecationReason
        })
      )
    });
  }
  function buildInputObjectDef(inputObjectIntrospection) {
    if (!inputObjectIntrospection.inputFields) {
      const inputObjectIntrospectionStr = inspect(inputObjectIntrospection);
      throw new Error(
        `Introspection result missing inputFields: ${inputObjectIntrospectionStr}.`
      );
    }
    return new GraphQLInputObjectType({
      name: inputObjectIntrospection.name,
      description: inputObjectIntrospection.description,
      fields: () => buildInputValueDefMap(inputObjectIntrospection.inputFields),
      isOneOf: inputObjectIntrospection.isOneOf
    });
  }
  function buildFieldDefMap(typeIntrospection) {
    if (!typeIntrospection.fields) {
      throw new Error(
        `Introspection result missing fields: ${inspect(typeIntrospection)}.`
      );
    }
    return keyValMap(
      typeIntrospection.fields,
      (fieldIntrospection) => fieldIntrospection.name,
      buildField
    );
  }
  function buildField(fieldIntrospection) {
    const type = getType(fieldIntrospection.type);
    if (!isOutputType(type)) {
      const typeStr = inspect(type);
      throw new Error(
        `Introspection must provide output type for fields, but received: ${typeStr}.`
      );
    }
    if (!fieldIntrospection.args) {
      const fieldIntrospectionStr = inspect(fieldIntrospection);
      throw new Error(
        `Introspection result missing field args: ${fieldIntrospectionStr}.`
      );
    }
    return {
      description: fieldIntrospection.description,
      deprecationReason: fieldIntrospection.deprecationReason,
      type,
      args: buildInputValueDefMap(fieldIntrospection.args)
    };
  }
  function buildInputValueDefMap(inputValueIntrospections) {
    return keyValMap(
      inputValueIntrospections,
      (inputValue) => inputValue.name,
      buildInputValue
    );
  }
  function buildInputValue(inputValueIntrospection) {
    const type = getType(inputValueIntrospection.type);
    if (!isInputType(type)) {
      const typeStr = inspect(type);
      throw new Error(
        `Introspection must provide input type for arguments, but received: ${typeStr}.`
      );
    }
    const defaultValue = inputValueIntrospection.defaultValue != null ? valueFromAST(parseValue(inputValueIntrospection.defaultValue), type) : void 0;
    return {
      description: inputValueIntrospection.description,
      type,
      defaultValue,
      deprecationReason: inputValueIntrospection.deprecationReason
    };
  }
  function buildDirective(directiveIntrospection) {
    if (!directiveIntrospection.args) {
      const directiveIntrospectionStr = inspect(directiveIntrospection);
      throw new Error(
        `Introspection result missing directive args: ${directiveIntrospectionStr}.`
      );
    }
    if (!directiveIntrospection.locations) {
      const directiveIntrospectionStr = inspect(directiveIntrospection);
      throw new Error(
        `Introspection result missing directive locations: ${directiveIntrospectionStr}.`
      );
    }
    return new GraphQLDirective({
      name: directiveIntrospection.name,
      description: directiveIntrospection.description,
      isRepeatable: directiveIntrospection.isRepeatable,
      locations: directiveIntrospection.locations.slice(),
      args: buildInputValueDefMap(directiveIntrospection.args)
    });
  }
}
var init_buildClientSchema = __esm({
  "node_modules/graphql/utilities/buildClientSchema.mjs"() {
    init_devAssert();
    init_inspect();
    init_isObjectLike();
    init_keyValMap();
    init_parser();
    init_definition();
    init_directives();
    init_introspection();
    init_scalars();
    init_schema();
    init_valueFromAST();
  }
});

// node_modules/graphql/utilities/extendSchema.mjs
function extendSchema(schema, documentAST, options) {
  assertSchema(schema);
  documentAST != null && documentAST.kind === Kind.DOCUMENT || devAssert(false, "Must provide valid Document AST.");
  if ((options === null || options === void 0 ? void 0 : options.assumeValid) !== true && (options === null || options === void 0 ? void 0 : options.assumeValidSDL) !== true) {
    assertValidSDLExtension(documentAST, schema);
  }
  const schemaConfig = schema.toConfig();
  const extendedConfig = extendSchemaImpl(schemaConfig, documentAST, options);
  return schemaConfig === extendedConfig ? schema : new GraphQLSchema(extendedConfig);
}
function extendSchemaImpl(schemaConfig, documentAST, options) {
  var _schemaDef, _schemaDef$descriptio, _schemaDef2, _options$assumeValid;
  const typeDefs = [];
  const typeExtensionsMap = /* @__PURE__ */ Object.create(null);
  const directiveDefs = [];
  let schemaDef;
  const schemaExtensions = [];
  for (const def of documentAST.definitions) {
    if (def.kind === Kind.SCHEMA_DEFINITION) {
      schemaDef = def;
    } else if (def.kind === Kind.SCHEMA_EXTENSION) {
      schemaExtensions.push(def);
    } else if (isTypeDefinitionNode(def)) {
      typeDefs.push(def);
    } else if (isTypeExtensionNode(def)) {
      const extendedTypeName = def.name.value;
      const existingTypeExtensions = typeExtensionsMap[extendedTypeName];
      typeExtensionsMap[extendedTypeName] = existingTypeExtensions ? existingTypeExtensions.concat([def]) : [def];
    } else if (def.kind === Kind.DIRECTIVE_DEFINITION) {
      directiveDefs.push(def);
    }
  }
  if (Object.keys(typeExtensionsMap).length === 0 && typeDefs.length === 0 && directiveDefs.length === 0 && schemaExtensions.length === 0 && schemaDef == null) {
    return schemaConfig;
  }
  const typeMap = /* @__PURE__ */ Object.create(null);
  for (const existingType of schemaConfig.types) {
    typeMap[existingType.name] = extendNamedType(existingType);
  }
  for (const typeNode of typeDefs) {
    var _stdTypeMap$name;
    const name = typeNode.name.value;
    typeMap[name] = (_stdTypeMap$name = stdTypeMap[name]) !== null && _stdTypeMap$name !== void 0 ? _stdTypeMap$name : buildType(typeNode);
  }
  const operationTypes = {
    // Get the extended root operation types.
    query: schemaConfig.query && replaceNamedType(schemaConfig.query),
    mutation: schemaConfig.mutation && replaceNamedType(schemaConfig.mutation),
    subscription: schemaConfig.subscription && replaceNamedType(schemaConfig.subscription),
    // Then, incorporate schema definition and all schema extensions.
    ...schemaDef && getOperationTypes([schemaDef]),
    ...getOperationTypes(schemaExtensions)
  };
  return {
    description: (_schemaDef = schemaDef) === null || _schemaDef === void 0 ? void 0 : (_schemaDef$descriptio = _schemaDef.description) === null || _schemaDef$descriptio === void 0 ? void 0 : _schemaDef$descriptio.value,
    ...operationTypes,
    types: Object.values(typeMap),
    directives: [
      ...schemaConfig.directives.map(replaceDirective),
      ...directiveDefs.map(buildDirective)
    ],
    extensions: /* @__PURE__ */ Object.create(null),
    astNode: (_schemaDef2 = schemaDef) !== null && _schemaDef2 !== void 0 ? _schemaDef2 : schemaConfig.astNode,
    extensionASTNodes: schemaConfig.extensionASTNodes.concat(schemaExtensions),
    assumeValid: (_options$assumeValid = options === null || options === void 0 ? void 0 : options.assumeValid) !== null && _options$assumeValid !== void 0 ? _options$assumeValid : false
  };
  function replaceType(type) {
    if (isListType(type)) {
      return new GraphQLList(replaceType(type.ofType));
    }
    if (isNonNullType(type)) {
      return new GraphQLNonNull(replaceType(type.ofType));
    }
    return replaceNamedType(type);
  }
  function replaceNamedType(type) {
    return typeMap[type.name];
  }
  function replaceDirective(directive) {
    const config = directive.toConfig();
    return new GraphQLDirective({
      ...config,
      args: mapValue(config.args, extendArg)
    });
  }
  function extendNamedType(type) {
    if (isIntrospectionType(type) || isSpecifiedScalarType(type)) {
      return type;
    }
    if (isScalarType(type)) {
      return extendScalarType(type);
    }
    if (isObjectType(type)) {
      return extendObjectType(type);
    }
    if (isInterfaceType(type)) {
      return extendInterfaceType(type);
    }
    if (isUnionType(type)) {
      return extendUnionType(type);
    }
    if (isEnumType(type)) {
      return extendEnumType(type);
    }
    if (isInputObjectType(type)) {
      return extendInputObjectType(type);
    }
    invariant(false, "Unexpected type: " + inspect(type));
  }
  function extendInputObjectType(type) {
    var _typeExtensionsMap$co;
    const config = type.toConfig();
    const extensions = (_typeExtensionsMap$co = typeExtensionsMap[config.name]) !== null && _typeExtensionsMap$co !== void 0 ? _typeExtensionsMap$co : [];
    return new GraphQLInputObjectType({
      ...config,
      fields: () => ({
        ...mapValue(config.fields, (field) => ({
          ...field,
          type: replaceType(field.type)
        })),
        ...buildInputFieldMap(extensions)
      }),
      extensionASTNodes: config.extensionASTNodes.concat(extensions)
    });
  }
  function extendEnumType(type) {
    var _typeExtensionsMap$ty;
    const config = type.toConfig();
    const extensions = (_typeExtensionsMap$ty = typeExtensionsMap[type.name]) !== null && _typeExtensionsMap$ty !== void 0 ? _typeExtensionsMap$ty : [];
    return new GraphQLEnumType({
      ...config,
      values: { ...config.values, ...buildEnumValueMap(extensions) },
      extensionASTNodes: config.extensionASTNodes.concat(extensions)
    });
  }
  function extendScalarType(type) {
    var _typeExtensionsMap$co2;
    const config = type.toConfig();
    const extensions = (_typeExtensionsMap$co2 = typeExtensionsMap[config.name]) !== null && _typeExtensionsMap$co2 !== void 0 ? _typeExtensionsMap$co2 : [];
    let specifiedByURL = config.specifiedByURL;
    for (const extensionNode of extensions) {
      var _getSpecifiedByURL;
      specifiedByURL = (_getSpecifiedByURL = getSpecifiedByURL(extensionNode)) !== null && _getSpecifiedByURL !== void 0 ? _getSpecifiedByURL : specifiedByURL;
    }
    return new GraphQLScalarType({
      ...config,
      specifiedByURL,
      extensionASTNodes: config.extensionASTNodes.concat(extensions)
    });
  }
  function extendObjectType(type) {
    var _typeExtensionsMap$co3;
    const config = type.toConfig();
    const extensions = (_typeExtensionsMap$co3 = typeExtensionsMap[config.name]) !== null && _typeExtensionsMap$co3 !== void 0 ? _typeExtensionsMap$co3 : [];
    return new GraphQLObjectType({
      ...config,
      interfaces: () => [
        ...type.getInterfaces().map(replaceNamedType),
        ...buildInterfaces(extensions)
      ],
      fields: () => ({
        ...mapValue(config.fields, extendField),
        ...buildFieldMap(extensions)
      }),
      extensionASTNodes: config.extensionASTNodes.concat(extensions)
    });
  }
  function extendInterfaceType(type) {
    var _typeExtensionsMap$co4;
    const config = type.toConfig();
    const extensions = (_typeExtensionsMap$co4 = typeExtensionsMap[config.name]) !== null && _typeExtensionsMap$co4 !== void 0 ? _typeExtensionsMap$co4 : [];
    return new GraphQLInterfaceType({
      ...config,
      interfaces: () => [
        ...type.getInterfaces().map(replaceNamedType),
        ...buildInterfaces(extensions)
      ],
      fields: () => ({
        ...mapValue(config.fields, extendField),
        ...buildFieldMap(extensions)
      }),
      extensionASTNodes: config.extensionASTNodes.concat(extensions)
    });
  }
  function extendUnionType(type) {
    var _typeExtensionsMap$co5;
    const config = type.toConfig();
    const extensions = (_typeExtensionsMap$co5 = typeExtensionsMap[config.name]) !== null && _typeExtensionsMap$co5 !== void 0 ? _typeExtensionsMap$co5 : [];
    return new GraphQLUnionType({
      ...config,
      types: () => [
        ...type.getTypes().map(replaceNamedType),
        ...buildUnionTypes(extensions)
      ],
      extensionASTNodes: config.extensionASTNodes.concat(extensions)
    });
  }
  function extendField(field) {
    return {
      ...field,
      type: replaceType(field.type),
      args: field.args && mapValue(field.args, extendArg)
    };
  }
  function extendArg(arg) {
    return { ...arg, type: replaceType(arg.type) };
  }
  function getOperationTypes(nodes) {
    const opTypes = {};
    for (const node of nodes) {
      var _node$operationTypes;
      const operationTypesNodes = (
        /* c8 ignore next */
        (_node$operationTypes = node.operationTypes) !== null && _node$operationTypes !== void 0 ? _node$operationTypes : []
      );
      for (const operationType of operationTypesNodes) {
        opTypes[operationType.operation] = getNamedType2(operationType.type);
      }
    }
    return opTypes;
  }
  function getNamedType2(node) {
    var _stdTypeMap$name2;
    const name = node.name.value;
    const type = (_stdTypeMap$name2 = stdTypeMap[name]) !== null && _stdTypeMap$name2 !== void 0 ? _stdTypeMap$name2 : typeMap[name];
    if (type === void 0) {
      throw new Error(`Unknown type: "${name}".`);
    }
    return type;
  }
  function getWrappedType(node) {
    if (node.kind === Kind.LIST_TYPE) {
      return new GraphQLList(getWrappedType(node.type));
    }
    if (node.kind === Kind.NON_NULL_TYPE) {
      return new GraphQLNonNull(getWrappedType(node.type));
    }
    return getNamedType2(node);
  }
  function buildDirective(node) {
    var _node$description;
    return new GraphQLDirective({
      name: node.name.value,
      description: (_node$description = node.description) === null || _node$description === void 0 ? void 0 : _node$description.value,
      // @ts-expect-error
      locations: node.locations.map(({ value }) => value),
      isRepeatable: node.repeatable,
      args: buildArgumentMap(node.arguments),
      astNode: node
    });
  }
  function buildFieldMap(nodes) {
    const fieldConfigMap = /* @__PURE__ */ Object.create(null);
    for (const node of nodes) {
      var _node$fields;
      const nodeFields = (
        /* c8 ignore next */
        (_node$fields = node.fields) !== null && _node$fields !== void 0 ? _node$fields : []
      );
      for (const field of nodeFields) {
        var _field$description;
        fieldConfigMap[field.name.value] = {
          // Note: While this could make assertions to get the correctly typed
          // value, that would throw immediately while type system validation
          // with validateSchema() will produce more actionable results.
          type: getWrappedType(field.type),
          description: (_field$description = field.description) === null || _field$description === void 0 ? void 0 : _field$description.value,
          args: buildArgumentMap(field.arguments),
          deprecationReason: getDeprecationReason(field),
          astNode: field
        };
      }
    }
    return fieldConfigMap;
  }
  function buildArgumentMap(args) {
    const argsNodes = (
      /* c8 ignore next */
      args !== null && args !== void 0 ? args : []
    );
    const argConfigMap = /* @__PURE__ */ Object.create(null);
    for (const arg of argsNodes) {
      var _arg$description;
      const type = getWrappedType(arg.type);
      argConfigMap[arg.name.value] = {
        type,
        description: (_arg$description = arg.description) === null || _arg$description === void 0 ? void 0 : _arg$description.value,
        defaultValue: valueFromAST(arg.defaultValue, type),
        deprecationReason: getDeprecationReason(arg),
        astNode: arg
      };
    }
    return argConfigMap;
  }
  function buildInputFieldMap(nodes) {
    const inputFieldMap = /* @__PURE__ */ Object.create(null);
    for (const node of nodes) {
      var _node$fields2;
      const fieldsNodes = (
        /* c8 ignore next */
        (_node$fields2 = node.fields) !== null && _node$fields2 !== void 0 ? _node$fields2 : []
      );
      for (const field of fieldsNodes) {
        var _field$description2;
        const type = getWrappedType(field.type);
        inputFieldMap[field.name.value] = {
          type,
          description: (_field$description2 = field.description) === null || _field$description2 === void 0 ? void 0 : _field$description2.value,
          defaultValue: valueFromAST(field.defaultValue, type),
          deprecationReason: getDeprecationReason(field),
          astNode: field
        };
      }
    }
    return inputFieldMap;
  }
  function buildEnumValueMap(nodes) {
    const enumValueMap = /* @__PURE__ */ Object.create(null);
    for (const node of nodes) {
      var _node$values;
      const valuesNodes = (
        /* c8 ignore next */
        (_node$values = node.values) !== null && _node$values !== void 0 ? _node$values : []
      );
      for (const value of valuesNodes) {
        var _value$description;
        enumValueMap[value.name.value] = {
          description: (_value$description = value.description) === null || _value$description === void 0 ? void 0 : _value$description.value,
          deprecationReason: getDeprecationReason(value),
          astNode: value
        };
      }
    }
    return enumValueMap;
  }
  function buildInterfaces(nodes) {
    return nodes.flatMap(
      // FIXME: https://github.com/graphql/graphql-js/issues/2203
      (node) => {
        var _node$interfaces$map, _node$interfaces;
        return (
          /* c8 ignore next */
          (_node$interfaces$map = (_node$interfaces = node.interfaces) === null || _node$interfaces === void 0 ? void 0 : _node$interfaces.map(getNamedType2)) !== null && _node$interfaces$map !== void 0 ? _node$interfaces$map : []
        );
      }
    );
  }
  function buildUnionTypes(nodes) {
    return nodes.flatMap(
      // FIXME: https://github.com/graphql/graphql-js/issues/2203
      (node) => {
        var _node$types$map, _node$types;
        return (
          /* c8 ignore next */
          (_node$types$map = (_node$types = node.types) === null || _node$types === void 0 ? void 0 : _node$types.map(getNamedType2)) !== null && _node$types$map !== void 0 ? _node$types$map : []
        );
      }
    );
  }
  function buildType(astNode) {
    var _typeExtensionsMap$na;
    const name = astNode.name.value;
    const extensionASTNodes = (_typeExtensionsMap$na = typeExtensionsMap[name]) !== null && _typeExtensionsMap$na !== void 0 ? _typeExtensionsMap$na : [];
    switch (astNode.kind) {
      case Kind.OBJECT_TYPE_DEFINITION: {
        var _astNode$description;
        const allNodes = [astNode, ...extensionASTNodes];
        return new GraphQLObjectType({
          name,
          description: (_astNode$description = astNode.description) === null || _astNode$description === void 0 ? void 0 : _astNode$description.value,
          interfaces: () => buildInterfaces(allNodes),
          fields: () => buildFieldMap(allNodes),
          astNode,
          extensionASTNodes
        });
      }
      case Kind.INTERFACE_TYPE_DEFINITION: {
        var _astNode$description2;
        const allNodes = [astNode, ...extensionASTNodes];
        return new GraphQLInterfaceType({
          name,
          description: (_astNode$description2 = astNode.description) === null || _astNode$description2 === void 0 ? void 0 : _astNode$description2.value,
          interfaces: () => buildInterfaces(allNodes),
          fields: () => buildFieldMap(allNodes),
          astNode,
          extensionASTNodes
        });
      }
      case Kind.ENUM_TYPE_DEFINITION: {
        var _astNode$description3;
        const allNodes = [astNode, ...extensionASTNodes];
        return new GraphQLEnumType({
          name,
          description: (_astNode$description3 = astNode.description) === null || _astNode$description3 === void 0 ? void 0 : _astNode$description3.value,
          values: buildEnumValueMap(allNodes),
          astNode,
          extensionASTNodes
        });
      }
      case Kind.UNION_TYPE_DEFINITION: {
        var _astNode$description4;
        const allNodes = [astNode, ...extensionASTNodes];
        return new GraphQLUnionType({
          name,
          description: (_astNode$description4 = astNode.description) === null || _astNode$description4 === void 0 ? void 0 : _astNode$description4.value,
          types: () => buildUnionTypes(allNodes),
          astNode,
          extensionASTNodes
        });
      }
      case Kind.SCALAR_TYPE_DEFINITION: {
        var _astNode$description5;
        return new GraphQLScalarType({
          name,
          description: (_astNode$description5 = astNode.description) === null || _astNode$description5 === void 0 ? void 0 : _astNode$description5.value,
          specifiedByURL: getSpecifiedByURL(astNode),
          astNode,
          extensionASTNodes
        });
      }
      case Kind.INPUT_OBJECT_TYPE_DEFINITION: {
        var _astNode$description6;
        const allNodes = [astNode, ...extensionASTNodes];
        return new GraphQLInputObjectType({
          name,
          description: (_astNode$description6 = astNode.description) === null || _astNode$description6 === void 0 ? void 0 : _astNode$description6.value,
          fields: () => buildInputFieldMap(allNodes),
          astNode,
          extensionASTNodes,
          isOneOf: isOneOf(astNode)
        });
      }
    }
  }
}
function getDeprecationReason(node) {
  const deprecated = getDirectiveValues(GraphQLDeprecatedDirective, node);
  return deprecated === null || deprecated === void 0 ? void 0 : deprecated.reason;
}
function getSpecifiedByURL(node) {
  const specifiedBy = getDirectiveValues(GraphQLSpecifiedByDirective, node);
  return specifiedBy === null || specifiedBy === void 0 ? void 0 : specifiedBy.url;
}
function isOneOf(node) {
  return Boolean(getDirectiveValues(GraphQLOneOfDirective, node));
}
var stdTypeMap;
var init_extendSchema = __esm({
  "node_modules/graphql/utilities/extendSchema.mjs"() {
    init_devAssert();
    init_inspect();
    init_invariant();
    init_keyMap();
    init_mapValue();
    init_kinds();
    init_predicates();
    init_definition();
    init_directives();
    init_introspection();
    init_scalars();
    init_schema();
    init_validate2();
    init_values();
    init_valueFromAST();
    stdTypeMap = keyMap(
      [...specifiedScalarTypes, ...introspectionTypes],
      (type) => type.name
    );
  }
});

// node_modules/graphql/utilities/buildASTSchema.mjs
function buildASTSchema(documentAST, options) {
  documentAST != null && documentAST.kind === Kind.DOCUMENT || devAssert(false, "Must provide valid Document AST.");
  if ((options === null || options === void 0 ? void 0 : options.assumeValid) !== true && (options === null || options === void 0 ? void 0 : options.assumeValidSDL) !== true) {
    assertValidSDL(documentAST);
  }
  const emptySchemaConfig = {
    description: void 0,
    types: [],
    directives: [],
    extensions: /* @__PURE__ */ Object.create(null),
    extensionASTNodes: [],
    assumeValid: false
  };
  const config = extendSchemaImpl(emptySchemaConfig, documentAST, options);
  if (config.astNode == null) {
    for (const type of config.types) {
      switch (type.name) {
        case "Query":
          config.query = type;
          break;
        case "Mutation":
          config.mutation = type;
          break;
        case "Subscription":
          config.subscription = type;
          break;
      }
    }
  }
  const directives = [
    ...config.directives,
    // If specified directives were not explicitly declared, add them.
    ...specifiedDirectives.filter(
      (stdDirective) => config.directives.every(
        (directive) => directive.name !== stdDirective.name
      )
    )
  ];
  return new GraphQLSchema({ ...config, directives });
}
function buildSchema(source, options) {
  const document2 = parse(source, {
    noLocation: options === null || options === void 0 ? void 0 : options.noLocation,
    allowLegacyFragmentVariables: options === null || options === void 0 ? void 0 : options.allowLegacyFragmentVariables
  });
  return buildASTSchema(document2, {
    assumeValidSDL: options === null || options === void 0 ? void 0 : options.assumeValidSDL,
    assumeValid: options === null || options === void 0 ? void 0 : options.assumeValid
  });
}
var init_buildASTSchema = __esm({
  "node_modules/graphql/utilities/buildASTSchema.mjs"() {
    init_devAssert();
    init_kinds();
    init_parser();
    init_directives();
    init_schema();
    init_validate2();
    init_extendSchema();
  }
});

// node_modules/graphql/utilities/lexicographicSortSchema.mjs
function lexicographicSortSchema(schema) {
  const schemaConfig = schema.toConfig();
  const typeMap = keyValMap(
    sortByName(schemaConfig.types),
    (type) => type.name,
    sortNamedType
  );
  return new GraphQLSchema({
    ...schemaConfig,
    types: Object.values(typeMap),
    directives: sortByName(schemaConfig.directives).map(sortDirective),
    query: replaceMaybeType(schemaConfig.query),
    mutation: replaceMaybeType(schemaConfig.mutation),
    subscription: replaceMaybeType(schemaConfig.subscription)
  });
  function replaceType(type) {
    if (isListType(type)) {
      return new GraphQLList(replaceType(type.ofType));
    } else if (isNonNullType(type)) {
      return new GraphQLNonNull(replaceType(type.ofType));
    }
    return replaceNamedType(type);
  }
  function replaceNamedType(type) {
    return typeMap[type.name];
  }
  function replaceMaybeType(maybeType) {
    return maybeType && replaceNamedType(maybeType);
  }
  function sortDirective(directive) {
    const config = directive.toConfig();
    return new GraphQLDirective({
      ...config,
      locations: sortBy(config.locations, (x) => x),
      args: sortArgs(config.args)
    });
  }
  function sortArgs(args) {
    return sortObjMap(args, (arg) => ({ ...arg, type: replaceType(arg.type) }));
  }
  function sortFields2(fieldsMap) {
    return sortObjMap(fieldsMap, (field) => ({
      ...field,
      type: replaceType(field.type),
      args: field.args && sortArgs(field.args)
    }));
  }
  function sortInputFields(fieldsMap) {
    return sortObjMap(fieldsMap, (field) => ({
      ...field,
      type: replaceType(field.type)
    }));
  }
  function sortTypes(array) {
    return sortByName(array).map(replaceNamedType);
  }
  function sortNamedType(type) {
    if (isScalarType(type) || isIntrospectionType(type)) {
      return type;
    }
    if (isObjectType(type)) {
      const config = type.toConfig();
      return new GraphQLObjectType({
        ...config,
        interfaces: () => sortTypes(config.interfaces),
        fields: () => sortFields2(config.fields)
      });
    }
    if (isInterfaceType(type)) {
      const config = type.toConfig();
      return new GraphQLInterfaceType({
        ...config,
        interfaces: () => sortTypes(config.interfaces),
        fields: () => sortFields2(config.fields)
      });
    }
    if (isUnionType(type)) {
      const config = type.toConfig();
      return new GraphQLUnionType({
        ...config,
        types: () => sortTypes(config.types)
      });
    }
    if (isEnumType(type)) {
      const config = type.toConfig();
      return new GraphQLEnumType({
        ...config,
        values: sortObjMap(config.values, (value) => value)
      });
    }
    if (isInputObjectType(type)) {
      const config = type.toConfig();
      return new GraphQLInputObjectType({
        ...config,
        fields: () => sortInputFields(config.fields)
      });
    }
    invariant(false, "Unexpected type: " + inspect(type));
  }
}
function sortObjMap(map, sortValueFn) {
  const sortedMap = /* @__PURE__ */ Object.create(null);
  for (const key of Object.keys(map).sort(naturalCompare)) {
    sortedMap[key] = sortValueFn(map[key]);
  }
  return sortedMap;
}
function sortByName(array) {
  return sortBy(array, (obj) => obj.name);
}
function sortBy(array, mapToKey) {
  return array.slice().sort((obj1, obj2) => {
    const key1 = mapToKey(obj1);
    const key2 = mapToKey(obj2);
    return naturalCompare(key1, key2);
  });
}
var init_lexicographicSortSchema = __esm({
  "node_modules/graphql/utilities/lexicographicSortSchema.mjs"() {
    init_inspect();
    init_invariant();
    init_keyValMap();
    init_naturalCompare();
    init_definition();
    init_directives();
    init_introspection();
    init_schema();
  }
});

// node_modules/graphql/utilities/printSchema.mjs
function printSchema(schema) {
  return printFilteredSchema(
    schema,
    (n2) => !isSpecifiedDirective(n2),
    isDefinedType
  );
}
function printIntrospectionSchema(schema) {
  return printFilteredSchema(schema, isSpecifiedDirective, isIntrospectionType);
}
function isDefinedType(type) {
  return !isSpecifiedScalarType(type) && !isIntrospectionType(type);
}
function printFilteredSchema(schema, directiveFilter, typeFilter) {
  const directives = schema.getDirectives().filter(directiveFilter);
  const types = Object.values(schema.getTypeMap()).filter(typeFilter);
  return [
    printSchemaDefinition(schema),
    ...directives.map((directive) => printDirective(directive)),
    ...types.map((type) => printType(type))
  ].filter(Boolean).join("\n\n");
}
function printSchemaDefinition(schema) {
  if (schema.description == null && isSchemaOfCommonNames(schema)) {
    return;
  }
  const operationTypes = [];
  const queryType = schema.getQueryType();
  if (queryType) {
    operationTypes.push(`  query: ${queryType.name}`);
  }
  const mutationType = schema.getMutationType();
  if (mutationType) {
    operationTypes.push(`  mutation: ${mutationType.name}`);
  }
  const subscriptionType = schema.getSubscriptionType();
  if (subscriptionType) {
    operationTypes.push(`  subscription: ${subscriptionType.name}`);
  }
  return printDescription(schema) + `schema {
${operationTypes.join("\n")}
}`;
}
function isSchemaOfCommonNames(schema) {
  const queryType = schema.getQueryType();
  if (queryType && queryType.name !== "Query") {
    return false;
  }
  const mutationType = schema.getMutationType();
  if (mutationType && mutationType.name !== "Mutation") {
    return false;
  }
  const subscriptionType = schema.getSubscriptionType();
  if (subscriptionType && subscriptionType.name !== "Subscription") {
    return false;
  }
  return true;
}
function printType(type) {
  if (isScalarType(type)) {
    return printScalar(type);
  }
  if (isObjectType(type)) {
    return printObject(type);
  }
  if (isInterfaceType(type)) {
    return printInterface(type);
  }
  if (isUnionType(type)) {
    return printUnion(type);
  }
  if (isEnumType(type)) {
    return printEnum(type);
  }
  if (isInputObjectType(type)) {
    return printInputObject(type);
  }
  invariant(false, "Unexpected type: " + inspect(type));
}
function printScalar(type) {
  return printDescription(type) + `scalar ${type.name}` + printSpecifiedByURL(type);
}
function printImplementedInterfaces(type) {
  const interfaces = type.getInterfaces();
  return interfaces.length ? " implements " + interfaces.map((i) => i.name).join(" & ") : "";
}
function printObject(type) {
  return printDescription(type) + `type ${type.name}` + printImplementedInterfaces(type) + printFields(type);
}
function printInterface(type) {
  return printDescription(type) + `interface ${type.name}` + printImplementedInterfaces(type) + printFields(type);
}
function printUnion(type) {
  const types = type.getTypes();
  const possibleTypes = types.length ? " = " + types.join(" | ") : "";
  return printDescription(type) + "union " + type.name + possibleTypes;
}
function printEnum(type) {
  const values = type.getValues().map(
    (value, i) => printDescription(value, "  ", !i) + "  " + value.name + printDeprecated(value.deprecationReason)
  );
  return printDescription(type) + `enum ${type.name}` + printBlock(values);
}
function printInputObject(type) {
  const fields = Object.values(type.getFields()).map(
    (f, i) => printDescription(f, "  ", !i) + "  " + printInputValue(f)
  );
  return printDescription(type) + `input ${type.name}` + (type.isOneOf ? " @oneOf" : "") + printBlock(fields);
}
function printFields(type) {
  const fields = Object.values(type.getFields()).map(
    (f, i) => printDescription(f, "  ", !i) + "  " + f.name + printArgs(f.args, "  ") + ": " + String(f.type) + printDeprecated(f.deprecationReason)
  );
  return printBlock(fields);
}
function printBlock(items) {
  return items.length !== 0 ? " {\n" + items.join("\n") + "\n}" : "";
}
function printArgs(args, indentation = "") {
  if (args.length === 0) {
    return "";
  }
  if (args.every((arg) => !arg.description)) {
    return "(" + args.map(printInputValue).join(", ") + ")";
  }
  return "(\n" + args.map(
    (arg, i) => printDescription(arg, "  " + indentation, !i) + "  " + indentation + printInputValue(arg)
  ).join("\n") + "\n" + indentation + ")";
}
function printInputValue(arg) {
  const defaultAST = astFromValue(arg.defaultValue, arg.type);
  let argDecl = arg.name + ": " + String(arg.type);
  if (defaultAST) {
    argDecl += ` = ${print(defaultAST)}`;
  }
  return argDecl + printDeprecated(arg.deprecationReason);
}
function printDirective(directive) {
  return printDescription(directive) + "directive @" + directive.name + printArgs(directive.args) + (directive.isRepeatable ? " repeatable" : "") + " on " + directive.locations.join(" | ");
}
function printDeprecated(reason) {
  if (reason == null) {
    return "";
  }
  if (reason !== DEFAULT_DEPRECATION_REASON) {
    const astValue = print({
      kind: Kind.STRING,
      value: reason
    });
    return ` @deprecated(reason: ${astValue})`;
  }
  return " @deprecated";
}
function printSpecifiedByURL(scalar) {
  if (scalar.specifiedByURL == null) {
    return "";
  }
  const astValue = print({
    kind: Kind.STRING,
    value: scalar.specifiedByURL
  });
  return ` @specifiedBy(url: ${astValue})`;
}
function printDescription(def, indentation = "", firstInBlock = true) {
  const { description } = def;
  if (description == null) {
    return "";
  }
  const blockString = print({
    kind: Kind.STRING,
    value: description,
    block: isPrintableAsBlockString(description)
  });
  const prefix = indentation && !firstInBlock ? "\n" + indentation : indentation;
  return prefix + blockString.replace(/\n/g, "\n" + indentation) + "\n";
}
var init_printSchema = __esm({
  "node_modules/graphql/utilities/printSchema.mjs"() {
    init_inspect();
    init_invariant();
    init_blockString();
    init_kinds();
    init_printer();
    init_definition();
    init_directives();
    init_introspection();
    init_scalars();
    init_astFromValue();
  }
});

// node_modules/graphql/utilities/concatAST.mjs
function concatAST(documents) {
  const definitions = [];
  for (const doc of documents) {
    definitions.push(...doc.definitions);
  }
  return {
    kind: Kind.DOCUMENT,
    definitions
  };
}
var init_concatAST = __esm({
  "node_modules/graphql/utilities/concatAST.mjs"() {
    init_kinds();
  }
});

// node_modules/graphql/utilities/separateOperations.mjs
function separateOperations(documentAST) {
  const operations = [];
  const depGraph = /* @__PURE__ */ Object.create(null);
  for (const definitionNode of documentAST.definitions) {
    switch (definitionNode.kind) {
      case Kind.OPERATION_DEFINITION:
        operations.push(definitionNode);
        break;
      case Kind.FRAGMENT_DEFINITION:
        depGraph[definitionNode.name.value] = collectDependencies(
          definitionNode.selectionSet
        );
        break;
      default:
    }
  }
  const separatedDocumentASTs = /* @__PURE__ */ Object.create(null);
  for (const operation of operations) {
    const dependencies = /* @__PURE__ */ new Set();
    for (const fragmentName of collectDependencies(operation.selectionSet)) {
      collectTransitiveDependencies(dependencies, depGraph, fragmentName);
    }
    const operationName = operation.name ? operation.name.value : "";
    separatedDocumentASTs[operationName] = {
      kind: Kind.DOCUMENT,
      definitions: documentAST.definitions.filter(
        (node) => node === operation || node.kind === Kind.FRAGMENT_DEFINITION && dependencies.has(node.name.value)
      )
    };
  }
  return separatedDocumentASTs;
}
function collectTransitiveDependencies(collected, depGraph, fromName) {
  if (!collected.has(fromName)) {
    collected.add(fromName);
    const immediateDeps = depGraph[fromName];
    if (immediateDeps !== void 0) {
      for (const toName of immediateDeps) {
        collectTransitiveDependencies(collected, depGraph, toName);
      }
    }
  }
}
function collectDependencies(selectionSet) {
  const dependencies = [];
  visit(selectionSet, {
    FragmentSpread(node) {
      dependencies.push(node.name.value);
    }
  });
  return dependencies;
}
var init_separateOperations = __esm({
  "node_modules/graphql/utilities/separateOperations.mjs"() {
    init_kinds();
    init_visitor();
  }
});

// node_modules/graphql/utilities/stripIgnoredCharacters.mjs
function stripIgnoredCharacters(source) {
  const sourceObj = isSource(source) ? source : new Source(source);
  const body = sourceObj.body;
  const lexer = new Lexer(sourceObj);
  let strippedBody = "";
  let wasLastAddedTokenNonPunctuator = false;
  while (lexer.advance().kind !== TokenKind.EOF) {
    const currentToken = lexer.token;
    const tokenKind = currentToken.kind;
    const isNonPunctuator = !isPunctuatorTokenKind(currentToken.kind);
    if (wasLastAddedTokenNonPunctuator) {
      if (isNonPunctuator || currentToken.kind === TokenKind.SPREAD) {
        strippedBody += " ";
      }
    }
    const tokenBody = body.slice(currentToken.start, currentToken.end);
    if (tokenKind === TokenKind.BLOCK_STRING) {
      strippedBody += printBlockString(currentToken.value, {
        minimize: true
      });
    } else {
      strippedBody += tokenBody;
    }
    wasLastAddedTokenNonPunctuator = isNonPunctuator;
  }
  return strippedBody;
}
var init_stripIgnoredCharacters = __esm({
  "node_modules/graphql/utilities/stripIgnoredCharacters.mjs"() {
    init_blockString();
    init_lexer();
    init_source();
    init_tokenKind();
  }
});

// node_modules/graphql/utilities/assertValidName.mjs
function assertValidName(name) {
  const error = isValidNameError(name);
  if (error) {
    throw error;
  }
  return name;
}
function isValidNameError(name) {
  typeof name === "string" || devAssert(false, "Expected name to be a string.");
  if (name.startsWith("__")) {
    return new GraphQLError(
      `Name "${name}" must not begin with "__", which is reserved by GraphQL introspection.`
    );
  }
  try {
    assertName(name);
  } catch (error) {
    return error;
  }
}
var init_assertValidName = __esm({
  "node_modules/graphql/utilities/assertValidName.mjs"() {
    init_devAssert();
    init_GraphQLError();
    init_assertName();
  }
});

// node_modules/graphql/utilities/findBreakingChanges.mjs
function findBreakingChanges(oldSchema, newSchema) {
  return findSchemaChanges(oldSchema, newSchema).filter(
    (change) => change.type in BreakingChangeType
  );
}
function findDangerousChanges(oldSchema, newSchema) {
  return findSchemaChanges(oldSchema, newSchema).filter(
    (change) => change.type in DangerousChangeType
  );
}
function findSchemaChanges(oldSchema, newSchema) {
  return [
    ...findTypeChanges(oldSchema, newSchema),
    ...findDirectiveChanges(oldSchema, newSchema)
  ];
}
function findDirectiveChanges(oldSchema, newSchema) {
  const schemaChanges = [];
  const directivesDiff = diff(
    oldSchema.getDirectives(),
    newSchema.getDirectives()
  );
  for (const oldDirective of directivesDiff.removed) {
    schemaChanges.push({
      type: BreakingChangeType.DIRECTIVE_REMOVED,
      description: `${oldDirective.name} was removed.`
    });
  }
  for (const [oldDirective, newDirective] of directivesDiff.persisted) {
    const argsDiff = diff(oldDirective.args, newDirective.args);
    for (const newArg of argsDiff.added) {
      if (isRequiredArgument(newArg)) {
        schemaChanges.push({
          type: BreakingChangeType.REQUIRED_DIRECTIVE_ARG_ADDED,
          description: `A required arg ${newArg.name} on directive ${oldDirective.name} was added.`
        });
      }
    }
    for (const oldArg of argsDiff.removed) {
      schemaChanges.push({
        type: BreakingChangeType.DIRECTIVE_ARG_REMOVED,
        description: `${oldArg.name} was removed from ${oldDirective.name}.`
      });
    }
    if (oldDirective.isRepeatable && !newDirective.isRepeatable) {
      schemaChanges.push({
        type: BreakingChangeType.DIRECTIVE_REPEATABLE_REMOVED,
        description: `Repeatable flag was removed from ${oldDirective.name}.`
      });
    }
    for (const location of oldDirective.locations) {
      if (!newDirective.locations.includes(location)) {
        schemaChanges.push({
          type: BreakingChangeType.DIRECTIVE_LOCATION_REMOVED,
          description: `${location} was removed from ${oldDirective.name}.`
        });
      }
    }
  }
  return schemaChanges;
}
function findTypeChanges(oldSchema, newSchema) {
  const schemaChanges = [];
  const typesDiff = diff(
    Object.values(oldSchema.getTypeMap()),
    Object.values(newSchema.getTypeMap())
  );
  for (const oldType of typesDiff.removed) {
    schemaChanges.push({
      type: BreakingChangeType.TYPE_REMOVED,
      description: isSpecifiedScalarType(oldType) ? `Standard scalar ${oldType.name} was removed because it is not referenced anymore.` : `${oldType.name} was removed.`
    });
  }
  for (const [oldType, newType] of typesDiff.persisted) {
    if (isEnumType(oldType) && isEnumType(newType)) {
      schemaChanges.push(...findEnumTypeChanges(oldType, newType));
    } else if (isUnionType(oldType) && isUnionType(newType)) {
      schemaChanges.push(...findUnionTypeChanges(oldType, newType));
    } else if (isInputObjectType(oldType) && isInputObjectType(newType)) {
      schemaChanges.push(...findInputObjectTypeChanges(oldType, newType));
    } else if (isObjectType(oldType) && isObjectType(newType)) {
      schemaChanges.push(
        ...findFieldChanges(oldType, newType),
        ...findImplementedInterfacesChanges(oldType, newType)
      );
    } else if (isInterfaceType(oldType) && isInterfaceType(newType)) {
      schemaChanges.push(
        ...findFieldChanges(oldType, newType),
        ...findImplementedInterfacesChanges(oldType, newType)
      );
    } else if (oldType.constructor !== newType.constructor) {
      schemaChanges.push({
        type: BreakingChangeType.TYPE_CHANGED_KIND,
        description: `${oldType.name} changed from ${typeKindName(oldType)} to ${typeKindName(newType)}.`
      });
    }
  }
  return schemaChanges;
}
function findInputObjectTypeChanges(oldType, newType) {
  const schemaChanges = [];
  const fieldsDiff = diff(
    Object.values(oldType.getFields()),
    Object.values(newType.getFields())
  );
  for (const newField of fieldsDiff.added) {
    if (isRequiredInputField(newField)) {
      schemaChanges.push({
        type: BreakingChangeType.REQUIRED_INPUT_FIELD_ADDED,
        description: `A required field ${newField.name} on input type ${oldType.name} was added.`
      });
    } else {
      schemaChanges.push({
        type: DangerousChangeType.OPTIONAL_INPUT_FIELD_ADDED,
        description: `An optional field ${newField.name} on input type ${oldType.name} was added.`
      });
    }
  }
  for (const oldField of fieldsDiff.removed) {
    schemaChanges.push({
      type: BreakingChangeType.FIELD_REMOVED,
      description: `${oldType.name}.${oldField.name} was removed.`
    });
  }
  for (const [oldField, newField] of fieldsDiff.persisted) {
    const isSafe = isChangeSafeForInputObjectFieldOrFieldArg(
      oldField.type,
      newField.type
    );
    if (!isSafe) {
      schemaChanges.push({
        type: BreakingChangeType.FIELD_CHANGED_KIND,
        description: `${oldType.name}.${oldField.name} changed type from ${String(oldField.type)} to ${String(newField.type)}.`
      });
    }
  }
  return schemaChanges;
}
function findUnionTypeChanges(oldType, newType) {
  const schemaChanges = [];
  const possibleTypesDiff = diff(oldType.getTypes(), newType.getTypes());
  for (const newPossibleType of possibleTypesDiff.added) {
    schemaChanges.push({
      type: DangerousChangeType.TYPE_ADDED_TO_UNION,
      description: `${newPossibleType.name} was added to union type ${oldType.name}.`
    });
  }
  for (const oldPossibleType of possibleTypesDiff.removed) {
    schemaChanges.push({
      type: BreakingChangeType.TYPE_REMOVED_FROM_UNION,
      description: `${oldPossibleType.name} was removed from union type ${oldType.name}.`
    });
  }
  return schemaChanges;
}
function findEnumTypeChanges(oldType, newType) {
  const schemaChanges = [];
  const valuesDiff = diff(oldType.getValues(), newType.getValues());
  for (const newValue of valuesDiff.added) {
    schemaChanges.push({
      type: DangerousChangeType.VALUE_ADDED_TO_ENUM,
      description: `${newValue.name} was added to enum type ${oldType.name}.`
    });
  }
  for (const oldValue of valuesDiff.removed) {
    schemaChanges.push({
      type: BreakingChangeType.VALUE_REMOVED_FROM_ENUM,
      description: `${oldValue.name} was removed from enum type ${oldType.name}.`
    });
  }
  return schemaChanges;
}
function findImplementedInterfacesChanges(oldType, newType) {
  const schemaChanges = [];
  const interfacesDiff = diff(oldType.getInterfaces(), newType.getInterfaces());
  for (const newInterface of interfacesDiff.added) {
    schemaChanges.push({
      type: DangerousChangeType.IMPLEMENTED_INTERFACE_ADDED,
      description: `${newInterface.name} added to interfaces implemented by ${oldType.name}.`
    });
  }
  for (const oldInterface of interfacesDiff.removed) {
    schemaChanges.push({
      type: BreakingChangeType.IMPLEMENTED_INTERFACE_REMOVED,
      description: `${oldType.name} no longer implements interface ${oldInterface.name}.`
    });
  }
  return schemaChanges;
}
function findFieldChanges(oldType, newType) {
  const schemaChanges = [];
  const fieldsDiff = diff(
    Object.values(oldType.getFields()),
    Object.values(newType.getFields())
  );
  for (const oldField of fieldsDiff.removed) {
    schemaChanges.push({
      type: BreakingChangeType.FIELD_REMOVED,
      description: `${oldType.name}.${oldField.name} was removed.`
    });
  }
  for (const [oldField, newField] of fieldsDiff.persisted) {
    schemaChanges.push(...findArgChanges(oldType, oldField, newField));
    const isSafe = isChangeSafeForObjectOrInterfaceField(
      oldField.type,
      newField.type
    );
    if (!isSafe) {
      schemaChanges.push({
        type: BreakingChangeType.FIELD_CHANGED_KIND,
        description: `${oldType.name}.${oldField.name} changed type from ${String(oldField.type)} to ${String(newField.type)}.`
      });
    }
  }
  return schemaChanges;
}
function findArgChanges(oldType, oldField, newField) {
  const schemaChanges = [];
  const argsDiff = diff(oldField.args, newField.args);
  for (const oldArg of argsDiff.removed) {
    schemaChanges.push({
      type: BreakingChangeType.ARG_REMOVED,
      description: `${oldType.name}.${oldField.name} arg ${oldArg.name} was removed.`
    });
  }
  for (const [oldArg, newArg] of argsDiff.persisted) {
    const isSafe = isChangeSafeForInputObjectFieldOrFieldArg(
      oldArg.type,
      newArg.type
    );
    if (!isSafe) {
      schemaChanges.push({
        type: BreakingChangeType.ARG_CHANGED_KIND,
        description: `${oldType.name}.${oldField.name} arg ${oldArg.name} has changed type from ${String(oldArg.type)} to ${String(newArg.type)}.`
      });
    } else if (oldArg.defaultValue !== void 0) {
      if (newArg.defaultValue === void 0) {
        schemaChanges.push({
          type: DangerousChangeType.ARG_DEFAULT_VALUE_CHANGE,
          description: `${oldType.name}.${oldField.name} arg ${oldArg.name} defaultValue was removed.`
        });
      } else {
        const oldValueStr = stringifyValue2(oldArg.defaultValue, oldArg.type);
        const newValueStr = stringifyValue2(newArg.defaultValue, newArg.type);
        if (oldValueStr !== newValueStr) {
          schemaChanges.push({
            type: DangerousChangeType.ARG_DEFAULT_VALUE_CHANGE,
            description: `${oldType.name}.${oldField.name} arg ${oldArg.name} has changed defaultValue from ${oldValueStr} to ${newValueStr}.`
          });
        }
      }
    }
  }
  for (const newArg of argsDiff.added) {
    if (isRequiredArgument(newArg)) {
      schemaChanges.push({
        type: BreakingChangeType.REQUIRED_ARG_ADDED,
        description: `A required arg ${newArg.name} on ${oldType.name}.${oldField.name} was added.`
      });
    } else {
      schemaChanges.push({
        type: DangerousChangeType.OPTIONAL_ARG_ADDED,
        description: `An optional arg ${newArg.name} on ${oldType.name}.${oldField.name} was added.`
      });
    }
  }
  return schemaChanges;
}
function isChangeSafeForObjectOrInterfaceField(oldType, newType) {
  if (isListType(oldType)) {
    return (
      // if they're both lists, make sure the underlying types are compatible
      isListType(newType) && isChangeSafeForObjectOrInterfaceField(
        oldType.ofType,
        newType.ofType
      ) || // moving from nullable to non-null of the same underlying type is safe
      isNonNullType(newType) && isChangeSafeForObjectOrInterfaceField(oldType, newType.ofType)
    );
  }
  if (isNonNullType(oldType)) {
    return isNonNullType(newType) && isChangeSafeForObjectOrInterfaceField(oldType.ofType, newType.ofType);
  }
  return (
    // if they're both named types, see if their names are equivalent
    isNamedType(newType) && oldType.name === newType.name || // moving from nullable to non-null of the same underlying type is safe
    isNonNullType(newType) && isChangeSafeForObjectOrInterfaceField(oldType, newType.ofType)
  );
}
function isChangeSafeForInputObjectFieldOrFieldArg(oldType, newType) {
  if (isListType(oldType)) {
    return isListType(newType) && isChangeSafeForInputObjectFieldOrFieldArg(oldType.ofType, newType.ofType);
  }
  if (isNonNullType(oldType)) {
    return (
      // if they're both non-null, make sure the underlying types are
      // compatible
      isNonNullType(newType) && isChangeSafeForInputObjectFieldOrFieldArg(
        oldType.ofType,
        newType.ofType
      ) || // moving from non-null to nullable of the same underlying type is safe
      !isNonNullType(newType) && isChangeSafeForInputObjectFieldOrFieldArg(oldType.ofType, newType)
    );
  }
  return isNamedType(newType) && oldType.name === newType.name;
}
function typeKindName(type) {
  if (isScalarType(type)) {
    return "a Scalar type";
  }
  if (isObjectType(type)) {
    return "an Object type";
  }
  if (isInterfaceType(type)) {
    return "an Interface type";
  }
  if (isUnionType(type)) {
    return "a Union type";
  }
  if (isEnumType(type)) {
    return "an Enum type";
  }
  if (isInputObjectType(type)) {
    return "an Input type";
  }
  invariant(false, "Unexpected type: " + inspect(type));
}
function stringifyValue2(value, type) {
  const ast = astFromValue(value, type);
  ast != null || invariant(false);
  return print(sortValueNode(ast));
}
function diff(oldArray, newArray) {
  const added = [];
  const removed = [];
  const persisted = [];
  const oldMap = keyMap(oldArray, ({ name }) => name);
  const newMap = keyMap(newArray, ({ name }) => name);
  for (const oldItem of oldArray) {
    const newItem = newMap[oldItem.name];
    if (newItem === void 0) {
      removed.push(oldItem);
    } else {
      persisted.push([oldItem, newItem]);
    }
  }
  for (const newItem of newArray) {
    if (oldMap[newItem.name] === void 0) {
      added.push(newItem);
    }
  }
  return {
    added,
    persisted,
    removed
  };
}
var BreakingChangeType, DangerousChangeType;
var init_findBreakingChanges = __esm({
  "node_modules/graphql/utilities/findBreakingChanges.mjs"() {
    init_inspect();
    init_invariant();
    init_keyMap();
    init_printer();
    init_definition();
    init_scalars();
    init_astFromValue();
    init_sortValueNode();
    (function(BreakingChangeType2) {
      BreakingChangeType2["TYPE_REMOVED"] = "TYPE_REMOVED";
      BreakingChangeType2["TYPE_CHANGED_KIND"] = "TYPE_CHANGED_KIND";
      BreakingChangeType2["TYPE_REMOVED_FROM_UNION"] = "TYPE_REMOVED_FROM_UNION";
      BreakingChangeType2["VALUE_REMOVED_FROM_ENUM"] = "VALUE_REMOVED_FROM_ENUM";
      BreakingChangeType2["REQUIRED_INPUT_FIELD_ADDED"] = "REQUIRED_INPUT_FIELD_ADDED";
      BreakingChangeType2["IMPLEMENTED_INTERFACE_REMOVED"] = "IMPLEMENTED_INTERFACE_REMOVED";
      BreakingChangeType2["FIELD_REMOVED"] = "FIELD_REMOVED";
      BreakingChangeType2["FIELD_CHANGED_KIND"] = "FIELD_CHANGED_KIND";
      BreakingChangeType2["REQUIRED_ARG_ADDED"] = "REQUIRED_ARG_ADDED";
      BreakingChangeType2["ARG_REMOVED"] = "ARG_REMOVED";
      BreakingChangeType2["ARG_CHANGED_KIND"] = "ARG_CHANGED_KIND";
      BreakingChangeType2["DIRECTIVE_REMOVED"] = "DIRECTIVE_REMOVED";
      BreakingChangeType2["DIRECTIVE_ARG_REMOVED"] = "DIRECTIVE_ARG_REMOVED";
      BreakingChangeType2["REQUIRED_DIRECTIVE_ARG_ADDED"] = "REQUIRED_DIRECTIVE_ARG_ADDED";
      BreakingChangeType2["DIRECTIVE_REPEATABLE_REMOVED"] = "DIRECTIVE_REPEATABLE_REMOVED";
      BreakingChangeType2["DIRECTIVE_LOCATION_REMOVED"] = "DIRECTIVE_LOCATION_REMOVED";
    })(BreakingChangeType || (BreakingChangeType = {}));
    (function(DangerousChangeType2) {
      DangerousChangeType2["VALUE_ADDED_TO_ENUM"] = "VALUE_ADDED_TO_ENUM";
      DangerousChangeType2["TYPE_ADDED_TO_UNION"] = "TYPE_ADDED_TO_UNION";
      DangerousChangeType2["OPTIONAL_INPUT_FIELD_ADDED"] = "OPTIONAL_INPUT_FIELD_ADDED";
      DangerousChangeType2["OPTIONAL_ARG_ADDED"] = "OPTIONAL_ARG_ADDED";
      DangerousChangeType2["IMPLEMENTED_INTERFACE_ADDED"] = "IMPLEMENTED_INTERFACE_ADDED";
      DangerousChangeType2["ARG_DEFAULT_VALUE_CHANGE"] = "ARG_DEFAULT_VALUE_CHANGE";
    })(DangerousChangeType || (DangerousChangeType = {}));
  }
});

// node_modules/graphql/utilities/index.mjs
var init_utilities = __esm({
  "node_modules/graphql/utilities/index.mjs"() {
    init_getIntrospectionQuery();
    init_getOperationAST();
    init_getOperationRootType();
    init_introspectionFromSchema();
    init_buildClientSchema();
    init_buildASTSchema();
    init_extendSchema();
    init_lexicographicSortSchema();
    init_printSchema();
    init_typeFromAST();
    init_valueFromAST();
    init_valueFromASTUntyped();
    init_astFromValue();
    init_TypeInfo();
    init_coerceInputValue();
    init_concatAST();
    init_separateOperations();
    init_stripIgnoredCharacters();
    init_typeComparators();
    init_assertValidName();
    init_findBreakingChanges();
  }
});

// node_modules/graphql/index.mjs
var graphql_exports = {};
__export(graphql_exports, {
  BREAK: () => BREAK,
  BreakingChangeType: () => BreakingChangeType,
  DEFAULT_DEPRECATION_REASON: () => DEFAULT_DEPRECATION_REASON,
  DangerousChangeType: () => DangerousChangeType,
  DirectiveLocation: () => DirectiveLocation,
  ExecutableDefinitionsRule: () => ExecutableDefinitionsRule,
  FieldsOnCorrectTypeRule: () => FieldsOnCorrectTypeRule,
  FragmentsOnCompositeTypesRule: () => FragmentsOnCompositeTypesRule,
  GRAPHQL_MAX_INT: () => GRAPHQL_MAX_INT,
  GRAPHQL_MIN_INT: () => GRAPHQL_MIN_INT,
  GraphQLBoolean: () => GraphQLBoolean,
  GraphQLDeprecatedDirective: () => GraphQLDeprecatedDirective,
  GraphQLDirective: () => GraphQLDirective,
  GraphQLEnumType: () => GraphQLEnumType,
  GraphQLError: () => GraphQLError,
  GraphQLFloat: () => GraphQLFloat,
  GraphQLID: () => GraphQLID,
  GraphQLIncludeDirective: () => GraphQLIncludeDirective,
  GraphQLInputObjectType: () => GraphQLInputObjectType,
  GraphQLInt: () => GraphQLInt,
  GraphQLInterfaceType: () => GraphQLInterfaceType,
  GraphQLList: () => GraphQLList,
  GraphQLNonNull: () => GraphQLNonNull,
  GraphQLObjectType: () => GraphQLObjectType,
  GraphQLOneOfDirective: () => GraphQLOneOfDirective,
  GraphQLScalarType: () => GraphQLScalarType,
  GraphQLSchema: () => GraphQLSchema,
  GraphQLSkipDirective: () => GraphQLSkipDirective,
  GraphQLSpecifiedByDirective: () => GraphQLSpecifiedByDirective,
  GraphQLString: () => GraphQLString,
  GraphQLUnionType: () => GraphQLUnionType,
  Kind: () => Kind,
  KnownArgumentNamesRule: () => KnownArgumentNamesRule,
  KnownDirectivesRule: () => KnownDirectivesRule,
  KnownFragmentNamesRule: () => KnownFragmentNamesRule,
  KnownTypeNamesRule: () => KnownTypeNamesRule,
  Lexer: () => Lexer,
  Location: () => Location,
  LoneAnonymousOperationRule: () => LoneAnonymousOperationRule,
  LoneSchemaDefinitionRule: () => LoneSchemaDefinitionRule,
  MaxIntrospectionDepthRule: () => MaxIntrospectionDepthRule,
  NoDeprecatedCustomRule: () => NoDeprecatedCustomRule,
  NoFragmentCyclesRule: () => NoFragmentCyclesRule,
  NoSchemaIntrospectionCustomRule: () => NoSchemaIntrospectionCustomRule,
  NoUndefinedVariablesRule: () => NoUndefinedVariablesRule,
  NoUnusedFragmentsRule: () => NoUnusedFragmentsRule,
  NoUnusedVariablesRule: () => NoUnusedVariablesRule,
  OperationTypeNode: () => OperationTypeNode,
  OverlappingFieldsCanBeMergedRule: () => OverlappingFieldsCanBeMergedRule,
  PossibleFragmentSpreadsRule: () => PossibleFragmentSpreadsRule,
  PossibleTypeExtensionsRule: () => PossibleTypeExtensionsRule,
  ProvidedRequiredArgumentsRule: () => ProvidedRequiredArgumentsRule,
  ScalarLeafsRule: () => ScalarLeafsRule,
  SchemaMetaFieldDef: () => SchemaMetaFieldDef,
  SingleFieldSubscriptionsRule: () => SingleFieldSubscriptionsRule,
  Source: () => Source,
  Token: () => Token,
  TokenKind: () => TokenKind,
  TypeInfo: () => TypeInfo,
  TypeKind: () => TypeKind,
  TypeMetaFieldDef: () => TypeMetaFieldDef,
  TypeNameMetaFieldDef: () => TypeNameMetaFieldDef,
  UniqueArgumentDefinitionNamesRule: () => UniqueArgumentDefinitionNamesRule,
  UniqueArgumentNamesRule: () => UniqueArgumentNamesRule,
  UniqueDirectiveNamesRule: () => UniqueDirectiveNamesRule,
  UniqueDirectivesPerLocationRule: () => UniqueDirectivesPerLocationRule,
  UniqueEnumValueNamesRule: () => UniqueEnumValueNamesRule,
  UniqueFieldDefinitionNamesRule: () => UniqueFieldDefinitionNamesRule,
  UniqueFragmentNamesRule: () => UniqueFragmentNamesRule,
  UniqueInputFieldNamesRule: () => UniqueInputFieldNamesRule,
  UniqueOperationNamesRule: () => UniqueOperationNamesRule,
  UniqueOperationTypesRule: () => UniqueOperationTypesRule,
  UniqueTypeNamesRule: () => UniqueTypeNamesRule,
  UniqueVariableNamesRule: () => UniqueVariableNamesRule,
  ValidationContext: () => ValidationContext,
  ValuesOfCorrectTypeRule: () => ValuesOfCorrectTypeRule,
  VariablesAreInputTypesRule: () => VariablesAreInputTypesRule,
  VariablesInAllowedPositionRule: () => VariablesInAllowedPositionRule,
  __Directive: () => __Directive,
  __DirectiveLocation: () => __DirectiveLocation,
  __EnumValue: () => __EnumValue,
  __Field: () => __Field,
  __InputValue: () => __InputValue,
  __Schema: () => __Schema,
  __Type: () => __Type,
  __TypeKind: () => __TypeKind,
  assertAbstractType: () => assertAbstractType,
  assertCompositeType: () => assertCompositeType,
  assertDirective: () => assertDirective,
  assertEnumType: () => assertEnumType,
  assertEnumValueName: () => assertEnumValueName,
  assertInputObjectType: () => assertInputObjectType,
  assertInputType: () => assertInputType,
  assertInterfaceType: () => assertInterfaceType,
  assertLeafType: () => assertLeafType,
  assertListType: () => assertListType,
  assertName: () => assertName,
  assertNamedType: () => assertNamedType,
  assertNonNullType: () => assertNonNullType,
  assertNullableType: () => assertNullableType,
  assertObjectType: () => assertObjectType,
  assertOutputType: () => assertOutputType,
  assertScalarType: () => assertScalarType,
  assertSchema: () => assertSchema,
  assertType: () => assertType,
  assertUnionType: () => assertUnionType,
  assertValidName: () => assertValidName,
  assertValidSchema: () => assertValidSchema,
  assertWrappingType: () => assertWrappingType,
  astFromValue: () => astFromValue,
  buildASTSchema: () => buildASTSchema,
  buildClientSchema: () => buildClientSchema,
  buildSchema: () => buildSchema,
  coerceInputValue: () => coerceInputValue,
  concatAST: () => concatAST,
  createSourceEventStream: () => createSourceEventStream,
  defaultFieldResolver: () => defaultFieldResolver,
  defaultTypeResolver: () => defaultTypeResolver,
  doTypesOverlap: () => doTypesOverlap,
  execute: () => execute,
  executeSync: () => executeSync,
  extendSchema: () => extendSchema,
  findBreakingChanges: () => findBreakingChanges,
  findDangerousChanges: () => findDangerousChanges,
  formatError: () => formatError,
  getArgumentValues: () => getArgumentValues,
  getDirectiveValues: () => getDirectiveValues,
  getEnterLeaveForKind: () => getEnterLeaveForKind,
  getIntrospectionQuery: () => getIntrospectionQuery,
  getLocation: () => getLocation,
  getNamedType: () => getNamedType,
  getNullableType: () => getNullableType,
  getOperationAST: () => getOperationAST,
  getOperationRootType: () => getOperationRootType,
  getVariableValues: () => getVariableValues,
  getVisitFn: () => getVisitFn,
  graphql: () => graphql,
  graphqlSync: () => graphqlSync,
  introspectionFromSchema: () => introspectionFromSchema,
  introspectionTypes: () => introspectionTypes,
  isAbstractType: () => isAbstractType,
  isCompositeType: () => isCompositeType,
  isConstValueNode: () => isConstValueNode,
  isDefinitionNode: () => isDefinitionNode,
  isDirective: () => isDirective,
  isEnumType: () => isEnumType,
  isEqualType: () => isEqualType,
  isExecutableDefinitionNode: () => isExecutableDefinitionNode,
  isInputObjectType: () => isInputObjectType,
  isInputType: () => isInputType,
  isInterfaceType: () => isInterfaceType,
  isIntrospectionType: () => isIntrospectionType,
  isLeafType: () => isLeafType,
  isListType: () => isListType,
  isNamedType: () => isNamedType,
  isNonNullType: () => isNonNullType,
  isNullableType: () => isNullableType,
  isObjectType: () => isObjectType,
  isOutputType: () => isOutputType,
  isRequiredArgument: () => isRequiredArgument,
  isRequiredInputField: () => isRequiredInputField,
  isScalarType: () => isScalarType,
  isSchema: () => isSchema,
  isSelectionNode: () => isSelectionNode,
  isSpecifiedDirective: () => isSpecifiedDirective,
  isSpecifiedScalarType: () => isSpecifiedScalarType,
  isType: () => isType,
  isTypeDefinitionNode: () => isTypeDefinitionNode,
  isTypeExtensionNode: () => isTypeExtensionNode,
  isTypeNode: () => isTypeNode,
  isTypeSubTypeOf: () => isTypeSubTypeOf,
  isTypeSystemDefinitionNode: () => isTypeSystemDefinitionNode,
  isTypeSystemExtensionNode: () => isTypeSystemExtensionNode,
  isUnionType: () => isUnionType,
  isValidNameError: () => isValidNameError,
  isValueNode: () => isValueNode,
  isWrappingType: () => isWrappingType,
  lexicographicSortSchema: () => lexicographicSortSchema,
  locatedError: () => locatedError,
  parse: () => parse,
  parseConstValue: () => parseConstValue,
  parseType: () => parseType,
  parseValue: () => parseValue,
  print: () => print,
  printError: () => printError,
  printIntrospectionSchema: () => printIntrospectionSchema,
  printLocation: () => printLocation,
  printSchema: () => printSchema,
  printSourceLocation: () => printSourceLocation,
  printType: () => printType,
  recommendedRules: () => recommendedRules,
  resolveObjMapThunk: () => resolveObjMapThunk,
  resolveReadonlyArrayThunk: () => resolveReadonlyArrayThunk,
  responsePathAsArray: () => pathToArray,
  separateOperations: () => separateOperations,
  specifiedDirectives: () => specifiedDirectives,
  specifiedRules: () => specifiedRules,
  specifiedScalarTypes: () => specifiedScalarTypes,
  stripIgnoredCharacters: () => stripIgnoredCharacters,
  subscribe: () => subscribe,
  syntaxError: () => syntaxError,
  typeFromAST: () => typeFromAST,
  validate: () => validate2,
  validateSchema: () => validateSchema,
  valueFromAST: () => valueFromAST,
  valueFromASTUntyped: () => valueFromASTUntyped,
  version: () => version,
  versionInfo: () => versionInfo,
  visit: () => visit,
  visitInParallel: () => visitInParallel,
  visitWithTypeInfo: () => visitWithTypeInfo
});
var init_graphql2 = __esm({
  "node_modules/graphql/index.mjs"() {
    init_version();
    init_graphql();
    init_type();
    init_language();
    init_execution();
    init_validation();
    init_error();
    init_utilities();
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/schema-input-types.js
var require_schema_input_types = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/schema-input-types.js"(exports, module) {
    var e2 = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var s = Object.prototype.hasOwnProperty;
    var a = (n2, r) => {
      for (var i in r)
        e2(n2, i, { get: r[i], enumerable: true });
    };
    var h2 = (n2, r, i, o) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let t2 of c(r))
          !s.call(n2, t2) && t2 !== i && e2(n2, t2, { get: () => r[t2], enumerable: !(o = u2(r, t2)) || o.enumerable });
      return n2;
    };
    var l = (n2) => h2(e2({}, "__esModule", { value: true }), n2);
    var I = {};
    a(I, { isApiSchemaInput: () => S, isIntrospectionInput: () => f, isPrebuiltSchema: () => x, isSdlSchemaInput: () => m });
    module.exports = l(I);
    var p = (init_graphql2(), __toCommonJS(graphql_exports));
    function m(n2) {
      return "sdl" in n2;
    }
    function f(n2) {
      return "introspection" in n2;
    }
    function S(n2) {
      return "url" in n2;
    }
    function x(n2) {
      return "graphql" in n2 && (0, p.isSchema)(n2.graphql);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/services/fetch-introspection.js
var require_fetch_introspection = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/services/fetch-introspection.js"(exports, module) {
    var a = Object.defineProperty;
    var w = Object.getOwnPropertyDescriptor;
    var y = Object.getOwnPropertyNames;
    var l = Object.prototype.hasOwnProperty;
    var b = (t2, n2) => {
      for (var e2 in n2)
        a(t2, e2, { get: n2[e2], enumerable: true });
    };
    var j = (t2, n2, e2, o) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let r of y(n2))
          !l.call(t2, r) && r !== e2 && a(t2, r, { get: () => n2[r], enumerable: !(o = w(n2, r)) || o.enumerable });
      return t2;
    };
    var m = (t2) => j(a({}, "__esModule", { value: true }), t2);
    var g = {};
    b(g, { fetchIntrospection: () => O });
    module.exports = m(g);
    var u2 = (init_graphql2(), __toCommonJS(graphql_exports));
    var v = function(t2, n2, e2, o) {
      function r(c) {
        return c instanceof e2 ? c : new e2(function(s) {
          s(c);
        });
      }
      return new (e2 || (e2 = Promise))(function(c, s) {
        function h2(i) {
          try {
            f(o.next(i));
          } catch (p) {
            s(p);
          }
        }
        function d2(i) {
          try {
            f(o.throw(i));
          } catch (p) {
            s(p);
          }
        }
        function f(i) {
          i.done ? c(i.value) : r(i.value).then(h2, d2);
        }
        f((o = o.apply(t2, n2 || [])).next());
      });
    };
    function O(t2, n2) {
      return v(this, void 0, void 0, function* () {
        if (!global.fetch)
          throw new Error("GraphQL API introspection is not supported outside of a browser.");
        const e2 = JSON.stringify({ query: (0, u2.getIntrospectionQuery)() }), o = yield fetch(t2, { method: "POST", headers: _(n2), body: e2 }).then((r) => r.json());
        if (o.errors)
          throw new Error("GraphQL service replied with errors: " + JSON.stringify(o.errors));
        return o.data;
      });
    }
    function _(t2) {
      return Object.assign({ "Content-Type": "application/json", Accept: "application/json" }, t2);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/services/get-graphql-schema.js
var require_get_graphql_schema = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/services/get-graphql-schema.js"(exports, module) {
    var u2 = Object.defineProperty;
    var g = Object.getOwnPropertyDescriptor;
    var y = Object.getOwnPropertyNames;
    var E = Object.prototype.hasOwnProperty;
    var F = (r, e2) => {
      for (var t2 in e2)
        u2(r, t2, { get: e2[t2], enumerable: true });
    };
    var I = (r, e2, t2, n2) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let o of y(e2))
          !E.call(r, o) && o !== t2 && u2(r, o, { get: () => e2[o], enumerable: !(n2 = g(e2, o)) || n2.enumerable });
      return r;
    };
    var b = (r) => I(u2({}, "__esModule", { value: true }), r);
    var _ = {};
    F(_, { getGraphQlSchema: () => v });
    module.exports = b(_);
    var h2 = (init_graphql2(), __toCommonJS(graphql_exports));
    var i = require_schema_input_types();
    var p = require_fetch_introspection();
    var l = function(r, e2, t2, n2) {
      function o(a) {
        return a instanceof t2 ? a : new t2(function(s) {
          s(a);
        });
      }
      return new (t2 || (t2 = Promise))(function(a, s) {
        function w(c) {
          try {
            m(n2.next(c));
          } catch (f) {
            s(f);
          }
        }
        function S(c) {
          try {
            m(n2.throw(c));
          } catch (f) {
            s(f);
          }
        }
        function m(c) {
          c.done ? a(c.value) : o(c.value).then(w, S);
        }
        m((n2 = n2.apply(r, e2 || [])).next());
      });
    };
    function v(r) {
      return l(this, void 0, void 0, function* () {
        if ((0, i.isSdlSchemaInput)(r))
          return G(r.sdl);
        if ((0, i.isIntrospectionInput)(r))
          return d2(r.introspection);
        if ((0, i.isApiSchemaInput)(r))
          return Q(r.url, r.headers);
        if ((0, i.isPrebuiltSchema)(r))
          return r.graphql;
        throw new Error("Unknown schema type");
      });
    }
    function G(r) {
      try {
        return (0, h2.buildSchema)(r);
      } catch (e2) {
        const t2 = e2 instanceof Error && e2.message;
        throw new Error("Failed building schema from SDL. " + t2);
      }
    }
    function d2(r) {
      try {
        return (0, h2.buildClientSchema)(r);
      } catch (e2) {
        const t2 = e2 instanceof Error && e2.message;
        throw new Error("Failed building schema from introspection. " + t2);
      }
    }
    function Q(r, e2) {
      return l(this, void 0, void 0, function* () {
        let t2;
        try {
          t2 = yield (0, p.fetchIntrospection)(r, e2);
        } catch (n2) {
          const o = n2 instanceof Error && n2.message;
          throw new Error("Failed fetching schema introspection. " + o);
        }
        return d2(t2);
      });
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/services/index.js
var require_services = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/services/index.js"(exports, module) {
    var m = Object.defineProperty;
    var t2 = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var f = Object.prototype.hasOwnProperty;
    var g = (r, e2) => {
      for (var h2 in e2)
        m(r, h2, { get: e2[h2], enumerable: true });
    };
    var l = (r, e2, h2, o) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let a of c(e2))
          !f.call(r, a) && a !== h2 && m(r, a, { get: () => e2[a], enumerable: !(o = t2(e2, a)) || o.enumerable });
      return r;
    };
    var x = (r) => l(m({}, "__esModule", { value: true }), r);
    var G = {};
    g(G, { getGraphQlSchema: () => p.getGraphQlSchema });
    module.exports = x(G);
    var p = require_get_graphql_schema();
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/use-graphql-schema.js
var require_use_graphql_schema = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/use-graphql-schema.js"(exports, module) {
    var l = Object.defineProperty;
    var S = Object.getOwnPropertyDescriptor;
    var w = Object.getOwnPropertyNames;
    var v = Object.prototype.hasOwnProperty;
    var y = (t2, e2) => {
      for (var n2 in e2)
        l(t2, n2, { get: e2[n2], enumerable: true });
    };
    var _ = (t2, e2, n2, i) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let c of w(e2))
          !v.call(t2, c) && c !== n2 && l(t2, c, { get: () => e2[c], enumerable: !(i = S(e2, c)) || i.enumerable });
      return t2;
    };
    var b = (t2) => _(l({}, "__esModule", { value: true }), t2);
    var x = {};
    y(x, { useGraphqlSchema: () => q });
    module.exports = b(x);
    var r = __require("react");
    var m = require_services();
    var d2 = require_schema_input_types();
    var g = function(t2, e2, n2, i) {
      function c(o) {
        return o instanceof n2 ? o : new n2(function(f) {
          f(o);
        });
      }
      return new (n2 || (n2 = Promise))(function(o, f) {
        function s(a) {
          try {
            h2(i.next(a));
          } catch (u2) {
            f(u2);
          }
        }
        function p(a) {
          try {
            h2(i.throw(a));
          } catch (u2) {
            f(u2);
          }
        }
        function h2(a) {
          a.done ? o(a.value) : c(a.value).then(s, p);
        }
        h2((i = i.apply(t2, e2 || [])).next());
      });
    };
    function q(t2) {
      const e2 = (0, d2.isPrebuiltSchema)(t2), [n2, i] = (0, r.useState)(e2 ? t2.graphql : void 0), [c, o] = (0, r.useState)(!e2);
      return (0, r.useEffect)(() => {
        function f() {
          return g(this, void 0, void 0, function* () {
            o(true);
            const s = yield (0, m.getGraphQlSchema)(t2);
            i(s), o(false);
          });
        }
        e2 ? i(t2.graphql) : f();
      }, Object.values(t2)), { schema: n2, loading: c };
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/models/graphql-fields-type.js
var require_graphql_fields_type = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/models/graphql-fields-type.js"(exports, module) {
    var t2 = Object.defineProperty;
    var a = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var d2 = (o, e2, x, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let p of b(e2))
          !c.call(o, p) && p !== x && t2(o, p, { get: () => e2[p], enumerable: !(r = a(e2, p)) || r.enumerable });
      return o;
    };
    var f = (o) => d2(t2({}, "__esModule", { value: true }), o);
    var g = {};
    module.exports = f(g);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/models/type-group.js
var require_type_group = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/models/type-group.js"(exports, module) {
    var c = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var m = Object.prototype.hasOwnProperty;
    var r = (s, i) => {
      for (var t2 in i)
        c(s, t2, { get: i[t2], enumerable: true });
    };
    var f = (s, i, t2, e2) => {
      if (i && typeof i == "object" || typeof i == "function")
        for (let n2 of b(i))
          !m.call(s, n2) && n2 !== t2 && c(s, n2, { get: () => i[n2], enumerable: !(e2 = u2(i, n2)) || e2.enumerable });
      return s;
    };
    var v = (s) => f(c({}, "__esModule", { value: true }), s);
    var d2 = {};
    r(d2, { TypeGroup: () => a });
    module.exports = v(d2);
    var a;
    (function(s) {
      s.queries = "queries", s.mutations = "mutations", s.subscriptions = "subscriptions", s.directives = "directives", s.objects = "objects", s.interfaces = "interfaces", s.unions = "unions", s.enums = "enums", s.inputs = "inputs", s.scalars = "scalars";
    })(a || (a = {}));
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/models/return-type.js
var require_return_type = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/models/return-type.js"(exports, module) {
    var t2 = Object.defineProperty;
    var a = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var d2 = (o, e2, x, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let p of b(e2))
          !c.call(o, p) && p !== x && t2(o, p, { get: () => e2[p], enumerable: !(r = a(e2, p)) || r.enumerable });
      return o;
    };
    var f = (o) => d2(t2({}, "__esModule", { value: true }), o);
    var g = {};
    module.exports = f(g);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/models/field.js
var require_field = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/models/field.js"(exports, module) {
    var t2 = Object.defineProperty;
    var a = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var d2 = (o, e2, x, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let p of b(e2))
          !c.call(o, p) && p !== x && t2(o, p, { get: () => e2[p], enumerable: !(r = a(e2, p)) || r.enumerable });
      return o;
    };
    var f = (o) => d2(t2({}, "__esModule", { value: true }), o);
    var g = {};
    module.exports = f(g);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/models/graphql-object-like-type.js
var require_graphql_object_like_type = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/models/graphql-object-like-type.js"(exports, module) {
    var t2 = Object.defineProperty;
    var a = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var d2 = (o, e2, x, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let p of b(e2))
          !c.call(o, p) && p !== x && t2(o, p, { get: () => e2[p], enumerable: !(r = a(e2, p)) || r.enumerable });
      return o;
    };
    var f = (o) => d2(t2({}, "__esModule", { value: true }), o);
    var g = {};
    module.exports = f(g);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/models/graphql-named-output-type.js
var require_graphql_named_output_type = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/models/graphql-named-output-type.js"(exports, module) {
    var t2 = Object.defineProperty;
    var a = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var d2 = (o, e2, x, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let p of b(e2))
          !c.call(o, p) && p !== x && t2(o, p, { get: () => e2[p], enumerable: !(r = a(e2, p)) || r.enumerable });
      return o;
    };
    var f = (o) => d2(t2({}, "__esModule", { value: true }), o);
    var g = {};
    module.exports = f(g);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/models/index.js
var require_models = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/models/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_graphql_fields_type(), module.exports);
    r(o, require_type_group(), module.exports);
    r(o, require_return_type(), module.exports);
    r(o, require_field(), module.exports);
    r(o, require_graphql_object_like_type(), module.exports);
    r(o, require_graphql_named_output_type(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/helpers/is-fields-type.js
var require_is_fields_type = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/helpers/is-fields-type.js"(exports, module) {
    var p = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var o = Object.getOwnPropertyNames;
    var T = Object.prototype.hasOwnProperty;
    var f = (e2, i) => {
      for (var n2 in i)
        p(e2, n2, { get: i[n2], enumerable: true });
    };
    var u2 = (e2, i, n2, s) => {
      if (i && typeof i == "object" || typeof i == "function")
        for (let t2 of o(i))
          !T.call(e2, t2) && t2 !== n2 && p(e2, t2, { get: () => i[t2], enumerable: !(s = c(i, t2)) || s.enumerable });
      return e2;
    };
    var b = (e2) => u2(p({}, "__esModule", { value: true }), e2);
    var m = {};
    f(m, { isFieldsType: () => j });
    module.exports = b(m);
    var r = (init_graphql2(), __toCommonJS(graphql_exports));
    function j(e2) {
      return (0, r.isObjectType)(e2) || (0, r.isInterfaceType)(e2) || (0, r.isInputObjectType)(e2);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/helpers/is-internal-type.js
var require_is_internal_type = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/helpers/is-internal-type.js"(exports, module) {
    var a = Object.defineProperty;
    var s = Object.getOwnPropertyDescriptor;
    var o = Object.getOwnPropertyNames;
    var p = Object.prototype.hasOwnProperty;
    var u2 = (t2, n2) => {
      for (var r in n2)
        a(t2, r, { get: n2[r], enumerable: true });
    };
    var _ = (t2, n2, r, i) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let e2 of o(n2))
          !p.call(t2, e2) && e2 !== r && a(t2, e2, { get: () => n2[e2], enumerable: !(i = s(n2, e2)) || i.enumerable });
      return t2;
    };
    var c = (t2) => _(a({}, "__esModule", { value: true }), t2);
    var h2 = {};
    u2(h2, { isInternalType: () => f });
    module.exports = c(h2);
    function f(t2) {
      return t2.name.startsWith("__");
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/helpers/is-root-type.js
var require_is_root_type = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/helpers/is-root-type.js"(exports, module) {
    var y = Object.defineProperty;
    var n2 = Object.getOwnPropertyDescriptor;
    var T = Object.getOwnPropertyNames;
    var i = Object.prototype.hasOwnProperty;
    var u2 = (o, t2) => {
      for (var e2 in t2)
        y(o, e2, { get: t2[e2], enumerable: true });
    };
    var s = (o, t2, e2, r) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let p of T(t2))
          !i.call(o, p) && p !== e2 && y(o, p, { get: () => t2[p], enumerable: !(r = n2(t2, p)) || r.enumerable });
      return o;
    };
    var g = (o) => s(y({}, "__esModule", { value: true }), o);
    var c = {};
    u2(c, { isRootType: () => b });
    module.exports = g(c);
    function b(o, t2) {
      return [t2.getQueryType(), t2.getMutationType(), t2.getSubscriptionType()].some((r) => r === o);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/helpers/is-excluded-type.js
var require_is_excluded_type = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/helpers/is-excluded-type.js"(exports, module) {
    var t2 = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var s = Object.getOwnPropertyNames;
    var u2 = Object.prototype.hasOwnProperty;
    var T = (r, o) => {
      for (var i in o)
        t2(r, i, { get: o[i], enumerable: true });
    };
    var c = (r, o, i, n2) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of s(o))
          !u2.call(r, e2) && e2 !== i && t2(r, e2, { get: () => o[e2], enumerable: !(n2 = f(o, e2)) || n2.enumerable });
      return r;
    };
    var d2 = (r) => c(t2({}, "__esModule", { value: true }), r);
    var x = {};
    T(x, { isExcludedType: () => l });
    module.exports = d2(x);
    var p = require_is_internal_type();
    var m = require_is_root_type();
    function l(r, o) {
      return (0, p.isInternalType)(r) || (0, m.isRootType)(r, o);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/helpers/is-connection-field.js
var require_is_connection_field = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/helpers/is-connection-field.js"(exports, module) {
    var s = Object.defineProperty;
    var i = Object.getOwnPropertyDescriptor;
    var a = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var r = (e2, n2) => {
      for (var t2 in n2)
        s(e2, t2, { get: n2[t2], enumerable: true });
    };
    var C = (e2, n2, t2, d2) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let o of a(n2))
          !c.call(e2, o) && o !== t2 && s(e2, o, { get: () => n2[o], enumerable: !(d2 = i(n2, o)) || d2.enumerable });
      return e2;
    };
    var I = (e2) => C(s({}, "__esModule", { value: true }), e2);
    var g = {};
    r(g, { isConnectionField: () => f });
    module.exports = I(g);
    var N = /* @__PURE__ */ new Set(["edges", "nodes", "node", "pageInfo"]);
    function f(e2) {
      return N.has(e2.name);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/helpers/get-type-group.js
var require_get_type_group = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/helpers/get-type-group.js"(exports, module) {
    var r = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var o = Object.getOwnPropertyNames;
    var u2 = Object.prototype.hasOwnProperty;
    var T = (t2, n2) => {
      for (var i in n2)
        r(t2, i, { get: n2[i], enumerable: true });
    };
    var a = (t2, n2, i, p) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let s of o(n2))
          !u2.call(t2, s) && s !== i && r(t2, s, { get: () => n2[s], enumerable: !(p = c(n2, s)) || p.enumerable });
      return t2;
    };
    var y = (t2) => a(r({}, "__esModule", { value: true }), t2);
    var P = {};
    T(P, { getTypeGroup: () => f });
    module.exports = y(P);
    var e2 = (init_graphql2(), __toCommonJS(graphql_exports));
    var E = { scalars: e2.isScalarType, objects: e2.isObjectType, interfaces: e2.isInterfaceType, unions: e2.isUnionType, enums: e2.isEnumType, inputs: e2.isInputType };
    function f(t2) {
      return Object.entries(E).find(([, i]) => i(t2))[0];
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/helpers/get-type-field.js
var require_get_type_field = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/helpers/get-type-field.js"(exports, module) {
    var l = Object.defineProperty;
    var s = Object.getOwnPropertyDescriptor;
    var f = Object.getOwnPropertyNames;
    var o = Object.prototype.hasOwnProperty;
    var r = (t2, e2) => {
      for (var i in e2)
        l(t2, i, { get: e2[i], enumerable: true });
    };
    var c = (t2, e2, i, n2) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let d2 of f(e2))
          !o.call(t2, d2) && d2 !== i && l(t2, d2, { get: () => e2[d2], enumerable: !(n2 = s(e2, d2)) || n2.enumerable });
      return t2;
    };
    var g = (t2) => c(l({}, "__esModule", { value: true }), t2);
    var u2 = {};
    r(u2, { getTypeField: () => p });
    module.exports = g(u2);
    function p(t2, e2) {
      return t2.getFields()[e2];
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/helpers/to-return-type.js
var require_to_return_type = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/helpers/to-return-type.js"(exports, module) {
    var l = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var u2 = Object.prototype.hasOwnProperty;
    var T = (s, n2) => {
      for (var t2 in n2)
        l(s, t2, { get: n2[t2], enumerable: true });
    };
    var r = (s, n2, t2, i) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let o of p(n2))
          !u2.call(s, o) && o !== t2 && l(s, o, { get: () => n2[o], enumerable: !(i = c(n2, o)) || i.enumerable });
      return s;
    };
    var y = (s) => r(l({}, "__esModule", { value: true }), s);
    var f = {};
    T(f, { toReturnType: () => a });
    module.exports = y(f);
    var e2 = (init_graphql2(), __toCommonJS(graphql_exports));
    function a(s) {
      const n2 = (0, e2.getNamedType)(s), t2 = (0, e2.isNonNullType)(s), i = t2 ? s.ofType : s, o = (0, e2.isListType)(i), N = o ? (0, e2.isNonNullType)(i.ofType) : t2;
      return Object.assign(Object.assign({}, n2), { isList: o, isNonNull: N, isListNonNull: o && t2 });
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/helpers/get-named-output-type.js
var require_get_named_output_type = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/helpers/get-named-output-type.js"(exports, module) {
    var m = Object.defineProperty;
    var n2 = Object.getOwnPropertyDescriptor;
    var a = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var f = (e2, t2) => {
      for (var r in t2)
        m(e2, r, { get: t2[r], enumerable: true });
    };
    var g = (e2, t2, r, o) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let p of a(t2))
          !d2.call(e2, p) && p !== r && m(e2, p, { get: () => t2[p], enumerable: !(o = n2(t2, p)) || o.enumerable });
      return e2;
    };
    var i = (e2) => g(m({}, "__esModule", { value: true }), e2);
    var N = {};
    f(N, { getNamedOutputType: () => y });
    module.exports = i(N);
    var u2 = (init_graphql2(), __toCommonJS(graphql_exports));
    function y(e2) {
      return (0, u2.getNamedType)(e2);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/helpers/index.js
var require_helpers = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/helpers/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_is_fields_type(), module.exports);
    r(o, require_is_excluded_type(), module.exports);
    r(o, require_is_connection_field(), module.exports);
    r(o, require_get_type_group(), module.exports);
    r(o, require_get_type_field(), module.exports);
    r(o, require_to_return_type(), module.exports);
    r(o, require_get_named_output_type(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/index.js
var require_schema = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_schema_input_types(), module.exports);
    r(o, require_use_graphql_schema(), module.exports);
    r(o, require_models(), module.exports);
    r(o, require_helpers(), module.exports);
    r(o, require_services(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/app/app.store.js
var require_app_store = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/app/app.store.js"(exports, module) {
    var p = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var o = Object.getOwnPropertyNames;
    var h2 = Object.prototype.hasOwnProperty;
    var f = (i, e2) => {
      for (var t2 in e2)
        p(i, t2, { get: e2[t2], enumerable: true });
    };
    var g = (i, e2, t2, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let n2 of o(e2))
          !h2.call(i, n2) && n2 !== t2 && p(i, n2, { get: () => e2[n2], enumerable: !(r = u2(e2, n2)) || r.enumerable });
      return i;
    };
    var y = (i) => g(p({}, "__esModule", { value: true }), i);
    var T = {};
    f(T, { AppStore: () => l });
    module.exports = y(T);
    var c = (init_graphql2(), __toCommonJS(graphql_exports));
    var s = require_schema();
    var l = class {
      constructor(e2) {
        Object.defineProperty(this, "schema", { enumerable: true, configurable: true, writable: true, value: e2 }), Object.defineProperty(this, "referenceMap", { enumerable: true, configurable: true, writable: true, value: /* @__PURE__ */ new Map() }), this.createReferenceMap();
      }
      getQueryType() {
        return this.schema.getQueryType();
      }
      getQueryField(e2) {
        const t2 = this.getQueryType();
        return t2 && (0, s.getTypeField)(t2, e2);
      }
      getMutationType() {
        return this.schema.getMutationType();
      }
      getMutationField(e2) {
        const t2 = this.getMutationType();
        return t2 && (0, s.getTypeField)(t2, e2);
      }
      getSubscriptionType() {
        return this.schema.getSubscriptionType();
      }
      getSubscriptionField(e2) {
        const t2 = this.getSubscriptionType();
        return t2 && (0, s.getTypeField)(t2, e2);
      }
      getDirective(e2) {
        return this.schema.getDirective(e2) || void 0;
      }
      getType(e2) {
        return this.schema.getType(e2) || void 0;
      }
      getTypeReferences(e2) {
        return this.referenceMap.get(e2);
      }
      createReferenceMap() {
        const e2 = this.schema.getTypeMap();
        Object.values(e2).forEach((t2) => {
          !(0, s.isExcludedType)(t2, this.schema) && (0, s.isFieldsType)(t2) ? this.setFieldsTypeReferences(t2) : (0, c.isUnionType)(t2) && this.setUnionTypeReferences(t2);
        });
      }
      setFieldsTypeReferences(e2) {
        const t2 = e2.getFields();
        Object.values(t2).forEach((r) => {
          const n2 = (0, c.getNamedType)(r.type), a = { name: e2.name, field: r.name, group: (0, s.getTypeGroup)(e2) };
          this.setReference(n2.name, a);
        });
      }
      setUnionTypeReferences(e2) {
        e2.getTypes().forEach((t2) => {
          const r = (0, c.getNamedType)(t2), n2 = { name: e2.name, group: s.TypeGroup.unions };
          this.setReference(r.name, n2);
        });
      }
      setReference(e2, t2) {
        const r = this.referenceMap.get(e2);
        r?.length ? r.push(t2) : this.referenceMap.set(e2, [t2]);
      }
    };
  }
});

// node_modules/@redocly/graphql-docs/lib/src/app/app-store.context.js
var require_app_store_context = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/app/app-store.context.js"(exports, module) {
    var p = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var s = (t2, e2) => {
      for (var r in e2)
        p(t2, r, { get: e2[r], enumerable: true });
    };
    var f = (t2, e2, r, i) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let o of c(e2))
          !d2.call(t2, o) && o !== r && p(t2, o, { get: () => e2[o], enumerable: !(i = u2(e2, o)) || i.enumerable });
      return t2;
    };
    var C = (t2) => f(p({}, "__esModule", { value: true }), t2);
    var v = {};
    s(v, { AppStoreContext: () => x, AppStoreProvider: () => S, useStore: () => m });
    module.exports = C(v);
    var n2 = __require("react");
    var x = (0, n2.createContext)(void 0);
    var S = x.Provider;
    function m() {
      return (0, n2.useContext)(x);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/app/models/type-reference.js
var require_type_reference = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/app/models/type-reference.js"(exports, module) {
    var t2 = Object.defineProperty;
    var a = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var d2 = (o, e2, x, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let p of b(e2))
          !c.call(o, p) && p !== x && t2(o, p, { get: () => e2[p], enumerable: !(r = a(e2, p)) || r.enumerable });
      return o;
    };
    var f = (o) => d2(t2({}, "__esModule", { value: true }), o);
    var g = {};
    module.exports = f(g);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/app/models/index.js
var require_models2 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/app/models/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var p = (r, o, f, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of c(o))
          !d2.call(r, e2) && e2 !== f && a(r, e2, { get: () => o[e2], enumerable: !(x = b(o, e2)) || x.enumerable });
      return r;
    };
    var t2 = (r, o, f) => (p(r, o, "default"), f && p(f, o, "default"));
    var g = (r) => p(a({}, "__esModule", { value: true }), r);
    var m = {};
    module.exports = g(m);
    t2(m, require_type_reference(), module.exports);
  }
});

// node_modules/marked/lib/marked.cjs
var require_marked = __commonJS({
  "node_modules/marked/lib/marked.cjs"(exports) {
    "use strict";
    function _defineProperties(target, props) {
      for (var i = 0; i < props.length; i++) {
        var descriptor = props[i];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor)
          descriptor.writable = true;
        Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps)
        _defineProperties(Constructor.prototype, protoProps);
      if (staticProps)
        _defineProperties(Constructor, staticProps);
      Object.defineProperty(Constructor, "prototype", {
        writable: false
      });
      return Constructor;
    }
    function _extends() {
      _extends = Object.assign ? Object.assign.bind() : function(target) {
        for (var i = 1; i < arguments.length; i++) {
          var source = arguments[i];
          for (var key in source) {
            if (Object.prototype.hasOwnProperty.call(source, key)) {
              target[key] = source[key];
            }
          }
        }
        return target;
      };
      return _extends.apply(this, arguments);
    }
    function _unsupportedIterableToArray(o, minLen) {
      if (!o)
        return;
      if (typeof o === "string")
        return _arrayLikeToArray(o, minLen);
      var n2 = Object.prototype.toString.call(o).slice(8, -1);
      if (n2 === "Object" && o.constructor)
        n2 = o.constructor.name;
      if (n2 === "Map" || n2 === "Set")
        return Array.from(o);
      if (n2 === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n2))
        return _arrayLikeToArray(o, minLen);
    }
    function _arrayLikeToArray(arr, len) {
      if (len == null || len > arr.length)
        len = arr.length;
      for (var i = 0, arr2 = new Array(len); i < len; i++)
        arr2[i] = arr[i];
      return arr2;
    }
    function _createForOfIteratorHelperLoose(o, allowArrayLike) {
      var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"];
      if (it)
        return (it = it.call(o)).next.bind(it);
      if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") {
        if (it)
          o = it;
        var i = 0;
        return function() {
          if (i >= o.length)
            return {
              done: true
            };
          return {
            done: false,
            value: o[i++]
          };
        };
      }
      throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    function _toPrimitive(input, hint) {
      if (typeof input !== "object" || input === null)
        return input;
      var prim = input[Symbol.toPrimitive];
      if (prim !== void 0) {
        var res = prim.call(input, hint || "default");
        if (typeof res !== "object")
          return res;
        throw new TypeError("@@toPrimitive must return a primitive value.");
      }
      return (hint === "string" ? String : Number)(input);
    }
    function _toPropertyKey(arg) {
      var key = _toPrimitive(arg, "string");
      return typeof key === "symbol" ? key : String(key);
    }
    function getDefaults() {
      return {
        async: false,
        baseUrl: null,
        breaks: false,
        extensions: null,
        gfm: true,
        headerIds: true,
        headerPrefix: "",
        highlight: null,
        hooks: null,
        langPrefix: "language-",
        mangle: true,
        pedantic: false,
        renderer: null,
        sanitize: false,
        sanitizer: null,
        silent: false,
        smartypants: false,
        tokenizer: null,
        walkTokens: null,
        xhtml: false
      };
    }
    exports.defaults = getDefaults();
    function changeDefaults(newDefaults) {
      exports.defaults = newDefaults;
    }
    var escapeTest = /[&<>"']/;
    var escapeReplace = new RegExp(escapeTest.source, "g");
    var escapeTestNoEncode = /[<>"']|&(?!(#\d{1,7}|#[Xx][a-fA-F0-9]{1,6}|\w+);)/;
    var escapeReplaceNoEncode = new RegExp(escapeTestNoEncode.source, "g");
    var escapeReplacements = {
      "&": "&amp;",
      "<": "&lt;",
      ">": "&gt;",
      '"': "&quot;",
      "'": "&#39;"
    };
    var getEscapeReplacement = function getEscapeReplacement2(ch) {
      return escapeReplacements[ch];
    };
    function escape(html, encode) {
      if (encode) {
        if (escapeTest.test(html)) {
          return html.replace(escapeReplace, getEscapeReplacement);
        }
      } else {
        if (escapeTestNoEncode.test(html)) {
          return html.replace(escapeReplaceNoEncode, getEscapeReplacement);
        }
      }
      return html;
    }
    var unescapeTest = /&(#(?:\d+)|(?:#x[0-9A-Fa-f]+)|(?:\w+));?/ig;
    function unescape(html) {
      return html.replace(unescapeTest, function(_, n2) {
        n2 = n2.toLowerCase();
        if (n2 === "colon")
          return ":";
        if (n2.charAt(0) === "#") {
          return n2.charAt(1) === "x" ? String.fromCharCode(parseInt(n2.substring(2), 16)) : String.fromCharCode(+n2.substring(1));
        }
        return "";
      });
    }
    var caret = /(^|[^\[])\^/g;
    function edit(regex, opt) {
      regex = typeof regex === "string" ? regex : regex.source;
      opt = opt || "";
      var obj = {
        replace: function replace(name, val) {
          val = val.source || val;
          val = val.replace(caret, "$1");
          regex = regex.replace(name, val);
          return obj;
        },
        getRegex: function getRegex() {
          return new RegExp(regex, opt);
        }
      };
      return obj;
    }
    var nonWordAndColonTest = /[^\w:]/g;
    var originIndependentUrl = /^$|^[a-z][a-z0-9+.-]*:|^[?#]/i;
    function cleanUrl(sanitize, base, href) {
      if (sanitize) {
        var prot;
        try {
          prot = decodeURIComponent(unescape(href)).replace(nonWordAndColonTest, "").toLowerCase();
        } catch (e2) {
          return null;
        }
        if (prot.indexOf("javascript:") === 0 || prot.indexOf("vbscript:") === 0 || prot.indexOf("data:") === 0) {
          return null;
        }
      }
      if (base && !originIndependentUrl.test(href)) {
        href = resolveUrl(base, href);
      }
      try {
        href = encodeURI(href).replace(/%25/g, "%");
      } catch (e2) {
        return null;
      }
      return href;
    }
    var baseUrls = {};
    var justDomain = /^[^:]+:\/*[^/]*$/;
    var protocol = /^([^:]+:)[\s\S]*$/;
    var domain = /^([^:]+:\/*[^/]*)[\s\S]*$/;
    function resolveUrl(base, href) {
      if (!baseUrls[" " + base]) {
        if (justDomain.test(base)) {
          baseUrls[" " + base] = base + "/";
        } else {
          baseUrls[" " + base] = rtrim(base, "/", true);
        }
      }
      base = baseUrls[" " + base];
      var relativeBase = base.indexOf(":") === -1;
      if (href.substring(0, 2) === "//") {
        if (relativeBase) {
          return href;
        }
        return base.replace(protocol, "$1") + href;
      } else if (href.charAt(0) === "/") {
        if (relativeBase) {
          return href;
        }
        return base.replace(domain, "$1") + href;
      } else {
        return base + href;
      }
    }
    var noopTest = {
      exec: function noopTest2() {
      }
    };
    function splitCells(tableRow, count) {
      var row = tableRow.replace(/\|/g, function(match, offset, str) {
        var escaped = false, curr = offset;
        while (--curr >= 0 && str[curr] === "\\") {
          escaped = !escaped;
        }
        if (escaped) {
          return "|";
        } else {
          return " |";
        }
      }), cells = row.split(/ \|/);
      var i = 0;
      if (!cells[0].trim()) {
        cells.shift();
      }
      if (cells.length > 0 && !cells[cells.length - 1].trim()) {
        cells.pop();
      }
      if (cells.length > count) {
        cells.splice(count);
      } else {
        while (cells.length < count) {
          cells.push("");
        }
      }
      for (; i < cells.length; i++) {
        cells[i] = cells[i].trim().replace(/\\\|/g, "|");
      }
      return cells;
    }
    function rtrim(str, c, invert) {
      var l = str.length;
      if (l === 0) {
        return "";
      }
      var suffLen = 0;
      while (suffLen < l) {
        var currChar = str.charAt(l - suffLen - 1);
        if (currChar === c && !invert) {
          suffLen++;
        } else if (currChar !== c && invert) {
          suffLen++;
        } else {
          break;
        }
      }
      return str.slice(0, l - suffLen);
    }
    function findClosingBracket(str, b) {
      if (str.indexOf(b[1]) === -1) {
        return -1;
      }
      var l = str.length;
      var level = 0, i = 0;
      for (; i < l; i++) {
        if (str[i] === "\\") {
          i++;
        } else if (str[i] === b[0]) {
          level++;
        } else if (str[i] === b[1]) {
          level--;
          if (level < 0) {
            return i;
          }
        }
      }
      return -1;
    }
    function checkSanitizeDeprecation(opt) {
      if (opt && opt.sanitize && !opt.silent) {
        console.warn("marked(): sanitize and sanitizer parameters are deprecated since version 0.7.0, should not be used and will be removed in the future. Read more here: https://marked.js.org/#/USING_ADVANCED.md#options");
      }
    }
    function repeatString(pattern, count) {
      if (count < 1) {
        return "";
      }
      var result = "";
      while (count > 1) {
        if (count & 1) {
          result += pattern;
        }
        count >>= 1;
        pattern += pattern;
      }
      return result + pattern;
    }
    function outputLink(cap, link, raw, lexer2) {
      var href = link.href;
      var title = link.title ? escape(link.title) : null;
      var text = cap[1].replace(/\\([\[\]])/g, "$1");
      if (cap[0].charAt(0) !== "!") {
        lexer2.state.inLink = true;
        var token = {
          type: "link",
          raw,
          href,
          title,
          text,
          tokens: lexer2.inlineTokens(text)
        };
        lexer2.state.inLink = false;
        return token;
      }
      return {
        type: "image",
        raw,
        href,
        title,
        text: escape(text)
      };
    }
    function indentCodeCompensation(raw, text) {
      var matchIndentToCode = raw.match(/^(\s+)(?:```)/);
      if (matchIndentToCode === null) {
        return text;
      }
      var indentToCode = matchIndentToCode[1];
      return text.split("\n").map(function(node) {
        var matchIndentInNode = node.match(/^\s+/);
        if (matchIndentInNode === null) {
          return node;
        }
        var indentInNode = matchIndentInNode[0];
        if (indentInNode.length >= indentToCode.length) {
          return node.slice(indentToCode.length);
        }
        return node;
      }).join("\n");
    }
    var Tokenizer = /* @__PURE__ */ function() {
      function Tokenizer2(options2) {
        this.options = options2 || exports.defaults;
      }
      var _proto = Tokenizer2.prototype;
      _proto.space = function space(src) {
        var cap = this.rules.block.newline.exec(src);
        if (cap && cap[0].length > 0) {
          return {
            type: "space",
            raw: cap[0]
          };
        }
      };
      _proto.code = function code(src) {
        var cap = this.rules.block.code.exec(src);
        if (cap) {
          var text = cap[0].replace(/^ {1,4}/gm, "");
          return {
            type: "code",
            raw: cap[0],
            codeBlockStyle: "indented",
            text: !this.options.pedantic ? rtrim(text, "\n") : text
          };
        }
      };
      _proto.fences = function fences(src) {
        var cap = this.rules.block.fences.exec(src);
        if (cap) {
          var raw = cap[0];
          var text = indentCodeCompensation(raw, cap[3] || "");
          return {
            type: "code",
            raw,
            lang: cap[2] ? cap[2].trim().replace(this.rules.inline._escapes, "$1") : cap[2],
            text
          };
        }
      };
      _proto.heading = function heading(src) {
        var cap = this.rules.block.heading.exec(src);
        if (cap) {
          var text = cap[2].trim();
          if (/#$/.test(text)) {
            var trimmed = rtrim(text, "#");
            if (this.options.pedantic) {
              text = trimmed.trim();
            } else if (!trimmed || / $/.test(trimmed)) {
              text = trimmed.trim();
            }
          }
          return {
            type: "heading",
            raw: cap[0],
            depth: cap[1].length,
            text,
            tokens: this.lexer.inline(text)
          };
        }
      };
      _proto.hr = function hr(src) {
        var cap = this.rules.block.hr.exec(src);
        if (cap) {
          return {
            type: "hr",
            raw: cap[0]
          };
        }
      };
      _proto.blockquote = function blockquote(src) {
        var cap = this.rules.block.blockquote.exec(src);
        if (cap) {
          var text = cap[0].replace(/^ *>[ \t]?/gm, "");
          var top = this.lexer.state.top;
          this.lexer.state.top = true;
          var tokens = this.lexer.blockTokens(text);
          this.lexer.state.top = top;
          return {
            type: "blockquote",
            raw: cap[0],
            tokens,
            text
          };
        }
      };
      _proto.list = function list(src) {
        var cap = this.rules.block.list.exec(src);
        if (cap) {
          var raw, istask, ischecked, indent3, i, blankLine, endsWithBlankLine, line, nextLine, rawLine, itemContents, endEarly;
          var bull = cap[1].trim();
          var isordered = bull.length > 1;
          var list2 = {
            type: "list",
            raw: "",
            ordered: isordered,
            start: isordered ? +bull.slice(0, -1) : "",
            loose: false,
            items: []
          };
          bull = isordered ? "\\d{1,9}\\" + bull.slice(-1) : "\\" + bull;
          if (this.options.pedantic) {
            bull = isordered ? bull : "[*+-]";
          }
          var itemRegex = new RegExp("^( {0,3}" + bull + ")((?:[	 ][^\\n]*)?(?:\\n|$))");
          while (src) {
            endEarly = false;
            if (!(cap = itemRegex.exec(src))) {
              break;
            }
            if (this.rules.block.hr.test(src)) {
              break;
            }
            raw = cap[0];
            src = src.substring(raw.length);
            line = cap[2].split("\n", 1)[0].replace(/^\t+/, function(t2) {
              return " ".repeat(3 * t2.length);
            });
            nextLine = src.split("\n", 1)[0];
            if (this.options.pedantic) {
              indent3 = 2;
              itemContents = line.trimLeft();
            } else {
              indent3 = cap[2].search(/[^ ]/);
              indent3 = indent3 > 4 ? 1 : indent3;
              itemContents = line.slice(indent3);
              indent3 += cap[1].length;
            }
            blankLine = false;
            if (!line && /^ *$/.test(nextLine)) {
              raw += nextLine + "\n";
              src = src.substring(nextLine.length + 1);
              endEarly = true;
            }
            if (!endEarly) {
              var nextBulletRegex = new RegExp("^ {0," + Math.min(3, indent3 - 1) + "}(?:[*+-]|\\d{1,9}[.)])((?:[ 	][^\\n]*)?(?:\\n|$))");
              var hrRegex = new RegExp("^ {0," + Math.min(3, indent3 - 1) + "}((?:- *){3,}|(?:_ *){3,}|(?:\\* *){3,})(?:\\n+|$)");
              var fencesBeginRegex = new RegExp("^ {0," + Math.min(3, indent3 - 1) + "}(?:```|~~~)");
              var headingBeginRegex = new RegExp("^ {0," + Math.min(3, indent3 - 1) + "}#");
              while (src) {
                rawLine = src.split("\n", 1)[0];
                nextLine = rawLine;
                if (this.options.pedantic) {
                  nextLine = nextLine.replace(/^ {1,4}(?=( {4})*[^ ])/g, "  ");
                }
                if (fencesBeginRegex.test(nextLine)) {
                  break;
                }
                if (headingBeginRegex.test(nextLine)) {
                  break;
                }
                if (nextBulletRegex.test(nextLine)) {
                  break;
                }
                if (hrRegex.test(src)) {
                  break;
                }
                if (nextLine.search(/[^ ]/) >= indent3 || !nextLine.trim()) {
                  itemContents += "\n" + nextLine.slice(indent3);
                } else {
                  if (blankLine) {
                    break;
                  }
                  if (line.search(/[^ ]/) >= 4) {
                    break;
                  }
                  if (fencesBeginRegex.test(line)) {
                    break;
                  }
                  if (headingBeginRegex.test(line)) {
                    break;
                  }
                  if (hrRegex.test(line)) {
                    break;
                  }
                  itemContents += "\n" + nextLine;
                }
                if (!blankLine && !nextLine.trim()) {
                  blankLine = true;
                }
                raw += rawLine + "\n";
                src = src.substring(rawLine.length + 1);
                line = nextLine.slice(indent3);
              }
            }
            if (!list2.loose) {
              if (endsWithBlankLine) {
                list2.loose = true;
              } else if (/\n *\n *$/.test(raw)) {
                endsWithBlankLine = true;
              }
            }
            if (this.options.gfm) {
              istask = /^\[[ xX]\] /.exec(itemContents);
              if (istask) {
                ischecked = istask[0] !== "[ ] ";
                itemContents = itemContents.replace(/^\[[ xX]\] +/, "");
              }
            }
            list2.items.push({
              type: "list_item",
              raw,
              task: !!istask,
              checked: ischecked,
              loose: false,
              text: itemContents
            });
            list2.raw += raw;
          }
          list2.items[list2.items.length - 1].raw = raw.trimRight();
          list2.items[list2.items.length - 1].text = itemContents.trimRight();
          list2.raw = list2.raw.trimRight();
          var l = list2.items.length;
          for (i = 0; i < l; i++) {
            this.lexer.state.top = false;
            list2.items[i].tokens = this.lexer.blockTokens(list2.items[i].text, []);
            if (!list2.loose) {
              var spacers = list2.items[i].tokens.filter(function(t2) {
                return t2.type === "space";
              });
              var hasMultipleLineBreaks = spacers.length > 0 && spacers.some(function(t2) {
                return /\n.*\n/.test(t2.raw);
              });
              list2.loose = hasMultipleLineBreaks;
            }
          }
          if (list2.loose) {
            for (i = 0; i < l; i++) {
              list2.items[i].loose = true;
            }
          }
          return list2;
        }
      };
      _proto.html = function html(src) {
        var cap = this.rules.block.html.exec(src);
        if (cap) {
          var token = {
            type: "html",
            raw: cap[0],
            pre: !this.options.sanitizer && (cap[1] === "pre" || cap[1] === "script" || cap[1] === "style"),
            text: cap[0]
          };
          if (this.options.sanitize) {
            var text = this.options.sanitizer ? this.options.sanitizer(cap[0]) : escape(cap[0]);
            token.type = "paragraph";
            token.text = text;
            token.tokens = this.lexer.inline(text);
          }
          return token;
        }
      };
      _proto.def = function def(src) {
        var cap = this.rules.block.def.exec(src);
        if (cap) {
          var tag = cap[1].toLowerCase().replace(/\s+/g, " ");
          var href = cap[2] ? cap[2].replace(/^<(.*)>$/, "$1").replace(this.rules.inline._escapes, "$1") : "";
          var title = cap[3] ? cap[3].substring(1, cap[3].length - 1).replace(this.rules.inline._escapes, "$1") : cap[3];
          return {
            type: "def",
            tag,
            raw: cap[0],
            href,
            title
          };
        }
      };
      _proto.table = function table(src) {
        var cap = this.rules.block.table.exec(src);
        if (cap) {
          var item = {
            type: "table",
            header: splitCells(cap[1]).map(function(c) {
              return {
                text: c
              };
            }),
            align: cap[2].replace(/^ *|\| *$/g, "").split(/ *\| */),
            rows: cap[3] && cap[3].trim() ? cap[3].replace(/\n[ \t]*$/, "").split("\n") : []
          };
          if (item.header.length === item.align.length) {
            item.raw = cap[0];
            var l = item.align.length;
            var i, j, k, row;
            for (i = 0; i < l; i++) {
              if (/^ *-+: *$/.test(item.align[i])) {
                item.align[i] = "right";
              } else if (/^ *:-+: *$/.test(item.align[i])) {
                item.align[i] = "center";
              } else if (/^ *:-+ *$/.test(item.align[i])) {
                item.align[i] = "left";
              } else {
                item.align[i] = null;
              }
            }
            l = item.rows.length;
            for (i = 0; i < l; i++) {
              item.rows[i] = splitCells(item.rows[i], item.header.length).map(function(c) {
                return {
                  text: c
                };
              });
            }
            l = item.header.length;
            for (j = 0; j < l; j++) {
              item.header[j].tokens = this.lexer.inline(item.header[j].text);
            }
            l = item.rows.length;
            for (j = 0; j < l; j++) {
              row = item.rows[j];
              for (k = 0; k < row.length; k++) {
                row[k].tokens = this.lexer.inline(row[k].text);
              }
            }
            return item;
          }
        }
      };
      _proto.lheading = function lheading(src) {
        var cap = this.rules.block.lheading.exec(src);
        if (cap) {
          return {
            type: "heading",
            raw: cap[0],
            depth: cap[2].charAt(0) === "=" ? 1 : 2,
            text: cap[1],
            tokens: this.lexer.inline(cap[1])
          };
        }
      };
      _proto.paragraph = function paragraph(src) {
        var cap = this.rules.block.paragraph.exec(src);
        if (cap) {
          var text = cap[1].charAt(cap[1].length - 1) === "\n" ? cap[1].slice(0, -1) : cap[1];
          return {
            type: "paragraph",
            raw: cap[0],
            text,
            tokens: this.lexer.inline(text)
          };
        }
      };
      _proto.text = function text(src) {
        var cap = this.rules.block.text.exec(src);
        if (cap) {
          return {
            type: "text",
            raw: cap[0],
            text: cap[0],
            tokens: this.lexer.inline(cap[0])
          };
        }
      };
      _proto.escape = function escape$1(src) {
        var cap = this.rules.inline.escape.exec(src);
        if (cap) {
          return {
            type: "escape",
            raw: cap[0],
            text: escape(cap[1])
          };
        }
      };
      _proto.tag = function tag(src) {
        var cap = this.rules.inline.tag.exec(src);
        if (cap) {
          if (!this.lexer.state.inLink && /^<a /i.test(cap[0])) {
            this.lexer.state.inLink = true;
          } else if (this.lexer.state.inLink && /^<\/a>/i.test(cap[0])) {
            this.lexer.state.inLink = false;
          }
          if (!this.lexer.state.inRawBlock && /^<(pre|code|kbd|script)(\s|>)/i.test(cap[0])) {
            this.lexer.state.inRawBlock = true;
          } else if (this.lexer.state.inRawBlock && /^<\/(pre|code|kbd|script)(\s|>)/i.test(cap[0])) {
            this.lexer.state.inRawBlock = false;
          }
          return {
            type: this.options.sanitize ? "text" : "html",
            raw: cap[0],
            inLink: this.lexer.state.inLink,
            inRawBlock: this.lexer.state.inRawBlock,
            text: this.options.sanitize ? this.options.sanitizer ? this.options.sanitizer(cap[0]) : escape(cap[0]) : cap[0]
          };
        }
      };
      _proto.link = function link(src) {
        var cap = this.rules.inline.link.exec(src);
        if (cap) {
          var trimmedUrl = cap[2].trim();
          if (!this.options.pedantic && /^</.test(trimmedUrl)) {
            if (!/>$/.test(trimmedUrl)) {
              return;
            }
            var rtrimSlash = rtrim(trimmedUrl.slice(0, -1), "\\");
            if ((trimmedUrl.length - rtrimSlash.length) % 2 === 0) {
              return;
            }
          } else {
            var lastParenIndex = findClosingBracket(cap[2], "()");
            if (lastParenIndex > -1) {
              var start = cap[0].indexOf("!") === 0 ? 5 : 4;
              var linkLen = start + cap[1].length + lastParenIndex;
              cap[2] = cap[2].substring(0, lastParenIndex);
              cap[0] = cap[0].substring(0, linkLen).trim();
              cap[3] = "";
            }
          }
          var href = cap[2];
          var title = "";
          if (this.options.pedantic) {
            var link2 = /^([^'"]*[^\s])\s+(['"])(.*)\2/.exec(href);
            if (link2) {
              href = link2[1];
              title = link2[3];
            }
          } else {
            title = cap[3] ? cap[3].slice(1, -1) : "";
          }
          href = href.trim();
          if (/^</.test(href)) {
            if (this.options.pedantic && !/>$/.test(trimmedUrl)) {
              href = href.slice(1);
            } else {
              href = href.slice(1, -1);
            }
          }
          return outputLink(cap, {
            href: href ? href.replace(this.rules.inline._escapes, "$1") : href,
            title: title ? title.replace(this.rules.inline._escapes, "$1") : title
          }, cap[0], this.lexer);
        }
      };
      _proto.reflink = function reflink(src, links) {
        var cap;
        if ((cap = this.rules.inline.reflink.exec(src)) || (cap = this.rules.inline.nolink.exec(src))) {
          var link = (cap[2] || cap[1]).replace(/\s+/g, " ");
          link = links[link.toLowerCase()];
          if (!link) {
            var text = cap[0].charAt(0);
            return {
              type: "text",
              raw: text,
              text
            };
          }
          return outputLink(cap, link, cap[0], this.lexer);
        }
      };
      _proto.emStrong = function emStrong(src, maskedSrc, prevChar) {
        if (prevChar === void 0) {
          prevChar = "";
        }
        var match = this.rules.inline.emStrong.lDelim.exec(src);
        if (!match)
          return;
        if (match[3] && prevChar.match(/(?:[0-9A-Za-z\xAA\xB2\xB3\xB5\xB9\xBA\xBC-\xBE\xC0-\xD6\xD8-\xF6\xF8-\u02C1\u02C6-\u02D1\u02E0-\u02E4\u02EC\u02EE\u0370-\u0374\u0376\u0377\u037A-\u037D\u037F\u0386\u0388-\u038A\u038C\u038E-\u03A1\u03A3-\u03F5\u03F7-\u0481\u048A-\u052F\u0531-\u0556\u0559\u0560-\u0588\u05D0-\u05EA\u05EF-\u05F2\u0620-\u064A\u0660-\u0669\u066E\u066F\u0671-\u06D3\u06D5\u06E5\u06E6\u06EE-\u06FC\u06FF\u0710\u0712-\u072F\u074D-\u07A5\u07B1\u07C0-\u07EA\u07F4\u07F5\u07FA\u0800-\u0815\u081A\u0824\u0828\u0840-\u0858\u0860-\u086A\u0870-\u0887\u0889-\u088E\u08A0-\u08C9\u0904-\u0939\u093D\u0950\u0958-\u0961\u0966-\u096F\u0971-\u0980\u0985-\u098C\u098F\u0990\u0993-\u09A8\u09AA-\u09B0\u09B2\u09B6-\u09B9\u09BD\u09CE\u09DC\u09DD\u09DF-\u09E1\u09E6-\u09F1\u09F4-\u09F9\u09FC\u0A05-\u0A0A\u0A0F\u0A10\u0A13-\u0A28\u0A2A-\u0A30\u0A32\u0A33\u0A35\u0A36\u0A38\u0A39\u0A59-\u0A5C\u0A5E\u0A66-\u0A6F\u0A72-\u0A74\u0A85-\u0A8D\u0A8F-\u0A91\u0A93-\u0AA8\u0AAA-\u0AB0\u0AB2\u0AB3\u0AB5-\u0AB9\u0ABD\u0AD0\u0AE0\u0AE1\u0AE6-\u0AEF\u0AF9\u0B05-\u0B0C\u0B0F\u0B10\u0B13-\u0B28\u0B2A-\u0B30\u0B32\u0B33\u0B35-\u0B39\u0B3D\u0B5C\u0B5D\u0B5F-\u0B61\u0B66-\u0B6F\u0B71-\u0B77\u0B83\u0B85-\u0B8A\u0B8E-\u0B90\u0B92-\u0B95\u0B99\u0B9A\u0B9C\u0B9E\u0B9F\u0BA3\u0BA4\u0BA8-\u0BAA\u0BAE-\u0BB9\u0BD0\u0BE6-\u0BF2\u0C05-\u0C0C\u0C0E-\u0C10\u0C12-\u0C28\u0C2A-\u0C39\u0C3D\u0C58-\u0C5A\u0C5D\u0C60\u0C61\u0C66-\u0C6F\u0C78-\u0C7E\u0C80\u0C85-\u0C8C\u0C8E-\u0C90\u0C92-\u0CA8\u0CAA-\u0CB3\u0CB5-\u0CB9\u0CBD\u0CDD\u0CDE\u0CE0\u0CE1\u0CE6-\u0CEF\u0CF1\u0CF2\u0D04-\u0D0C\u0D0E-\u0D10\u0D12-\u0D3A\u0D3D\u0D4E\u0D54-\u0D56\u0D58-\u0D61\u0D66-\u0D78\u0D7A-\u0D7F\u0D85-\u0D96\u0D9A-\u0DB1\u0DB3-\u0DBB\u0DBD\u0DC0-\u0DC6\u0DE6-\u0DEF\u0E01-\u0E30\u0E32\u0E33\u0E40-\u0E46\u0E50-\u0E59\u0E81\u0E82\u0E84\u0E86-\u0E8A\u0E8C-\u0EA3\u0EA5\u0EA7-\u0EB0\u0EB2\u0EB3\u0EBD\u0EC0-\u0EC4\u0EC6\u0ED0-\u0ED9\u0EDC-\u0EDF\u0F00\u0F20-\u0F33\u0F40-\u0F47\u0F49-\u0F6C\u0F88-\u0F8C\u1000-\u102A\u103F-\u1049\u1050-\u1055\u105A-\u105D\u1061\u1065\u1066\u106E-\u1070\u1075-\u1081\u108E\u1090-\u1099\u10A0-\u10C5\u10C7\u10CD\u10D0-\u10FA\u10FC-\u1248\u124A-\u124D\u1250-\u1256\u1258\u125A-\u125D\u1260-\u1288\u128A-\u128D\u1290-\u12B0\u12B2-\u12B5\u12B8-\u12BE\u12C0\u12C2-\u12C5\u12C8-\u12D6\u12D8-\u1310\u1312-\u1315\u1318-\u135A\u1369-\u137C\u1380-\u138F\u13A0-\u13F5\u13F8-\u13FD\u1401-\u166C\u166F-\u167F\u1681-\u169A\u16A0-\u16EA\u16EE-\u16F8\u1700-\u1711\u171F-\u1731\u1740-\u1751\u1760-\u176C\u176E-\u1770\u1780-\u17B3\u17D7\u17DC\u17E0-\u17E9\u17F0-\u17F9\u1810-\u1819\u1820-\u1878\u1880-\u1884\u1887-\u18A8\u18AA\u18B0-\u18F5\u1900-\u191E\u1946-\u196D\u1970-\u1974\u1980-\u19AB\u19B0-\u19C9\u19D0-\u19DA\u1A00-\u1A16\u1A20-\u1A54\u1A80-\u1A89\u1A90-\u1A99\u1AA7\u1B05-\u1B33\u1B45-\u1B4C\u1B50-\u1B59\u1B83-\u1BA0\u1BAE-\u1BE5\u1C00-\u1C23\u1C40-\u1C49\u1C4D-\u1C7D\u1C80-\u1C88\u1C90-\u1CBA\u1CBD-\u1CBF\u1CE9-\u1CEC\u1CEE-\u1CF3\u1CF5\u1CF6\u1CFA\u1D00-\u1DBF\u1E00-\u1F15\u1F18-\u1F1D\u1F20-\u1F45\u1F48-\u1F4D\u1F50-\u1F57\u1F59\u1F5B\u1F5D\u1F5F-\u1F7D\u1F80-\u1FB4\u1FB6-\u1FBC\u1FBE\u1FC2-\u1FC4\u1FC6-\u1FCC\u1FD0-\u1FD3\u1FD6-\u1FDB\u1FE0-\u1FEC\u1FF2-\u1FF4\u1FF6-\u1FFC\u2070\u2071\u2074-\u2079\u207F-\u2089\u2090-\u209C\u2102\u2107\u210A-\u2113\u2115\u2119-\u211D\u2124\u2126\u2128\u212A-\u212D\u212F-\u2139\u213C-\u213F\u2145-\u2149\u214E\u2150-\u2189\u2460-\u249B\u24EA-\u24FF\u2776-\u2793\u2C00-\u2CE4\u2CEB-\u2CEE\u2CF2\u2CF3\u2CFD\u2D00-\u2D25\u2D27\u2D2D\u2D30-\u2D67\u2D6F\u2D80-\u2D96\u2DA0-\u2DA6\u2DA8-\u2DAE\u2DB0-\u2DB6\u2DB8-\u2DBE\u2DC0-\u2DC6\u2DC8-\u2DCE\u2DD0-\u2DD6\u2DD8-\u2DDE\u2E2F\u3005-\u3007\u3021-\u3029\u3031-\u3035\u3038-\u303C\u3041-\u3096\u309D-\u309F\u30A1-\u30FA\u30FC-\u30FF\u3105-\u312F\u3131-\u318E\u3192-\u3195\u31A0-\u31BF\u31F0-\u31FF\u3220-\u3229\u3248-\u324F\u3251-\u325F\u3280-\u3289\u32B1-\u32BF\u3400-\u4DBF\u4E00-\uA48C\uA4D0-\uA4FD\uA500-\uA60C\uA610-\uA62B\uA640-\uA66E\uA67F-\uA69D\uA6A0-\uA6EF\uA717-\uA71F\uA722-\uA788\uA78B-\uA7CA\uA7D0\uA7D1\uA7D3\uA7D5-\uA7D9\uA7F2-\uA801\uA803-\uA805\uA807-\uA80A\uA80C-\uA822\uA830-\uA835\uA840-\uA873\uA882-\uA8B3\uA8D0-\uA8D9\uA8F2-\uA8F7\uA8FB\uA8FD\uA8FE\uA900-\uA925\uA930-\uA946\uA960-\uA97C\uA984-\uA9B2\uA9CF-\uA9D9\uA9E0-\uA9E4\uA9E6-\uA9FE\uAA00-\uAA28\uAA40-\uAA42\uAA44-\uAA4B\uAA50-\uAA59\uAA60-\uAA76\uAA7A\uAA7E-\uAAAF\uAAB1\uAAB5\uAAB6\uAAB9-\uAABD\uAAC0\uAAC2\uAADB-\uAADD\uAAE0-\uAAEA\uAAF2-\uAAF4\uAB01-\uAB06\uAB09-\uAB0E\uAB11-\uAB16\uAB20-\uAB26\uAB28-\uAB2E\uAB30-\uAB5A\uAB5C-\uAB69\uAB70-\uABE2\uABF0-\uABF9\uAC00-\uD7A3\uD7B0-\uD7C6\uD7CB-\uD7FB\uF900-\uFA6D\uFA70-\uFAD9\uFB00-\uFB06\uFB13-\uFB17\uFB1D\uFB1F-\uFB28\uFB2A-\uFB36\uFB38-\uFB3C\uFB3E\uFB40\uFB41\uFB43\uFB44\uFB46-\uFBB1\uFBD3-\uFD3D\uFD50-\uFD8F\uFD92-\uFDC7\uFDF0-\uFDFB\uFE70-\uFE74\uFE76-\uFEFC\uFF10-\uFF19\uFF21-\uFF3A\uFF41-\uFF5A\uFF66-\uFFBE\uFFC2-\uFFC7\uFFCA-\uFFCF\uFFD2-\uFFD7\uFFDA-\uFFDC]|\uD800[\uDC00-\uDC0B\uDC0D-\uDC26\uDC28-\uDC3A\uDC3C\uDC3D\uDC3F-\uDC4D\uDC50-\uDC5D\uDC80-\uDCFA\uDD07-\uDD33\uDD40-\uDD78\uDD8A\uDD8B\uDE80-\uDE9C\uDEA0-\uDED0\uDEE1-\uDEFB\uDF00-\uDF23\uDF2D-\uDF4A\uDF50-\uDF75\uDF80-\uDF9D\uDFA0-\uDFC3\uDFC8-\uDFCF\uDFD1-\uDFD5]|\uD801[\uDC00-\uDC9D\uDCA0-\uDCA9\uDCB0-\uDCD3\uDCD8-\uDCFB\uDD00-\uDD27\uDD30-\uDD63\uDD70-\uDD7A\uDD7C-\uDD8A\uDD8C-\uDD92\uDD94\uDD95\uDD97-\uDDA1\uDDA3-\uDDB1\uDDB3-\uDDB9\uDDBB\uDDBC\uDE00-\uDF36\uDF40-\uDF55\uDF60-\uDF67\uDF80-\uDF85\uDF87-\uDFB0\uDFB2-\uDFBA]|\uD802[\uDC00-\uDC05\uDC08\uDC0A-\uDC35\uDC37\uDC38\uDC3C\uDC3F-\uDC55\uDC58-\uDC76\uDC79-\uDC9E\uDCA7-\uDCAF\uDCE0-\uDCF2\uDCF4\uDCF5\uDCFB-\uDD1B\uDD20-\uDD39\uDD80-\uDDB7\uDDBC-\uDDCF\uDDD2-\uDE00\uDE10-\uDE13\uDE15-\uDE17\uDE19-\uDE35\uDE40-\uDE48\uDE60-\uDE7E\uDE80-\uDE9F\uDEC0-\uDEC7\uDEC9-\uDEE4\uDEEB-\uDEEF\uDF00-\uDF35\uDF40-\uDF55\uDF58-\uDF72\uDF78-\uDF91\uDFA9-\uDFAF]|\uD803[\uDC00-\uDC48\uDC80-\uDCB2\uDCC0-\uDCF2\uDCFA-\uDD23\uDD30-\uDD39\uDE60-\uDE7E\uDE80-\uDEA9\uDEB0\uDEB1\uDF00-\uDF27\uDF30-\uDF45\uDF51-\uDF54\uDF70-\uDF81\uDFB0-\uDFCB\uDFE0-\uDFF6]|\uD804[\uDC03-\uDC37\uDC52-\uDC6F\uDC71\uDC72\uDC75\uDC83-\uDCAF\uDCD0-\uDCE8\uDCF0-\uDCF9\uDD03-\uDD26\uDD36-\uDD3F\uDD44\uDD47\uDD50-\uDD72\uDD76\uDD83-\uDDB2\uDDC1-\uDDC4\uDDD0-\uDDDA\uDDDC\uDDE1-\uDDF4\uDE00-\uDE11\uDE13-\uDE2B\uDE80-\uDE86\uDE88\uDE8A-\uDE8D\uDE8F-\uDE9D\uDE9F-\uDEA8\uDEB0-\uDEDE\uDEF0-\uDEF9\uDF05-\uDF0C\uDF0F\uDF10\uDF13-\uDF28\uDF2A-\uDF30\uDF32\uDF33\uDF35-\uDF39\uDF3D\uDF50\uDF5D-\uDF61]|\uD805[\uDC00-\uDC34\uDC47-\uDC4A\uDC50-\uDC59\uDC5F-\uDC61\uDC80-\uDCAF\uDCC4\uDCC5\uDCC7\uDCD0-\uDCD9\uDD80-\uDDAE\uDDD8-\uDDDB\uDE00-\uDE2F\uDE44\uDE50-\uDE59\uDE80-\uDEAA\uDEB8\uDEC0-\uDEC9\uDF00-\uDF1A\uDF30-\uDF3B\uDF40-\uDF46]|\uD806[\uDC00-\uDC2B\uDCA0-\uDCF2\uDCFF-\uDD06\uDD09\uDD0C-\uDD13\uDD15\uDD16\uDD18-\uDD2F\uDD3F\uDD41\uDD50-\uDD59\uDDA0-\uDDA7\uDDAA-\uDDD0\uDDE1\uDDE3\uDE00\uDE0B-\uDE32\uDE3A\uDE50\uDE5C-\uDE89\uDE9D\uDEB0-\uDEF8]|\uD807[\uDC00-\uDC08\uDC0A-\uDC2E\uDC40\uDC50-\uDC6C\uDC72-\uDC8F\uDD00-\uDD06\uDD08\uDD09\uDD0B-\uDD30\uDD46\uDD50-\uDD59\uDD60-\uDD65\uDD67\uDD68\uDD6A-\uDD89\uDD98\uDDA0-\uDDA9\uDEE0-\uDEF2\uDFB0\uDFC0-\uDFD4]|\uD808[\uDC00-\uDF99]|\uD809[\uDC00-\uDC6E\uDC80-\uDD43]|\uD80B[\uDF90-\uDFF0]|[\uD80C\uD81C-\uD820\uD822\uD840-\uD868\uD86A-\uD86C\uD86F-\uD872\uD874-\uD879\uD880-\uD883][\uDC00-\uDFFF]|\uD80D[\uDC00-\uDC2E]|\uD811[\uDC00-\uDE46]|\uD81A[\uDC00-\uDE38\uDE40-\uDE5E\uDE60-\uDE69\uDE70-\uDEBE\uDEC0-\uDEC9\uDED0-\uDEED\uDF00-\uDF2F\uDF40-\uDF43\uDF50-\uDF59\uDF5B-\uDF61\uDF63-\uDF77\uDF7D-\uDF8F]|\uD81B[\uDE40-\uDE96\uDF00-\uDF4A\uDF50\uDF93-\uDF9F\uDFE0\uDFE1\uDFE3]|\uD821[\uDC00-\uDFF7]|\uD823[\uDC00-\uDCD5\uDD00-\uDD08]|\uD82B[\uDFF0-\uDFF3\uDFF5-\uDFFB\uDFFD\uDFFE]|\uD82C[\uDC00-\uDD22\uDD50-\uDD52\uDD64-\uDD67\uDD70-\uDEFB]|\uD82F[\uDC00-\uDC6A\uDC70-\uDC7C\uDC80-\uDC88\uDC90-\uDC99]|\uD834[\uDEE0-\uDEF3\uDF60-\uDF78]|\uD835[\uDC00-\uDC54\uDC56-\uDC9C\uDC9E\uDC9F\uDCA2\uDCA5\uDCA6\uDCA9-\uDCAC\uDCAE-\uDCB9\uDCBB\uDCBD-\uDCC3\uDCC5-\uDD05\uDD07-\uDD0A\uDD0D-\uDD14\uDD16-\uDD1C\uDD1E-\uDD39\uDD3B-\uDD3E\uDD40-\uDD44\uDD46\uDD4A-\uDD50\uDD52-\uDEA5\uDEA8-\uDEC0\uDEC2-\uDEDA\uDEDC-\uDEFA\uDEFC-\uDF14\uDF16-\uDF34\uDF36-\uDF4E\uDF50-\uDF6E\uDF70-\uDF88\uDF8A-\uDFA8\uDFAA-\uDFC2\uDFC4-\uDFCB\uDFCE-\uDFFF]|\uD837[\uDF00-\uDF1E]|\uD838[\uDD00-\uDD2C\uDD37-\uDD3D\uDD40-\uDD49\uDD4E\uDE90-\uDEAD\uDEC0-\uDEEB\uDEF0-\uDEF9]|\uD839[\uDFE0-\uDFE6\uDFE8-\uDFEB\uDFED\uDFEE\uDFF0-\uDFFE]|\uD83A[\uDC00-\uDCC4\uDCC7-\uDCCF\uDD00-\uDD43\uDD4B\uDD50-\uDD59]|\uD83B[\uDC71-\uDCAB\uDCAD-\uDCAF\uDCB1-\uDCB4\uDD01-\uDD2D\uDD2F-\uDD3D\uDE00-\uDE03\uDE05-\uDE1F\uDE21\uDE22\uDE24\uDE27\uDE29-\uDE32\uDE34-\uDE37\uDE39\uDE3B\uDE42\uDE47\uDE49\uDE4B\uDE4D-\uDE4F\uDE51\uDE52\uDE54\uDE57\uDE59\uDE5B\uDE5D\uDE5F\uDE61\uDE62\uDE64\uDE67-\uDE6A\uDE6C-\uDE72\uDE74-\uDE77\uDE79-\uDE7C\uDE7E\uDE80-\uDE89\uDE8B-\uDE9B\uDEA1-\uDEA3\uDEA5-\uDEA9\uDEAB-\uDEBB]|\uD83C[\uDD00-\uDD0C]|\uD83E[\uDFF0-\uDFF9]|\uD869[\uDC00-\uDEDF\uDF00-\uDFFF]|\uD86D[\uDC00-\uDF38\uDF40-\uDFFF]|\uD86E[\uDC00-\uDC1D\uDC20-\uDFFF]|\uD873[\uDC00-\uDEA1\uDEB0-\uDFFF]|\uD87A[\uDC00-\uDFE0]|\uD87E[\uDC00-\uDE1D]|\uD884[\uDC00-\uDF4A])/))
          return;
        var nextChar = match[1] || match[2] || "";
        if (!nextChar || nextChar && (prevChar === "" || this.rules.inline.punctuation.exec(prevChar))) {
          var lLength = match[0].length - 1;
          var rDelim, rLength, delimTotal = lLength, midDelimTotal = 0;
          var endReg = match[0][0] === "*" ? this.rules.inline.emStrong.rDelimAst : this.rules.inline.emStrong.rDelimUnd;
          endReg.lastIndex = 0;
          maskedSrc = maskedSrc.slice(-1 * src.length + lLength);
          while ((match = endReg.exec(maskedSrc)) != null) {
            rDelim = match[1] || match[2] || match[3] || match[4] || match[5] || match[6];
            if (!rDelim)
              continue;
            rLength = rDelim.length;
            if (match[3] || match[4]) {
              delimTotal += rLength;
              continue;
            } else if (match[5] || match[6]) {
              if (lLength % 3 && !((lLength + rLength) % 3)) {
                midDelimTotal += rLength;
                continue;
              }
            }
            delimTotal -= rLength;
            if (delimTotal > 0)
              continue;
            rLength = Math.min(rLength, rLength + delimTotal + midDelimTotal);
            var raw = src.slice(0, lLength + match.index + (match[0].length - rDelim.length) + rLength);
            if (Math.min(lLength, rLength) % 2) {
              var _text = raw.slice(1, -1);
              return {
                type: "em",
                raw,
                text: _text,
                tokens: this.lexer.inlineTokens(_text)
              };
            }
            var text = raw.slice(2, -2);
            return {
              type: "strong",
              raw,
              text,
              tokens: this.lexer.inlineTokens(text)
            };
          }
        }
      };
      _proto.codespan = function codespan(src) {
        var cap = this.rules.inline.code.exec(src);
        if (cap) {
          var text = cap[2].replace(/\n/g, " ");
          var hasNonSpaceChars = /[^ ]/.test(text);
          var hasSpaceCharsOnBothEnds = /^ /.test(text) && / $/.test(text);
          if (hasNonSpaceChars && hasSpaceCharsOnBothEnds) {
            text = text.substring(1, text.length - 1);
          }
          text = escape(text, true);
          return {
            type: "codespan",
            raw: cap[0],
            text
          };
        }
      };
      _proto.br = function br(src) {
        var cap = this.rules.inline.br.exec(src);
        if (cap) {
          return {
            type: "br",
            raw: cap[0]
          };
        }
      };
      _proto.del = function del(src) {
        var cap = this.rules.inline.del.exec(src);
        if (cap) {
          return {
            type: "del",
            raw: cap[0],
            text: cap[2],
            tokens: this.lexer.inlineTokens(cap[2])
          };
        }
      };
      _proto.autolink = function autolink(src, mangle2) {
        var cap = this.rules.inline.autolink.exec(src);
        if (cap) {
          var text, href;
          if (cap[2] === "@") {
            text = escape(this.options.mangle ? mangle2(cap[1]) : cap[1]);
            href = "mailto:" + text;
          } else {
            text = escape(cap[1]);
            href = text;
          }
          return {
            type: "link",
            raw: cap[0],
            text,
            href,
            tokens: [{
              type: "text",
              raw: text,
              text
            }]
          };
        }
      };
      _proto.url = function url(src, mangle2) {
        var cap;
        if (cap = this.rules.inline.url.exec(src)) {
          var text, href;
          if (cap[2] === "@") {
            text = escape(this.options.mangle ? mangle2(cap[0]) : cap[0]);
            href = "mailto:" + text;
          } else {
            var prevCapZero;
            do {
              prevCapZero = cap[0];
              cap[0] = this.rules.inline._backpedal.exec(cap[0])[0];
            } while (prevCapZero !== cap[0]);
            text = escape(cap[0]);
            if (cap[1] === "www.") {
              href = "http://" + cap[0];
            } else {
              href = cap[0];
            }
          }
          return {
            type: "link",
            raw: cap[0],
            text,
            href,
            tokens: [{
              type: "text",
              raw: text,
              text
            }]
          };
        }
      };
      _proto.inlineText = function inlineText(src, smartypants2) {
        var cap = this.rules.inline.text.exec(src);
        if (cap) {
          var text;
          if (this.lexer.state.inRawBlock) {
            text = this.options.sanitize ? this.options.sanitizer ? this.options.sanitizer(cap[0]) : escape(cap[0]) : cap[0];
          } else {
            text = escape(this.options.smartypants ? smartypants2(cap[0]) : cap[0]);
          }
          return {
            type: "text",
            raw: cap[0],
            text
          };
        }
      };
      return Tokenizer2;
    }();
    var block2 = {
      newline: /^(?: *(?:\n|$))+/,
      code: /^( {4}[^\n]+(?:\n(?: *(?:\n|$))*)?)+/,
      fences: /^ {0,3}(`{3,}(?=[^`\n]*(?:\n|$))|~{3,})([^\n]*)(?:\n|$)(?:|([\s\S]*?)(?:\n|$))(?: {0,3}\1[~`]* *(?=\n|$)|$)/,
      hr: /^ {0,3}((?:-[\t ]*){3,}|(?:_[ \t]*){3,}|(?:\*[ \t]*){3,})(?:\n+|$)/,
      heading: /^ {0,3}(#{1,6})(?=\s|$)(.*)(?:\n+|$)/,
      blockquote: /^( {0,3}> ?(paragraph|[^\n]*)(?:\n|$))+/,
      list: /^( {0,3}bull)([ \t][^\n]+?)?(?:\n|$)/,
      html: "^ {0,3}(?:<(script|pre|style|textarea)[\\s>][\\s\\S]*?(?:</\\1>[^\\n]*\\n+|$)|comment[^\\n]*(\\n+|$)|<\\?[\\s\\S]*?(?:\\?>\\n*|$)|<![A-Z][\\s\\S]*?(?:>\\n*|$)|<!\\[CDATA\\[[\\s\\S]*?(?:\\]\\]>\\n*|$)|</?(tag)(?: +|\\n|/?>)[\\s\\S]*?(?:(?:\\n *)+\\n|$)|<(?!script|pre|style|textarea)([a-z][\\w-]*)(?:attribute)*? */?>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n *)+\\n|$)|</(?!script|pre|style|textarea)[a-z][\\w-]*\\s*>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n *)+\\n|$))",
      def: /^ {0,3}\[(label)\]: *(?:\n *)?([^<\s][^\s]*|<.*?>)(?:(?: +(?:\n *)?| *\n *)(title))? *(?:\n+|$)/,
      table: noopTest,
      lheading: /^((?:.|\n(?!\n))+?)\n {0,3}(=+|-+) *(?:\n+|$)/,
      // regex template, placeholders will be replaced according to different paragraph
      // interruption rules of commonmark and the original markdown spec:
      _paragraph: /^([^\n]+(?:\n(?!hr|heading|lheading|blockquote|fences|list|html|table| +\n)[^\n]+)*)/,
      text: /^[^\n]+/
    };
    block2._label = /(?!\s*\])(?:\\.|[^\[\]\\])+/;
    block2._title = /(?:"(?:\\"?|[^"\\])*"|'[^'\n]*(?:\n[^'\n]+)*\n?'|\([^()]*\))/;
    block2.def = edit(block2.def).replace("label", block2._label).replace("title", block2._title).getRegex();
    block2.bullet = /(?:[*+-]|\d{1,9}[.)])/;
    block2.listItemStart = edit(/^( *)(bull) */).replace("bull", block2.bullet).getRegex();
    block2.list = edit(block2.list).replace(/bull/g, block2.bullet).replace("hr", "\\n+(?=\\1?(?:(?:- *){3,}|(?:_ *){3,}|(?:\\* *){3,})(?:\\n+|$))").replace("def", "\\n+(?=" + block2.def.source + ")").getRegex();
    block2._tag = "address|article|aside|base|basefont|blockquote|body|caption|center|col|colgroup|dd|details|dialog|dir|div|dl|dt|fieldset|figcaption|figure|footer|form|frame|frameset|h[1-6]|head|header|hr|html|iframe|legend|li|link|main|menu|menuitem|meta|nav|noframes|ol|optgroup|option|p|param|section|source|summary|table|tbody|td|tfoot|th|thead|title|tr|track|ul";
    block2._comment = /<!--(?!-?>)[\s\S]*?(?:-->|$)/;
    block2.html = edit(block2.html, "i").replace("comment", block2._comment).replace("tag", block2._tag).replace("attribute", / +[a-zA-Z:_][\w.:-]*(?: *= *"[^"\n]*"| *= *'[^'\n]*'| *= *[^\s"'=<>`]+)?/).getRegex();
    block2.paragraph = edit(block2._paragraph).replace("hr", block2.hr).replace("heading", " {0,3}#{1,6} ").replace("|lheading", "").replace("|table", "").replace("blockquote", " {0,3}>").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", block2._tag).getRegex();
    block2.blockquote = edit(block2.blockquote).replace("paragraph", block2.paragraph).getRegex();
    block2.normal = _extends({}, block2);
    block2.gfm = _extends({}, block2.normal, {
      table: "^ *([^\\n ].*\\|.*)\\n {0,3}(?:\\| *)?(:?-+:? *(?:\\| *:?-+:? *)*)(?:\\| *)?(?:\\n((?:(?! *\\n|hr|heading|blockquote|code|fences|list|html).*(?:\\n|$))*)\\n*|$)"
      // Cells
    });
    block2.gfm.table = edit(block2.gfm.table).replace("hr", block2.hr).replace("heading", " {0,3}#{1,6} ").replace("blockquote", " {0,3}>").replace("code", " {4}[^\\n]").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", block2._tag).getRegex();
    block2.gfm.paragraph = edit(block2._paragraph).replace("hr", block2.hr).replace("heading", " {0,3}#{1,6} ").replace("|lheading", "").replace("table", block2.gfm.table).replace("blockquote", " {0,3}>").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", block2._tag).getRegex();
    block2.pedantic = _extends({}, block2.normal, {
      html: edit(`^ *(?:comment *(?:\\n|\\s*$)|<(tag)[\\s\\S]+?</\\1> *(?:\\n{2,}|\\s*$)|<tag(?:"[^"]*"|'[^']*'|\\s[^'"/>\\s]*)*?/?> *(?:\\n{2,}|\\s*$))`).replace("comment", block2._comment).replace(/tag/g, "(?!(?:a|em|strong|small|s|cite|q|dfn|abbr|data|time|code|var|samp|kbd|sub|sup|i|b|u|mark|ruby|rt|rp|bdi|bdo|span|br|wbr|ins|del|img)\\b)\\w+(?!:|[^\\w\\s@]*@)\\b").getRegex(),
      def: /^ *\[([^\]]+)\]: *<?([^\s>]+)>?(?: +(["(][^\n]+[")]))? *(?:\n+|$)/,
      heading: /^(#{1,6})(.*)(?:\n+|$)/,
      fences: noopTest,
      // fences not supported
      lheading: /^(.+?)\n {0,3}(=+|-+) *(?:\n+|$)/,
      paragraph: edit(block2.normal._paragraph).replace("hr", block2.hr).replace("heading", " *#{1,6} *[^\n]").replace("lheading", block2.lheading).replace("blockquote", " {0,3}>").replace("|fences", "").replace("|list", "").replace("|html", "").getRegex()
    });
    var inline = {
      escape: /^\\([!"#$%&'()*+,\-./:;<=>?@\[\]\\^_`{|}~])/,
      autolink: /^<(scheme:[^\s\x00-\x1f<>]*|email)>/,
      url: noopTest,
      tag: "^comment|^</[a-zA-Z][\\w:-]*\\s*>|^<[a-zA-Z][\\w-]*(?:attribute)*?\\s*/?>|^<\\?[\\s\\S]*?\\?>|^<![a-zA-Z]+\\s[\\s\\S]*?>|^<!\\[CDATA\\[[\\s\\S]*?\\]\\]>",
      // CDATA section
      link: /^!?\[(label)\]\(\s*(href)(?:\s+(title))?\s*\)/,
      reflink: /^!?\[(label)\]\[(ref)\]/,
      nolink: /^!?\[(ref)\](?:\[\])?/,
      reflinkSearch: "reflink|nolink(?!\\()",
      emStrong: {
        lDelim: /^(?:\*+(?:([punct_])|[^\s*]))|^_+(?:([punct*])|([^\s_]))/,
        //        (1) and (2) can only be a Right Delimiter. (3) and (4) can only be Left.  (5) and (6) can be either Left or Right.
        //          () Skip orphan inside strong                                      () Consume to delim     (1) #***                (2) a***#, a***                             (3) #***a, ***a                 (4) ***#              (5) #***#                 (6) a***a
        rDelimAst: /^(?:[^_*\\]|\\.)*?\_\_(?:[^_*\\]|\\.)*?\*(?:[^_*\\]|\\.)*?(?=\_\_)|(?:[^*\\]|\\.)+(?=[^*])|[punct_](\*+)(?=[\s]|$)|(?:[^punct*_\s\\]|\\.)(\*+)(?=[punct_\s]|$)|[punct_\s](\*+)(?=[^punct*_\s])|[\s](\*+)(?=[punct_])|[punct_](\*+)(?=[punct_])|(?:[^punct*_\s\\]|\\.)(\*+)(?=[^punct*_\s])/,
        rDelimUnd: /^(?:[^_*\\]|\\.)*?\*\*(?:[^_*\\]|\\.)*?\_(?:[^_*\\]|\\.)*?(?=\*\*)|(?:[^_\\]|\\.)+(?=[^_])|[punct*](\_+)(?=[\s]|$)|(?:[^punct*_\s\\]|\\.)(\_+)(?=[punct*\s]|$)|[punct*\s](\_+)(?=[^punct*_\s])|[\s](\_+)(?=[punct*])|[punct*](\_+)(?=[punct*])/
        // ^- Not allowed for _
      },
      code: /^(`+)([^`]|[^`][\s\S]*?[^`])\1(?!`)/,
      br: /^( {2,}|\\)\n(?!\s*$)/,
      del: noopTest,
      text: /^(`+|[^`])(?:(?= {2,}\n)|[\s\S]*?(?:(?=[\\<!\[`*_]|\b_|$)|[^ ](?= {2,}\n)))/,
      punctuation: /^([\spunctuation])/
    };
    inline._punctuation = "!\"#$%&'()+\\-.,/:;<=>?@\\[\\]`^{|}~";
    inline.punctuation = edit(inline.punctuation).replace(/punctuation/g, inline._punctuation).getRegex();
    inline.blockSkip = /\[[^\]]*?\]\([^\)]*?\)|`[^`]*?`|<[^>]*?>/g;
    inline.escapedEmSt = /(?:^|[^\\])(?:\\\\)*\\[*_]/g;
    inline._comment = edit(block2._comment).replace("(?:-->|$)", "-->").getRegex();
    inline.emStrong.lDelim = edit(inline.emStrong.lDelim).replace(/punct/g, inline._punctuation).getRegex();
    inline.emStrong.rDelimAst = edit(inline.emStrong.rDelimAst, "g").replace(/punct/g, inline._punctuation).getRegex();
    inline.emStrong.rDelimUnd = edit(inline.emStrong.rDelimUnd, "g").replace(/punct/g, inline._punctuation).getRegex();
    inline._escapes = /\\([!"#$%&'()*+,\-./:;<=>?@\[\]\\^_`{|}~])/g;
    inline._scheme = /[a-zA-Z][a-zA-Z0-9+.-]{1,31}/;
    inline._email = /[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+(@)[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)+(?![-_])/;
    inline.autolink = edit(inline.autolink).replace("scheme", inline._scheme).replace("email", inline._email).getRegex();
    inline._attribute = /\s+[a-zA-Z:_][\w.:-]*(?:\s*=\s*"[^"]*"|\s*=\s*'[^']*'|\s*=\s*[^\s"'=<>`]+)?/;
    inline.tag = edit(inline.tag).replace("comment", inline._comment).replace("attribute", inline._attribute).getRegex();
    inline._label = /(?:\[(?:\\.|[^\[\]\\])*\]|\\.|`[^`]*`|[^\[\]\\`])*?/;
    inline._href = /<(?:\\.|[^\n<>\\])+>|[^\s\x00-\x1f]*/;
    inline._title = /"(?:\\"?|[^"\\])*"|'(?:\\'?|[^'\\])*'|\((?:\\\)?|[^)\\])*\)/;
    inline.link = edit(inline.link).replace("label", inline._label).replace("href", inline._href).replace("title", inline._title).getRegex();
    inline.reflink = edit(inline.reflink).replace("label", inline._label).replace("ref", block2._label).getRegex();
    inline.nolink = edit(inline.nolink).replace("ref", block2._label).getRegex();
    inline.reflinkSearch = edit(inline.reflinkSearch, "g").replace("reflink", inline.reflink).replace("nolink", inline.nolink).getRegex();
    inline.normal = _extends({}, inline);
    inline.pedantic = _extends({}, inline.normal, {
      strong: {
        start: /^__|\*\*/,
        middle: /^__(?=\S)([\s\S]*?\S)__(?!_)|^\*\*(?=\S)([\s\S]*?\S)\*\*(?!\*)/,
        endAst: /\*\*(?!\*)/g,
        endUnd: /__(?!_)/g
      },
      em: {
        start: /^_|\*/,
        middle: /^()\*(?=\S)([\s\S]*?\S)\*(?!\*)|^_(?=\S)([\s\S]*?\S)_(?!_)/,
        endAst: /\*(?!\*)/g,
        endUnd: /_(?!_)/g
      },
      link: edit(/^!?\[(label)\]\((.*?)\)/).replace("label", inline._label).getRegex(),
      reflink: edit(/^!?\[(label)\]\s*\[([^\]]*)\]/).replace("label", inline._label).getRegex()
    });
    inline.gfm = _extends({}, inline.normal, {
      escape: edit(inline.escape).replace("])", "~|])").getRegex(),
      _extended_email: /[A-Za-z0-9._+-]+(@)[a-zA-Z0-9-_]+(?:\.[a-zA-Z0-9-_]*[a-zA-Z0-9])+(?![-_])/,
      url: /^((?:ftp|https?):\/\/|www\.)(?:[a-zA-Z0-9\-]+\.?)+[^\s<]*|^email/,
      _backpedal: /(?:[^?!.,:;*_'"~()&]+|\([^)]*\)|&(?![a-zA-Z0-9]+;$)|[?!.,:;*_'"~)]+(?!$))+/,
      del: /^(~~?)(?=[^\s~])([\s\S]*?[^\s~])\1(?=[^~]|$)/,
      text: /^([`~]+|[^`~])(?:(?= {2,}\n)|(?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)|[\s\S]*?(?:(?=[\\<!\[`*~_]|\b_|https?:\/\/|ftp:\/\/|www\.|$)|[^ ](?= {2,}\n)|[^a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-](?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)))/
    });
    inline.gfm.url = edit(inline.gfm.url, "i").replace("email", inline.gfm._extended_email).getRegex();
    inline.breaks = _extends({}, inline.gfm, {
      br: edit(inline.br).replace("{2,}", "*").getRegex(),
      text: edit(inline.gfm.text).replace("\\b_", "\\b_| {2,}\\n").replace(/\{2,\}/g, "*").getRegex()
    });
    function smartypants(text) {
      return text.replace(/---/g, "\u2014").replace(/--/g, "\u2013").replace(/(^|[-\u2014/(\[{"\s])'/g, "$1\u2018").replace(/'/g, "\u2019").replace(/(^|[-\u2014/(\[{\u2018\s])"/g, "$1\u201C").replace(/"/g, "\u201D").replace(/\.{3}/g, "\u2026");
    }
    function mangle(text) {
      var out = "", i, ch;
      var l = text.length;
      for (i = 0; i < l; i++) {
        ch = text.charCodeAt(i);
        if (Math.random() > 0.5) {
          ch = "x" + ch.toString(16);
        }
        out += "&#" + ch + ";";
      }
      return out;
    }
    var Lexer2 = /* @__PURE__ */ function() {
      function Lexer3(options2) {
        this.tokens = [];
        this.tokens.links = /* @__PURE__ */ Object.create(null);
        this.options = options2 || exports.defaults;
        this.options.tokenizer = this.options.tokenizer || new Tokenizer();
        this.tokenizer = this.options.tokenizer;
        this.tokenizer.options = this.options;
        this.tokenizer.lexer = this;
        this.inlineQueue = [];
        this.state = {
          inLink: false,
          inRawBlock: false,
          top: true
        };
        var rules = {
          block: block2.normal,
          inline: inline.normal
        };
        if (this.options.pedantic) {
          rules.block = block2.pedantic;
          rules.inline = inline.pedantic;
        } else if (this.options.gfm) {
          rules.block = block2.gfm;
          if (this.options.breaks) {
            rules.inline = inline.breaks;
          } else {
            rules.inline = inline.gfm;
          }
        }
        this.tokenizer.rules = rules;
      }
      Lexer3.lex = function lex(src, options2) {
        var lexer2 = new Lexer3(options2);
        return lexer2.lex(src);
      };
      Lexer3.lexInline = function lexInline(src, options2) {
        var lexer2 = new Lexer3(options2);
        return lexer2.inlineTokens(src);
      };
      var _proto = Lexer3.prototype;
      _proto.lex = function lex(src) {
        src = src.replace(/\r\n|\r/g, "\n");
        this.blockTokens(src, this.tokens);
        var next;
        while (next = this.inlineQueue.shift()) {
          this.inlineTokens(next.src, next.tokens);
        }
        return this.tokens;
      };
      _proto.blockTokens = function blockTokens(src, tokens) {
        var _this = this;
        if (tokens === void 0) {
          tokens = [];
        }
        if (this.options.pedantic) {
          src = src.replace(/\t/g, "    ").replace(/^ +$/gm, "");
        } else {
          src = src.replace(/^( *)(\t+)/gm, function(_, leading, tabs) {
            return leading + "    ".repeat(tabs.length);
          });
        }
        var token, lastToken, cutSrc, lastParagraphClipped;
        while (src) {
          if (this.options.extensions && this.options.extensions.block && this.options.extensions.block.some(function(extTokenizer) {
            if (token = extTokenizer.call({
              lexer: _this
            }, src, tokens)) {
              src = src.substring(token.raw.length);
              tokens.push(token);
              return true;
            }
            return false;
          })) {
            continue;
          }
          if (token = this.tokenizer.space(src)) {
            src = src.substring(token.raw.length);
            if (token.raw.length === 1 && tokens.length > 0) {
              tokens[tokens.length - 1].raw += "\n";
            } else {
              tokens.push(token);
            }
            continue;
          }
          if (token = this.tokenizer.code(src)) {
            src = src.substring(token.raw.length);
            lastToken = tokens[tokens.length - 1];
            if (lastToken && (lastToken.type === "paragraph" || lastToken.type === "text")) {
              lastToken.raw += "\n" + token.raw;
              lastToken.text += "\n" + token.text;
              this.inlineQueue[this.inlineQueue.length - 1].src = lastToken.text;
            } else {
              tokens.push(token);
            }
            continue;
          }
          if (token = this.tokenizer.fences(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.heading(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.hr(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.blockquote(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.list(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.html(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.def(src)) {
            src = src.substring(token.raw.length);
            lastToken = tokens[tokens.length - 1];
            if (lastToken && (lastToken.type === "paragraph" || lastToken.type === "text")) {
              lastToken.raw += "\n" + token.raw;
              lastToken.text += "\n" + token.raw;
              this.inlineQueue[this.inlineQueue.length - 1].src = lastToken.text;
            } else if (!this.tokens.links[token.tag]) {
              this.tokens.links[token.tag] = {
                href: token.href,
                title: token.title
              };
            }
            continue;
          }
          if (token = this.tokenizer.table(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.lheading(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          cutSrc = src;
          if (this.options.extensions && this.options.extensions.startBlock) {
            (function() {
              var startIndex = Infinity;
              var tempSrc = src.slice(1);
              var tempStart = void 0;
              _this.options.extensions.startBlock.forEach(function(getStartIndex) {
                tempStart = getStartIndex.call({
                  lexer: this
                }, tempSrc);
                if (typeof tempStart === "number" && tempStart >= 0) {
                  startIndex = Math.min(startIndex, tempStart);
                }
              });
              if (startIndex < Infinity && startIndex >= 0) {
                cutSrc = src.substring(0, startIndex + 1);
              }
            })();
          }
          if (this.state.top && (token = this.tokenizer.paragraph(cutSrc))) {
            lastToken = tokens[tokens.length - 1];
            if (lastParagraphClipped && lastToken.type === "paragraph") {
              lastToken.raw += "\n" + token.raw;
              lastToken.text += "\n" + token.text;
              this.inlineQueue.pop();
              this.inlineQueue[this.inlineQueue.length - 1].src = lastToken.text;
            } else {
              tokens.push(token);
            }
            lastParagraphClipped = cutSrc.length !== src.length;
            src = src.substring(token.raw.length);
            continue;
          }
          if (token = this.tokenizer.text(src)) {
            src = src.substring(token.raw.length);
            lastToken = tokens[tokens.length - 1];
            if (lastToken && lastToken.type === "text") {
              lastToken.raw += "\n" + token.raw;
              lastToken.text += "\n" + token.text;
              this.inlineQueue.pop();
              this.inlineQueue[this.inlineQueue.length - 1].src = lastToken.text;
            } else {
              tokens.push(token);
            }
            continue;
          }
          if (src) {
            var errMsg = "Infinite loop on byte: " + src.charCodeAt(0);
            if (this.options.silent) {
              console.error(errMsg);
              break;
            } else {
              throw new Error(errMsg);
            }
          }
        }
        this.state.top = true;
        return tokens;
      };
      _proto.inline = function inline2(src, tokens) {
        if (tokens === void 0) {
          tokens = [];
        }
        this.inlineQueue.push({
          src,
          tokens
        });
        return tokens;
      };
      _proto.inlineTokens = function inlineTokens(src, tokens) {
        var _this2 = this;
        if (tokens === void 0) {
          tokens = [];
        }
        var token, lastToken, cutSrc;
        var maskedSrc = src;
        var match;
        var keepPrevChar, prevChar;
        if (this.tokens.links) {
          var links = Object.keys(this.tokens.links);
          if (links.length > 0) {
            while ((match = this.tokenizer.rules.inline.reflinkSearch.exec(maskedSrc)) != null) {
              if (links.includes(match[0].slice(match[0].lastIndexOf("[") + 1, -1))) {
                maskedSrc = maskedSrc.slice(0, match.index) + "[" + repeatString("a", match[0].length - 2) + "]" + maskedSrc.slice(this.tokenizer.rules.inline.reflinkSearch.lastIndex);
              }
            }
          }
        }
        while ((match = this.tokenizer.rules.inline.blockSkip.exec(maskedSrc)) != null) {
          maskedSrc = maskedSrc.slice(0, match.index) + "[" + repeatString("a", match[0].length - 2) + "]" + maskedSrc.slice(this.tokenizer.rules.inline.blockSkip.lastIndex);
        }
        while ((match = this.tokenizer.rules.inline.escapedEmSt.exec(maskedSrc)) != null) {
          maskedSrc = maskedSrc.slice(0, match.index + match[0].length - 2) + "++" + maskedSrc.slice(this.tokenizer.rules.inline.escapedEmSt.lastIndex);
          this.tokenizer.rules.inline.escapedEmSt.lastIndex--;
        }
        while (src) {
          if (!keepPrevChar) {
            prevChar = "";
          }
          keepPrevChar = false;
          if (this.options.extensions && this.options.extensions.inline && this.options.extensions.inline.some(function(extTokenizer) {
            if (token = extTokenizer.call({
              lexer: _this2
            }, src, tokens)) {
              src = src.substring(token.raw.length);
              tokens.push(token);
              return true;
            }
            return false;
          })) {
            continue;
          }
          if (token = this.tokenizer.escape(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.tag(src)) {
            src = src.substring(token.raw.length);
            lastToken = tokens[tokens.length - 1];
            if (lastToken && token.type === "text" && lastToken.type === "text") {
              lastToken.raw += token.raw;
              lastToken.text += token.text;
            } else {
              tokens.push(token);
            }
            continue;
          }
          if (token = this.tokenizer.link(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.reflink(src, this.tokens.links)) {
            src = src.substring(token.raw.length);
            lastToken = tokens[tokens.length - 1];
            if (lastToken && token.type === "text" && lastToken.type === "text") {
              lastToken.raw += token.raw;
              lastToken.text += token.text;
            } else {
              tokens.push(token);
            }
            continue;
          }
          if (token = this.tokenizer.emStrong(src, maskedSrc, prevChar)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.codespan(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.br(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.del(src)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (token = this.tokenizer.autolink(src, mangle)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          if (!this.state.inLink && (token = this.tokenizer.url(src, mangle))) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            continue;
          }
          cutSrc = src;
          if (this.options.extensions && this.options.extensions.startInline) {
            (function() {
              var startIndex = Infinity;
              var tempSrc = src.slice(1);
              var tempStart = void 0;
              _this2.options.extensions.startInline.forEach(function(getStartIndex) {
                tempStart = getStartIndex.call({
                  lexer: this
                }, tempSrc);
                if (typeof tempStart === "number" && tempStart >= 0) {
                  startIndex = Math.min(startIndex, tempStart);
                }
              });
              if (startIndex < Infinity && startIndex >= 0) {
                cutSrc = src.substring(0, startIndex + 1);
              }
            })();
          }
          if (token = this.tokenizer.inlineText(cutSrc, smartypants)) {
            src = src.substring(token.raw.length);
            if (token.raw.slice(-1) !== "_") {
              prevChar = token.raw.slice(-1);
            }
            keepPrevChar = true;
            lastToken = tokens[tokens.length - 1];
            if (lastToken && lastToken.type === "text") {
              lastToken.raw += token.raw;
              lastToken.text += token.text;
            } else {
              tokens.push(token);
            }
            continue;
          }
          if (src) {
            var errMsg = "Infinite loop on byte: " + src.charCodeAt(0);
            if (this.options.silent) {
              console.error(errMsg);
              break;
            } else {
              throw new Error(errMsg);
            }
          }
        }
        return tokens;
      };
      _createClass(Lexer3, null, [{
        key: "rules",
        get: function get() {
          return {
            block: block2,
            inline
          };
        }
      }]);
      return Lexer3;
    }();
    var Renderer = /* @__PURE__ */ function() {
      function Renderer2(options2) {
        this.options = options2 || exports.defaults;
      }
      var _proto = Renderer2.prototype;
      _proto.code = function code(_code, infostring, escaped) {
        var lang = (infostring || "").match(/\S*/)[0];
        if (this.options.highlight) {
          var out = this.options.highlight(_code, lang);
          if (out != null && out !== _code) {
            escaped = true;
            _code = out;
          }
        }
        _code = _code.replace(/\n$/, "") + "\n";
        if (!lang) {
          return "<pre><code>" + (escaped ? _code : escape(_code, true)) + "</code></pre>\n";
        }
        return '<pre><code class="' + this.options.langPrefix + escape(lang) + '">' + (escaped ? _code : escape(_code, true)) + "</code></pre>\n";
      };
      _proto.blockquote = function blockquote(quote) {
        return "<blockquote>\n" + quote + "</blockquote>\n";
      };
      _proto.html = function html(_html) {
        return _html;
      };
      _proto.heading = function heading(text, level, raw, slugger) {
        if (this.options.headerIds) {
          var id = this.options.headerPrefix + slugger.slug(raw);
          return "<h" + level + ' id="' + id + '">' + text + "</h" + level + ">\n";
        }
        return "<h" + level + ">" + text + "</h" + level + ">\n";
      };
      _proto.hr = function hr() {
        return this.options.xhtml ? "<hr/>\n" : "<hr>\n";
      };
      _proto.list = function list(body, ordered, start) {
        var type = ordered ? "ol" : "ul", startatt = ordered && start !== 1 ? ' start="' + start + '"' : "";
        return "<" + type + startatt + ">\n" + body + "</" + type + ">\n";
      };
      _proto.listitem = function listitem(text) {
        return "<li>" + text + "</li>\n";
      };
      _proto.checkbox = function checkbox(checked) {
        return "<input " + (checked ? 'checked="" ' : "") + 'disabled="" type="checkbox"' + (this.options.xhtml ? " /" : "") + "> ";
      };
      _proto.paragraph = function paragraph(text) {
        return "<p>" + text + "</p>\n";
      };
      _proto.table = function table(header, body) {
        if (body)
          body = "<tbody>" + body + "</tbody>";
        return "<table>\n<thead>\n" + header + "</thead>\n" + body + "</table>\n";
      };
      _proto.tablerow = function tablerow(content) {
        return "<tr>\n" + content + "</tr>\n";
      };
      _proto.tablecell = function tablecell(content, flags) {
        var type = flags.header ? "th" : "td";
        var tag = flags.align ? "<" + type + ' align="' + flags.align + '">' : "<" + type + ">";
        return tag + content + ("</" + type + ">\n");
      };
      _proto.strong = function strong(text) {
        return "<strong>" + text + "</strong>";
      };
      _proto.em = function em(text) {
        return "<em>" + text + "</em>";
      };
      _proto.codespan = function codespan(text) {
        return "<code>" + text + "</code>";
      };
      _proto.br = function br() {
        return this.options.xhtml ? "<br/>" : "<br>";
      };
      _proto.del = function del(text) {
        return "<del>" + text + "</del>";
      };
      _proto.link = function link(href, title, text) {
        href = cleanUrl(this.options.sanitize, this.options.baseUrl, href);
        if (href === null) {
          return text;
        }
        var out = '<a href="' + href + '"';
        if (title) {
          out += ' title="' + title + '"';
        }
        out += ">" + text + "</a>";
        return out;
      };
      _proto.image = function image(href, title, text) {
        href = cleanUrl(this.options.sanitize, this.options.baseUrl, href);
        if (href === null) {
          return text;
        }
        var out = '<img src="' + href + '" alt="' + text + '"';
        if (title) {
          out += ' title="' + title + '"';
        }
        out += this.options.xhtml ? "/>" : ">";
        return out;
      };
      _proto.text = function text(_text) {
        return _text;
      };
      return Renderer2;
    }();
    var TextRenderer = /* @__PURE__ */ function() {
      function TextRenderer2() {
      }
      var _proto = TextRenderer2.prototype;
      _proto.strong = function strong(text) {
        return text;
      };
      _proto.em = function em(text) {
        return text;
      };
      _proto.codespan = function codespan(text) {
        return text;
      };
      _proto.del = function del(text) {
        return text;
      };
      _proto.html = function html(text) {
        return text;
      };
      _proto.text = function text(_text) {
        return _text;
      };
      _proto.link = function link(href, title, text) {
        return "" + text;
      };
      _proto.image = function image(href, title, text) {
        return "" + text;
      };
      _proto.br = function br() {
        return "";
      };
      return TextRenderer2;
    }();
    var Slugger = /* @__PURE__ */ function() {
      function Slugger2() {
        this.seen = {};
      }
      var _proto = Slugger2.prototype;
      _proto.serialize = function serialize(value) {
        return value.toLowerCase().trim().replace(/<[!\/a-z].*?>/ig, "").replace(/[\u2000-\u206F\u2E00-\u2E7F\\'!"#$%&()*+,./:;<=>?@[\]^`{|}~]/g, "").replace(/\s/g, "-");
      };
      _proto.getNextSafeSlug = function getNextSafeSlug(originalSlug, isDryRun) {
        var slug = originalSlug;
        var occurenceAccumulator = 0;
        if (this.seen.hasOwnProperty(slug)) {
          occurenceAccumulator = this.seen[originalSlug];
          do {
            occurenceAccumulator++;
            slug = originalSlug + "-" + occurenceAccumulator;
          } while (this.seen.hasOwnProperty(slug));
        }
        if (!isDryRun) {
          this.seen[originalSlug] = occurenceAccumulator;
          this.seen[slug] = 0;
        }
        return slug;
      };
      _proto.slug = function slug(value, options2) {
        if (options2 === void 0) {
          options2 = {};
        }
        var slug2 = this.serialize(value);
        return this.getNextSafeSlug(slug2, options2.dryrun);
      };
      return Slugger2;
    }();
    var Parser2 = /* @__PURE__ */ function() {
      function Parser3(options2) {
        this.options = options2 || exports.defaults;
        this.options.renderer = this.options.renderer || new Renderer();
        this.renderer = this.options.renderer;
        this.renderer.options = this.options;
        this.textRenderer = new TextRenderer();
        this.slugger = new Slugger();
      }
      Parser3.parse = function parse3(tokens, options2) {
        var parser2 = new Parser3(options2);
        return parser2.parse(tokens);
      };
      Parser3.parseInline = function parseInline2(tokens, options2) {
        var parser2 = new Parser3(options2);
        return parser2.parseInline(tokens);
      };
      var _proto = Parser3.prototype;
      _proto.parse = function parse3(tokens, top) {
        if (top === void 0) {
          top = true;
        }
        var out = "", i, j, k, l2, l3, row, cell, header, body, token, ordered, start, loose, itemBody, item, checked, task, checkbox, ret;
        var l = tokens.length;
        for (i = 0; i < l; i++) {
          token = tokens[i];
          if (this.options.extensions && this.options.extensions.renderers && this.options.extensions.renderers[token.type]) {
            ret = this.options.extensions.renderers[token.type].call({
              parser: this
            }, token);
            if (ret !== false || !["space", "hr", "heading", "code", "table", "blockquote", "list", "html", "paragraph", "text"].includes(token.type)) {
              out += ret || "";
              continue;
            }
          }
          switch (token.type) {
            case "space": {
              continue;
            }
            case "hr": {
              out += this.renderer.hr();
              continue;
            }
            case "heading": {
              out += this.renderer.heading(this.parseInline(token.tokens), token.depth, unescape(this.parseInline(token.tokens, this.textRenderer)), this.slugger);
              continue;
            }
            case "code": {
              out += this.renderer.code(token.text, token.lang, token.escaped);
              continue;
            }
            case "table": {
              header = "";
              cell = "";
              l2 = token.header.length;
              for (j = 0; j < l2; j++) {
                cell += this.renderer.tablecell(this.parseInline(token.header[j].tokens), {
                  header: true,
                  align: token.align[j]
                });
              }
              header += this.renderer.tablerow(cell);
              body = "";
              l2 = token.rows.length;
              for (j = 0; j < l2; j++) {
                row = token.rows[j];
                cell = "";
                l3 = row.length;
                for (k = 0; k < l3; k++) {
                  cell += this.renderer.tablecell(this.parseInline(row[k].tokens), {
                    header: false,
                    align: token.align[k]
                  });
                }
                body += this.renderer.tablerow(cell);
              }
              out += this.renderer.table(header, body);
              continue;
            }
            case "blockquote": {
              body = this.parse(token.tokens);
              out += this.renderer.blockquote(body);
              continue;
            }
            case "list": {
              ordered = token.ordered;
              start = token.start;
              loose = token.loose;
              l2 = token.items.length;
              body = "";
              for (j = 0; j < l2; j++) {
                item = token.items[j];
                checked = item.checked;
                task = item.task;
                itemBody = "";
                if (item.task) {
                  checkbox = this.renderer.checkbox(checked);
                  if (loose) {
                    if (item.tokens.length > 0 && item.tokens[0].type === "paragraph") {
                      item.tokens[0].text = checkbox + " " + item.tokens[0].text;
                      if (item.tokens[0].tokens && item.tokens[0].tokens.length > 0 && item.tokens[0].tokens[0].type === "text") {
                        item.tokens[0].tokens[0].text = checkbox + " " + item.tokens[0].tokens[0].text;
                      }
                    } else {
                      item.tokens.unshift({
                        type: "text",
                        text: checkbox
                      });
                    }
                  } else {
                    itemBody += checkbox;
                  }
                }
                itemBody += this.parse(item.tokens, loose);
                body += this.renderer.listitem(itemBody, task, checked);
              }
              out += this.renderer.list(body, ordered, start);
              continue;
            }
            case "html": {
              out += this.renderer.html(token.text);
              continue;
            }
            case "paragraph": {
              out += this.renderer.paragraph(this.parseInline(token.tokens));
              continue;
            }
            case "text": {
              body = token.tokens ? this.parseInline(token.tokens) : token.text;
              while (i + 1 < l && tokens[i + 1].type === "text") {
                token = tokens[++i];
                body += "\n" + (token.tokens ? this.parseInline(token.tokens) : token.text);
              }
              out += top ? this.renderer.paragraph(body) : body;
              continue;
            }
            default: {
              var errMsg = 'Token with "' + token.type + '" type was not found.';
              if (this.options.silent) {
                console.error(errMsg);
                return;
              } else {
                throw new Error(errMsg);
              }
            }
          }
        }
        return out;
      };
      _proto.parseInline = function parseInline2(tokens, renderer) {
        renderer = renderer || this.renderer;
        var out = "", i, token, ret;
        var l = tokens.length;
        for (i = 0; i < l; i++) {
          token = tokens[i];
          if (this.options.extensions && this.options.extensions.renderers && this.options.extensions.renderers[token.type]) {
            ret = this.options.extensions.renderers[token.type].call({
              parser: this
            }, token);
            if (ret !== false || !["escape", "html", "link", "image", "strong", "em", "codespan", "br", "del", "text"].includes(token.type)) {
              out += ret || "";
              continue;
            }
          }
          switch (token.type) {
            case "escape": {
              out += renderer.text(token.text);
              break;
            }
            case "html": {
              out += renderer.html(token.text);
              break;
            }
            case "link": {
              out += renderer.link(token.href, token.title, this.parseInline(token.tokens, renderer));
              break;
            }
            case "image": {
              out += renderer.image(token.href, token.title, token.text);
              break;
            }
            case "strong": {
              out += renderer.strong(this.parseInline(token.tokens, renderer));
              break;
            }
            case "em": {
              out += renderer.em(this.parseInline(token.tokens, renderer));
              break;
            }
            case "codespan": {
              out += renderer.codespan(token.text);
              break;
            }
            case "br": {
              out += renderer.br();
              break;
            }
            case "del": {
              out += renderer.del(this.parseInline(token.tokens, renderer));
              break;
            }
            case "text": {
              out += renderer.text(token.text);
              break;
            }
            default: {
              var errMsg = 'Token with "' + token.type + '" type was not found.';
              if (this.options.silent) {
                console.error(errMsg);
                return;
              } else {
                throw new Error(errMsg);
              }
            }
          }
        }
        return out;
      };
      return Parser3;
    }();
    var Hooks = /* @__PURE__ */ function() {
      function Hooks2(options2) {
        this.options = options2 || exports.defaults;
      }
      var _proto = Hooks2.prototype;
      _proto.preprocess = function preprocess(markdown) {
        return markdown;
      };
      _proto.postprocess = function postprocess(html) {
        return html;
      };
      return Hooks2;
    }();
    Hooks.passThroughHooks = /* @__PURE__ */ new Set(["preprocess", "postprocess"]);
    function onError(silent, async, callback) {
      return function(e2) {
        e2.message += "\nPlease report this to https://github.com/markedjs/marked.";
        if (silent) {
          var msg = "<p>An error occurred:</p><pre>" + escape(e2.message + "", true) + "</pre>";
          if (async) {
            return Promise.resolve(msg);
          }
          if (callback) {
            callback(null, msg);
            return;
          }
          return msg;
        }
        if (async) {
          return Promise.reject(e2);
        }
        if (callback) {
          callback(e2);
          return;
        }
        throw e2;
      };
    }
    function parseMarkdown(lexer2, parser2) {
      return function(src, opt, callback) {
        if (typeof opt === "function") {
          callback = opt;
          opt = null;
        }
        var origOpt = _extends({}, opt);
        opt = _extends({}, marked.defaults, origOpt);
        var throwError = onError(opt.silent, opt.async, callback);
        if (typeof src === "undefined" || src === null) {
          return throwError(new Error("marked(): input parameter is undefined or null"));
        }
        if (typeof src !== "string") {
          return throwError(new Error("marked(): input parameter is of type " + Object.prototype.toString.call(src) + ", string expected"));
        }
        checkSanitizeDeprecation(opt);
        if (opt.hooks) {
          opt.hooks.options = opt;
        }
        if (callback) {
          var highlight = opt.highlight;
          var tokens;
          try {
            if (opt.hooks) {
              src = opt.hooks.preprocess(src);
            }
            tokens = lexer2(src, opt);
          } catch (e2) {
            return throwError(e2);
          }
          var done = function done2(err) {
            var out;
            if (!err) {
              try {
                if (opt.walkTokens) {
                  marked.walkTokens(tokens, opt.walkTokens);
                }
                out = parser2(tokens, opt);
                if (opt.hooks) {
                  out = opt.hooks.postprocess(out);
                }
              } catch (e2) {
                err = e2;
              }
            }
            opt.highlight = highlight;
            return err ? throwError(err) : callback(null, out);
          };
          if (!highlight || highlight.length < 3) {
            return done();
          }
          delete opt.highlight;
          if (!tokens.length)
            return done();
          var pending = 0;
          marked.walkTokens(tokens, function(token) {
            if (token.type === "code") {
              pending++;
              setTimeout(function() {
                highlight(token.text, token.lang, function(err, code) {
                  if (err) {
                    return done(err);
                  }
                  if (code != null && code !== token.text) {
                    token.text = code;
                    token.escaped = true;
                  }
                  pending--;
                  if (pending === 0) {
                    done();
                  }
                });
              }, 0);
            }
          });
          if (pending === 0) {
            done();
          }
          return;
        }
        if (opt.async) {
          return Promise.resolve(opt.hooks ? opt.hooks.preprocess(src) : src).then(function(src2) {
            return lexer2(src2, opt);
          }).then(function(tokens2) {
            return opt.walkTokens ? Promise.all(marked.walkTokens(tokens2, opt.walkTokens)).then(function() {
              return tokens2;
            }) : tokens2;
          }).then(function(tokens2) {
            return parser2(tokens2, opt);
          }).then(function(html2) {
            return opt.hooks ? opt.hooks.postprocess(html2) : html2;
          })["catch"](throwError);
        }
        try {
          if (opt.hooks) {
            src = opt.hooks.preprocess(src);
          }
          var _tokens = lexer2(src, opt);
          if (opt.walkTokens) {
            marked.walkTokens(_tokens, opt.walkTokens);
          }
          var html = parser2(_tokens, opt);
          if (opt.hooks) {
            html = opt.hooks.postprocess(html);
          }
          return html;
        } catch (e2) {
          return throwError(e2);
        }
      };
    }
    function marked(src, opt, callback) {
      return parseMarkdown(Lexer2.lex, Parser2.parse)(src, opt, callback);
    }
    marked.options = marked.setOptions = function(opt) {
      marked.defaults = _extends({}, marked.defaults, opt);
      changeDefaults(marked.defaults);
      return marked;
    };
    marked.getDefaults = getDefaults;
    marked.defaults = exports.defaults;
    marked.use = function() {
      var extensions = marked.defaults.extensions || {
        renderers: {},
        childTokens: {}
      };
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      args.forEach(function(pack) {
        var opts = _extends({}, pack);
        opts.async = marked.defaults.async || opts.async || false;
        if (pack.extensions) {
          pack.extensions.forEach(function(ext) {
            if (!ext.name) {
              throw new Error("extension name required");
            }
            if (ext.renderer) {
              var prevRenderer = extensions.renderers[ext.name];
              if (prevRenderer) {
                extensions.renderers[ext.name] = function() {
                  for (var _len2 = arguments.length, args2 = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
                    args2[_key2] = arguments[_key2];
                  }
                  var ret = ext.renderer.apply(this, args2);
                  if (ret === false) {
                    ret = prevRenderer.apply(this, args2);
                  }
                  return ret;
                };
              } else {
                extensions.renderers[ext.name] = ext.renderer;
              }
            }
            if (ext.tokenizer) {
              if (!ext.level || ext.level !== "block" && ext.level !== "inline") {
                throw new Error("extension level must be 'block' or 'inline'");
              }
              if (extensions[ext.level]) {
                extensions[ext.level].unshift(ext.tokenizer);
              } else {
                extensions[ext.level] = [ext.tokenizer];
              }
              if (ext.start) {
                if (ext.level === "block") {
                  if (extensions.startBlock) {
                    extensions.startBlock.push(ext.start);
                  } else {
                    extensions.startBlock = [ext.start];
                  }
                } else if (ext.level === "inline") {
                  if (extensions.startInline) {
                    extensions.startInline.push(ext.start);
                  } else {
                    extensions.startInline = [ext.start];
                  }
                }
              }
            }
            if (ext.childTokens) {
              extensions.childTokens[ext.name] = ext.childTokens;
            }
          });
          opts.extensions = extensions;
        }
        if (pack.renderer) {
          (function() {
            var renderer = marked.defaults.renderer || new Renderer();
            var _loop = function _loop2(prop2) {
              var prevRenderer = renderer[prop2];
              renderer[prop2] = function() {
                for (var _len3 = arguments.length, args2 = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
                  args2[_key3] = arguments[_key3];
                }
                var ret = pack.renderer[prop2].apply(renderer, args2);
                if (ret === false) {
                  ret = prevRenderer.apply(renderer, args2);
                }
                return ret;
              };
            };
            for (var prop in pack.renderer) {
              _loop(prop);
            }
            opts.renderer = renderer;
          })();
        }
        if (pack.tokenizer) {
          (function() {
            var tokenizer = marked.defaults.tokenizer || new Tokenizer();
            var _loop2 = function _loop22(prop2) {
              var prevTokenizer = tokenizer[prop2];
              tokenizer[prop2] = function() {
                for (var _len4 = arguments.length, args2 = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
                  args2[_key4] = arguments[_key4];
                }
                var ret = pack.tokenizer[prop2].apply(tokenizer, args2);
                if (ret === false) {
                  ret = prevTokenizer.apply(tokenizer, args2);
                }
                return ret;
              };
            };
            for (var prop in pack.tokenizer) {
              _loop2(prop);
            }
            opts.tokenizer = tokenizer;
          })();
        }
        if (pack.hooks) {
          (function() {
            var hooks = marked.defaults.hooks || new Hooks();
            var _loop3 = function _loop32(prop2) {
              var prevHook = hooks[prop2];
              if (Hooks.passThroughHooks.has(prop2)) {
                hooks[prop2] = function(arg) {
                  if (marked.defaults.async) {
                    return Promise.resolve(pack.hooks[prop2].call(hooks, arg)).then(function(ret2) {
                      return prevHook.call(hooks, ret2);
                    });
                  }
                  var ret = pack.hooks[prop2].call(hooks, arg);
                  return prevHook.call(hooks, ret);
                };
              } else {
                hooks[prop2] = function() {
                  for (var _len5 = arguments.length, args2 = new Array(_len5), _key5 = 0; _key5 < _len5; _key5++) {
                    args2[_key5] = arguments[_key5];
                  }
                  var ret = pack.hooks[prop2].apply(hooks, args2);
                  if (ret === false) {
                    ret = prevHook.apply(hooks, args2);
                  }
                  return ret;
                };
              }
            };
            for (var prop in pack.hooks) {
              _loop3(prop);
            }
            opts.hooks = hooks;
          })();
        }
        if (pack.walkTokens) {
          var _walkTokens = marked.defaults.walkTokens;
          opts.walkTokens = function(token) {
            var values = [];
            values.push(pack.walkTokens.call(this, token));
            if (_walkTokens) {
              values = values.concat(_walkTokens.call(this, token));
            }
            return values;
          };
        }
        marked.setOptions(opts);
      });
    };
    marked.walkTokens = function(tokens, callback) {
      var values = [];
      var _loop4 = function _loop42() {
        var token = _step.value;
        values = values.concat(callback.call(marked, token));
        switch (token.type) {
          case "table": {
            for (var _iterator2 = _createForOfIteratorHelperLoose(token.header), _step2; !(_step2 = _iterator2()).done; ) {
              var cell = _step2.value;
              values = values.concat(marked.walkTokens(cell.tokens, callback));
            }
            for (var _iterator3 = _createForOfIteratorHelperLoose(token.rows), _step3; !(_step3 = _iterator3()).done; ) {
              var row = _step3.value;
              for (var _iterator4 = _createForOfIteratorHelperLoose(row), _step4; !(_step4 = _iterator4()).done; ) {
                var _cell = _step4.value;
                values = values.concat(marked.walkTokens(_cell.tokens, callback));
              }
            }
            break;
          }
          case "list": {
            values = values.concat(marked.walkTokens(token.items, callback));
            break;
          }
          default: {
            if (marked.defaults.extensions && marked.defaults.extensions.childTokens && marked.defaults.extensions.childTokens[token.type]) {
              marked.defaults.extensions.childTokens[token.type].forEach(function(childTokens) {
                values = values.concat(marked.walkTokens(token[childTokens], callback));
              });
            } else if (token.tokens) {
              values = values.concat(marked.walkTokens(token.tokens, callback));
            }
          }
        }
      };
      for (var _iterator = _createForOfIteratorHelperLoose(tokens), _step; !(_step = _iterator()).done; ) {
        _loop4();
      }
      return values;
    };
    marked.parseInline = parseMarkdown(Lexer2.lexInline, Parser2.parseInline);
    marked.Parser = Parser2;
    marked.parser = Parser2.parse;
    marked.Renderer = Renderer;
    marked.TextRenderer = TextRenderer;
    marked.Lexer = Lexer2;
    marked.lexer = Lexer2.lex;
    marked.Tokenizer = Tokenizer;
    marked.Slugger = Slugger;
    marked.Hooks = Hooks;
    marked.parse = marked;
    var options = marked.options;
    var setOptions = marked.setOptions;
    var use = marked.use;
    var walkTokens = marked.walkTokens;
    var parseInline = marked.parseInline;
    var parse2 = marked;
    var parser = Parser2.parse;
    var lexer = Lexer2.lex;
    exports.Hooks = Hooks;
    exports.Lexer = Lexer2;
    exports.Parser = Parser2;
    exports.Renderer = Renderer;
    exports.Slugger = Slugger;
    exports.TextRenderer = TextRenderer;
    exports.Tokenizer = Tokenizer;
    exports.getDefaults = getDefaults;
    exports.lexer = lexer;
    exports.marked = marked;
    exports.options = options;
    exports.parse = parse2;
    exports.parseInline = parseInline;
    exports.parser = parser;
    exports.setOptions = setOptions;
    exports.use = use;
    exports.walkTokens = walkTokens;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/markdown/create-markdown-parser-marked.js
var require_create_markdown_parser_marked = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/markdown/create-markdown-parser-marked.js"(exports, module) {
    var n2 = Object.defineProperty;
    var k = Object.getOwnPropertyDescriptor;
    var m = Object.getOwnPropertyNames;
    var p = Object.prototype.hasOwnProperty;
    var s = (e2, r) => {
      for (var a in r)
        n2(e2, a, { get: r[a], enumerable: true });
    };
    var c = (e2, r, a, t2) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let o of m(r))
          !p.call(e2, o) && o !== a && n2(e2, o, { get: () => r[o], enumerable: !(t2 = k(r, o)) || t2.enumerable });
      return e2;
    };
    var f = (e2) => c(n2({}, "__esModule", { value: true }), e2);
    var M = {};
    s(M, { createMarkdownParserMarked: () => u2 });
    module.exports = f(M);
    var d2 = require_marked();
    function u2(e2) {
      const r = e2 ? { highlight: e2 } : {};
      return (a) => d2.marked.parse(a, r);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/settings/default-settings.js
var require_default_settings = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/settings/default-settings.js"(exports, module) {
    var r = Object.defineProperty;
    var o = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var l = (a, e2) => {
      for (var t2 in e2)
        r(a, t2, { get: e2[t2], enumerable: true });
    };
    var m = (a, e2, t2, s) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let n2 of p(e2))
          !d2.call(a, n2) && n2 !== t2 && r(a, n2, { get: () => e2[n2], enumerable: !(s = o(e2, n2)) || s.enumerable });
      return a;
    };
    var f = (a) => m(r({}, "__esModule", { value: true }), a);
    var k = {};
    l(k, { defaultSettings: () => g });
    module.exports = f(k);
    var i = require_create_markdown_parser_marked();
    var g = { pagination: "item", jsonSamplesDepth: 1, samplesMaxInlineArgs: 2, markdown: { parser: (0, i.createMarkdownParserMarked)() }, fieldExpandLevel: 4, hidePaginationButtons: false };
  }
});

// node_modules/@redocly/graphql-docs/lib/src/markdown/styled.js
var require_styled = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/markdown/styled.js"(exports, module) {
    var m = Object.create;
    var a = Object.defineProperty;
    var d2 = Object.getOwnPropertyDescriptor;
    var f = Object.getOwnPropertyNames;
    var g = Object.getPrototypeOf;
    var h2 = Object.prototype.hasOwnProperty;
    var b = (r, e2) => {
      for (var o in e2)
        a(r, o, {
          get: e2[o],
          enumerable: true
        });
    };
    var n2 = (r, e2, o, i) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let t2 of f(e2))
          !h2.call(r, t2) && t2 !== o && a(r, t2, {
            get: () => e2[t2],
            enumerable: !(i = d2(e2, t2)) || i.enumerable
          });
      return r;
    };
    var p = (r, e2, o) => (o = r != null ? m(g(r)) : {}, n2(e2 || !r || !r.__esModule ? a(o, "default", {
      value: r,
      enumerable: true
    }) : o, r));
    var k = (r) => n2(a({}, "__esModule", {
      value: true
    }), r);
    var w = {};
    b(w, {
      StyledMarkdown: () => v
    });
    module.exports = k(w);
    var s = (init_src(), __toCommonJS(src_exports));
    var l = p((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var v = (0, l.default)(s.Markdown)`
  font-size: ${({
      isField: r
    }) => r ? "var(--font-size-base)" : "var(--font-size-lg)"}; // TODO: refactor parameters
  line-height: ${({
      isField: r
    }) => r ? "var(--line-height-base)" : "var(--line-height-lg)"};
  word-break: break-word;

  p {
    margin-bottom: 0;
  }
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/markdown/Markdown.js
var require_Markdown = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/markdown/Markdown.js"(exports, module) {
    var d2 = Object.create;
    var r = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var s = Object.getOwnPropertyNames;
    var u2 = Object.getPrototypeOf;
    var c = Object.prototype.hasOwnProperty;
    var p = (e2, t2) => {
      for (var n2 in t2)
        r(e2, n2, { get: t2[n2], enumerable: true });
    };
    var m = (e2, t2, n2, o) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let l of s(t2))
          !c.call(e2, l) && l !== n2 && r(e2, l, { get: () => t2[l], enumerable: !(o = f(t2, l)) || o.enumerable });
      return e2;
    };
    var h2 = (e2, t2, n2) => (n2 = e2 != null ? d2(u2(e2)) : {}, m(t2 || !e2 || !e2.__esModule ? r(n2, "default", { value: e2, enumerable: true }) : n2, e2));
    var y = (e2) => m(r({}, "__esModule", { value: true }), e2);
    var R = {};
    p(R, { Markdown: () => M });
    module.exports = y(R);
    var i = h2(__require("react"));
    var a = require_styled();
    function M({ html: e2, rawMarkdown: t2, parser: n2, isField: o = false }) {
      let l = e2;
      return !l && t2 && n2 && (l = n2(t2)), l ? i.default.createElement(a.StyledMarkdown, { as: "span", dangerouslySetInnerHTML: { __html: l || "" }, isField: o }) : null;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/markdown/index.js
var require_markdown = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/markdown/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (r, o, p, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let m of c(o))
          !d2.call(r, m) && m !== p && a(r, m, { get: () => o[m], enumerable: !(x = b(o, m)) || x.enumerable });
      return r;
    };
    var f = (r, o, p) => (t2(r, o, "default"), p && t2(p, o, "default"));
    var g = (r) => t2(a({}, "__esModule", { value: true }), r);
    var e2 = {};
    module.exports = g(e2);
    f(e2, require_create_markdown_parser_marked(), module.exports);
    f(e2, require_Markdown(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/settings/normalize-settings.js
var require_normalize_settings = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/settings/normalize-settings.js"(exports, module) {
    var c = Object.create;
    var m = Object.defineProperty;
    var p = Object.getOwnPropertyDescriptor;
    var w = Object.getOwnPropertyNames;
    var f = Object.getPrototypeOf;
    var l = Object.prototype.hasOwnProperty;
    var b = (r, o) => {
      for (var a in o)
        m(r, a, { get: o[a], enumerable: true });
    };
    var d2 = (r, o, a, n2) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of w(o))
          !l.call(r, e2) && e2 !== a && m(r, e2, { get: () => o[e2], enumerable: !(n2 = p(o, e2)) || n2.enumerable });
      return r;
    };
    var g = (r, o, a) => (a = r != null ? c(f(r)) : {}, d2(o || !r || !r.__esModule ? m(a, "default", { value: r, enumerable: true }) : a, r));
    var j = (r) => d2(m({}, "__esModule", { value: true }), r);
    var O = {};
    b(O, { normalizeSettings: () => u2 });
    module.exports = j(O);
    var i = g(require_cjs());
    var k = require_default_settings();
    var t2 = require_markdown();
    function u2(r) {
      const o = r?.markdown ? { markdown: Object.assign(Object.assign({}, r.markdown), { parser: r.markdown.parser || (0, t2.createMarkdownParserMarked)(r.markdown.highlight) }) } : {};
      return (0, i.default)(k.defaultSettings, Object.assign(Object.assign({}, r || {}), o || {}));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/settings/settings.context.js
var require_settings_context = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/settings/settings.context.js"(exports, module) {
    var a = Object.create;
    var i = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var x = Object.getPrototypeOf;
    var S = Object.prototype.hasOwnProperty;
    var d2 = (t2, e2) => {
      for (var r in e2)
        i(t2, r, { get: e2[r], enumerable: true });
    };
    var m = (t2, e2, r, s) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let o of p(e2))
          !S.call(t2, o) && o !== r && i(t2, o, { get: () => e2[o], enumerable: !(s = f(e2, o)) || s.enumerable });
      return t2;
    };
    var l = (t2, e2, r) => (r = t2 != null ? a(x(t2)) : {}, m(e2 || !t2 || !t2.__esModule ? i(r, "default", { value: t2, enumerable: true }) : r, t2));
    var C = (t2) => m(i({}, "__esModule", { value: true }), t2);
    var z = {};
    d2(z, { SettingsProvider: () => v, useSettings: () => P });
    module.exports = C(z);
    var n2 = l(__require("react"));
    var c = require_default_settings();
    var g = require_normalize_settings();
    var u2 = (0, n2.createContext)(c.defaultSettings);
    var v = ({ value: t2, children: e2 }) => {
      const r = (0, n2.useMemo)(() => (0, g.normalizeSettings)(t2), [t2]);
      return n2.default.createElement(u2.Provider, { value: r }, e2);
    };
    function P() {
      return (0, n2.useContext)(u2);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/settings/settings.js
var require_settings = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/settings/settings.js"(exports, module) {
    var t2 = Object.defineProperty;
    var a = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var d2 = (o, e2, x, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let p of b(e2))
          !c.call(o, p) && p !== x && t2(o, p, { get: () => e2[p], enumerable: !(r = a(e2, p)) || r.enumerable });
      return o;
    };
    var f = (o) => d2(t2({}, "__esModule", { value: true }), o);
    var g = {};
    module.exports = f(g);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/settings/models/reference-docs-location.js
var require_reference_docs_location = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/settings/models/reference-docs-location.js"(exports, module) {
    var c = Object.defineProperty;
    var n2 = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var r = Object.prototype.hasOwnProperty;
    var s = (o, O) => {
      for (var T in O)
        c(o, T, { get: O[T], enumerable: true });
    };
    var x = (o, O, T, e2) => {
      if (O && typeof O == "object" || typeof O == "function")
        for (let t2 of p(O))
          !r.call(o, t2) && t2 !== T && c(o, t2, { get: () => O[t2], enumerable: !(e2 = n2(O, t2)) || e2.enumerable });
      return o;
    };
    var A = (o) => x(c({}, "__esModule", { value: true }), o);
    var I = {};
    s(I, { ROOT_LOCATION: () => C });
    module.exports = A(I);
    var C = {};
  }
});

// node_modules/@redocly/graphql-docs/lib/src/settings/models/index.js
var require_models3 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/settings/models/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var p = (r, o, f, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of c(o))
          !d2.call(r, e2) && e2 !== f && a(r, e2, { get: () => o[e2], enumerable: !(x = b(o, e2)) || x.enumerable });
      return r;
    };
    var t2 = (r, o, f) => (p(r, o, "default"), f && p(f, o, "default"));
    var g = (r) => p(a({}, "__esModule", { value: true }), r);
    var m = {};
    module.exports = g(m);
    t2(m, require_reference_docs_location(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/settings/index.js
var require_settings2 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/settings/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_settings_context(), module.exports);
    r(o, require_settings(), module.exports);
    r(o, require_default_settings(), module.exports);
    r(o, require_models3(), module.exports);
    r(o, require_normalize_settings(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/utils/is-regexp.js
var require_is_regexp = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/utils/is-regexp.js"(exports, module) {
    var i = Object.defineProperty;
    var p = Object.getOwnPropertyDescriptor;
    var x = Object.getOwnPropertyNames;
    var s = Object.prototype.hasOwnProperty;
    var u2 = (n2, t2) => {
      for (var r in t2)
        i(n2, r, { get: t2[r], enumerable: true });
    };
    var c = (n2, t2, r, g) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let e2 of x(t2))
          !s.call(n2, e2) && e2 !== r && i(n2, e2, { get: () => t2[e2], enumerable: !(g = p(t2, e2)) || g.enumerable });
      return n2;
    };
    var f = (n2) => c(i({}, "__esModule", { value: true }), n2);
    var R = {};
    u2(R, { isRegExp: () => o, isRegExpString: () => E });
    module.exports = f(R);
    function o(n2) {
      return n2 instanceof RegExp;
    }
    function E(n2) {
      return !o(n2) && /^\/.+\/[igm]*$/.test(n2);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/utils/is-string.js
var require_is_string = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/utils/is-string.js"(exports, module) {
    var g = Object.defineProperty;
    var s = Object.getOwnPropertyDescriptor;
    var e2 = Object.getOwnPropertyNames;
    var p = Object.prototype.hasOwnProperty;
    var x = (i, r) => {
      for (var t2 in r)
        g(i, t2, { get: r[t2], enumerable: true });
    };
    var S = (i, r, t2, o) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let n2 of e2(r))
          !p.call(i, n2) && n2 !== t2 && g(i, n2, { get: () => r[n2], enumerable: !(o = s(r, n2)) || o.enumerable });
      return i;
    };
    var c = (i) => S(g({}, "__esModule", { value: true }), i);
    var u2 = {};
    x(u2, { isString: () => m });
    module.exports = c(u2);
    var f = require_is_regexp();
    function m(i) {
      return typeof i == "string" && !(0, f.isRegExpString)(i);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/utils/regexp-from-string.js
var require_regexp_from_string = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/utils/regexp-from-string.js"(exports, module) {
    var p = Object.defineProperty;
    var s = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var g = Object.prototype.hasOwnProperty;
    var f = (t2, r) => {
      for (var e2 in r)
        p(t2, e2, { get: r[e2], enumerable: true });
    };
    var u2 = (t2, r, e2, o) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let n2 of c(r))
          !g.call(t2, n2) && n2 !== e2 && p(t2, n2, { get: () => r[n2], enumerable: !(o = s(r, n2)) || o.enumerable });
      return t2;
    };
    var x = (t2) => u2(p({}, "__esModule", { value: true }), t2);
    var d2 = {};
    f(d2, { getRegExpFromString: () => a });
    module.exports = x(d2);
    var i = require_is_regexp();
    function a(t2) {
      if ((0, i.isRegExp)(t2))
        return t2;
      try {
        const r = t2.split("/"), e2 = r.pop(), o = r.slice(1).join("/");
        return new RegExp(o, e2);
      } catch {
        return;
      }
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/config/index-config.js
var require_index_config = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/config/index-config.js"(exports, module) {
    var o = Object.defineProperty;
    var p = Object.getOwnPropertyDescriptor;
    var a = Object.getOwnPropertyNames;
    var S = Object.prototype.hasOwnProperty;
    var m = (e2, r) => {
      for (var n2 in r)
        o(e2, n2, { get: r[n2], enumerable: true });
    };
    var E = (e2, r, n2, u2) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let i of a(r))
          !S.call(e2, i) && i !== n2 && o(e2, i, { get: () => r[i], enumerable: !(u2 = p(r, i)) || u2.enumerable });
      return e2;
    };
    var R = (e2) => E(o({}, "__esModule", { value: true }), e2);
    var B = {};
    m(B, { indexMenuGroupConfigs: () => x, indexMenuGroupingConfig: () => q });
    module.exports = R(B);
    var s = require_is_regexp();
    var l = require_is_string();
    var v = require_regexp_from_string();
    var b = "Other";
    function q(e2) {
      return { requireExactGroups: e2?.requireExactGroups || false, otherItemsGroupName: e2?.otherItemsGroupName || b, groups: e2?.groups ? x(e2.groups) : [] };
    }
    function x(e2) {
      return e2.map((r) => Object.assign(Object.assign({}, r), { items: t2(r.items), queries: t2(r.queries), mutations: t2(r.mutations), subscriptions: t2(r.subscriptions), types: t2(r.types), directives: t2(r.directives) }));
    }
    function t2(e2) {
      return { includeByName: d2(e2?.includeByName), excludeByName: d2(e2?.excludeByName) };
    }
    function d2(e2) {
      const r = new Set(e2?.filter(l.isString)), u2 = (e2?.filter(s.isRegExpString) || []).map((c) => (0, v.getRegExpFromString)(c)).filter(s.isRegExp), i = e2?.filter(s.isRegExp) || [];
      return { names: r, regexps: [...u2, ...i] };
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/config/menu-config.js
var require_menu_config = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/config/menu-config.js"(exports, module) {
    var t2 = Object.defineProperty;
    var a = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var d2 = (o, e2, x, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let p of b(e2))
          !c.call(o, p) && p !== x && t2(o, p, { get: () => e2[p], enumerable: !(r = a(e2, p)) || r.enumerable });
      return o;
    };
    var f = (o) => d2(t2({}, "__esModule", { value: true }), o);
    var g = {};
    module.exports = f(g);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/config/validate-menu-config.js
var require_validate_menu_config = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/config/validate-menu-config.js"(exports, module) {
    var s = Object.defineProperty;
    var l = Object.getOwnPropertyDescriptor;
    var v = Object.getOwnPropertyNames;
    var u2 = Object.prototype.hasOwnProperty;
    var c = (e2, i) => {
      for (var t2 in i)
        s(e2, t2, { get: i[t2], enumerable: true });
    };
    var f = (e2, i, t2, r) => {
      if (i && typeof i == "object" || typeof i == "function")
        for (let n2 of v(i))
          !u2.call(e2, n2) && n2 !== t2 && s(e2, n2, { get: () => i[n2], enumerable: !(r = l(i, n2)) || r.enumerable });
      return e2;
    };
    var y = (e2) => f(s({}, "__esModule", { value: true }), e2);
    var _ = {};
    c(_, { validateMenuConfig: () => w });
    module.exports = y(_);
    var d2 = require_is_string();
    function w(e2, i) {
      var t2;
      (t2 = i?.groups) === null || t2 === void 0 || t2.forEach((r) => {
        E(r.name), h2(e2, r), N(e2, r), T(e2, r), m(e2, r), p(e2, r), x(e2, r);
      });
    }
    function E(e2) {
      if (e2.includes("/"))
        throw new Error(`Group name ${e2} is invalid. "/" is not allowed in group names.`);
    }
    function h2(e2, i) {
      i.items && o(i.items).forEach((t2) => {
        var r, n2, a;
        if (!(!((r = e2.getQueryType()) === null || r === void 0) && r.getFields()[t2]) && !(!((n2 = e2.getMutationType()) === null || n2 === void 0) && n2.getFields()[t2]) && !(!((a = e2.getSubscriptionType()) === null || a === void 0) && a.getFields()[t2]) && !e2.getDirective(t2) && !e2.getType(t2))
          throw new Error(`Item ${t2} does not exist in schema.`);
      });
    }
    function N(e2, i) {
      i.queries && o(i.queries).forEach((t2) => {
        var r;
        if (!(!((r = e2.getQueryType()) === null || r === void 0) && r.getFields()[t2]))
          throw new Error(`Query ${t2} does not exist in schema.`);
      });
    }
    function T(e2, i) {
      i.mutations && o(i.mutations).forEach((t2) => {
        var r;
        if (!(!((r = e2.getMutationType()) === null || r === void 0) && r.getFields()[t2]))
          throw new Error(`Mutation ${t2} does not exist in schema.`);
      });
    }
    function m(e2, i) {
      i.subscriptions && o(i.subscriptions).forEach((t2) => {
        var r;
        if (!(!((r = e2.getSubscriptionType()) === null || r === void 0) && r.getFields()[t2]))
          throw new Error(`Subscription ${t2} does not exist in schema.`);
      });
    }
    function p(e2, i) {
      i.directives && o(i.directives).forEach((t2) => {
        if (!e2.getDirective(t2))
          throw new Error(`Directive ${t2} does not exist in schema.`);
      });
    }
    function x(e2, i) {
      i.types && o(i.types).forEach((t2) => {
        if (!e2.getType(t2))
          throw new Error(`Type ${t2} does not exist in schema.`);
      });
    }
    function o(e2) {
      var i;
      return ((i = e2.includeByName) === null || i === void 0 ? void 0 : i.concat(e2.excludeByName || []).filter(d2.isString)) || [];
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/config/index.js
var require_config = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/config/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, r, p, x) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let m of c(r))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => r[m], enumerable: !(x = b(r, m)) || x.enumerable });
      return f;
    };
    var e2 = (f, r, p) => (t2(f, r, "default"), p && t2(p, r, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    e2(o, require_index_config(), module.exports);
    e2(o, require_menu_config(), module.exports);
    e2(o, require_validate_menu_config(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/models/menu-group.js
var require_menu_group = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/models/menu-group.js"(exports, module) {
    var e2 = Object.defineProperty;
    var D = Object.getOwnPropertyDescriptor;
    var E = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var h2 = (t2, o) => {
      for (var _ in o)
        e2(t2, _, { get: o[_], enumerable: true });
    };
    var n2 = (t2, o, _, r) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let U of E(o))
          !c.call(t2, U) && U !== _ && e2(t2, U, { get: () => o[U], enumerable: !(r = D(o, U)) || r.enumerable });
      return t2;
    };
    var p = (t2) => n2(e2({}, "__esModule", { value: true }), t2);
    var x = {};
    h2(x, { DEFAULT_MENU_GROUP_ID: () => s });
    module.exports = p(x);
    var s = "other";
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/models/menu-list.js
var require_menu_list = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/models/menu-list.js"(exports, module) {
    var t2 = Object.defineProperty;
    var a = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var d2 = (o, e2, x, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let p of b(e2))
          !c.call(o, p) && p !== x && t2(o, p, { get: () => e2[p], enumerable: !(r = a(e2, p)) || r.enumerable });
      return o;
    };
    var f = (o) => d2(t2({}, "__esModule", { value: true }), o);
    var g = {};
    module.exports = f(g);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/models/index.js
var require_models4 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/models/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (r, o, p, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let m of c(o))
          !d2.call(r, m) && m !== p && a(r, m, { get: () => o[m], enumerable: !(x = b(o, m)) || x.enumerable });
      return r;
    };
    var f = (r, o, p) => (t2(r, o, "default"), p && t2(p, o, "default"));
    var g = (r) => t2(a({}, "__esModule", { value: true }), r);
    var e2 = {};
    module.exports = g(e2);
    f(e2, require_menu_group(), module.exports);
    f(e2, require_menu_list(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/errors.js
var require_errors = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/errors.js"(exports, module) {
    var r = Object.defineProperty;
    var i = Object.getOwnPropertyDescriptor;
    var h2 = Object.getOwnPropertyNames;
    var u2 = Object.prototype.hasOwnProperty;
    var a = (o, t2) => {
      for (var n2 in t2)
        r(o, n2, { get: t2[n2], enumerable: true });
    };
    var g = (o, t2, n2, s) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let e2 of h2(t2))
          !u2.call(o, e2) && e2 !== n2 && r(o, e2, { get: () => t2[e2], enumerable: !(s = i(t2, e2)) || s.enumerable });
      return o;
    };
    var m = (o) => g(r({}, "__esModule", { value: true }), o);
    var c = {};
    a(c, { getItemDoesNotMatchAnyGroupErrorMessage: () => p });
    module.exports = m(c);
    function p(o) {
      return `All items in the GraphQL definition should belong to some group in strict mode. ${o} doesn't match any of the groups.`;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/initial-group.js
var require_initial_group = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/initial-group.js"(exports, module) {
    var s = Object.defineProperty;
    var o = Object.getOwnPropertyDescriptor;
    var u2 = Object.getOwnPropertyNames;
    var p = Object.prototype.hasOwnProperty;
    var c = (a, n2) => {
      for (var e2 in n2)
        s(a, e2, { get: n2[e2], enumerable: true });
    };
    var m = (a, n2, e2, l) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let i of u2(n2))
          !p.call(a, i) && i !== e2 && s(a, i, { get: () => n2[i], enumerable: !(l = o(n2, i)) || l.enumerable });
      return a;
    };
    var b = (a) => m(s({}, "__esModule", { value: true }), a);
    var y = {};
    c(y, { TYPE_GROUP_NAMES: () => t2, getInitialMenuGroup: () => T, getInitialTypeGroup: () => g, getTypeGroupId: () => r });
    module.exports = b(y);
    var t2 = { queries: { name: "Queries", labelTranslationKey: "graphql.queries" }, mutations: { name: "Mutations", labelTranslationKey: "graphql.mutations" }, subscriptions: { name: "Subscriptions", labelTranslationKey: "graphql.subscriptions" }, directives: { name: "Directives", labelTranslationKey: "graphql.directives" }, objects: { name: "Objects", labelTranslationKey: "graphql.objects" }, interfaces: { name: "Interfaces", labelTranslationKey: "graphql.interfaces" }, unions: { name: "Unions", labelTranslationKey: "graphql.unions" }, enums: { name: "Enums", labelTranslationKey: "graphql.enums" }, inputs: { name: "Inputs", labelTranslationKey: "graphql.inputs" }, scalars: { name: "Scalars", labelTranslationKey: "graphql.scalars" } };
    function T(a, n2) {
      return { id: a, name: n2 || a, typeGroups: /* @__PURE__ */ new Map() };
    }
    function g(a, n2, e2) {
      return { id: r(a, n2, e2), name: t2[n2].name, items: [], typeGroupId: n2, labelTranslationKey: t2[n2].labelTranslationKey };
    }
    function r(a, n2, e2) {
      return e2.groups.length ? `${a}/${n2}` : n2;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/insert-into-group.js
var require_insert_into_group = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/insert-into-group.js"(exports, module) {
    var p = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var g = Object.getOwnPropertyNames;
    var G = Object.prototype.hasOwnProperty;
    var a = (e2, t2) => {
      for (var o in t2)
        p(e2, o, { get: t2[o], enumerable: true });
    };
    var f = (e2, t2, o, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let r of g(t2))
          !G.call(e2, r) && r !== o && p(e2, r, { get: () => t2[r], enumerable: !(n2 = c(t2, r)) || n2.enumerable });
      return e2;
    };
    var l = (e2) => f(p({}, "__esModule", { value: true }), e2);
    var h2 = {};
    a(h2, { insertIntoGroup: () => y });
    module.exports = l(h2);
    var i = require_initial_group();
    function y(e2, t2, o, n2, r) {
      const s = u2(e2, o, (0, i.getInitialMenuGroup)(o));
      u2(s.typeGroups, n2, (0, i.getInitialTypeGroup)(o, n2, r)).items.push(t2);
    }
    function u2(e2, t2, o) {
      const n2 = e2.get(t2) || o;
      return e2.set(t2, n2), n2;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/insert-into-index.js
var require_insert_into_index = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/insert-into-index.js"(exports, module) {
    var p = Object.defineProperty;
    var I = Object.getOwnPropertyDescriptor;
    var d2 = Object.getOwnPropertyNames;
    var f = Object.prototype.hasOwnProperty;
    var m = (e2, t2) => {
      for (var n2 in t2)
        p(e2, n2, { get: t2[n2], enumerable: true });
    };
    var s = (e2, t2, n2, r) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let o of d2(t2))
          !f.call(e2, o) && o !== n2 && p(e2, o, { get: () => t2[o], enumerable: !(r = I(t2, o)) || r.enumerable });
      return e2;
    };
    var u2 = (e2) => s(p({}, "__esModule", { value: true }), e2);
    var c = {};
    m(c, { insertIntoIndex: () => x });
    module.exports = u2(c);
    var i = require_initial_group();
    function x(e2, t2, n2, r, o) {
      e2.set(t2, (0, i.getTypeGroupId)(n2, r, o));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/item-matches-group.js
var require_item_matches_group = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/item-matches-group.js"(exports, module) {
    var u2 = Object.defineProperty;
    var i = Object.getOwnPropertyDescriptor;
    var d2 = Object.getOwnPropertyNames;
    var l = Object.prototype.hasOwnProperty;
    var m = (t2, e2) => {
      for (var c in e2)
        u2(t2, c, { get: e2[c], enumerable: true });
    };
    var h2 = (t2, e2, c, s) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let n2 of d2(e2))
          !l.call(t2, n2) && n2 !== c && u2(t2, n2, { get: () => e2[n2], enumerable: !(s = i(e2, n2)) || s.enumerable });
      return t2;
    };
    var o = (t2) => h2(u2({}, "__esModule", { value: true }), t2);
    var M = {};
    m(M, { directiveMatchesGroup: () => y, fieldMatchesGroup: () => x, typeMatchesGroup: () => p });
    module.exports = o(M);
    function p(t2, e2) {
      return a(t2.name, e2.types, e2.items);
    }
    function x(t2, e2, c) {
      const s = c[e2];
      return a(t2.name, s, c.items);
    }
    function y(t2, e2) {
      return a(t2.name, e2.directives, e2.items);
    }
    function a(t2, e2, c) {
      const s = r(t2, e2.includeByName) || r(t2, c.includeByName), n2 = r(t2, e2.excludeByName) || r(t2, c.excludeByName);
      return s && !n2;
    }
    function r(t2, e2) {
      const c = e2.names.has(t2), s = e2.regexps.some((n2) => n2.test(t2));
      return c || s;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/group-and-index-types.js
var require_group_and_index_types = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/group-and-index-types.js"(exports, module) {
    var u2 = Object.defineProperty;
    var d2 = Object.getOwnPropertyDescriptor;
    var E = Object.getOwnPropertyNames;
    var T = Object.prototype.hasOwnProperty;
    var g = (o, r) => {
      for (var t2 in r)
        u2(o, t2, { get: r[t2], enumerable: true });
    };
    var h2 = (o, r, t2, p) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let e2 of E(r))
          !T.call(o, e2) && e2 !== t2 && u2(o, e2, { get: () => r[e2], enumerable: !(p = d2(r, e2)) || p.enumerable });
      return o;
    };
    var l = (o) => h2(u2({}, "__esModule", { value: true }), o);
    var y = {};
    g(y, { groupAndIndexTypes: () => M });
    module.exports = l(y);
    var a = require_models4();
    var i = require_errors();
    var c = require_insert_into_group();
    var f = require_insert_into_index();
    var G = require_item_matches_group();
    var n2 = require_schema();
    function M(o, r, t2, p) {
      Object.values(t2.getTypeMap()).filter((e2) => !(0, n2.isExcludedType)(e2, t2)).forEach((e2) => {
        const m = x(e2, p), s = (0, n2.getTypeGroup)(e2);
        m.forEach((I) => {
          (0, c.insertIntoGroup)(o, e2.name, I, s, p);
        }), (0, f.insertIntoIndex)(r, e2.name, m[0], s, p);
      });
    }
    function x(o, r) {
      const t2 = r.groups.filter((p) => (0, G.typeMatchesGroup)(o, p)).map((p) => p.name);
      if (r.requireExactGroups && !t2.length)
        throw new Error((0, i.getItemDoesNotMatchAnyGroupErrorMessage)(`Type ${o.name}`));
      return t2.length ? t2 : [a.DEFAULT_MENU_GROUP_ID];
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/group-directives.js
var require_group_directives = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/group-directives.js"(exports, module) {
    var i = Object.defineProperty;
    var G = Object.getOwnPropertyDescriptor;
    var f = Object.getOwnPropertyNames;
    var h2 = Object.prototype.hasOwnProperty;
    var D = (o, r) => {
      for (var e2 in r)
        i(o, e2, { get: r[e2], enumerable: true });
    };
    var E = (o, r, e2, t2) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let p of f(r))
          !h2.call(o, p) && p !== e2 && i(o, p, { get: () => r[p], enumerable: !(t2 = G(r, p)) || t2.enumerable });
      return o;
    };
    var g = (o) => E(i({}, "__esModule", { value: true }), o);
    var v = {};
    D(v, { groupDirectives: () => I });
    module.exports = g(v);
    var u2 = require_models4();
    var m = require_errors();
    var n2 = require_insert_into_group();
    var s = require_item_matches_group();
    var c = require_schema();
    function I(o, r, e2) {
      r.getDirectives().forEach((t2) => {
        M(t2, e2).forEach((a) => {
          (0, n2.insertIntoGroup)(o, t2.name, a, c.TypeGroup.directives, e2);
        });
      });
    }
    function M(o, r) {
      const e2 = r.groups.filter((t2) => (0, s.directiveMatchesGroup)(o, t2)).map((t2) => t2.name);
      if (r.requireExactGroups && !e2.length)
        throw new Error((0, m.getItemDoesNotMatchAnyGroupErrorMessage)(`Directive ${o.name}`));
      return e2.length ? e2 : [u2.DEFAULT_MENU_GROUP_ID];
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/group-fields.js
var require_group_fields = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/group-fields.js"(exports, module) {
    var s = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var G = Object.getOwnPropertyNames;
    var f = Object.prototype.hasOwnProperty;
    var h2 = (o, r) => {
      for (var t2 in r)
        s(o, t2, { get: r[t2], enumerable: true });
    };
    var E = (o, r, t2, n2) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let e2 of G(r))
          !f.call(o, e2) && e2 !== t2 && s(o, e2, { get: () => r[e2], enumerable: !(n2 = c(r, e2)) || n2.enumerable });
      return o;
    };
    var g = (o) => E(s({}, "__esModule", { value: true }), o);
    var M = {};
    h2(M, { groupFields: () => l });
    module.exports = g(M);
    var u2 = require_models4();
    var p = require_errors();
    var m = require_insert_into_group();
    var a = require_item_matches_group();
    function l(o, r, t2, n2) {
      Object.values(r.getFields()).forEach((e2) => {
        I(e2, t2, n2).forEach((i) => {
          (0, m.insertIntoGroup)(o, e2.name, i, t2, n2);
        });
      });
    }
    function I(o, r, t2) {
      const n2 = t2.groups.filter((e2) => (0, a.fieldMatchesGroup)(o, r, e2)).map((e2) => e2.name);
      if (t2.requireExactGroups && !n2.length)
        throw new Error((0, p.getItemDoesNotMatchAnyGroupErrorMessage)(`Operation ${o.name}`));
      return n2.length ? n2 : [u2.DEFAULT_MENU_GROUP_ID];
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/group-and-index-schema.js
var require_group_and_index_schema = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/group-and-index-schema.js"(exports, module) {
    var u2 = Object.defineProperty;
    var T = Object.getOwnPropertyDescriptor;
    var f = Object.getOwnPropertyNames;
    var G = Object.prototype.hasOwnProperty;
    var M = (t2, r) => {
      for (var o in r)
        u2(t2, o, { get: r[o], enumerable: true });
    };
    var a = (t2, r, o, e2) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let p of f(r))
          !G.call(t2, p) && p !== o && u2(t2, p, { get: () => r[p], enumerable: !(e2 = T(r, p)) || e2.enumerable });
      return t2;
    };
    var g = (t2) => a(u2({}, "__esModule", { value: true }), t2);
    var b = {};
    M(b, { groupAndIndexSchema: () => x });
    module.exports = g(b);
    var n2 = require_models4();
    var y = require_group_and_index_types();
    var d2 = require_group_directives();
    var i = require_group_fields();
    var I = require_initial_group();
    var s = require_schema();
    function x(t2, r) {
      const o = /* @__PURE__ */ new Map([[n2.DEFAULT_MENU_GROUP_ID, (0, I.getInitialMenuGroup)(n2.DEFAULT_MENU_GROUP_ID, r.otherItemsGroupName)]]), e2 = /* @__PURE__ */ new Map();
      (0, y.groupAndIndexTypes)(o, e2, t2, r), (0, d2.groupDirectives)(o, t2, r);
      const p = t2.getQueryType(), m = t2.getMutationType(), c = t2.getSubscriptionType();
      return p && (0, i.groupFields)(o, p, s.TypeGroup.queries, r), m && (0, i.groupFields)(o, m, s.TypeGroup.mutations, r), c && (0, i.groupFields)(o, c, s.TypeGroup.subscriptions, r), { groups: o, typeGroupIndex: e2 };
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/index.js
var require_grouping = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var p = (r, o, f, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of c(o))
          !d2.call(r, e2) && e2 !== f && a(r, e2, { get: () => o[e2], enumerable: !(x = b(o, e2)) || x.enumerable });
      return r;
    };
    var t2 = (r, o, f) => (p(r, o, "default"), f && p(f, o, "default"));
    var g = (r) => p(a({}, "__esModule", { value: true }), r);
    var m = {};
    module.exports = g(m);
    t2(m, require_group_and_index_schema(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/groups-to-sidebar-items.js
var require_groups_to_sidebar_items = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/grouping/groups-to-sidebar-items.js"(exports, module) {
    var r = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var m = Object.prototype.hasOwnProperty;
    var T = (e2, s) => {
      for (var n2 in s)
        r(e2, n2, { get: s[n2], enumerable: true });
    };
    var i = (e2, s, n2, t2) => {
      if (s && typeof s == "object" || typeof s == "function")
        for (let l of b(s))
          !m.call(e2, l) && l !== n2 && r(e2, l, { get: () => s[l], enumerable: !(t2 = u2(s, l)) || t2.enumerable });
      return e2;
    };
    var y = (e2) => i(r({}, "__esModule", { value: true }), e2);
    var f = {};
    T(f, { groupsToSidebarItems: () => h2 });
    module.exports = y(f);
    var c = require_models4();
    var h2 = (e2, s = "item", n2) => e2.length === 1 && e2[0].id === c.DEFAULT_MENU_GROUP_ID ? e2[0].typeGroups.map((t2) => d2(t2, `${n2}`, "", s)) : e2.map((t2) => I(t2, s, n2));
    var I = (e2, s, n2) => {
      const l = `${n2 ? `${n2}` : ""}${s === "none" ? "" : `${e2.id}/`}`, o = s === "none" ? `#${e2.id}` : "";
      return { type: "group", label: e2.name, slug: l, routeSlug: l, link: s === "none" ? l + o : void 0, items: e2.typeGroups.map(($) => d2($, l, e2.id, s)) };
    };
    var d2 = (e2, s, n2, t2) => {
      const l = t2 === "none" ? s : `${s}${e2.typeGroupId}/`, o = t2 === "none" ? `#${n2 ? `${n2}/` : ""}${e2.typeGroupId}` : "";
      return { type: "group", label: e2.name, labelTranslationKey: e2.labelTranslationKey, slug: l, routeSlug: l, link: t2 !== "item" ? l + o : void 0, items: e2.items.map(($) => S($, l, `${n2 ? `${n2}/` : ""}${e2.typeGroupId}`, t2)) };
    };
    var S = (e2, s, n2, t2) => {
      const l = t2 === "item" ? `${s}${e2}/` : s, o = t2 === "none" ? `#${n2}/${e2}` : t2 === "section" ? `#${e2}` : "";
      return { type: "link", label: e2, link: l + o, slug: l, routeSlug: l, items: [] };
    };
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/utils/is-defined.js
var require_is_defined = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/utils/is-defined.js"(exports, module) {
    var t2 = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var l = Object.getOwnPropertyNames;
    var o = Object.prototype.hasOwnProperty;
    var c = (n2, e2) => {
      for (var r in e2)
        t2(n2, r, { get: e2[r], enumerable: true });
    };
    var d2 = (n2, e2, r, u2) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let i of l(e2))
          !o.call(n2, i) && i !== r && t2(n2, i, { get: () => e2[i], enumerable: !(u2 = f(e2, i)) || u2.enumerable });
      return n2;
    };
    var p = (n2) => d2(t2({}, "__esModule", { value: true }), n2);
    var x = {};
    c(x, { isDefined: () => s });
    module.exports = p(x);
    function s(n2) {
      return n2 != null;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/order-menu-groups.js
var require_order_menu_groups = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/order-menu-groups.js"(exports, module) {
    var o = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var s = Object.getOwnPropertyNames;
    var a = Object.prototype.hasOwnProperty;
    var G = (e2, r) => {
      for (var t2 in r)
        o(e2, t2, { get: r[t2], enumerable: true });
    };
    var c = (e2, r, t2, i) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let n2 of s(r))
          !a.call(e2, n2) && n2 !== t2 && o(e2, n2, { get: () => r[n2], enumerable: !(i = f(r, n2)) || i.enumerable });
      return e2;
    };
    var y = (e2) => c(o({}, "__esModule", { value: true }), e2);
    var M = {};
    G(M, { orderMenuGroups: () => d2 });
    module.exports = y(M);
    var u2 = require_models4();
    var m = require_initial_group();
    var p = require_is_defined();
    function d2(e2, r) {
      return r.groups.map((t2) => e2.get(t2.name)).concat(e2.get(u2.DEFAULT_MENU_GROUP_ID)).filter(p.isDefined).filter(_).map((t2) => ({ id: t2.id, name: t2.name, typeGroups: E(t2.typeGroups) }));
    }
    function _(e2) {
      return e2.typeGroups.size > 0;
    }
    function E(e2) {
      return Object.keys(m.TYPE_GROUP_NAMES).map((r) => e2.get(r)).filter(p.isDefined);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/menu.store.js
var require_menu_store = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/menu.store.js"(exports, module) {
    var i = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var h2 = Object.prototype.hasOwnProperty;
    var I = (r, e2) => {
      for (var o in e2)
        i(r, o, { get: e2[o], enumerable: true });
    };
    var y = (r, e2, o, u2) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let t2 of c(e2))
          !h2.call(r, t2) && t2 !== o && i(r, t2, { get: () => e2[t2], enumerable: !(u2 = f(e2, t2)) || u2.enumerable });
      return r;
    };
    var G = (r) => y(i({}, "__esModule", { value: true }), r);
    var x = {};
    I(x, { MenuStore: () => v });
    module.exports = G(x);
    var s = require_config();
    var p = require_validate_menu_config();
    var a = require_grouping();
    var m = require_groups_to_sidebar_items();
    var d2 = require_order_menu_groups();
    var v = class {
      constructor(e2, o) {
        Object.defineProperty(this, "groups", { enumerable: true, configurable: true, writable: true, value: void 0 }), Object.defineProperty(this, "typeGroupIndex", { enumerable: true, configurable: true, writable: true, value: void 0 }), Object.defineProperty(this, "sidebarItems", { enumerable: true, configurable: true, writable: true, value: void 0 });
        const { menu: u2, baseUrlPath: t2 = "/" } = o || {}, g = "item";
        (0, p.validateMenuConfig)(e2, u2);
        const n2 = (0, s.indexMenuGroupingConfig)(u2), { groups: b, typeGroupIndex: l } = (0, a.groupAndIndexSchema)(e2, n2);
        this.groups = (0, d2.orderMenuGroups)(b, n2), this.sidebarItems = (0, m.groupsToSidebarItems)(this.groups, g, t2), this.typeGroupIndex = l;
      }
      getGroups() {
        return this.groups;
      }
      getGroupIdByTypeName(e2) {
        return this.typeGroupIndex.get(e2);
      }
      getSidebarItems() {
        return this.sidebarItems;
      }
    };
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/store/index.js
var require_store = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/store/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (r, o, p, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let m of c(o))
          !d2.call(r, m) && m !== p && a(r, m, { get: () => o[m], enumerable: !(x = b(o, m)) || x.enumerable });
      return r;
    };
    var f = (r, o, p) => (t2(r, o, "default"), p && t2(p, o, "default"));
    var g = (r) => t2(a({}, "__esModule", { value: true }), r);
    var e2 = {};
    module.exports = g(e2);
    f(e2, require_menu_store(), module.exports);
    f(e2, require_menu_config(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/menu-store.context.js
var require_menu_store_context = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/menu-store.context.js"(exports, module) {
    var n2 = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var u2 = Object.prototype.hasOwnProperty;
    var f = (o, e2) => {
      for (var r in e2)
        n2(o, r, { get: e2[r], enumerable: true });
    };
    var m = (o, e2, r, d2) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let t2 of p(e2))
          !u2.call(o, t2) && t2 !== r && n2(o, t2, { get: () => e2[t2], enumerable: !(d2 = c(e2, t2)) || d2.enumerable });
      return o;
    };
    var s = (o) => m(n2({}, "__esModule", { value: true }), o);
    var C = {};
    f(C, { MenuStoreContext: () => x, MenuStoreProvider: () => v });
    module.exports = s(C);
    var i = __require("react");
    var x = (0, i.createContext)(void 0);
    var v = x.Provider;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/menu/index.js
var require_menu = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/menu/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, r, p, x) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let m of c(r))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => r[m], enumerable: !(x = b(r, m)) || x.enumerable });
      return f;
    };
    var e2 = (f, r, p) => (t2(f, r, "default"), p && t2(p, r, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    e2(o, require_store(), module.exports);
    e2(o, require_menu_store_context(), module.exports);
    e2(o, require_models4(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/theme/globalStyles.js
var require_globalStyles = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/theme/globalStyles.js"(exports, module) {
    var s = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var i = Object.getOwnPropertyNames;
    var p = Object.prototype.hasOwnProperty;
    var r = (t2, o) => {
      for (var a in o)
        s(t2, a, {
          get: o[a],
          enumerable: true
        });
    };
    var d2 = (t2, o, a, l) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of i(o))
          !p.call(t2, e2) && e2 !== a && s(t2, e2, {
            get: () => o[e2],
            enumerable: !(l = c(o, e2)) || l.enumerable
          });
      return t2;
    };
    var b = (t2) => d2(s({}, "__esModule", {
      value: true
    }), t2);
    var x = {};
    r(x, {
      GlobalStyles: () => g
    });
    module.exports = b(x);
    var n2 = (init_styled_components_esm(), __toCommonJS(styled_components_esm_exports));
    var g = n2.createGlobalStyle`
  /*
  * Static classnames that can be used to override styles for components:
  * download-specification-button, next-section-button, button-base
  */
:root {
  /* === graphql docs specific === */
  /*
  * Logo
  */
  --logo-max-height: 285px;
  --logo-max-width: 285px;
  --logo-padding: 2px;
}
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/theme/index.js
var require_theme = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/theme/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var p = (r, o, f, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of c(o))
          !d2.call(r, e2) && e2 !== f && a(r, e2, { get: () => o[e2], enumerable: !(x = b(o, e2)) || x.enumerable });
      return r;
    };
    var t2 = (r, o, f) => (p(r, o, "default"), f && p(f, o, "default"));
    var g = (r) => p(a({}, "__esModule", { value: true }), r);
    var m = {};
    module.exports = g(m);
    t2(m, require_globalStyles(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/app/components/AppProviders.js
var require_AppProviders = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/app/components/AppProviders.js"(exports, module) {
    var E = Object.create;
    var n2 = Object.defineProperty;
    var d2 = Object.getOwnPropertyDescriptor;
    var P = Object.getOwnPropertyNames;
    var b = Object.getPrototypeOf;
    var y = Object.prototype.hasOwnProperty;
    var A = (e2, r) => {
      for (var o in r)
        n2(e2, o, { get: r[o], enumerable: true });
    };
    var i = (e2, r, o, m) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let l of P(r))
          !y.call(e2, l) && l !== o && n2(e2, l, { get: () => r[l], enumerable: !(m = d2(r, l)) || m.enumerable });
      return e2;
    };
    var M = (e2, r, o) => (o = e2 != null ? E(b(e2)) : {}, i(r || !e2 || !e2.__esModule ? n2(o, "default", { value: e2, enumerable: true }) : o, e2));
    var w = (e2) => i(n2({}, "__esModule", { value: true }), e2);
    var j = {};
    A(j, { AppProviders: () => G });
    module.exports = w(j);
    var t2 = M(__require("react"));
    var u2 = (init_src(), __toCommonJS(src_exports));
    var a = require_app_store();
    var f = require_app_store_context();
    var S = require_settings2();
    var p = require_menu();
    var v = require_theme();
    function G({ schema: e2, settings: r, children: o, withCommonStyles: m }) {
      const l = (0, t2.useMemo)(() => e2 && new a.AppStore(e2), [e2]), c = (0, t2.useMemo)(() => e2 && new p.MenuStore(e2, Object.assign({}, r)), [e2, r]);
      return t2.default.createElement(S.SettingsProvider, { value: r }, t2.default.createElement(f.AppStoreProvider, { value: l }, m && t2.default.createElement(u2.GlobalStyle, null), t2.default.createElement(v.GlobalStyles, null), t2.default.createElement(p.MenuStoreProvider, { value: c }, o)));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/app/components/MiddleSection.js
var require_MiddleSection = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/app/components/MiddleSection.js"(exports, module) {
    var c = Object.create;
    var r = Object.defineProperty;
    var l = Object.getOwnPropertyDescriptor;
    var g = Object.getOwnPropertyNames;
    var s = Object.getPrototypeOf;
    var h2 = Object.prototype.hasOwnProperty;
    var m = (a, i) => {
      for (var t2 in i)
        r(a, t2, {
          get: i[t2],
          enumerable: true
        });
    };
    var o = (a, i, t2, d2) => {
      if (i && typeof i == "object" || typeof i == "function")
        for (let n2 of g(i))
          !h2.call(a, n2) && n2 !== t2 && r(a, n2, {
            get: () => i[n2],
            enumerable: !(d2 = l(i, n2)) || d2.enumerable
          });
      return a;
    };
    var v = (a, i, t2) => (t2 = a != null ? c(s(a)) : {}, o(i || !a || !a.__esModule ? r(t2, "default", {
      value: a,
      enumerable: true
    }) : t2, a));
    var f = (a) => o(r({}, "__esModule", {
      value: true
    }), a);
    var z = {};
    m(z, {
      MiddleSection: () => w
    });
    module.exports = f(z);
    var p = v((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var e2 = (init_src(), __toCommonJS(src_exports));
    var w = p.default.section`
  padding-top: var(--spacing-vertical);
  padding-bottom: var(--spacing-vertical);
  padding-left: var(--spacing-horizontal);
  padding-right: var(--spacing-horizontal);
  width: 100%;

  @media screen and (min-width: ${e2.breakpoints.large}) {
    width: calc(100% - var(--panel-samples-width));
    padding-left: calc(var(--spacing-horizontal) * 2);
  }
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/app/components/RightSection.js
var require_RightSection = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/app/components/RightSection.js"(exports, module) {
    var d2 = Object.create;
    var n2 = Object.defineProperty;
    var e2 = Object.getOwnPropertyDescriptor;
    var l = Object.getOwnPropertyNames;
    var s = Object.getPrototypeOf;
    var v = Object.prototype.hasOwnProperty;
    var h2 = (a, i) => {
      for (var t2 in i)
        n2(a, t2, {
          get: i[t2],
          enumerable: true
        });
    };
    var o = (a, i, t2, p) => {
      if (i && typeof i == "object" || typeof i == "function")
        for (let r of l(i))
          !v.call(a, r) && r !== t2 && n2(a, r, {
            get: () => i[r],
            enumerable: !(p = e2(i, r)) || p.enumerable
          });
      return a;
    };
    var m = (a, i, t2) => (t2 = a != null ? d2(s(a)) : {}, o(i || !a || !a.__esModule ? n2(t2, "default", {
      value: a,
      enumerable: true
    }) : t2, a));
    var w = (a) => o(n2({}, "__esModule", {
      value: true
    }), a);
    var z = {};
    h2(z, {
      RightSection: () => f
    });
    module.exports = w(z);
    var c = m((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var g = (init_src(), __toCommonJS(src_exports));
    var f = c.default.section`
  padding-top: var(--spacing-vertical);
  padding-bottom: var(--spacing-vertical);
  padding-left: var(--spacing-horizontal);
  padding-right: var(--spacing-horizontal);
  width: 100%;
  margin-top: var(--spacing-vertical);

  @media screen and (min-width: ${g.breakpoints.large}) {
    width: var(--panel-samples-width);
    padding-right: calc(var(--spacing-horizontal) * 2);
    margin-top: calc(var(--spacing-vertical) * 2);
  }
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/app/components/index.js
var require_components = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/app/components/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, r, p, x) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let m of c(r))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => r[m], enumerable: !(x = b(r, m)) || x.enumerable });
      return f;
    };
    var e2 = (f, r, p) => (t2(f, r, "default"), p && t2(p, r, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    e2(o, require_AppProviders(), module.exports);
    e2(o, require_MiddleSection(), module.exports);
    e2(o, require_RightSection(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/app/index.js
var require_app = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/app/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_app_store(), module.exports);
    r(o, require_app_store_context(), module.exports);
    r(o, require_models2(), module.exports);
    r(o, require_components(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/NullableType.js
var require_NullableType = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/NullableType.js"(exports, module) {
    var u2 = Object.create;
    var n2 = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var f = Object.getPrototypeOf;
    var i = Object.prototype.hasOwnProperty;
    var b = (e2, t2) => {
      for (var r in t2)
        n2(e2, r, { get: t2[r], enumerable: true });
    };
    var o = (e2, t2, r, m) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let l of p(t2))
          !i.call(e2, l) && l !== r && n2(e2, l, { get: () => t2[l], enumerable: !(m = c(t2, l)) || m.enumerable });
      return e2;
    };
    var g = (e2, t2, r) => (r = e2 != null ? u2(f(e2)) : {}, o(t2 || !e2 || !e2.__esModule ? n2(r, "default", { value: e2, enumerable: true }) : r, e2));
    var x = (e2) => o(n2({}, "__esModule", { value: true }), e2);
    var E = {};
    b(E, { NullableType: () => y });
    module.exports = x(E);
    var a = g(__require("react"));
    function y({ isNonNull: e2, children: t2 }) {
      return a.default.createElement(a.default.Fragment, null, t2, e2 ? "!" : "");
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/common/HiddenNavigationAnchor.js
var require_HiddenNavigationAnchor = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/common/HiddenNavigationAnchor.js"(exports, module) {
    var d2 = Object.create;
    var i = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var s = Object.getOwnPropertyNames;
    var y = Object.getPrototypeOf;
    var u2 = Object.prototype.hasOwnProperty;
    var O = (t2, e2) => {
      for (var r in e2)
        i(t2, r, {
          get: e2[r],
          enumerable: true
        });
    };
    var c = (t2, e2, r, n2) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let o of s(e2))
          !u2.call(t2, o) && o !== r && i(t2, o, {
            get: () => e2[o],
            enumerable: !(n2 = f(e2, o)) || n2.enumerable
          });
      return t2;
    };
    var l = (t2, e2, r) => (r = t2 != null ? d2(y(t2)) : {}, c(e2 || !t2 || !t2.__esModule ? i(r, "default", {
      value: t2,
      enumerable: true
    }) : r, t2));
    var b = (t2) => c(i({}, "__esModule", {
      value: true
    }), t2);
    var j = {};
    O(j, {
      HiddenNavigationAnchor: () => g
    });
    module.exports = b(j);
    var a = l(__require("react"));
    var p = l((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var h2 = function(t2, e2) {
      var r = {};
      for (var n2 in t2)
        Object.prototype.hasOwnProperty.call(t2, n2) && e2.indexOf(n2) < 0 && (r[n2] = t2[n2]);
      if (t2 != null && typeof Object.getOwnPropertySymbols == "function")
        for (var o = 0, n2 = Object.getOwnPropertySymbols(t2); o < n2.length; o++)
          e2.indexOf(n2[o]) < 0 && Object.prototype.propertyIsEnumerable.call(t2, n2[o]) && (r[n2[o]] = t2[n2[o]]);
      return r;
    };
    var m = p.default.span`
  display: inline;
  position: relative;
`;
    var v = p.default.a`
  top: var(--navigation-anchor-offset, -30px);
  position: absolute;
`;
    function g(t2) {
      var {
        id: e2
      } = t2, r = h2(t2, ["id"]);
      return a.default.createElement(m, null, a.default.createElement(v, Object.assign({
        id: e2,
        "aria-hidden": "true"
      }, r)));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/common/SplittedWbrText.js
var require_SplittedWbrText = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/common/SplittedWbrText.js"(exports, module) {
    var o = Object.create;
    var a = Object.defineProperty;
    var p = Object.getOwnPropertyDescriptor;
    var u2 = Object.getOwnPropertyNames;
    var s = Object.getPrototypeOf;
    var g = Object.prototype.hasOwnProperty;
    var i = (t2, e2) => {
      for (var r in e2)
        a(t2, r, { get: e2[r], enumerable: true });
    };
    var c = (t2, e2, r, m) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let n2 of u2(e2))
          !g.call(t2, n2) && n2 !== r && a(t2, n2, { get: () => e2[n2], enumerable: !(m = p(e2, n2)) || m.enumerable });
      return t2;
    };
    var E = (t2, e2, r) => (r = t2 != null ? o(s(t2)) : {}, c(e2 || !t2 || !t2.__esModule ? a(r, "default", { value: t2, enumerable: true }) : r, t2));
    var b = (t2) => c(a({}, "__esModule", { value: true }), t2);
    var w = {};
    i(w, { SplittedWbrText: () => f });
    module.exports = b(w);
    var l = E(__require("react"));
    function f(t2) {
      const { text: e2 } = t2, r = e2.split(/(?=[A-Z])/);
      return l.default.createElement(l.default.Fragment, null, r.map((m, n2) => l.default.createElement(l.default.Fragment, { key: n2 }, m, n2 < r.length - 1 ? l.default.createElement("wbr", null) : null)));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/styled.js
var require_styled2 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/styled.js"(exports, module) {
    var e2 = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var l = Object.getOwnPropertyNames;
    var v = Object.prototype.hasOwnProperty;
    var d2 = (r, o) => {
      for (var t2 in o)
        e2(r, t2, {
          get: o[t2],
          enumerable: true
        });
    };
    var k = (r, o, t2, n2) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let i of l(o))
          !v.call(r, i) && i !== t2 && e2(r, i, {
            get: () => o[i],
            enumerable: !(n2 = c(o, i)) || n2.enumerable
          });
      return r;
    };
    var h2 = (r) => k(e2({}, "__esModule", {
      value: true
    }), r);
    var m = {};
    d2(m, {
      anchorCSS: () => s
    });
    module.exports = h2(m);
    var a = (init_styled_components_esm(), __toCommonJS(styled_components_esm_exports));
    var s = a.css`
  text-decoration: var(--link-decoration);
  color: var(--link-color-primary);
  font-weight: var(--link-font-weight);

  &:visited {
    color: var(--link-color-visited);
    text-decoration: var(--link-visited-decoration);
  }

  &:hover {
    color: var(--link-color-primary-hover);
    text-decoration: var(--link-decoration-hover);
  }
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/common/Anchor.js
var require_Anchor = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/common/Anchor.js"(exports, module) {
    var f = Object.create;
    var c = Object.defineProperty;
    var h2 = Object.getOwnPropertyDescriptor;
    var i = Object.getOwnPropertyNames;
    var s = Object.getPrototypeOf;
    var S = Object.prototype.hasOwnProperty;
    var d2 = (o, r) => {
      for (var t2 in r)
        c(o, t2, {
          get: r[t2],
          enumerable: true
        });
    };
    var p = (o, r, t2, n2) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let m of i(r))
          !S.call(o, m) && m !== t2 && c(o, m, {
            get: () => r[m],
            enumerable: !(n2 = h2(r, m)) || n2.enumerable
          });
      return o;
    };
    var l = (o, r, t2) => (t2 = o != null ? f(s(o)) : {}, p(r || !o || !o.__esModule ? c(t2, "default", {
      value: o,
      enumerable: true
    }) : t2, o));
    var x = (o) => p(c({}, "__esModule", {
      value: true
    }), o);
    var A = {};
    d2(A, {
      Anchor: () => y
    });
    module.exports = x(A);
    var a = l((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var e2 = require_styled2();
    var y = a.default.a`
  ${e2.anchorCSS}
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/common/index.js
var require_common = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/common/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, r, p, x) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let m of c(r))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => r[m], enumerable: !(x = b(r, m)) || x.enumerable });
      return f;
    };
    var e2 = (f, r, p) => (t2(f, r, "default"), p && t2(p, r, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    e2(o, require_HiddenNavigationAnchor(), module.exports);
    e2(o, require_SplittedWbrText(), module.exports);
    e2(o, require_Anchor(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/icons/Shelf.js
var require_Shelf = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/icons/Shelf.js"(exports, module) {
    var c = Object.create;
    var i = Object.defineProperty;
    var m = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var f = Object.getPrototypeOf;
    var g = Object.prototype.hasOwnProperty;
    var h2 = (e2, t2) => {
      for (var o in t2)
        i(e2, o, {
          get: t2[o],
          enumerable: true
        });
    };
    var s = (e2, t2, o, a) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let r of p(t2))
          !g.call(e2, r) && r !== o && i(e2, r, {
            get: () => t2[r],
            enumerable: !(a = m(t2, r)) || a.enumerable
          });
      return e2;
    };
    var l = (e2, t2, o) => (o = e2 != null ? c(f(e2)) : {}, s(t2 || !e2 || !e2.__esModule ? i(o, "default", {
      value: e2,
      enumerable: true
    }) : o, e2));
    var w = (e2) => s(i({}, "__esModule", {
      value: true
    }), e2);
    var u2 = {};
    h2(u2, {
      ShelfIcon: () => $
    });
    module.exports = w(u2);
    var n2 = l(__require("react"));
    var d2 = l((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var C = {
      right: "0deg",
      down: "90deg",
      left: "180deg",
      up: "270deg"
    };
    var x = ({
      className: e2,
      style: t2
    }) => n2.default.createElement("svg", {
      className: e2,
      style: t2,
      viewBox: "0 0 4.25 7",
      xmlns: "http://www.w3.org/2000/svg",
      "aria-hidden": "true"
    }, n2.default.createElement("path", {
      d: "M0.205025 1.19467L3.00503 3.99467C3.27839 4.26804 3.72161 4.26804 3.99497 3.99467C4.26834 3.7213 4.26834 3.27809 3.99497 3.00472L1.19497 0.20472C0.921608 -0.0686469 0.478392 -0.0686469 0.205025 0.20472C-0.0683418 0.478087 -0.0683417 0.921303 0.205025 1.19467Z"
    }), n2.default.createElement("path", {
      d: "M3.00503 3.00501L0.205025 5.80501C-0.0683417 6.07838 -0.0683417 6.5216 0.205025 6.79496C0.478392 7.06833 0.921608 7.06833 1.19497 6.79496L3.99497 3.99496C4.26834 3.7216 4.26834 3.27838 3.99497 3.00501C3.72161 2.73165 3.27839 2.73165 3.00503 3.00501Z"
    }));
    var $ = (0, d2.default)(x)`
  height: ${(e2) => e2.size || "9px"};
  width: ${(e2) => e2.size || "9px"};
  vertical-align: middle;
  float: ${(e2) => e2.float || ""};
  transition: transform 0.2s ease-out;
  transform: rotateZ(${(e2) => C[e2.direction || "down"]});
  fill: ${({
      color: e2
    }) => e2};
  cursor: pointer;
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/icons/index.js
var require_icons = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/icons/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var p = (r, o, f, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of c(o))
          !d2.call(r, e2) && e2 !== f && a(r, e2, { get: () => o[e2], enumerable: !(x = b(o, e2)) || x.enumerable });
      return r;
    };
    var t2 = (r, o, f) => (p(r, o, "default"), f && p(f, o, "default"));
    var g = (r) => p(a({}, "__esModule", { value: true }), r);
    var m = {};
    module.exports = g(m);
    t2(m, require_Shelf(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/license/helpers/has-sub-host.js
var require_has_sub_host = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/license/helpers/has-sub-host.js"(exports, module) {
    var f = Object.defineProperty;
    var s = Object.getOwnPropertyDescriptor;
    var l = Object.getOwnPropertyNames;
    var a = Object.prototype.hasOwnProperty;
    var u2 = (r, t2) => {
      for (var e2 in t2)
        f(r, e2, { get: t2[e2], enumerable: true });
    };
    var d2 = (r, t2, e2, i) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let n2 of l(t2))
          !a.call(r, n2) && n2 !== e2 && f(r, n2, { get: () => t2[n2], enumerable: !(i = s(t2, n2)) || i.enumerable });
      return r;
    };
    var p = (r) => d2(f({}, "__esModule", { value: true }), r);
    var c = {};
    u2(c, { hasSubHost: () => x });
    module.exports = p(c);
    function x(r, t2) {
      if (r.indexOf("*") !== r.lastIndexOf("*"))
        return false;
      const [e2, i] = r.split("*");
      return i ? t2.startsWith(e2) && t2.endsWith(i) && i.split(".").length > 2 : false;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/license/helpers/host.js
var require_host = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/license/helpers/host.js"(exports, module) {
    var n2 = Object.defineProperty;
    var l = Object.getOwnPropertyDescriptor;
    var s = Object.getOwnPropertyNames;
    var a = Object.prototype.hasOwnProperty;
    var d2 = (t2, r) => {
      for (var u2 in r)
        n2(t2, u2, { get: r[u2], enumerable: true });
    };
    var H = (t2, r, u2, i) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let e2 of s(r))
          !a.call(t2, e2) && e2 !== u2 && n2(t2, e2, { get: () => r[e2], enumerable: !(i = l(r, e2)) || i.enumerable });
      return t2;
    };
    var p = (t2) => H(n2({}, "__esModule", { value: true }), t2);
    var y = {};
    d2(y, { isHostAllowed: () => x, isLocalhost: () => c });
    module.exports = p(y);
    var f = require_has_sub_host();
    function A(t2, r) {
      return t2 === r || r.endsWith(`.${t2}`);
    }
    function o(t2, r) {
      return t2.some((u2) => A(u2, r));
    }
    function c(t2) {
      return o(["localhost", "127.0.0.1"], t2);
    }
    function x(t2, r) {
      return r === true ? true : Array.isArray(r) ? c(t2) || o(r, t2) ? true : r.some((u2) => (0, f.hasSubHost)(u2, t2)) : false;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/license/helpers/index.js
var require_helpers2 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/license/helpers/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (r, o, p, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let m of c(o))
          !d2.call(r, m) && m !== p && a(r, m, { get: () => o[m], enumerable: !(x = b(o, m)) || x.enumerable });
      return r;
    };
    var f = (r, o, p) => (t2(r, o, "default"), p && t2(p, o, "default"));
    var g = (r) => t2(a({}, "__esModule", { value: true }), r);
    var e2 = {};
    module.exports = g(e2);
    f(e2, require_has_sub_host(), module.exports);
    f(e2, require_host(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/license/verify.js
var require_verify = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/license/verify.js"(exports, module) {
    var y = Object.defineProperty;
    var l = Object.getOwnPropertyDescriptor;
    var w = Object.getOwnPropertyNames;
    var b = Object.prototype.hasOwnProperty;
    var v = (t2, n2) => {
      for (var i in n2)
        y(t2, i, { get: n2[i], enumerable: true });
    };
    var _ = (t2, n2, i, e2) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let o of w(n2))
          !b.call(t2, o) && o !== i && y(t2, o, { get: () => n2[o], enumerable: !(e2 = l(n2, o)) || e2.enumerable });
      return t2;
    };
    var x = (t2) => _(y({}, "__esModule", { value: true }), t2);
    var C = {};
    v(C, { algorithm: () => h2, verify: () => m });
    module.exports = x(C);
    var A = function(t2, n2, i, e2) {
      function o(c) {
        return c instanceof i ? c : new i(function(u2) {
          u2(c);
        });
      }
      return new (i || (i = Promise))(function(c, u2) {
        function d2(r) {
          try {
            f(e2.next(r));
          } catch (a) {
            u2(a);
          }
        }
        function p(r) {
          try {
            f(e2.throw(r));
          } catch (a) {
            u2(a);
          }
        }
        function f(r) {
          r.done ? c(r.value) : o(r.value).then(d2, p);
        }
        f((e2 = e2.apply(t2, n2 || [])).next());
      });
    };
    var h2 = { name: "RSASSA-PKCS1-v1_5", hash: "SHA-256" };
    function s(t2) {
      const n2 = new ArrayBuffer(t2.length), i = new Uint8Array(n2);
      for (let e2 = 0, o = t2.length; e2 < o; e2++)
        i[e2] = t2.charCodeAt(e2);
      return n2;
    }
    function K() {
      const t2 = s(atob(REDOCLY_PUBLIC_KEY));
      return crypto.subtle.importKey("spki", t2, h2, true, ["verify"]);
    }
    function S(t2) {
      return new TextEncoder().encode(t2);
    }
    function m(t2, n2) {
      return A(this, void 0, void 0, function* () {
        return crypto.subtle.verify(h2, yield K(), s(atob(n2)), S(t2));
      });
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/license/validate.js
var require_validate = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/license/validate.js"(exports, module) {
    var s = Object.defineProperty;
    var l = Object.getOwnPropertyDescriptor;
    var v = Object.getOwnPropertyNames;
    var w = Object.prototype.hasOwnProperty;
    var y = (n2, t2) => {
      for (var i in t2)
        s(n2, i, { get: t2[i], enumerable: true });
    };
    var _ = (n2, t2, i, e2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let o of v(t2))
          !w.call(n2, o) && o !== i && s(n2, o, { get: () => t2[o], enumerable: !(e2 = l(t2, o)) || e2.enumerable });
      return n2;
    };
    var m = (n2) => _(s({}, "__esModule", { value: true }), n2);
    var k = {};
    y(k, { validate: () => b });
    module.exports = m(k);
    var d2 = require_verify();
    var x = function(n2, t2, i, e2) {
      function o(a) {
        return a instanceof i ? a : new i(function(f) {
          f(a);
        });
      }
      return new (i || (i = Promise))(function(a, f) {
        function p(c) {
          try {
            r(e2.next(c));
          } catch (u2) {
            f(u2);
          }
        }
        function h2(c) {
          try {
            r(e2.throw(c));
          } catch (u2) {
            f(u2);
          }
        }
        function r(c) {
          c.done ? a(c.value) : o(c.value).then(p, h2);
        }
        r((e2 = e2.apply(n2, t2 || [])).next());
      });
    };
    function b(n2) {
      return x(this, void 0, void 0, function* () {
        const [t2, i] = n2.split(".");
        return (yield (0, d2.verify)(atob(t2), i)) ? JSON.parse(atob(t2)) : { valid: false };
      });
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/license/parse-token.js
var require_parse_token = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/license/parse-token.js"(exports, module) {
    var d2 = Object.defineProperty;
    var w = Object.getOwnPropertyDescriptor;
    var v = Object.getOwnPropertyNames;
    var m = Object.prototype.hasOwnProperty;
    var _ = (n2, e2) => {
      for (var t2 in e2)
        d2(n2, t2, { get: e2[t2], enumerable: true });
    };
    var x = (n2, e2, t2, i) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let o of v(e2))
          !m.call(n2, o) && o !== t2 && d2(n2, o, { get: () => e2[o], enumerable: !(i = w(e2, o)) || i.enumerable });
      return n2;
    };
    var y = (n2) => x(d2({}, "__esModule", { value: true }), n2);
    var A = {};
    _(A, { parseToken: () => N });
    module.exports = y(A);
    var u2 = require_helpers2();
    var s = require_validate();
    var E = function(n2, e2, t2, i) {
      function o(a) {
        return a instanceof t2 ? a : new t2(function(c) {
          c(a);
        });
      }
      return new (t2 || (t2 = Promise))(function(a, c) {
        function p(r) {
          try {
            l(i.next(r));
          } catch (f) {
            c(f);
          }
        }
        function h2(r) {
          try {
            l(i.throw(r));
          } catch (f) {
            c(f);
          }
        }
        function l(r) {
          r.done ? a(r.value) : o(r.value).then(p, h2);
        }
        l((i = i.apply(n2, e2 || [])).next());
      });
    };
    function D(n2) {
      return Date.now() / 1e3 > n2;
    }
    function N(n2) {
      return E(this, void 0, void 0, function* () {
        if (true)
          return { valid: true, allowed: true };
        const e2 = window.location.hostname;
        if (!n2)
          return { local: (0, u2.isLocalhost)(e2) };
        let t2;
        try {
          t2 = yield (0, s.validate)(n2);
        } catch {
          return { valid: false, cryptoMissing: true };
        }
        return t2.valid = true, t2.expired = t2.e ? D(t2.e) : true, t2.allowed = (0, u2.isHostAllowed)(e2, t2.h), t2;
      });
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/license/is-valid-license.js
var require_is_valid_license = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/license/is-valid-license.js"(exports, module) {
    var i = Object.defineProperty;
    var t2 = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var u2 = Object.prototype.hasOwnProperty;
    var x = (a, d2) => {
      for (var r in d2)
        i(a, r, { get: d2[r], enumerable: true });
    };
    var e2 = (a, d2, r, l) => {
      if (d2 && typeof d2 == "object" || typeof d2 == "function")
        for (let o of p(d2))
          !u2.call(a, o) && o !== r && i(a, o, { get: () => d2[o], enumerable: !(l = t2(d2, o)) || l.enumerable });
      return a;
    };
    var f = (a) => e2(i({}, "__esModule", { value: true }), a);
    var v = {};
    x(v, { isValidLicense: () => n2 });
    module.exports = f(v);
    function n2(a) {
      return a.valid && a.allowed && !a.expired || !!a.local;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/license/use-license-key.js
var require_use_license_key = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/license/use-license-key.js"(exports, module) {
    var d2 = Object.defineProperty;
    var w = Object.getOwnPropertyDescriptor;
    var v = Object.getOwnPropertyNames;
    var y = Object.prototype.hasOwnProperty;
    var _ = (n2, t2) => {
      for (var i in t2)
        d2(n2, i, { get: t2[i], enumerable: true });
    };
    var x = (n2, t2, i, e2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let o of v(t2))
          !y.call(n2, o) && o !== i && d2(n2, o, { get: () => t2[o], enumerable: !(e2 = w(t2, o)) || e2.enumerable });
      return n2;
    };
    var L = (n2) => x(d2({}, "__esModule", { value: true }), n2);
    var I = {};
    _(I, { useLicenseKey: () => E });
    module.exports = L(I);
    var a = __require("react");
    var l = require_is_valid_license();
    var p = require_parse_token();
    var V = function(n2, t2, i, e2) {
      function o(c) {
        return c instanceof i ? c : new i(function(s) {
          s(c);
        });
      }
      return new (i || (i = Promise))(function(c, s) {
        function h2(f) {
          try {
            u2(e2.next(f));
          } catch (r) {
            s(r);
          }
        }
        function m(f) {
          try {
            u2(e2.throw(f));
          } catch (r) {
            s(r);
          }
        }
        function u2(f) {
          f.done ? c(f.value) : o(f.value).then(h2, m);
        }
        u2((e2 = e2.apply(n2, t2 || [])).next());
      });
    };
    function E(n2) {
      const [t2, i] = (0, a.useState)(false), [e2, o] = (0, a.useState)(true);
      return (0, a.useEffect)(() => {
        function c() {
          return V(this, void 0, void 0, function* () {
            const s = yield (0, p.parseToken)(n2);
            i((0, l.isValidLicense)(s)), o(false);
          });
        }
        c();
      }, [n2]), { loading: e2, isValid: t2 };
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/license/index.js
var require_license = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/license/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, r, p, x) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let m of c(r))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => r[m], enumerable: !(x = b(r, m)) || x.enumerable });
      return f;
    };
    var e2 = (f, r, p) => (t2(f, r, "default"), p && t2(p, r, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    e2(o, require_parse_token(), module.exports);
    e2(o, require_is_valid_license(), module.exports);
    e2(o, require_use_license_key(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/shared/index.js
var require_shared = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/shared/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_common(), module.exports);
    r(o, require_styled2(), module.exports);
    r(o, require_icons(), module.exports);
    r(o, require_license(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/ReturnTypeView.js
var require_ReturnTypeView = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/ReturnTypeView.js"(exports, module) {
    var a = Object.create;
    var i = Object.defineProperty;
    var s = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var E = Object.getPrototypeOf;
    var f = Object.prototype.hasOwnProperty;
    var x = (e2, t2) => {
      for (var l in t2)
        i(e2, l, { get: t2[l], enumerable: true });
    };
    var u2 = (e2, t2, l, N) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let r of c(t2))
          !f.call(e2, r) && r !== l && i(e2, r, { get: () => t2[r], enumerable: !(N = s(t2, r)) || N.enumerable });
      return e2;
    };
    var T = (e2, t2, l) => (l = e2 != null ? a(E(e2)) : {}, u2(t2 || !e2 || !e2.__esModule ? i(l, "default", { value: e2, enumerable: true }) : l, e2));
    var b = (e2) => u2(i({}, "__esModule", { value: true }), e2);
    var L = {};
    x(L, { ReturnTypeView: () => p });
    module.exports = b(L);
    var n2 = T(__require("react"));
    var m = require_shared();
    var o = require_NullableType();
    function p({ type: e2 }) {
      return e2.isList ? n2.default.createElement(o.NullableType, { isNonNull: e2.isListNonNull }, "[", n2.default.createElement(o.NullableType, { isNonNull: e2.isNonNull }, n2.default.createElement(m.SplittedWbrText, { text: e2.name })), "]") : n2.default.createElement(o.NullableType, { isNonNull: e2.isNonNull }, n2.default.createElement(m.SplittedWbrText, { text: e2.name }));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/constants.js
var require_constants = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/constants.js"(exports, module) {
    var e2 = Object.defineProperty;
    var s = Object.getOwnPropertyDescriptor;
    var A = Object.getOwnPropertyNames;
    var O = Object.prototype.hasOwnProperty;
    var _ = (o, t2) => {
      for (var c in t2)
        e2(o, c, { get: t2[c], enumerable: true });
    };
    var a = (o, t2, c, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let T of A(t2))
          !O.call(o, T) && T !== c && e2(o, T, { get: () => t2[T], enumerable: !(n2 = s(t2, T)) || n2.enumerable });
      return o;
    };
    var d2 = (o) => a(e2({}, "__esModule", { value: true }), o);
    var r = {};
    _(r, { DATA_SECTION_ID: () => i, ROOT_PATH: () => p });
    module.exports = d2(r);
    var i = "data-section-id";
    var p = "/";
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/utils/paths.js
var require_paths = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/utils/paths.js"(exports, module) {
    var t2 = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var u2 = Object.getOwnPropertyNames;
    var l = Object.prototype.hasOwnProperty;
    var o = (r, n2) => {
      for (var e2 in n2)
        t2(r, e2, { get: n2[e2], enumerable: true });
    };
    var g = (r, n2, e2, s) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let i of u2(n2))
          !l.call(r, i) && i !== e2 && t2(r, i, { get: () => n2[i], enumerable: !(s = c(n2, i)) || s.enumerable });
      return r;
    };
    var d2 = (r) => g(t2({}, "__esModule", { value: true }), r);
    var A = {};
    o(A, { addLeadingSlash: () => a, stripLeadingSlash: () => f, stripTrailingSlash: () => x });
    module.exports = d2(A);
    function a(r) {
      return r.charAt(0) === "/" ? r : "/" + r;
    }
    function f(r) {
      return r.charAt(0) === "/" ? r.substring(1) : r;
    }
    function x(r) {
      return r.charAt(r.length - 1) === "/" ? r.slice(0, -1) : r;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/use-base-url-path.js
var require_use_base_url_path = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/use-base-url-path.js"(exports, module) {
    var i = Object.defineProperty;
    var l = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var f = Object.prototype.hasOwnProperty;
    var g = (t2, r) => {
      for (var a in r)
        i(t2, a, { get: r[a], enumerable: true });
    };
    var h2 = (t2, r, a, n2) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let s of p(r))
          !f.call(t2, s) && s !== a && i(t2, s, { get: () => r[s], enumerable: !(n2 = l(r, s)) || n2.enumerable });
      return t2;
    };
    var m = (t2) => h2(i({}, "__esModule", { value: true }), t2);
    var d2 = {};
    g(d2, { useBaseUrlPath: () => u2 });
    module.exports = m(d2);
    var o = require_settings2();
    var e2 = require_paths();
    function u2() {
      const { baseUrlPath: t2 } = (0, o.useSettings)();
      return typeof t2 == "string" ? (0, e2.addLeadingSlash)((0, e2.stripTrailingSlash)(t2)) : "";
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/use-location.js
var require_use_location = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/use-location.js"(exports, module) {
    var r = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var h2 = Object.prototype.hasOwnProperty;
    var l = (o, t2) => {
      for (var e2 in t2)
        r(o, e2, { get: t2[e2], enumerable: true });
    };
    var f = (o, t2, e2, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let a of p(t2))
          !h2.call(o, a) && a !== e2 && r(o, a, { get: () => t2[a], enumerable: !(n2 = u2(t2, a)) || n2.enumerable });
      return o;
    };
    var b = (o) => f(r({}, "__esModule", { value: true }), o);
    var d2 = {};
    l(d2, { useLocation: () => L });
    module.exports = b(d2);
    var c = __require("react");
    var i = __require("react-router-dom");
    var s = require_use_base_url_path();
    function L(o = true) {
      const t2 = (0, s.useBaseUrlPath)(), e2 = (0, i.useLocation)();
      return (0, c.useMemo)(() => {
        const a = Object.assign(Object.create(e2), e2);
        return Object.defineProperty(a, "pathname", { get() {
          return o ? e2.pathname.replace(t2, "") : e2.pathname;
        }, set(m) {
          e2.pathname = m;
        } }), a;
      }, [t2, e2, o]);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/use-automatic-scroll-by-location.js
var require_use_automatic_scroll_by_location = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/use-automatic-scroll-by-location.js"(exports, module) {
    var a = Object.create;
    var s = Object.defineProperty;
    var m = Object.getOwnPropertyDescriptor;
    var u2 = Object.getOwnPropertyNames;
    var d2 = Object.getPrototypeOf;
    var h2 = Object.prototype.hasOwnProperty;
    var p = (o, e2) => {
      for (var t2 in e2)
        s(o, t2, { get: e2[t2], enumerable: true });
    };
    var i = (o, e2, t2, n2) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let c of u2(e2))
          !h2.call(o, c) && c !== t2 && s(o, c, { get: () => e2[c], enumerable: !(n2 = m(e2, c)) || n2.enumerable });
      return o;
    };
    var I = (o, e2, t2) => (t2 = o != null ? a(d2(o)) : {}, i(e2 || !o || !o.__esModule ? s(t2, "default", { value: o, enumerable: true }) : t2, o));
    var w = (o) => i(s({}, "__esModule", { value: true }), o);
    var A = {};
    p(A, { useAutomaticScrollByLocation: () => T });
    module.exports = w(A);
    var S = I(__require("react"));
    var r = require_constants();
    var l = require_use_location();
    var f = require_settings2();
    function T({ scrollIfNeeded: o } = {}) {
      const e2 = (0, l.useLocation)(), { pagination: t2 } = (0, f.useSettings)();
      S.useEffect(() => {
        y({ location: e2, scrollIfNeeded: o || false, pagination: t2 });
      }, [e2, o, t2]);
    }
    function y({ location: o, scrollIfNeeded: e2, pagination: t2 }) {
      const n2 = o.hash && decodeURI(o.hash.slice(1));
      if (!n2 && t2 !== "section")
        return;
      const c = document.querySelector(`[${r.DATA_SECTION_ID}="${n2}"]`);
      c ? e2 ? c.scrollIntoViewIfNeeded(true) : c.scrollIntoView() : window.scrollTo(0, 0);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/navigation.context.js
var require_navigation_context = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/navigation.context.js"(exports, module) {
    var d2 = Object.create;
    var i = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var v = Object.getOwnPropertyNames;
    var c = Object.getPrototypeOf;
    var C = Object.prototype.hasOwnProperty;
    var f = (t2, o) => {
      for (var e2 in o)
        i(t2, e2, { get: o[e2], enumerable: true });
    };
    var a = (t2, o, e2, r) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let n2 of v(o))
          !C.call(t2, n2) && n2 !== e2 && i(t2, n2, { get: () => o[n2], enumerable: !(r = u2(o, n2)) || r.enumerable });
      return t2;
    };
    var g = (t2, o, e2) => (e2 = t2 != null ? d2(c(t2)) : {}, a(o || !t2 || !t2.__esModule ? i(e2, "default", { value: t2, enumerable: true }) : e2, t2));
    var N = (t2) => a(i({}, "__esModule", { value: true }), t2);
    var h2 = {};
    f(h2, { NavigationContextProvider: () => p, useNavigationContext: () => P });
    module.exports = N(h2);
    var s = g(__require("react"));
    var x = s.createContext(void 0);
    var p = x.Provider;
    function P() {
      const t2 = s.useContext(x);
      if (!t2)
        throw new Error("useNavigationContext should only be used inside of NavigationContextProvider.");
      return t2;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/utils/is-empty.js
var require_is_empty = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/utils/is-empty.js"(exports, module) {
    var e2 = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var f = Object.prototype.hasOwnProperty;
    var m = (t2, n2) => {
      for (var o in n2)
        e2(t2, o, { get: n2[o], enumerable: true });
    };
    var s = (t2, n2, o, i) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let r of c(n2))
          !f.call(t2, r) && r !== o && e2(t2, r, { get: () => n2[r], enumerable: !(i = u2(n2, r)) || i.enumerable });
      return t2;
    };
    var x = (t2) => s(e2({}, "__esModule", { value: true }), t2);
    var E = {};
    m(E, { isEmpty: () => p, isNotEmpty: () => y });
    module.exports = x(E);
    function p(t2) {
      return t2 === "";
    }
    function y(t2) {
      return !p(t2);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/utils/join.js
var require_join = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/utils/join.js"(exports, module) {
    var e2 = Object.defineProperty;
    var m = Object.getOwnPropertyDescriptor;
    var u2 = Object.getOwnPropertyNames;
    var l = Object.prototype.hasOwnProperty;
    var h2 = (i, t2) => {
      for (var n2 in t2)
        e2(i, n2, { get: t2[n2], enumerable: true });
    };
    var c = (i, t2, n2, p) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let r of u2(t2))
          !l.call(i, r) && r !== n2 && e2(i, r, { get: () => t2[r], enumerable: !(p = m(t2, r)) || p.enumerable });
      return i;
    };
    var d2 = (i) => c(e2({}, "__esModule", { value: true }), i);
    var x = {};
    h2(x, { joinId: () => j, joinPath: () => S });
    module.exports = d2(x);
    var f = require_is_defined();
    var a = require_is_empty();
    var o = require_paths();
    function S(...i) {
      return "/" + s(...i);
    }
    function j(...i) {
      return s(...i);
    }
    function s(...i) {
      return i.filter(f.isDefined).map(g).filter(a.isNotEmpty).join("/");
    }
    function g(i) {
      const t2 = (0, o.stripLeadingSlash)(i);
      return (0, o.stripTrailingSlash)(t2);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/utils/pagination-item.js
var require_pagination_item = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/utils/pagination-item.js"(exports, module) {
    var x = Object.create;
    var r = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var g = Object.getOwnPropertyNames;
    var P = Object.getPrototypeOf;
    var b = Object.prototype.hasOwnProperty;
    var j = (t2, e2) => {
      for (var n2 in e2)
        r(t2, n2, { get: e2[n2], enumerable: true });
    };
    var m = (t2, e2, n2, p) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let o of g(e2))
          !b.call(t2, o) && o !== n2 && r(t2, o, { get: () => e2[o], enumerable: !(p = f(e2, o)) || p.enumerable });
      return t2;
    };
    var v = (t2, e2, n2) => (n2 = t2 != null ? x(P(t2)) : {}, m(e2 || !t2 || !t2.__esModule ? r(n2, "default", { value: t2, enumerable: true }) : n2, t2));
    var I = (t2) => m(r({}, "__esModule", { value: true }), t2);
    var k = {};
    j(k, { toItemRoute: () => R, withPaginationItem: () => O, withPaginationItemFallback: () => E });
    module.exports = I(k);
    var c = v(__require("react"));
    var i = __require("react-router-dom");
    var h2 = require_join();
    function O(t2, e2, n2, p, o) {
      const a = n2[e2 - 1], s = n2[e2 + 1], l = a && { name: a.props.name, location: Object.assign(Object.assign({}, p), { [o]: a.props.path }) }, u2 = s && { name: s.props.name, location: Object.assign(Object.assign({}, p), { [o]: s.props.path }) };
      return c.cloneElement(t2, { prev: l, next: u2 });
    }
    function E(t2, e2) {
      return c.cloneElement(t2, { prev: t2.props.prev, next: t2.props.next || e2 });
    }
    function R(t2, e2) {
      const n2 = (0, h2.joinPath)(e2, t2.props.path, "*");
      return c.createElement(i.Route, { key: n2, path: n2, element: t2 });
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/utils/pagination-section.js
var require_pagination_section = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/utils/pagination-section.js"(exports, module) {
    var m = Object.create;
    var i = Object.defineProperty;
    var x = Object.getOwnPropertyDescriptor;
    var l = Object.getOwnPropertyNames;
    var h2 = Object.getPrototypeOf;
    var d2 = Object.prototype.hasOwnProperty;
    var S = (t2, e2) => {
      for (var n2 in e2)
        i(t2, n2, { get: e2[n2], enumerable: true });
    };
    var p = (t2, e2, n2, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let o of l(e2))
          !d2.call(t2, o) && o !== n2 && i(t2, o, { get: () => e2[o], enumerable: !(r = x(e2, o)) || r.enumerable });
      return t2;
    };
    var I = (t2, e2, n2) => (n2 = t2 != null ? m(h2(t2)) : {}, p(e2 || !t2 || !t2.__esModule ? i(n2, "default", { value: t2, enumerable: true }) : n2, t2));
    var f = (t2) => p(i({}, "__esModule", { value: true }), t2);
    var F = {};
    S(F, { withNextSectionFirstItem: () => u2 });
    module.exports = f(F);
    var c = I(__require("react"));
    function u2(t2, e2, n2) {
      const r = n2[e2 + 1], o = r && c.Children.toArray(r.props.children), s = o?.[0], a = s && { name: s.props.name, location: { section: r.props.path, item: s.props.path } };
      return c.cloneElement(t2, { next: t2.props.next && Object.assign(Object.assign({}, t2.props.next), { firstItem: a }) });
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/utils/navigation.js
var require_navigation = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/utils/navigation.js"(exports, module) {
    var a = Object.defineProperty;
    var y = Object.getOwnPropertyDescriptor;
    var K = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var s = (r, t2) => {
      for (var o in t2)
        a(r, o, { get: t2[o], enumerable: true });
    };
    var f = (r, t2, o, u2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let i of K(t2))
          !c.call(r, i) && i !== o && a(r, i, { get: () => t2[i], enumerable: !(u2 = y(t2, i)) || u2.enumerable });
      return r;
    };
    var g = (r) => f(a({}, "__esModule", { value: true }), r);
    var n2 = {};
    s(n2, { userIsForcingNavigation: () => l });
    module.exports = g(n2);
    function l(r) {
      return r.button !== 0 || r.altKey || r.ctrlKey || r.metaKey || r.shiftKey;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/utils/render-entire-documentation.js
var require_render_entire_documentation = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/utils/render-entire-documentation.js"(exports, module) {
    var u2 = Object.create;
    var a = Object.defineProperty;
    var E = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var f = Object.getPrototypeOf;
    var g = Object.prototype.hasOwnProperty;
    var R = (e2, t2) => {
      for (var r in t2)
        a(e2, r, { get: t2[r], enumerable: true });
    };
    var c = (e2, t2, r, o) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let m of p(t2))
          !g.call(e2, m) && m !== r && a(e2, m, { get: () => t2[m], enumerable: !(o = E(t2, m)) || o.enumerable });
      return e2;
    };
    var d2 = (e2, t2, r) => (r = e2 != null ? u2(f(e2)) : {}, c(t2 || !e2 || !e2.__esModule ? a(r, "default", { value: e2, enumerable: true }) : r, e2));
    var v = (e2) => c(a({}, "__esModule", { value: true }), e2);
    var O = {};
    R(O, { renderEntireDocumentation: () => F });
    module.exports = v(O);
    var n2 = d2(__require("react"));
    var l = __require("react-router-dom");
    var i = require_constants();
    function F(e2, { header: t2, path: r = i.ROOT_PATH }) {
      return n2.default.createElement(n2.default.Fragment, null, n2.default.createElement(l.Route, { path: r, element: n2.default.createElement(n2.default.Fragment, null, n2.default.createElement("div", null, t2), e2) }), n2.default.createElement(l.Route, { path: "*", element: n2.default.createElement(l.Navigate, { to: r, replace: true }) }));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/utils/render-individual-sections.js
var require_render_individual_sections = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/utils/render-individual-sections.js"(exports, module) {
    var R = Object.create;
    var a = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var v = Object.getOwnPropertyNames;
    var T = Object.getPrototypeOf;
    var g = Object.prototype.hasOwnProperty;
    var h2 = (t2, m) => {
      for (var e2 in m)
        a(t2, e2, { get: m[e2], enumerable: true });
    };
    var f = (t2, m, e2, o) => {
      if (m && typeof m == "object" || typeof m == "function")
        for (let r of v(m))
          !g.call(t2, r) && r !== e2 && a(t2, r, { get: () => m[r], enumerable: !(o = c(m, r)) || o.enumerable });
      return t2;
    };
    var E = (t2, m, e2) => (e2 = t2 != null ? R(T(t2)) : {}, f(m || !t2 || !t2.__esModule ? a(e2, "default", { value: t2, enumerable: true }) : e2, t2));
    var N = (t2) => f(a({}, "__esModule", { value: true }), t2);
    var x = {};
    h2(x, { renderIndividualSections: () => w });
    module.exports = N(x);
    var i = E(__require("react"));
    var n2 = __require("react-router-dom");
    var u2 = require_constants();
    var O = require_pagination_section();
    var p = require_pagination_item();
    var d2 = require_join();
    var I = require_settings2();
    function w(t2, { hidePaginationButtons: m, parentPath: e2 = u2.ROOT_PATH } = {}) {
      return i.default.createElement(i.default.Fragment, null, m ? t2.map((o) => (0, p.toItemRoute)(o, e2)) : t2.map((o, r, l) => (0, p.withPaginationItem)(o, r, l, I.ROOT_LOCATION, "section")).map((o, r, l) => (0, O.withNextSectionFirstItem)(o, r, l)).map((o) => (0, p.toItemRoute)(o, e2)), i.default.createElement(n2.Route, { path: "*", element: i.default.createElement(n2.Navigate, { to: (0, d2.joinPath)(e2, t2?.[0].props.path) }) }));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/utils.js
var require_utils = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/utils.js"(exports, module) {
    var e2 = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var d2 = Object.getOwnPropertyNames;
    var l = Object.prototype.hasOwnProperty;
    var m = (n2, t2) => {
      for (var o in t2)
        e2(n2, o, { get: t2[o], enumerable: true });
    };
    var u2 = (n2, t2, o, i) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let r of d2(t2))
          !l.call(n2, r) && r !== o && e2(n2, r, { get: () => t2[r], enumerable: !(i = c(t2, r)) || i.enumerable });
      return n2;
    };
    var I = (n2) => u2(e2({}, "__esModule", { value: true }), n2);
    var h2 = {};
    m(h2, { getLocationId: () => p, getNavigationIdProp: () => f, normalizeElementId: () => g });
    module.exports = I(h2);
    var s = require_join();
    var a = require_constants();
    function p(n2) {
      const { pathname: t2, hash: o } = n2, i = o.substring(1);
      return (0, s.joinId)(t2, i);
    }
    function g(n2, t2 = {}) {
      const o = t2.trimLeadingParts || 0, i = t2.resultParts;
      return n2.split("/").slice(o).slice(0, i).join("/");
    }
    function f(n2) {
      return { [a.DATA_SECTION_ID]: n2 };
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/use-active-state.js
var require_use_active_state = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/use-active-state.js"(exports, module) {
    var c = Object.defineProperty;
    var d2 = Object.getOwnPropertyDescriptor;
    var m = Object.getOwnPropertyNames;
    var f = Object.prototype.hasOwnProperty;
    var u2 = (o, t2) => {
      for (var n2 in t2)
        c(o, n2, { get: t2[n2], enumerable: true });
    };
    var I = (o, t2, n2, a) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let e2 of m(t2))
          !f.call(o, e2) && e2 !== n2 && c(o, e2, { get: () => t2[e2], enumerable: !(a = d2(t2, e2)) || a.enumerable });
      return o;
    };
    var h2 = (o) => I(c({}, "__esModule", { value: true }), o);
    var v = {};
    u2(v, { useActiveState: () => p });
    module.exports = h2(v);
    var r = require_use_location();
    var i = require_utils();
    function p(o, { locationMatchDepth: t2, elementIdOffset: n2 } = { locationMatchDepth: 3, elementIdOffset: 1 }) {
      const a = (0, r.useLocation)(), e2 = (0, i.getLocationId)(a);
      if (!e2)
        return { isActive: false };
      const l = (0, i.normalizeElementId)(e2, { resultParts: t2 }), s = (0, i.normalizeElementId)(o, { trimLeadingParts: n2 });
      return { isActive: l === s };
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/use-combined-navigation-id.js
var require_use_combined_navigation_id = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/use-combined-navigation-id.js"(exports, module) {
    var r = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var a = Object.getOwnPropertyNames;
    var m = Object.prototype.hasOwnProperty;
    var c = (t2, n2) => {
      for (var o in n2)
        r(t2, o, { get: n2[o], enumerable: true });
    };
    var g = (t2, n2, o, i) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let e2 of a(n2))
          !m.call(t2, e2) && e2 !== o && r(t2, e2, { get: () => n2[e2], enumerable: !(i = f(n2, e2)) || i.enumerable });
      return t2;
    };
    var p = (t2) => g(r({}, "__esModule", { value: true }), t2);
    var x = {};
    c(x, { useCombinedNavigationId: () => v });
    module.exports = p(x);
    var s = require_settings2();
    var u2 = require_navigation_context();
    function v(t2, n2) {
      const o = (0, u2.useNavigationContext)(), { pagination: i } = (0, s.useSettings)();
      return t2 === "item" ? i !== "none" ? n2 || "" : `${o.location.section || ""}/${n2}` : t2 === "section" && i === "none" && n2 || "";
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/use-pagination-aware-location.js
var require_use_pagination_aware_location = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/use-pagination-aware-location.js"(exports, module) {
    var c = Object.defineProperty;
    var m = Object.getOwnPropertyDescriptor;
    var u2 = Object.getOwnPropertyNames;
    var h2 = Object.prototype.hasOwnProperty;
    var f = (t2, e2) => {
      for (var n2 in e2)
        c(t2, n2, { get: e2[n2], enumerable: true });
    };
    var p = (t2, e2, n2, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let o of u2(e2))
          !h2.call(t2, o) && o !== n2 && c(t2, o, { get: () => e2[o], enumerable: !(r = m(e2, o)) || r.enumerable });
      return t2;
    };
    var P = (t2) => p(c({}, "__esModule", { value: true }), t2);
    var j = {};
    f(j, { usePaginationAwareLocation: () => g });
    module.exports = P(j);
    var a = require_constants();
    var s = require_join();
    var i = require_use_base_url_path();
    function g(t2, e2) {
      const n2 = (0, i.useBaseUrlPath)(), r = (0, s.joinPath)(n2 || a.ROOT_PATH, l(t2, e2));
      return { pathname: r.length > 1 ? `${r}/` : r, hash: w(t2, e2) };
    }
    function l(t2, e2) {
      const { section: n2, item: r } = e2;
      switch (t2) {
        case "none":
          return "";
        case "section":
          return n2 || "";
        case "item":
          return (0, s.joinPath)(n2, r);
      }
    }
    function w(t2, e2) {
      const { section: n2, item: r, deepItem: o } = e2;
      switch (t2) {
        case "none":
          return (0, s.joinId)(n2, r, o);
        case "section":
          return (0, s.joinId)(r, o);
        case "item":
          return o || "";
      }
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/index.js
var require_hooks = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/hooks/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_use_active_state(), module.exports);
    r(o, require_use_automatic_scroll_by_location(), module.exports);
    r(o, require_use_combined_navigation_id(), module.exports);
    r(o, require_use_base_url_path(), module.exports);
    r(o, require_use_pagination_aware_location(), module.exports);
    r(o, require_use_location(), module.exports);
    r(o, require_utils(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/components/AnchorLink.js
var require_AnchorLink = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/components/AnchorLink.js"(exports, module) {
    var h2 = Object.create;
    var i = Object.defineProperty;
    var k = Object.getOwnPropertyDescriptor;
    var s = Object.getOwnPropertyNames;
    var L = Object.getPrototypeOf;
    var S = Object.prototype.hasOwnProperty;
    var a = (o, r) => {
      for (var m in r)
        i(o, m, {
          get: r[m],
          enumerable: true
        });
    };
    var p = (o, r, m, n2) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let t2 of s(r))
          !S.call(o, t2) && t2 !== m && i(o, t2, {
            get: () => r[t2],
            enumerable: !(n2 = k(r, t2)) || n2.enumerable
          });
      return o;
    };
    var d2 = (o, r, m) => (m = o != null ? h2(L(o)) : {}, p(r || !o || !o.__esModule ? i(m, "default", {
      value: o,
      enumerable: true
    }) : m, o));
    var l = (o) => p(i({}, "__esModule", {
      value: true
    }), o);
    var y = {};
    a(y, {
      AnchorLink: () => x
    });
    module.exports = l(y);
    var c = __require("react-router-dom");
    var f = d2((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var e2 = require_styled2();
    var x = (0, f.default)(c.Link)`
  ${e2.anchorCSS}
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/components/TextOverflow.js
var require_TextOverflow = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/components/TextOverflow.js"(exports, module) {
    var w = Object.create;
    var l = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var n2 = Object.getOwnPropertyNames;
    var a = Object.getPrototypeOf;
    var d2 = Object.prototype.hasOwnProperty;
    var v = (e2, o) => {
      for (var t2 in o)
        l(e2, t2, {
          get: o[t2],
          enumerable: true
        });
    };
    var s = (e2, o, t2, p) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let r of n2(o))
          !d2.call(e2, r) && r !== t2 && l(e2, r, {
            get: () => o[r],
            enumerable: !(p = f(o, r)) || p.enumerable
          });
      return e2;
    };
    var x = (e2, o, t2) => (t2 = e2 != null ? w(a(e2)) : {}, s(o || !e2 || !e2.__esModule ? l(t2, "default", {
      value: e2,
      enumerable: true
    }) : t2, e2));
    var c = (e2) => s(l({}, "__esModule", {
      value: true
    }), e2);
    var m = {};
    v(m, {
      TextOverflow: () => h2
    });
    module.exports = c(m);
    var i = x((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var h2 = i.default.span`
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/components/Message.js
var require_Message = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/components/Message.js"(exports, module) {
    var n2 = Object.create;
    var a = Object.defineProperty;
    var g = Object.getOwnPropertyDescriptor;
    var h2 = Object.getOwnPropertyNames;
    var c = Object.getPrototypeOf;
    var p = Object.prototype.hasOwnProperty;
    var f = (e2, r) => {
      for (var t2 in r)
        a(e2, t2, {
          get: r[t2],
          enumerable: true
        });
    };
    var i = (e2, r, t2, s) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let o of h2(r))
          !p.call(e2, o) && o !== t2 && a(e2, o, {
            get: () => r[o],
            enumerable: !(s = g(r, o)) || s.enumerable
          });
      return e2;
    };
    var v = (e2, r, t2) => (t2 = e2 != null ? n2(c(e2)) : {}, i(r || !e2 || !e2.__esModule ? a(t2, "default", {
      value: e2,
      enumerable: true
    }) : t2, e2));
    var m = (e2) => i(a({}, "__esModule", {
      value: true
    }), e2);
    var b = {};
    f(b, {
      Message: () => x
    });
    module.exports = m(b);
    var l = v((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var x = l.default.span`
  font-weight: var(--font-weight-regular);
  font-size: var(--schema-property-labels-font-size);
  line-height: var(--line-height-base);
  margin-left: var(--spacing-xxs);

  color: ${({
      type: e2
    }) => e2 === "error" ? "var(--schema-property-required-label-text-color)" : `var(--color-${e2}-text)`};
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/components/Box.js
var require_Box = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/components/Box.js"(exports, module) {
    var c = Object.create;
    var o = Object.defineProperty;
    var r = Object.getOwnPropertyDescriptor;
    var f = Object.getOwnPropertyNames;
    var $ = Object.getPrototypeOf;
    var g = Object.prototype.hasOwnProperty;
    var x = (t2, i) => {
      for (var n2 in i)
        o(t2, n2, {
          get: i[n2],
          enumerable: true
        });
    };
    var d2 = (t2, i, n2, p) => {
      if (i && typeof i == "object" || typeof i == "function")
        for (let a of f(i))
          !g.call(t2, a) && a !== n2 && o(t2, a, {
            get: () => i[a],
            enumerable: !(p = r(i, a)) || p.enumerable
          });
      return t2;
    };
    var y = (t2, i, n2) => (n2 = t2 != null ? c($(t2)) : {}, d2(i || !t2 || !t2.__esModule ? o(n2, "default", {
      value: t2,
      enumerable: true
    }) : n2, t2));
    var m = (t2) => d2(o({}, "__esModule", {
      value: true
    }), t2);
    var B = {};
    x(B, {
      Box: () => u2,
      BoxCss: () => s,
      BoxGap: () => l,
      InlineBox: () => v
    });
    module.exports = m(B);
    var e2 = y((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var l = e2.css`
  padding: ${({
      rowGap: t2 = 0
    }) => `calc(var(--spacing-unit) * ${t2}) 0`};

  &:first-of-type {
    padding-top: 0;
  }

  &:last-of-type {
    padding-bottom: 0;
  }

  & > & {
    padding-left: ${({
      gutter: t2 = 0
    }) => `calc(var(--spacing-unit) * ${t2})`};
    padding-right: ${({
      gutter: t2 = 0
    }) => `calc(var(--spacing-unit) * ${t2})`};

    &:first-of-type {
      padding-left: 0;
    }

    &:last-of-type {
      padding-right: 0;
    }
  }
`;
    var s = e2.css`
  ${({
      direction: t2,
      basis: i,
      wrap: n2,
      align: p,
      justify: a
    }) => e2.css`
    flex-direction: ${t2};
    flex-basis: ${i};
    flex-wrap: ${n2};
    align-items: ${p};
    justify-content: ${a};
  `}

  ${({
      gutter: t2
    }) => t2 != null ? l : ""};
`;
    var u2 = e2.default.div`
  display: flex;
  width: 100%;

  ${s};
`;
    var v = e2.default.div`
  display: inline-flex;
  align-items: center;

  ${s};
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/components/index.js
var require_components2 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/components/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_AnchorLink(), module.exports);
    r(o, require_TextOverflow(), module.exports);
    r(o, require_Message(), module.exports);
    r(o, require_Box(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/icons/ListIcon.js
var require_ListIcon = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/icons/ListIcon.js"(exports, module) {
    var p = Object.create;
    var o = Object.defineProperty;
    var d2 = Object.getOwnPropertyDescriptor;
    var h2 = Object.getOwnPropertyNames;
    var g = Object.getPrototypeOf;
    var w = Object.prototype.hasOwnProperty;
    var f = (e2, t2) => {
      for (var c in t2)
        o(e2, c, {
          get: t2[c],
          enumerable: true
        });
    };
    var a = (e2, t2, c, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let r of h2(t2))
          !w.call(e2, r) && r !== c && o(e2, r, {
            get: () => t2[r],
            enumerable: !(n2 = d2(t2, r)) || n2.enumerable
          });
      return e2;
    };
    var m = (e2, t2, c) => (c = e2 != null ? p(g(e2)) : {}, a(t2 || !e2 || !e2.__esModule ? o(c, "default", {
      value: e2,
      enumerable: true
    }) : c, e2));
    var E = (e2) => a(o({}, "__esModule", {
      value: true
    }), e2);
    var _ = {};
    f(_, {
      ListIcon: () => I
    });
    module.exports = E(_);
    var l = m(__require("react"));
    var s = m((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var i = (init_src(), __toCommonJS(src_exports));
    var x = (e2) => l.default.createElement("svg", Object.assign({
      viewBox: "0 0 22 22",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg"
    }, e2), l.default.createElement("g", {
      clipPath: "url(#clip0_10754_58823)"
    }, l.default.createElement("path", {
      d: "M0 11H22",
      strokeLinecap: "round"
    }), l.default.createElement("circle", {
      cx: "4",
      cy: "11",
      r: "4"
    })), l.default.createElement("defs", null, l.default.createElement("clipPath", {
      id: "clip0_10754_58823"
    }, l.default.createElement("rect", {
      width: "22",
      height: "22",
      fill: "white"
    }))));
    var I = (0, s.default)(x).attrs(() => ({
      "data-component-name": "icons/FolderAddIcon/FolderAddIcon"
    }))`
  path,
  circle {
    fill: ${({
      color: e2
    }) => (0, i.getCssColorVariable)(e2)};
    stroke: ${({
      color: e2
    }) => (0, i.getCssColorVariable)(e2)};
  }

  height: ${({
      size: e2
    }) => e2 || "16px"};
  width: ${({
      size: e2
    }) => e2 || "16px"};
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/icons/index.js
var require_icons2 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/icons/index.js"(exports, module) {
    var e2 = Object.defineProperty;
    var m = Object.getOwnPropertyDescriptor;
    var n2 = Object.getOwnPropertyNames;
    var p = Object.prototype.hasOwnProperty;
    var s = (r, o) => {
      for (var c in o)
        e2(r, c, { get: o[c], enumerable: true });
    };
    var x = (r, o, c, f) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let t2 of n2(o))
          !p.call(r, t2) && t2 !== c && e2(r, t2, { get: () => o[t2], enumerable: !(f = m(o, t2)) || f.enumerable });
      return r;
    };
    var I = (r) => x(e2({}, "__esModule", { value: true }), r);
    var L = {};
    s(L, { ListIcon: () => i.ListIcon });
    module.exports = I(L);
    var i = require_ListIcon();
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/index.js
var require_common2 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (r, o, p, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let m of c(o))
          !d2.call(r, m) && m !== p && a(r, m, { get: () => o[m], enumerable: !(x = b(o, m)) || x.enumerable });
      return r;
    };
    var f = (r, o, p) => (t2(r, o, "default"), p && t2(p, o, "default"));
    var g = (r) => t2(a({}, "__esModule", { value: true }), r);
    var e2 = {};
    module.exports = g(e2);
    f(e2, require_components2(), module.exports);
    f(e2, require_icons2(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/components/PaginationAwareLink.js
var require_PaginationAwareLink = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/components/PaginationAwareLink.js"(exports, module) {
    var f = Object.create;
    var r = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var L = Object.getOwnPropertyNames;
    var k = Object.getPrototypeOf;
    var A = Object.prototype.hasOwnProperty;
    var w = (o, n2) => {
      for (var i in n2)
        r(o, i, { get: n2[i], enumerable: true });
    };
    var c = (o, n2, i, a) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let t2 of L(n2))
          !A.call(o, t2) && t2 !== i && r(o, t2, { get: () => n2[t2], enumerable: !(a = u2(n2, t2)) || a.enumerable });
      return o;
    };
    var C = (o, n2, i) => (i = o != null ? f(k(o)) : {}, c(n2 || !o || !o.__esModule ? r(i, "default", { value: o, enumerable: true }) : i, o));
    var d2 = (o) => c(r({}, "__esModule", { value: true }), o);
    var v = {};
    w(v, { PaginationAwareLink: () => h2 });
    module.exports = d2(v);
    var m = C(__require("react"));
    var s = require_hooks();
    var p = require_common2();
    var g = require_settings2();
    function h2({ to: o, className: n2, children: i }) {
      const { pagination: a, onLocationChange: t2 } = (0, g.useSettings)(), e2 = (0, s.usePaginationAwareLocation)(a, o), l = () => {
        t2?.(e2);
      };
      return m.default.createElement(p.AnchorLink, { className: n2, to: e2, onClick: l }, i);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/components/styled.js
var require_styled3 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/components/styled.js"(exports, module) {
    var h2 = Object.create;
    var g = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var d2 = Object.getOwnPropertyNames;
    var v = Object.getPrototypeOf;
    var N = Object.prototype.hasOwnProperty;
    var x = (t2, e2) => {
      for (var n2 in e2)
        g(t2, n2, {
          get: e2[n2],
          enumerable: true
        });
    };
    var r = (t2, e2, n2, s) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let a of d2(e2))
          !N.call(t2, a) && a !== n2 && g(t2, a, {
            get: () => e2[a],
            enumerable: !(s = c(e2, a)) || s.enumerable
          });
      return t2;
    };
    var b = (t2, e2, n2) => (n2 = t2 != null ? h2(v(t2)) : {}, r(e2 || !t2 || !t2.__esModule ? g(n2, "default", {
      value: t2,
      enumerable: true
    }) : n2, t2));
    var f = (t2) => r(g({}, "__esModule", {
      value: true
    }), t2);
    var U = {};
    x(U, {
      ButtonWrapper: () => D,
      NavigationLabel: () => u2,
      PageNavigationWrapper: () => L,
      ShareIconLink: () => l,
      ShareLinkWrapper: () => m,
      StyledButton: () => w
    });
    module.exports = f(U);
    var i = b((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var o = (init_src(), __toCommonJS(src_exports));
    var A = require_PaginationAwareLink();
    var p = require_app();
    var l = (0, i.default)(A.PaginationAwareLink)`
  ${({
      size: t2 = 15
    }) => i.css`
    & {
      cursor: pointer;
      padding: 0;
      line-height: 1;
      width: ${t2}px;
      margin-left: calc(-${t2}px - 4px);
      display: inline-block;
      outline: 0;
      position: relative;
      padding-right: calc(${t2}px + 4px);
    }

    &:before {
      content: '';
      width: ${t2}px;
      height: ${t2}px;
      background-size: contain;
      background-image: url('data:image/png;base64,');
      visibility: hidden;
      display: inline-block;
      vertical-align: middle;
    }

    &:focus::before {
      visibility: visible;
    }

    &:hover::before {
      visibility: visible;
    }
  `}
`;
    var m = i.default.span`
  &:hover > ${l}::before {
    visibility: visible;
  }
`;
    var w = (0, i.default)(o.Button)`
  text-wrap: wrap;
  padding-left: 0;
  padding-right: 0;
`;
    var D = i.default.div`
  display: flex;
  flex-direction: column;
  text-align: ${({
      align: t2 = "left"
    }) => t2};
  ${({
      align: t2
    }) => i.css`
    margin-${t2 === "left" ? "right" : "left"}: auto;
    `};
  gap: var(--spacing-xxs);
`;
    var u2 = i.default.span`
  font-size: var(--font-size-sm);
  line-height: var(--line-height-sm);
  font-weight: var(--font-weight-regular);
`;
    var L = (0, i.default)(p.MiddleSection)`
  display: flex;
  justify-content: space-between;
  margin: var(--spacing-vertical) 0;

  @media print {
    display: none;
  }
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/components/NavigationButton.js
var require_NavigationButton = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/components/NavigationButton.js"(exports, module) {
    var B = Object.create;
    var s = Object.defineProperty;
    var E = Object.getOwnPropertyDescriptor;
    var N = Object.getOwnPropertyNames;
    var L = Object.getPrototypeOf;
    var P = Object.prototype.hasOwnProperty;
    var w = (t2, n2) => {
      for (var e2 in n2)
        s(t2, e2, { get: n2[e2], enumerable: true });
    };
    var g = (t2, n2, e2, l) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let o of N(n2))
          !P.call(t2, o) && o !== e2 && s(t2, o, { get: () => n2[o], enumerable: !(l = E(n2, o)) || l.enumerable });
      return t2;
    };
    var A = (t2, n2, e2) => (e2 = t2 != null ? B(L(t2)) : {}, g(n2 || !t2 || !t2.__esModule ? s(e2, "default", { value: t2, enumerable: true }) : e2, t2));
    var C = (t2) => g(s({}, "__esModule", { value: true }), t2);
    var b = {};
    w(b, { NavigationButton: () => I });
    module.exports = C(b);
    var f = __require("react-router-dom");
    var a = A(__require("react"));
    var i = (init_src(), __toCommonJS(src_exports));
    var r = require_styled3();
    var p = require_utils2();
    var v = require_navigation2();
    var h2 = require_settings2();
    function I({ item: t2, type: n2 }) {
      const e2 = (0, f.useNavigate)(), { useTranslate: l } = (0, i.useThemeHooks)(), { translate: o } = l(), { pagination: d2, onLocationChange: u2 } = (0, h2.useSettings)(), c = (0, v.usePaginationAwareLocation)(d2, t2.location), x = (m) => {
        m.defaultPrevented || (0, p.userIsForcingNavigation)(m) || (m.preventDefault(), e2(c), u2?.(c));
      }, k = (c.pathname || "") + (c.hash || "");
      return a.default.createElement(r.ButtonWrapper, { align: n2 === "next" ? "right" : "left" }, a.default.createElement(r.NavigationLabel, null, n2 === "next" ? o("page.nextButton", "Next page") : o("page.previousButton", "Previous page")), a.default.createElement(r.StyledButton, { onClick: x, to: k, size: "large", variant: "link", icon: n2 === "next" ? a.default.createElement(i.ArrowRightIcon, null) : a.default.createElement(i.ArrowLeftIcon, null), iconPosition: n2 === "next" ? "right" : "left", "data-testid": `${n2}-button` }, t2.name));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/components/PageNavigation.js
var require_PageNavigation = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/components/PageNavigation.js"(exports, module) {
    var u2 = Object.create;
    var i = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var f = Object.getOwnPropertyNames;
    var g = Object.getPrototypeOf;
    var E = Object.prototype.hasOwnProperty;
    var N = (t2, e2) => {
      for (var r in e2)
        i(t2, r, { get: e2[r], enumerable: true });
    };
    var l = (t2, e2, r, m) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let a of f(e2))
          !E.call(t2, a) && a !== r && i(t2, a, { get: () => e2[a], enumerable: !(m = c(e2, a)) || m.enumerable });
      return t2;
    };
    var v = (t2, e2, r) => (r = t2 != null ? u2(g(t2)) : {}, l(e2 || !t2 || !t2.__esModule ? i(r, "default", { value: t2, enumerable: true }) : r, t2));
    var y = (t2) => l(i({}, "__esModule", { value: true }), t2);
    var B = {};
    N(B, { PageNavigation: () => P });
    module.exports = y(B);
    var n2 = v(__require("react"));
    var o = require_NavigationButton();
    var p = require_styled3();
    function P({ prev: t2, next: e2 }) {
      return !t2 && !e2 ? null : n2.default.createElement(p.PageNavigationWrapper, null, t2 && n2.default.createElement(o.NavigationButton, { item: t2, type: "prev" }), e2 && n2.default.createElement(o.NavigationButton, { item: e2, type: "next" }));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/utils/render-entire-section.js
var require_render_entire_section = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/utils/render-entire-section.js"(exports, module) {
    var l = Object.create;
    var a = Object.defineProperty;
    var p = Object.getOwnPropertyDescriptor;
    var f = Object.getOwnPropertyNames;
    var g = Object.getPrototypeOf;
    var u2 = Object.prototype.hasOwnProperty;
    var E = (e2, t2) => {
      for (var r in t2)
        a(e2, r, { get: t2[r], enumerable: true });
    };
    var m = (e2, t2, r, o) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let n2 of f(t2))
          !u2.call(e2, n2) && n2 !== r && a(e2, n2, { get: () => t2[n2], enumerable: !(o = p(t2, n2)) || o.enumerable });
      return e2;
    };
    var v = (e2, t2, r) => (r = e2 != null ? l(g(e2)) : {}, m(t2 || !e2 || !e2.__esModule ? a(r, "default", { value: e2, enumerable: true }) : r, e2));
    var x = (e2) => m(a({}, "__esModule", { value: true }), e2);
    var F = {};
    E(F, { renderEntireSection: () => d2 });
    module.exports = x(F);
    var i = v(__require("react"));
    var c = require_PageNavigation();
    function d2(e2, t2, r, o) {
      return i.default.createElement(i.default.Fragment, null, o, e2, i.default.createElement(c.PageNavigation, { prev: t2, next: r }));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/utils/render-individual-item.js
var require_render_individual_item = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/utils/render-individual-item.js"(exports, module) {
    var I = Object.create;
    var p = Object.defineProperty;
    var g = Object.getOwnPropertyDescriptor;
    var E = Object.getOwnPropertyNames;
    var R = Object.getPrototypeOf;
    var h2 = Object.prototype.hasOwnProperty;
    var P = (t2, e2) => {
      for (var o in e2)
        p(t2, o, { get: e2[o], enumerable: true });
    };
    var d2 = (t2, e2, o, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of E(e2))
          !h2.call(t2, m) && m !== o && p(t2, m, { get: () => e2[m], enumerable: !(r = g(e2, m)) || r.enumerable });
      return t2;
    };
    var w = (t2, e2, o) => (o = t2 != null ? I(R(t2)) : {}, d2(e2 || !t2 || !t2.__esModule ? p(o, "default", { value: t2, enumerable: true }) : o, t2));
    var F = (t2) => d2(p({}, "__esModule", { value: true }), t2);
    var j = {};
    P(j, { renderIndividualItem: () => b });
    module.exports = F(j);
    var l = w(__require("react"));
    var n2 = __require("react-router-dom");
    var i = require_pagination_item();
    var v = require_join();
    function b(t2, e2, o, r, m) {
      var u2;
      return l.default.createElement(l.default.Fragment, null, l.default.createElement(n2.Routes, null, m ? t2.map((a) => (0, i.toItemRoute)(a, e2)) : t2.map((a, c, f) => (0, i.withPaginationItem)(a, c, f, o, "item")).map((a) => r?.firstItem ? (0, i.withPaginationItemFallback)(a, r.firstItem) : a).map((a) => (0, i.toItemRoute)(a, e2)), l.default.createElement(n2.Route, { path: "*", element: l.default.createElement(n2.Navigate, { to: (0, v.joinPath)(e2, (u2 = t2?.[0]) === null || u2 === void 0 ? void 0 : u2.props.path) }) })));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/utils/index.js
var require_utils2 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/utils/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_join(), module.exports);
    r(o, require_pagination_item(), module.exports);
    r(o, require_pagination_section(), module.exports);
    r(o, require_navigation(), module.exports);
    r(o, require_render_entire_documentation(), module.exports);
    r(o, require_render_individual_sections(), module.exports);
    r(o, require_render_entire_section(), module.exports);
    r(o, require_render_individual_item(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/components/Documentation.js
var require_Documentation = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/components/Documentation.js"(exports, module) {
    var v = Object.create;
    var a = Object.defineProperty;
    var O = Object.getOwnPropertyDescriptor;
    var h2 = Object.getOwnPropertyNames;
    var P = Object.getPrototypeOf;
    var A = Object.prototype.hasOwnProperty;
    var I = (t2, o) => {
      for (var n2 in o)
        a(t2, n2, { get: o[n2], enumerable: true });
    };
    var p = (t2, o, n2, i) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let r of h2(o))
          !A.call(t2, r) && r !== n2 && a(t2, r, { get: () => o[r], enumerable: !(i = O(o, r)) || i.enumerable });
      return t2;
    };
    var R = (t2, o, n2) => (n2 = t2 != null ? v(P(t2)) : {}, p(o || !t2 || !t2.__esModule ? a(n2, "default", { value: t2, enumerable: true }) : n2, t2));
    var T = (t2) => p(a({}, "__esModule", { value: true }), t2);
    var B = {};
    I(B, { Documentation: () => x });
    module.exports = T(B);
    var e2 = R(__require("react"));
    var u2 = __require("react-router-dom");
    var l = require_use_automatic_scroll_by_location();
    var f = require_navigation_context();
    var m = require_utils2();
    var d2 = require_use_base_url_path();
    var g = require_constants();
    var c = require_settings2();
    function x({ header: t2, children: o }) {
      const { pagination: n2, hidePaginationButtons: i } = (0, c.useSettings)();
      (0, l.useAutomaticScrollByLocation)();
      const r = (0, d2.useBaseUrlPath)() || g.ROOT_PATH, s = e2.Children.toArray(o);
      return e2.default.createElement(f.NavigationContextProvider, { value: { prefix: "", navigationId: "", location: c.ROOT_LOCATION } }, e2.default.createElement(u2.Routes, null, n2 === "none" ? (0, m.renderEntireDocumentation)(s, { path: r, header: t2, navigationId: "" }) : (0, m.renderIndividualSections)(s, { hidePaginationButtons: i, parentPath: r })));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/components/Item.js
var require_Item = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/components/Item.js"(exports, module) {
    var E = Object.create;
    var s = Object.defineProperty;
    var I = Object.getOwnPropertyDescriptor;
    var N = Object.getOwnPropertyNames;
    var O = Object.getPrototypeOf;
    var w = Object.prototype.hasOwnProperty;
    var z = (t2, e2) => {
      for (var a in e2)
        s(t2, a, {
          get: e2[a],
          enumerable: true
        });
    };
    var m = (t2, e2, a, c) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let n2 of N(e2))
          !w.call(t2, n2) && n2 !== a && s(t2, n2, {
            get: () => e2[n2],
            enumerable: !(c = I(e2, n2)) || c.enumerable
          });
      return t2;
    };
    var p = (t2, e2, a) => (a = t2 != null ? E(O(t2)) : {}, m(e2 || !t2 || !t2.__esModule ? s(a, "default", {
      value: t2,
      enumerable: true
    }) : a, t2));
    var C = (t2) => m(s({}, "__esModule", {
      value: true
    }), t2);
    var M = {};
    z(M, {
      Item: () => F
    });
    module.exports = C(M);
    var o = p(__require("react"));
    var r = (init_src(), __toCommonJS(src_exports));
    var v = p((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var f = __require("react-router-dom");
    var u2 = require_settings2();
    var b = require_utils();
    var i = require_navigation2();
    function F({
      path: t2,
      prev: e2,
      next: a,
      children: c
    }) {
      const n2 = (0, i.useNavigationContext)(), d2 = (0, i.useCombinedNavigationId)("item", t2), l = (0, u2.useSettings)(), h2 = (0, f.useLocation)().pathname, g = o.default.useMemo(() => Object.assign(Object.assign({}, n2), {
        navigationId: d2,
        location: Object.assign(Object.assign({}, n2.location), {
          item: t2
        })
      }), [n2, d2, t2]), {
        hide: k,
        type: y,
        settings: $
      } = l.feedback || {}, j = y || "sentiment", x = k !== true;
      return o.default.createElement("div", Object.assign({}, (0, b.getNavigationIdProp)(d2)), o.default.createElement(i.NavigationContextProvider, {
        value: g
      }, c, x && o.default.createElement(P, null, o.default.createElement(r.Feedback, {
        type: j,
        settings: $,
        path: S(h2, l.pagination, g)
      })), o.default.createElement(i.PageNavigation, {
        prev: e2,
        next: a
      })));
    }
    var P = v.default.div`
  padding-top: var(--spacing-vertical);
  padding-bottom: var(--spacing-vertical);
  padding-left: calc(var(--spacing-horizontal));
  padding-right: calc(var(--spacing-horizontal));
  width: 100%;
  display: flex;

  @media screen and (min-width: ${r.breakpoints.large}) {
    padding-left: calc(var(--spacing-horizontal) * 2);
    padding-right: calc(var(--spacing-horizontal) * 2);
  }
`;
    function S(t2, e2, a) {
      switch (e2) {
        case "item":
          return `${t2}`;
        case "section":
          return `${t2}#${a.location.item}`;
        case "none":
          return encodeURI(`${t2}#${a.location.section}/${a.location.item}`);
        default:
          return t2;
      }
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/components/Section.js
var require_Section = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/components/Section.js"(exports, module) {
    var N = Object.create;
    var a = Object.defineProperty;
    var P = Object.getOwnPropertyDescriptor;
    var S = Object.getOwnPropertyNames;
    var y = Object.getPrototypeOf;
    var E = Object.prototype.hasOwnProperty;
    var M = (t2, e2) => {
      for (var n2 in e2)
        a(t2, n2, {
          get: e2[n2],
          enumerable: true
        });
    };
    var l = (t2, e2, n2, i) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let o of S(e2))
          !E.call(t2, o) && o !== n2 && a(t2, o, {
            get: () => e2[o],
            enumerable: !(i = P(e2, o)) || i.enumerable
          });
      return t2;
    };
    var u2 = (t2, e2, n2) => (n2 = t2 != null ? N(y(t2)) : {}, l(e2 || !t2 || !t2.__esModule ? a(n2, "default", {
      value: t2,
      enumerable: true
    }) : n2, t2));
    var W = (t2) => l(a({}, "__esModule", {
      value: true
    }), t2);
    var B = {};
    M(B, {
      Section: () => A
    });
    module.exports = W(B);
    var r = u2(__require("react"));
    var v = __require("react-router-dom");
    var f = u2((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var b = require_use_combined_navigation_id();
    var s = require_navigation_context();
    var c = require_utils2();
    var j = require_settings2();
    var C = require_utils();
    var z = f.default.div`
  padding: 0 var(--panel-gap-horizontal);
`;
    function A({
      path: t2,
      header: e2,
      prev: n2,
      next: i,
      children: o
    }) {
      const {
        pathname: I = ""
      } = (0, v.useMatch)(t2) || {}, {
        pagination: O,
        hidePaginationButtons: h2
      } = (0, j.useSettings)(), m = (0, s.useNavigationContext)(), d2 = (0, b.useCombinedNavigationId)("section", t2), g = r.useMemo(() => Object.assign(Object.assign({}, m), {
        navigationId: d2,
        location: Object.assign(Object.assign({}, m.location), {
          section: t2
        })
      }), [m, d2, t2]), p = r.Children.toArray(o), x = r.createElement(z, Object.assign({}, (0, C.getNavigationIdProp)(d2)), e2);
      return r.createElement(s.NavigationContextProvider, {
        value: g
      }, ["none", "section"].includes(O) ? (0, c.renderEntireSection)(p, n2, i, x) : (0, c.renderIndividualItem)(p, I, g.location, i, h2));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/components/ShareLink.js
var require_ShareLink = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/components/ShareLink.js"(exports, module) {
    var p = Object.create;
    var n2 = Object.defineProperty;
    var h2 = Object.getOwnPropertyDescriptor;
    var l = Object.getOwnPropertyNames;
    var v = Object.getPrototypeOf;
    var u2 = Object.prototype.hasOwnProperty;
    var I = (e2, t2) => {
      for (var i in t2)
        n2(e2, i, { get: t2[i], enumerable: true });
    };
    var c = (e2, t2, i, m) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let o of l(t2))
          !u2.call(e2, o) && o !== i && n2(e2, o, { get: () => t2[o], enumerable: !(m = h2(t2, o)) || m.enumerable });
      return e2;
    };
    var N = (e2, t2, i) => (i = e2 != null ? p(v(e2)) : {}, c(t2 || !e2 || !e2.__esModule ? n2(i, "default", { value: e2, enumerable: true }) : i, e2));
    var b = (e2) => c(n2({}, "__esModule", { value: true }), e2);
    var E = {};
    I(E, { ShareLink: () => k });
    module.exports = b(E);
    var r = N(__require("react"));
    var s = require_common();
    var g = require_navigation_context();
    var a = require_styled3();
    var d2 = require_utils();
    var f = require_use_combined_navigation_id();
    function k(e2) {
      const { location: t2 } = (0, g.useNavigationContext)(), i = (0, f.useCombinedNavigationId)(t2.item ? "item" : "section", t2.item || t2.section);
      return r.default.createElement(a.ShareLinkWrapper, Object.assign({}, e2), r.default.createElement(a.ShareIconLink, { size: e2.size, to: t2 }), r.default.createElement(s.HiddenNavigationAnchor, Object.assign({}, (0, d2.getNavigationIdProp)(i))), e2.children);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/components/index.js
var require_components3 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/components/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_Documentation(), module.exports);
    r(o, require_Item(), module.exports);
    r(o, require_PaginationAwareLink(), module.exports);
    r(o, require_Section(), module.exports);
    r(o, require_ShareLink(), module.exports);
    r(o, require_PageNavigation(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/router/AppRouter.js
var require_AppRouter = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/router/AppRouter.js"(exports, module) {
    var p = Object.create;
    var n2 = Object.defineProperty;
    var s = Object.getOwnPropertyDescriptor;
    var a = Object.getOwnPropertyNames;
    var c = Object.getPrototypeOf;
    var i = Object.prototype.hasOwnProperty;
    var x = (t2, e2) => {
      for (var o in e2)
        n2(t2, o, { get: e2[o], enumerable: true });
    };
    var l = (t2, e2, o, R) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let r of a(e2))
          !i.call(t2, r) && r !== o && n2(t2, r, { get: () => e2[r], enumerable: !(R = s(e2, r)) || R.enumerable });
      return t2;
    };
    var f = (t2, e2, o) => (o = t2 != null ? p(c(t2)) : {}, l(e2 || !t2 || !t2.__esModule ? n2(o, "default", { value: t2, enumerable: true }) : o, t2));
    var C = (t2) => l(n2({}, "__esModule", { value: true }), t2);
    var E = {};
    x(E, { AppRouter: () => I });
    module.exports = C(E);
    var u2 = f(__require("react"));
    var m = __require("react-router-dom");
    function I({ children: t2 }) {
      return (0, m.useInRouterContext)() ? u2.default.createElement(u2.default.Fragment, null, t2) : u2.default.createElement(m.BrowserRouter, null, t2);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/router/index.js
var require_router = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/router/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var p = (r, o, f, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of c(o))
          !d2.call(r, e2) && e2 !== f && a(r, e2, { get: () => o[e2], enumerable: !(x = b(o, e2)) || x.enumerable });
      return r;
    };
    var t2 = (r, o, f) => (p(r, o, "default"), f && p(f, o, "default"));
    var g = (r) => p(a({}, "__esModule", { value: true }), r);
    var m = {};
    module.exports = g(m);
    t2(m, require_AppRouter(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/navigation/index.js
var require_navigation2 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/navigation/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_components3(), module.exports);
    r(o, require_hooks(), module.exports);
    r(o, require_router(), module.exports);
    r(o, require_navigation_context(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/ReturnTypeDetails.js
var require_ReturnTypeDetails = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/ReturnTypeDetails.js"(exports, module) {
    var b = Object.create;
    var i = Object.defineProperty;
    var E = Object.getOwnPropertyDescriptor;
    var q = Object.getOwnPropertyNames;
    var w = Object.getPrototypeOf;
    var x = Object.prototype.hasOwnProperty;
    var k = (e2, t2) => {
      for (var n2 in t2)
        i(e2, n2, {
          get: t2[n2],
          enumerable: true
        });
    };
    var d2 = (e2, t2, n2, l) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let o of q(t2))
          !x.call(e2, o) && o !== n2 && i(e2, o, {
            get: () => t2[o],
            enumerable: !(l = E(t2, o)) || l.enumerable
          });
      return e2;
    };
    var f = (e2, t2, n2) => (n2 = e2 != null ? b(w(e2)) : {}, d2(t2 || !e2 || !e2.__esModule ? i(n2, "default", {
      value: e2,
      enumerable: true
    }) : n2, e2));
    var z = (e2) => d2(i({}, "__esModule", {
      value: true
    }), e2);
    var M = {};
    k(M, {
      ReturnTypeDetails: () => L
    });
    module.exports = z(M);
    var r = f(__require("react"));
    var p = f((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var g = (init_src(), __toCommonJS(src_exports));
    var y = require_ReturnTypeView();
    var h2 = require_menu();
    var v = require_navigation2();
    var N = require_schema();
    var c = require_common2();
    function R({
      type: e2,
      inline: t2 = false,
      size: n2 = "base",
      deprecated: l = false,
      required: o = false
    }) {
      const s = r.useContext(h2.MenuStoreContext), {
        useTranslate: $
      } = (0, g.useThemeHooks)(), {
        translate: m
      } = $(), a = (0, N.toReturnType)(e2), u2 = s?.getGroupIdByTypeName(a.name), T = a.isListNonNull || a.isNonNull;
      return u2 ? r.createElement(C, {
        "data-testid": "return-type",
        inline: t2,
        size: n2
      }, r.createElement(v.PaginationAwareLink, {
        to: {
          section: u2,
          item: a.name
        }
      }, r.createElement(y.ReturnTypeView, {
        type: a
      })), T ? r.createElement(r.Fragment, null, ",", r.createElement(D, {
        size: n2
      }, m("graphql.nonNull", "non-null"))) : null, l && r.createElement(c.Message, {
        "data-testid": "deprecated",
        type: "warning"
      }, m("graphql.deprecated", "deprecated")), o && r.createElement(c.Message, {
        "data-testid": "required-arg",
        type: "error"
      }, m("graphql.required", "required"))) : null;
    }
    var L = r.memo(R);
    var C = p.default.div`
  display: ${({
      inline: e2
    }) => e2 ? "inline-block" : "block"};
  font-size: ${({
      size: e2
    }) => `var(--font-size-${e2})`};
  line-height: ${({
      size: e2
    }) => `var(--line-height-${e2})`};
  font-family: var(--font-family-base);
`;
    var D = p.default.span`
  display: inline-block;
  color: var(--text-color-secondary);
  font-weight: var(--font-weight-regular);
  font-size: ${({
      size: e2
    }) => `var(--font-size-${e2})`};
  line-height: ${({
      size: e2
    }) => `var(--line-height-${e2})`};
  margin-left: var(--spacing-xxs);
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/FieldList.js
var require_FieldList = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/FieldList.js"(exports, module) {
    var i = Object.create;
    var r = Object.defineProperty;
    var p = Object.getOwnPropertyDescriptor;
    var l = Object.getOwnPropertyNames;
    var s = Object.getPrototypeOf;
    var u2 = Object.prototype.hasOwnProperty;
    var F = (t2, e2) => {
      for (var m in e2)
        r(t2, m, { get: e2[m], enumerable: true });
    };
    var c = (t2, e2, m, o) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let n2 of l(e2))
          !u2.call(t2, n2) && n2 !== m && r(t2, n2, { get: () => e2[n2], enumerable: !(o = p(e2, n2)) || o.enumerable });
      return t2;
    };
    var L = (t2, e2, m) => (m = t2 != null ? i(s(t2)) : {}, c(e2 || !t2 || !t2.__esModule ? r(m, "default", { value: t2, enumerable: true }) : m, t2));
    var f = (t2) => c(r({}, "__esModule", { value: true }), t2);
    var x = {};
    F(x, { FieldList: () => k });
    module.exports = f(x);
    var a = L(__require("react"));
    function g({ contrast: t2, fields: e2, fieldExpandLevel: m = 0, renderField: o }) {
      return a.default.createElement(a.default.Fragment, null, e2.map((n2) => o && o({ contrast: t2, field: n2, key: n2.name, fieldExpandLevel: m })));
    }
    var k = (0, a.memo)(g);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/FieldsTypeItemEmbedded.js
var require_FieldsTypeItemEmbedded = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/FieldsTypeItemEmbedded.js"(exports, module) {
    var a = Object.create;
    var m = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var F = Object.getOwnPropertyNames;
    var v = Object.getPrototypeOf;
    var y = Object.prototype.hasOwnProperty;
    var E = (e2, t2) => {
      for (var o in t2)
        m(e2, o, { get: t2[o], enumerable: true });
    };
    var n2 = (e2, t2, o, d2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let i of F(t2))
          !y.call(e2, i) && i !== o && m(e2, i, { get: () => t2[i], enumerable: !(d2 = f(t2, i)) || d2.enumerable });
      return e2;
    };
    var b = (e2, t2, o) => (o = e2 != null ? a(v(e2)) : {}, n2(t2 || !e2 || !e2.__esModule ? m(o, "default", { value: e2, enumerable: true }) : o, e2));
    var g = (e2) => n2(m({}, "__esModule", { value: true }), e2);
    var j = {};
    E(j, { FieldsTypeItemEmbedded: () => x });
    module.exports = g(j);
    var r = b(__require("react"));
    var s = require_common2();
    var p = require_app();
    var c = require_schema();
    var u2 = require_FieldList();
    function T({ name: e2, contrast: t2, renderField: o, fieldExpandLevel: d2 }) {
      const i = (0, p.useStore)(), l = (0, r.useMemo)(() => i?.getType(e2), [e2, i]);
      return (0, c.isFieldsType)(l) ? r.default.createElement(s.Box, { "data-testid": l.name, direction: "column" }, Object.values(l.getFields()).length ? r.default.createElement(u2.FieldList, { fields: Object.values(l.getFields()), contrast: t2, renderField: o, fieldExpandLevel: d2 }) : null) : null;
    }
    var x = (0, r.memo)(T);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/styled.js
var require_styled4 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/styled.js"(exports, module) {
    var g = Object.create;
    var i = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var x = Object.getPrototypeOf;
    var h2 = Object.prototype.hasOwnProperty;
    var y = (o, a) => {
      for (var e2 in a)
        i(o, e2, {
          get: a[e2],
          enumerable: true
        });
    };
    var d2 = (o, a, e2, l) => {
      if (a && typeof a == "object" || typeof a == "function")
        for (let t2 of b(a))
          !h2.call(o, t2) && t2 !== e2 && i(o, t2, {
            get: () => a[t2],
            enumerable: !(l = f(a, t2)) || l.enumerable
          });
      return o;
    };
    var u2 = (o, a, e2) => (e2 = o != null ? g(x(o)) : {}, d2(a || !o || !o.__esModule ? i(e2, "default", {
      value: o,
      enumerable: true
    }) : e2, o));
    var $ = (o) => d2(i({}, "__esModule", {
      value: true
    }), o);
    var L = {};
    y(L, {
      Annotation: () => c,
      ArgsCollapsed: () => k,
      Breadcrumbs: () => S,
      ExpandableFieldBody: () => N,
      FieldInfo: () => H,
      FieldName: () => I,
      FieldNameBox: () => F,
      FieldWrapper: () => v,
      InlineCode: () => B,
      PanelAnnotation: () => w,
      PanelBody: () => E,
      PanelHeading: () => q,
      PanelWrapper: () => W,
      ReferenceField: () => P,
      ScalarAnnotation: () => z,
      SchemaItemWrapper: () => A,
      Value: () => C,
      requiredCss: () => m
    });
    module.exports = $(L);
    var r = u2((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var s = (init_src(), __toCommonJS(src_exports));
    var p = require_shared();
    var n2 = require_common2();
    var c = r.default.div`
  margin-bottom: var(--spacing-base);
  color: var(--text-color-primary);
  word-break: break-all;
`;
    var w = (0, r.default)(c)`
  margin-bottom: 0;
  color: var(--text-color-secondary);
`;
    var z = (0, r.default)(c)`
  margin-top: var(--spacing-base);
`;
    var k = r.default.span`
  font-size: ${({
      size: o
    }) => `var(--font-size-${o})`};
  line-height: ${({
      size: o
    }) => `var(--line-height-${o})`};
  white-space: normal;

  margin-left: var(--spacing-unit);
  color: var(--text-color-description);
  font-weight: var(--font-weight-bold);
`;
    var m = r.css`
  &:after {
    content: '*';
    vertical-align: text-bottom;
    color: var(--schema-property-required-label-text-color);
  }
`;
    var B = r.default.span`
  border: 1px solid var(--schema-inline-border-color);
  border-radius: var(--border-radius);
  padding: 0 4px;
  background-color: var(--schema-inline-background-color);

  color: var(--schema-inline-code-text-color);

  ${({
      required: o
    }) => o ? m : null}
`;
    var v = r.default.div`
  padding: var(--spacing-base) 0;

  ${({
      firstOfType: o
    }) => o && r.css`
      padding-top: var(--spacing-sm);
    `};

  ${({
      showBorder: o
    }) => o ? r.css`
          border-bottom: 1px solid var(--border-color-primary);
        ` : r.css`
          padding-bottom: 0;
        `}

  &:first-of-type {
    padding-top: var(--spacing-sm);
  }
`;
    var F = (0, r.default)(n2.Box)`
  display: flex;
  gap: var(--spacing-unit);
  overflow: hidden;
  color: var(--schemas-property-name-text-color);
  font-size: var(--schemas-property-name-font-size);
  font-family: var(--schemas-property-name-font-family);

  ${p.ShelfIcon} {
    margin-right: 4px;
    height: var(--schema-chevron-size);
    width: var(--schema-chevron-size);
    fill: var(--schema-chevron-color);
  }
`;
    var A = r.default.div`
  border-bottom: 1px solid var(--border-color-primary);
  padding: var(--spacing-sm) 0;
`;
    var I = r.default.span`
  color: var(--schemas-property-name-text-color);
  font-size: var(--schemas-property-name-font-size);
  font-family: var(--schemas-property-name-font-family);
  font-weight: var(--font-weight-bold);

  ${({
      isDeprecated: o
    }) => r.css`
    text-decoration: ${o && "line-through"};
  `};
`;
    var P = r.default.span`
  color: var(--text-color-secondary);

  ${n2.AnchorLink} {
    color: var(--text-color-secondary);
  }
`;
    var C = r.default.div`
  display: flex;
  font-family: var(--font-family-base);
  font-size: var(--font-size-base);
  font-weight: var(--font-weight-semibold);
  line-height: var(--line-height-md);
  color: var(--text-color-secondary);
`;
    var N = r.default.div`
  display: flex;
  flex-direction: column;
  ${({
      contrast: o
    }) => r.css`
    border-radius: var(--panel-border-radius);
    background-color: ${o ? "var(--schema-nested-background-color)" : ""};
  `};

  ${v} {
    background-color: transparent;
  }

  h4 {
    margin-top: 0;
  }
`;
    var S = (0, r.default)(s.Breadcrumbs)`
  padding: var(--panel-gap-vertical) var(--panel-gap-horizontal);
  padding-bottom: 0;
`;
    var W = r.default.div.attrs({
      className: "panel-overview"
    })`
  border: 1px solid var(--border-color-secondary);
  border-radius: var(--border-radius);
  background-color: var(--panel-bg-color);

  @media screen and (min-width: ${s.breakpoints.large}) {
    margin-top: ${({
      $spacing: o = "base"
    }) => `var(--spacing-${o})`};
  }
`;
    var q = r.default.div`
  font-family: var(--font-family-base);
  font-size: var(--font-size-base);
  font-weight: var(--font-weight-semibold);
  padding: calc(var(--spacing-sm) + 2px) var(--spacing-md) calc(var(--spacing-xs) + 2px);
  border-bottom: 1px solid var(--border-color-secondary);
`;
    var E = r.default.div`
  display: flex;
  flex-direction: column;
  padding: var(--spacing-sm) var(--spacing-md) var(--spacing-md);
  gap: var(--spacing-xs);
  max-height: 400px;
  overflow-x: auto;
`;
    var H = (0, r.default)(n2.Box)`
  border-bottom: ${({
      expanded: o
    }) => o ? "none!important" : "inherit"};
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/GroupTypeList.js
var require_GroupTypeList = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/GroupTypeList.js"(exports, module) {
    var w = Object.create;
    var i = Object.defineProperty;
    var g = Object.getOwnPropertyDescriptor;
    var k = Object.getOwnPropertyNames;
    var v = Object.getPrototypeOf;
    var E = Object.prototype.hasOwnProperty;
    var L = (e2, t2) => {
      for (var n2 in t2)
        i(e2, n2, {
          get: t2[n2],
          enumerable: true
        });
    };
    var d2 = (e2, t2, n2, o) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let m of k(t2))
          !E.call(e2, m) && m !== n2 && i(e2, m, {
            get: () => t2[m],
            enumerable: !(o = g(t2, m)) || o.enumerable
          });
      return e2;
    };
    var s = (e2, t2, n2) => (n2 = e2 != null ? w(v(e2)) : {}, d2(t2 || !e2 || !e2.__esModule ? i(n2, "default", {
      value: e2,
      enumerable: true
    }) : n2, e2));
    var S = (e2) => d2(i({}, "__esModule", {
      value: true
    }), e2);
    var A = {};
    L(A, {
      GroupTypeList: () => x
    });
    module.exports = S(A);
    var r = s(__require("react"));
    var p = s((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var a = require_styled4();
    var c = require_markdown();
    var l = require_menu();
    var u2 = require_navigation2();
    var f = require_settings2();
    function h2({
      types: e2
    }) {
      const {
        markdown: t2
      } = (0, f.useSettings)(), n2 = r.useContext(l.MenuStoreContext);
      return r.createElement(r.Fragment, null, e2.map((o) => r.createElement(a.SchemaItemWrapper, {
        "data-testid": "item",
        key: o.name
      }, r.createElement(y, {
        "data-testid": "item-name",
        to: {
          section: n2?.getGroupIdByTypeName(o.name),
          item: o.name
        }
      }, o.name), r.createElement(a.PanelAnnotation, {
        "data-testid": "item-description"
      }, r.createElement(c.Markdown, {
        rawMarkdown: o.description,
        parser: t2?.parser,
        isField: true
      })))));
    }
    var x = r.memo(h2);
    var y = (0, p.default)(u2.PaginationAwareLink)`
  font-weight: var(--font-weight-medium);
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/UnionItemEmbedded.js
var require_UnionItemEmbedded = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/UnionItemEmbedded.js"(exports, module) {
    var c = Object.create;
    var r = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var y = Object.getOwnPropertyNames;
    var T = Object.getPrototypeOf;
    var g = Object.prototype.hasOwnProperty;
    var E = (e2, o) => {
      for (var t2 in o)
        r(e2, t2, { get: o[t2], enumerable: true });
    };
    var p = (e2, o, t2, i) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let m of y(o))
          !g.call(e2, m) && m !== t2 && r(e2, m, { get: () => o[m], enumerable: !(i = f(o, m)) || i.enumerable });
      return e2;
    };
    var a = (e2, o, t2) => (t2 = e2 != null ? c(T(e2)) : {}, p(o || !e2 || !e2.__esModule ? r(t2, "default", { value: e2, enumerable: true }) : t2, e2));
    var U = (e2) => p(r({}, "__esModule", { value: true }), e2);
    var x = {};
    E(x, { UnionItemEmbedded: () => v });
    module.exports = U(x);
    var s = (init_graphql2(), __toCommonJS(graphql_exports));
    var n2 = a(__require("react"));
    var d2 = require_app();
    var l = require_GroupTypeList();
    var u2 = require_common2();
    function b({ name: e2 }) {
      const o = (0, d2.useStore)(), t2 = (0, n2.useMemo)(() => o?.getType(e2), [e2, o]);
      return (0, s.isUnionType)(t2) ? n2.default.createElement(u2.Box, { "data-testid": e2, direction: "column" }, t2.getTypes().length ? n2.default.createElement(l.GroupTypeList, { types: t2.getTypes() }) : null) : null;
    }
    var v = (0, n2.memo)(b);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/ValueList.js
var require_ValueList = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/ValueList.js"(exports, module) {
    var f = Object.create;
    var m = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var E = Object.getOwnPropertyNames;
    var k = Object.getPrototypeOf;
    var w = Object.prototype.hasOwnProperty;
    var y = (e2, t2) => {
      for (var r in t2)
        m(e2, r, {
          get: t2[r],
          enumerable: true
        });
    };
    var d2 = (e2, t2, r, i) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let o of E(t2))
          !w.call(e2, o) && o !== r && m(e2, o, {
            get: () => t2[o],
            enumerable: !(i = u2(t2, o)) || i.enumerable
          });
      return e2;
    };
    var l = (e2, t2, r) => (r = e2 != null ? f(k(e2)) : {}, d2(t2 || !e2 || !e2.__esModule ? m(r, "default", {
      value: e2,
      enumerable: true
    }) : r, e2));
    var S = (e2) => d2(m({}, "__esModule", {
      value: true
    }), e2);
    var x = {};
    y(x, {
      ValueList: () => V
    });
    module.exports = S(x);
    var a = l(__require("react"));
    var c = l((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var n2 = require_styled4();
    var p = require_settings2();
    var s = require_markdown();
    function V({
      values: e2
    }) {
      const {
        markdown: t2
      } = (0, p.useSettings)();
      return a.default.createElement(a.default.Fragment, null, e2.map((r) => a.default.createElement(n2.SchemaItemWrapper, {
        "data-testid": "item",
        key: r.name
      }, a.default.createElement(g, {
        "data-testid": "name"
      }, r.name), a.default.createElement(n2.PanelAnnotation, {
        "data-testid": r.name + "-description"
      }, a.default.createElement(s.Markdown, {
        rawMarkdown: r.description,
        parser: t2?.parser,
        isField: true
      })))));
    }
    var g = (0, c.default)(n2.Value)`
  color: var(--text-color-primary);
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/EnumItemEmbedded.js
var require_EnumItemEmbedded = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/EnumItemEmbedded.js"(exports, module) {
    var a = Object.create;
    var r = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var E = Object.getOwnPropertyNames;
    var f = Object.getPrototypeOf;
    var g = Object.prototype.hasOwnProperty;
    var v = (e2, t2) => {
      for (var o in t2)
        r(e2, o, { get: t2[o], enumerable: true });
    };
    var l = (e2, t2, o, u2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let n2 of E(t2))
          !g.call(e2, n2) && n2 !== o && r(e2, n2, { get: () => t2[n2], enumerable: !(u2 = c(t2, n2)) || u2.enumerable });
      return e2;
    };
    var y = (e2, t2, o) => (o = e2 != null ? a(f(e2)) : {}, l(t2 || !e2 || !e2.__esModule ? r(o, "default", { value: e2, enumerable: true }) : o, e2));
    var V = (e2) => l(r({}, "__esModule", { value: true }), e2);
    var I = {};
    v(I, { EnumItemEmbedded: () => x });
    module.exports = V(I);
    var i = (init_graphql2(), __toCommonJS(graphql_exports));
    var m = y(__require("react"));
    var s = require_common2();
    var d2 = require_app();
    var p = require_ValueList();
    function b({ name: e2 }) {
      const t2 = (0, d2.useStore)(), o = (0, m.useMemo)(() => t2?.getType(e2), [e2, t2]);
      return (0, i.isEnumType)(o) ? m.default.createElement(s.Box, { "data-testid": o.name, direction: "column" }, o.getValues().length ? m.default.createElement(p.ValueList, { values: o.getValues() }) : null) : null;
    }
    var x = (0, m.memo)(b);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/ScalarItemEmbedded.js
var require_ScalarItemEmbedded = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/ScalarItemEmbedded.js"(exports, module) {
    var u2 = Object.create;
    var a = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var E = Object.getOwnPropertyNames;
    var S = Object.getPrototypeOf;
    var v = Object.prototype.hasOwnProperty;
    var w = (e2, t2) => {
      for (var o in t2)
        a(e2, o, { get: t2[o], enumerable: true });
    };
    var i = (e2, t2, o, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let m of E(t2))
          !v.call(e2, m) && m !== o && a(e2, m, { get: () => t2[m], enumerable: !(n2 = f(t2, m)) || n2.enumerable });
      return e2;
    };
    var b = (e2, t2, o) => (o = e2 != null ? u2(S(e2)) : {}, i(t2 || !e2 || !e2.__esModule ? a(o, "default", { value: e2, enumerable: true }) : o, e2));
    var k = (e2) => i(a({}, "__esModule", { value: true }), e2);
    var x = {};
    w(x, { ScalarItemEmbedded: () => g });
    module.exports = k(x);
    var r = b(__require("react"));
    var d2 = require_styled4();
    var c = require_common2();
    var s = require_app();
    var p = require_markdown();
    var l = require_settings2();
    function M({ name: e2 }) {
      const t2 = (0, s.useStore)(), { markdown: o } = (0, l.useSettings)(), n2 = (0, r.useMemo)(() => t2?.getType(e2), [e2, t2]);
      return n2 ? r.default.createElement(c.Box, { "data-testid": n2.name, direction: "column" }, r.default.createElement(d2.ScalarAnnotation, { "data-testid": "object-description" }, r.default.createElement(p.Markdown, { rawMarkdown: n2.description, parser: o?.parser }))) : null;
    }
    var g = (0, r.memo)(M);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/TypeView.js
var require_TypeView = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/TypeView.js"(exports, module) {
    var y = Object.create;
    var a = Object.defineProperty;
    var E = Object.getOwnPropertyDescriptor;
    var T = Object.getOwnPropertyNames;
    var u2 = Object.getPrototypeOf;
    var b = Object.prototype.hasOwnProperty;
    var I = (e2, m) => {
      for (var r in m)
        a(e2, r, { get: m[r], enumerable: true });
    };
    var d2 = (e2, m, r, o) => {
      if (m && typeof m == "object" || typeof m == "function")
        for (let n2 of T(m))
          !b.call(e2, n2) && n2 !== r && a(e2, n2, { get: () => m[n2], enumerable: !(o = E(m, n2)) || o.enumerable });
      return e2;
    };
    var j = (e2, m, r) => (r = e2 != null ? y(u2(e2)) : {}, d2(m || !e2 || !e2.__esModule ? a(r, "default", { value: e2, enumerable: true }) : r, e2));
    var w = (e2) => d2(a({}, "__esModule", { value: true }), e2);
    var O = {};
    I(O, { TypeView: () => F });
    module.exports = w(O);
    var i = j(__require("react"));
    var t2 = (init_graphql2(), __toCommonJS(graphql_exports));
    var c = require_FieldsTypeItemEmbedded();
    var l = require_UnionItemEmbedded();
    var s = require_EnumItemEmbedded();
    var f = require_ScalarItemEmbedded();
    function x({ type: e2, contrast: m, renderField: r, fieldExpandLevel: o = 0 }) {
      const n2 = (0, t2.getNamedType)(e2), { name: p } = n2;
      return (0, t2.isEnumType)(n2) ? i.default.createElement(s.EnumItemEmbedded, { name: p }) : (0, t2.isScalarType)(n2) ? i.default.createElement(f.ScalarItemEmbedded, { name: p }) : (0, t2.isObjectType)(n2) || (0, t2.isInterfaceType)(n2) || (0, t2.isInputObjectType)(n2) ? i.default.createElement(c.FieldsTypeItemEmbedded, { contrast: m, name: p, fieldExpandLevel: o, renderField: r }) : (0, t2.isUnionType)(n2) ? i.default.createElement(l.UnionItemEmbedded, { name: p }) : null;
    }
    var F = (0, i.memo)(x);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/InlineArgsList.js
var require_InlineArgsList = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/InlineArgsList.js"(exports, module) {
    var m = Object.create;
    var i = Object.defineProperty;
    var s = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var u2 = Object.getPrototypeOf;
    var c = Object.prototype.hasOwnProperty;
    var f = (e2, t2) => {
      for (var l in t2)
        i(e2, l, { get: t2[l], enumerable: true });
    };
    var r = (e2, t2, l, o) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let n2 of p(t2))
          !c.call(e2, n2) && n2 !== l && i(e2, n2, { get: () => t2[n2], enumerable: !(o = s(t2, n2)) || o.enumerable });
      return e2;
    };
    var g = (e2, t2, l) => (l = e2 != null ? m(u2(e2)) : {}, r(t2 || !e2 || !e2.__esModule ? i(l, "default", { value: e2, enumerable: true }) : l, e2));
    var h2 = (e2) => r(i({}, "__esModule", { value: true }), e2);
    var A = {};
    f(A, { InlineArgsList: () => v });
    module.exports = h2(A);
    var a = g(__require("react"));
    var d2 = require_styled4();
    function v({ args: e2, size: t2 = "base" }) {
      return e2?.length ? a.default.createElement(d2.ArgsCollapsed, { "data-testid": "collapsed-args", size: t2 }, "(", e2.length === 1 ? e2[0].name : "...args", ")") : null;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/DefaultValue.js
var require_DefaultValue = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/DefaultValue.js"(exports, module) {
    var f = Object.create;
    var l = Object.defineProperty;
    var m = Object.getOwnPropertyDescriptor;
    var d2 = Object.getOwnPropertyNames;
    var c = Object.getPrototypeOf;
    var p = Object.prototype.hasOwnProperty;
    var g = (e2, t2) => {
      for (var a in t2)
        l(e2, a, { get: t2[a], enumerable: true });
    };
    var i = (e2, t2, a, u2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let n2 of d2(t2))
          !p.call(e2, n2) && n2 !== a && l(e2, n2, { get: () => t2[n2], enumerable: !(u2 = m(t2, n2)) || u2.enumerable });
      return e2;
    };
    var h2 = (e2, t2, a) => (a = e2 != null ? f(c(e2)) : {}, i(t2 || !e2 || !e2.__esModule ? l(a, "default", { value: e2, enumerable: true }) : a, e2));
    var v = (e2) => i(l({}, "__esModule", { value: true }), e2);
    var E = {};
    g(E, { DefaultValue: () => D });
    module.exports = v(E);
    var r = h2(__require("react"));
    var s = (init_src(), __toCommonJS(src_exports));
    var o = require_styled4();
    function D({ defaultValue: e2 }) {
      const { useTranslate: t2 } = (0, s.useThemeHooks)(), { translate: a } = t2();
      return e2 !== void 0 ? r.default.createElement(o.PanelAnnotation, { "data-testid": "default-value" }, a("graphql.defaultValue", "Default"), ":", r.default.createElement(o.InlineCode, { "data-testid": "value" }, JSON.stringify(e2))) : null;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/schema/helpers/is-deprecated.js
var require_is_deprecated = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/schema/helpers/is-deprecated.js"(exports, module) {
    var i = Object.defineProperty;
    var a = Object.getOwnPropertyDescriptor;
    var t2 = Object.getOwnPropertyNames;
    var u2 = Object.prototype.hasOwnProperty;
    var l = (e2, n2) => {
      for (var d2 in n2)
        i(e2, d2, { get: n2[d2], enumerable: true });
    };
    var p = (e2, n2, d2, o) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let r of t2(n2))
          !u2.call(e2, r) && r !== d2 && i(e2, r, { get: () => n2[r], enumerable: !(o = a(n2, r)) || o.enumerable });
      return e2;
    };
    var c = (e2) => p(i({}, "__esModule", { value: true }), e2);
    var x = {};
    l(x, { isDeprecatedDirective: () => f, isDeprecatedField: () => v });
    module.exports = c(x);
    function v(e2) {
      return !!e2.deprecationReason;
    }
    function f(e2) {
      var n2;
      return ((n2 = e2?.name) === null || n2 === void 0 ? void 0 : n2.value) === "deprecated";
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/FieldRow.js
var require_FieldRow = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/FieldRow.js"(exports, module) {
    var q = Object.create;
    var d2 = Object.defineProperty;
    var T = Object.getOwnPropertyDescriptor;
    var A = Object.getOwnPropertyNames;
    var M = Object.getPrototypeOf;
    var V = Object.prototype.hasOwnProperty;
    var _ = (e2, r) => {
      for (var a in r)
        d2(e2, a, { get: r[a], enumerable: true });
    };
    var g = (e2, r, a, i) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let n2 of A(r))
          !V.call(e2, n2) && n2 !== a && d2(e2, n2, { get: () => r[n2], enumerable: !(i = T(r, n2)) || i.enumerable });
      return e2;
    };
    var $ = (e2, r, a) => (a = e2 != null ? q(M(e2)) : {}, g(r || !e2 || !e2.__esModule ? d2(a, "default", { value: e2, enumerable: true }) : a, e2));
    var b = (e2) => g(d2({}, "__esModule", { value: true }), e2);
    var N = {};
    _(N, { FieldRow: () => I });
    module.exports = b(N);
    var t2 = $(__require("react"));
    var v = (init_graphql2(), __toCommonJS(graphql_exports));
    var l = (init_src(), __toCommonJS(src_exports));
    var E = require_shared();
    var w = require_InlineArgsList();
    var R = require_DefaultValue();
    var y = require_ReturnTypeDetails();
    var o = require_styled4();
    var p = require_markdown();
    var F = require_settings2();
    var f = require_common2();
    var x = require_is_deprecated();
    function B({ field: e2, expanded: r, isArgument: a }) {
      var i, n2, s;
      const { markdown: m } = (0, F.useSettings)(), { useTranslate: D } = (0, l.useThemeHooks)(), { translate: h2 } = D(), u2 = (0, x.isDeprecatedField)(e2), k = a && (0, v.isRequiredArgument)(e2), c = a ? "argument" : "field";
      return t2.default.createElement(f.Box, { "data-testid": `${c}-item`, direction: "column", rowGap: 0.5 }, t2.default.createElement(o.FieldNameBox, { "data-testid": e2.name, direction: !((i = e2.args) === null || i === void 0) && i.length ? "column" : "row" }, t2.default.createElement("div", null, t2.default.createElement(o.FieldName, { "data-testid": `${c}-name`, isDeprecated: u2 }, t2.default.createElement(E.SplittedWbrText, { text: e2.name })), !((n2 = e2.args) === null || n2 === void 0) && n2.length ? t2.default.createElement(w.InlineArgsList, { args: e2.args }) : null), t2.default.createElement(y.ReturnTypeDetails, { "data-testid": "return-type", type: e2.type, inline: !(!((s = e2.args) === null || s === void 0) && s.length), deprecated: u2, required: k })), t2.default.createElement(o.FieldInfo, { gutter: 0, expanded: r, direction: "column", "data-testid": e2.name + "-type" }, t2.default.createElement(R.DefaultValue, { "data-testid": "default-value", defaultValue: e2.defaultValue }), t2.default.createElement(o.PanelAnnotation, { "data-testid": `${c}-description` }, t2.default.createElement(p.Markdown, { rawMarkdown: e2.description, parser: m?.parser, isField: true })), e2.deprecationReason && t2.default.createElement(l.Admonition, { name: h2("graphql.deprecationReason", "Deprecation reason"), type: "warning" }, t2.default.createElement(p.Markdown, { rawMarkdown: e2.deprecationReason, parser: m?.parser }))));
    }
    var I = (0, t2.memo)(B);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/utils/is-field-expandable.js
var require_is_field_expandable = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/utils/is-field-expandable.js"(exports, module) {
    var i = Object.defineProperty;
    var s = Object.getOwnPropertyDescriptor;
    var u2 = Object.getOwnPropertyNames;
    var v = Object.prototype.hasOwnProperty;
    var g = (e2, n2) => {
      for (var a in n2)
        i(e2, a, { get: n2[a], enumerable: true });
    };
    var x = (e2, n2, a, r) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let o of u2(n2))
          !v.call(e2, o) && o !== a && i(e2, o, { get: () => n2[o], enumerable: !(r = s(n2, o)) || r.enumerable });
      return e2;
    };
    var c = (e2) => x(i({}, "__esModule", { value: true }), e2);
    var E = {};
    g(E, { useIsFieldExpandable: () => d2, useShowExpandableBorder: () => m });
    module.exports = c(E);
    var t2 = (init_graphql2(), __toCommonJS(graphql_exports));
    var l = require_settings2();
    function d2(e2, n2) {
      var a;
      const r = (0, l.useSettings)().fieldExpandLevel;
      return (!!(!((a = e2.args) === null || a === void 0) && a.length) || !(0, t2.isScalarType)((0, t2.getNamedType)(e2.type))) && n2 < r;
    }
    function m(e2, n2, { expandedTypes: a, expandedArguments: r }) {
      var o;
      const p = d2(e2, n2);
      return !p || p && !a || p && !!(!((o = e2.args) === null || o === void 0) && o.length) && !r && (0, t2.isScalarType)((0, t2.getNamedType)(e2.type));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/icons/CircleIcon.js
var require_CircleIcon = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/icons/CircleIcon.js"(exports, module) {
    var d2 = Object.create;
    var n2 = Object.defineProperty;
    var C = Object.getOwnPropertyDescriptor;
    var f = Object.getOwnPropertyNames;
    var p = Object.getPrototypeOf;
    var u2 = Object.prototype.hasOwnProperty;
    var F = (r, o) => {
      for (var e2 in o)
        n2(r, e2, {
          get: o[e2],
          enumerable: true
        });
    };
    var l = (r, o, e2, c) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let t2 of f(o))
          !u2.call(r, t2) && t2 !== e2 && n2(r, t2, {
            get: () => o[t2],
            enumerable: !(c = C(o, t2)) || c.enumerable
          });
      return r;
    };
    var i = (r, o, e2) => (e2 = r != null ? d2(p(r)) : {}, l(o || !r || !r.__esModule ? n2(e2, "default", {
      value: r,
      enumerable: true
    }) : e2, r));
    var g = (r) => l(n2({}, "__esModule", {
      value: true
    }), r);
    var x = {};
    F(x, {
      CircleIcon: () => h2,
      cycleColorsByLevel: () => m
    });
    module.exports = g(x);
    var s = i(__require("react"));
    var a = i((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var m = (r) => {
      if (r === void 0)
        return;
      const o = ["#9D00FF", "#615CEA", "#147DF5", "#08BFCC", "#08CC7A", "#81CC08", "#E5BE00", "#FF8700", "#FF00B8"];
      return o[r % o.length];
    };
    function h2({
      sign: r,
      className: o,
      color: e2
    }) {
      return s.default.createElement(v, {
        className: o,
        color: e2
      }, r);
    }
    var v = a.default.span`
  background-color: var(--bg-color);
  border-radius: 50%;
  border: 1px solid ${({
      color: r
    }) => r || "var(--border-color-primary)"};
  font-size: var(--font-size-lg);
  line-height: var(--font-size-lg);
  color: ${({
      color: r
    }) => r || "var(--text-color-secondary)"};
  width: 20px;
  height: 20px;
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/components/ViewNested.js
var require_ViewNested = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/components/ViewNested.js"(exports, module) {
    var x = Object.create;
    var l = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var h2 = Object.getPrototypeOf;
    var w = Object.prototype.hasOwnProperty;
    var y = (e2, r) => {
      for (var n2 in r)
        l(e2, n2, {
          get: r[n2],
          enumerable: true
        });
    };
    var m = (e2, r, n2, i) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let a of b(r))
          !w.call(e2, a) && a !== n2 && l(e2, a, {
            get: () => r[a],
            enumerable: !(i = u2(r, a)) || i.enumerable
          });
      return e2;
    };
    var d2 = (e2, r, n2) => (n2 = e2 != null ? x(h2(e2)) : {}, m(r || !e2 || !e2.__esModule ? l(n2, "default", {
      value: e2,
      enumerable: true
    }) : n2, e2));
    var E = (e2) => m(l({}, "__esModule", {
      value: true
    }), e2);
    var B = {};
    y(B, {
      ViewNested: () => k
    });
    module.exports = E(B);
    var t2 = d2(__require("react"));
    var o = d2((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var f = (init_graphql2(), __toCommonJS(graphql_exports));
    var s = require_CircleIcon();
    function N({
      expanded: e2,
      level: r,
      expandText: n2 = "Show details",
      collapseText: i = "Hide details",
      children: a,
      args: g = [],
      onClick: v
    }) {
      const p = e2 ? (0, s.cycleColorsByLevel)(r) : void 0;
      return t2.default.createElement(z, {
        $expanded: e2,
        className: "view-nested-wrapper"
      }, t2.default.createElement(C, null, t2.default.createElement($, {
        onClick: v
      }, t2.default.createElement(s.CircleIcon, {
        sign: e2 ? "-" : "+",
        color: p
      }), t2.default.createElement(W, null, e2 ? i : n2)), !e2 && t2.default.createElement(A, null, g.map((c) => t2.default.createElement(R, {
        key: c.name
      }, c.name, (0, f.isRequiredArgument)(c) ? t2.default.createElement(S, null, "*") : null)))), e2 && t2.default.createElement(q, {
        color: p
      }, a));
    }
    var k = (0, t2.memo)(N);
    var z = o.default.div`
  width: 100%;
  margin: var(--spacing-xxs) 0;
  display: flex;
  flex-direction: row;

  &:last-child {
    margin-bottom: 0;
  }

  ${({
      $expanded: e2
    }) => e2 && o.css`
      flex-direction: column;
    `}
`;
    var C = o.default.div`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  gap: var(--spacing-xxs);
`;
    var S = o.default.span`
  color: var(--color-error-base);
`;
    var W = o.default.div`
  width: max-content;
`;
    var A = o.default.div`
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-xxs);
  align-items: baseline;
`;
    var R = o.default.span`
  color: var(--schema-inline-code-text-color);
  background-color: var(--bg-color-active);
  font-size: var(--font-size-base);
  line-height: var(--line-height-base);
  padding: 0 var(--spacing-xs);
  border-radius: var(--border-radius);
`;
    var $ = o.default.button`
  background: none;
  border: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  padding: 0;
  gap: var(--spacing-xxs);
  color: var(--text-color-secondary);
  font-size: var(--font-size-base);
  font-family: var(--font-family-base);
  line-height: var(--line-height-base);
`;
    var q = o.default.div`
  position: relative;
  width: 100%;
  padding-left: var(--schema-nested-offset);
  margin: 0 0 0 10px;

  &:before {
    content: '';
    height: calc(100% + 1px);
    position: absolute;
    top: -1px;
    left: -0.5px;
    border-left: 1px solid ${({
      color: e2
    }) => e2 || "var(--border-color-primary)"};
  }
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/ArgumentView.js
var require_ArgumentView = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/ArgumentView.js"(exports, module) {
    var E = Object.create;
    var a = Object.defineProperty;
    var h2 = Object.getOwnPropertyDescriptor;
    var F = Object.getOwnPropertyNames;
    var V = Object.getPrototypeOf;
    var g = Object.prototype.hasOwnProperty;
    var A = (e2, t2) => {
      for (var r in t2)
        a(e2, r, { get: t2[r], enumerable: true });
    };
    var l = (e2, t2, r, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let p of F(t2))
          !g.call(e2, p) && p !== r && a(e2, p, { get: () => t2[p], enumerable: !(n2 = h2(t2, p)) || n2.enumerable });
      return e2;
    };
    var b = (e2, t2, r) => (r = e2 != null ? E(V(e2)) : {}, l(t2 || !e2 || !e2.__esModule ? a(r, "default", { value: e2, enumerable: true }) : r, e2));
    var k = (e2) => l(a({}, "__esModule", { value: true }), e2);
    var C = {};
    A(C, { ArgumentView: () => B });
    module.exports = k(C);
    var o = b(__require("react"));
    var m = (init_src(), __toCommonJS(src_exports));
    var c = require_FieldRow();
    var u2 = require_TypeView();
    var s = require_styled4();
    var y = require_is_field_expandable();
    var f = require_ViewNested();
    function q({ argument: e2, contrast: t2, renderField: r, fieldExpandLevel: n2 = 0, firstOfType: p }) {
      const x = n2 + 1, [d2, T] = o.default.useState(false), { useTranslate: w } = (0, m.useThemeHooks)(), { translate: i } = w();
      return o.default.createElement(s.FieldWrapper, { firstOfType: p, showBorder: !d2, "data-testid": "arg-type" }, o.default.createElement(c.FieldRow, { isArgument: true, fieldExpandLevel: n2, "data-testid": e2.name + "-data", field: e2 }), (0, y.useIsFieldExpandable)(e2, n2) ? o.default.createElement(s.ExpandableFieldBody, { contrast: !t2 }, o.default.createElement(f.ViewNested, { expanded: d2, level: n2, expandText: i("graphql.returnTypes.show", "Show return type"), collapseText: i("graphql.returnTypes.hide", "Hide return type"), onClick: () => T(!d2) }, o.default.createElement(u2.TypeView, { type: e2.type, contrast: !t2, fieldExpandLevel: x, renderField: r }))) : null);
    }
    var B = (0, o.memo)(q);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/components/Headers.js
var require_Headers = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/components/Headers.js"(exports, module) {
    var n2 = Object.create;
    var i = Object.defineProperty;
    var g = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.getPrototypeOf;
    var l = Object.prototype.hasOwnProperty;
    var x = (r, o) => {
      for (var t2 in o)
        i(r, t2, {
          get: o[t2],
          enumerable: true
        });
    };
    var p = (r, o, t2, m) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let a of c(o))
          !l.call(r, a) && a !== t2 && i(r, a, {
            get: () => o[a],
            enumerable: !(m = g(o, a)) || m.enumerable
          });
      return r;
    };
    var f = (r, o, t2) => (t2 = r != null ? n2(d2(r)) : {}, p(o || !r || !r.__esModule ? i(t2, "default", {
      value: r,
      enumerable: true
    }) : t2, r));
    var v = (r) => p(i({}, "__esModule", {
      value: true
    }), r);
    var b = {};
    x(b, {
      FieldsGroupHeader: () => H
    });
    module.exports = v(b);
    var s = (init_src(), __toCommonJS(src_exports));
    var e2 = f((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var H = (0, e2.default)(s.H4)`
  margin-top: var(--spacing-xl);
  margin-bottom: var(--spacing-xs);
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/Arguments.js
var require_Arguments = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/Arguments.js"(exports, module) {
    var s = Object.create;
    var o = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var f = Object.getOwnPropertyNames;
    var c = Object.getPrototypeOf;
    var g = Object.prototype.hasOwnProperty;
    var E = (e2, t2) => {
      for (var r in t2)
        o(e2, r, { get: t2[r], enumerable: true });
    };
    var i = (e2, t2, r, a) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let m of f(t2))
          !g.call(e2, m) && m !== r && o(e2, m, { get: () => t2[m], enumerable: !(a = u2(t2, m)) || a.enumerable });
      return e2;
    };
    var A = (e2, t2, r) => (r = e2 != null ? s(c(e2)) : {}, i(t2 || !e2 || !e2.__esModule ? o(r, "default", { value: e2, enumerable: true }) : r, e2));
    var F = (e2) => i(o({}, "__esModule", { value: true }), e2);
    var x = {};
    E(x, { Arguments: () => w });
    module.exports = F(x);
    var n2 = A(__require("react"));
    var d2 = require_ArgumentView();
    var l = require_Headers();
    function v({ args: e2, renderField: t2, fieldExpandLevel: r = 0, title: a }) {
      return n2.default.createElement(n2.default.Fragment, null, a && n2.default.createElement(l.FieldsGroupHeader, { "data-testid": "arguments-title" }, a), e2.map((m, p) => n2.default.createElement(d2.ArgumentView, { "data-testid": "arguments-view", key: m.name, argument: m, renderField: t2, fieldExpandLevel: r, firstOfType: p === 0 })));
    }
    var w = (0, n2.memo)(v);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/FieldView.js
var require_FieldView = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/FieldView.js"(exports, module) {
    var S = Object.create;
    var m = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var k = Object.getOwnPropertyNames;
    var q = Object.getPrototypeOf;
    var A = Object.prototype.hasOwnProperty;
    var H = (e2, t2) => {
      for (var a in t2)
        m(e2, a, { get: t2[a], enumerable: true });
    };
    var h2 = (e2, t2, a, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let o of k(t2))
          !A.call(e2, o) && o !== a && m(e2, o, { get: () => t2[o], enumerable: !(n2 = b(t2, o)) || n2.enumerable });
      return e2;
    };
    var B = (e2, t2, a) => (a = e2 != null ? S(q(e2)) : {}, h2(t2 || !e2 || !e2.__esModule ? m(a, "default", { value: e2, enumerable: true }) : a, e2));
    var V = (e2) => h2(m({}, "__esModule", { value: true }), e2);
    var I = {};
    H(I, { FieldView: () => u2 });
    module.exports = V(I);
    var r = B(__require("react"));
    var E = require_shared();
    var v = (init_src(), __toCommonJS(src_exports));
    var y = require_TypeView();
    var T = require_Arguments();
    var w = require_FieldRow();
    var i = require_styled4();
    var s = require_navigation2();
    var l = require_is_field_expandable();
    var g = require_ViewNested();
    function u2({ field: e2, contrast: t2, key: a, fieldExpandLevel: n2 }) {
      var o;
      const x = n2 + 1, F = (0, s.useNavigationContext)(), [d2, f] = r.default.useState(false), [c, C] = r.default.useState(false), { useTranslate: N } = (0, v.useThemeHooks)(), { translate: p } = N();
      return r.default.createElement(s.NavigationContextProvider, { key: a, value: F }, r.default.createElement(E.HiddenNavigationAnchor, Object.assign({}, (0, s.getNavigationIdProp)(""))), r.default.createElement(i.FieldWrapper, { showBorder: (0, l.useShowExpandableBorder)(e2, n2, { expandedTypes: d2, expandedArguments: c }) }, r.default.createElement(w.FieldRow, { fieldExpandLevel: n2, field: e2 }), (0, l.useIsFieldExpandable)(e2, n2) ? r.default.createElement(i.ExpandableFieldBody, { contrast: !t2 }, !!(!((o = e2.args) === null || o === void 0) && o.length) && r.default.createElement(g.ViewNested, { expanded: c, level: n2, expandText: p("graphql.arguments.show", "Show arguments"), collapseText: p("graphql.arguments.hide", "Hide arguments"), args: e2.args, onClick: () => C(!c) }, r.default.createElement(T.Arguments, { args: e2.args, fieldExpandLevel: x, renderField: u2 })), r.default.createElement(g.ViewNested, { expanded: d2, level: n2, expandText: p("graphql.returnTypes.show", "Show return type"), collapseText: p("graphql.returnTypes.hide", "Hide return type"), onClick: () => f(!d2) }, r.default.createElement(y.TypeView, { type: e2.type, contrast: !t2, fieldExpandLevel: x, renderField: u2 }))) : null));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/FieldSummary.js
var require_FieldSummary = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/FieldSummary.js"(exports, module) {
    var g = Object.create;
    var i = Object.defineProperty;
    var E = Object.getOwnPropertyDescriptor;
    var d2 = Object.getOwnPropertyNames;
    var u2 = Object.getPrototypeOf;
    var y = Object.prototype.hasOwnProperty;
    var f = (e2, t2) => {
      for (var r in t2)
        i(e2, r, { get: t2[r], enumerable: true });
    };
    var o = (e2, t2, r, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let a of d2(t2))
          !y.call(e2, a) && a !== r && i(e2, a, { get: () => t2[a], enumerable: !(n2 = E(t2, a)) || n2.enumerable });
      return e2;
    };
    var x = (e2, t2, r) => (r = e2 != null ? g(u2(e2)) : {}, o(t2 || !e2 || !e2.__esModule ? i(r, "default", { value: e2, enumerable: true }) : r, e2));
    var S = (e2) => o(i({}, "__esModule", { value: true }), e2);
    var k = {};
    f(k, { FieldSummary: () => b });
    module.exports = S(k);
    var m = x(__require("react"));
    var l = require_shared();
    var p = require_InlineArgsList();
    var s = require_ReturnTypeDetails();
    var c = require_navigation2();
    function b({ field: e2 }) {
      return m.default.createElement("div", { "data-testid": e2.name + "-block" }, m.default.createElement(c.ShareLink, { "data-testid": "title" }, m.default.createElement(l.SplittedWbrText, { text: e2.name })), m.default.createElement(p.InlineArgsList, { args: e2.args, size: "lg" }), m.default.createElement(s.ReturnTypeDetails, { type: e2.type, size: "lg" }));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/ExtendedFieldView.js
var require_ExtendedFieldView = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/ExtendedFieldView.js"(exports, module) {
    var y = Object.create;
    var a = Object.defineProperty;
    var F = Object.getOwnPropertyDescriptor;
    var h2 = Object.getOwnPropertyNames;
    var k = Object.getPrototypeOf;
    var T = Object.prototype.hasOwnProperty;
    var V = (e2, t2) => {
      for (var o in t2)
        a(e2, o, { get: t2[o], enumerable: true });
    };
    var p = (e2, t2, o, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let m of h2(t2))
          !T.call(e2, m) && m !== o && a(e2, m, { get: () => t2[m], enumerable: !(n2 = F(t2, m)) || n2.enumerable });
      return e2;
    };
    var b = (e2, t2, o) => (o = e2 != null ? y(k(e2)) : {}, p(t2 || !e2 || !e2.__esModule ? a(o, "default", { value: e2, enumerable: true }) : o, e2));
    var x = (e2) => p(a({}, "__esModule", { value: true }), e2);
    var M = {};
    V(M, { ExtendedFieldView: () => H });
    module.exports = x(M);
    var r = b(__require("react"));
    var i = (init_src(), __toCommonJS(src_exports));
    var d2 = require_Arguments();
    var s = require_TypeView();
    var c = require_FieldSummary();
    var u2 = require_styled4();
    var g = require_app();
    var E = require_markdown();
    var f = require_settings2();
    var l = require_FieldView();
    var w = require_Headers();
    function A({ field: e2 }) {
      const { markdown: t2 } = (0, f.useSettings)(), { useTranslate: o } = (0, i.useThemeHooks)(), { translate: n2 } = o();
      return r.default.createElement(g.MiddleSection, { "data-testid": "operation-item" }, r.default.createElement(i.H2, null, r.default.createElement(c.FieldSummary, { field: e2 })), r.default.createElement(u2.Annotation, { "data-testid": "object-description" }, r.default.createElement(E.Markdown, { rawMarkdown: e2.description, parser: t2?.parser })), e2.args.length ? r.default.createElement(d2.Arguments, { args: e2.args, renderField: l.FieldView, title: n2("graphql.arguments.label", "Arguments") }) : null, r.default.createElement(w.FieldsGroupHeader, { "data-testid": "return-type-title" }, n2("graphql.returnTypes.label", "Return type")), r.default.createElement(s.TypeView, { type: e2.type, renderField: l.FieldView }));
    }
    var H = (0, r.memo)(A);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/ReferenceItem.js
var require_ReferenceItem = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/ReferenceItem.js"(exports, module) {
    var u2 = Object.create;
    var a = Object.defineProperty;
    var c = Object.getOwnPropertyDescriptor;
    var f = Object.getOwnPropertyNames;
    var g = Object.getPrototypeOf;
    var E = Object.prototype.hasOwnProperty;
    var v = (t2, e2) => {
      for (var o in e2)
        a(t2, o, { get: e2[o], enumerable: true });
    };
    var d2 = (t2, e2, o, l) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of f(e2))
          !E.call(t2, m) && m !== o && a(t2, m, { get: () => e2[m], enumerable: !(l = c(e2, m)) || l.enumerable });
      return t2;
    };
    var x = (t2, e2, o) => (o = t2 != null ? u2(g(t2)) : {}, d2(e2 || !t2 || !t2.__esModule ? a(o, "default", { value: t2, enumerable: true }) : o, t2));
    var y = (t2) => d2(a({}, "__esModule", { value: true }), t2);
    var R = {};
    v(R, { ReferenceItem: () => I });
    module.exports = y(R);
    var i = x(__require("react"));
    var n2 = require_styled4();
    var p = require_menu();
    var r = require_navigation2();
    var s = require_common2();
    function I({ reference: t2 }) {
      const e2 = i.useContext(p.MenuStoreContext), o = e2?.getGroupIdByTypeName(t2.name);
      return o ? i.createElement(n2.Value, { "data-testid": "ref-item" }, i.createElement(s.ListIcon, { size: "22px", color: "var(--border-color-primary)", style: { marginRight: "var(--spacing-sm)" } }), i.createElement(r.PaginationAwareLink, { to: { section: o, item: t2.name } }, t2.name), t2.field ? i.createElement(n2.ReferenceField, { "data-testid": "ref-field" }, ".", i.createElement(r.PaginationAwareLink, { to: { section: o, item: t2.name, deepItem: t2.field } }, t2.field)) : null) : null;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/ReferenceList.js
var require_ReferenceList = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/ReferenceList.js"(exports, module) {
    var p = Object.create;
    var l = Object.defineProperty;
    var s = Object.getOwnPropertyDescriptor;
    var u2 = Object.getOwnPropertyNames;
    var d2 = Object.getPrototypeOf;
    var R = Object.prototype.hasOwnProperty;
    var E = (e2, n2) => {
      for (var t2 in n2)
        l(e2, t2, { get: n2[t2], enumerable: true });
    };
    var c = (e2, n2, t2, m) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let a of u2(n2))
          !R.call(e2, a) && a !== t2 && l(e2, a, { get: () => n2[a], enumerable: !(m = s(n2, a)) || m.enumerable });
      return e2;
    };
    var P = (e2, n2, t2) => (t2 = e2 != null ? p(d2(e2)) : {}, c(n2 || !e2 || !e2.__esModule ? l(t2, "default", { value: e2, enumerable: true }) : t2, e2));
    var g = (e2) => c(l({}, "__esModule", { value: true }), e2);
    var y = {};
    E(y, { ReferenceList: () => k });
    module.exports = g(y);
    var r = P(__require("react"));
    var f = (init_src(), __toCommonJS(src_exports));
    var i = require_ReferenceItem();
    var o = require_styled4();
    function h2({ references: e2 }) {
      const { useTranslate: n2 } = (0, f.useThemeHooks)(), { translate: t2 } = n2();
      return r.default.createElement(o.PanelWrapper, null, r.default.createElement(o.PanelHeading, null, t2("graphql.referenced", "Referenced in")), r.default.createElement(o.PanelBody, null, e2.map((m) => r.default.createElement(i.ReferenceItem, { key: `${m.name}.${m.field}`, reference: m }))));
    }
    var k = (0, r.memo)(h2);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/LocationList.js
var require_LocationList = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/LocationList.js"(exports, module) {
    var p = Object.create;
    var i = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var g = Object.getOwnPropertyNames;
    var L = Object.getPrototypeOf;
    var d2 = Object.prototype.hasOwnProperty;
    var f = (e2, t2) => {
      for (var o in t2)
        i(e2, o, { get: t2[o], enumerable: true });
    };
    var l = (e2, t2, o, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let m of g(t2))
          !d2.call(e2, m) && m !== o && i(e2, m, { get: () => t2[m], enumerable: !(n2 = u2(t2, m)) || n2.enumerable });
      return e2;
    };
    var E = (e2, t2, o) => (o = e2 != null ? p(L(e2)) : {}, l(t2 || !e2 || !e2.__esModule ? i(o, "default", { value: e2, enumerable: true }) : o, e2));
    var x = (e2) => l(i({}, "__esModule", { value: true }), e2);
    var P = {};
    f(P, { LocationList: () => h2 });
    module.exports = x(P);
    var a = E(__require("react"));
    var c = (init_src(), __toCommonJS(src_exports));
    var r = require_styled4();
    var s = require_common2();
    function y({ locations: e2 }) {
      const { useTranslate: t2 } = (0, c.useThemeHooks)(), { translate: o } = t2();
      return a.default.createElement(r.PanelWrapper, { $spacing: "xxl" }, a.default.createElement(r.PanelHeading, null, o("graphql.locations", "Locations")), a.default.createElement(r.PanelBody, null, e2.map((n2) => a.default.createElement(r.Value, { "data-testid": "location-item", key: n2 }, a.default.createElement(s.ListIcon, { size: "22px", color: "var(--border-color-primary)", style: { marginRight: "var(--spacing-sm)" } }), n2))));
    }
    var h2 = (0, a.memo)(y);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/index.js
var require_components4 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_NullableType(), module.exports);
    r(o, require_ReturnTypeDetails(), module.exports);
    r(o, require_FieldView(), module.exports);
    r(o, require_TypeView(), module.exports);
    r(o, require_ArgumentView(), module.exports);
    r(o, require_FieldSummary(), module.exports);
    r(o, require_ExtendedFieldView(), module.exports);
    r(o, require_Arguments(), module.exports);
    r(o, require_FieldRow(), module.exports);
    r(o, require_DefaultValue(), module.exports);
    r(o, require_InlineArgsList(), module.exports);
    r(o, require_GroupTypeList(), module.exports);
    r(o, require_FieldList(), module.exports);
    r(o, require_ReferenceItem(), module.exports);
    r(o, require_ReferenceList(), module.exports);
    r(o, require_LocationList(), module.exports);
    r(o, require_ValueList(), module.exports);
    r(o, require_styled4(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/DirectiveItem.js
var require_DirectiveItem = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/DirectiveItem.js"(exports, module) {
    var E = Object.create;
    var s = Object.defineProperty;
    var v = Object.getOwnPropertyDescriptor;
    var w = Object.getOwnPropertyNames;
    var h2 = Object.getPrototypeOf;
    var k = Object.prototype.hasOwnProperty;
    var S = (e2, t2) => {
      for (var o in t2)
        s(e2, o, { get: t2[o], enumerable: true });
    };
    var d2 = (e2, t2, o, l) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let i of w(t2))
          !k.call(e2, i) && i !== o && s(e2, i, { get: () => t2[i], enumerable: !(l = v(t2, i)) || l.enumerable });
      return e2;
    };
    var M = (e2, t2, o) => (o = e2 != null ? E(h2(e2)) : {}, d2(t2 || !e2 || !e2.__esModule ? s(o, "default", { value: e2, enumerable: true }) : o, e2));
    var A = (e2) => d2(s({}, "__esModule", { value: true }), e2);
    var b = {};
    S(b, { DirectiveItem: () => L });
    module.exports = A(b);
    var r = M(__require("react"));
    var c = (init_src(), __toCommonJS(src_exports));
    var a = require_components4();
    var p = require_common2();
    var m = require_app();
    var u2 = require_markdown();
    var g = require_settings2();
    var f = require_navigation2();
    function D({ name: e2 }) {
      const t2 = (0, m.useStore)(), { markdown: o } = (0, g.useSettings)(), { useTranslate: l } = (0, c.useThemeHooks)(), { translate: i } = l(), n2 = (0, r.useMemo)(() => t2?.getDirective(e2), [e2, t2]);
      return n2 ? r.default.createElement(p.Box, { "data-testid": n2.name, direction: "row", wrap: "wrap" }, r.default.createElement(m.MiddleSection, { "data-testid": n2.name }, r.default.createElement(c.H2, { "data-testid": "title" }, r.default.createElement(f.ShareLink, null, n2.name)), r.default.createElement(a.Annotation, { "data-testid": "object-description" }, r.default.createElement(u2.Markdown, { rawMarkdown: n2.description, parser: o?.parser })), !!n2.args.length && r.default.createElement(a.Arguments, { args: n2.args, title: i("graphql.arguments.label", "Arguments") })), n2.locations.length ? r.default.createElement(m.RightSection, null, r.default.createElement(a.LocationList, { locations: n2.locations })) : null) : null;
    }
    var L = (0, r.memo)(D);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/EnumItem.js
var require_EnumItem = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/EnumItem.js"(exports, module) {
    var g = Object.create;
    var i = Object.defineProperty;
    var v = Object.getOwnPropertyDescriptor;
    var k = Object.getOwnPropertyNames;
    var w = Object.getPrototypeOf;
    var M = Object.prototype.hasOwnProperty;
    var S = (e2, t2) => {
      for (var o in t2)
        i(e2, o, { get: t2[o], enumerable: true });
    };
    var s = (e2, t2, o, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let m of k(t2))
          !M.call(e2, m) && m !== o && i(e2, m, { get: () => t2[m], enumerable: !(n2 = v(t2, m)) || n2.enumerable });
      return e2;
    };
    var y = (e2, t2, o) => (o = e2 != null ? g(w(e2)) : {}, s(t2 || !e2 || !e2.__esModule ? i(o, "default", { value: e2, enumerable: true }) : o, e2));
    var V = (e2) => s(i({}, "__esModule", { value: true }), e2);
    var I = {};
    S(I, { EnumItem: () => x });
    module.exports = V(I);
    var d2 = (init_graphql2(), __toCommonJS(graphql_exports));
    var r = y(__require("react"));
    var c = (init_src(), __toCommonJS(src_exports));
    var a = require_components4();
    var l = require_app();
    var p = require_markdown();
    var u2 = require_settings2();
    var f = require_navigation2();
    var E = require_common2();
    function h2({ name: e2 }) {
      const t2 = (0, l.useStore)(), { markdown: o } = (0, u2.useSettings)(), n2 = (0, r.useMemo)(() => t2?.getType(e2), [e2, t2]);
      return (0, d2.isEnumType)(n2) ? r.default.createElement(l.MiddleSection, { "data-testid": n2.name }, r.default.createElement(c.H2, { "data-testid": "title" }, " ", r.default.createElement(f.ShareLink, null, n2.name)), r.default.createElement(a.Annotation, { "data-testid": "object-description" }, r.default.createElement(p.Markdown, { rawMarkdown: n2.description, parser: o?.parser })), n2.getValues().length ? r.default.createElement(E.Box, { "data-testid": e2, direction: "column" }, r.default.createElement(a.ValueList, { values: n2.getValues() })) : null) : null;
    }
    var x = (0, r.memo)(h2);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/type/enum-example.js
var require_enum_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/type/enum-example.js"(exports, module) {
    var a = Object.defineProperty;
    var r = Object.getOwnPropertyDescriptor;
    var g = Object.getOwnPropertyNames;
    var l = Object.prototype.hasOwnProperty;
    var o = (e2, n2) => {
      for (var u2 in n2)
        a(e2, u2, { get: n2[u2], enumerable: true });
    };
    var p = (e2, n2, u2, m) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let t2 of g(n2))
          !l.call(e2, t2) && t2 !== u2 && a(e2, t2, { get: () => n2[t2], enumerable: !(m = r(n2, t2)) || m.enumerable });
      return e2;
    };
    var x = (e2) => p(a({}, "__esModule", { value: true }), e2);
    var c = {};
    o(c, { getEnumExample: () => E });
    module.exports = x(c);
    function E(e2) {
      return e2.getValues()[0].name;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/type/list-example.js
var require_list_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/type/list-example.js"(exports, module) {
    var r = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var i = Object.getOwnPropertyNames;
    var u2 = Object.prototype.hasOwnProperty;
    var x = (t2, e2) => {
      for (var p in e2)
        r(t2, p, { get: e2[p], enumerable: true });
    };
    var y = (t2, e2, p, o) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let n2 of i(e2))
          !u2.call(t2, n2) && n2 !== p && r(t2, n2, { get: () => e2[n2], enumerable: !(o = f(e2, n2)) || o.enumerable });
      return t2;
    };
    var a = (t2) => y(r({}, "__esModule", { value: true }), t2);
    var g = {};
    x(g, { getListExample: () => c });
    module.exports = a(g);
    function c(t2, e2, p, o) {
      return [o({ type: t2.ofType, expandLevel: e2, level: p })];
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/type/non-null-example.js
var require_non_null_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/type/non-null-example.js"(exports, module) {
    var l = Object.defineProperty;
    var r = Object.getOwnPropertyDescriptor;
    var u2 = Object.getOwnPropertyNames;
    var f = Object.prototype.hasOwnProperty;
    var x = (t2, e2) => {
      for (var n2 in e2)
        l(t2, n2, { get: e2[n2], enumerable: true });
    };
    var y = (t2, e2, n2, p) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let o of u2(e2))
          !f.call(t2, o) && o !== n2 && l(t2, o, { get: () => e2[o], enumerable: !(p = r(e2, o)) || p.enumerable });
      return t2;
    };
    var N = (t2) => y(l({}, "__esModule", { value: true }), t2);
    var c = {};
    x(c, { getNonNullExample: () => a });
    module.exports = N(c);
    function a(t2, e2, n2, p) {
      return p({ type: t2.ofType, expandLevel: e2, level: n2 });
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/type/object-example.js
var require_object_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/type/object-example.js"(exports, module) {
    var m = Object.defineProperty;
    var i = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var s = Object.prototype.hasOwnProperty;
    var u2 = (e2, t2) => {
      for (var n2 in t2)
        m(e2, n2, { get: t2[n2], enumerable: true });
    };
    var E = (e2, t2, n2, o) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let r of p(t2))
          !s.call(e2, r) && r !== n2 && m(e2, r, { get: () => t2[r], enumerable: !(o = i(t2, r)) || o.enumerable });
      return e2;
    };
    var l = (e2) => E(m({}, "__esModule", { value: true }), e2);
    var f = {};
    u2(f, { getObjectExample: () => x });
    module.exports = l(f);
    var a = require_is_connection_field();
    function x(e2, t2, n2, o) {
      if (n2 === t2)
        return { __typename: e2.name };
      const r = Object.values(e2.getFields()).map((c) => b(c, t2, n2, o));
      return Object.fromEntries(r);
    }
    function b(e2, t2, n2, o) {
      const r = (0, a.isConnectionField)(e2) ? n2 : n2 + 1, c = o({ type: e2.type, expandLevel: t2, level: r });
      return [e2.name, c];
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/type/scalar-example.js
var require_scalar_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/type/scalar-example.js"(exports, module) {
    var n2 = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var l = Object.getOwnPropertyNames;
    var s = Object.prototype.hasOwnProperty;
    var o = (e2, r) => {
      for (var a in r)
        n2(e2, a, { get: r[a], enumerable: true });
    };
    var f = (e2, r, a, c) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let t2 of l(r))
          !s.call(e2, t2) && t2 !== a && n2(e2, t2, { get: () => r[t2], enumerable: !(c = u2(r, t2)) || c.enumerable });
      return e2;
    };
    var m = (e2) => f(n2({}, "__esModule", { value: true }), e2);
    var p = {};
    o(p, { getScalarExample: () => i });
    module.exports = m(p);
    function i(e2) {
      switch (e2.name) {
        case "Int":
          return 40;
        case "Float":
          return 40;
        case "String":
          return "Example String";
        case "Boolean":
          return true;
        case "ID":
          return "9cfb1c81-4c79-452f-b1f5-8ee6571276b4";
        default:
          return "Example Custom Scalar";
      }
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/type/union-example.js
var require_union_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/type/union-example.js"(exports, module) {
    var r = Object.defineProperty;
    var g = Object.getOwnPropertyDescriptor;
    var i = Object.getOwnPropertyNames;
    var u2 = Object.prototype.hasOwnProperty;
    var x = (t2, e2) => {
      for (var n2 in e2)
        r(t2, n2, { get: e2[n2], enumerable: true });
    };
    var y = (t2, e2, n2, o) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let p of i(e2))
          !u2.call(t2, p) && p !== n2 && r(t2, p, { get: () => e2[p], enumerable: !(o = g(e2, p)) || o.enumerable });
      return t2;
    };
    var a = (t2) => y(r({}, "__esModule", { value: true }), t2);
    var f = {};
    x(f, { getUnionExample: () => c });
    module.exports = a(f);
    function c(t2, e2, n2, o) {
      return o({ type: t2.getTypes()[0], expandLevel: e2, level: n2 });
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/type/type-example.js
var require_type_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/type/type-example.js"(exports, module) {
    var n2 = Object.defineProperty;
    var e2 = Object.getOwnPropertyDescriptor;
    var g = Object.getOwnPropertyNames;
    var x = Object.prototype.hasOwnProperty;
    var b = (r, i) => {
      for (var o in i)
        n2(r, o, { get: i[o], enumerable: true });
    };
    var j = (r, i, o, s) => {
      if (i && typeof i == "object" || typeof i == "function")
        for (let t2 of g(i))
          !x.call(r, t2) && t2 !== o && n2(r, t2, { get: () => i[t2], enumerable: !(s = e2(i, t2)) || s.enumerable });
      return r;
    };
    var O = (r) => j(n2({}, "__esModule", { value: true }), r);
    var l = {};
    b(l, { getTypeExample: () => f });
    module.exports = O(l);
    var m = (init_graphql2(), __toCommonJS(graphql_exports));
    var u2 = require_enum_example();
    var a = require_list_example();
    var p = require_non_null_example();
    var E = require_object_example();
    var T = require_scalar_example();
    var c = require_union_example();
    var f = ({ type: r, expandLevel: i, level: o = 0 }) => (0, m.isScalarType)(r) ? (0, T.getScalarExample)(r) : (0, m.isObjectType)(r) || (0, m.isInterfaceType)(r) || (0, m.isInputObjectType)(r) ? (0, E.getObjectExample)(r, i, o, f) : (0, m.isUnionType)(r) ? (0, c.getUnionExample)(r, i, o, f) : (0, m.isEnumType)(r) ? (0, u2.getEnumExample)(r) : (0, m.isListType)(r) ? (0, a.getListExample)(r, i, o, f) : (0, p.getNonNullExample)(r, i, o, f);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/type/index.js
var require_type = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/type/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var p = (r, o, f, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of c(o))
          !d2.call(r, e2) && e2 !== f && a(r, e2, { get: () => o[e2], enumerable: !(x = b(o, e2)) || x.enumerable });
      return r;
    };
    var t2 = (r, o, f) => (p(r, o, "default"), f && p(f, o, "default"));
    var g = (r) => p(a({}, "__esModule", { value: true }), r);
    var m = {};
    module.exports = g(m);
    t2(m, require_type_example(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/operation/indentation.js
var require_indentation = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/operation/indentation.js"(exports, module) {
    var N = Object.defineProperty;
    var I = Object.getOwnPropertyDescriptor;
    var T = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var i = (n2, t2) => {
      for (var r in t2)
        N(n2, r, { get: t2[r], enumerable: true });
    };
    var p = (n2, t2, r, o) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let e2 of T(t2))
          !c.call(n2, e2) && e2 !== r && N(n2, e2, { get: () => t2[e2], enumerable: !(o = I(t2, e2)) || o.enumerable });
      return n2;
    };
    var u2 = (n2) => p(N({}, "__esModule", { value: true }), n2);
    var f = {};
    i(f, { indent: () => d2 });
    module.exports = u2(f);
    var a = "  ";
    function d2(n2) {
      return a.repeat(n2);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/operation/arguments-example.js
var require_arguments_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/operation/arguments-example.js"(exports, module) {
    var m = Object.defineProperty;
    var g = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var $ = Object.prototype.hasOwnProperty;
    var c = (n2, e2) => {
      for (var t2 in e2)
        m(n2, t2, { get: e2[t2], enumerable: true });
    };
    var f = (n2, e2, t2, u2) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let r of p(e2))
          !$.call(n2, r) && r !== t2 && m(n2, r, { get: () => e2[r], enumerable: !(u2 = g(e2, r)) || u2.enumerable });
      return n2;
    };
    var s = (n2) => f(m({}, "__esModule", { value: true }), n2);
    var d2 = {};
    c(d2, { getArgumentsExample: () => x });
    module.exports = s(d2);
    var i = require_indentation();
    function x(n2, e2, t2, u2, r) {
      const o = (0, i.indent)(t2), l = (0, i.indent)(t2 + 1);
      return n2.length === 0 ? "" : u2 > 0 ? `(
${l}# ${r("graphql.arguments.here", "Arguments Here")}
${o})` : e2 ? A(n2, o, l) : E(n2);
    }
    function E(n2) {
      return `(${n2.map(a).join(", ")})`;
    }
    function a(n2) {
      return `${n2.name}: $${n2.name}`;
    }
    function A(n2, e2, t2) {
      return `(
${n2.map((r) => h2(r, t2)).join(`
`)}
${e2})`;
    }
    function h2(n2, e2) {
      return `${e2}${a(n2)}`;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/operation/selection-set/scalar-example.js
var require_scalar_example2 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/operation/selection-set/scalar-example.js"(exports, module) {
    var a = Object.defineProperty;
    var l = Object.getOwnPropertyDescriptor;
    var o = Object.getOwnPropertyNames;
    var S = Object.prototype.hasOwnProperty;
    var i = (t2, e2) => {
      for (var r in e2)
        a(t2, r, { get: e2[r], enumerable: true });
    };
    var p = (t2, e2, r, c) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let n2 of o(e2))
          !S.call(t2, n2) && n2 !== r && a(t2, n2, { get: () => e2[n2], enumerable: !(c = l(e2, n2)) || c.enumerable });
      return t2;
    };
    var u2 = (t2) => p(a({}, "__esModule", { value: true }), t2);
    var f = {};
    i(f, { getScalarSelectionSetExample: () => x });
    module.exports = u2(f);
    function x() {
      return "";
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/operation/selection-set/truncated-object-example.js
var require_truncated_object_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/operation/selection-set/truncated-object-example.js"(exports, module) {
    var a = Object.defineProperty;
    var m = Object.getOwnPropertyDescriptor;
    var i = Object.getOwnPropertyNames;
    var p = Object.prototype.hasOwnProperty;
    var $ = (t2, n2) => {
      for (var o in n2)
        a(t2, o, { get: n2[o], enumerable: true });
    };
    var d2 = (t2, n2, o, r) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let e2 of i(n2))
          !p.call(t2, e2) && e2 !== o && a(t2, e2, { get: () => n2[e2], enumerable: !(r = m(n2, e2)) || r.enumerable });
      return t2;
    };
    var g = (t2) => d2(a({}, "__esModule", { value: true }), t2);
    var f = {};
    $(f, { getTruncatedObjectSelectionSetExample: () => l });
    module.exports = g(f);
    var c = require_indentation();
    function l(t2, n2, o) {
      const r = (0, c.indent)(n2), e2 = (0, c.indent)(n2 + 1);
      return `{
${e2}__typename
${e2}# ...${t2.name}${o("graphql.content.fragment", "Fragment")}
${r}}`;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/operation/selection-set/object-example.js
var require_object_example2 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/operation/selection-set/object-example.js"(exports, module) {
    var m = Object.defineProperty;
    var a = Object.getOwnPropertyDescriptor;
    var x = Object.getOwnPropertyNames;
    var E = Object.prototype.hasOwnProperty;
    var g = (e2, n2) => {
      for (var o in n2)
        m(e2, o, { get: n2[o], enumerable: true });
    };
    var j = (e2, n2, o, i) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let t2 of x(n2))
          !E.call(e2, t2) && t2 !== o && m(e2, t2, { get: () => n2[t2], enumerable: !(i = a(n2, t2)) || i.enumerable });
      return e2;
    };
    var S = (e2) => j(m({}, "__esModule", { value: true }), e2);
    var F = {};
    g(F, { getObjectSelectionSetExample: () => b });
    module.exports = S(F);
    var s = require_indentation();
    var p = require_truncated_object_example();
    var f = require_schema();
    function b(e2, n2, o, i, t2, l, c) {
      const r = (0, s.indent)(o);
      if (t2 === i)
        return (0, p.getTruncatedObjectSelectionSetExample)(e2, o, c);
      const d2 = e2.getFields();
      return `{
${Object.values(d2).map((u2) => h2(u2, n2, o, i, t2, l, c)).join(`
`)}
${r}}`;
    }
    function h2(e2, n2, o, i, t2, l, c) {
      const r = (0, f.isConnectionField)(e2) ? t2 : t2 + 1, d2 = o + 1;
      return l({ field: e2, multilineArguments: n2, indentation: d2, maxLevel: i, level: r, translate: c });
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/operation/selection-set/selection-set-example.js
var require_selection_set_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/operation/selection-set/selection-set-example.js"(exports, module) {
    var f = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var g = Object.getOwnPropertyNames;
    var j = Object.prototype.hasOwnProperty;
    var x = (e2, r) => {
      for (var t2 in r)
        f(e2, t2, { get: r[t2], enumerable: true });
    };
    var E = (e2, r, t2, i) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let o of g(r))
          !j.call(e2, o) && o !== t2 && f(e2, o, { get: () => r[o], enumerable: !(i = b(r, o)) || i.enumerable });
      return e2;
    };
    var O = (e2) => E(f({}, "__esModule", { value: true }), e2);
    var a = {};
    x(a, { getSelectionSetExample: () => s });
    module.exports = O(a);
    var c = (init_graphql2(), __toCommonJS(graphql_exports));
    var m = require_scalar_example2();
    var n2 = require_object_example2();
    var s = (e2, r, t2, i, o, p, S) => {
      if ((0, c.isObjectType)(e2) || (0, c.isInterfaceType)(e2))
        return (0, n2.getObjectSelectionSetExample)(e2, r, t2, i, o, p, S);
      if ((0, c.isUnionType)(e2)) {
        const [T] = e2.getTypes();
        return s(T, r, t2, i, o, p, S);
      }
      return (0, m.getScalarSelectionSetExample)();
    };
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/operation/field-example.js
var require_field_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/operation/field-example.js"(exports, module) {
    var r = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var S = Object.getOwnPropertyNames;
    var n2 = Object.prototype.hasOwnProperty;
    var y = (t2, e2) => {
      for (var m in e2)
        r(t2, m, { get: e2[m], enumerable: true });
    };
    var i = (t2, e2, m, p) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let o of S(e2))
          !n2.call(t2, o) && o !== m && r(t2, o, { get: () => e2[o], enumerable: !(p = u2(e2, o)) || p.enumerable });
      return t2;
    };
    var l = (t2) => i(r({}, "__esModule", { value: true }), t2);
    var A = {};
    y(A, { getFieldExample: () => $ });
    module.exports = l(A);
    var c = require_arguments_example();
    var s = require_indentation();
    var x = require_selection_set_example();
    var E = require_schema();
    var $ = ({ field: t2, multilineArguments: e2, indentation: m, translate: p, maxLevel: o, level: g = 0 }) => {
      const a = (0, c.getArgumentsExample)(t2.args, e2, m, g, p), f = (0, x.getSelectionSetExample)((0, E.getNamedOutputType)(t2.type), e2, m, o, g, $, p);
      return `${(0, s.indent)(m)}${t2.name}${a} ${f}`;
    };
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/operation/variables-example.js
var require_variables_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/operation/variables-example.js"(exports, module) {
    var a = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var p = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var $ = (n2, e2) => {
      for (var r in e2)
        a(n2, r, { get: e2[r], enumerable: true });
    };
    var x = (n2, e2, r, u2) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let t2 of p(e2))
          !c.call(n2, t2) && t2 !== r && a(n2, t2, { get: () => e2[t2], enumerable: !(u2 = f(e2, t2)) || u2.enumerable });
      return n2;
    };
    var s = (n2) => x(a({}, "__esModule", { value: true }), n2);
    var L = {};
    $(L, { getVariablesExample: () => E });
    module.exports = s(L);
    var i = (init_graphql2(), __toCommonJS(graphql_exports));
    var m = require_indentation();
    function E(n2, e2) {
      return n2.length === 0 ? "" : e2 ? g(n2) : b(n2);
    }
    function b(n2) {
      return `(${n2.map(o).join(", ")})`;
    }
    function o(n2) {
      return `$${n2.name}: ${l(n2.type)}`;
    }
    function l(n2) {
      return n2 instanceof i.GraphQLList ? `[${l(n2.ofType)}]` : n2 instanceof i.GraphQLNonNull ? `${l(n2.ofType)}!` : n2.name;
    }
    function g(n2) {
      return `(
${n2.map(V).join(`
`)}
)`;
    }
    function V(n2) {
      return `${(0, m.indent)(1)}${o(n2)}`;
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/operation/operation-example.js
var require_operation_example = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/operation/operation-example.js"(exports, module) {
    var p = Object.defineProperty;
    var x = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var f = (t2, e2) => {
      for (var n2 in e2)
        p(t2, n2, { get: e2[n2], enumerable: true });
    };
    var g = (t2, e2, n2, a) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let i of c(e2))
          !d2.call(t2, i) && i !== n2 && p(t2, i, { get: () => e2[i], enumerable: !(a = x(e2, i)) || a.enumerable });
      return t2;
    };
    var E = (t2) => g(p({}, "__esModule", { value: true }), t2);
    var y = {};
    f(y, { getOperationExample: () => b, getOperationResponseExample: () => u2, getOperationVariablesExample: () => O });
    module.exports = E(y);
    var r = require_type();
    var o = require_field_example();
    var l = require_variables_example();
    function b({ operationType: t2, operation: e2, expandLevel: n2, multilineArguments: a, translate: i }) {
      const m = (0, l.getVariablesExample)(e2.args, a), s = (0, o.getFieldExample)({ field: e2, multilineArguments: a, indentation: 1, translate: i, maxLevel: n2 });
      return `${t2} ${e2.name}${m} {
${s}
}`;
    }
    function u2({ responseType: t2, expandLevel: e2 }) {
      return { data: (0, r.getTypeExample)({ type: t2, expandLevel: e2 }) };
    }
    function O({ args: t2, expandLevel: e2 }) {
      return t2.reduce((n2, a) => Object.assign(Object.assign({}, n2), { [a.name]: (0, r.getTypeExample)({ type: a.type, expandLevel: e2 }) }), {});
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/operation/operation-type.js
var require_operation_type = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/operation/operation-type.js"(exports, module) {
    var t2 = Object.defineProperty;
    var a = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var c = Object.prototype.hasOwnProperty;
    var d2 = (o, e2, x, r) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let p of b(e2))
          !c.call(o, p) && p !== x && t2(o, p, { get: () => e2[p], enumerable: !(r = a(e2, p)) || r.enumerable });
      return o;
    };
    var f = (o) => d2(t2({}, "__esModule", { value: true }), o);
    var g = {};
    module.exports = f(g);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/operation/index.js
var require_operation = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/operation/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (r, o, p, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let m of c(o))
          !d2.call(r, m) && m !== p && a(r, m, { get: () => o[m], enumerable: !(x = b(o, m)) || x.enumerable });
      return r;
    };
    var f = (r, o, p) => (t2(r, o, "default"), p && t2(p, o, "default"));
    var g = (r) => t2(a({}, "__esModule", { value: true }), r);
    var e2 = {};
    module.exports = g(e2);
    f(e2, require_operation_example(), module.exports);
    f(e2, require_operation_type(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/components/styled.js
var require_styled5 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/components/styled.js"(exports, module) {
    var p = Object.create;
    var i = Object.defineProperty;
    var s = Object.getOwnPropertyDescriptor;
    var l = Object.getOwnPropertyNames;
    var g = Object.getPrototypeOf;
    var v = Object.prototype.hasOwnProperty;
    var x = (o, e2) => {
      for (var r in e2)
        i(o, r, {
          get: e2[r],
          enumerable: true
        });
    };
    var c = (o, e2, r, d2) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let a of l(e2))
          !v.call(o, a) && a !== r && i(o, a, {
            get: () => e2[a],
            enumerable: !(d2 = s(e2, a)) || d2.enumerable
          });
      return o;
    };
    var b = (o, e2, r) => (r = o != null ? p(g(o)) : {}, c(e2 || !o || !o.__esModule ? i(r, "default", {
      value: o,
      enumerable: true
    }) : r, o));
    var h2 = (o) => c(i({}, "__esModule", {
      value: true
    }), o);
    var y = {};
    x(y, {
      SectionHeader: () => m,
      StyledCodeBlock: () => k,
      StyledJsonViewer: () => w,
      VariablesContainer: () => f
    });
    module.exports = h2(y);
    var n2 = b((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var t2 = (init_src(), __toCommonJS(src_exports));
    var m = n2.default.div`
  font-weight: var(--panel-heading-font-weight-local);
  line-height: var(--line-height-base);
  color: var(--panel-heading-text-color);
`;
    var k = (0, n2.default)(t2.CodeBlock)`
  border: none;
  margin: 0 0 0 auto;
  --code-block-padding: var(--spacing-xs) 0 var(--spacing-xs) 20px;

  .code-block-header {
    border-bottom: 0;
    padding-right: var(--spacing-sm);
    margin-top: 0;
  }
`;
    var f = n2.default.div`
  border-top: 1px solid var(--border-color-secondary);
`;
    var w = (0, n2.default)(t2.JsonViewer)`
  .code-block-header {
    margin-top: ${({
      $headerMarginTop: o = "var(--spacing-sm)"
    }) => o};
  }
  --code-block-padding: var(--spacing-xs) 0 var(--spacing-xs) 20px;
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/components/CodePanel.js
var require_CodePanel = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/components/CodePanel.js"(exports, module) {
    var s = Object.create;
    var o = Object.defineProperty;
    var t2 = Object.getOwnPropertyDescriptor;
    var g = Object.getOwnPropertyNames;
    var l = Object.getPrototypeOf;
    var c = Object.prototype.hasOwnProperty;
    var $ = (a, e2) => {
      for (var n2 in e2)
        o(a, n2, {
          get: e2[n2],
          enumerable: true
        });
    };
    var m = (a, e2, n2, d2) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let i of g(e2))
          !c.call(a, i) && i !== n2 && o(a, i, {
            get: () => e2[i],
            enumerable: !(d2 = t2(e2, i)) || d2.enumerable
          });
      return a;
    };
    var P = (a, e2, n2) => (n2 = a != null ? s(l(a)) : {}, m(e2 || !a || !a.__esModule ? o(n2, "default", {
      value: a,
      enumerable: true
    }) : n2, a));
    var b = (a) => m(o({}, "__esModule", {
      value: true
    }), a);
    var v = {};
    $(v, {
      CodePanel: () => f
    });
    module.exports = b(v);
    var p = P((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var r = (init_src(), __toCommonJS(src_exports));
    var f = (0, p.default)(r.Panel)`
  ${r.PanelBody} {
    padding: var(--spacing-sm) 0 0;
  }
  @media screen and (min-width: ${r.breakpoints.large}) {
    margin-top: ${({
      $spacing: a = "base"
    }) => `var(--spacing-${a})`};
  }
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/components/OperationRequestExample.js
var require_OperationRequestExample = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/components/OperationRequestExample.js"(exports, module) {
    var b = Object.create;
    var o = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var S = Object.getOwnPropertyNames;
    var q = Object.getPrototypeOf;
    var v = Object.prototype.hasOwnProperty;
    var y = (e2, a) => {
      for (var t2 in a)
        o(e2, t2, { get: a[t2], enumerable: true });
    };
    var p = (e2, a, t2, n2) => {
      if (a && typeof a == "object" || typeof a == "function")
        for (let s of S(a))
          !v.call(e2, s) && s !== t2 && o(e2, s, { get: () => a[s], enumerable: !(n2 = f(a, s)) || n2.enumerable });
      return e2;
    };
    var O = (e2, a, t2) => (t2 = e2 != null ? b(q(e2)) : {}, p(a || !e2 || !e2.__esModule ? o(t2, "default", { value: e2, enumerable: true }) : t2, e2));
    var V = (e2) => p(o({}, "__esModule", { value: true }), e2);
    var N = {};
    y(N, { OperationRequestExample: () => I });
    module.exports = V(N);
    var r = O(__require("react"));
    var c = (init_src(), __toCommonJS(src_exports));
    var m = require_operation();
    var l = require_styled5();
    var d2 = require_settings2();
    var u2 = require_CodePanel();
    var A = { query: "Query", mutation: "Mutation", subscription: "Subscription" };
    function I({ operation: e2, operationType: a }) {
      const { jsonSamplesDepth: t2, samplesMaxInlineArgs: n2 } = (0, d2.useSettings)(), { useTranslate: s } = (0, c.useThemeHooks)(), { translate: i } = s(), g = e2.args.length > n2, x = (0, m.getOperationExample)({ operationType: a, operation: e2, expandLevel: t2, multilineArguments: g, translate: i }), E = (0, m.getOperationVariablesExample)({ args: e2.args, expandLevel: t2 }), h2 = e2.args.length > 0;
      return r.default.createElement(u2.CodePanel, { className: "panel-response-samples", header: i(`graphql.${a}Sample`, `${A[a]} sample`), isExpandable: false, expanded: true, $spacing: "xxl" }, r.default.createElement(l.StyledCodeBlock, { lang: "graphql", source: x, dataTestId: "query-sample", header: { className: "code-block-header", controls: { copy: {} } } }), h2 ? r.default.createElement(l.VariablesContainer, null, r.default.createElement(l.StyledJsonViewer, { data: E, expandLevel: 1 / 0, title: r.default.createElement(l.SectionHeader, { "data-testid": "vars-title" }, i("graphql.variables", "Variables")) })) : null);
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/components/OperationReturnTypeExample.js
var require_OperationReturnTypeExample = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/components/OperationReturnTypeExample.js"(exports, module) {
    var f = Object.create;
    var p = Object.defineProperty;
    var x = Object.getOwnPropertyDescriptor;
    var u2 = Object.getOwnPropertyNames;
    var h2 = Object.getPrototypeOf;
    var E = Object.prototype.hasOwnProperty;
    var T = (e2, a) => {
      for (var t2 in a)
        p(e2, t2, { get: a[t2], enumerable: true });
    };
    var s = (e2, a, t2, o) => {
      if (a && typeof a == "object" || typeof a == "function")
        for (let n2 of u2(a))
          !E.call(e2, n2) && n2 !== t2 && p(e2, n2, { get: () => a[n2], enumerable: !(o = x(a, n2)) || o.enumerable });
      return e2;
    };
    var g = (e2, a, t2) => (t2 = e2 != null ? f(h2(e2)) : {}, s(a || !e2 || !e2.__esModule ? p(t2, "default", { value: e2, enumerable: true }) : t2, e2));
    var y = (e2) => s(p({}, "__esModule", { value: true }), e2);
    var S = {};
    T(S, { OperationReturnTypeExample: () => R });
    module.exports = y(S);
    var r = g(__require("react"));
    var m = (init_src(), __toCommonJS(src_exports));
    var l = require_operation();
    var i = require_settings2();
    var d2 = require_styled5();
    var c = require_CodePanel();
    function R({ type: e2 }) {
      const { jsonSamplesDepth: a } = (0, i.useSettings)(), { useTranslate: t2 } = (0, m.useThemeHooks)(), { translate: o } = t2(), n2 = (0, l.getOperationResponseExample)({ responseType: e2, expandLevel: a });
      return r.default.createElement(c.CodePanel, { className: "panel-response-samples", header: o("graphql.responseSample", "Response sample"), expanded: true, isExpandable: false }, r.default.createElement(d2.StyledJsonViewer, { data: n2, expandLevel: 1 / 0, $headerMarginTop: "0" }));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/components/OperationExample.js
var require_OperationExample = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/components/OperationExample.js"(exports, module) {
    var E = Object.create;
    var m = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var s = Object.getOwnPropertyNames;
    var u2 = Object.getPrototypeOf;
    var x = Object.prototype.hasOwnProperty;
    var y = (e2, t2) => {
      for (var r in t2)
        m(e2, r, { get: t2[r], enumerable: true });
    };
    var i = (e2, t2, r, a) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let o of s(t2))
          !x.call(e2, o) && o !== r && m(e2, o, { get: () => t2[o], enumerable: !(a = f(t2, o)) || a.enumerable });
      return e2;
    };
    var R = (e2, t2, r) => (r = e2 != null ? E(u2(e2)) : {}, i(t2 || !e2 || !e2.__esModule ? m(r, "default", { value: e2, enumerable: true }) : r, e2));
    var O = (e2) => i(m({}, "__esModule", { value: true }), e2);
    var g = {};
    y(g, { OperationExample: () => d2 });
    module.exports = O(g);
    var p = R(__require("react"));
    var n2 = require_OperationRequestExample();
    var c = require_OperationReturnTypeExample();
    var l = require_app();
    function d2({ operationType: e2, operation: t2 }) {
      return p.default.createElement(l.RightSection, { "data-testid": ("right-section-" + t2.name).toLowerCase() }, p.default.createElement(n2.OperationRequestExample, { operation: t2, operationType: e2 }), p.default.createElement(c.OperationReturnTypeExample, { type: t2.type }));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/components/TypeExample.js
var require_TypeExample = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/components/TypeExample.js"(exports, module) {
    var c = Object.create;
    var m = Object.defineProperty;
    var x = Object.getOwnPropertyDescriptor;
    var E = Object.getOwnPropertyNames;
    var f = Object.getPrototypeOf;
    var u2 = Object.prototype.hasOwnProperty;
    var T = (e2, a) => {
      for (var t2 in a)
        m(e2, t2, { get: a[t2], enumerable: true });
    };
    var r = (e2, a, t2, p) => {
      if (a && typeof a == "object" || typeof a == "function")
        for (let n2 of E(a))
          !u2.call(e2, n2) && n2 !== t2 && m(e2, n2, { get: () => a[n2], enumerable: !(p = x(a, n2)) || p.enumerable });
      return e2;
    };
    var y = (e2, a, t2) => (t2 = e2 != null ? c(f(e2)) : {}, r(a || !e2 || !e2.__esModule ? m(t2, "default", { value: e2, enumerable: true }) : t2, e2));
    var g = (e2) => r(m({}, "__esModule", { value: true }), e2);
    var C = {};
    T(C, { TypeExample: () => v });
    module.exports = g(C);
    var o = y(__require("react"));
    var l = (init_src(), __toCommonJS(src_exports));
    var s = require_type();
    var d2 = require_styled5();
    var i = require_CodePanel();
    var h2 = 1;
    function L({ type: e2 }) {
      const a = (0, s.getTypeExample)({ type: e2, expandLevel: h2 }), { useTranslate: t2 } = (0, l.useThemeHooks)(), { translate: p } = t2();
      return o.default.createElement(i.CodePanel, { className: "panel-response-samples", "data-testid": "\u0441ode-panel", header: p("graphql.sample", "Sample"), expanded: true, isExpandable: false, $spacing: "xxl" }, o.default.createElement(d2.StyledJsonViewer, { data: a, expandLevel: 1 / 0, $headerMarginTop: "0" }));
    }
    var v = (0, o.memo)(L);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/components/index.js
var require_components5 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/components/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (r, o, p, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let m of c(o))
          !d2.call(r, m) && m !== p && a(r, m, { get: () => o[m], enumerable: !(x = b(o, m)) || x.enumerable });
      return r;
    };
    var f = (r, o, p) => (t2(r, o, "default"), p && t2(p, o, "default"));
    var g = (r) => t2(a({}, "__esModule", { value: true }), r);
    var e2 = {};
    module.exports = g(e2);
    f(e2, require_OperationExample(), module.exports);
    f(e2, require_TypeExample(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/examples/index.js
var require_examples = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/examples/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var p = (r, o, f, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of c(o))
          !d2.call(r, e2) && e2 !== f && a(r, e2, { get: () => o[e2], enumerable: !(x = b(o, e2)) || x.enumerable });
      return r;
    };
    var t2 = (r, o, f) => (p(r, o, "default"), f && p(f, o, "default"));
    var g = (r) => p(a({}, "__esModule", { value: true }), r);
    var m = {};
    module.exports = g(m);
    t2(m, require_components5(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/FieldsTypeItem.js
var require_FieldsTypeItem = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/FieldsTypeItem.js"(exports, module) {
    var h2 = Object.create;
    var c = Object.defineProperty;
    var I = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var S = Object.getPrototypeOf;
    var j = Object.prototype.hasOwnProperty;
    var k = (r, t2) => {
      for (var n2 in t2)
        c(r, n2, { get: t2[n2], enumerable: true });
    };
    var f = (r, t2, n2, a) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let l of b(t2))
          !j.call(r, l) && l !== n2 && c(r, l, { get: () => t2[l], enumerable: !(a = I(t2, l)) || a.enumerable });
      return r;
    };
    var x = (r, t2, n2) => (n2 = r != null ? h2(S(r)) : {}, f(t2 || !r || !r.__esModule ? c(n2, "default", { value: r, enumerable: true }) : n2, r));
    var L = (r) => f(c({}, "__esModule", { value: true }), r);
    var H = {};
    k(H, { FieldsTypeItem: () => R });
    module.exports = L(H);
    var u2 = (init_graphql2(), __toCommonJS(graphql_exports));
    var e2 = x(__require("react"));
    var p = (init_src(), __toCommonJS(src_exports));
    var E = require_shared();
    var o = require_components4();
    var m = require_app();
    var g = require_schema();
    var y = require_examples();
    var T = require_markdown();
    var v = require_settings2();
    var w = require_navigation2();
    var d2 = require_common2();
    var F = require_Headers();
    function M({ name: r }) {
      const t2 = (0, m.useStore)(), { markdown: n2 } = (0, v.useSettings)(), { useTranslate: a } = (0, p.useThemeHooks)(), { translate: l } = a(), { type: i, references: s } = (0, e2.useMemo)(() => ({ type: t2?.getType(r), references: t2?.getTypeReferences(r) }), [r, t2]);
      return (0, g.isFieldsType)(i) ? e2.default.createElement(d2.Box, { "data-testid": i.name, direction: "row", wrap: "wrap" }, e2.default.createElement(m.MiddleSection, { "data-testid": "object-item" }, e2.default.createElement(p.H2, { "data-testid": "title" }, e2.default.createElement(w.ShareLink, null, e2.default.createElement(E.SplittedWbrText, { text: i.name }))), e2.default.createElement(o.Annotation, { "data-testid": "object-description" }, e2.default.createElement(T.Markdown, { rawMarkdown: i.description, parser: n2?.parser })), Object.values(i.getFields()).length ? e2.default.createElement(d2.Box, { direction: "column", wrap: "wrap" }, e2.default.createElement(o.FieldList, { fields: Object.values(i.getFields()), renderField: o.FieldView })) : null, !(0, u2.isInputObjectType)(i) && i.getInterfaces().length ? e2.default.createElement(d2.Box, { direction: "column" }, e2.default.createElement(F.FieldsGroupHeader, { "data-testid": "implements-interfaces" }, l("graphql.implementedInterfaces", "Implements interfaces")), e2.default.createElement(o.GroupTypeList, { types: i.getInterfaces() })) : null), e2.default.createElement(m.RightSection, { "data-testid": "right-section" }, e2.default.createElement(y.TypeExample, { type: i }), s?.length ? e2.default.createElement(o.ReferenceList, { references: s }) : null)) : null;
    }
    var R = (0, e2.memo)(M);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/MutationItem.js
var require_MutationItem = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/MutationItem.js"(exports, module) {
    var c = Object.create;
    var n2 = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var s = Object.getOwnPropertyNames;
    var E = Object.getPrototypeOf;
    var w = Object.prototype.hasOwnProperty;
    var x = (t2, e2) => {
      for (var o in e2)
        n2(t2, o, { get: e2[o], enumerable: true });
    };
    var a = (t2, e2, o, m) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let i of s(e2))
          !w.call(t2, i) && i !== o && n2(t2, i, { get: () => e2[i], enumerable: !(m = f(e2, i)) || m.enumerable });
      return t2;
    };
    var M = (t2, e2, o) => (o = t2 != null ? c(E(t2)) : {}, a(e2 || !t2 || !t2.__esModule ? n2(o, "default", { value: t2, enumerable: true }) : o, t2));
    var v = (t2) => a(n2({}, "__esModule", { value: true }), t2);
    var g = {};
    x(g, { MutationItem: () => I });
    module.exports = v(g);
    var r = M(__require("react"));
    var p = require_components4();
    var l = require_common2();
    var d2 = require_app();
    var u2 = require_examples();
    function F({ name: t2 }) {
      const e2 = (0, d2.useStore)(), o = (0, r.useMemo)(() => e2?.getMutationField(t2), [t2, e2]);
      return o ? r.default.createElement(l.Box, { "data-testid": o.name, direction: "row", wrap: "wrap" }, r.default.createElement(p.ExtendedFieldView, { field: o }), r.default.createElement(u2.OperationExample, { operationType: "mutation", operation: o })) : null;
    }
    var I = (0, r.memo)(F);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/QueryItem.js
var require_QueryItem = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/QueryItem.js"(exports, module) {
    var c = Object.create;
    var m = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var s = Object.getOwnPropertyNames;
    var y = Object.getPrototypeOf;
    var E = Object.prototype.hasOwnProperty;
    var w = (e2, t2) => {
      for (var o in t2)
        m(e2, o, { get: t2[o], enumerable: true });
    };
    var p = (e2, t2, o, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let i of s(t2))
          !E.call(e2, i) && i !== o && m(e2, i, { get: () => t2[i], enumerable: !(n2 = f(t2, i)) || n2.enumerable });
      return e2;
    };
    var x = (e2, t2, o) => (o = e2 != null ? c(y(e2)) : {}, p(t2 || !e2 || !e2.__esModule ? m(o, "default", { value: e2, enumerable: true }) : o, e2));
    var Q = (e2) => p(m({}, "__esModule", { value: true }), e2);
    var I = {};
    w(I, { QueryItem: () => F });
    module.exports = Q(I);
    var r = x(__require("react"));
    var a = require_components4();
    var l = require_common2();
    var d2 = require_app();
    var u2 = require_examples();
    function v({ name: e2 }) {
      const t2 = (0, d2.useStore)(), o = (0, r.useMemo)(() => t2?.getQueryField(e2), [e2, t2]);
      return o ? r.default.createElement(l.Box, { "data-testid": o.name, direction: "row", wrap: "wrap" }, r.default.createElement(a.ExtendedFieldView, { field: o }), r.default.createElement(u2.OperationExample, { operationType: "query", operation: o })) : null;
    }
    var F = (0, r.memo)(v);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/ScalarItem.js
var require_ScalarItem = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/ScalarItem.js"(exports, module) {
    var v = Object.create;
    var c = Object.defineProperty;
    var E = Object.getOwnPropertyDescriptor;
    var w = Object.getOwnPropertyNames;
    var S = Object.getPrototypeOf;
    var g = Object.prototype.hasOwnProperty;
    var h2 = (e2, t2) => {
      for (var o in t2)
        c(e2, o, { get: t2[o], enumerable: true });
    };
    var l = (e2, t2, o, i) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let n2 of w(t2))
          !g.call(e2, n2) && n2 !== o && c(e2, n2, { get: () => t2[n2], enumerable: !(i = E(t2, n2)) || i.enumerable });
      return e2;
    };
    var k = (e2, t2, o) => (o = e2 != null ? v(S(e2)) : {}, l(t2 || !e2 || !e2.__esModule ? c(o, "default", { value: e2, enumerable: true }) : o, e2));
    var y = (e2) => l(c({}, "__esModule", { value: true }), e2);
    var x = {};
    h2(x, { ScalarItem: () => R });
    module.exports = y(x);
    var r = k(__require("react"));
    var d2 = (init_src(), __toCommonJS(src_exports));
    var m = require_components4();
    var a = require_app();
    var s = require_markdown();
    var p = require_settings2();
    var f = require_navigation2();
    var u2 = require_common2();
    function M({ name: e2 }) {
      const t2 = (0, a.useStore)(), { markdown: o } = (0, p.useSettings)(), { type: i, references: n2 } = (0, r.useMemo)(() => ({ type: t2?.getType(e2), references: t2?.getTypeReferences(e2) }), [e2, t2]);
      return i ? r.default.createElement(u2.Box, { "data-testid": i.name, direction: "row", wrap: "wrap" }, r.default.createElement(a.MiddleSection, { "data-testid": e2 }, r.default.createElement(d2.H2, { "data-testid": "title" }, r.default.createElement(f.ShareLink, null, e2)), r.default.createElement(m.Annotation, { "data-testid": "object-description" }, r.default.createElement(s.Markdown, { rawMarkdown: i.description, parser: o?.parser }))), n2?.length ? r.default.createElement(a.RightSection, { "data-testid": "right-section" }, r.default.createElement(m.ReferenceList, { references: n2 })) : null) : null;
    }
    var R = (0, r.memo)(M);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/SubscriptionItem.js
var require_SubscriptionItem = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/SubscriptionItem.js"(exports, module) {
    var d2 = Object.create;
    var n2 = Object.defineProperty;
    var u2 = Object.getOwnPropertyDescriptor;
    var f = Object.getOwnPropertyNames;
    var E = Object.getPrototypeOf;
    var b = Object.prototype.hasOwnProperty;
    var w = (e2, t2) => {
      for (var o in t2)
        n2(e2, o, { get: t2[o], enumerable: true });
    };
    var p = (e2, t2, o, m) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let i of f(t2))
          !b.call(e2, i) && i !== o && n2(e2, i, { get: () => t2[i], enumerable: !(m = u2(t2, i)) || m.enumerable });
      return e2;
    };
    var x = (e2, t2, o) => (o = e2 != null ? d2(E(e2)) : {}, p(t2 || !e2 || !e2.__esModule ? n2(o, "default", { value: e2, enumerable: true }) : o, e2));
    var S = (e2) => p(n2({}, "__esModule", { value: true }), e2);
    var I = {};
    w(I, { SubscriptionItem: () => F });
    module.exports = S(I);
    var r = x(__require("react"));
    var c = require_components4();
    var a = require_common2();
    var l = require_app();
    var s = require_examples();
    function v({ name: e2 }) {
      const t2 = (0, l.useStore)(), o = (0, r.useMemo)(() => t2?.getSubscriptionField(e2), [e2, t2]);
      return o ? r.default.createElement(a.Box, { "data-testid": o.name, direction: "row", wrap: "wrap" }, r.default.createElement(c.ExtendedFieldView, { field: o }), r.default.createElement(s.OperationExample, { operationType: "subscription", operation: o })) : null;
    }
    var F = (0, r.memo)(v);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/UnionItem.js
var require_UnionItem = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/UnionItem.js"(exports, module) {
    var v = Object.create;
    var c = Object.defineProperty;
    var h2 = Object.getOwnPropertyDescriptor;
    var w = Object.getOwnPropertyNames;
    var k = Object.getPrototypeOf;
    var S = Object.prototype.hasOwnProperty;
    var M = (e2, t2) => {
      for (var r in t2)
        c(e2, r, { get: t2[r], enumerable: true });
    };
    var u2 = (e2, t2, r, m) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let n2 of w(t2))
          !S.call(e2, n2) && n2 !== r && c(e2, n2, { get: () => t2[n2], enumerable: !(m = h2(t2, n2)) || m.enumerable });
      return e2;
    };
    var R = (e2, t2, r) => (r = e2 != null ? v(k(e2)) : {}, u2(t2 || !e2 || !e2.__esModule ? c(r, "default", { value: e2, enumerable: true }) : r, e2));
    var b = (e2) => u2(c({}, "__esModule", { value: true }), e2);
    var U = {};
    M(U, { UnionItem: () => L });
    module.exports = b(U);
    var o = R(__require("react"));
    var f = (init_graphql2(), __toCommonJS(graphql_exports));
    var d2 = (init_src(), __toCommonJS(src_exports));
    var s = require_components4();
    var a = require_app();
    var E = require_markdown();
    var g = require_settings2();
    var y = require_navigation2();
    var p = require_common2();
    var T = require_Headers();
    function H({ name: e2 }) {
      const t2 = (0, a.useStore)(), { markdown: r } = (0, g.useSettings)(), { useTranslate: m } = (0, d2.useThemeHooks)(), { translate: n2 } = m(), { union: i, references: l } = (0, o.useMemo)(() => ({ union: t2?.getType(e2), references: t2?.getTypeReferences(e2) }), [e2, t2]);
      return (0, f.isUnionType)(i) ? o.default.createElement(p.Box, { "data-testid": i.name, direction: "row", wrap: "wrap" }, o.default.createElement(a.MiddleSection, { "data-testid": i.name }, o.default.createElement(d2.H2, { "data-testid": "title" }, o.default.createElement(y.ShareLink, null, i.name)), o.default.createElement(s.Annotation, { "data-testid": "object-description" }, o.default.createElement(E.Markdown, { rawMarkdown: i.description, parser: r?.parser })), i.getTypes().length ? o.default.createElement(p.Box, { "data-testid": e2, direction: "column" }, o.default.createElement(T.FieldsGroupHeader, { "data-testid": "return-type-title" }, n2("graphql.possibleTypes", "Possible types")), o.default.createElement(s.GroupTypeList, { types: i.getTypes() })) : null), l?.length ? o.default.createElement(a.RightSection, { "data-testid": "right-section" }, o.default.createElement(s.ReferenceList, { references: l })) : null) : null;
    }
    var L = (0, o.memo)(H);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/GraphQlItem.js
var require_GraphQlItem = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/GraphQlItem.js"(exports, module) {
    var E = Object.create;
    var i = Object.defineProperty;
    var G = Object.getOwnPropertyDescriptor;
    var M = Object.getOwnPropertyNames;
    var Q = Object.getPrototypeOf;
    var T = Object.prototype.hasOwnProperty;
    var d2 = (t2, m) => {
      for (var o in m)
        i(t2, o, { get: m[o], enumerable: true });
    };
    var s = (t2, m, o, p) => {
      if (m && typeof m == "object" || typeof m == "function")
        for (let e2 of M(m))
          !T.call(t2, e2) && e2 !== o && i(t2, e2, { get: () => m[e2], enumerable: !(p = G(m, e2)) || p.enumerable });
      return t2;
    };
    var v = (t2, m, o) => (o = t2 != null ? E(Q(t2)) : {}, s(m || !t2 || !t2.__esModule ? i(o, "default", { value: t2, enumerable: true }) : o, t2));
    var A = (t2) => s(i({}, "__esModule", { value: true }), t2);
    var S = {};
    d2(S, { GraphQlItem: () => R });
    module.exports = A(S);
    var c = v(__require("react"));
    var u2 = require_DirectiveItem();
    var f = require_EnumItem();
    var n2 = require_FieldsTypeItem();
    var a = require_MutationItem();
    var I = require_QueryItem();
    var l = require_ScalarItem();
    var b = require_SubscriptionItem();
    var y = require_UnionItem();
    var r = require_schema();
    var P = { [r.TypeGroup.queries]: I.QueryItem, [r.TypeGroup.mutations]: a.MutationItem, [r.TypeGroup.subscriptions]: b.SubscriptionItem, [r.TypeGroup.directives]: u2.DirectiveItem, [r.TypeGroup.objects]: n2.FieldsTypeItem, [r.TypeGroup.interfaces]: n2.FieldsTypeItem, [r.TypeGroup.inputs]: n2.FieldsTypeItem, [r.TypeGroup.unions]: y.UnionItem, [r.TypeGroup.enums]: f.EnumItem, [r.TypeGroup.scalars]: l.ScalarItem };
    function R({ name: t2, type: m }) {
      const o = P[m];
      return c.default.createElement(o, { name: t2 });
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/items/index.js
var require_items = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/items/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var t2 = (f, e2, p, x) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let m of c(e2))
          !d2.call(f, m) && m !== p && a(f, m, { get: () => e2[m], enumerable: !(x = b(e2, m)) || x.enumerable });
      return f;
    };
    var r = (f, e2, p) => (t2(f, e2, "default"), p && t2(p, e2, "default"));
    var g = (f) => t2(a({}, "__esModule", { value: true }), f);
    var o = {};
    module.exports = g(o);
    r(o, require_GraphQlItem(), module.exports);
    r(o, require_QueryItem(), module.exports);
    r(o, require_MutationItem(), module.exports);
    r(o, require_SubscriptionItem(), module.exports);
    r(o, require_FieldsTypeItem(), module.exports);
    r(o, require_DirectiveItem(), module.exports);
    r(o, require_EnumItem(), module.exports);
    r(o, require_UnionItem(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/DocsContent.js
var require_DocsContent = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/DocsContent.js"(exports, module) {
    var y = Object.create;
    var a = Object.defineProperty;
    var E = Object.getOwnPropertyDescriptor;
    var h2 = Object.getOwnPropertyNames;
    var k = Object.getPrototypeOf;
    var C = Object.prototype.hasOwnProperty;
    var G = (e2, n2) => {
      for (var t2 in n2)
        a(e2, t2, {
          get: n2[t2],
          enumerable: true
        });
    };
    var c = (e2, n2, t2, i) => {
      if (n2 && typeof n2 == "object" || typeof n2 == "function")
        for (let m of h2(n2))
          !C.call(e2, m) && m !== t2 && a(e2, m, {
            get: () => n2[m],
            enumerable: !(i = E(n2, m)) || i.enumerable
          });
      return e2;
    };
    var p = (e2, n2, t2) => (t2 = e2 != null ? y(k(e2)) : {}, c(n2 || !e2 || !e2.__esModule ? a(t2, "default", {
      value: e2,
      enumerable: true
    }) : t2, e2));
    var I = (e2) => c(a({}, "__esModule", {
      value: true
    }), e2);
    var M = {};
    G(M, {
      DocsContent: () => b
    });
    module.exports = I(M);
    var o = p(__require("react"));
    var s = p((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var l = (init_src(), __toCommonJS(src_exports));
    var u2 = require_items();
    var d2 = require_components4();
    var r = require_navigation2();
    var f = require_menu();
    var S = (0, s.default)(l.ThreePanelLayout)`
  && {
    display: block;
  }
`;
    function b() {
      const e2 = o.default.useContext(f.MenuStoreContext), n2 = o.default.useMemo(() => e2?.getGroups().flatMap((t2) => t2.typeGroups).map((t2) => x(t2)), [e2]);
      return n2 ? o.default.createElement(S, {
        "data-testid": "content-panel"
      }, o.default.createElement(d2.Breadcrumbs, null), o.default.createElement(r.Documentation, null, n2)) : null;
    }
    function x(e2) {
      return o.default.createElement(r.Section, {
        header: o.default.createElement(l.H1, {
          style: {
            display: "inline-block"
          },
          "data-testid": "group-title"
        }, e2.name),
        name: e2.name,
        path: e2.id,
        key: e2.id
      }, D(e2.items, e2.typeGroupId));
    }
    function D(e2, n2) {
      return e2.map((t2) => o.default.createElement(r.Item, {
        name: t2,
        path: t2,
        key: t2
      }, o.default.createElement(u2.GraphQlItem, {
        name: t2,
        type: n2
      })));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/definition/index.js
var require_definition = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/definition/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var p = (r, o, f, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of c(o))
          !d2.call(r, e2) && e2 !== f && a(r, e2, { get: () => o[e2], enumerable: !(x = b(o, e2)) || x.enumerable });
      return r;
    };
    var t2 = (r, o, f) => (p(r, o, "default"), f && p(f, o, "default"));
    var g = (r) => p(a({}, "__esModule", { value: true }), r);
    var m = {};
    module.exports = g(m);
    t2(m, require_DocsContent(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/sidebar/styled.js
var require_styled6 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/sidebar/styled.js"(exports, module) {
    var l = Object.create;
    var d2 = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var s = Object.getOwnPropertyNames;
    var n2 = Object.getPrototypeOf;
    var p = Object.prototype.hasOwnProperty;
    var h2 = (o, r) => {
      for (var i in r)
        d2(o, i, {
          get: r[i],
          enumerable: true
        });
    };
    var a = (o, r, i, t2) => {
      if (r && typeof r == "object" || typeof r == "function")
        for (let e2 of s(r))
          !p.call(o, e2) && e2 !== i && d2(o, e2, {
            get: () => r[e2],
            enumerable: !(t2 = b(r, e2)) || t2.enumerable
          });
      return o;
    };
    var f = (o, r, i) => (i = o != null ? l(n2(o)) : {}, a(r || !o || !o.__esModule ? d2(i, "default", {
      value: o,
      enumerable: true
    }) : i, o));
    var v = (o) => a(d2({}, "__esModule", {
      value: true
    }), o);
    var g = {};
    h2(g, {
      SidebarWrapper: () => x
    });
    module.exports = v(g);
    var c = f((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var x = c.default.aside`
  position: sticky;
  top: 0;
  flex-direction: column;
  flex: 0 0 auto;
  overflow: hidden;
  backface-visibility: hidden;
  height: 100vh;
  z-index: 20;
  background-color: var(--sidebar-bg-color);
  border-right: 1px solid var(--sidebar-border-color);
`;
  }
});

// node_modules/@redocly/graphql-docs/lib/src/sidebar/use-sidebar-items-with-state.js
var require_use_sidebar_items_with_state = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/sidebar/use-sidebar-items-with-state.js"(exports, module) {
    var r = Object.defineProperty;
    var L = Object.getOwnPropertyDescriptor;
    var S = Object.getOwnPropertyNames;
    var f = Object.prototype.hasOwnProperty;
    var h2 = (e2, t2) => {
      for (var s in t2)
        r(e2, s, { get: t2[s], enumerable: true });
    };
    var C = (e2, t2, s, o) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let n2 of S(t2))
          !f.call(e2, n2) && n2 !== s && r(e2, n2, { get: () => t2[n2], enumerable: !(o = L(t2, n2)) || o.enumerable });
      return e2;
    };
    var d2 = (e2) => C(r({}, "__esModule", { value: true }), e2);
    var b = {};
    h2(b, { useSidebarItemsWithState: () => A });
    module.exports = d2(b);
    var l = (init_src(), __toCommonJS(src_exports));
    var p = __require("react-router-dom");
    var i = require_paths();
    var A = (e2) => {
      const t2 = (0, p.useLocation)(), s = (0, l.useActiveSectionId)(t2);
      return c(e2, s, t2);
    };
    var c = (e2, t2, s) => e2.map((o) => {
      const n2 = m(o, t2, s), a = u2(o, t2, s);
      return Object.assign(Object.assign({}, o), { items: c(o.items, t2, s), active: n2 && !a, hasActiveSubItem: a });
    });
    var m = (e2, t2, s) => {
      if (!e2.link)
        return false;
      const [o, n2] = e2.link.split("#");
      return (0, i.stripTrailingSlash)(decodeURIComponent(s.pathname.toLocaleLowerCase())) === (0, i.stripTrailingSlash)(decodeURIComponent(o.toLocaleLowerCase())) && (!n2 || n2.toLocaleLowerCase() === t2.toLocaleLowerCase());
    };
    var u2 = (e2, t2, s) => e2.items.some((o) => m(o, t2, s) || u2(o, t2, s));
  }
});

// node_modules/@redocly/graphql-docs/lib/src/sidebar/Sidebar.js
var require_Sidebar = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/sidebar/Sidebar.js"(exports, module) {
    var b = Object.create;
    var s = Object.defineProperty;
    var S = Object.getOwnPropertyDescriptor;
    var E = Object.getOwnPropertyNames;
    var f = Object.getPrototypeOf;
    var v = Object.prototype.hasOwnProperty;
    var g = (t2, e2) => {
      for (var n2 in e2)
        s(t2, n2, { get: e2[n2], enumerable: true });
    };
    var m = (t2, e2, n2, o) => {
      if (e2 && typeof e2 == "object" || typeof e2 == "function")
        for (let a of E(e2))
          !v.call(t2, a) && a !== n2 && s(t2, a, { get: () => e2[a], enumerable: !(o = S(e2, a)) || o.enumerable });
      return t2;
    };
    var I = (t2, e2, n2) => (n2 = t2 != null ? b(f(t2)) : {}, m(e2 || !t2 || !t2.__esModule ? s(n2, "default", { value: t2, enumerable: true }) : n2, t2));
    var h2 = (t2) => m(s({}, "__esModule", { value: true }), t2);
    var C = {};
    g(C, { Sidebar: () => k });
    module.exports = h2(C);
    var r = I(__require("react"));
    var i = (init_src(), __toCommonJS(src_exports));
    var c = require_styled6();
    var l = require_menu();
    var d2 = require_use_sidebar_items_with_state();
    var p = require_settings_context();
    function k() {
      const { sidebar: t2, apiLogo: e2 } = (0, p.useSettings)(), n2 = (0, r.useContext)(l.MenuStoreContext), o = n2?.getSidebarItems() || [], a = (0, d2.useSidebarItemsWithState)(o);
      return t2?.hide ? null : r.default.createElement(c.SidebarWrapper, null, r.default.createElement(i.Sidebar, { versions: null, menu: r.default.createElement(r.default.Fragment, null, r.default.createElement(i.SidebarLogo, Object.assign({ dataTestId: "logo" }, e2)), u2(a)) }));
    }
    var u2 = (t2) => r.default.createElement(r.default.Fragment, null, t2.map((e2, n2) => e2.type === "group" && e2.items.length > 0 ? r.default.createElement(x, { key: n2, item: e2 }) : r.default.createElement(i.MenuItem, { key: n2, item: e2 })));
    var x = (t2) => {
      const { item: e2 } = t2, [n2, o] = (0, r.useState)(e2.active || e2.hasActiveSubItem);
      (0, r.useEffect)(() => {
        o(e2.active || e2.hasActiveSubItem);
      }, [e2.active, e2.hasActiveSubItem]);
      const a = (0, r.useCallback)(() => {
        o(!n2);
      }, [n2]);
      return r.default.createElement(i.MenuItem, { item: e2, isExpanded: n2, onClick: a }, u2(e2.items));
    };
  }
});

// node_modules/@redocly/graphql-docs/lib/src/sidebar/index.js
var require_sidebar = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/sidebar/index.js"(exports, module) {
    var a = Object.defineProperty;
    var b = Object.getOwnPropertyDescriptor;
    var c = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var p = (r, o, f, x) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of c(o))
          !d2.call(r, e2) && e2 !== f && a(r, e2, { get: () => o[e2], enumerable: !(x = b(o, e2)) || x.enumerable });
      return r;
    };
    var t2 = (r, o, f) => (p(r, o, "default"), f && p(f, o, "default"));
    var g = (r) => p(a({}, "__esModule", { value: true }), r);
    var m = {};
    module.exports = g(m);
    t2(m, require_Sidebar(), module.exports);
  }
});

// node_modules/@redocly/graphql-docs/lib/src/styled.js
var require_styled7 = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/styled.js"(exports, module) {
    var l = Object.create;
    var a = Object.defineProperty;
    var f = Object.getOwnPropertyDescriptor;
    var g = Object.getOwnPropertyNames;
    var h2 = Object.getPrototypeOf;
    var v = Object.prototype.hasOwnProperty;
    var p = (t2, i) => {
      for (var o in i)
        a(t2, o, {
          get: i[o],
          enumerable: true
        });
    };
    var n2 = (t2, i, o, r) => {
      if (i && typeof i == "object" || typeof i == "function")
        for (let e2 of g(i))
          !v.call(t2, e2) && e2 !== o && a(t2, e2, {
            get: () => i[e2],
            enumerable: !(r = f(i, e2)) || r.enumerable
          });
      return t2;
    };
    var b = (t2, i, o) => (o = t2 != null ? l(h2(t2)) : {}, n2(i || !t2 || !t2.__esModule ? a(o, "default", {
      value: t2,
      enumerable: true
    }) : o, t2));
    var d2 = (t2) => n2(a({}, "__esModule", {
      value: true
    }), t2);
    var m = {};
    p(m, {
      GraphQLDocsWrapper: () => c
    });
    module.exports = d2(m);
    var s = b((init_styled_components_esm(), __toCommonJS(styled_components_esm_exports)));
    var c = s.default.div`
  display: flex;
  width: 100%;
  position: relative;
  text-align: left;
  text-size-adjust: 100%;

  font-family: var(--font-family-base);
  font-size: var(--font-size-base);
  line-height: var(--line-height-base);
  font-weight: var(--font-weight-regular);
  color: var(--text-color-primary);
  -webkit-font-smoothing: var(--text-smoothing);

  * {
    box-sizing: border-box;
    -webkit-tap-highlight-color: rgba(255, 255, 255, 0);
  }

  div[data-section-id] {
    scroll-margin-top: var(--navigation-anchor-offset);
  }
`;
  }
});

// node_modules/@redocly/realm/dist/server/esbuild/plugins/openapi-docs-module-replacer/empty-use-license-key.js
var empty_use_license_key_exports = {};
__export(empty_use_license_key_exports, {
  useLicenseKey: () => e
});
function e() {
  return { isValid: true, loading: false };
}
var init_empty_use_license_key = __esm({
  "node_modules/@redocly/realm/dist/server/esbuild/plugins/openapi-docs-module-replacer/empty-use-license-key.js"() {
  }
});

// node_modules/@redocly/graphql-docs/lib/src/common/utils/is-browser.js
var require_is_browser = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/common/utils/is-browser.js"(exports, module) {
    var c = Object.defineProperty;
    var i = Object.getOwnPropertyDescriptor;
    var s = Object.getOwnPropertyNames;
    var u2 = Object.prototype.hasOwnProperty;
    var w = (n2, t2) => {
      for (var r in t2)
        c(n2, r, { get: t2[r], enumerable: true });
    };
    var a = (n2, t2, r, o) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let e2 of s(t2))
          !u2.call(n2, e2) && e2 !== r && c(n2, e2, { get: () => t2[e2], enumerable: !(o = i(t2, e2)) || o.enumerable });
      return n2;
    };
    var h2 = (n2) => a(c({}, "__esModule", { value: true }), n2);
    var S = {};
    w(S, { IS_BROWSER: () => R });
    module.exports = h2(S);
    var R = new Function("try { return this === window; } catch (e) { return false; }")();
  }
});

// node_modules/@redocly/graphql-docs/lib/src/RedoclyGraphQLDocs.js
var require_RedoclyGraphQLDocs = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/RedoclyGraphQLDocs.js"(exports, module) {
    var S = Object.create;
    var i = Object.defineProperty;
    var L = Object.getOwnPropertyDescriptor;
    var b = Object.getOwnPropertyNames;
    var w = Object.getPrototypeOf;
    var j = Object.prototype.hasOwnProperty;
    var C = (e2, t2) => {
      for (var r in t2)
        i(e2, r, { get: t2[r], enumerable: true });
    };
    var c = (e2, t2, r, n2) => {
      if (t2 && typeof t2 == "object" || typeof t2 == "function")
        for (let o of b(t2))
          !j.call(e2, o) && o !== r && i(e2, o, { get: () => t2[o], enumerable: !(n2 = L(t2, o)) || n2.enumerable });
      return e2;
    };
    var R = (e2, t2, r) => (r = e2 != null ? S(w(e2)) : {}, c(t2 || !e2 || !e2.__esModule ? i(r, "default", { value: e2, enumerable: true }) : r, e2));
    var x = (e2) => c(i({}, "__esModule", { value: true }), e2);
    var K = {};
    C(K, { RedoclyGraphQLDocs: () => G });
    module.exports = x(K);
    var l = R(__require("react"));
    var _ = require_polyfills();
    var m = require_app();
    var s = require_definition();
    var p = require_navigation2();
    var f = require_schema();
    var u2 = require_sidebar();
    var h2 = require_styled7();
    var y = (init_empty_use_license_key(), __toCommonJS(empty_use_license_key_exports));
    var d2 = require_is_browser();
    var g = require_common2();
    var D = function(e2, t2) {
      var r = {};
      for (var n2 in e2)
        Object.prototype.hasOwnProperty.call(e2, n2) && t2.indexOf(n2) < 0 && (r[n2] = e2[n2]);
      if (e2 != null && typeof Object.getOwnPropertySymbols == "function")
        for (var o = 0, n2 = Object.getOwnPropertySymbols(e2); o < n2.length; o++)
          t2.indexOf(n2[o]) < 0 && Object.prototype.propertyIsEnumerable.call(e2, n2[o]) && (r[n2[o]] = e2[n2[o]]);
      return r;
    };
    function a() {
      return l.default.createElement(h2.GraphQLDocsWrapper, null, l.default.createElement(u2.Sidebar, null), l.default.createElement(s.DocsContent, null));
    }
    function G(e2) {
      var { settings: t2, withCommonStyles: r } = e2, n2 = D(e2, ["settings", "withCommonStyles"]);
      const { schema: o, loading: E } = (0, f.useGraphqlSchema)(n2.schema), { isValid: v, loading: O } = (0, y.useLicenseKey)(t2?.licenseKey);
      return E || O ? l.default.createElement("div", null, "Loading...") : v ? d2.IS_BROWSER ? l.default.createElement(m.AppProviders, { schema: o, settings: t2, withCommonStyles: r }, l.default.createElement(p.AppRouter, null, l.default.createElement(a, null))) : l.default.createElement(m.AppProviders, { schema: o, settings: t2, withCommonStyles: r }, l.default.createElement(a, null)) : l.default.createElement(g.Box, { style: { height: "100vh" }, align: "center", justify: "center" }, l.default.createElement("div", null, "Invalid License Key"));
    }
  }
});

// node_modules/@redocly/graphql-docs/lib/src/index.js
var require_src = __commonJS({
  "node_modules/@redocly/graphql-docs/lib/src/index.js"(exports, module) {
    var t2 = Object.defineProperty;
    var S = Object.getOwnPropertyDescriptor;
    var a = Object.getOwnPropertyNames;
    var d2 = Object.prototype.hasOwnProperty;
    var h2 = (r, o) => {
      for (var p in o)
        t2(r, p, { get: o[p], enumerable: true });
    };
    var l = (r, o, p, f) => {
      if (o && typeof o == "object" || typeof o == "function")
        for (let e2 of a(o))
          !d2.call(r, e2) && e2 !== p && t2(r, e2, { get: () => o[e2], enumerable: !(f = S(o, e2)) || f.enumerable });
      return r;
    };
    var n2 = (r) => l(t2({}, "__esModule", { value: true }), r);
    var s = {};
    h2(s, { AppStore: () => c.AppStore, MenuStore: () => x.MenuStore, RedoclyGraphQLDocs: () => m.RedoclyGraphQLDocs });
    module.exports = n2(s);
    var m = require_RedoclyGraphQLDocs();
    var x = require_menu();
    var c = require_app();
  }
});

// node_modules/@redocly/realm/dist/server/plugins/graphql-docs/template/GraphQLDocs.js
init_styled_components_esm();
var import_graphql_docs = __toESM(require_src(), 1);
init_graphql2();
init_src();
import t from "react";
function d({
  pageProps: i
}) {
  const e2 = n("graphQlSettings"), {
    settings: o
  } = i, {
    graphql: r
  } = useThemeConfig(), {
    highlight: n2
  } = Sr(), s = t.useMemo(() => ({
    graphql: buildSchema(e2)
  }), [e2]), m = t.useMemo(() => {
    const a = {
      ...r,
      ...o
    };
    return {
      ...a,
      markdown: {
        ...a.markdown,
        highlight: n2
      },
      navigation: {
        contentPrefix: ""
      },
      pagination: "item"
    };
  }, [r, o, n2]);
  return t.createElement(u, null, t.createElement(import_graphql_docs.RedoclyGraphQLDocs, {
    schema: s,
    settings: m
  }));
}
var u = styled_components_esm_default.div.withConfig({
  displayName: "GraphQLDocs__u",
  componentId: "sc-xx16vs-0"
})(["--navigation-anchor-offset:var(--navbar-height);--sidebar-width:0px;"]);
export {
  d as default
};
