import{reporter}from"./reporter/reporter.js";function parseTsxFrontMatter(content){const regex=/export\s+(?:const|let|var)\s+frontmatter\s*=\s*(\{[^;]*\});/m,match=regex.exec(content);if(!match)return{};try{const environment=eval(`(${match[1]})`);return environment}catch{reporter.panicOnBuildContentError("Invalid format of frontmatter. We support only basic object definition.")}}export{parseTsxFrontMatter};
