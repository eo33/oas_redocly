import type { LoggerContext } from './logger.js';
import type { ErrorScope } from './error-scope.js';
type LogData = {
    level: LogLevel;
    message?: string;
    duration?: number;
    context?: LoggerContext;
    scope?: ErrorScope;
};
export declare enum LogLevel {
    INFO = "INFO",
    WARN = "WARN",
    ERROR = "ERROR",
    SUCCESS = "SUCCESS",
    VERBOSE = "VERBOSE",
    HTTP = "HTTP"
}
declare const formatter: {
    format(log: LogData): string;
    interpolate(pattern: string, ...args: any[]): string;
};
export default formatter;
//# sourceMappingURL=formatter.d.ts.map