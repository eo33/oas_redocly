import type { Logger } from '../utils/reporter/logger.js';
import type { Adapter, UserClaims } from '../plugins/dev-onboarding/api/types.js';
export interface ServerOptions {
    port?: number;
}
export type RequestContext = {
    remoteAddr: {
        hostname?: string;
        port?: number;
        ipAddress?: string;
    };
    url?: {
        search: string;
        origin: string;
        href: string;
    };
};
export type AuthDetails = {
    isAuthenticated: boolean;
    idpAccessToken?: string;
    teams: string[];
    claims: UserClaims;
};
declare global {
    interface Request {
        context: RequestContext;
    }
}
declare module 'hono' {
    interface ContextVariableMap {
        auth: AuthDetails;
        logger: Logger;
        devOnboardingAdapter: Adapter;
    }
}
//# sourceMappingURL=types.d.ts.map