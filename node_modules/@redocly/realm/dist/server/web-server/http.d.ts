import * as http from 'node:http';
import { AsyncLocalStorage } from 'node:async_hooks';
import type { Hono } from 'hono';
declare global {
    var redoclyCookieStorage: AsyncLocalStorage<string>;
}
export declare function startHttpServer(router: Hono, port?: number | string): Promise<http.Server>;
//# sourceMappingURL=http.d.ts.map