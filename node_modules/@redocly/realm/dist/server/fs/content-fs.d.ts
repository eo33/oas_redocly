import type { FileInfo } from './types';
import { Fs } from './fs.js';
/**
 * This class extends fs and adds l10n and last modified time support
 */
export declare class ContentFs extends Fs {
    cwd: string;
    ignored: string[];
    private l10nFolders;
    private lastModifiedTracker;
    private activeLocalizationFolder;
    constructor(cwd: string, ignored?: string[]);
    get localeFolders(): string[];
    get localizationFolder(): string | undefined;
    getFileInfo(relativePath: string): Promise<FileInfo | undefined>;
    readdir(dir: string): Promise<FileInfo[]>;
    exists(file: string): Promise<boolean>;
    read(file: string, encoding?: BufferEncoding): Promise<string>;
    scan(pattern?: RegExp): Promise<FileInfo[]>;
    getLastModified(relativePath: string): Promise<string | null>;
    private init;
}
//# sourceMappingURL=content-fs.d.ts.map