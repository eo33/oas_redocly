import { Events } from './types';
export declare class OtelClientTelemetry {
    private sessionId;
    private initialReferrer;
    init(): void;
    send<K extends keyof Events>(event: K, data: Events[K]): void;
    sendError(error: unknown, handler: string): void;
    private disabled;
}
export declare const otelTelemetry: OtelClientTelemetry;
//# sourceMappingURL=telemetry.d.ts.map