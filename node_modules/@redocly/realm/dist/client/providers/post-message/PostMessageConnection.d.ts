import type { OutgoingMessage } from './types';
export declare class PostMessageConnection {
    private source;
    private origin;
    constructor(source: any, origin: string);
    send(message: OutgoingMessage): void;
}
//# sourceMappingURL=PostMessageConnection.d.ts.map