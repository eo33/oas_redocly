const t=r=>`import React, { PropsWithChildren } from 'react';
  
interface ${r}Props {}

export function ${r}({ children }: PropsWithChildren<${r}Props>) {
  return (
    <>
      ${r}
      {children}
    </>
  )
}
`;export{t as getComponentTemplate};
