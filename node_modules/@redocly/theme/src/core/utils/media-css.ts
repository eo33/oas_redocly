export type Breakpoints = {
  small: `${number}px`;
  medium: `${number}px`;
  large: `${number}px`;
  max: `${number}px`;
};

export const breakpoints: Breakpoints = {
  small: '672px',
  medium: '960px',
  large: '1280px',
  max: '1504px',
};
