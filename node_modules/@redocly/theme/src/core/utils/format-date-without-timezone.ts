export function formatDateWithoutTimeZone(date?: Date | null) {
  if (!date) return date;

  return `${date.getFullYear()}-${padZero(date.getMonth() + 1)}-${padZero(date.getDate())}`;
}

function padZero(num: number) {
  return num < 10 ? '0' + num : num;
}
