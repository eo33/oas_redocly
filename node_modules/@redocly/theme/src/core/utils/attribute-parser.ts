export function parseSrcSet(srcSet: string): Map<string, string> {
  const result = new Map<string, string>();
  const parts = srcSet.split(', ');

  for (const part of parts) {
    const [url, theme] = part.split(' ');
    result.set(theme, url);
  }

  return result;
}
