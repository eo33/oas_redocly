export const slug = (value: string) => {
  const matches = value.match(/[a-zA-Z0-9_-]*/g);
  const result = matches
    ?.filter((value) => value !== '')
    .join('-')
    .toLowerCase();
  return result ?? value;
};
